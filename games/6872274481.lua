-- bedwars ingame

local _runCount = 0
local _runBusy = false
local run = function(func)
	if not _runBusy then
		_runBusy = true
		_runCount = _runCount + 1
		if _runCount % 8 == 0 then
			task.wait()
		end
		local ok, err = pcall(func)
		_runBusy = false
		if not ok then
			warn('[aerov4] module failed to load: ' .. tostring(err))
		end
		return
	end
	local ok, err = pcall(func)
	if not ok then
		warn('[aerov4] module failed to load: ' .. tostring(err))
	end
end
task.wait()
local vapeEvents = setmetatable({}, {
	__index = function(self, index)
		self[index] = Instance.new('BindableEvent')
		return self[index]
	end
})
getgenv().vapeEvents = vapeEvents

local cloneref = cloneref or function(obj)
	return obj
end

local function safeGetProto(func, index)
    if not func then return nil end
    local success, proto = pcall(debug.getconstant, func, index)
    if success then
        return proto
    end
end

local inventoryDebounce = false
local function fireInventoryChanged()
    if inventoryDebounce then return end
    inventoryDebounce = true
    task.spawn(function()
        task.wait() 
        vapeEvents.InventoryChanged:Fire()
        inventoryDebounce = false
    end)
end

local playersService = cloneref(game:GetService('Players'))
local collectionService = cloneref(game:GetService('CollectionService'))
local function getLocalKits()
	local raw = playersService.LocalPlayer:GetAttribute('PlayingAsKits')
	local list = {}
	if type(raw) == 'string' then
		for kit in raw:gmatch('[%w_]+') do
			if kit ~= '' and kit ~= 'none' then
				table.insert(list, kit)
			end
		end
	end
	return list
end
getgenv().aeroTeamColors = {
	[1] = {name = 'blue',   color = Color3.fromRGB(85, 150, 255)},
	[2] = {name = 'orange', color = Color3.fromRGB(255, 150, 50)},
	[3] = {name = 'pink',   color = Color3.fromRGB(255, 100, 200)},
	[4] = {name = 'yellow', color = Color3.fromRGB(255, 255, 50)},
	[5] = {name = 'brown',  color = Color3.fromRGB(139, 69, 19)},  
	[6] = {name = 'white',  color = Color3.fromRGB(255, 255, 255)}, 
	[7] = {name = 'cyan',   color = Color3.fromRGB(120, 255, 255)}, 
	[8] = {name = 'purple', color = Color3.fromRGB(180, 90, 255)},  
}
local replicatedStorage = cloneref(game:GetService('ReplicatedStorage'))
local runService = cloneref(game:GetService('RunService'))
local inputService = cloneref(game:GetService('UserInputService'))
local tweenService = cloneref(game:GetService('TweenService'))
local httpService = cloneref(game:GetService('HttpService'))
local textChatService = cloneref(game:GetService('TextChatService'))
local collectionService = cloneref(game:GetService('CollectionService'))
local contextActionService = cloneref(game:GetService('ContextActionService'))
local guiService = cloneref(game:GetService('GuiService'))
local coreGui = cloneref(game:GetService('CoreGui'))
local starterGui = cloneref(game:GetService('StarterGui'))
local VirtualInputManager = game:GetService("VirtualInputManager")
local lightingService = cloneref(game:GetService('Lighting'))

local isnetworkowner = identifyexecutor and table.find({'Delta', 'Volt'}, ({identifyexecutor()})[1]) and isnetworkowner or function()
	return true
end
local gameCamera = workspace.CurrentCamera
local lplr = playersService.LocalPlayer
local assetfunction = getcustomasset

local vape = shared.vape
if vape and not vape.Clean then
	vape.Clean = function(self, conn)
		if not conn then return end
		
		if not vape.Connections then
			vape.Connections = {}
		end

		if self and self.Enabled then
			vape.Connections[conn] = true
			return conn
		else
			if vape.Connections[conn] then
				if typeof(conn) == "RBXScriptConnection" then
					pcall(conn.Disconnect, conn)
				end
				vape.Connections[conn] = nil
			end
		end
	end
end
if vape and not vape.Remove then
    vape.Remove = function(module) 
		return module 
	end
end
local entitylib = vape.Libraries.entity
local targetinfo = vape.Libraries.targetinfo
local sessioninfo = vape.Libraries.sessioninfo
local uipallet = vape.Libraries.uipallet
local tween = vape.Libraries.tween
local color = vape.Libraries.color
local whitelist = { get = function() return nil, true end, tag = function() return '' end, customtags = {} }
local prediction = vape.Libraries.prediction
local getfontsize = vape.Libraries.getfontsize
local getcustomasset = vape.Libraries.getcustomasset

local store = {
    attackReach = 0,
    attackReachUpdate = tick(),
    hand = {},
    inventory = {
        inventory = {
            items = {},
            armor = {}
        },
        hotbar = {}
    },
    inventories = setmetatable({}, { __mode = "k" }), 
    matchState = 0,
    queueType = 'bedwars_test',
    tools = {},
    lastToolUpdate = 0,
	lastKrystalUpdateCheck = 0,
	BedAlarmNotifyTick = 0,
	BedAlarmIsTrigged = false,
	BedAlarmHighlightedEnimes = {},
	BedAlarm = {},
	BedAlarmSoundTick = 0,
	silasAbilityTime = 0,
	terraStompTime = 0,
	terraKickTime = 0,
}
getgenv().store = store
local Reach = {}
local HitBoxes = {}
local TrapDisabler
local AntiFallPart
local bedwars, remotes, sides, oldinvrender, oldSwing = {}, {}, {}, {}, {}
local _missingRemotes = {}
setmetatable(remotes, {
	__index = function(_, key)
		if not _missingRemotes[key] then
			_missingRemotes[key] = true
			task.delay(10, function()
				if rawget(remotes, key) == nil then
					pcall(function()
						vape.Notify('[aerov4] remote "' .. tostring(key) .. '" changed or removed some features may not work bru (dm @5qvx for fix)', 6)
					end)
				end
			end)
		end
		return nil
	end
})
local originalKnit

local function addBlur(parent)
	local blur = Instance.new('ImageLabel')
	blur.Name = 'Blur'
	blur.Size = UDim2.new(1, 89, 1, 52)
	blur.Position = UDim2.fromOffset(-48, -31)
	blur.BackgroundTransparency = 1
	blur.Image = getcustomasset('aerov4/assets/new/blur.png')
	blur.ScaleType = Enum.ScaleType.Slice
	blur.SliceCenter = Rect.new(52, 31, 261, 502)
	blur.Parent = parent
	return blur
end

local function notif(...) return
	vape:CreateNotification(...)
end

local function collection(tags, module, customadd, customremove)
	tags = typeof(tags) ~= 'table' and {tags} or tags
	local objs, connections = {}, {}

	for _, tag in tags do
		table.insert(connections, collectionService:GetInstanceAddedSignal(tag):Connect(function(v)
			if customadd then
				customadd(objs, v, tag)
				return
			end
			table.insert(objs, v)
		end))
		table.insert(connections, collectionService:GetInstanceRemovedSignal(tag):Connect(function(v)
			if customremove then
				customremove(objs, v, tag)
				return
			end
			local idx = table.find(objs, v)
			if idx then
				local last = #objs
				objs[idx] = objs[last]
				objs[last] = nil
			end
		end))

		for _, v in collectionService:GetTagged(tag) do
			if customadd then
				customadd(objs, v, tag)
				continue
			end
			table.insert(objs, v)
		end
	end

	local cleanFunc = function(self)
		for _, v in connections do
			v:Disconnect()
		end
		table.clear(connections)
		table.clear(objs)
		table.clear(self)
	end
	if module then
		module:Clean(cleanFunc)
	end
	return objs, cleanFunc
end

local function scanDescendants(root, fn, module)
	local state = {cancelled = false}
	if module then
		module:Clean(function() state.cancelled = true end)
	end
	task.spawn(function()
		local stack = {root or workspace}
		local budget = 0
		while #stack > 0 do
			if state.cancelled then return end
			local node = stack[#stack]
			stack[#stack] = nil
			local kids = node:GetChildren()
			for i = 1, #kids do
				local child = kids[i]
				stack[#stack + 1] = child
				fn(child)
			end
			budget += #kids
			if budget >= 700 then
				budget = 0
				task.wait()
				if state.cancelled then return end
			end
		end
	end)
	return state
end

local function getBestArmor(slot)
	local closest, mag = nil, 0

	for _, item in store.inventory.inventory.items do
		local meta = item and bedwars.ItemMeta[item.itemType] or {}

		if meta.armor and meta.armor.slot == slot then
			local newmag = (meta.armor.damageReductionMultiplier or 0)

			if newmag > mag then
				closest, mag = item, newmag
			end
		end
	end

	return closest
end

local function getBow()
	local bestBow, bestBowSlot, bestBowDamage = nil, nil, 0
	for slot, item in store.inventory.inventory.items do
		local _bowItemMeta = bedwars.ItemMeta[item.itemType]
        local bowMeta = _bowItemMeta and _bowItemMeta.projectileSource
		if bowMeta and table.find(bowMeta.ammoItemTypes, 'arrow') then
			local bowDamage = bedwars.ProjectileMeta[bowMeta.projectileType('arrow')].combat.damage or 0
			if bowDamage > bestBowDamage then
				bestBow, bestBowSlot, bestBowDamage = item, slot, bowDamage
			end
		end
	end
	return bestBow, bestBowSlot
end

local function getItem(itemName, inv)
	for slot, item in (inv or store.inventory.inventory.items) do
		if item.itemType == itemName then
			return item, slot
		end
	end
	return nil
end

local function GetItems(item: string): table
	local Items: table = {};
	for _, v in next, Enum[item]:GetEnumItems() do 
		table.insert(Items, v["Name"]) ;
	end;
	return Items;
end;

local function getRoactRender(func)
	return debug.getupvalue(debug.getupvalue(debug.getupvalue(func, 3).render, 2).render, 1)
end

local function getSword()
	local bestSword, bestSwordSlot, bestSwordDamage = nil, nil, 0
	for slot, item in store.inventory.inventory.items do
		local _swordItemMeta = bedwars.ItemMeta[item.itemType]
        local swordMeta = _swordItemMeta and _swordItemMeta.sword
		if swordMeta then
			local swordDamage = swordMeta.damage or 0
			if swordDamage > bestSwordDamage then
				bestSword, bestSwordSlot, bestSwordDamage = item, slot, swordDamage
			end
		end
	end
	return bestSword, bestSwordSlot
end

local function getTool(breakType)
	local bestTool, bestToolSlot, bestToolDamage = nil, nil, 0
	for slot, item in store.inventory.inventory.items do
		local _toolItemMeta = bedwars.ItemMeta[item.itemType]
        local toolMeta = _toolItemMeta and _toolItemMeta.breakBlock
		if toolMeta then
			local toolDamage = toolMeta[breakType] or 0
			if toolDamage > bestToolDamage then
				bestTool, bestToolSlot, bestToolDamage = item, slot, toolDamage
			end
		end
	end
	return bestTool, bestToolSlot
end

local function getWool()
	for _, wool in store.inventory.inventory.items do
		if wool.itemType:find('wool') then
			return wool and wool.itemType, wool and wool.amount
		end
	end
end

local function getStrength(plr)
	if not plr or not plr.Player then
		return 0
	end

	local strength = 0
	for _, v in (store.inventories[plr.Player] or {items = {}}).items do
		local itemmeta = bedwars.ItemMeta[v.itemType]
		if itemmeta and itemmeta.sword and itemmeta.sword.damage > strength then
			strength = itemmeta.sword.damage
		end
	end

	return strength
end

local function getPlacedBlock(pos)
	if not pos then
		return
	end
	local roundedPosition = bedwars.BlockController:getBlockPosition(pos)
	return bedwars.BlockController:getStore():getBlockAt(roundedPosition), roundedPosition
end

local function getBlocksInPoints(s, e)
	local blocks, list = bedwars.BlockController:getStore(), {}
	for x = s.X, e.X do
		for y = s.Y, e.Y do
			for z = s.Z, e.Z do
				local vec = Vector3.new(x, y, z)
				if blocks:getBlockAt(vec) then
					table.insert(list, vec * 3)
				end
			end
		end
	end
	return list
end

local function getNearGround(range)
	range = Vector3.new(3, 3, 3) * (range or 10)
	local localPosition, mag, closest = entitylib.character.RootPart.Position, 60
	local blocks = getBlocksInPoints(bedwars.BlockController:getBlockPosition(localPosition - range), bedwars.BlockController:getBlockPosition(localPosition + range))

	for _, v in blocks do
		if not getPlacedBlock(v + Vector3.new(0, 3, 0)) then
			local newmag = (localPosition - v).Magnitude
			if newmag < mag then
				mag, closest = newmag, v + Vector3.new(0, 3, 0)
			end
		end
	end

	table.clear(blocks)
	return closest
end

local function getShieldAttribute(char)
	local returned = 0
	for name, val in char:GetAttributes() do
		if name:find('Shield') and type(val) == 'number' and val > 0 then
			returned += val
		end
	end
	return returned
end

local function getSpeed()
	local multi, increase, modifiers = 0, true, bedwars.SprintController:getMovementStatusModifier():getModifiers()

	local modifiers2 = bedwars.SprintController:getMovementStatusModifier():getModifiers()
	for _, v in modifiers do
		if type(v) == 'table' then
			local val = v.constantSpeedMultiplier and v.constantSpeedMultiplier or 0
			if val and val > math.max(multi, 1) then
				increase = false
				multi = val - (0.06 * math.round(val))
			end
		end
	end

	for _, v in modifiers2 do
		if type(v) == 'table' then
			multi += math.max((v.moveSpeedMultiplier or 0) - 1, 0)
		end
	end

	if multi > 0 and increase then
		multi += 0.16 + (0.02 * math.round(multi))
	end

	return 20 * (multi + 1)
end

local function getTableSize(tab)
	local ind = 0
	for _ in tab do
		ind += 1
	end
	return ind
end

local function hotbarSwitch(slot)
	if slot and store.inventory.hotbarSlot ~= slot then
		bedwars.Store:dispatch({
			type = 'InventorySelectHotbarSlot',
			slot = slot
		})
		vapeEvents.InventoryChanged.Event:Wait()
		return true
	end
	return false
end

local function getHotbar(tool)
	for i, v in (store.inventory.hotbar or {}) do
		if v.item and v.item.tool == tool then
			return i - 1
		end
	end
	return nil
end
getgenv().getHotbar = getHotbar

local function isFriend(plr, recolor)
	if vape.Categories.Friends.Options['Use friends'].Enabled then
		local friend = table.find(vape.Categories.Friends.ListEnabled, plr.Name) and true
		if recolor then
			friend = friend and vape.Categories.Friends.Options['Recolor visuals'].Enabled
		end
		return friend
	end
	return nil
end

local function isTarget(plr)
	return table.find(vape.Categories.Targets.ListEnabled, plr.Name) and true
end

local function removeTags(str)
	str = str:gsub('<br%s*/>', '\n')
	return (str:gsub('<[^<>]->', ''))
end

local function roundPos(vec)
    return Vector3.new(
        math.round(vec.X / 3) * 3,
        math.round(vec.Y / 3) * 3,
        math.round(vec.Z / 3) * 3
    )
end

local function switchItem(tool, delayTime)
	delayTime = delayTime or 0.05
	local check = lplr.Character and lplr.Character:FindFirstChild('HandInvItem') or nil
	if check and check.Value ~= tool and tool.Parent ~= nil then
		task.spawn(function()
			bedwars.Client:Get(remotes.EquipItem):CallServerAsync({hand = tool})
		end)
		check.Value = tool
		if delayTime > 0 then
			task.wait(delayTime)
		end
		return true
	end
end

local function waitForChildOfType(obj, name, timeout, prop)
	local check, returned = tick() + timeout
	repeat
		returned = prop and obj[name] or obj:FindFirstChildOfClass(name)
		if (returned and returned.Name ~= 'UpperTorso') or check < tick() then
			break
		end
		task.wait()
	until false
	return returned
end

local frictionTable, oldfrict = {}, {}
local lastUpdate = {}
local lagConnections = {}
local frictionConnection
local frictionState

local function modifyVelocity(v)
	if v:IsA('BasePart') and v.Name ~= 'HumanoidRootPart' and not oldfrict[v] then
		oldfrict[v] = v.CustomPhysicalProperties or 'none'
		v.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0.2, 0.5, 1, 1)
	end
end

local function updateVelocity(force)
	local newState = getTableSize(frictionTable) > 0
	if frictionState ~= newState or force then
		if frictionConnection then
			frictionConnection:Disconnect()
		end
		if newState then
			if entitylib.isAlive then
				for _, v in entitylib.character.Character:GetDescendants() do
					modifyVelocity(v)
				end
				frictionConnection = entitylib.character.Character.DescendantAdded:Connect(modifyVelocity)
			end
		else
			for i, v in oldfrict do
				i.CustomPhysicalProperties = v ~= 'none' and v or nil
			end
			table.clear(oldfrict)
		end
	end
	frictionState = newState
end

local function isEveryoneDead()
	return #bedwars.Store:getState().Party.members <= 0
end
	
local function joinQueue()
	if not bedwars.Store:getState().Game.customMatch and bedwars.Store:getState().Party.leader.userId == lplr.UserId and bedwars.Store:getState().Party.queueState == 0 then
		bedwars.QueueController:joinQueue(store.queueType)
	end
end

local function lobby()
    bedwars.Client:Get(remotes.TeleportToLobby):FireServer()
end

local kitorder = {
	hannah = 5,
	spirit_assassin = 4,
	dasher = 3,
	jade = 2,
	regent = 1
}

local function HasSeed(character)
    if not character then return false end
    return character:FindFirstChild("Seed", true) ~= nil
end

local sortmethods = {
	Threat = function(a, b)
		if not a.Entity then return false end
		if not b.Entity then return true end
		return getStrength(a.Entity) > getStrength(b.Entity)
	end,
	Health = function(a, b)
		if not a.Entity then return false end
		if not b.Entity then return true end
		return a.Entity.Health < b.Entity.Health
	end,
	Angle = function(a, b)
		if not a.Entity or not a.Entity.RootPart then return false end
		if not b.Entity or not b.Entity.RootPart then return true end
		local selfrootpos = entitylib.character.RootPart.Position
		local rawFacing = (getgenv().ViewMode and getgenv().ViewMode.Value == 'Third Person') and gameCamera.CFrame.LookVector or entitylib.character.RootPart.CFrame.LookVector
		local flatFacing = Vector3.new(rawFacing.X, 0, rawFacing.Z)
		if flatFacing.Magnitude < 0.001 then return false end
		local localFacing = flatFacing.Unit
		local deltaA = Vector3.new(a.Entity.RootPart.Position.X - selfrootpos.X, 0, a.Entity.RootPart.Position.Z - selfrootpos.Z)
		local deltaB = Vector3.new(b.Entity.RootPart.Position.X - selfrootpos.X, 0, b.Entity.RootPart.Position.Z - selfrootpos.Z)
		if deltaA.Magnitude < 0.001 or deltaB.Magnitude < 0.001 then return deltaA.Magnitude < deltaB.Magnitude end
		local angle = math.acos(math.clamp(localFacing:Dot(deltaA.Unit), -1, 1))
		local angle2 = math.acos(math.clamp(localFacing:Dot(deltaB.Unit), -1, 1))
		return angle < angle2
	end,
	Distance = function(a, b)
		if not a.Entity or not a.Entity.RootPart then return false end
		if not b.Entity or not b.Entity.RootPart then return true end
		local selfpos = entitylib.character.RootPart.Position
		local distA = (a.Entity.RootPart.Position - selfpos).Magnitude
		local distB = (b.Entity.RootPart.Position - selfpos).Magnitude
		return distA < distB
	end,
	Cursor = function(a, b)
		if not a.Entity or not a.Entity.RootPart then return false end
		if not b.Entity or not b.Entity.RootPart then return true end
		local camera = gameCamera
		local mousePos = inputService:GetMouseLocation()
		local function screenDist(ent)
			local rootPart = ent.RootPart
			if not rootPart then return math.huge end
			local screenPos, onScreen = camera:WorldToScreenPoint(rootPart.Position)
			if not onScreen then return math.huge end
			return (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
		end
		local distA = screenDist(a.Entity)
		local distB = screenDist(b.Entity)
		if distA == math.huge and distB == math.huge then
			local selfpos = entitylib.character.RootPart.Position
			local worldA = (a.Entity.RootPart.Position - selfpos).Magnitude
			local worldB = (b.Entity.RootPart.Position - selfpos).Magnitude
			return worldA < worldB
		end
		return distA < distB
	end,
	Forest = function(a, b)
		if not a.Entity then return false end
		if not b.Entity then return true end
		local aHasSeed = HasSeed(a.Entity.Character)
		local bHasSeed = HasSeed(b.Entity.Character)
		if aHasSeed and not bHasSeed then return true end
		if not aHasSeed and bHasSeed then return false end
		if not a.Entity.RootPart then return false end
		if not b.Entity.RootPart then return true end
		local selfpos = entitylib.character.RootPart.Position
		local distA = (a.Entity.RootPart.Position - selfpos).Magnitude
		local distB = (b.Entity.RootPart.Position - selfpos).Magnitude
		return distA < distB
	end
}

local function getSortList(priority)
	local methods = {}
	for _, v in ipairs(priority or {}) do
		table.insert(methods, v)
	end
	for i in sortmethods do
		if not table.find(methods, i) then
			table.insert(methods, i)
		end
	end
	return methods
end

run(function()
	local oldstart = entitylib.start
	local function customEntity(ent)
		if ent:HasTag('inventory-entity') and not ent:HasTag('Monster') then
			return
		end

		entitylib.addEntity(ent, nil, ent:HasTag('Drone') and function(self)
			local droneplr = playersService:GetPlayerByUserId(self.Character:GetAttribute('PlayerUserId'))
			return not droneplr or lplr:GetAttribute('Team') ~= droneplr:GetAttribute('Team')
		end or function(self)
			return lplr:GetAttribute('Team') ~= self.Character:GetAttribute('Team')
		end)
	end

	entitylib.start = function()
		if entitylib.Running then entitylib.stop() end

		local function customEntity(ent)
			if playersService:GetPlayerFromCharacter(ent) then return end
			if collectionService:HasTag(ent.Parent, 'entity') then return end
			local teamFunc = function(self)
				local npcTeam = self.Character:GetAttribute('Team')
				if not npcTeam then return true end
				return lplr:GetAttribute('Team') ~= npcTeam
			end
			entitylib.addEntity(ent, nil, teamFunc)
		end

		table.insert(entitylib.Connections, playersService.PlayerAdded:Connect(function(v)
			entitylib.addPlayer(v)
		end))
		table.insert(entitylib.Connections, playersService.PlayerRemoving:Connect(function(v)
			entitylib.removePlayer(v)
		end))

		for _, v in playersService:GetPlayers() do
			entitylib.addPlayer(v)
		end

		for _, ent in collectionService:GetTagged('entity') do
			customEntity(ent)
		end

		table.insert(entitylib.Connections, collectionService:GetInstanceAddedSignal('entity'):Connect(customEntity))
		table.insert(entitylib.Connections, collectionService:GetInstanceRemovedSignal('entity'):Connect(function(ent)
			entitylib.removeEntity(ent)
		end))

		local function addDesertPot(pot)
			if not pot:IsA('Model') then return end
			entitylib.addEntity(pot, nil, function() return true end)
		end
		for _, v in collectionService:GetTagged('desert_pot') do
			addDesertPot(v)
		end
		table.insert(entitylib.Connections, collectionService:GetInstanceAddedSignal('desert_pot'):Connect(addDesertPot))
		table.insert(entitylib.Connections, collectionService:GetInstanceRemovedSignal('desert_pot'):Connect(function(v)
			entitylib.removeEntity(v)
		end))

		table.insert(entitylib.Connections, workspace:GetPropertyChangedSignal('CurrentCamera'):Connect(function()
			gameCamera = workspace.CurrentCamera or workspace:FindFirstChildWhichIsA('Camera')
		end))

		entitylib.Running = true
	end

	entitylib.addPlayer = function(plr)
		if entitylib.PlayerConnections[plr] then
			for _, conn in ipairs(entitylib.PlayerConnections[plr]) do
				if conn and typeof(conn) == "RBXScriptConnection" then
					conn:Disconnect()
				end
			end
		end

		if plr.Character then
			entitylib.refreshEntity(plr.Character, plr)
		end
		entitylib.PlayerConnections[plr] = {
			plr.CharacterAdded:Connect(function(char)
				entitylib.refreshEntity(char, plr)
			end),
			plr.CharacterRemoving:Connect(function(char)
				entitylib.removeEntity(char, plr == lplr)
			end),
			plr:GetAttributeChangedSignal('Team'):Connect(function()
				if plr == lplr then
					for _, v in entitylib.List do
						local newTargetable = entitylib.targetCheck(v)
						if v.Targetable ~= newTargetable then
							v.Targetable = newTargetable
							entitylib.Events.EntityUpdated:Fire(v)
						end
					end
				else
					entitylib.refreshEntity(plr.Character, plr)
					for _, v in entitylib.List do
						if v.Player ~= plr and v.Targetable ~= entitylib.targetCheck(v) then
							local newTargetable = entitylib.targetCheck(v)
							v.Targetable = newTargetable
							entitylib.Events.EntityUpdated:Fire(v)
						end
					end
				end
			end)
		}
	end

	entitylib.addEntity = function(char, plr, teamfunc)
		if not char then return end
		entitylib.EntityThreads[char] = task.spawn(function()
			local hum, humrootpart, head
			if plr then
				hum = waitForChildOfType(char, 'Humanoid', 10)
				humrootpart = hum and waitForChildOfType(hum, 'RootPart', workspace.StreamingEnabled and 9e9 or 10, true)
				head = char:WaitForChild('Head', 10) or humrootpart
			else
				hum = {HipHeight = 0.5}
				humrootpart = waitForChildOfType(char, 'PrimaryPart', 10, true)
				head = humrootpart
			end
			local updateobjects = {}
			if plr and plr ~= lplr then
				local names = {'ArmorInvItem_0', 'ArmorInvItem_1', 'ArmorInvItem_2', 'HandInvItem'}
				for _, name in names do
					local found = char:FindFirstChild(name)
					if found then
						table.insert(updateobjects, found)
					end
				end
			end

			if hum and humrootpart then
				local entity = {
					Connections = {},
					Character = char,
					Health = (function()
						local hp = char:GetAttribute('Health') or 100
						local shield = 0
						for k, v in pairs(char:GetAttributes()) do
							if type(k) == 'string' and k:find('Shield') and type(v) == 'number' and v > 0 then
								shield = shield + v
							end
						end
						return hp + shield
					end)(),
					Head = head,
					Humanoid = hum,
					HumanoidRootPart = humrootpart,
					HipHeight = hum.HipHeight + (humrootpart.Size.Y / 2) + (hum.RigType == Enum.HumanoidRigType.R6 and 2 or 0),
					Jumps = 0,
					JumpTick = tick(),
					Jumping = false,
					LandTick = tick(),
					MaxHealth = char:GetAttribute('MaxHealth') or 100,
					NPC = plr == nil,
					Player = plr,
					RootPart = humrootpart,
					TeamCheck = teamfunc
				}

				if plr == lplr then
					entity.AirTime = tick()
					entitylib.character = entity
					entitylib.isAlive = true
					entitylib.Events.LocalAdded:Fire(entity)
					table.insert(entity.Connections, char.AttributeChanged:Connect(function(attr)
						vapeEvents.AttributeChanged:Fire(attr)
					end))
				else
					entity.Targetable = entitylib.targetCheck(entity)

					if not plr then
						table.insert(entity.Connections, char.AttributeChanged:Connect(function(attr)
							if attr == 'Team' then
								entity.Targetable = entitylib.targetCheck(entity)
								entitylib.Events.EntityUpdated:Fire(entity)
							end
						end))
					end

					for _, v in entitylib.getUpdateConnections(entity) do
						table.insert(entity.Connections, v:Connect(function()
							entity.Health = (char:GetAttribute('Health') or 100) + getShieldAttribute(char)
							entity.MaxHealth = char:GetAttribute('MaxHealth') or 100
							entitylib.Events.EntityUpdated:Fire(entity)
						end))
					end

					local invUpdatePending = {}

					for _, v in updateobjects do
						table.insert(entity.Connections, v:GetPropertyChangedSignal('Value'):Connect(function()
							if invUpdatePending[entity] then return end
							invUpdatePending[entity] = true
							task.delay(0.1, function()
								invUpdatePending[entity] = nil
								if bedwars.getInventory then
									store.inventories[plr] = bedwars.getInventory(plr)
									entitylib.Events.EntityUpdated:Fire(entity)
								end
							end)
						end))
					end

					if plr then
						local anim = char:FindFirstChild('Animate')
						if anim then
							pcall(function()
								local jumpAnimId = anim.jump:FindFirstChildWhichIsA('Animation').AnimationId
								table.insert(entity.Connections, hum.StateChanged:Connect(function(old, new)
									if new == Enum.HumanoidStateType.Jumping then
										entity.JumpTick = tick()
										entity.Jumps += 1
										entity.LandTick = tick() + 1
										entity.Jumping = entity.Jumps > 1
									elseif new == Enum.HumanoidStateType.Landed or new == Enum.HumanoidStateType.Running or new == Enum.HumanoidStateType.Freefall then
										entity.Jumping = false
									end
								end))
							end)
						end

						task.spawn(function()
							local waitTime = 0.3
							while plr and plr.Parent and entity.Character and entity.Character.Parent do
								if bedwars.getInventory then
									local inv = bedwars.getInventory(plr)
									if inv then
										local old = store.inventories[plr]
										store.inventories[plr] = inv
										local changed = true
										if old then
											local oh = old.hand and old.hand.itemType or ''
											local nh = inv.hand and inv.hand.itemType or ''
											local oa = ''
											local na = ''
											for i = 4, 6 do
												oa = oa .. '|' .. tostring(old.armor and old.armor[i] and old.armor[i].itemType or '')
												na = na .. '|' .. tostring(inv.armor and inv.armor[i] and inv.armor[i].itemType or '')
											end
											changed = (oh ~= nh) or (oa ~= na)
										end
										if changed then
											entitylib.Events.EntityUpdated:Fire(entity)
										end
									end
								end
								task.wait(waitTime)
								if waitTime < 0.6 then
									waitTime = math.min(0.6, waitTime + 0.1)
								end
							end
						end)
					end
					table.insert(entitylib.List, entity)
					entitylib.Events.EntityAdded:Fire(entity)
				end

				table.insert(entity.Connections, char.ChildRemoved:Connect(function(part)
					if part == humrootpart or part == hum or part == head then
						if part == humrootpart and hum.RootPart then
							humrootpart = hum.RootPart
							entity.RootPart = hum.RootPart
							entity.HumanoidRootPart = hum.RootPart
							return
						end
						entitylib.removeEntity(char, plr == lplr)
					end
				end))
			end
			entitylib.EntityThreads[char] = nil
		end)
	end

	entitylib.getUpdateConnections = function(ent)
		local char = ent.Character
		local tab = {
			char:GetAttributeChangedSignal('Health'),
			char:GetAttributeChangedSignal('MaxHealth'),
			{
				Connect = function()
					ent.Friend = ent.Player and isFriend(ent.Player) or nil
					ent.Target = ent.Player and isTarget(ent.Player) or nil
					return {Disconnect = function() end}
				end
			}
		}

		if ent.Player then
			table.insert(tab, ent.Player:GetAttributeChangedSignal('PlayingAsKit'))
			table.insert(tab, ent.Player:GetAttributeChangedSignal('PlayingAsKits'))

			local vkSignal = {
				Connect = function(_, func)
					local conn = ent.Player:GetAttributeChangedSignal('VoidKnightTier'):Connect(function()
						lastUpdate[ent] = 0
						func()
					end)
					return conn
				end
			}
			table.insert(tab, vkSignal)
		end

		local blockKickerSignal = {
			Connect = function(_, func)
				local conn = char.AttributeChanged:Connect(function(attr)
					if attr == 'BlockKickerKit_BlockCount' then
						lastUpdate[ent] = 0
						func()
					end
				end)
				return conn
			end
		}
		table.insert(tab, blockKickerSignal)

		local shieldSignal = {
			Connect = function(_, func)
				local conn = char.AttributeChanged:Connect(function(attr)
					if attr:find('Shield') then
						func()
					end
				end)
				return conn
			end
		}
		table.insert(tab, shieldSignal)

		return tab
	end

	entitylib.targetCheck = function(ent)
		if ent.Character and ent.Character:HasTag('petrified-player') then return false end
		if ent.TeamCheck then
			return ent:TeamCheck()
		end
		if ent.NPC then
			local npcTeam = ent.Character and ent.Character:GetAttribute('Team')
			if not npcTeam then return true end
			return lplr:GetAttribute('Team') ~= npcTeam
		end
		if isFriend(ent.Player) then return false end
		return lplr:GetAttribute('Team') ~= ent.Player:GetAttribute('Team')
	end
	vape:Clean(entitylib.Events.LocalAdded:Connect(updateVelocity))
end)
entitylib.start()

run(function()
	local KnitInit, Knit
	repeat
		KnitInit, Knit = pcall(function()
			return debug.getupvalue(require(lplr.PlayerScripts.TS.knit).setup, 9)
		end)
		if KnitInit then break end
		task.wait()
	until KnitInit

	if not debug.getupvalue(Knit.Start, 1) then
		repeat task.wait() until debug.getupvalue(Knit.Start, 1)
	end

	local Flamework = require(replicatedStorage['rbxts_include']['node_modules']['@flamework'].core.out).Flamework
	local InventoryUtil = require(replicatedStorage.TS.inventory['inventory-util']).InventoryUtil
	local Client = require(replicatedStorage.TS.remotes).default.Client
	local OldGet, OldBreak = Client.Get

	local rakNet = false
	run(function()
		rakNet = typeof(raknet) == 'table'
	end)

	bedwars = setmetatable({
		RankMeta = require(replicatedStorage.TS.rank['rank-meta']).RankMeta,
        BalanceFile = require(replicatedStorage.TS.balance["balance-file"]).BalanceFile,
        ClientSyncEvents = require(lplr.PlayerScripts.TS['client-sync-events']).ClientSyncEvents,
        SyncEventPriority = require(replicatedStorage.rbxts_include.node_modules['@easy-games']['sync-event'].out),
		AbilityId = require(replicatedStorage.TS.ability['ability-id']).AbilityId,
        IdUtil = require(replicatedStorage.TS.util['id-util']).IdUtil,
		BlockSelector = require(game:GetService("ReplicatedStorage").rbxts_include.node_modules["@easy-games"]["block-engine"].out.client.select["block-selector"]).BlockSelector,
		KnockbackUtilInstance = replicatedStorage.TS.damage['knockback-util'],
		BedwarsKitSkin = require(replicatedStorage.TS.games.bedwars['kit-skin']['bedwars-kit-skin-meta']).BedwarsKitSkinMeta,
		KitController = Knit.Controllers.KitController,
		FishermanUtil = require(replicatedStorage.TS.games.bedwars.kit.kits.fisherman['fisherman-util']).FishermanUtil,
		FishMeta = require(replicatedStorage.TS.games.bedwars.kit.kits.fisherman['fish-meta']),
	 	MatchHistroyApp = require(lplr.PlayerScripts.TS.controllers.global["match-history"].ui["match-history-moderation-app"]).MatchHistoryModerationApp,
	 	MatchHistroyController = Knit.Controllers.MatchHistoryController,
		BlockEngine = require(game:GetService("ReplicatedStorage").rbxts_include.node_modules["@easy-games"]["block-engine"].out).BlockEngine,
		BlockSelectorMode = require(game:GetService("ReplicatedStorage").rbxts_include.node_modules["@easy-games"]["block-engine"].out.client.select["block-selector"]).BlockSelectorMode,
		EntityUtil = require(game:GetService("ReplicatedStorage").TS.entity["entity-util"]).EntityUtil,
		GamePlayer = require(replicatedStorage.TS.player['game-player']),
		OfflinePlayerUtil = require(replicatedStorage.TS.player['offline-player-util']),
		PlayerUtil = require(replicatedStorage.TS.player['player-util']),
		KKKnitController = require(lplr.PlayerScripts.TS.lib.knit['knit-controller']),
		AbilityController = Flamework.resolveDependency('@easy-games/game-core:client/controllers/ability/ability-controller@AbilityController'),
		CooldownController = Flamework.resolveDependency("@easy-games/game-core:client/controllers/cooldown/cooldown-controller@CooldownController"),
		CooldownIDS = require(replicatedStorage.TS.cooldown["cooldown-id"]).CooldownId,		
		AnimationType = require(replicatedStorage.TS.animation['animation-type']).AnimationType,
		AnimationUtil = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out['shared'].util['animation-util']).AnimationUtil,
		AppController = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out.client.controllers['app-controller']).AppController,
		BedBreakEffectMeta = require(replicatedStorage.TS.locker['bed-break-effect']['bed-break-effect-meta']).BedBreakEffectMeta,
		BedwarsKitMeta = require(replicatedStorage.TS.games.bedwars.kit['bedwars-kit-meta']).BedwarsKitMeta,
		BlockBreaker = Knit.Controllers.BlockBreakController.blockBreaker,
		BlockController = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['block-engine'].out).BlockEngine,
		BlockEngine = require(lplr.PlayerScripts.TS.lib['block-engine']['client-block-engine']).ClientBlockEngine,
		BlockPlacer = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['block-engine'].out.client.placement['block-placer']).BlockPlacer,
		BowConstantsTable = (Knit.Controllers.ProjectileController and Knit.Controllers.ProjectileController.enableBeam) and debug.getupvalue(Knit.Controllers.ProjectileController.enableBeam, 8) or {},
		ClickHold = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out.client.ui.lib.util['click-hold']).ClickHold,
		Client = Client,
		ClientConstructor = require(replicatedStorage['rbxts_include']['node_modules']['@rbxts'].net.out.client),
		ClientDamageBlock = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['block-engine'].out.shared.remotes).BlockEngineRemotes.Client,
		CombatConstant = require(replicatedStorage.TS.combat['combat-constant']).CombatConstant,
		SharedConstants = require(replicatedStorage.TS['shared-constants']),
		DamageIndicator = Knit.Controllers.DamageIndicatorController.spawnDamageIndicator,
		DefaultKillEffect = require(lplr.PlayerScripts.TS.controllers.global.locker['kill-effect'].effects['default-kill-effect']),
		EmoteType = require(replicatedStorage.TS.locker.emote['emote-type']).EmoteType,
		EmoteMeta = require(replicatedStorage.TS.locker.emote['emote-meta']).EmoteMeta,
		GameAnimationUtil = require(replicatedStorage.TS.animation['animation-util']).GameAnimationUtil,
		NotificationController = Flamework.resolveDependency('@easy-games/game-core:client/controllers/notification-controller@NotificationController'),
		getIcon = function(item, showinv)
			local itemmeta = bedwars.ItemMeta[item.itemType]
			return itemmeta and showinv and itemmeta.image or ''
		end,
		getInventory = function(plr)
			local suc, res = pcall(function()
				return InventoryUtil.getInventory(plr)
			end)
			return suc and res or {
				items = {},
				armor = {}
			}
		end,
		MatchHistoryController = require(lplr.PlayerScripts.TS.controllers.global['match-history']['match-history-controller']),
		PlayerProfileUIController = require(lplr.PlayerScripts.TS.controllers.global['player-profile']['player-profile-ui-controller']),
		HudAliveCount = require(lplr.PlayerScripts.TS.controllers.global['top-bar'].ui.game['hud-alive-player-counts']).HudAlivePlayerCounts,
		ItemMeta = (function()
			local fn = require(replicatedStorage.TS.item['item-meta']).getItemMeta
			for i = 1, 6 do
				local v = debug.getupvalue(fn, i)
				if type(v) == 'table' and next(v) then return v end
			end
			return {}
		end)(),
		KillEffectMeta = require(replicatedStorage.TS.locker['kill-effect']['kill-effect-meta']).KillEffectMeta,
		KillFeedController = Flamework.resolveDependency('client/controllers/game/kill-feed/kill-feed-controller@KillFeedController'),
		Knit = Knit,
		KnockbackUtil = require(replicatedStorage.TS.damage['knockback-util']).KnockbackUtil,
		MageKitUtil = require(replicatedStorage.TS.games.bedwars.kit.kits.mage['mage-kit-util']).MageKitUtil,
		NametagController = Knit.Controllers.NametagController,
		PartyController = Flamework.resolveDependency("@easy-games/lobby:client/controllers/party-controller@PartyController"),
		ProjectileMeta = require(replicatedStorage.TS.projectile['projectile-meta']).ProjectileMeta,
		QueryUtil = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out).GameQueryUtil,
		QueueCard = require(lplr.PlayerScripts.TS.controllers.global.queue.ui['queue-card']).QueueCard,
		QueueMeta = require(replicatedStorage.TS.game['queue-meta']).QueueMeta,
		Roact = require(replicatedStorage['rbxts_include']['node_modules']['@rbxts']['roact'].src),
		RuntimeLib = require(replicatedStorage['rbxts_include'].RuntimeLib),
		SoundList = require(replicatedStorage.TS.sound['game-sound']).GameSound,
		SoundManager = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out).SoundManager,
		Store = require(lplr.PlayerScripts.TS.ui.store).ClientStore,
		TeamUpgradeMeta = debug.getupvalue(require(replicatedStorage.TS.games.bedwars['team-upgrade']['team-upgrade-meta']).getTeamUpgradeMetaForQueue, 7) or {},
		UILayers = require(replicatedStorage['rbxts_include']['node_modules']['@easy-games']['game-core'].out).UILayers,
		VisualizerUtils = require(lplr.PlayerScripts.TS.lib.visualizer['visualizer-utils']).VisualizerUtils,
		WeldTable = require(replicatedStorage.TS.util['weld-util']).WeldUtil,
		WinEffectMeta = require(replicatedStorage.TS.locker['win-effect']['win-effect-meta']).WinEffectMeta,
		ZapNetworking = require(lplr.PlayerScripts.TS.lib.network),
	}, {
		__index = function(self, ind)
			rawset(self, ind, Knit.Controllers[ind])
			return rawget(self, ind)
		end
	})

	getgenv().bedwars = bedwars

	local remoteNames = {
		AfkStatus = safeGetProto(Knit.Controllers.AfkController.KnitStart, 1),
		AttackEntity = Knit.Controllers.SwordController.sendServerRequest,
		BeePickup = Knit.Controllers.BeeNetController.trigger,
		CannonAim = safeGetProto(Knit.Controllers.CannonController.startAiming, 5),
		CannonLaunch = Knit.Controllers.CannonHandController.launchSelf,
		ConsumeBattery = safeGetProto(Knit.Controllers.BatteryController.onKitLocalActivated, 1),
		ConsumeItem = safeGetProto(Knit.Controllers.ConsumeController.onEnable, 1),
		ConsumeSoul = Knit.Controllers.GrimReaperController.consumeSoul,
		ConsumeTreeOrb = safeGetProto(Knit.Controllers.EldertreeController.createTreeOrbInteraction, 1),
		DepositPinata = safeGetProto(safeGetProto(Knit.Controllers.PiggyBankController.KnitStart, 2), 5),
		DragonBreath = safeGetProto(Knit.Controllers.VoidDragonController.onKitLocalActivated, 5),
		DragonEndFly = safeGetProto(Knit.Controllers.VoidDragonController.flapWings, 1),
		DragonFly = Knit.Controllers.VoidDragonController.flapWings,
		DropItem = Knit.Controllers.ItemDropController.dropItemInHand,
		EquipItem = safeGetProto(require(replicatedStorage.TS.entity.entities['inventory-entity']).InventoryEntity.equipItem, 3),
		FireProjectile = safeGetProto(Knit.Controllers.ProjectileController.launchProjectileWithValues, 2) or function() end,
		GroundHit = Knit.Controllers.FallDamageController.KnitStart,
		GuitarHeal = Knit.Controllers.GuitarController.performHeal,
		HannahKill = safeGetProto(Knit.Controllers.HannahController.registerExecuteInteractions, 1),
		HarvestCrop = safeGetProto(safeGetProto(Knit.Controllers.CropController.KnitStart, 4), 1),
		KaliyahPunch = safeGetProto(Knit.Controllers.DragonSlayerController.onKitLocalActivated, 1),
		MageSelect = safeGetProto(Knit.Controllers.MageController.registerTomeInteraction, 1),
		PickupItem = Knit.Controllers.ItemDropController.checkForPickup,
		PickupMetal = safeGetProto(Knit.Controllers.HiddenMetalController.onKitLocalActivated, 4),
		ReportPlayer = require(lplr.PlayerScripts.TS.controllers.global.report['report-controller']).default.reportPlayer,
		ResetCharacter = safeGetProto(Knit.Controllers.ResetController.createBindable, 1),
		SummonerClawAttack = Knit.Controllers.SummonerClawHandController.attack,
		WarlockTarget = safeGetProto(Knit.Controllers.WarlockStaffController.KnitStart, 2)
	}

	local function dumpRemote(tab)
		local ind
		for i, v in tab do
			if v == 'Client' then
				ind = i
				break
			end
		end
		return ind and tab[ind + 1] or ''
	end

	local preDumped = {
		EquipItem = 'SetInvItem',
		ActivateGravestone = 'ActivateGravestone',
		CollectCollectableEntity = 'CollectCollectableEntity',
		DefenderRequestPlaceBlock = 'DefenderRequestPlaceBlock',
		RequestDragonPunch = 'RequestDragonPunch',
		Harvest = 'CropHarvest',
		DepositCoins = 'DepositCoins',
		BedwarsPurchaseItem = 'BedwarsPurchaseItem',
		BedBreakEffectTriggered = 'BedBreakEffectTriggered',
		BloodAssassinSelectContract = 'BloodAssassinSelectContract',
		Mimic = 'MimicBlock',
		StyxPortal = 'UseStyxPortalFromClient',
		StyxExitPortal = 'StyxOpenExitPortalFromServer',
		StyxSpawnExitPortal = 'StyxSpawnExitPortalFromServer',
		StyxSpawnEntrancePortal = 'StyxSpawnEntrancePortalFromServer',
		TryOpenStyxPortalExit = 'StyxTryOpenExitPortalFromClient',
		TeleportToLobby = 'TeletoLobby',
		FishCaught = 'FishCaught',
		BountyHunterTargetChanged = 'BountyHunterTargetChanged',
		SpawnRaven = 'SpawnRaven',
		PaladinAbilityRequest = 'PaladinAbilityRequest',
		OwlActionAbilities = 'OwlActionAbilities',
		DrillAttack = 'DrillAttack',
		UpgradeFrostyHammer = 'UpgradeFrostyHammer',
		UpgradeFlamethrower = 'UpgradeFlamethrower',
		TryBlockKick = 'TryBlockKick',   
		Ranks = 'FetchRanks',
		ResearchEnchant = 'EnchantTableResearch',
		DropDroneItem = 'DropDroneItem',
		AttemptFireOasisProjectiles = 'AttemptFireOasisProjectiles',
		WinEffectTriggered = 'WinEffectTriggered',
		ExtractFromDrill = 'ExtractFromDrill',
		HannahPromptTrigger = 'HannahPromptTrigger',
		DragonFlap = 'DragonFlap',
		DragonBreath = 'DragonBreath',
		AttemptCardThrow = 'AttemptCardThrow',
		LearnElementTome = 'LearnElementTome',
		RequestMoveSlime = 'RequestMoveSlime',
		SummonOwl = 'SummonOwl',
		RemoveOwl = 'RemoveOwl',
		OwlFireProjectile = 'OwlFireProjectile',
		OwlAiming = 'OwlAiming',
		MimicBlockPickPocketPlayer = 'MimicBlockPickPocketPlayer',
		DestroyPetrifiedPlayer = 'DestroyPetrifiedPlayer',
		UseAbility = 'useAbility',
		FishFound = 'FishFound',
	}

	for k, v in pairs(preDumped) do
		if not remotes[k] then
			remotes[k] = v
		end
	end

	for i, v in remoteNames do
		local remote
		if type(v) == "string" then
			remote = v
		elseif type(v) == "function" then
			local consts = debug.getconstants(v)
			remote = dumpRemote(consts)
		else
			remote = ""
		end

		if remote == '' or remote == nil then
			if not preDumped[i] then
				notif('aerov4', 'Failed to grab remote ('..tostring(i)..')', 10, 'alert')
			end
			remote = preDumped[i] or ''
		end
		remotes[i] = remote
	end

	pcall(function()
		local net = game:GetService('ReplicatedStorage').rbxts_include.node_modules['@rbxts'].net.out._NetManaged
		local knownFixes = {
			FireProjectile = 'ProjectileFire',
			AttackEntity = 'SwordHit',
		}
		for k, v in pairs(remotes) do
			if type(v) == 'string' and v ~= '' and not net:FindFirstChild(v) then
				if knownFixes[k] and net:FindFirstChild(knownFixes[k]) then
					remotes[k] = knownFixes[k]
				elseif preDumped[k] and net:FindFirstChild(preDumped[k]) then
					remotes[k] = preDumped[k]
				end
			end
		end
	end)

	getgenv().remotes = remotes

	OldBreak = bedwars.BlockController.isBlockBreakable

	Client.Get = function(self, remoteName, ...)
		local call = OldGet(self, remoteName, ...)

		if remoteName == remotes.AttackEntity then
			return {
				instance = call.instance,
				SendToServer = function(_, attackTable, ...)
					local suc, plr = pcall(function()
						return playersService:GetPlayerFromCharacter(attackTable.entityInstance)
					end)

					local selfpos = attackTable.validate.selfPosition.value
					local targetpos = attackTable.validate.targetPosition.value
					store.attackReach = ((selfpos - targetpos).Magnitude * 100) // 1 / 100
					store.attackReachUpdate = tick() + 1

					if (Reach.Enabled or HitBoxes.Enabled) and (selfpos - targetpos).Magnitude > 14.399 then
						attackTable.validate.raycast = attackTable.validate.raycast or {}
						attackTable.validate.selfPosition.value += CFrame.lookAt(selfpos, targetpos).LookVector * math.max((selfpos - targetpos).Magnitude - 14.399, 0)
					end

					return call:SendToServer(attackTable, ...)
				end
			}
		elseif remoteName == 'StepOnSnapTrap' and TrapDisabler and TrapDisabler.Enabled then
			return {SendToServer = function() end}
		end

		return call
	end

	local bedtms = {}

	local cache, blockhealthbar = {}, {blockHealth = -1, breakingBlockPosition = Vector3.zero}
	
	local cacheCleanThread = task.spawn(function()
		while vape.Loaded do
			task.wait(60)
			if vape.Loaded then
				table.clear(cache)
				table.clear(bedtms)
			end
		end
	end)
	vape:Clean(function() task.cancel(cacheCleanThread) end)

	store.blockPlacer = bedwars.BlockPlacer.new(bedwars.BlockEngine, 'wool_white')

	local function getBlockHealth(block, blockpos)
		local blockdata = bedwars.BlockController:getStore():getBlockData(blockpos)
		return (blockdata and (blockdata:GetAttribute('1') or blockdata:GetAttribute('Health')) or block:GetAttribute('Health'))
	end

	local function getBlockHits(block, blockpos)
		if not block then return 0 end
		local breaktype = bedwars.ItemMeta[block.Name].block.breakType
		local tool = store.tools[breaktype]
		tool = tool and bedwars.ItemMeta[tool.itemType].breakBlock[breaktype] or 2
		return getBlockHealth(block, bedwars.BlockController:getBlockPosition(blockpos)) / tool
	end

	local function calculatePath(target, blockpos, angle, anchor)
		if not anchor and cache[blockpos] then
			if tick() - (cache[blockpos].timestamp or 0) < 1 then
				return unpack(cache[blockpos])
			else
				cache[blockpos] = nil
			end
		end
		angle = angle or 360
		local visited = {}
		local unvisited = {{0, blockpos}}
		local distances = {[blockpos] = 0}
		local air = {}
		local path = {}
		for _ = 1, 10000 do
			local _, node = next(unvisited)
			if not node then break end
			table.remove(unvisited, 1)
			visited[node[2]] = true

			for _, side in sides do
				local neighbor = node[2] + side
				if visited[neighbor] then continue end

				local block = getPlacedBlock(neighbor)
				if not block or block:GetAttribute('NoBreak') or block == target then
					if not block then
						air[node[2]] = true
					end
					continue
				end

				if angle < 360 then
					local camFlat = gameCamera.CFrame.LookVector * Vector3.new(1, 0, 1)
					local blockFlat = (block.Position - entitylib.character.RootPart.Position) * Vector3.new(1, 0, 1)
					if camFlat.Magnitude > 0.01 and blockFlat.Magnitude > 0.01 then
						if math.acos(math.clamp(camFlat.Unit:Dot(blockFlat.Unit), -1, 1)) > math.rad(angle) / 2 then
							continue
						end
					end
				end

				local curdist = getBlockHits(block, neighbor) + node[1]
				if curdist < (distances[neighbor] or math.huge) then
					table.insert(unvisited, {curdist, neighbor})
					distances[neighbor] = curdist
					path[neighbor] = node[2]
				end
			end
		end

		local pos, cost = nil, math.huge
		for node in air do
			local d = distances[node]
			if d then
				local score = d
				if anchor then
					local delta = node - anchor
					score = (delta * Vector3.new(1, 0, 1)).Magnitude + math.abs(delta.Y) * 2 + d * 0.02
				end
				if score < cost then
					pos, cost = node, score
				end
			end
		end

		if pos then
			if not anchor then
				cache[blockpos] = {pos, cost, path, timestamp = tick()}
			end
			return pos, cost, path
		end
	end
	bedwars.calculatePath = calculatePath

	bedwars.placeBlock = function(pos, item)
		if getItem(item) then
			store.blockPlacer.blockType = item
			local ok, result = pcall(function()
				return store.blockPlacer:placeBlock(bedwars.BlockController:getBlockPosition(pos))
			end)
			if ok then return result end
		end
	end

	bedwars.breakBlock = function(block, effects, anim, customHealthbar, visualise, angle, anchor)
		if lplr:GetAttribute('DenyBlockBreak') or not entitylib.isAlive then return end
		local handler = bedwars.BlockController:getHandlerRegistry():getHandler(block.Name)
		local cost, pos, target, path = math.huge, nil, nil, nil
		local playerPos = entitylib.character.RootPart.Position

		local mag = 9e9
		for _, v in (handler and handler:getContainedPositions(block) or {block.Position / 3}) do
			local dpos, dcost, dpath = calculatePath(block, v * 3, angle or 360, anchor)
			local dmag = dpos and (playerPos - dpos).Magnitude or 9e9
			if dpos and dcost <= cost and dmag < mag then
				cost, pos, target, path, mag = dcost, dpos, v * 3, dpath, dmag
			end
		end

		if pos then
			if (entitylib.character.RootPart.Position - pos).Magnitude > 30 then return end
			local dblock, dpos = getPlacedBlock(pos)
			if not dblock and target then
				dblock, dpos = getPlacedBlock(target)
			end
			if not dblock then
				dblock, dpos = block, bedwars.BlockController:getBlockPosition(block.Position)
			end
			if not dblock or not dpos then return end

			if (workspace:GetServerTimeNow() - bedwars.SwordController.lastAttack) > 0.4 then
				local breaktype = dblock.Name == 'gumdrop_bounce_pad' and 'stone' or bedwars.ItemMeta[dblock.Name].block.breakType
				local tool = store.tools[breaktype]
				if tool then
					if visualise then
						local found = false
						for i, v in store.inventory.hotbar do
							if v.item and v.item.tool == tool.tool and i ~= (store.inventory.hotbarSlot + 1) then
								hotbarSwitch(i - 1)
								found = true
								break
							end
						end
						if not found then
							switchItem(tool.tool)
						end
					else
						switchItem(tool.tool)
					end
				end
			end

			if blockhealthbar.blockHealth == -1 or dpos ~= blockhealthbar.breakingBlockPosition then
				blockhealthbar.blockHealth = getBlockHealth(dblock, dpos)
				blockhealthbar.breakingBlockPosition = dpos
			end

			bedwars.ClientDamageBlock:Get('DamageBlock'):CallServerAsync({
				blockRef = {blockPosition = dpos},
				hitPosition = pos,
				hitNormal = Vector3.FromNormalId(Enum.NormalId.Top)
			}):andThen(function(result)
				if result then
					if result == 'cancelled' then
						table.clear(cache)
						return
					end
					if result == 'destroyed' then
						table.clear(cache) 
					end
					if effects then
						local blockdmg = (blockhealthbar.blockHealth - (result == 'destroyed' and 0 or getBlockHealth(dblock, dpos)))
						customHealthbar = customHealthbar or bedwars.BlockBreaker.updateHealthbar
						customHealthbar(bedwars.BlockBreaker, {blockPosition = dpos}, blockhealthbar.blockHealth, dblock:GetAttribute('MaxHealth'), blockdmg, dblock)
						blockhealthbar.blockHealth = math.max(blockhealthbar.blockHealth - blockdmg, 0)
						pcall(function()
							if blockhealthbar.blockHealth <= 0 then
								bedwars.BlockBreaker.breakEffect:playBreak(dblock.Name, dpos, lplr)
								bedwars.BlockBreaker.healthbarMaid:DoCleaning()
								blockhealthbar.breakingBlockPosition = Vector3.zero
							else
								bedwars.BlockBreaker.breakEffect:playHit(dblock.Name, dpos, lplr)
							end
						end)
					end
					if anim then
						local animation = bedwars.AnimationUtil:playAnimation(lplr, bedwars.BlockController:getAnimationController():getAssetId(1))
						bedwars.ViewmodelController:playAnimation(15)
						task.wait(0.3)
						animation:Stop()
						animation:Destroy()
					end
				end
			end)

			return pos, path, target
		end
	end

	for _, v in Enum.NormalId:GetEnumItems() do
		table.insert(sides, Vector3.FromNormalId(v) * 3)
	end

	local function updateStore(new, old)
		if new.Bedwars ~= old.Bedwars then
			store.equippedKit = new.Bedwars.kit ~= 'none' and new.Bedwars.kit or ''
		end
		if new.Game ~= old.Game then
			store.matchState = new.Game.matchState
			store.queueType = new.Game.queueType or 'bedwars_test'
		end

		if new.Inventory ~= old.Inventory then
			local newinv = (new.Inventory and new.Inventory.observedInventory or {inventory = {}})
			local oldinv = (old.Inventory and old.Inventory.observedInventory or {inventory = {}})
			store.inventory = newinv

			if newinv ~= oldinv then
				fireInventoryChanged()
			end

			if newinv.inventory.items ~= oldinv.inventory.items then
				vapeEvents.InventoryAmountChanged:Fire()
				local now = tick()
				if not store.lastToolUpdate or now - store.lastToolUpdate > 0.5 then
					store.lastToolUpdate = now
					store.tools.sword = getSword()
					for _, v in {'stone', 'wood', 'wool'} do
						store.tools[v] = getTool(v)
					end
				elseif not store.toolUpdatePending then
					store.toolUpdatePending = true
					task.delay(0.5, function()
						store.toolUpdatePending = false
						store.lastToolUpdate = tick()
						store.tools.sword = getSword()
						for _, v in {'stone', 'wood', 'wool'} do
							store.tools[v] = getTool(v)
						end
					end)
				end
			end

			if newinv.inventory.hand ~= oldinv.inventory.hand then
				store.lastToolUpdate = tick()
				store.tools.sword = getSword()
				for _, v in {'stone', 'wood', 'wool'} do
					store.tools[v] = getTool(v)
				end
				local currentHand, toolType = new.Inventory.observedInventory.inventory.hand, ''
				if currentHand then
					local handData = bedwars.ItemMeta[currentHand.itemType]
					toolType = handData.sword and 'sword' or handData.block and 'block' or currentHand.itemType:find('bow') and 'bow'
				end

				store.hand = {
					tool = currentHand and currentHand.tool,
					amount = currentHand and currentHand.amount or 0,
					toolType = toolType
				}
			end
		end
	end

	local storeChanged = bedwars.Store.changed:connect(updateStore)
	vape:Clean(function() storeChanged:disconnect() end)
	updateStore(bedwars.Store:getState(), {})

	for _, event in {'MatchEndEvent', 'EntityDeathEvent', 'BedwarsBedBreak', 'BalloonPopped', 'AngelProgress', 'GrapplingHookFunctions'} do
		if not vape.Connections then return end
		bedwars.Client:WaitFor(event):andThen(function(connection)
			vape:Clean(connection:Connect(function(...)
				vapeEvents[event]:Fire(...)
			end))
		end)
	end

	local _dmgEventData = {entityInstance=nil,damage=nil,damageType=nil,fromPosition=nil,fromEntity=nil,knockbackMultiplier=nil,knockbackId=nil,disableDamageHighlight=nil}
	vape:Clean(bedwars.ZapNetworking.EntityDamageEventZap.On(function(...)
		_dmgEventData.entityInstance = ...
		_dmgEventData.damage = select(2, ...)
		_dmgEventData.damageType = select(3, ...)
		_dmgEventData.fromPosition = select(4, ...)
		_dmgEventData.fromEntity = select(5, ...)
		_dmgEventData.knockbackMultiplier = select(6, ...)
		_dmgEventData.knockbackId = select(7, ...)
		_dmgEventData.disableDamageHighlight = select(13, ...)
		vapeEvents.EntityDamageEvent:Fire(_dmgEventData)
	end))

	vape:Clean(playersService.PlayerRemoving:Connect(function(plr)
		store.inventories[plr] = nil
	end))

	for _, event in {'PlaceBlockEvent', 'BreakBlockEvent'} do
		vape:Clean(bedwars.ZapNetworking[event..'Zap'].On(function(...)
			local pos = ...
			local plr = select(5, ...)
			task.spawn(function()
				pcall(function()
					vapeEvents[event]:Fire({blockRef = {blockPosition = pos}, player = plr})
				end)
			end)
		end))
	end

	store.blocks = collection('block', vape)
	store.shop = collection({'BedwarsItemShop', 'TeamUpgradeShopkeeper'}, vape, function(tab, obj)
		table.insert(tab, {
			Id = obj.Name,
			RootPart = obj,
			Shop = obj:HasTag('BedwarsItemShop'),
			Upgrades = obj:HasTag('TeamUpgradeShopkeeper')
		})
	end)
	store.enchant = collection({'enchant-table', 'broken-enchant-table'}, vape, nil, function(tab, obj, tag)
		if obj:HasTag('enchant-table') and tag == 'broken-enchant-table' then return end
		obj = table.find(tab, obj)
		if obj then
			table.remove(tab, obj)
		end
	end)

	local kills = sessioninfo:AddItem('Kills')
	local beds = sessioninfo:AddItem('Beds')
	local wins = sessioninfo:AddItem('Wins')
	local games = sessioninfo:AddItem('Games')

	local mapname = 'Unknown'
	sessioninfo:AddItem('Map', 0, function()
		return mapname
	end, false)

	task.delay(1, function()
		games:Increment()
	end)

	task.spawn(function()
		pcall(function()
			repeat task.wait() until store.matchState ~= 0 or vape.Loaded == nil
			if vape.Loaded == nil then return end
			store.map = waitForChildYield(workspace, 9e9, 'Map', 'Worlds'):GetChildren()[1]
			mapname = store.map.Name
			mapname = string.gsub(string.split(mapname, '_')[2] or mapname, '-', '') or 'Blank'
			if store.map then
				vape:Clean(store.map.Blocks.ChildAdded:Connect(function(v) 
					task.delay(0, function()
						if v:GetAttribute('Block') and (v:GetAttribute('PlacedByUserId') or 0) ~= 0 then
							local data = {
								blockRef = {
									blockPosition = v.Position / 3,
								},
								player = playersService:GetPlayerByUserId(v:GetAttribute('PlacedByUserId')),
							}
							for i, v in cache do
								if ((data.blockRef.blockPosition * 3) - v[1]).Magnitude <= 30 then
									table.clear(v[3])
									table.clear(v)
									cache[i] = nil
								end
							end
							vapeEvents.PlaceBlockEvent:Fire(data)
						end
					end)
				end))
			end
		end)
	end)

	vape:Clean(vapeEvents.BedwarsBedBreak.Event:Connect(function(bedTable)
		if bedTable.player and bedTable.player.UserId == lplr.UserId then
			beds:Increment()
		end
	end))

	vape:Clean(vapeEvents.MatchEndEvent.Event:Connect(function(winTable)
		if (bedwars.Store:getState().Game.myTeam or {}).id == winTable.winningTeamId or lplr.Neutral then
			wins:Increment()
		end
	end))

	vape:Clean(vapeEvents.EntityDeathEvent.Event:Connect(function(deathTable)
		local killer = playersService:GetPlayerFromCharacter(deathTable.fromEntity)
		local killed = playersService:GetPlayerFromCharacter(deathTable.entityInstance)
		if not killed or not killer then return end

		if killed ~= lplr and killer == lplr then
			kills:Increment()
		end
	end))

	pcall(function()
		bedwars.Shop = require(replicatedStorage.TS.games.bedwars.shop['bedwars-shop']).BedwarsShop
		bedwars.ShopItems = bedwars.Shop.ShopItems
		bedwars.Shop.getShopItem('iron_sword', lplr)
		store.shopLoaded = true
	end)

	vape:Clean(function()
		Client.Get = OldGet
		bedwars.BlockController.isBlockBreakable = OldBreak
		store.blockPlacer:disable()
		for _, v in vapeEvents do
			v:Destroy()
		end
		for _, v in cache do
			table.clear(v[3])
			table.clear(v)
		end
		table.clear(store.blockPlacer)
		table.clear(vapeEvents)
		table.clear(bedwars)
		table.clear(store)
		table.clear(cache)
		table.clear(sides)
		table.clear(remotes)
		storeChanged:disconnect()
		storeChanged = nil

		if entitylib.Connections then
			for _, conn in ipairs(entitylib.Connections) do
				if conn and type(conn) == "userdata" and conn.Connected then
					conn:Disconnect()
				end
			end
			table.clear(entitylib.Connections)
		end

		if entitylib.PlayerConnections then
			for _, plrConns in pairs(entitylib.PlayerConnections) do
				if type(plrConns) == "table" then
					for _, conn in ipairs(plrConns) do
						if conn and type(conn) == "userdata" and conn.Connected then
							conn:Disconnect()
						end
					end
				end
			end
			table.clear(entitylib.PlayerConnections)
		end

		if entitylib.EntityThreads then
			for char, thread in pairs(entitylib.EntityThreads) do
				if thread and task.cancel then
					task.cancel(thread)
				end
			end
			table.clear(entitylib.EntityThreads)
		end

		if entitylib.List then
			for _, ent in ipairs(entitylib.List) do
				if ent.Connections then
					for _, conn in ipairs(ent.Connections) do
						if conn and type(conn) == "userdata" and conn.Connected then
							conn:Disconnect()
						end
					end
					table.clear(ent.Connections)
				end
			end
			table.clear(entitylib.List)
		end
		if entitylib.stop then
			entitylib.stop()
		end
		for playerId, data in pairs(lagConnections) do
			if data and data.connection then
				pcall(function() data.connection:Disconnect() end)
			end
		end
		table.clear(lagConnections)
	end)
end)

for _, v in {'AntiRagdoll', 'TriggerBot', 'SilentAim', 'AutoRejoin', 'Rejoin', 'Disabler', 'Timer', 'ServerHop', 'MouseTP', 'MurderMystery', 'NameTags', 'Killaura', 'AimAssist', 'AutoClicker', 'Reach', 'AntiFall', 'Fly', 'HitBoxes', 'LongJump', 'Speed', 'Swim', 'PlayerModel', 'Search', 'Waypoints', 'Blink', 'StaffDetector', ''} do
	vape:Remove(v)
end

local Fly
local LongJump

local kitImageIds = {
	['none'] = "rbxassetid://16493320215",
	["random"] = "rbxassetid://79773209697352",
	["cowgirl"] = "rbxassetid://9155462968",
	["davey"] = "rbxassetid://9155464612",
	["warlock"] = "rbxassetid://15186338366",
	["ember"] = "rbxassetid://9630017904",
	["black_market_trader"] = "rbxassetid://18922642482",
	["yeti"] = "rbxassetid://9166205917",
	["scarab"] = "rbxassetid://137137517627492",
	["defender"] = "rbxassetid://131690429591874",
	["cactus"] = "rbxassetid://104436517801089",
	["oasis"] = "rbxassetid://120283205213823",
	["berserker"] = "rbxassetid://90258047545241",
	["sword_shield"] = "rbxassetid://131690429591874",
	["airbender"] = "rbxassetid://74712750354593",
	["gun_blade"] = "rbxassetid://138231219644853",
	["frost_hammer_kit"] = "rbxassetid://11838567073",
	["spider_queen"] = "rbxassetid://95237509752482",
	["archer"] = "rbxassetid://9224796984",
	["axolotl"] = "rbxassetid://9155466713",
	["baker"] = "rbxassetid://9155463919",
	["barbarian"] = "rbxassetid://9166207628",
	["builder"] = "rbxassetid://9155463708",
	["necromancer"] = "rbxassetid://11343458097",
	["cyber"] = "rbxassetid://9507126891",
	["sorcerer"] = "rbxassetid://97940108361528",
	["bigman"] = "rbxassetid://9155467211",
	["spirit_assassin"] = "rbxassetid://10406002412",
	["farmer_cletus"] = "rbxassetid://9155466936",
	["ice_queen"] = "rbxassetid://9155466204",
	["grim_reaper"] = "rbxassetid://9155467410",
	["spirit_gardener"] = "rbxassetid://132108376114488",
	["hannah"] = "rbxassetid://10726577232",
	["shielder"] = "rbxassetid://9155464114",
	["summoner"] = "rbxassetid://18922378956",
	["glacial_skater"] = "rbxassetid://84628060516931",
	["dragon_sword"] = "rbxassetid://16215630104",
	["lumen"] = "rbxassetid://9630018371",
	["flower_bee"] = "rbxassetid://101569742252812",
	["jellyfish"] = "rbxassetid://18129974852",
	["melody"] = "rbxassetid://9155464915",
	["mimic"] = "rbxassetid://14783283296",
	["miner"] = "rbxassetid://9166208461",
	["nazar"] = "rbxassetid://18926951849",
	["seahorse"] = "rbxassetid://11902552560",
	["elk_master"] = "rbxassetid://15714972287",
	["rebellion_leader"] = "rbxassetid://18926409564",
	["void_hunter"] = "rbxassetid://122370766273698",
	["taliyah"] = "rbxassetid://13989437601",
	["angel"] = "rbxassetid://9166208240",
	["harpoon"] = "rbxassetid://18250634847",
	["void_walker"] = "rbxassetid://78915127961078",
	["spirit_summoner"] = "rbxassetid://95760990786863",
	["triple_shot"] = "rbxassetid://9166208149",
	["void_knight"] = "rbxassetid://73636326782144",
	["regent"] = "rbxassetid://9166208904",
	["vulcan"] = "rbxassetid://9155465543",
	["owl"] = "rbxassetid://12509401147",
	["dasher"] = "rbxassetid://9155467645",
	["disruptor"] = "rbxassetid://11596993583",
	["wizard"] = "rbxassetid://13353923546",
	["aery"] = "rbxassetid://9155463221",
	["agni"] = "rbxassetid://17024640133",
	["alchemist"] = "rbxassetid://9155462512",
	["spearman"] = "rbxassetid://9166207341",
	["beekeeper"] = "rbxassetid://9312831285",
	["falconer"] = "rbxassetid://17022941869",
	["bounty_hunter"] = "rbxassetid://9166208649",
	["blood_assassin"] = "rbxassetid://12520290159",
	["battery"] = "rbxassetid://10159166528",
	["steam_engineer"] = "rbxassetid://15380413567",
	["vesta"] = "rbxassetid://9568930198",
	["beast"] = "rbxassetid://9155465124",
	["dino_tamer"] = "rbxassetid://9872357009",
	["drill"] = "rbxassetid://12955100280",
	["elektra"] = "rbxassetid://13841413050",
	["fisherman"] = "rbxassetid://9166208359",
	["queen_bee"] = "rbxassetid://12671498918",
	["card"] = "rbxassetid://13841410580",
	["frosty"] = "rbxassetid://9166208762",
	["gingerbread_man"] = "rbxassetid://9155464364",
	["ghost_catcher"] = "rbxassetid://9224802656",
	["tinker"] = "rbxassetid://17025762404",
	["ignis"] = "rbxassetid://13835258938",
	["oil_man"] = "rbxassetid://9166206259",
	["jade"] = "rbxassetid://9166306816",
	["dragon_slayer"] = "rbxassetid://10982192175",
	["paladin"] = "rbxassetid://11202785737",
	["pinata"] = "rbxassetid://10011261147",
	["merchant"] = "rbxassetid://9872356790",
	["metal_detector"] = "rbxassetid://9378298061",
	["slime_tamer"] = "rbxassetid://15379766168",
	["nyoka"] = "rbxassetid://17022941410",
	["midnight"] = "rbxassetid://9155462763",
	["pyro"] = "rbxassetid://9155464770",
	["raven"] = "rbxassetid://9166206554",
	["santa"] = "rbxassetid://9166206101",
	["sheep_herder"] = "rbxassetid://9155465730",
	["smoke"] = "rbxassetid://9155462247",
	["spirit_catcher"] = "rbxassetid://9166207943",
	["star_collector"] = "rbxassetid://9872356516",
	["styx"] = "rbxassetid://17014536631",
	["block_kicker"] = "rbxassetid://15382536098",
	["trapper"] = "rbxassetid://9166206875",
	["hatter"] = "rbxassetid://12509388633",
	["ninja"] = "rbxassetid://15517037848",
	["jailor"] = "rbxassetid://11664116980",
	["warrior"] = "rbxassetid://9166207008",
	["mage"] = "rbxassetid://10982191792",
	["void_dragon"] = "rbxassetid://10982192753",
	["cat"] = "rbxassetid://15350740470",
	["wind_walker"] = "rbxassetid://9872355499",
	['skeleton'] = "rbxassetid://120123419412119",
	['winter_lady'] = "rbxassetid://83274578564074",
	['soul_broker'] = 'rbxassetid://130409166262430'
}

local function isFirstPerson()
    if not (lplr.Character and lplr.Character:FindFirstChild("Head")) then
        return false
    end
    return (lplr.Character.Head.Position - gameCamera.CFrame.Position).Magnitude < 2
end

local function isFrozen(entity, threshold)
    threshold = threshold or 10
    local char
    if type(entity) == "table" and entity.Character then
        char = entity.Character
    elseif type(entity) == "Instance" and entity:IsA("Model") then
        char = entity
    elseif entity == nil then
        if not entitylib.isAlive then return false end
        char = entitylib.character.Character
    else
        return false
    end

    local stacks = char:GetAttribute("ColdStacks") or char:GetAttribute("FrostStacks")
               or char:GetAttribute("FreezeStacks") or char:GetAttribute("FROZEN_STACKS")
    if stacks and stacks >= threshold then return true end

    local statusEffects = char:GetAttribute("StatusEffects")
    if type(statusEffects) == "table" then
        for effectName, stackCount in pairs(statusEffects) do
            local nameLower = tostring(effectName):lower()
            if nameLower:match("cold") or nameLower:match("frost") or nameLower:match("freeze") then
                if type(stackCount) == "number" then
                    if stackCount >= threshold then return true end
                elseif stackCount then
                    return true
                end
            end
        end
    end

    if char:FindFirstChild("IceBlock") or char:FindFirstChild("FrozenBlock") or char:FindFirstChild("IceShell") then
        return true
    end

    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid and humanoid.WalkSpeed <= 2 then
        return true
    end

    return false
end

local sharedRaycast = RaycastParams.new()
sharedRaycast.FilterType = Enum.RaycastFilterType.Include
sharedRaycast.FilterDescendantsInstances = {workspace:FindFirstChild('Map') or workspace}

local function cloneRaycast()
    local r = RaycastParams.new()
    r.FilterType = sharedRaycast.FilterType
    r.FilterDescendantsInstances = sharedRaycast.FilterDescendantsInstances
    r.RespectCanCollide = sharedRaycast.RespectCanCollide
    return r
end

local function isSword()
    return store.hand and store.hand.toolType == 'sword'
end

local function hasValidWeapon()
    if not store.hand or not store.hand.tool then return false end
    local toolType = store.hand.toolType
    local toolName = store.hand.tool.Name:lower()
    if toolName:find('headhunter') then return true end
    return toolType == 'sword' or toolType == 'bow' or toolType == 'crossbow'
end

local function fixPosition(pos)
    if bedwars and bedwars.BlockController and bedwars.BlockController.getBlockPosition then
        return bedwars.BlockController:getBlockPosition(pos) * 3
    end
    return pos * 3
end

local function getAmmoForProjectile(check)
    for _, item in store.inventory.inventory.items do
        if check.ammoItemTypes and table.find(check.ammoItemTypes, item.itemType) then
            return item.itemType
        end
    end
end

local function getProjectileItems(ammoFilter)
    local items = {}
    for _, item in store.inventory.inventory.items do
        local _itemMeta = bedwars.ItemMeta[item.itemType]
        local proj = _itemMeta and _itemMeta.projectileSource
        local ammo = proj and getAmmoForProjectile(proj)
        if ammo and table.find(ammoFilter, ammo) then
            table.insert(items, {item, ammo, proj.projectileType(ammo), proj})
        end
    end
    return items
end

local function isHoldingItem(keywords, includeProjectileSource)
    if not store.hand or not store.hand.tool then return false end
    local toolName = store.hand.tool.Name:lower()
    for _, kw in ipairs(keywords) do
        if toolName:find(kw) then return true end
    end
    if includeProjectileSource then
        return bedwars.ItemMeta[toolName] and bedwars.ItemMeta[toolName].projectileSource and true or false
    end
    return false
end

local function isHoldingBowCrossbow(includeProjectileSource)
    return isHoldingItem({'bow', 'crossbow', 'headhunter'}, includeProjectileSource)
end

local function isHoldingPickaxe()
    return isHoldingItem({'pickaxe'})
end

local function isEnemy(ent)
    if not ent then return false end
    if ent.Character and ent.Character:HasTag('petrified-player') then return false end
    if ent.Player then
        local myTeam = lplr:GetAttribute('Team')
        local theirTeam = ent.Player:GetAttribute('Team')
        if not myTeam or not theirTeam or myTeam == theirTeam then return false end
        return true
    elseif ent.NPC then
        local npcTeam = ent.Character:GetAttribute('Team')
        if npcTeam then return lplr:GetAttribute('Team') ~= npcTeam end
        return true
    end
    return false
end

local function getShopNPC()
    local shop, items, upgrades, newid = nil, false, false, nil
    if entitylib.isAlive then
        local localPosition = entitylib.character.RootPart.Position
        for _, v in store.shop do
            if (v.RootPart.Position - localPosition).Magnitude <= 20 then
                shop = v.Upgrades or v.Shop or nil
                upgrades = upgrades or v.Upgrades
                items = items or v.Shop
                newid = v.Shop and v.Id or newid
            end
        end
    end
    return shop, items, upgrades, newid
end

local function isTeammate(player)
    if not lplr or not player then return false end
    local myTeam = lplr:GetAttribute('Team')
    local theirTeam = player:GetAttribute('Team')
    return myTeam and theirTeam and myTeam == theirTeam
end

local function getPlayerName(player, useDisplayName)
    if not player then return '' end
    return (useDisplayName and player.DisplayName ~= "" and player.DisplayName) or player.Name
end

local armorTiers = {'none','leather_chestplate','iron_chestplate','diamond_chestplate','emerald_chestplate'}
local function getArmorTier(player)
    if not player or not store.inventories[player] then return 0 end
    local chest = store.inventories[player].armor and store.inventories[player].armor[5]
    if not chest or chest == 'empty' then return 1 end
    return table.find(armorTiers, chest.itemType) or 1
end

local function checkFaceAdjacent(pos, faces)
    faces = faces or {
        Vector3.new(3,0,0), Vector3.new(-3,0,0), Vector3.new(0,3,0),
        Vector3.new(0,-3,0), Vector3.new(0,0,3), Vector3.new(0,0,-3)
    }
    for _, v in ipairs(faces) do
        if getPlacedBlock(pos + v) then return true end
    end
    return false
end

local _isAboveVoidParams = RaycastParams.new()
_isAboveVoidParams.FilterType = Enum.RaycastFilterType.Exclude
local function isAboveVoid(position)
    if not entitylib.isAlive or not entitylib.character then return false end
    _isAboveVoidParams.FilterDescendantsInstances = {entitylib.character.Character}
    local result = workspace:Raycast(position, Vector3.new(0, -500, 0), _isAboveVoidParams)
    return result == nil
end

local function hasFaceBelowOrSide(pos)
    if getPlacedBlock(pos - Vector3.new(0,3,0)) then return true end
    local sides = {Vector3.new(3,0,0), Vector3.new(-3,0,0), Vector3.new(0,0,3), Vector3.new(0,0,-3)}
    for _, v in ipairs(sides) do
        if getPlacedBlock(pos + v) then return true end
    end
    return false
end

local function nearCorner(poscheck, pos)
    local start = poscheck - Vector3.new(3,3,3)
    local fin = poscheck + Vector3.new(3,3,3)
    local dir = (pos - poscheck).Unit * 100
    local check = poscheck + dir
    return Vector3.new(
        math.clamp(check.X, start.X, fin.X),
        math.clamp(check.Y, start.Y, fin.Y),
        math.clamp(check.Z, start.Z, fin.Z)
    )
end

local function blockProximity(pos, rangeBlocks)
    rangeBlocks = rangeBlocks or 21
    local mag, best = 60, nil
    local blocks = getBlocksInPoints(
        bedwars.BlockController:getBlockPosition(pos - Vector3.new(rangeBlocks,rangeBlocks,rangeBlocks)),
        bedwars.BlockController:getBlockPosition(pos + Vector3.new(rangeBlocks,rangeBlocks,rangeBlocks))
    )
    for _, v in ipairs(blocks) do
        local bp = nearCorner(v, pos)
        local d = (pos - bp).Magnitude
        if hasFaceBelowOrSide(bp) and d < mag then
            mag, best = d, bp
        end
    end
    return best
end

local function isGUIOpen()
    return bedwars.AppController:isLayerOpen(bedwars.UILayers.MAIN)
        or bedwars.AppController:isLayerOpen(bedwars.UILayers.DIALOG)
        or bedwars.AppController:isLayerOpen(bedwars.UILayers.POPUP)
        or bedwars.AppController:isAppOpen('BedwarsItemShopApp')
        or (bedwars.Store:getState().Inventory and bedwars.Store:getState().Inventory.open)
end

local function isTargetValid(ent, maxDist, checkWalls)
    if not ent or not ent.RootPart or not ent.Character then return false end
    if not entitylib.isAlive then return false end
    local dist = (ent.RootPart.Position - entitylib.character.RootPart.Position).Magnitude
    if dist > maxDist then return false end
    if checkWalls then
        local ray = workspace:Raycast(
            entitylib.character.RootPart.Position,
            (ent.RootPart.Position - entitylib.character.RootPart.Position),
            sharedRaycast
        )
        if ray then return false end
    end
    local hum = ent.Character:FindFirstChild("Humanoid")
    return hum and hum.Health > 0
end

local function getTargetByPriority(originPos, range, opts)
    opts = opts or {}
    local players = opts.players == nil and true or opts.players
    local npcs = opts.npcs or false
    local walls = opts.walls or false
    local sort = opts.sort or 'distance' -- 'health','armor','damage'
    local damageTracker = opts.damageTracker 

    local valid = {}
    for _, ent in ipairs(entitylib.List) do
        if (players and ent.Player) or (npcs and ent.NPC) then
            if isEnemy(ent) and ent.RootPart then
                local dist = (ent.RootPart.Position - originPos).Magnitude
                if dist <= range then
                    if walls then
                        local ray = workspace:Raycast(originPos, (ent.RootPart.Position - originPos), sharedRaycast)
                        if not ray then
                            table.insert(valid, ent)
                        end
                    else
                        table.insert(valid, ent)
                    end
                end
            end
        end
    end
    if #valid == 0 then return nil end

    if sort == 'distance' then
        table.sort(valid, function(a,b)
            return (a.RootPart.Position - originPos).Magnitude < (b.RootPart.Position - originPos).Magnitude
        end)
    elseif sort == 'damage' and damageTracker then
        table.sort(valid, function(a,b)
            local keyA = a.Player and a.Player.UserId or tostring(a)
            local keyB = b.Player and b.Player.UserId or tostring(b)
            return (damageTracker[keyA] or 0) > (damageTracker[keyB] or 0)
        end)
    elseif sort == 'health' then
        table.sort(valid, function(a,b)
            return a.Health < b.Health
        end)
    elseif sort == 'armor' then
        table.sort(valid, function(a,b)
            return getArmorTier(a.Player) < getArmorTier(b.Player)
        end)
    end
    return valid[1]
end

local isMobile = inputService.TouchEnabled and not inputService.KeyboardEnabled and not inputService.MouseEnabled

local function getTeammates(namesOnly)
    local result = {}
    local myTeam = lplr:GetAttribute('Team')
    if not myTeam then return result end
    for _, player in playersService:GetPlayers() do
        if player ~= lplr and player:GetAttribute('Team') == myTeam then
            if namesOnly then
                table.insert(result, player.Name)
            elseif player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
                table.insert(result, player)
            end
        end
    end
    if namesOnly then
        table.sort(result)
    end
    return result
end

local function getNearestTeammateInRange(range, condition)
    if not entitylib.isAlive then return nil end
    local myPos = entitylib.character.RootPart.Position
    local nearest = nil
    local nearestDist = math.huge
    for _, player in ipairs(getTeammates()) do
        if player.Character and player.Character.PrimaryPart then
            local dist = (player.Character.PrimaryPart.Position - myPos).Magnitude
            if dist <= range then
                if condition and not condition(player) then continue end
                if dist < nearestDist then
                    nearestDist = dist
                    nearest = player
                end
            end
        end
    end
    return nearest
end

local function getPlayerHealth(player)
    if not player or not player.Character then return 0, 100 end
    local health = player.Character:GetAttribute('Health') or (player.Character:FindFirstChildOfClass('Humanoid') and player.Character.Humanoid.Health) or 0
    local maxHealth = player.Character:GetAttribute('MaxHealth') or (player.Character:FindFirstChildOfClass('Humanoid') and player.Character.Humanoid.MaxHealth) or 100
    return health, maxHealth
end

local function getPlayerHealthPercent(player)
    local health, maxHealth = getPlayerHealth(player)
    if maxHealth == 0 then return 0 end
    return (health / maxHealth) * 100
end

local function leftClick()
	pcall(function()
		VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
		task.wait(0.05)
		VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
	end)
end

local function getWorldFolder()
    local Map = workspace:FindFirstChild("Map")
    if not Map then return nil end
    local Worlds = Map:FindFirstChild("Worlds")
    if not Worlds then return nil end
    for _, world in Worlds:GetChildren() do
        return world
    end
    return nil
end

local function getPickaxeSlot()
	for i, v in store.inventory.hotbar do
		if v.item and bedwars.ItemMeta[v.item.itemType] then
			local meta = bedwars.ItemMeta[v.item.itemType]
			if meta.breakBlock then
				return i - 1
			end
		end
	end
	return nil
end

local function getScaffoldBlockForModule(limitItem)
	if limitItem.Enabled then
		if store.hand.toolType == 'block' then
			return store.hand.tool.Name
		end
		return nil
	else
		local wool = getWool()
		if wool then
			return wool
		else
			for _, item in store.inventory.inventory.items do
				local meta = bedwars.ItemMeta[item.itemType]
				if meta and meta.block then
					return item.itemType
				end
			end
		end
	end
	return nil
end

local function safeIsBreakable(pos)
    if not bedwars.BlockController then return false end
    local ok, result = pcall(function()
        return bedwars.BlockController:isBlockBreakable({blockPosition = pos / 3}, lplr)
    end)
    return ok and result
end

--[[
	Combat Modules
]]

run(function()
    local Mode
    local Expand
    local AutoToggle
    local Visible
    local VisibleColor
    local Targets
    local objects = {}
    local set = false
    local hitboxesActive = false
    local autoToggleConnection = nil
    local autoToggleFrameCounter = 0

    local vector3new = Vector3.new
    local vector3one = Vector3.one

    local colorList = {
        Red = Color3.fromRGB(255, 0, 0),
        Blue = Color3.fromRGB(0, 100, 255),
        Green = Color3.fromRGB(0, 255, 0),
        Yellow = Color3.fromRGB(255, 255, 0),
        Orange = Color3.fromRGB(255, 140, 0),
        Purple = Color3.fromRGB(180, 0, 255),
        White = Color3.fromRGB(255, 255, 255),
        Cyan = Color3.fromRGB(0, 255, 255),
        Pink = Color3.fromRGB(255, 50, 150),
        Black = Color3.fromRGB(0, 0, 0)
    }

    local function shouldCreateHitbox(ent)
        if not ent.Targetable then return false end
        if ent.Player and Targets and Targets.Players and Targets.Players.Enabled then return true end
        if not ent.Player and Targets and Targets.NPCs and Targets.NPCs.Enabled then return true end
        return false
    end

    local _wallRayParams = RaycastParams.new()
    _wallRayParams.FilterType = Enum.RaycastFilterType.Exclude
    local function isTargetBehindWall(ent)
        if not Targets or not Targets.Walls or not Targets.Walls.Enabled then return false end
        if not ent.RootPart then return false end
        if not entitylib.isAlive or not entitylib.character or not entitylib.character.RootPart then return false end
        local origin = entitylib.character.RootPart.Position
        local target = ent.RootPart.Position
        local direction = target - origin
        _wallRayParams.FilterDescendantsInstances = {entitylib.character, ent.Character}
        local result = workspace:Raycast(origin, direction, _wallRayParams)
        if result then
            local hitDist = (result.Position - origin).Magnitude
            local targetDist = direction.Magnitude
            if hitDist < targetDist - 0.5 then return true end
        end
        return false
    end

    local cachedExpandSize = vector3new(3, 6, 3)
    local lastExpandValue = 0
    local function updateExpandSize(val)
        if val ~= lastExpandValue then
            lastExpandValue = val
            cachedExpandSize = vector3new(3, 6, 3) + vector3one * (val / 5)
        end
    end

    local function createHitbox(ent)
        if not shouldCreateHitbox(ent) then return end
        if isTargetBehindWall(ent) then return end
        if objects[ent] then return end
        local hitbox = Instance.new('Part')
        hitbox.Size = cachedExpandSize
        hitbox.Position = ent.RootPart.Position
        hitbox.CanCollide = false
        hitbox.Massless = true
        hitbox.Transparency = Visible and Visible.Enabled and 0.5 or 1
        if Visible and Visible.Enabled and VisibleColor then
            hitbox.Color = colorList[VisibleColor.Value] or colorList.Red
        end
        hitbox.Parent = ent.Character
        local weld = Instance.new('Motor6D')
        weld.Part0 = hitbox
        weld.Part1 = ent.RootPart
        weld.Parent = hitbox
        local ev = Instance.new('ObjectValue')
        ev.Name = 'EntityValue'
        ev.Value = ent.Character
        ev.Parent = hitbox
        game:GetService('CollectionService'):AddTag(hitbox, 'Hitbox')
        objects[ent] = hitbox
    end

    local function clearHitboxes()
        for _, part in pairs(objects) do part:Destroy() end
        table.clear(objects)
    end

    local function refreshAllHitboxes()
        clearHitboxes()
        local entityList = entitylib.List
        for i = 1, #entityList do
            createHitbox(entityList[i])
        end
    end

    local function handleAutoToggle()
        if not AutoToggle or not AutoToggle.Enabled then return end
        if not HitBoxes.Enabled or Mode.Value ~= 'Player' then return end
        local holdingSword = isSword()
        if holdingSword and not hitboxesActive then
            hitboxesActive = true
            refreshAllHitboxes()
        elseif not holdingSword and hitboxesActive then
            hitboxesActive = false
            clearHitboxes()
        end
    end

    HitBoxes = vape.Categories.Combat:CreateModule({
        Name = 'HitBoxes',
        Function = function(callback)
            if callback then
                updateExpandSize(Expand.Value)
                if Mode.Value == 'Sword' then
                    debug.setconstant(bedwars.SwordController.swingSwordInRegion, 6, (Expand.Value / 3))
                    set = true
                else
                    HitBoxes:Clean(entitylib.Events.EntityAdded:Connect(function(ent)
                        if AutoToggle and AutoToggle.Enabled then
                            if hitboxesActive then createHitbox(ent) end
                        else
                            createHitbox(ent)
                        end
                    end))
                    HitBoxes:Clean(entitylib.Events.EntityRemoving:Connect(function(ent)
                        local obj = objects[ent]
                        if obj then obj:Destroy() objects[ent] = nil end
                    end))
                    if AutoToggle and AutoToggle.Enabled then
                        handleAutoToggle()
                        if not autoToggleConnection or not autoToggleConnection.Connected then
                            autoToggleFrameCounter = 0
                            autoToggleConnection = runService.Heartbeat:Connect(function()
                                autoToggleFrameCounter = autoToggleFrameCounter + 1
                                if autoToggleFrameCounter % 5 == 0 then
                                    handleAutoToggle()
                                end
                            end)
                            HitBoxes:Clean(autoToggleConnection)
                        end
                    else
                        refreshAllHitboxes()
                    end
                    local hitboxThrottleCounter = 0
                    HitBoxes:Clean(runService.Heartbeat:Connect(function()
                        if not Targets or not Targets.Walls or not Targets.Walls.Enabled then return end
                        hitboxThrottleCounter = hitboxThrottleCounter + 1
                        if hitboxThrottleCounter % 20 ~= 0 then return end
                        for ent, part in pairs(objects) do
                            if isTargetBehindWall(ent) then
                                part:Destroy()
                                objects[ent] = nil
                            end
                        end
                        local entityList = entitylib.List
                        for i = 1, #entityList do
                            local ent = entityList[i]
                            if not objects[ent] then
                                if AutoToggle and AutoToggle.Enabled then
                                    if hitboxesActive then createHitbox(ent) end
                                else
                                    createHitbox(ent)
                                end
                            end
                        end
                    end))
                end
            else
                hitboxesActive = false
                if set then
                    debug.setconstant(bedwars.SwordController.swingSwordInRegion, 6, 3.8)
                    set = false
                end
                clearHitboxes()
            end
        end,
        Tooltip = 'increases attack hitbox'
    })

    Targets = HitBoxes:CreateTargets({
        Players = true,
        Walls = false,
        NPCs = false,
        Function = function()
            if HitBoxes.Enabled and Mode.Value == 'Player' then
                if AutoToggle and AutoToggle.Enabled then
                    if hitboxesActive then refreshAllHitboxes() end
                else
                    refreshAllHitboxes()
                end
            end
        end
    })

    Mode = HitBoxes:CreateDropdown({
        Name = 'Mode',
        List = {'Sword', 'Player'},
        Function = function(val)
            local isPlayer = val == 'Player'
            if AutoToggle then AutoToggle.Object.Visible = isPlayer end
            if Visible then Visible.Object.Visible = isPlayer end
            if VisibleColor then VisibleColor.Object.Visible = isPlayer and Visible.Enabled end
            if HitBoxes.Enabled then HitBoxes:Toggle() HitBoxes:Toggle() end
        end,
    })

    Expand = HitBoxes:CreateSlider({
        Name = 'Expand amount',
        Min = 0,
        Max = 30,
        Default = 14.4,
        Decimal = 10,
        Function = function(val)
            updateExpandSize(val)
            if HitBoxes.Enabled then
                if Mode.Value == 'Sword' then
                    debug.setconstant(bedwars.SwordController.swingSwordInRegion, 6, (val / 3))
                else
                    for _, part in pairs(objects) do part.Size = cachedExpandSize end
                end
            end
        end,
        Suffix = function(val)
            return val == 1 and 'stud' or 'studs'
        end
    })

    AutoToggle = HitBoxes:CreateToggle({
        Name = 'Auto Toggle',
        Default = false,
        Tooltip = 'enables hitbox when holding sword, disable when not',
        Function = function(callback)
            if callback then
                if autoToggleConnection then autoToggleConnection:Disconnect() end
                hitboxesActive = false
                autoToggleFrameCounter = 0
                autoToggleConnection = runService.Heartbeat:Connect(function()
                    autoToggleFrameCounter = autoToggleFrameCounter + 1
                    if autoToggleFrameCounter % 5 == 0 then
                        handleAutoToggle()
                    end
                end)
                HitBoxes:Clean(autoToggleConnection)
                handleAutoToggle()
            else
                if autoToggleConnection then
                    autoToggleConnection:Disconnect()
                    autoToggleConnection = nil
                end
                hitboxesActive = false
                if HitBoxes.Enabled and Mode.Value == 'Player' then
                    refreshAllHitboxes()
                end
            end
        end
    })

    Visible = HitBoxes:CreateToggle({
        Name = 'Visible',
        Default = false,
        Function = function(callback)
            if VisibleColor then VisibleColor.Object.Visible = callback end
            if HitBoxes.Enabled and Mode.Value == 'Player' then
                local transparency = callback and 0.5 or 1
                local col = callback and VisibleColor and (colorList[VisibleColor.Value] or colorList.Red) or nil
                for _, part in pairs(objects) do
                    part.Transparency = transparency
                    if col then part.Color = col end
                end
            end
        end
    })

    VisibleColor = HitBoxes:CreateDropdown({
        Name = 'Hitbox Color',
        List = {'Red', 'Blue', 'Green', 'Yellow', 'Orange', 'Purple', 'White', 'Cyan', 'Pink', 'Black'},
        Default = 'Red',
        Visible = false,
        Function = function(val)
            if HitBoxes.Enabled and Mode.Value == 'Player' and Visible.Enabled then
                local col = colorList[val] or colorList.Red
                for _, part in pairs(objects) do part.Color = col end
            end
        end
    })

    task.spawn(function()
        repeat task.wait() until Mode and Mode.Value
        local isPlayer = Mode.Value == 'Player'
        AutoToggle.Object.Visible = isPlayer
        Visible.Object.Visible = isPlayer
    end)

    task.defer(function()
        if VisibleColor and VisibleColor.Object then
            VisibleColor.Object.Visible = false
        end
    end)
end)

run(function()
    local ShopAutoClicker 
    local CPS
    local holding = false
    local clickThread
    
    local function getShopId()
        if not entitylib.isAlive then return nil end
        local localPosition = entitylib.character.RootPart.Position
        local id
        for _, v in store.shop do
            if v.Shop and (v.RootPart.Position - localPosition).Magnitude <= 20 then
                id = v.Id
            end
        end
        return id
    end
    
    local function getHoveredItem(pos)
        local mousepos = (pos or inputService:GetMouseLocation()) - guiService:GetGuiInset()
        for _, v in lplr.PlayerGui:GetGuiObjectsAtPosition(mousepos.X, mousepos.Y) do
            local obj = v
            while obj and obj ~= lplr.PlayerGui do
                local itemType = obj.Name:match('^(.+)_ShopItemCard$')
                if itemType then
                    return itemType
                end
                obj = obj.Parent
            end
        end
    end
    
    local function canBuy(item)
        if item.ignoredByKit and table.find(item.ignoredByKit, store.equippedKit or '') then return false end
        if item.lockedByForge or item.disabled then return false end
        if item.require and item.require.teamUpgrade then
            if (bedwars.Store:getState().Bedwars.teamUpgrades[item.require.teamUpgrade.upgradeId] or -1) < item.require.teamUpgrade.lowestTierIndex then
                return false
            end
        end
        local currency = getItem(item.currency)
        return (currency and currency.amount or 0) >= item.price
    end
    
    local function purchase(itemType, shopId)
        if bedwars.BedwarsShopController.alreadyPurchasedMap[itemType] ~= nil then return end
    
        local item = bedwars.Shop.getShopItem(itemType, lplr, {shopId = shopId})
        if not item or not canBuy(item) then return end
    
        bedwars.Client:Get('BedwarsPurchaseItem'):CallServerAsync({
            shopItem = item,
            shopId = shopId
        }):andThen(function(suc)
            if not suc then return end
            bedwars.SoundManager:playSound(bedwars.SoundList.BEDWARS_PURCHASE_ITEM)
            bedwars.Store:dispatch({
                type = 'BedwarsAddItemPurchased',
                itemType = itemType
            })
            if item.tiered then
                bedwars.BedwarsShopController.alreadyPurchasedMap[itemType] = true
            end
        end)
    end
    
    local function startClicking(itemType)
        if clickThread then
            task.cancel(clickThread)
        end
        clickThread = task.spawn(function()
            repeat
                local shopId = bedwars.AppController:isAppOpen('BedwarsItemShopApp') and store.shopLoaded and getShopId()
                if shopId then
                    purchase(itemType, shopId)
                end
                task.wait(1 / CPS.Value)
            until not holding
            clickThread = nil
        end)
    end
    
    ShopAutoClicker = vape.Categories.Combat:CreateModule({
        Name = 'ShopAutoClicker',
        Tooltip = 'hold on a shop item to buy - idea from seven (cv)',
        Function = function(callback)
            if callback then
                ShopAutoClicker:Clean(inputService.InputBegan:Connect(function(input)
                    if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
                    if not bedwars.AppController:isAppOpen('BedwarsItemShopApp') then return end
    
                    local pos
                    if input.UserInputType == Enum.UserInputType.Touch then
                        pos = Vector2.new(input.Position.X, input.Position.Y)
                    end
                    local itemType = getHoveredItem(pos)
                    if not itemType then return end
    
                    holding = true
                    task.delay(0.1, function() 
                        if holding and getHoveredItem(pos) == itemType then
                            startClicking(itemType)
                        end
                    end)
                end))
    
                ShopAutoClicker:Clean(inputService.InputEnded:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        holding = false
                    end
                end))
            else
                holding = false
                if clickThread then
                    task.cancel(clickThread)
                    clickThread = nil
                end
            end
        end
    })
    CPS = ShopAutoClicker:CreateSlider({
        Name = 'CPS',
        Min = 1,
        Max = 20,
        Default = 20
    })
end)

run(function()
	local AimAssist
	local Targets
	local Sort
	local AimSpeed
	local Smoothness
	local SmoothnessToggle
	local Distance
	local AngleSlider
	local KillauraTarget
	local ClickAim
	local ShopCheck
	local AimPart
	local ViewMode
	local PriorityMode
	local ShakeToggle
	local ShakeAmount
	local WorkWithProjectiles
	local lockedTarget = nil
	local lastValidTarget = nil
	local lastValidTime = 0
	local GRACE_PERIOD = 0.15
	local rng = Random.new()
	local shakeTime = 0
	local shakeSeed = rng:NextNumber() * 1000

	local function getAimAlpha(speedVal, smoothVal, dt)
		local baseAlpha = math.clamp(speedVal * 0.025, 0.005, 0.98)
		if smoothVal then
			local smoothFactor = math.max(1 - ((smoothVal - 1) / 9) * 0.85, 0.05)
			baseAlpha = baseAlpha * smoothFactor
		end
		return 1 - (1 - baseAlpha) ^ (dt * 60)
	end

	local function getClosestPartToCursor(character)
		local mousePos = inputService.TouchEnabled and (gameCamera.ViewportSize / 2) or inputService:GetMouseLocation()
		local mouseRay = gameCamera:ViewportPointToRay(mousePos.X, mousePos.Y, 0)
		local bestAngle = math.huge
		local bestPart = nil
		local partNames = {
			'Head', 'UpperTorso', 'LowerTorso', 'HumanoidRootPart',
			'LeftUpperArm', 'RightUpperArm', 'LeftLowerArm', 'RightLowerArm',
			'LeftUpperLeg', 'RightUpperLeg', 'LeftLowerLeg', 'RightLowerLeg',
			'LeftFoot', 'RightFoot', 'LeftHand', 'RightHand'
		}
		for _, partName in partNames do
			local part = character:FindFirstChild(partName)
			if part then
				local dirToPart = (part.Position - mouseRay.Origin).Unit
				local angle = math.acos(math.clamp(mouseRay.Direction:Dot(dirToPart), -1, 1))
				if angle < bestAngle then
					bestAngle = angle
					bestPart = part
				end
			end
		end
		return bestPart
	end

	local function isEntValid(ent)
		if not ent or not ent.RootPart or not ent.Character or not ent.Character.Parent then return false end
		if not entitylib.isAlive or not entitylib.character or not entitylib.character.RootPart then return false end
		local hum = ent.Character:FindFirstChildOfClass('Humanoid')
		if not hum or hum.Health <= 0 then return false end
		local dist = (ent.RootPart.Position - entitylib.character.RootPart.Position).Magnitude
		if dist > Distance.Value then return false end
		if not isEnemy(ent) then return false end
		return true
	end

	local function isInAngle(ent)
		if not ent or not ent.RootPart then return false end
		if not entitylib.character or not entitylib.character.RootPart then return false end
		local delta = (ent.RootPart.Position - entitylib.character.RootPart.Position)
		local localFacing = ((getgenv().ViewMode and getgenv().ViewMode.Value == 'Third Person') and gameCamera.CFrame.LookVector or entitylib.character.RootPart.CFrame.LookVector) * Vector3.new(1, 0, 1)
		local flatDelta = delta * Vector3.new(1, 0, 1)
		if flatDelta.Magnitude <= 0.001 then return false end
		local angle = math.acos(math.clamp(localFacing:Dot(flatDelta.Unit), -1, 1))
		return angle < math.rad(AngleSlider.Value / 2)
	end

	AimAssist = vape.Categories.Combat:CreateModule({
		Name = 'AimAssist',
		Function = function(callback)
			if callback then
				lockedTarget = nil
				lastValidTarget = nil
				lastValidTime = 0
				shakeTime = 0
				local aimBindName = 'vapeAimAssist'
				AimAssist:Clean(function()
					pcall(function() runService:UnbindFromRenderStep(aimBindName) end)
				end)
				runService:BindToRenderStep(aimBindName, Enum.RenderPriority.Camera.Value + 1, (function(dt)

					if not entitylib.isAlive or not entitylib.character or not entitylib.character.RootPart then
						lockedTarget = nil
						return
					end

					local validWeapon = store.hand.toolType == 'sword'
					if WorkWithProjectiles and WorkWithProjectiles.Enabled then
						validWeapon = validWeapon or isHoldingBowCrossbow()
					end
					if not validWeapon then
						lockedTarget = nil
						return
					end

					if ClickAim and ClickAim.Enabled then
						local sc = bedwars.SwordController
						local lastAttack = sc and rawget(sc, 'lastAttack')
						if not lastAttack or (workspace:GetServerTimeNow() - lastAttack) >= 0.4 then
							return
						end
					end

					local inFirstPerson = isFirstPerson()
					if ViewMode.Value == 'First Person' and not inFirstPerson then return end
					if ViewMode.Value == 'Third Person' and inFirstPerson then return end

					if ShopCheck and ShopCheck.Enabled then
						if isGUIOpen() then
							lockedTarget = nil
							return
						end
					end

					local ent = nil

					if KillauraTarget and KillauraTarget.Enabled then
						local ka = store.KillauraTarget
						if ka and ka.RootPart and ka.Character and ka.Character.Parent then
							local hum = ka.Character:FindFirstChildOfClass('Humanoid')
							local dist = ka.RootPart and entitylib.character and entitylib.character.RootPart and (ka.RootPart.Position - entitylib.character.RootPart.Position).Magnitude
							if hum and hum.Health > 0 and dist and dist <= Distance.Value then
								ent = ka
							end
						end
					else
						if PriorityMode and PriorityMode.Enabled and lockedTarget then
							if isEntValid(lockedTarget) and isInAngle(lockedTarget) then
								ent = lockedTarget
							else
								lockedTarget = nil
							end
						end

						if not ent then
							local found = entitylib.EntityPosition({
								Range = Distance.Value,
								Part = 'RootPart',
								Wallcheck = Targets.Walls.Enabled,
								Players = Targets.Players.Enabled,
								NPCs = Targets.NPCs.Enabled,
								Sort = sortmethods[Sort.Value]
							})

							if found then
								lastValidTarget = found
								lastValidTime = tick()
								ent = found
							elseif lastValidTarget and (tick() - lastValidTime) < GRACE_PERIOD then
								local hum = lastValidTarget.Character and lastValidTarget.Character:FindFirstChildOfClass('Humanoid')
								if hum and hum.Health > 0 and lastValidTarget.Character.Parent and isInAngle(lastValidTarget) then
									ent = lastValidTarget
								else
									lastValidTarget = nil
								end
							end

							if ent and PriorityMode and PriorityMode.Enabled then
								lockedTarget = ent
							end
						end
					end	

					if not ent then return end

					if not (KillauraTarget and KillauraTarget.Enabled) then
						if not isEntValid(ent) then
							if PriorityMode and PriorityMode.Enabled then lockedTarget = nil end
							lastValidTarget = nil
							return
						end
						if not isInAngle(ent) then
							if PriorityMode and PriorityMode.Enabled then lockedTarget = nil end
							return
						end
					end

					targetinfo.Targets[ent] = tick() + 1

					local aimPosition
					if AimPart.Value == 'Head' then
						local head = ent.Character and ent.Character:FindFirstChild('Head')
						aimPosition = head and head.Position or ent.RootPart.Position
					elseif AimPart.Value == 'Torso' then
						local torso = ent.Character and (ent.Character:FindFirstChild('UpperTorso') or ent.Character:FindFirstChild('Torso'))
						aimPosition = torso and torso.Position or ent.RootPart.Position
					elseif AimPart.Value == 'Closest' then
						local closest = ent.Character and getClosestPartToCursor(ent.Character)
						aimPosition = closest and closest.Position or ent.RootPart.Position
					else
						aimPosition = ent.RootPart.Position
					end

					if ShakeToggle and ShakeToggle.Enabled and ShakeAmount.Value > 0 then
						shakeTime = shakeTime + dt
						local camCF = gameCamera.CFrame
						local dist = (aimPosition - camCF.Position).Magnitude
						local intensity = ShakeAmount.Value * 0.0035 * math.max(dist, 1)
						local driftX = math.noise(shakeTime * 0.8, 0, shakeSeed) * 2
						local driftY = math.noise(0, shakeTime * 0.7, shakeSeed) * 2
						local microX = math.noise(shakeTime * 9.5, shakeSeed, 0) * 2
						local microY = math.noise(shakeSeed, shakeTime * 10.7, 0) * 2
						local sx = (driftX * 0.7 + microX * 0.3) * intensity
						local sy = (driftY * 0.55 + microY * 0.25) * intensity
						aimPosition = aimPosition + camCF.RightVector * sx + camCF.UpVector * sy
					end

					local targetCFrame = CFrame.lookAt(gameCamera.CFrame.p, aimPosition)
					local smoothVal = (SmoothnessToggle and SmoothnessToggle.Enabled and Smoothness) and Smoothness.Value or nil
					local alpha = getAimAlpha(AimSpeed.Value, smoothVal, dt)
					gameCamera.CFrame = gameCamera.CFrame:Lerp(targetCFrame, alpha)
				end))
			else
				lockedTarget = nil
				lastValidTarget = nil
				shakeTime = 0
			end
		end,
		Tooltip = 'aa with smooth tracking'
	})

	Targets = AimAssist:CreateTargets({
		Players = true,
		Walls = true
	})

	Sort = AimAssist:CreateDropdown({
		Name = 'Target Mode',
		List = getSortList({'Damage', 'Distance'}),
		Tooltip = 'prioritize targets'
	})

	AimPart = AimAssist:CreateDropdown({
		Name = 'Aim Part',
		List = {'Torso', 'Head', 'Closest'},
		Default = 'Torso'
	})

	ViewMode = AimAssist:CreateDropdown({
		Name = 'View Mode',
		List = {'First Person', 'Third Person', 'Both'},
		Default = 'Both',
		Tooltip = 'either only aim in first person or third person, or both'
	})
	getgenv().ViewMode = ViewMode

	AimSpeed = AimAssist:CreateSlider({
		Name = 'Aim Speed',
		Min = 1,
		Max = 20,
		Default = 6,
		Tooltip = 'how fast aim assist moves toward the target'
	})

	Distance = AimAssist:CreateSlider({
		Name = 'Distance',
		Min = 1,
		Max = 30,
		Default = 25,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})

	AngleSlider = AimAssist:CreateSlider({
		Name = 'Max Angle',
		Min = 1,
		Max = 360,
		Default = 60,
	})

	SmoothnessToggle = AimAssist:CreateToggle({
		Name = 'Smoothness',
		Default = false,
		Tooltip = 'makes aim assist feels more legit',
		Function = function(callback)
			if Smoothness then Smoothness.Object.Visible = callback end
		end
	})

	Smoothness = AimAssist:CreateSlider({
		Name = 'Smoothness Amount',
		Min = 1,
		Max = 10,
		Default = 5,
		Visible = false
	})

	PriorityMode = AimAssist:CreateToggle({
		Name = 'Priority Mode',
		Default = false,
		Tooltip = 'stays on one target until target is lost'
	})

	ClickAim = AimAssist:CreateToggle({
		Name = 'Click Aim',
		Default = true,
		Tooltip = 'only aims when clicking'
	})

	KillauraTarget = AimAssist:CreateToggle({
		Name = 'Use Killaura Target',
	})

	ShakeToggle = AimAssist:CreateToggle({
		Name = 'Shake',
		Default = false,
		Tooltip = 'shakes your screen for more legitness',
		Function = function(callback)
			if ShakeAmount then ShakeAmount.Object.Visible = callback end
		end
	})

	ShakeAmount = AimAssist:CreateSlider({
		Name = 'Shake Amount',
		Min = 1,
		Max = 10,
		Default = 3,
		Visible = false
	})

	ShopCheck = AimAssist:CreateToggle({
		Name = 'Shop Check',
		Default = false,
	})

	WorkWithProjectiles = AimAssist:CreateToggle({
		Name = 'Work With Projectiles',
		Default = false,
	})

	task.defer(function()
		if Smoothness and Smoothness.Object then
			Smoothness.Object.Visible = SmoothnessToggle and SmoothnessToggle.Enabled or false
		end
		if ShakeAmount and ShakeAmount.Object then
			ShakeAmount.Object.Visible = false
		end
	end)
end)

run(function()
	local KnockbackDisplace
	local KBDirection
	local originalApplyKnockback
	local hookedUtil
	local installedHook
	local hooked = false

	local _cachedKnockbackUtil = nil
	local function findKnockbackUtil()
		if _cachedKnockbackUtil and rawget(_cachedKnockbackUtil, 'applyKnockback') then
			return _cachedKnockbackUtil
		end
		for _, v in getgc(true) do
			if type(v) == 'table' and rawget(v, 'applyKnockback') and rawget(v, 'getDirection') and rawget(v, 'applyKnockbackDirection') then
				_cachedKnockbackUtil = v
				return v
			end
		end
	end

	local function unhookKnockback()
		if hookedUtil and originalApplyKnockback then
			pcall(function()
				if rawget(hookedUtil, 'applyKnockback') == installedHook then
					hookedUtil.applyKnockback = originalApplyKnockback
				end
			end)
		end
		hookedUtil = nil
		installedHook = nil
		originalApplyKnockback = nil
		hooked = false
	end

	KnockbackDisplace = vape.Categories.Combat:CreateModule({
		Name = 'KnockbackDisplace',
		Function = function(callback)
			if callback then
				if hooked then return end
				local util = findKnockbackUtil()
				if not util then
					notif('KnockbackDisplace', 'Failed to hook knockback!', 3)
					KnockbackDisplace:Toggle()
					return
				end
				originalApplyKnockback = util.applyKnockback
				local capturedOriginal = originalApplyKnockback
				local hookFn = function(hrp, mass, sourcePos, modifier)
					if not capturedOriginal then return end
					if not KnockbackDisplace.Enabled then
						return capturedOriginal(hrp, mass, sourcePos, modifier)
					end
					local dir = KBDirection.Value
					if dir == 'Default' then
						return capturedOriginal(hrp, mass, sourcePos, modifier)
					elseif dir == 'Void' then
						hrp:ApplyImpulse(Vector3.new(0, -mass * 60, 0))
						return
					elseif dir == 'Up' then
						hrp:ApplyImpulse(Vector3.new(0, mass * 120, 0))
						return
					else
						local hrpPos = hrp.Position
						local cf = hrp.CFrame
						local fakeSource
						if dir == 'Left' then
							fakeSource = hrpPos + cf.RightVector * 10
						elseif dir == 'Right' then
							fakeSource = hrpPos - cf.RightVector * 10
						elseif dir == 'Reverse' then
							if sourcePos then
								fakeSource = Vector3.new(2 * hrpPos.X - sourcePos.X, sourcePos.Y, 2 * hrpPos.Z - sourcePos.Z)
							end
						end
						return capturedOriginal(hrp, mass, fakeSource or sourcePos, modifier)
					end
				end
				util.applyKnockback = hookFn
				hookedUtil = util
				installedHook = hookFn
				hooked = true
			else
				unhookKnockback()
			end
		end,
		Tooltip = 'changes your knockback direction'
	})

	vape:Clean(unhookKnockback)

	KBDirection = KnockbackDisplace:CreateDropdown({
		Name = 'KBDirection',
		List = {'Default', 'Backwards', 'Up', 'Left', 'Right', 'Reverse'},
		Default = 'Default',
		Tooltip = 'Choose which direction to redirect your knockback'
	})
end)

run(function()
	local Viewmodel
	local Depth
	local Horizontal
	local Vertical
	local NoBob
	local Rots = {}
	local old, oldc1
	
	Viewmodel = vape.Categories.Combat:CreateModule({
		Name = 'Viewmodel',
		Function = function(callback)
			local viewmodel = gameCamera:FindFirstChild('Viewmodel')
			if callback then
				old = bedwars.ViewmodelController.playAnimation
				oldc1 = viewmodel and viewmodel.RightHand.RightWrist.C1 or CFrame.identity
				if NoBob.Enabled then
					bedwars.ViewmodelController.playAnimation = function(self, animtype, ...)
						if bedwars.AnimationType and animtype == bedwars.AnimationType.FP_WALK then return end
						return old(self, animtype, ...)
					end
				end
	
				bedwars.InventoryViewmodelController:handleStore(bedwars.Store:getState())
				if viewmodel then
					gameCamera.Viewmodel.RightHand.RightWrist.C1 = oldc1 * CFrame.Angles(math.rad(Rots[1].Value), math.rad(Rots[2].Value), math.rad(Rots[3].Value))
				end
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_DEPTH_OFFSET', -Depth.Value)
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_HORIZONTAL_OFFSET', Horizontal.Value)
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_VERTICAL_OFFSET', Vertical.Value)
			else
				bedwars.ViewmodelController.playAnimation = old
				if viewmodel then
					viewmodel.RightHand.RightWrist.C1 = oldc1
				end
	
				bedwars.InventoryViewmodelController:handleStore(bedwars.Store:getState())
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_DEPTH_OFFSET', 0)
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_HORIZONTAL_OFFSET', 0)
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_VERTICAL_OFFSET', 0)
				old = nil
			end
		end,
		Tooltip = 'change viewmodel animations'
	})
	Depth = Viewmodel:CreateSlider({
		Name = 'Depth',
		Min = 0,
		Max = 2,
		Default = 0.8,
		Decimal = 10,
		Function = function(val)
			if Viewmodel.Enabled then
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_DEPTH_OFFSET', -val)
			end
		end
	})
	Horizontal = Viewmodel:CreateSlider({
		Name = 'Horizontal',
		Min = 0,
		Max = 2,
		Default = 0.8,
		Decimal = 10,
		Function = function(val)
			if Viewmodel.Enabled then
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_HORIZONTAL_OFFSET', val)
			end
		end
	})
	Vertical = Viewmodel:CreateSlider({
		Name = 'Vertical',
		Min = -0.2,
		Max = 2,
		Default = -0.2,
		Decimal = 10,
		Function = function(val)
			if Viewmodel.Enabled then
				lplr.PlayerScripts.TS.controllers.global.viewmodel['viewmodel-controller']:SetAttribute('ConstantManager_VERTICAL_OFFSET', val)
			end
		end
	})
	for _, name in {'Rotation X', 'Rotation Y', 'Rotation Z'} do
		table.insert(Rots, Viewmodel:CreateSlider({
			Name = name,
			Min = 0,
			Max = 360,
			Function = function(val)
				if Viewmodel.Enabled then
					gameCamera.Viewmodel.RightHand.RightWrist.C1 = oldc1 * CFrame.Angles(math.rad(Rots[1].Value), math.rad(Rots[2].Value), math.rad(Rots[3].Value))
				end
			end
		}))
	end
	NoBob = Viewmodel:CreateToggle({
		Name = 'No Bobbing',
		Default = true,
		Function = function()
			if Viewmodel.Enabled then
				Viewmodel:Toggle()
				Viewmodel:Toggle()
			end
		end
	})
end)

run(function()
    if isMobile then
        local AutoClicker
        local CPS
        local BlockCPS = {}
        local Thread

        local function getSafeCPS()
            if store.hand and store.hand.toolType == 'block' and BlockCPS and BlockCPS.GetRandomValue then
                return BlockCPS
            end
            if CPS and CPS.GetRandomValue then
                return CPS
            end
            return nil
        end

        local function AutoClick()
            if Thread then
                task.cancel(Thread)
                Thread = nil
            end

            local initialCPS = getSafeCPS()
            if not initialCPS then return end

            Thread = task.delay(1 / initialCPS.GetRandomValue(), function()
                repeat
                    if not bedwars.AppController:isLayerOpen(bedwars.UILayers.MAIN) then
                        local blockPlacer = bedwars.BlockPlacementController and bedwars.BlockPlacementController.blockPlacer
                        local toolType = store.hand and store.hand.toolType

                        if toolType == 'block' and blockPlacer then
                            task.spawn(function()
                                blockPlacer:autoBridge(workspace:GetServerTimeNow() - bedwars.KnockbackController:getLastKnockbackTime() >= 0.2)
                            end)
                        elseif toolType == 'sword' then
                            bedwars.SwordController:swingSwordAtMouse(0.39)
                        end
                    end

                    local currentCPS = getSafeCPS()
                    if not currentCPS then
                        task.wait(0.1)
                    else
                        task.wait(1 / currentCPS.GetRandomValue())
                    end
                until not AutoClicker.Enabled
            end)
        end

        local function StopClick()
            if Thread then
                task.cancel(Thread)
                Thread = nil
            end
        end

        AutoClicker = vape.Categories.Combat:CreateModule({
            Name = 'AutoClicker',
            Function = function(callback)
                if callback then
                    AutoClicker:Clean(inputService.InputBegan:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then
                            AutoClick()
                        end
                    end))

                    AutoClicker:Clean(inputService.InputEnded:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then
                            StopClick()
                        end
                    end))

                    for _, v in {'2', '5'} do
                        pcall(function()
                            AutoClicker:Clean(lplr.PlayerGui.MobileUI[v].MouseButton1Down:Connect(AutoClick))
                            AutoClicker:Clean(lplr.PlayerGui.MobileUI[v].MouseButton1Up:Connect(StopClick))
                        end)
                    end
                else
                    StopClick()
                end
            end,
            Tooltip = 'clicks for you'
        })

        CPS = AutoClicker:CreateTwoSlider({
            Name = 'CPS',
            Min = 1,
            Max = 9,
            DefaultMin = 7,
            DefaultMax = 7
        })

        AutoClicker:CreateToggle({
            Name = 'Place Blocks',
            Default = true,
            Function = function(callback)
                if BlockCPS.Object then
                    BlockCPS.Object.Visible = callback
                end
            end
        })

        BlockCPS = AutoClicker:CreateTwoSlider({
            Name = 'Block CPS',
            Min = 1,
            Max = 20,
            DefaultMin = 12,
            DefaultMax = 12,
            Darker = true
        })

        task.defer(function()
            if BlockCPS and BlockCPS.Object then
                BlockCPS.Object.Visible = PlaceBlocksToggle and PlaceBlocksToggle.Enabled
            end
        end)

    else
        local AutoClicker
        local CPS
        local BlockCPS = {}
        local SwordCPS = {}
        local PlaceBlocksToggle
        local SwingSwordToggle
        local Thread

        local task_wait = task.wait
        local task_spawn = task.spawn
        local workspace_GetServerTimeNow = function() return workspace:GetServerTimeNow() end

        local function getSafeCPS()
            local toolType = store.hand and store.hand.toolType or nil
            if toolType == 'block' and PlaceBlocksToggle and PlaceBlocksToggle.Enabled and BlockCPS and BlockCPS.GetRandomValue then
                return BlockCPS
            elseif toolType == 'sword' and SwingSwordToggle and SwingSwordToggle.Enabled and SwordCPS and SwordCPS.GetRandomValue then
                return SwordCPS
            elseif CPS and CPS.GetRandomValue then
                return CPS
            end
            return nil
        end

        local function AutoClickskid()
            if Thread then task.cancel(Thread) end
            Thread = task_spawn(function()
                repeat
                    if not bedwars.AppController:isLayerOpen(bedwars.UILayers.MAIN) then
                        local toolType = store.hand and store.hand.toolType
                        if PlaceBlocksToggle.Enabled and toolType == 'block' then
                            local blockPlacer = bedwars.BlockPlacementController and bedwars.BlockPlacementController.blockPlacer
                            if blockPlacer then
                                if (workspace_GetServerTimeNow() - bedwars.BlockCpsController.lastPlaceTimestamp) >= ((1 / 12) * 0.5) then
                                    local mouseinfo = blockPlacer.clientManager:getBlockSelector():getMouseInfo(0)
                                    if mouseinfo and mouseinfo.placementPosition == mouseinfo.placementPosition then
                                        task_spawn(blockPlacer.placeBlock, blockPlacer, mouseinfo.placementPosition)
                                    end
                                end
                            end
                        elseif SwingSwordToggle.Enabled and toolType == 'sword' then
                            bedwars.SwordController:swingSwordAtMouse(0.39)
                        end
                    end

                    local currentCPS = getSafeCPS()
                    task_wait(1 / (currentCPS and currentCPS.GetRandomValue() or 7))
                until not AutoClicker.Enabled
            end)
        end

        local function StopAutoClick()
            if Thread then
                task.cancel(Thread)
                Thread = nil
            end
        end

        local MIN_HOLD_TIME = 0.12
        local ActivationScheduled = nil

        AutoClicker = vape.Categories.Combat:CreateModule({
            Name = 'AutoClicker',
            Function = function(callback)
                if callback then
                    AutoClicker:Clean(inputService.InputBegan:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then
                            ActivationScheduled = task.delay(MIN_HOLD_TIME, function()
                                ActivationScheduled = nil
                                if inputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
                                    AutoClickskid()
                                end
                            end)
                        end
                    end))
                    AutoClicker:Clean(inputService.InputEnded:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then
                            if ActivationScheduled then
                                task.cancel(ActivationScheduled)
                                ActivationScheduled = nil
                            end
                            if Thread then
                                task.cancel(Thread)
                                Thread = nil
                            end
                        end
                    end))
                else
                    StopAutoClick()
                end
            end,
            Tooltip = 'clicks for you'
        })

        PlaceBlocksToggle = AutoClicker:CreateToggle({
            Name = 'Place Blocks',
            Default = false,
            Function = function(callback)
                task.defer(function()
                    if BlockCPS and BlockCPS.Object then BlockCPS.Object.Visible = callback end
                end)
            end
        })

        BlockCPS = AutoClicker:CreateTwoSlider({
            Name = 'Block CPS',
            Min = 1,
            Max = 20,
            DefaultMin = 12,
            DefaultMax = 12,
            Darker = true
        })

        SwingSwordToggle = AutoClicker:CreateToggle({
            Name = 'Swing Sword',
            Default = false,
            Function = function(callback)
                if SwordCPS.Object then SwordCPS.Object.Visible = callback end
            end
        })

        SwordCPS = AutoClicker:CreateTwoSlider({
            Name = 'Sword CPS',
            Min = 1,
            Max = 9,
            DefaultMin = 7,
            DefaultMax = 7,
            Darker = true
        })

        task.defer(function()
            if BlockCPS and BlockCPS.Object then
                BlockCPS.Object.Visible = PlaceBlocksToggle and PlaceBlocksToggle.Enabled
            end
            if SwordCPS and SwordCPS.Object then
                SwordCPS.Object.Visible = SwingSwordToggle and SwingSwordToggle.Enabled
            end
        end)
    end
end)  
	
run(function()
	local Attack
	local Mine
	local Place
	local oldAttackReach, oldMineReach, oldPlaceReach
	local SwordReach, MineReach
	local AirReach, AirReachChance
	local airReachState = false
	local airReachRolled = false
	local _airReachThread

	Reach = vape.Categories.Combat:CreateModule({
		Name = 'Reach',
		Function = function(callback)
			if callback then
				if SwordReach and SwordReach.Enabled then
					task.spawn(function()
						repeat task.wait(0.1) until (bedwars.CombatConstant and bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE) or not Reach.Enabled
						if not Reach.Enabled or not SwordReach.Enabled then return end
						oldAttackReach = bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE
						bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = Attack.Value + 2
					end)
				end
				
				task.spawn(function()
					repeat task.wait(0.1) until bedwars.BlockBreakController or not Reach.Enabled
					if not Reach.Enabled or not MineReach or not MineReach.Enabled then return end
					
					pcall(function()
						local blockBreaker = bedwars.BlockBreakController:getBlockBreaker()
						if blockBreaker then
							oldMineReach = oldMineReach or blockBreaker:getRange()
							blockBreaker:setRange(Mine.Value)
						end
					end)
				end)
				
				local _reachLoopThread = task.spawn(function()
					while Reach.Enabled do
						task.wait(5)
						if not Reach.Enabled then break end
						if SwordReach.Enabled and not (AirReach and AirReach.Enabled) and bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE ~= Attack.Value + 2 then
							bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = Attack.Value + 2
						end
						if MineReach.Enabled then
							pcall(function()
								local blockBreaker = bedwars.BlockBreakController:getBlockBreaker()
								if blockBreaker and blockBreaker:getRange() ~= Mine.Value then
									blockBreaker:setRange(Mine.Value)
								end
							end)
						end
					end
				end)
				airReachRolled = false
				_airReachThread = task.spawn(function()
					while Reach.Enabled do
						task.wait(0.05)
						if not Reach.Enabled then break end
						if not AirReach or not AirReach.Enabled or not SwordReach.Enabled then
							airReachRolled = false
						else
							local ent = store.KillauraTarget
							local tchar = ent and ent.Character
							local hum = tchar and tchar:FindFirstChildOfClass('Humanoid')
							local inAir = false
							if hum then
								inAir = hum.FloorMaterial == Enum.Material.Air
								if not inAir then
									local st = hum:GetState()
									inAir = st == Enum.HumanoidStateType.Jumping or st == Enum.HumanoidStateType.Freefall
								end
								if not inAir then
									local root = tchar.PrimaryPart
									local vy = root and root.AssemblyLinearVelocity.Y or 0
									inAir = math.abs(vy) > 3
								end
							end
							if not inAir then
								airReachRolled = false
								bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = Attack.Value + 2
							else
								if not airReachRolled then
									airReachRolled = true
									airReachState = math.random(1, 100) <= AirReachChance.Value
								end
								bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = airReachState and (Attack.Value + 2) or (oldAttackReach or 14.4)
							end
						end
					end
				end)
				Reach:Clean(function()
					if _airReachThread then
						pcall(task.cancel, _airReachThread)
						_airReachThread = nil
					end
					airReachRolled = false
				end)
				Reach:Clean(function()
					if _reachLoopThread then
						pcall(task.cancel, _reachLoopThread)
						_reachLoopThread = nil
					end
				end)
			else
				if oldAttackReach then
					bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = oldAttackReach
				end
				
				if oldMineReach then
					pcall(function()
						local blockBreaker = bedwars.BlockBreakController:getBlockBreaker()
						if blockBreaker then
							blockBreaker:setRange(oldMineReach)
						end
					end)
				end

				oldAttackReach, oldMineReach = nil, nil
			end
		end,
		Tooltip = 'increases reach for attacking, mining'
	})
	
	SwordReach = Reach:CreateToggle({
		Name = 'Sword Reach',
		Default = true,
		Function = function(v)
			if Attack then Attack.Object.Visible = v end
			if Reach.Enabled then
				if v then
					bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = Attack.Value + 2
				else
					bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = oldAttackReach or 14.4
				end
			end
		end
	})

	Attack = Reach:CreateSlider({
		Name = 'Attack Range',
		Darker = true,
		Visible = true,
		Min = 0,
		Max = 20,
		Default = 18,
		Function = function(val)
			if Reach.Enabled then
				bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = val + 2
			end
		end,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	
	AirReach = Reach:CreateToggle({
		Name = 'Air Reach',
		Default = false,
		Tooltip = 'puts ur reach back to normal when they in the air so it looks legit',
		Function = function(v)
			if AirReachChance then AirReachChance.Object.Visible = v end
			if not v and Reach.Enabled and SwordReach and SwordReach.Enabled then
				bedwars.CombatConstant.RAYCAST_SWORD_CHARACTER_DISTANCE = Attack.Value + 2
			end
		end
	})

	AirReachChance = Reach:CreateSlider({
		Name = 'Air Reach Chance',
		Min = 0,
		Max = 100,
		Default = 50,
		Suffix = '%',
		Darker = true,
		Visible = false,
		Tooltip = 'how often u still get reach while they jumpin'
	})

	MineReach = Reach:CreateToggle({
		Name = 'Mine Reach',
		Default = false,
		Function = function(v)
			if Mine then Mine.Object.Visible = v end
		end
	})

	Mine = Reach:CreateSlider({
		Name = 'Mine Range',
		Darker = true,
		Visible = false,
		Min = 0,
		Max = 30,
		Default = 18,
		Function = function(val)
			if Reach.Enabled then
				pcall(function()
					local blockBreaker = bedwars.BlockBreakController:getBlockBreaker()
					if blockBreaker then
						blockBreaker:setRange(val)
					end
				end)
			end
		end,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
end)

run(function()
	local Sprint
	local old
	
	Sprint = vape.Categories.Combat:CreateModule({
		Name = 'Sprint',
		Function = function(callback)
			if callback then
				if inputService.TouchEnabled then 
					pcall(function() 
						lplr.PlayerGui.MobileUI['4'].Visible = false 
					end) 
				end
				old = bedwars.SprintController.stopSprinting
				bedwars.SprintController.stopSprinting = function(...)
					local call = old(...)
					bedwars.SprintController:startSprinting()
					return call
				end
				Sprint:Clean(entitylib.Events.LocalAdded:Connect(function() 
					task.delay(0.1, function() 
						bedwars.SprintController:stopSprinting() 
					end) 
				end))
				bedwars.SprintController:stopSprinting()
			else
				if inputService.TouchEnabled then 
					pcall(function() 
						lplr.PlayerGui.MobileUI['4'].Visible = true 
					end) 
				end
				bedwars.SprintController.stopSprinting = old
				bedwars.SprintController:stopSprinting()
			end
		end,
	})
end)
	
run(function()
	local TriggerBot
	local CPS
	local ProjectileMode
	local ProjectileFireRate
	local ProjectileWaitDelay
	local ProjectileFirstPerson
	local rayParams = cloneRaycast()
	local lastProjectileShot = 0
	local wasHoldingProjectile = false
	local VirtualInputManager = game:GetService("VirtualInputManager")
	local tick = tick
	local task_wait = task.wait
	local pcall = pcall
	local lastClickTime = 0
	local clickCooldown = 0.015
	
	local function leftClick()
		local now = tick()
		if now - lastClickTime < clickCooldown then
			return false
		end
		
		local success = pcall(function()
			VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 0)
			task_wait(0.02)
			VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 0)
		end)
		
		if success then
			lastClickTime = now
		end
		return success
	end
	
	local lastProjectileCheck = 0
	local cachedProjectileResult = false
	local lastHotbarSlot = -1
	local cachedSwordRange = nil
	local lastSwordTool = nil
	
	TriggerBot = vape.Categories.Combat:CreateModule({
		Name = 'TriggerBot',
		Function = function(callback)
			if callback then
				local frameCounter = 0
				local lastToolType = nil
				
				repeat
					frameCounter = frameCounter + 1
					local doAttack = false
					local holdingProjectile = isHoldingProjectile()
					
					if not bedwars.AppController:isLayerOpen(bedwars.UILayers.MAIN) and entitylib.isAlive then
						if ProjectileMode.Enabled and holdingProjectile then
							if ProjectileFirstPerson.Enabled and not isFirstPerson() then
								wasHoldingProjectile = false
							else
								if holdingProjectile and not wasHoldingProjectile then
									task_wait(ProjectileWaitDelay.Value)
									leftClick()
									lastProjectileShot = tick()
									wasHoldingProjectile = true
								elseif holdingProjectile then
									local currentTime = tick()
									if (currentTime - lastProjectileShot) >= ProjectileFireRate.Value then
										leftClick()
										lastProjectileShot = currentTime
									end
								else
									wasHoldingProjectile = false
								end
							end
						elseif store.hand.toolType == 'sword' and bedwars.DaoController.chargingMaid == nil then
							local currentTool = store.hand.tool
							if currentTool ~= lastSwordTool then
								lastSwordTool = currentTool
								local itemMeta = bedwars.ItemMeta[currentTool.Name]
								cachedSwordRange = itemMeta and itemMeta.sword and itemMeta.sword.attackRange or 14.4
							end
							
							local attackRange = cachedSwordRange or 14.4
							
							if frameCounter % 2 == 0 then
								rayParams.FilterDescendantsInstances = {lplr.Character}
								
								local unit = lplr:GetMouse().UnitRay
								local localPos = entitylib.character.RootPart.Position
								local rayRange = attackRange
								local ray = bedwars.QueryUtil:raycast(unit.Origin, unit.Direction * 200, rayParams)
								
								if ray and (localPos - ray.Instance.Position).Magnitude <= rayRange then
									local entityList = entitylib.List
									for i = 1, #entityList do
										local ent = entityList[i]
										doAttack = ent.Targetable and ray.Instance:IsDescendantOf(ent.Character) and (localPos - ent.RootPart.Position).Magnitude <= rayRange
										if doAttack then
											break
										end
									end
								end
							end
							
							if not doAttack then
								doAttack = bedwars.SwordController:getTargetInRegion(attackRange or 3.8 * 3, 0)
							end
							
							if doAttack then
								bedwars.SwordController:swingSwordAtMouse()
							end
						else
							wasHoldingProjectile = false
						end
					end
					
					if doAttack and not holdingProjectile then
						task_wait(1 / CPS.GetRandomValue())
					else
						task_wait(holdingProjectile and 0.033 or 0.05)
					end
				until not TriggerBot.Enabled
			else
				cachedSwordRange = nil
				lastSwordTool = nil
				lastHotbarSlot = -1
				wasHoldingProjectile = false
			end
		end,
		Tooltip = 'Automatically swings when hovering over a entity'
	})
	
	CPS = TriggerBot:CreateTwoSlider({
		Name = 'CPS',
		Min = 1,
		Max = 9,
		DefaultMin = 7,
		DefaultMax = 7
	})
	
	ProjectileMode = TriggerBot:CreateToggle({
		Name = 'Projectile Mode',
		Tooltip = 'Auto-shoots crossbow/bow when holding projectile weapon'
	})
	
	ProjectileFireRate = TriggerBot:CreateSlider({
		Name = 'Projectile Fire Rate',
		Min = 0.1,
		Max = 3,
		Default = 1.2,
		Decimal = 10,
		Suffix = function(val)
			return val == 1 and 'second' or 'seconds'
		end,
		Tooltip = 'How fast to auto-fire (1.2 = every 1.2 seconds)',
		Visible = function()
			return ProjectileMode.Enabled
		end
	})
	
	ProjectileWaitDelay = TriggerBot:CreateSlider({
		Name = 'Projectile Wait Delay',
		Min = 0,
		Max = 1,
		Default = 0,
		Decimal = 100,
		Suffix = 's',
		Tooltip = 'Delay before shooting (helps prevent ghosting)',
		Visible = function()
			return ProjectileMode.Enabled
		end
	})
	
	ProjectileFirstPerson = TriggerBot:CreateToggle({
		Name = 'Projectile First Person Only',
		Default = false,
		Tooltip = 'Only works in first person mode',
		Visible = function()
			return ProjectileMode.Enabled
		end
	})
end)

run(function()
	local Velocity
	local Vertical
	local VerticalChance
	local Horizontal
	local HorizontalChance
	local Mode
	local DelayGround
	local DelayAir
	local Targetting
	local Chance
	local old = nil
	local rand = Random.new()

	Velocity = vape.Categories.Combat:CreateModule({
		Name = 'Velocity',
		Tooltip = 'control you to edit ur knockback',
		Function = function(callback)
			if callback then
				old = bedwars.KnockbackUtil.applyKnockback
				Velocity:Clean(vapeEvents.TakeKnockback.Event:Connect(function(root, mass, dir, knockback, ...)
					local args = {...}
					local clone = table.clone(knockback)

					local air, ground = false, false
					task.delay(DelayAir.Value / 1000, function()
						clone.horizontal = knockback.horizontal or 1
						air = true
					end)
					task.delay(DelayGround.Value / 1000, function()
						clone.vertical = knockback.vertical or 1
						ground = true
					end)
					repeat task.wait(0.1) until air
					repeat task.wait(0.05) until ground
					old(root, mass, dir, clone, unpack(args))
				end))

				bedwars.KnockbackUtil.applyKnockback = function(root, mass, dir, knockback, ...)
					local chance = rand:NextNumber(0, 100)
					chance = math.floor(chance)
					if Mode.Value == 'Default' then
						if chance >= Chance.Value then return old(root, mass, dir, knockback, ...) end
					end
						
					local check = (not Targetting.Enabled) or entitylib.EntityPosition({
						Range = 20,
						Part = 'RootPart',
						Players = true
					})
		
					if check then
						knockback = knockback or {}
						if Mode.Value == 'Lag' then
							if chance < Chance.Value then
								return vapeEvents.TakeKnockback:Fire(root, mass, dir, knockback, ...)
							end
						else
							local horiz = (knockback.horizontal or 1) * (Horizontal.Value / 100)
							local vert = (knockback.vertical or 1) * (Vertical.Value / 100)
							chance = rand:NextNumber(0, 100)
							chance = math.floor(chance)
							if Horizontal.Value == 0 and Vertical.Value == 0 and HorizontalChance.Value == 100 and VerticalChance.Value == 100 then return end
							local horizChance = math.floor(rand:NextNumber(0, 100))
							local vertChance = math.floor(rand:NextNumber(0, 100))
							if horizChance >= HorizontalChance.Value then
								horiz = knockback.horizontal
							end
							if vertChance >= VerticalChance.Value then
								vert = knockback.vertical
							end
							knockback.horizontal = horiz
							knockback.vertical = vert
						end
					end
						
					return old(root, mass, dir, knockback, ...)
				end
			else
				bedwars.KnockbackUtil.applyKnockback = old
				old = nil
			end
		end
	})
	Mode = Velocity:CreateDropdown({
		Name = "Mode",
		List = {'Lag','Default'},
		Function = function(val)
			if val == 'Default' then
				if HorizontalChance then HorizontalChance.Object.Visible = true end
				if Horizontal then Horizontal.Object.Visible = true end
				if VerticalChance then VerticalChance.Object.Visible = true end
				if Vertical then Vertical.Object.Visible = true end
				if DelayGround then DelayGround.Object.Visible = false end
				if DelayAir then DelayAir.Object.Visible = false end
			elseif val == 'Lag' then
				if HorizontalChance then HorizontalChance.Object.Visible = false end
				if Horizontal then Horizontal.Object.Visible = false end
				if VerticalChance then VerticalChance.Object.Visible = false end
				if Vertical then Vertical.Object.Visible = false end
				if DelayGround then DelayGround.Object.Visible = true end
				if DelayAir then DelayAir.Object.Visible = true end
			else
				if HorizontalChance then HorizontalChance.Object.Visible = false end
				if Horizontal then Horizontal.Object.Visible = false end
				if VerticalChance then VerticalChance.Object.Visible = false end
				if Vertical then Vertical.Object.Visible = false end
				if DelayGround then DelayGround.Object.Visible = false end
				if DelayAir then DelayAir.Object.Visible = false end
				vape:CreateNotification('Velocity',`Storing packets... returned {val or Mode.Value} report ASAP`,16)
			end
		end
	})
	Vertical = Velocity:CreateSlider({
		Name = "Vertical",
		Min = 0,
		Max = 100,
		Default = 100,
		Decimal = 1,
		Visible = (Mode.Value == 'Default')
	})
	VerticalChance = Velocity:CreateSlider({
		Name = "Vertical Chance",
		Min = 0,
		Max = 100,
		Default = 100,
		Decimal = 1,
		Suffix = '%',
		Visible = (Mode.Value == 'Default')
	})
	Horizontal = Velocity:CreateSlider({
		Name = "Horizontal",
		Min = 0,
		Max = 100,
		Default = 100,
		Decimal = 1,
		Visible = (Mode.Value == 'Default')
	})
	HorizontalChance = Velocity:CreateSlider({
		Name = "Horizontal Chance",
		Min = 0,
		Max = 100,
		Default = 100,
		Decimal = 1,
		Suffix = '%',
		Visible = (Mode.Value == 'Default')
	})
	DelayGround = Velocity:CreateSlider({
		Name = "Delay Ground",
		Min = 0,
		Max = 3000,
		Default = 1000,
		Suffix = 'ms',
		Decimal = 1,
		Visible = (Mode.Value == 'Lag')
	})
	DelayAir = Velocity:CreateSlider({
		Name = "Delay Air",
		Min = 0,
		Max = 3000,
		Default = 1000,
		Suffix = 'ms',
		Decimal = 1,
		Visible = (Mode.Value == 'Lag')
	})
	Targetting = Velocity:CreateToggle({
		Name = 'Only when targetting',
		Default = false
	})
	Chance = Velocity:CreateSlider({
		Name = "Chance",
		Min = 0,
		Max = 100,
		Default = 100,
		Decimal = 1,
		Suffix = '%',
	})
end)

--[[
	Blatant Modules
]]

local AntiFallDirection
run(function()
    local AntiFall
    local Mode
    local Material
    local Color
	local rayCheck = cloneRaycast()
    rayCheck.RespectCanCollide = true

    local math_huge = math.huge
    local tick = tick
    local task_wait = task.wait
    local vector3new = Vector3.new
    local vector3zero = Vector3.zero
    
    local cachedLowGround = math_huge
    local lastGroundScan = 0
    local groundScanInterval = 2 
    
    local function getLowGround()
        local now = tick()
        if now - lastGroundScan < groundScanInterval and cachedLowGround ~= math_huge then
            return cachedLowGround
        end
        
        lastGroundScan = now
        local mag = math_huge
        local blockStore = bedwars.BlockController:getStore()
        local allPositions = blockStore:getAllBlockPositions()
        
        for i = 1, #allPositions do
            local pos = allPositions[i] * 3
            if pos.Y < mag and not getPlacedBlock(pos + vector3new(0, 3, 0)) then
                mag = pos.Y
            end
        end
        
        cachedLowGround = mag
        return mag
    end

    AntiFall = vape.Categories.Blatant:CreateModule({
        Name = 'AntiFall',
        Function = function(callback)
            if callback then
                repeat task_wait() until store.matchState ~= 0 or (not AntiFall.Enabled)
                if not AntiFall.Enabled then return end

                local pos, debounce = getLowGround(), tick()
                if pos ~= math_huge then
                    AntiFallPart = Instance.new('Part')
                    AntiFallPart.Size = vector3new(10000, 1, 10000)
                    AntiFallPart.Transparency = 1 - Color.Opacity
                    AntiFallPart.Material = Enum.Material[Material.Value]
                    AntiFallPart.Color = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
                    AntiFallPart.Position = vector3new(0, pos - 2, 0)
                    AntiFallPart.CanCollide = Mode.Value == 'Collide'
                    AntiFallPart.Anchored = true
                    AntiFallPart.CanQuery = false
                    AntiFallPart.Parent = workspace
                    AntiFall:Clean(AntiFallPart)
                    
                    AntiFall:Clean(AntiFallPart.Touched:Connect(function(touched)
                        if touched.Parent == lplr.Character and entitylib.isAlive and debounce < tick() then
                            debounce = tick() + 0.1
                            
                            if Mode.Value == 'Normal' then
                                local top = getNearGround()
                                if top then
                                    local lastTeleport = lplr:GetAttribute('LastTeleported')
                                    local connection
                                    local frameCounter = 0
                                    
                                    local vapeModules = vape.Modules
                                    local flyEnabled = vapeModules.Fly
                                    local infFlyEnabled = vapeModules.InfiniteFly
                                    local longJumpEnabled = vapeModules.LongJump
                                    
                                    local yMask = vector3new(1, 0, 1)
                                    local yOnly = vector3new(0, 1, 0)
                                    
                                    connection = runService.PreSimulation:Connect(function()
                                        frameCounter = frameCounter + 1
                                        
                                        if frameCounter % 5 == 0 then
                                            if flyEnabled.Enabled or infFlyEnabled.Enabled or longJumpEnabled.Enabled then
                                                connection:Disconnect()
                                                AntiFallDirection = nil
                                                return
                                            end
                                        end

                                        if entitylib.isAlive and lplr:GetAttribute('LastTeleported') == lastTeleport then
                                            local root = entitylib.character.RootPart
                                            local rootPos = root.Position
                                            local delta = (top - rootPos) * yMask
                                            
                                            AntiFallDirection = delta.Unit == delta.Unit and delta.Unit or vector3zero
                                            root.Velocity *= yMask
                                            
                                            if frameCounter % 3 == 0 then
                                                rayCheck.FilterDescendantsInstances = {gameCamera, lplr.Character}
                                                rayCheck.CollisionGroup = root.CollisionGroup

                                                local ray = workspace:Raycast(rootPos, AntiFallDirection, rayCheck)
                                                if ray then
                                                    for i = 1, 5 do
                                                        local dpos = roundPos(ray.Position + ray.Normal * 1.5) + vector3new(0, 3, 0)
                                                        if not getPlacedBlock(dpos) then
                                                            top = vector3new(top.X, pos.Y, top.Z)
                                                            break
                                                        end
                                                    end
                                                end
                                            end

                                            local yDiff = top.Y - rootPos.Y
                                            root.CFrame += vector3new(0, yDiff, 0)
                                            
                                            if not frictionTable.Speed then
                                                local speed = getSpeed()
                                                local newVelocity = (AntiFallDirection * speed) + vector3new(0, root.AssemblyLinearVelocity.Y, 0)
                                                root.AssemblyLinearVelocity = newVelocity
                                            end

                                            if delta.Magnitude < 1 then
                                                connection:Disconnect()
                                                AntiFallDirection = nil
                                            end
                                        else
                                            connection:Disconnect()
                                            AntiFallDirection = nil
                                        end
                                    end)
                                    AntiFall:Clean(connection)
                                end
                            elseif Mode.Value == 'Velocity' then
                                local rootVel = entitylib.character.RootPart.Velocity
                                entitylib.character.RootPart.Velocity = vector3new(rootVel.X, 100, rootVel.Z)
                            end
                        end
                    end))
                end
            else
                AntiFallDirection = nil
                cachedLowGround = math_huge
                lastGroundScan = 0
            end
        end,
        Tooltip = 'Help\'s you with your Parkinson\'s\nPrevents you from falling into the void.'
    })
    
    Mode = AntiFall:CreateDropdown({
        Name = 'Move Mode',
        List = {'Normal', 'Collide', 'Velocity'},
        Function = function(val)
            if AntiFallPart then
                AntiFallPart.CanCollide = val == 'Collide'
            end
        end,
        Tooltip = 'Normal - Smoothly moves you towards the nearest safe point\nVelocity - Launches you upward after touching\nCollide - Allows you to walk on the part'
    })
    
    local materials = {'ForceField'}
    for _, v in Enum.Material:GetEnumItems() do
        if v.Name ~= 'ForceField' then
            table.insert(materials, v.Name)
        end
    end
    
    Material = AntiFall:CreateDropdown({
        Name = 'Material',
        List = materials,
        Function = function(val)
            if AntiFallPart then
                AntiFallPart.Material = Enum.Material[val]
            end
        end
    })
    
    Color = AntiFall:CreateColorSlider({
        Name = 'Color',
        DefaultOpacity = 0.5,
        Function = function(h, s, v, o)
            if AntiFallPart then
                AntiFallPart.Color = Color3.fromHSV(h, s, v)
                AntiFallPart.Transparency = 1 - o
            end
        end
    })
end)

run(function()
    local _sharedHitBlock = nil
    local _hitBlockPatchers = {}

    getgenv().registerHitBlockPatch = function(key, fn)
        _hitBlockPatchers[key] = fn
        if not _sharedHitBlock and bedwars.BlockBreaker then
            _sharedHitBlock = bedwars.BlockBreaker.hitBlock
        end
        if not bedwars.BlockBreaker then return end
        bedwars.BlockBreaker.hitBlock = function(self, maid, raycastparams, ...)
            for _, patcher in _hitBlockPatchers do
                local result = patcher(self, maid, raycastparams, ...)
                if result ~= nil then return result end
            end
            if _sharedHitBlock then return _sharedHitBlock(self, maid, raycastparams, ...) end
        end
    end

    getgenv().unregisterHitBlockPatch = function(key)
        _hitBlockPatchers[key] = nil
        if not next(_hitBlockPatchers) then
            if bedwars.BlockBreaker then
                bedwars.BlockBreaker.hitBlock = _sharedHitBlock
            end
            _sharedHitBlock = nil
        end
    end

    local FastBreak
    local Time
    local BedCheck
    local Blacklist
    local blocks
    local string_lower = string.lower
    local string_find = string.find
    local task_wait = task.wait
    local currentBlock = nil
    local oldHitBlock = nil
    local lastHotbarSlot = nil
    local bedCache = {}
    local blacklistCache = {}
    local lastCacheClean = 0
    local cacheCleanInterval = 5 
    
    local function isBed(block)
        if not block then return false end
        local cached = bedCache[block]
        if cached ~= nil then return cached end
        
        local result = false
        pcall(function()
            if collectionService:HasTag(block, 'bed') or (block.Parent and collectionService:HasTag(block.Parent, 'bed')) then
                result = true
            elseif string_find(string_lower(block.Name), 'bed', 1, true) then
                result = true
            end
        end)
        
        if result then bedCache[block] = true end
        return result
    end
    
    local cachedBlacklistLower = {}
    local function updateBlacklistCache()
        if not blocks or not blocks.ListEnabled then return end
        
        cachedBlacklistLower = {}
        for _, v in pairs(blocks.ListEnabled) do
            table.insert(cachedBlacklistLower, string_lower(v))
        end
    end
    
    local function isBlacklisted(block)
        if not block or #cachedBlacklistLower == 0 then return false end
        local cached = blacklistCache[block]
        if cached ~= nil then return cached end
        
        local name = string_lower(block.Name)
        local result = false
        for i = 1, #cachedBlacklistLower do
            if string_find(name, cachedBlacklistLower[i], 1, true) then
                result = true
                break
            end
        end
        
        blacklistCache[block] = result
        return result
    end
    
    local function shouldSkip(block)
        if not block then return false end
        if BedCheck and BedCheck.Enabled and isBed(block) then return true end
        if Blacklist and Blacklist.Enabled and isBlacklisted(block) then return true end
        return false
    end
    
    local lastBreakUpdate = 0
    local breakUpdateCooldown = 0.05
    local pendingUpdate = false
    
    local function updateBreakSpeed()
        if not FastBreak or not FastBreak.Enabled then return end
        local now = tick()
        if now - lastBreakUpdate < breakUpdateCooldown then
            pendingUpdate = true
            return
        end
        lastBreakUpdate = now
        pendingUpdate = false
        
        pcall(function()
            local cooldown = (shouldSkip(currentBlock)) and 0.3 or Time.Value
            bedwars.BlockBreakController.blockBreaker:setCooldown(cooldown)
        end)
    end
    
    FastBreak = vape.Categories.Blatant:CreateModule({
        Name = 'FastBreak',
        Function = function(callback)
            if callback then
                lastHotbarSlot = nil

				registerHitBlockPatch('FastBreak', function(self, maid, raycastparams, ...)
					local block = nil
					pcall(function()
						local blockInfo = self.clientManager:getBlockSelector():getMouseInfo(1, {ray = raycastparams})
						if blockInfo and blockInfo.target and blockInfo.target.blockInstance then
							block = blockInfo.target.blockInstance
						end
					end)
					
					local currentSlot = store.inventory and store.inventory.hotbarSlot
					local slotChanged = currentSlot ~= lastHotbarSlot
					if slotChanged then
						lastHotbarSlot = currentSlot
					end

					if block ~= currentBlock or slotChanged then
						currentBlock = block
						updateBreakSpeed()
					end
					end)
                
                updateBlacklistCache()
                
                task.spawn(function()
                    while FastBreak.Enabled do
                        if tick() - lastCacheClean > cacheCleanInterval then
                            lastCacheClean = tick()
                            bedCache = {}
                            blacklistCache = {}
                        end
                        if pendingUpdate then updateBreakSpeed() end
                        task_wait(0.05) 
                    end
                end)
			else
				pcall(function() bedwars.BlockBreakController.blockBreaker:setCooldown(0.3) end)
				unregisterHitBlockPatch('FastBreak')
				currentBlock = nil
				lastHotbarSlot = nil
				bedCache, blacklistCache, cachedBlacklistLower = {}, {}, {}
			end
        end,
        Tooltip = 'mine faster'
    })
    
    Time = FastBreak:CreateSlider({
        Name = 'Break speed',
        Min = 0, Max = 0.3, Default = 0.25, Decimal = 100, Suffix = 'seconds',
        Function = function() updateBreakSpeed() end
    })
    
    BedCheck = FastBreak:CreateToggle({
        Name = 'Bed Check',
        Default = false,
        Tooltip = 'mining is normal when breaking beds',
        Function = function() bedCache = {}; updateBreakSpeed() end
    })
    
    Blacklist = FastBreak:CreateToggle({
        Name = 'Blacklist Blocks',
        Default = false,
        Tooltip = 'mining is normal for blacklisted blocks',
        Function = function(v)
            if blocks then blocks.Object.Visible = v end
            blacklistCache = {}
            if v then updateBlacklistCache() end
            updateBreakSpeed()
        end
    })
    
    blocks = FastBreak:CreateTextList({
        Name = 'Blacklisted Blocks',
        Placeholder = 'bed',
        Visible = false,
        Function = function()
            updateBlacklistCache()
            blacklistCache = {}
            updateBreakSpeed()
        end
    })
end)

run(function()
    local Value
    local VerticalValue
    local WallCheck
    local PopBalloons
    local TP
    local lastonground = false
    local MobileButtons
    local FlyAnywayProgressBar = {Enabled = false}
    local FlyAnywayProgressBarFrame
    local BarColor
    local rayCheck = RaycastParams.new()
    rayCheck.RespectCanCollide = true
    local up, down, old = 0, 0
    local mobileControls = {}
    local groundtime = nil
    local onground = false
    local flyCooldownActive = false
    local lastGroundTouchTime = 0
    local MAX_FLY_TIME = 2.5
    local tick = tick
    local task_wait = task.wait
    local math_max = math.max
    local math_floor = math.floor
    local string_format = string.format
    local vector3new = Vector3.new
    local vector3zero = Vector3.zero
    local udim2new = UDim2.new
    local cframeLookAlong = CFrame.lookAlong
    local cachedBalloonCount = 0
    local lastBalloonCheck = 0
    local balloonCheckInterval = 0.2 
    local cachedMatchState = 0
    local lastMatchStateCheck = 0
    local lastGroundTime = tick()
    local airTime = 0
    
    local function createMobileButton(name, position, icon)
        local button = Instance.new("TextButton")
        button.Name = name
        button.Size = udim2new(0, 60, 0, 60)
        button.Position = position
        button.BackgroundTransparency = 0.2
        button.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        button.BorderSizePixel = 0
        button.Text = icon
        button.TextScaled = true
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        button.Font = Enum.Font.SourceSansBold
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 8)
        corner.Parent = button
        return button
    end

    local function cleanupMobileControls()
        for _, control in pairs(mobileControls) do
            if control then
                control:Destroy()
            end
        end
        mobileControls = {}
    end

    local progressBarFrameCounter = 0
    local MAX_FRAME_COUNTER = 600
    local function updateProgressBar()
        if not FlyAnywayProgressBarFrame then return end

        if (TP and TP.Enabled) or not entitylib.isAlive then
            FlyAnywayProgressBarFrame.Visible = false
            return
        end
        
        local now = tick()
        if now - lastBalloonCheck > balloonCheckInterval then
            lastBalloonCheck = now
            cachedBalloonCount = lplr.Character:GetAttribute('InflatedBalloons') or 0
            cachedMatchState = store.matchState
        end
        
        local flyAllowed = cachedBalloonCount > 0 or cachedMatchState == 2
        
        if flyAllowed then
            FlyAnywayProgressBarFrame.Frame.Size = udim2new(1, 0, 0, 20)
            FlyAnywayProgressBarFrame.TextLabel.Text = "∞"
            FlyAnywayProgressBarFrame.Visible = FlyAnywayProgressBar.Enabled
            return
        end
        
        progressBarFrameCounter = (progressBarFrameCounter + 1) % MAX_FRAME_COUNTER
        if progressBarFrameCounter % 3 == 0 then
            local hipHeight = entitylib.character.Humanoid.HipHeight
            local checkPos = entitylib.character.HumanoidRootPart.Position + vector3new(0, (hipHeight * -2) - 1, 0)
            local newray = getPlacedBlock(checkPos)
            onground = newray ~= nil
        end
        
        if onground then
            groundtime = nil
            flyCooldownActive = false
            lastGroundTouchTime = now
            
            FlyAnywayProgressBarFrame.Frame.Size = udim2new(1, 0, 0, 20)
            FlyAnywayProgressBarFrame.TextLabel.Text = string_format("%.1fs", MAX_FLY_TIME)
            FlyAnywayProgressBarFrame.Visible = FlyAnywayProgressBar.Enabled and Fly.Enabled
            
            local tween = FlyAnywayProgressBarFrame.Frame:FindFirstChild("Tween")
            if tween then
                tween:Destroy()
            end
        else
            if not groundtime then
                groundtime = now + MAX_FLY_TIME
                flyCooldownActive = false
            end
            
            local timeLeft = math_max(0, groundtime - now)
            local progress = timeLeft / MAX_FLY_TIME
            
            FlyAnywayProgressBarFrame.Frame.Size = udim2new(progress, 0, 0, 20)
            FlyAnywayProgressBarFrame.TextLabel.Text = string_format("%.1fs", timeLeft)
            FlyAnywayProgressBarFrame.Visible = FlyAnywayProgressBar.Enabled and Fly.Enabled
            
            if timeLeft <= 0 and not flyCooldownActive then
                flyCooldownActive = true
            end
        end
        
        lastonground = onground
    end

    Fly = vape.Categories.Blatant:CreateModule({
        Name = 'Fly',
        Function = function(callback)
            frictionTable.Fly = callback or nil
            updateVelocity()
            if callback then
                up, down, old = 0, 0, bedwars.BalloonController.deflateBalloon
                bedwars.BalloonController.deflateBalloon = function() end
                local tpTick, tpToggle, oldy = tick(), true

                if lplr.Character and (lplr.Character:GetAttribute('InflatedBalloons') or 0) == 0 and getItem('balloon') then
                    bedwars.BalloonController:inflateBalloon()
                end

                Fly:Clean(vapeEvents.AttributeChanged.Event:Connect(function(changed)
                    if changed == 'InflatedBalloons' then
                        local char = lplr.Character
                        if not char then return end
                        cachedBalloonCount = char:GetAttribute('InflatedBalloons') or 0
                        if cachedBalloonCount == 0 and getItem('balloon') then
                            bedwars.BalloonController:inflateBalloon()
                        end
                    end
                end))

                local renderFrameCounter = 0
                Fly:Clean(runService.RenderStepped:Connect(function(delta)
                    if FlyAnywayProgressBar.Enabled and Fly.Enabled then
                        renderFrameCounter = renderFrameCounter + 1
                        if renderFrameCounter % 2 == 0 then
                            updateProgressBar()
                        end
                    end
                end))

                local preSimFrameCounter = 0
                local lastWallRaycast = 0
                local wallRaycastInterval = 0.05
                
                Fly:Clean(runService.PreSimulation:Connect(function(dt)
                    if entitylib.isAlive and isnetworkowner(entitylib.character.RootPart) then
                        preSimFrameCounter = preSimFrameCounter + 1
                        local now = tick()
                        
                        if preSimFrameCounter % 12 == 0 then
                            cachedBalloonCount = lplr.Character and lplr.Character:GetAttribute('InflatedBalloons') or 0
                            cachedMatchState = store.matchState
                        end

                        local humanoid = entitylib.character.Humanoid
                        if humanoid.FloorMaterial ~= Enum.Material.Air then
                            lastGroundTime = now
                        end
                        airTime = now - lastGroundTime
                        
                        local flyAllowed = cachedBalloonCount > 0 or cachedMatchState == 2
                        
                        local oscillation = (now % 0.4 < 0.2) and -1 or 1
                        local mass = (1.95 + (flyAllowed and 6 or 0) * oscillation) + ((up + down) * VerticalValue.Value)
                        
                        local root = entitylib.character.RootPart
                        local moveDirection = entitylib.character.Humanoid.MoveDirection
                        local velo = getSpeed()
                        local destination = (moveDirection * math_max(Value.Value - velo, 0) * dt)
                        
                        if WallCheck.Enabled and (now - lastWallRaycast) > wallRaycastInterval then
                            lastWallRaycast = now
                            local filterList = {lplr.Character, gameCamera}
                            if AntiVoidPart then table.insert(filterList, AntiVoidPart) end
                            rayCheck.FilterDescendantsInstances = filterList
                            rayCheck.CollisionGroup = root.CollisionGroup

                            if destination.Magnitude > 0.001 then
                                local ray = workspace:Raycast(root.Position, destination, rayCheck)
                                if ray then
                                    destination = ((ray.Position + ray.Normal) - root.Position)
                                end
                            end
                        end

                        if not flyAllowed then
                            if tpToggle then
                                if airTime > 2 then  
                                    if not oldy then
                                        rayCheck.FilterDescendantsInstances = {lplr.Character, gameCamera, AntiVoidPart}
                                        rayCheck.CollisionGroup = root.CollisionGroup
                                        local ray = workspace:Raycast(root.Position, vector3new(0, -1000, 0), rayCheck)
                                        if ray and TP.Enabled then
                                            tpToggle = false
                                            oldy = root.Position.Y
                                            tpTick = now + 0.11
                                            root.CFrame = cframeLookAlong(vector3new(root.Position.X, ray.Position.Y + entitylib.character.HipHeight, root.Position.Z), root.CFrame.LookVector)
                                        end
                                    end
                                end
                            else
                                if oldy then
                                    if tpTick < now then
                                        local newpos = vector3new(root.Position.X, oldy, root.Position.Z)
                                        root.CFrame = cframeLookAlong(newpos, root.CFrame.LookVector)
                                        tpToggle = true
                                        oldy = nil
                                    else
                                        mass = 0
                                    end
                                end
                            end
                        end

                        root.CFrame += destination
                        root.AssemblyLinearVelocity = (moveDirection * velo) + vector3new(0, mass, 0)
                    end
                end))

                local isMobile = inputService.TouchEnabled and not inputService.KeyboardEnabled and not inputService.MouseEnabled
                local MobileEnabled = MobileButtons.Enabled or isMobile
                if MobileEnabled then
                    local gui = Instance.new("ScreenGui")
                    gui.Name = "FlyControls"
                    gui.ResetOnSpawn = false
                    gui.Parent = lplr.PlayerGui

                    local upButton = createMobileButton("UpButton", udim2new(0.9, -70, 0.7, -140), "↑")
                    local downButton = createMobileButton("DownButton", udim2new(0.9, -70, 0.7, -70), "↓")

                    mobileControls.UpButton = upButton
                    mobileControls.DownButton = downButton
                    mobileControls.ScreenGui = gui

                    upButton.Parent = gui
                    downButton.Parent = gui

                    Fly:Clean(upButton.MouseButton1Down:Connect(function()
                        up = 1
                    end))
                    Fly:Clean(upButton.MouseButton1Up:Connect(function()
                        up = 0
                    end))
                    Fly:Clean(downButton.MouseButton1Down:Connect(function()
                        down = -1
                    end))
                    Fly:Clean(downButton.MouseButton1Up:Connect(function()
                        down = 0
                    end))
                end

                Fly:Clean(inputService.InputBegan:Connect(function(input)
                    if not inputService:GetFocusedTextBox() then
                        if input.KeyCode == Enum.KeyCode.Space or input.KeyCode == Enum.KeyCode.ButtonA then
                            up = 1
                        elseif input.KeyCode == Enum.KeyCode.LeftShift or input.KeyCode == Enum.KeyCode.ButtonL2 then
                            down = -1
                        end
                    end
                end))
                Fly:Clean(inputService.InputEnded:Connect(function(input)
                    if input.KeyCode == Enum.KeyCode.Space or input.KeyCode == Enum.KeyCode.ButtonA then
                        up = 0
                    elseif input.KeyCode == Enum.KeyCode.LeftShift or input.KeyCode == Enum.KeyCode.ButtonL2 then
                        down = 0
                    end
                end))
                if inputService.TouchEnabled then
                    pcall(function()
                        local jumpButton = lplr.PlayerGui.TouchGui.TouchControlFrame.JumpButton
                        Fly:Clean(jumpButton:GetPropertyChangedSignal('ImageRectOffset'):Connect(function()
                            if not mobileControls.UpButton then
                                up = jumpButton.ImageRectOffset.X == 146 and 1 or 0
                            end
                        end))
                    end)
                end
            else
                if FlyAnywayProgressBarFrame then
                    FlyAnywayProgressBarFrame.Visible = false
                end
                lastonground = nil
                groundtime = nil
                flyCooldownActive = false
                bedwars.BalloonController.deflateBalloon = old
                if PopBalloons.Enabled and entitylib.isAlive and (lplr.Character:GetAttribute('InflatedBalloons') or 0) > 0 then
                    for _ = 1, 3 do
                        bedwars.BalloonController:deflateBalloon()
                    end
                end
                cleanupMobileControls()
                cachedBalloonCount = 0
                lastBalloonCheck = 0
                cachedMatchState = 0
            end
        end,
        ExtraText = function()
            return 'Heatseeker'
        end,
        Tooltip = 'makes you go zoom!'
    })
    Value = Fly:CreateSlider({
        Name = 'Speed',
        Min = 1,
        Max = 23,
        Default = 23,
        Suffix = function(val)
            return val == 1 and 'stud' or 'studs'
        end
    })
    VerticalValue = Fly:CreateSlider({
        Name = 'Vertical Speed',
        Min = 1,
        Max = 150,
        Default = 50,
        Suffix = function(val)
            return val == 1 and 'stud' or 'studs'
        end
    })
    WallCheck = Fly:CreateToggle({
        Name = 'Wall Check',
        Default = true
    })
    PopBalloons = Fly:CreateToggle({
        Name = 'Pop Balloons',
        Default = true
    })
    FlyAnywayProgressBar = Fly:CreateToggle({
        Name = "Progress Bar",
        Function = function(callback)
            if BarColor and BarColor.Object then
                BarColor.Object.Visible = callback
            end
            if callback then
                FlyAnywayProgressBarFrame = Instance.new("Frame")
                FlyAnywayProgressBarFrame.AnchorPoint = Vector2.new(0.5, 0)
                FlyAnywayProgressBarFrame.Position = udim2new(0.5, 0, 1, -200)
                FlyAnywayProgressBarFrame.Size = udim2new(0.2, 0, 0, 20)
                FlyAnywayProgressBarFrame.BackgroundTransparency = 0.5
                FlyAnywayProgressBarFrame.BorderSizePixel = 0
                FlyAnywayProgressBarFrame.BackgroundColor3 = Color3.new(0, 0, 0)
                FlyAnywayProgressBarFrame.Visible = false
                FlyAnywayProgressBarFrame.Parent = vape.gui
                
                local FlyAnywayProgressBarFrame2 = Instance.new("Frame")
                FlyAnywayProgressBarFrame2.Name = "Frame"
                FlyAnywayProgressBarFrame2.AnchorPoint = Vector2.new(0, 0)
                FlyAnywayProgressBarFrame2.Position = udim2new(0, 0, 0, 0)
                FlyAnywayProgressBarFrame2.Size = udim2new(1, 0, 0, 20)
                FlyAnywayProgressBarFrame2.BackgroundTransparency = 0
                FlyAnywayProgressBarFrame2.BorderSizePixel = 0
                FlyAnywayProgressBarFrame2.BackgroundColor3 = BarColor and Color3.fromHSV(BarColor.H, BarColor.S, BarColor.V) or Color3.fromHSV(vape.GUIColor.Hue, vape.GUIColor.Sat, vape.GUIColor.Value)
                FlyAnywayProgressBarFrame2.Visible = true
                FlyAnywayProgressBarFrame2.Parent = FlyAnywayProgressBarFrame
                
                local FlyAnywayProgressBartext = Instance.new("TextLabel")
                FlyAnywayProgressBartext.Name = "TextLabel"
                FlyAnywayProgressBartext.Text = "2.5s"
                FlyAnywayProgressBartext.Font = Enum.Font.Gotham
                FlyAnywayProgressBartext.TextStrokeTransparency = 0
                FlyAnywayProgressBartext.TextColor3 = Color3.new(0.9, 0.9, 0.9)
                FlyAnywayProgressBartext.TextSize = 20
                FlyAnywayProgressBartext.Size = udim2new(1, 0, 1, 0)
                FlyAnywayProgressBartext.BackgroundTransparency = 1
                FlyAnywayProgressBartext.Position = udim2new(0, 0, 0, 0)
                FlyAnywayProgressBartext.Parent = FlyAnywayProgressBarFrame
            else
                if FlyAnywayProgressBarFrame then 
                    FlyAnywayProgressBarFrame:Destroy() 
                    FlyAnywayProgressBarFrame = nil 
                end
            end
        end,
        Tooltip = "show amount of time for fly",
        Default = true
    })
    BarColor = Fly:CreateColorSlider({
        Name = 'Bar Color',
        Darker = true,
        Visible = false,
        Function = function(h, s, v, o)
            if FlyAnywayProgressBarFrame then
                FlyAnywayProgressBarFrame:FindFirstChild('Frame').BackgroundColor3 = Color3.fromHSV(h, s, v)
                FlyAnywayProgressBarFrame:FindFirstChild('Frame').BackgroundTransparency = 1 - o
            end
        end
    })

    TP = Fly:CreateToggle({
        Name = 'TP Down',
        Default = true,
        Function = function(callback)
            if callback and FlyAnywayProgressBarFrame then
                FlyAnywayProgressBarFrame.Visible = false
            end
        end
    })
    MobileButtons = Fly:CreateToggle({
        Name = "Mobile Buttons",
        Visible = false,
        Tooltip = 'puts up n down buttons on ur screen gng',
        Function = function() 
            if Fly.Enabled then
                Fly:Toggle()
                Fly:Toggle()
            end
        end
    })

    task.defer(function()
        if BarColor and BarColor.Object then
            BarColor.Object.Visible = FlyAnywayProgressBar.Enabled
        end
        if MobileButtons and MobileButtons.Object then
            MobileButtons.Object.Visible = inputService.TouchEnabled
        end
    end)
end)

run(function()
	vape.Categories.Blatant:CreateModule({
		Name = 'KeepSprint',
		Function = function(callback)
			debug.setconstant(bedwars.SprintController.startSprinting, 5, callback and 'blockSprinting' or 'blockSprint')
			bedwars.SprintController:stopSprinting()
		end,
		Tooltip = 'allows you to sprint with potions'
	})
end)

run(function()
	local NoSlowDown
    local old
    local SophiaCheck
    local FROZEN_THRESHOLD = 10
    local cachedModifier = nil
    local nsHooked = false
    local origPunch = nil
    local origGroveDisable = nil

    local function hookKitNoSlow()
        if bedwars.DragonSlayerController and not origPunch then
            origPunch = bedwars.DragonSlayerController.playPunchAnimation
            bedwars.DragonSlayerController.playPunchAnimation = function(self, arg2)
                local ok, maid = pcall(function()
                    local imp = debug.getupvalue(origPunch, 1)
                    local anim = debug.getupvalue(origPunch, 2)
                    local plrs = debug.getupvalue(origPunch, 3)
                    local atype = debug.getupvalue(origPunch, 4)
                    local rs = debug.getupvalue(origPunch, 6)
                    local m = imp.new()
                    local track = anim:playAnimation(plrs.LocalPlayer, atype.DRAGON_SLAYER_PUNCH)
                    m:GiveTask(function()
                        if track then track:Stop() end
                    end)
                    m:GiveTask(rs.Heartbeat:Connect(function()
                        local char = plrs.LocalPlayer.Character
                        if not char or not char.PrimaryPart then
                            m:DoCleaning()
                            return
                        end
                        char:PivotTo(CFrame.new(char:GetPrimaryPartCFrame().Position) * arg2)
                    end))
                    task.delay(0.46, function() m:DoCleaning() end)
                    return m
                end)
                if ok and maid then return maid end
                return origPunch(self, arg2)
            end
        end

        if bedwars.SpiritGardenerController and not origGroveDisable then
            origGroveDisable = bedwars.SpiritGardenerController.disableActionsOnCharge
            bedwars.SpiritGardenerController.disableActionsOnCharge = function(self, maid, character)
                if character ~= lplr.Character then
                    return origGroveDisable(self, maid, character)
                end
                local ok = pcall(function()
                    local KnitClient = bedwars.KnitClient
                    KnitClient.Controllers.SwordController:toggleSwordSwing(true)
                    KnitClient.Controllers.BlockPlacementController:disableBlockPlacer()
                    local ClientSyncEvents = debug.getupvalue(origGroveDisable, 3)
                    local projConn = ClientSyncEvents.BeginProjectileTargeting:connect(function(event)
                        event:setCancelled(true)
                    end)
                    local jumpMod = KnitClient.Controllers.JumpHeightController:getJumpModifier():addModifier({
                        jumpHeightMultiplier = 0
                    })
                    maid:GiveTask(function()
                        KnitClient.Controllers.SwordController:toggleSwordSwing(false)
                        KnitClient.Controllers.BlockPlacementController:enableBlockPlacer()
                        projConn:Destroy()
                        jumpMod.Destroy()
                    end)
                end)
                if not ok then
                    return origGroveDisable(self, maid, character)
                end
            end
        end
    end

    local function unhookKitNoSlow()
        if origPunch and bedwars.DragonSlayerController then
            bedwars.DragonSlayerController.playPunchAnimation = origPunch
        end
        origPunch = nil
        if origGroveDisable and bedwars.SpiritGardenerController then
            bedwars.SpiritGardenerController.disableActionsOnCharge = origGroveDisable
        end
        origGroveDisable = nil
    end
    NoSlowdown = vape.Categories.Blatant:CreateModule({
        Name = 'NoSlowdown',
        Function = function(callback)
            if not cachedModifier then
                if not bedwars.SprintController then return end
                cachedModifier = bedwars.SprintController:getMovementStatusModifier()
            end
            local modifier = cachedModifier
            if callback then
                if nsHooked then return end
                hookKitNoSlow()
                old = modifier.addModifier
                if not old then return end
                local orig = old
                nsHooked = true
                modifier.addModifier = function(self, tab)
                    if not orig then return end
                    if SophiaCheck and SophiaCheck.Enabled and isFrozen(nil, FROZEN_THRESHOLD) then
                        return orig(self, tab)
                    end

                    if tab.moveSpeedMultiplier then
                        tab.moveSpeedMultiplier = math.max(tab.moveSpeedMultiplier, 1)
                    end
                    return orig(self, tab)
                end

                for i, v in modifier.modifiers do
                    local mod = type(v) == 'table' and v or (type(i) == 'table' and i or nil)
                    if mod and (mod.moveSpeedMultiplier or 1) < 1 then
                        modifier:removeModifier(mod)
                    end
                end
            else
                unhookKitNoSlow()
                if cachedModifier then
                    cachedModifier.addModifier = old
                    cachedModifier = nil
                end
                old = nil
                nsHooked = false
            end
        end,
        Tooltip = 'prevents slowing down when using items.'
    })

    SophiaCheck = NoSlowdown:CreateToggle({
        Name = 'Sophia Check',
        Default = false
    })
end)

run(function()
	local DamageBoost
	local stack
	
	DamageBoost = vape.Categories.Blatant:CreateModule({
		Name = 'DamageBoost',
		Function = function(callback)
			if callback then
				DamageBoost:Clean(vapeEvents.EntityDamageEvent.Event:Connect(function(damageTable)
					if entitylib.isAlive and tick() > (stack or 0) and damageTable.entityInstance == lplr.Character and not vape.Modules.LongJump.Enabled then
						local horizontal = (damageTable.knockbackMultiplier and damageTable.knockbackMultiplier.horizontal or 0)
						knockbackSpeed = bedwars.KnockbackUtil.calculateKnockbackVelocity(Vector3.one, 1, {
							vertical = 0,
							horizontal = horizontal,
						}).Magnitude * (0.9 + (store.ping and store.ping.total or 0))
						stack = tick() + (knockbackSpeed / 45)
						knockbackBoost = tick() + (horizontal / 3.5)
					end
				end))
			end
		end,
		Tooltip = 'makes you go slightly faster when damaged'
	})
end)

run(function()
	local NoFall
	local Chance
	local voidParams = RaycastParams.new()
	voidParams.FilterType = Enum.RaycastFilterType.Exclude
	voidParams.IgnoreWater = true
	local lastVoidCheck = 0
	local cachedVoid = false

	local function nearVoid(position)
		if not entitylib.isAlive then return false end
		local now = tick()
		if (now - lastVoidCheck) < 0.15 then
			return cachedVoid
		end
		lastVoidCheck = now
		voidParams.FilterDescendantsInstances = {entitylib.character.Character, gameCamera}
		local result = workspace:Raycast(position, Vector3.new(0, -8, 0), voidParams)
		cachedVoid = result == nil and position.Y < 0
		return cachedVoid
	end

	NoFall = vape.Categories.Blatant:CreateModule({
		Name = 'NoFall',
		Function = function(callback)
			if callback then
				NoFall:Clean(runService.Heartbeat:Connect(function(dt)
					if entitylib.isAlive and bedwars.Knit.Controllers.MatchController:getMatchState() == 1 then
						local root = entitylib.character.RootPart
						local v = root.Velocity

						if root.Velocity.Y < -35 then
							if nearVoid(root.Position) or math.random(1, 100) <= Chance.Value then
								root.Velocity = Vector3.new(0,2.5,0)
								entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Landed)
								runService.PreRender:Wait()
								root.Velocity = v
							end
						end
					end
				end))

				NoFall:Clean(entitylib.Events.LocalAdded:Connect(function(char)
					local animator = char.Humanoid:WaitForChild('Animator', 1)
					if animator and NoFall.Enabled then
						task.wait(.5)
						NoFall:Toggle()
						NoFall:Toggle()
					end
				end))
			end
		end,
		Tooltip = 'Take no fall damage.'
	})

	Chance = NoFall:CreateSlider({
		Name = 'Chance',
		Min = 1,
		Max = 100,
		Default = 100,
		Suffix = '%'
	})
end)

run(function()
	local FastPickup
	local FastPickupDelay
	local PickupRemote = replicatedStorage:WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged'):WaitForChild('PickupItemDrop')

	FastPickup = vape.Categories.Blatant:CreateModule({
		Name = 'FastPickup',
		Tooltip = 'picks up items fast asl',
		Function = function(callback)
			if callback then
				FastPickup:Clean(task.spawn(function()
					while FastPickup.Enabled do
						if entitylib.isAlive then
							for _, drop in pairs(workspace:WaitForChild('ItemDropsCache'):GetChildren()) do
								task.spawn(function()
									task.wait(FastPickupDelay.Value)
									pcall(function()
										PickupRemote:InvokeServer({ itemDrop = drop })
									end)
								end)
							end
						end
						task.wait(0.05)
					end
				end))
			end
		end,
	})

	FastPickupDelay = FastPickup:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 0.5,
		Default = 0,
		Decimal = 100,
		Suffix = 's',
	})
end)

run(function()
	local ProjectileAura
	local Targets
	local Range
	local List
	local HandCheck
	local FireSpeed
    local rayCheck = cloneRaycast()
	local projectileRemote = {InvokeServer = function() end}
	local FireDelays = {}
	task.spawn(function()
		projectileRemote = bedwars.Client:Get(remotes.FireProjectile).instance
	end)
	
	local function getAmmo(check)
		for _, item in store.inventory.inventory.items do
			if check.ammoItemTypes and table.find(check.ammoItemTypes, item.itemType) then
				return item.itemType
			end
		end
	end
	
	local function getProjectiles()
		local items = {}
		for _, item in store.inventory.inventory.items do
			local proj = bedwars.ItemMeta[item.itemType].projectileSource
			local ammo = proj and getAmmo(proj)
			if ammo and table.find(List.ListEnabled, ammo) then
				table.insert(items, {
					item,
					ammo,
					proj.projectileType(ammo),
					proj
				})
			end
		end
		return items
	end
	
	ProjectileAura = vape.Categories.Blatant:CreateModule({
		Name = 'ProjectileAura',
		Function = function(callback)
			if callback then
				repeat
					local holdingCrossbow = store.hand and store.hand.tool and store.hand.tool.Name:find('crossbow')
					local holdingBow = store.hand and store.hand.tool and store.hand.tool.Name:find('bow')
					
					if HandCheck.Enabled and not holdingCrossbow and not holdingBow then
						task.wait(0.1)
						continue
					end
					
					if (workspace:GetServerTimeNow() - bedwars.SwordController.lastAttack) > 0.5 then
						local ent = entitylib.EntityPosition({
							Part = 'RootPart',
							Range = Range.Value,
							Players = Targets.Players.Enabled,
							NPCs = Targets.NPCs.Enabled,
							Wallcheck = Targets.Walls.Enabled
						})
	
						if ent then
							local pos = entitylib.character.RootPart.Position
							for _, data in getProjectiles() do
								local item, ammo, projectile, itemMeta = unpack(data)
								if (FireDelays[item.itemType] or 0) < tick() then
									rayCheck.FilterDescendantsInstances = {workspace.Map}
									local meta = bedwars.ProjectileMeta[projectile]
									local projSpeed, gravity = meta.launchVelocity, meta.gravitationalAcceleration or 196.2
									local lifetime = tonumber(meta.predictionLifetimeSec) or tonumber(meta.lifetimeSec) or (projSpeed > 0 and math.min(3, 120 / projSpeed) or 3)
									local entGravity = workspace.Gravity
									local balloons = ent.Character and ent.Character:GetAttribute('InflatedBalloons')
									if balloons and balloons > 0 then
										entGravity = workspace.Gravity * (1 - (balloons >= 4 and 1.2 or balloons >= 3 and 1 or 0.975))
									end
									local entVel = ent.RootPart.AssemblyLinearVelocity or ent.RootPart.Velocity or Vector3.zero
									local calc = prediction.SolveTrajectory(pos, projSpeed, gravity, ent.RootPart.Position, entVel, entGravity, ent.HipHeight, ent.Jumping and 42.6 or nil, rayCheck, nil, 1, 1, lifetime)
									if calc then
										targetinfo.Targets[ent] = tick() + 1
										local switched = switchItem(item.tool)
	
										task.spawn(function()
											local dir, id = CFrame.lookAt(pos, calc).LookVector, httpService:GenerateGUID(true)
											local shootPosition = (CFrame.new(pos, calc) * CFrame.new(Vector3.new(-bedwars.BowConstantsTable.RelX, -bedwars.BowConstantsTable.RelY, -bedwars.BowConstantsTable.RelZ))).Position
											
											if holdingCrossbow then
												bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_CROSSBOW_FIRE)
												bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.CROSSBOW_FIRE)
											elseif holdingBow then
												bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_CROSSBOW_FIRE)
												bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.BOW_FIRE)
											else
												local shootAnim = bedwars.ItemMeta[item.tool.Name].thirdPerson and bedwars.ItemMeta[item.tool.Name].thirdPerson.shootAnimation
												if shootAnim then
													bedwars.GameAnimationUtil:playAnimation(lplr, shootAnim)
												end
											end
											
											bedwars.ProjectileController:createLocalProjectile(meta, ammo, projectile, shootPosition, id, dir * projSpeed, {drawDurationSeconds = 1})
											local res = projectileRemote:InvokeServer(item.tool, ammo, projectile, shootPosition, pos, dir * projSpeed, id, {drawDurationSeconds = 1, shotId = httpService:GenerateGUID(false)}, workspace:GetServerTimeNow() - 0.045)
											if not res then
												FireDelays[item.itemType] = tick()
											else
												local shoot = itemMeta.launchSound
												shoot = shoot and shoot[math.random(1, #shoot)] or nil
												if shoot then
													bedwars.SoundManager:playSound(shoot)
												end
											end
										end)
	
										FireDelays[item.itemType] = tick() + ((tonumber(itemMeta.fireDelaySec) or 0.5) / FireSpeed.Value)
										if switched then
											task.wait(0.05)
										end
									end
								end
							end
						end
					end
					task.wait(0.1)
				until not ProjectileAura.Enabled
			end
		end,
		Tooltip = 'Shoots people around you with viewmodel animations'
	})
	Targets = ProjectileAura:CreateTargets({
		Players = true,
		Walls = true
	})
	List = ProjectileAura:CreateTextList({
		Name = 'Projectiles',
		Default = {'arrow', 'snowball'}
	})
	Range = ProjectileAura:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 50,
		Default = 50,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	FireSpeed = ProjectileAura:CreateSlider({
		Name = 'Fire Speed',
		Min = 0.5,
		Max = 3,
		Default = 1,
		Decimal = 10,
	})
	HandCheck = ProjectileAura:CreateToggle({
		Name = 'Hand Check',
		Default = false,
		Tooltip = 'Only shoot when holding a bow or crossbow'
	})
end)

run(function()
	local Mode
	local Value
	local WallCheck
	local AutoJump
	local AlwaysJump
	local savedImpulseTime
	local rayCheck = cloneRaycast()
	rayCheck.RespectCanCollide = true
	rayCheck.FilterDescendantsInstances = {lplr.Character, gameCamera}
	
	Speed = vape.Categories.Blatant:CreateModule({
		Name = 'Speed',
		Function = function(callback)
			frictionTable.Speed = callback or nil
			updateVelocity()
			pcall(function()
				debug.setconstant(bedwars.WindWalkerController.updateSpeed, 7, callback and 'constantSpeedMultiplier' or 'moveSpeedMultiplier')
			end)
	
			if callback then
				if savedImpulseTime == nil then
					savedImpulseTime = bedwars.StatefulEntityKnockbackController.lastImpulseTime
				end
				Speed:Clean(function()
					pcall(function()
						bedwars.StatefulEntityKnockbackController.lastImpulseTime = (savedImpulseTime ~= nil and savedImpulseTime ~= math.huge) and savedImpulseTime or time()
					end)
					savedImpulseTime = nil
					pcall(function()
						if entitylib.isAlive then
							local root = entitylib.character.RootPart
							root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0)
						end
					end)
					pcall(function()
						bedwars.SprintController:setSpeed(bedwars.SprintController:isSprinting() and 20 or 14)
					end)
				end)
				Speed:Clean(runService.PreSimulation:Connect(function(dt)
					if not Speed.Enabled then return end
					bedwars.StatefulEntityKnockbackController.lastImpulseTime = math.huge
					if entitylib.isAlive then
						if not (Fly and Fly.Enabled) and not (LongJump and LongJump.Enabled) then
							bedwars.SprintController:setSpeed(Mode.Value == 'CFrame' and 20 or Value.Value)
							if Mode.Value == 'CFrame' then
								local state = entitylib.character.Humanoid:GetState()
								if state == Enum.HumanoidStateType.Climbing then return end
			
								local root, velo = entitylib.character.RootPart, getSpeed()
								local moveDirection = AntiFallDirection or entitylib.character.Humanoid.MoveDirection
								local destination = (moveDirection * math.max(Value.Value - velo, 0) * dt)
			
								if WallCheck.Enabled then
									rayCheck.CollisionGroup = root.CollisionGroup
									local ray = workspace:Raycast(root.Position, destination, rayCheck)
									if ray then
										destination = ((ray.Position + ray.Normal) - root.Position)
									end
								end
			
								root.CFrame += destination
								root.AssemblyLinearVelocity = (moveDirection * velo) + Vector3.new(0, root.AssemblyLinearVelocity.Y, 0)
								if AutoJump.Enabled and (state == Enum.HumanoidStateType.Running or state == Enum.HumanoidStateType.Landed) and moveDirection ~= Vector3.zero and (Attacking or AlwaysJump.Enabled) then
									entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
								end
							end
						end
					end
				end))
			else
				pcall(function()
					bedwars.StatefulEntityKnockbackController.lastImpulseTime = (savedImpulseTime ~= nil and savedImpulseTime ~= math.huge) and savedImpulseTime or time()
				end)
				savedImpulseTime = nil
				pcall(function()
					if entitylib.isAlive then
						local root = entitylib.character.RootPart
						root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0)
					end
				end)
				pcall(function()
					bedwars.SprintController:setSpeed(bedwars.SprintController:isSprinting() and 20 or 14)
				end)
			end
		end,
		ExtraText = function()
			return 'Heatseeker'
		end,
		Tooltip = 'increases your movement with various methods.'
	})
	Mode = Speed:CreateDropdown({
		Name = 'Method',
		List = {'Bedwars', 'CFrame'},
		Default = 'CFrame'
	})
	Value = Speed:CreateSlider({
		Name = 'Speed',
		Min = 1,
		Max = 23,
		Default = 23,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	WallCheck = Speed:CreateToggle({
		Name = 'Wall Check',
		Default = true
	})
	AutoJump = Speed:CreateToggle({
		Name = 'AutoJump',
		Function = function(callback)
			AlwaysJump.Object.Visible = callback
		end
	})
	AlwaysJump = Speed:CreateToggle({
		Name = 'Always Jump',
		Visible = false,
		Darker = true
	})
end)

run(function()
    local BCR
    local Value
    local CpsConstants = nil
    
    BCR = vape.Categories.Blatant:CreateModule({
        Name = "BlockCPSRemover",
        Function = function(callback)
            
            if callback then
                task.wait(1)
                
                pcall(function()
                    CpsConstants = require(replicatedStorage.TS['shared-constants']).CpsConstants
                end)
                
                if not CpsConstants then
                    pcall(function()
                        CpsConstants = bedwars.CpsConstants
                    end)
                end
                
                if CpsConstants then
                    local newCPS = Value.Value == 0 and 1000 or Value.Value
                    CpsConstants.BLOCK_PLACE_CPS = newCPS
                    
                    if isMobile then
                        for _, v in {'2', '5'} do
                            pcall(function()
                                BCR:Clean(lplr.PlayerGui.MobileUI[v].MouseButton1Down:Connect(function()
                                    if CpsConstants then
                                        local currentValue = Value.Value == 0 and 1000 or Value.Value
                                        CpsConstants.BLOCK_PLACE_CPS = currentValue
                                    end
                                end))
                            end)
                        end
                    end
                    
                    task.spawn(function()
                        while BCR.Enabled do
                            local currentValue = Value.Value == 0 and 1000 or Value.Value
                            if CpsConstants.BLOCK_PLACE_CPS ~= currentValue then
                                CpsConstants.BLOCK_PLACE_CPS = currentValue
                            end
                            task.wait(0.3)
                        end
                    end)
                end
                
            else
                if CpsConstants then
                    CpsConstants.BLOCK_PLACE_CPS = 12
                end
            end
        end,
        Tooltip = 'place blocks faster'
    })
    
    Value = BCR:CreateSlider({
        Name = "CPS Limit",
        Suffix = "CPS",
        Default = 12,
        Min = 12,
        Max = 20,
        Function = function()
            if BCR.Enabled and CpsConstants then
                local newCPS = Value.Value == 0 and 1000 or Value.Value
                CpsConstants.BLOCK_PLACE_CPS = newCPS
            end
        end,
    })
end)

run(function()
	local Value
	local CameraDir
	local start
	local JumpTick, JumpSpeed, Direction = tick(), 0
	local projectileRemote = {InvokeServer = function() end}
	task.spawn(function()
		projectileRemote = bedwars.Client:Get(remotes.FireProjectile).instance
	end)
	local handGunRemote = {InvokeServer = function() end}
	task.spawn(function()
		handGunRemote = game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("HandGunFireRequest")
	end)
	
	local function launchProjectile(item, pos, proj, speed, dir)
		if not pos then return end
	
		pos = pos - dir * 0.1
		local shootPosition = (CFrame.lookAlong(pos, Vector3.new(0, -speed, 0)) * CFrame.new(Vector3.new(-bedwars.BowConstantsTable.RelX, -bedwars.BowConstantsTable.RelY, -bedwars.BowConstantsTable.RelZ)))
		switchItem(item.tool, 0)
		task.wait(0.1)
		bedwars.ProjectileController:createLocalProjectile(bedwars.ProjectileMeta[proj], proj, proj, shootPosition.Position, '', shootPosition.LookVector * speed, {drawDurationSeconds = 1})
		if projectileRemote:InvokeServer(item.tool, proj, proj, shootPosition.Position, pos, shootPosition.LookVector * speed, httpService:GenerateGUID(true), {drawDurationSeconds = 1}, workspace:GetServerTimeNow() - 0.045) then
			local shoot = bedwars.ItemMeta[item.itemType].projectileSource.launchSound
			shoot = shoot and shoot[math.random(1, #shoot)] or nil
			if shoot then
				bedwars.SoundManager:playSound(shoot)
			end
		end
	end
	
	local LongJumpMethods = {
		cannon = function(_, pos, dir)
			pos = pos - Vector3.new(0, (entitylib.character.HipHeight + (entitylib.character.RootPart.Size.Y / 2)) - 3, 0)
			local rounded = Vector3.new(math.round(pos.X / 3) * 3, math.round(pos.Y / 3) * 3, math.round(pos.Z / 3) * 3)
			bedwars.placeBlock(rounded, 'cannon', false)
	
			task.delay(0, function()
				local block, blockpos = getPlacedBlock(rounded)
				if block and block.Name == 'cannon' and (entitylib.character.RootPart.Position - block.Position).Magnitude < 20 then
					local breaktype = bedwars.ItemMeta[block.Name].block.breakType
					local tool = store.tools[breaktype]
					if tool then
						switchItem(tool.tool)
					end
	
					bedwars.Client:Get(remotes.CannonAim):SendToServer({
						cannonBlockPos = blockpos,
						lookVector = dir
					})
	
					local broken = 0.1
					if bedwars.BlockController:calculateBlockDamage(lplr, {blockPosition = blockpos}) < block:GetAttribute('Health') then
						broken = 0.4
						bedwars.breakBlock(block, true, true)
					end
	
					task.delay(broken, function()
						for _ = 1, 4 do
							local call = bedwars.Client:Get(remotes.CannonLaunch):CallServer({cannonBlockPos = blockpos})
							if call then
								bedwars.breakBlock(block, true, true)
								JumpSpeed = 5.25 * Value.Value
								JumpTick = tick() + 2.3
								Direction = Vector3.new(dir.X, 0, dir.Z).Unit
								break
							end
							task.wait(0.1)
						end
					end)
				end
			end)
		end,
		cat = function(_, _, dir)
			LongJump:Clean(vapeEvents.CatPounce.Event:Connect(function()
				JumpSpeed = 4 * Value.Value
				JumpTick = tick() + 2.5
				Direction = Vector3.new(dir.X, 0, dir.Z).Unit
				entitylib.character.RootPart.Velocity = Vector3.zero
			end))
	
			if not bedwars.AbilityController:canUseAbility('CAT_POUNCE') then
				repeat task.wait() until bedwars.AbilityController:canUseAbility('CAT_POUNCE') or not LongJump.Enabled
			end
	
			if bedwars.AbilityController:canUseAbility('CAT_POUNCE') and LongJump.Enabled then
				bedwars.AbilityController:useAbility('CAT_POUNCE')
			end
		end,
		fireball = function(item, pos, dir)
			launchProjectile(item, pos, 'fireball', 60, dir)
		end,
		grappling_hook = function(item, pos, dir)
			launchProjectile(item, pos, 'grappling_hook_projectile', 140, dir)
		end,
		jade_hammer = function(item, _, dir)
			if not bedwars.AbilityController:canUseAbility(item.itemType..'_jump') then
				repeat task.wait() until bedwars.AbilityController:canUseAbility(item.itemType..'_jump') or not LongJump.Enabled
			end
	
			if bedwars.AbilityController:canUseAbility(item.itemType..'_jump') and LongJump.Enabled then
				bedwars.AbilityController:useAbility(item.itemType..'_jump')
				JumpSpeed = 1.4 * Value.Value
				JumpTick = tick() + 2.5
				Direction = Vector3.new(dir.X, 0, dir.Z).Unit
			end
		end,
		tnt = function(item, pos, dir)
			pos = pos - Vector3.new(0, (entitylib.character.HipHeight + (entitylib.character.RootPart.Size.Y / 2)) - 3, 0)
			local rounded = Vector3.new(math.round(pos.X / 3) * 3, math.round(pos.Y / 3) * 3, math.round(pos.Z / 3) * 3)
			start = Vector3.new(rounded.X, start.Y, rounded.Z) + (dir * (item.itemType == 'pirate_gunpowder_barrel' and 2.6 or 0.2))
			bedwars.placeBlock(rounded, item.itemType, false)
		end,
		wood_dao = function(item, pos, dir)
			if (lplr.Character:GetAttribute('CanDashNext') or 0) > workspace:GetServerTimeNow() or not bedwars.AbilityController:canUseAbility('dash') then
				repeat task.wait() until (lplr.Character:GetAttribute('CanDashNext') or 0) < workspace:GetServerTimeNow() and bedwars.AbilityController:canUseAbility('dash') or not LongJump.Enabled
			end
	
			if LongJump.Enabled then
				if not (LegitAura and LegitAura.Enabled) then
					bedwars.SwordController.lastAttack = workspace:GetServerTimeNow()
				end
				switchItem(item.tool, 0.1)
				replicatedStorage['events-@easy-games/game-core:shared/game-core-networking@getEvents.Events'].useAbility:FireServer('dash', {
					direction = dir,
					origin = pos,
					weapon = item.itemType
				})
				JumpSpeed = 4.5 * Value.Value
				JumpTick = tick() + 2.4
				Direction = Vector3.new(dir.X, 0, dir.Z).Unit
			end
		end
	}
	for _, v in {'stone_dao', 'iron_dao', 'diamond_dao', 'emerald_dao'} do
		LongJumpMethods[v] = LongJumpMethods.wood_dao
	end
	LongJumpMethods.void_axe = LongJumpMethods.jade_hammer
	LongJumpMethods.siege_tnt = LongJumpMethods.tnt
	LongJumpMethods.pirate_gunpowder_barrel = LongJumpMethods.tnt
	
	LongJump = vape.Categories.Blatant:CreateModule({
		Name = 'LongJump',
		Function = function(callback)
			frictionTable.LongJump = callback or nil
			updateVelocity()
			if callback then
				LongJump:Clean(vapeEvents.EntityDamageEvent.Event:Connect(function(damageTable)
					if damageTable.entityInstance == lplr.Character and damageTable.fromEntity == lplr.Character and (not damageTable.knockbackMultiplier or not damageTable.knockbackMultiplier.disabled) then
						local knockbackBoost = bedwars.KnockbackUtil.calculateKnockbackVelocity(Vector3.one, 1, {
							vertical = 0,
							horizontal = (damageTable.knockbackMultiplier and damageTable.knockbackMultiplier.horizontal or 1)
						}).Magnitude * 1.1
	
						if knockbackBoost >= JumpSpeed then
							local pos = damageTable.fromPosition and Vector3.new(damageTable.fromPosition.X, damageTable.fromPosition.Y, damageTable.fromPosition.Z) or damageTable.fromEntity and damageTable.fromEntity.PrimaryPart.Position
							if not pos then return end
							local vec = (entitylib.character.RootPart.Position - pos)
							JumpSpeed = knockbackBoost
							JumpTick = tick() + 2.5
							Direction = Vector3.new(vec.X, 0, vec.Z).Unit
						end
					end
				end))
				LongJump:Clean(vapeEvents.GrapplingHookFunctions.Event:Connect(function(dataTable)
					if dataTable.hookFunction == 'PLAYER_IN_TRANSIT' then
						local vec = entitylib.character.RootPart.CFrame.LookVector
						JumpSpeed = 2.5 * Value.Value
						JumpTick = tick() + 2.5
						Direction = Vector3.new(vec.X, 0, vec.Z).Unit
					end
				end))
	
				start = entitylib.isAlive and entitylib.character.RootPart.Position or nil
				LongJump:Clean(runService.PreSimulation:Connect(function(dt)
					local root = entitylib.isAlive and entitylib.character.RootPart or nil
	
					if root and isnetworkowner(root) then
						if JumpTick > tick() then
							root.AssemblyLinearVelocity = Direction * (getSpeed() + ((JumpTick - tick()) > 1.1 and JumpSpeed or 0)) + Vector3.new(0, root.AssemblyLinearVelocity.Y, 0)
							if entitylib.character.Humanoid.FloorMaterial == Enum.Material.Air and not start then
								root.AssemblyLinearVelocity += Vector3.new(0, dt * (workspace.Gravity - 23), 0)
							else
								root.AssemblyLinearVelocity = Vector3.new(root.AssemblyLinearVelocity.X, 15, root.AssemblyLinearVelocity.Z)
							end
							start = nil
						else
							if start then
								root.CFrame = CFrame.lookAlong(start, root.CFrame.LookVector)
							end
							root.AssemblyLinearVelocity = Vector3.zero
							JumpSpeed = 0
						end
					else
						start = nil
					end
				end))

				if store.hand and store.hand.tool and LongJumpMethods[store.hand.tool.Name] then
					task.spawn(LongJumpMethods[store.hand.tool.Name], getItem(store.hand.tool.Name), start, (CameraDir.Enabled and gameCamera or entitylib.character.RootPart).CFrame.LookVector)
					return
				end
				
				local foundItem = false
				for i, v in LongJumpMethods do
					local item = getItem(i)
					if item or store.equippedKit == i then
						foundItem = true
						task.spawn(v, item, start, (CameraDir.Enabled and gameCamera or entitylib.character.RootPart).CFrame.LookVector)
						break
					end
				end
				if not foundItem then
					notif("LongJump", "unable to find tool to use Long Jump with gng", 3)
					LongJump:Toggle()
					return
				end
			else
				JumpTick = tick()
				Direction = nil
				JumpSpeed = 0
			end
		end,
		ExtraText = function()
			return 'Heatseeker'
		end,
		Tooltip = 'lets you jump farther'
	})
	Value = LongJump:CreateSlider({
		Name = 'Speed',
		Min = 1,
		Max = 37,
		Default = 37,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	CameraDir = LongJump:CreateToggle({
		Name = 'Camera Direction'
	})
end)

--[[
	Render Modules
]]

run(function()
	local BedESP
	local Reference = {}
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	
	local function Added(bed)
		if not BedESP.Enabled then return end
		local BedFolder = Instance.new('Folder')
		BedFolder.Parent = Folder
		Reference[bed] = BedFolder
		local parts = bed:GetChildren()
		table.sort(parts, function(a, b)
			return a.Name > b.Name
		end)
	
		for _, part in parts do
			if part:IsA('BasePart') and part.Name ~= 'Blanket' then
				local handle = Instance.new('BoxHandleAdornment')
				handle.Size = part.Size + Vector3.new(.01, .01, .01)
				handle.AlwaysOnTop = true
				handle.ZIndex = 2
				handle.Visible = true
				handle.Adornee = part
				handle.Color3 = part.Color
				if part.Name == 'Legs' then
					handle.Color3 = Color3.fromRGB(167, 112, 64)
					handle.Size = part.Size + Vector3.new(.01, -1, .01)
					handle.CFrame = CFrame.new(0, -0.4, 0)
					handle.ZIndex = 0
				end
				handle.Parent = BedFolder
			end
		end
	
		table.clear(parts)
	end
	
	BedESP = vape.Categories.Render:CreateModule({
		Name = 'BedESP',
		Function = function(callback)
			if callback then
				BedESP:Clean(collectionService:GetInstanceAddedSignal('bed'):Connect(function(bed)
					task.delay(0.2, Added, bed)
				end))
				BedESP:Clean(collectionService:GetInstanceRemovedSignal('bed'):Connect(function(bed)
					if Reference[bed] then
						Reference[bed]:Destroy()
						Reference[bed] = nil
					end
				end))
				for _, bed in collectionService:GetTagged('bed') do
					Added(bed)
				end
			else
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'Render Beds through walls'
	})
end)

run(function()
	local LootESP
	local IronToggle
	local DiamondToggle
	local EmeraldToggle
	local Reference = {}
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	
	local CollectionService = collectionService
	
	local lootTypes = {
		iron = {
			keywords = {'iron'},
			color = Color3.fromRGB(200, 200, 200),
			icon = 'iron',
			displayName = 'IRON'
		},
		diamond = {
			keywords = {'diamond'},
			color = Color3.fromRGB(85, 200, 255),
			icon = 'diamond',
			displayName = 'DIAMOND'
		},
		emerald = {
			keywords = {'emerald'},
			color = Color3.fromRGB(0, 255, 100),
			icon = 'emerald',
			displayName = 'EMERALD'
		}
	}
	
	local function getLootType(itemName)
		local nameLower = itemName:lower()
		for lootType, config in pairs(lootTypes) do
			for _, keyword in ipairs(config.keywords) do
				if nameLower:find(keyword, 1, true) then 
					return lootType, config
				end
			end
		end
		return nil
	end
	
	local function isLootEnabled(lootType)
		if lootType == 'iron' then
			return IronToggle.Enabled
		elseif lootType == 'diamond' then
			return DiamondToggle.Enabled
		elseif lootType == 'emerald' then
			return EmeraldToggle.Enabled
		end
		return false
	end
	
	local function getProperIcon(lootType)
		local icon = bedwars.getIcon({itemType = lootType}, true)
		
		if not icon or icon == "" then
			return nil
		end
		
		return icon
	end
	
	local function Added(lootHandle, lootType, config)
		if not isLootEnabled(lootType) then return end
		if Reference[lootHandle] then return end 
		
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = lootType
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(40, 40)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = lootHandle
		
		local blur = addBlur(billboard)
		blur.Visible = true 
		
		local iconImage = getProperIcon(config.icon)
		
		if iconImage then
			local image = Instance.new('ImageLabel')
			image.Size = UDim2.fromOffset(40, 40)
			image.Position = UDim2.fromScale(0.5, 0.5)
			image.AnchorPoint = Vector2.new(0.5, 0.5)
			image.BackgroundColor3 = Color3.new(0, 0, 0) 
			image.BackgroundTransparency = 0.3 
			image.BorderSizePixel = 0
			image.Image = iconImage
			image.Parent = billboard
			
			local uicorner = Instance.new('UICorner')
			uicorner.CornerRadius = UDim.new(0, 4)
			uicorner.Parent = image
		else
			local frame = Instance.new('Frame')
			frame.Size = UDim2.fromScale(1, 1)
			frame.BackgroundColor3 = Color3.new(0, 0, 0) 
			frame.BackgroundTransparency = 0.3 
			frame.BorderSizePixel = 0
			frame.Parent = billboard
			
			local uicorner = Instance.new('UICorner')
			uicorner.CornerRadius = UDim.new(0, 4)
			uicorner.Parent = frame
			
			local textLabel = Instance.new('TextLabel')
			textLabel.Size = UDim2.fromScale(1, 1)
			textLabel.Position = UDim2.fromScale(0.5, 0.5)
			textLabel.AnchorPoint = Vector2.new(0.5, 0.5)
			textLabel.BackgroundTransparency = 1
			textLabel.Text = config.displayName
			textLabel.TextColor3 = config.color
			textLabel.TextScaled = true
			textLabel.Font = Enum.Font.GothamBold
			textLabel.TextStrokeTransparency = 0.5
			textLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
			textLabel.Parent = frame
		end
		
		Reference[lootHandle] = billboard
	end
	
	local function Removed(lootHandle)
		if Reference[lootHandle] then
			Reference[lootHandle]:Destroy()
			Reference[lootHandle] = nil
		end
	end
	
	local function findExistingLoot()
		local tagged = CollectionService:GetTagged('ItemDrop')
		for _, drop in ipairs(tagged) do
			local handle = drop:FindFirstChild('Handle')
			if handle then
				local lootType, config = getLootType(drop.Name)
				if lootType and isLootEnabled(lootType) then
					if not Reference[handle] then
						Added(handle, lootType, config)
					end
				end
			end
		end
	end
	
	local function refreshLootType(lootType)
		if not LootESP.Enabled then return end
		
		local enabled = isLootEnabled(lootType)
		
		if not enabled then
			for handle, billboard in pairs(Reference) do
				if billboard.Name == lootType then
					billboard:Destroy()
					Reference[handle] = nil
				end
			end
		else
			local tagged = CollectionService:GetTagged('ItemDrop')
			for _, drop in ipairs(tagged) do
				local handle = drop:FindFirstChild('Handle')
				if handle then
					local dropLootType, config = getLootType(drop.Name)
					if dropLootType == lootType and not Reference[handle] then
						Added(handle, lootType, config)
					end
				end
			end
		end
	end
	
	LootESP = vape.Categories.Render:CreateModule({
		Name = 'LootESP',
		Function = function(callback)
			if callback then
				findExistingLoot()
				
				LootESP:Clean(CollectionService:GetInstanceAddedSignal('ItemDrop'):Connect(function(drop)
					if not LootESP.Enabled then return end
					
					task.defer(function()
						local handle = drop:FindFirstChild('Handle')
						if not handle then return end
						
						local lootType, config = getLootType(drop.Name)
						if lootType and isLootEnabled(lootType) then
							Added(handle, lootType, config)
						end
					end)
				end))
				
				LootESP:Clean(CollectionService:GetInstanceRemovedSignal('ItemDrop'):Connect(function(drop)
					local handle = drop:FindFirstChild('Handle')
					if handle then
						Removed(handle)
					end
				end))
				
			else
				for handle, billboard in pairs(Reference) do
					billboard:Destroy()
				end
				table.clear(Reference)
			end
		end,
		Tooltip = 'esp for loot (iron, emerald, diamonds)'
	})
	
	IronToggle = LootESP:CreateToggle({
		Name = 'Iron',
		Function = function(callback)
			refreshLootType('iron')
		end,
		Default = true
	})
	
	DiamondToggle = LootESP:CreateToggle({
		Name = 'Diamond',
		Function = function(callback)
			refreshLootType('diamond')
		end,
		Default = true
	})
	
	EmeraldToggle = LootESP:CreateToggle({
		Name = 'Emerald',
		Function = function(callback)
			refreshLootType('emerald')
		end,
		Default = true
	})
end)

run(function()
	local StorageESP
	local List
	local Background
	local Color = {}
	local Reference = {}
	local ChestContents = {} 
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	
	local function getEnabledItemsSet()
		local set = {}
		for _, v in ipairs(List.ListEnabled) do
			set[v] = true
		end
		return set
	end
	
	local function nearStorageItem(item, enabledSet)
		for itemName in pairs(enabledSet) do
			if item:find(itemName, 1, true) then return itemName end
		end
		return nil
	end
	
	local function refreshAdornee(billboard)
		local chestPart = billboard.Adornee
		local folderValue = chestPart and chestPart:FindFirstChild('ChestFolderValue')
		local chest = folderValue and folderValue.Value or nil

		for _, obj in ipairs(billboard:GetChildren()) do
			if obj:IsA('ImageLabel') and obj.Name == 'Icon' then
				obj:Destroy()
			end
		end

		if not chest then
			billboard.Enabled = false
			return
		end

		local chestitems = chest:GetChildren()
		local enabledSet = getEnabledItemsSet()
		local listIsEmpty = next(enabledSet) == nil

		local matchedItems = {}
		local amounts = {}
		for _, item in ipairs(chestitems) do
			if item:IsA('Accessory') then
				if listIsEmpty or enabledSet[item.Name] or nearStorageItem(item.Name, enabledSet) then
					if amounts[item.Name] == nil then
						amounts[item.Name] = 0
						table.insert(matchedItems, item.Name)
					end
					amounts[item.Name] = amounts[item.Name] + (item:GetAttribute('Amount') or 1)
				end
			end
		end

		local count = #matchedItems
		if count == 0 then
			billboard.Enabled = false
			return
		end

		billboard.Enabled = true
		local iconSize = 36
		local padding = 2
		local totalWidth = count * iconSize + (count - 1) * padding
		billboard.Size = UDim2.fromOffset(totalWidth, iconSize)

		for i, itemName in ipairs(matchedItems) do
			local icon = Instance.new('ImageLabel')
			icon.Name = 'Icon'
			icon.Size = UDim2.fromOffset(iconSize, iconSize)
			icon.Position = UDim2.fromOffset((i - 1) * (iconSize + padding), 0)
			icon.AnchorPoint = Vector2.new(0, 0)
			icon.BackgroundColor3 = Color3.new(0, 0, 0)
			icon.BackgroundTransparency = 0.3
			icon.BorderSizePixel = 0
			icon.Image = bedwars.getIcon({itemType = itemName}, true)
			icon.Parent = billboard

			local uicorner = Instance.new('UICorner')
			uicorner.CornerRadius = UDim.new(0, 4)
			uicorner.Parent = icon

			local amt = Instance.new('TextLabel')
			amt.Name = 'Amt'
			amt.AnchorPoint = Vector2.new(1, 1)
			amt.Position = UDim2.new(1, -1, 1, -1)
			amt.Size = UDim2.fromOffset(iconSize, 14)
			amt.BackgroundTransparency = 1
			amt.Text = tostring(amounts[itemName] or 0)
			amt.TextColor3 = Color3.fromRGB(255, 255, 255)
			amt.TextStrokeTransparency = 0.4
			amt.TextSize = 13
			amt.Font = Enum.Font.GothamBold
			amt.TextXAlignment = Enum.TextXAlignment.Right
			amt.TextYAlignment = Enum.TextYAlignment.Bottom
			amt.Parent = icon
		end
	end
	
	local function Added(v)
		if Reference[v] then return end

		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'chest'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = v
		billboard.Enabled = false

		local blur = addBlur(billboard)

		Reference[v] = billboard

		refreshAdornee(billboard)

		task.defer(function()
			local folderValue = v:FindFirstChild('ChestFolderValue')
			local chest = folderValue and folderValue.Value or nil
			if chest and Reference[v] then
				local conn1 = chest.ChildAdded:Connect(function()
					if Reference[v] then refreshAdornee(Reference[v]) end
				end)
				local conn2 = chest.ChildRemoved:Connect(function()
					if Reference[v] then refreshAdornee(Reference[v]) end
				end)
				billboard.AncestryChanged:Connect(function()
					conn1:Disconnect()
					conn2:Disconnect()
				end)
			end
		end)
	end
	
	local function Removed(v)
		if Reference[v] then
			Reference[v]:Destroy()
			Reference[v] = nil
			ChestContents[v] = nil
		end
	end
	
	StorageESP = vape.Categories.Render:CreateModule({
		Name = 'StorageESP',
		Function = function(callback)
			if callback then
				local tagged = collectionService:GetTagged('chest')
				for _, v in ipairs(tagged) do
					Added(v)
				end
				
				StorageESP:Clean(collectionService:GetInstanceAddedSignal('chest'):Connect(Added))
				StorageESP:Clean(collectionService:GetInstanceRemovedSignal('chest'):Connect(Removed))
				StorageESP:Clean(task.spawn(function()
					while StorageESP.Enabled do
						task.wait(0.5)
						if not StorageESP.Enabled then break end
						for chest, billboard in pairs(Reference) do
							if not chest or not chest.Parent then
								Removed(chest)
							else
								refreshAdornee(billboard)
							end
						end
					end
				end))
			else
				for chest in pairs(Reference) do
					Removed(chest)
				end
			end
		end,
		Tooltip = 'shows what items are in a chest'
	})
	
	List = StorageESP:CreateTextList({
		Name = 'Item',
		Function = function()
			table.clear(ChestContents)
			for _, v in pairs(Reference) do
				refreshAdornee(v)
			end
		end
	})
	
	Background = StorageESP:CreateToggle({
		Name = 'Background',
		Function = function(callback)
			if Color.Object then Color.Object.Visible = callback end
			for _, v in pairs(Reference) do
				local icon = v:FindFirstChild('Icon')
				if icon then
					icon.BackgroundTransparency = callback and 0.3 or 1
				end
			end
		end,
		Default = true
	})
	
    Color = StorageESP:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, v in pairs(Reference) do
                v.Frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                v.Frame.BackgroundTransparency = 1 - opacity
            end
        end,
        Darker = true
    })

    task.defer(function()
        if Color and Color.Object then
            Color.Object.Visible = Background.Enabled  
        end
    end)
end)

run(function()
	local TrapESP
	local Background = {}
	local Color = {}
	local Reference = {}
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui

	local function Added(v, icon)
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = icon
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = v:IsA('Model') and (v.PrimaryPart or v:FindFirstChildWhichIsA('BasePart')) or v
		local blur = addBlur(billboard)
		blur.Visible = Background.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(36, 36)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
		image.BackgroundTransparency = 1 - (Background.Enabled and Color.Opacity or 0)
		image.BorderSizePixel = 0
		local result = bedwars.getIcon({itemType = icon}, true)
		image.Image = result		
		image.Image = result
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[v] = billboard
	end
	


	TrapESP = vape.Categories.Render:CreateModule({
		Name = 'TrapESP',
		Function = function(callback)
			if callback then
				TrapESP:Clean(collectionService:GetInstanceAddedSignal('snap_trap'):Connect(function(v)
					if tostring(v:GetAttribute("SnapTrapTeamId")) == lplr.Team.Name then
						return
					end
					Added(v, 'snap_trap')
				end))
				TrapESP:Clean(collectionService:GetInstanceRemovedSignal('snap_trap'):Connect(function(v)
					if tostring(v:GetAttribute("SnapTrapTeamId")) == lplr.Team.Name then
						return
					end
					if Reference[v] then
						Reference[v]:Destroy()
						Reference[v] = nil
					end
				end))
				for _, v in collectionService:GetTagged('snap_trap') do
					if tostring(v:GetAttribute("SnapTrapTeamId")) == lplr.Team.Name then
						return
					end
					Added(v, 'snap_trap')
				end
			else
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'allows you to see invisible traps'
	})
	Background = TrapESP:CreateToggle({
		Name = 'Background',
		Function = function(callback)
			if Color.Object then Color.Object.Visible = callback end
			for _, v in Reference do
				v.ImageLabel.BackgroundTransparency = 1 - (callback and Color.Opacity or 0)
				v.Blur.Visible = callback
			end
		end,
		Default = true,
				Visible = true
	})
	Color = TrapESP:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Function = function(hue, sat, val, opacity)
			for _, v in Reference do
				v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
				v.ImageLabel.BackgroundTransparency = 1 - opacity
			end
		end,
		Darker = true
	})
end)

run(function()
	local NameTagSpoofer
	local CustomNameBox
	local nametagConnection = nil
	local trackedElements = {}
	local fakeLabels = {}

	local function getCustomName()
		if CustomNameBox and type(CustomNameBox.Value) == "string" and CustomNameBox.Value ~= "" then
			return CustomNameBox.Value
		end
		return "Me"
	end

	local function trackElement(element)
		if not element then return end
		if not element:IsA("TextLabel") then return end
		if element.Name ~= "PlayerName" and element.Name ~= "EntityName" then return end
		if trackedElements[element] then
			pcall(function() element.Text = getCustomName() end)
			return
		end
		pcall(function()
			local t = element.Text
			if type(t) ~= "string" then return end
			if t:find(lplr.Name, 1, true) or t:find(lplr.DisplayName, 1, true) then
				trackedElements[element] = t
				element.Text = getCustomName()
			end
		end)
	end

	local function handlePlayerUsername(element)
		if not element or not element:IsA("TextBox") then return end
		if element.Name ~= "PlayerUsername" then return end
		if fakeLabels[element] then
			fakeLabels[element].Text = "@" .. getCustomName()
			return
		end
		pcall(function()
			local t = element.Text
			if type(t) ~= "string" then return end
			if t:find(lplr.Name, 1, true) or t:find(lplr.DisplayName, 1, true) then
				element.Visible = false
				element.TextTransparency = 1
				element.TextStrokeTransparency = 1
				local fake = Instance.new("TextLabel")
				fake.Name = "FakeUsername"
				fake.Size = element.Size
				fake.Position = element.Position
				fake.BackgroundTransparency = 1
				fake.TextColor3 = element.TextColor3
				fake.TextScaled = element.TextScaled
				fake.Font = element.Font
				fake.TextXAlignment = element.TextXAlignment
				fake.TextYAlignment = element.TextYAlignment
				fake.Text = "@" .. getCustomName()
				fake.ZIndex = element.ZIndex + 1
				fake.Parent = element.Parent
				fakeLabels[element] = fake
			end
		end)
	end

	local function hideAtLabel(element)
		if not element then return end
		if not element:IsA("TextLabel") then return end
		if element.Name ~= "@" then return end
		pcall(function()
			local parent = element.Parent
			if parent and parent.Name == "PlayerUsername" then
				element.Visible = false
			end
		end)
	end

	local function processGui(gui)
		if not gui then return end
		pcall(function()
			for _, desc in pairs(gui:GetDescendants()) do
				trackElement(desc)
				handlePlayerUsername(desc)
				hideAtLabel(desc)
			end
		end)
	end

	NameTagSpoofer = vape.Categories.Render:CreateModule({
		Name = 'NameTagSpoofer',
		Tooltip = 'script made by sleepvs w mans',
		Function = function(callback)
			if callback then
				NameTagSpoofer:Clean(lplr.PlayerGui.ChildAdded:Connect(function(gui)
					if gui.Name == "TabListScreenGui" then
						task.wait(0.3)
						processGui(gui)
						NameTagSpoofer:Clean(gui.DescendantAdded:Connect(function(desc)
							task.wait()
							trackElement(desc)
							handlePlayerUsername(desc)
							hideAtLabel(desc)
						end))
					end
					if gui.Name == "KillFeedGui" then
						processGui(gui)
						NameTagSpoofer:Clean(gui.DescendantAdded:Connect(function(desc)
							task.wait()
							trackElement(desc)
						end))
					end
				end))

				local killFeed = lplr.PlayerGui:FindFirstChild("KillFeedGui")
				if killFeed then
					processGui(killFeed)
					NameTagSpoofer:Clean(killFeed.DescendantAdded:Connect(function(desc)
						task.wait()
						trackElement(desc)
					end))
				end

				nametagConnection = runService.RenderStepped:Connect(function()
					if not NameTagSpoofer.Enabled then return end
					pcall(function()
						local customName = getCustomName()

						for element, original in pairs(trackedElements) do
							if not element or not element.Parent then
								trackedElements[element] = nil
							else
								pcall(function() element.Text = customName end)
							end
						end

						for element, fake in pairs(fakeLabels) do
							if not element or not element.Parent then
								if fake then fake:Destroy() end
								fakeLabels[element] = nil
							else
								pcall(function() fake.Text = "@" .. customName end)
							end
						end

						local tl = lplr.PlayerGui:FindFirstChild("TabListScreenGui")
						if tl then processGui(tl) end

						local kf = lplr.PlayerGui:FindFirstChild("KillFeedGui")
						if kf then processGui(kf) end

						if lplr.Character then
							local head = lplr.Character:FindFirstChild("Head")
							if not head then return end
							local nametag = head:FindFirstChild("Nametag")
							if not nametag then return end
							local dc = nametag:FindFirstChild("DisplayNameContainer")
							if not dc then return end
							local dn = dc:FindFirstChild("DisplayName")
							if not dn or not dn:IsA("TextLabel") then return end
							pcall(function() dn.Text = customName end)
						end
					end)
				end)

			else
				if nametagConnection then
					nametagConnection:Disconnect()
					nametagConnection = nil
				end
				for element, original in pairs(trackedElements) do
					if element and element.Parent then
						pcall(function() element.Text = original end)
					end
				end
				table.clear(trackedElements)
				for element, fake in pairs(fakeLabels) do
					if fake then pcall(function() fake:Destroy() end) end
					if element and element.Parent then
						pcall(function()
							element.Visible = true
							element.TextTransparency = 0
							element.TextStrokeTransparency = 0
						end)
					end
				end
				table.clear(fakeLabels)
				local tl = lplr.PlayerGui:FindFirstChild("TabListScreenGui")
				if tl then
					for _, desc in pairs(tl:GetDescendants()) do
						if desc:IsA("TextLabel") and desc.Name == "@" then
							pcall(function() desc.Visible = true end)
						end
					end
				end
			end
		end,
		Tooltip = 'customize ur name in varius places'
	})

	CustomNameBox = NameTagSpoofer:CreateTextBox({
		Name = 'Custom Name',
		Default = 'Me',
		Placeholder = 'Enter name...',
		Function = function(value)
		end
	})
end)

run(function()
	local ArmorTrims
	local TrimType
	local TrimEffect
	local TrimRank
	local TrimColor

	local RS = game:GetService('ReplicatedStorage')
	local ANCHORS = {
		boots_left_trim = 'LeftFoot',
		boots_right_trim = 'RightFoot',
		chestplate_left_shoulder_trim = 'LeftUpperArm',
		chestplate_right_shoulder_trim = 'RightUpperArm',
		chestplate_trim = 'UpperTorso',
		chestplate_lower_trim = 'LowerTorso',
		helmet_trim = 'Head'
	}
	local EFFECTS = {'default', 'void', 'spirit', 'phoenix', 'bat', 'anniversary', 'frosty', 'fire_warm', 'fire_cold', 'fire_red', 'fire_black', 'fire_purple'}
	local RANKS = {'T1', 'T2', 'T3', 'T4', 'T5', 'T6', 'T7'}
	local COLORS = {'RED', 'ORANGE', 'YELLOW', 'GREEN', 'BLUE', 'INDIGO', 'PURPLE', 'WHITE', 'BLACK', 'BROWN', 'PINK'}
	local TRIMS = {}
	for i = 1, 12 do TRIMS[i] = 'trim_' .. i end

	local spawned = {}
	local cache = {}
	local controller = nil
	local applyToken = 0

	local function cached(path, key)
		local id = path .. '|' .. tostring(key)
		local hit = cache[id]
		if hit ~= nil then
			return hit ~= false and hit or nil
		end
		local ok, mod = pcall(function()
			return require(RS.TS['armor-trim'][path])
		end)
		local value = nil
		if ok and type(mod) == 'table' then
			value = key and mod[key] or mod
		end
		cache[id] = value == nil and false or value
		return value
	end

	local function colorIndex()
		local m = cached('armor-trim-colors', 'ArmorTrimColor')
		return m and m[TrimColor.Value] or 0
	end

	local function rankIndex()
		local m = cached('armor-trim-rank', 'ArmorTrimEffectRank')
		return m and m[TrimRank.Value] or 0
	end

	local function colorValue()
		local m = cached('armor-trim-color-meta', 'ArmorTrimColorMeta')
		local entry = m and m[colorIndex()]
		return entry and entry.color or Color3.new(1, 1, 1)
	end

	local function rankMaterial()
		local m = cached('armor-trim-rank', 'ArmorTrimEffectRankMeta')
		local entry = m and m[rankIndex()]
		return entry and entry.material or Enum.Material.Plastic
	end

	local function getController()
		if controller then return controller end
		local ok, Knit = pcall(function()
			return require(RS.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
		end)
		if ok and Knit then
			controller = Knit.Controllers.ArmorTrimController
		end
		return controller
	end

	local function clearAccessories()
		for _, acc in spawned do
			pcall(function() acc:Destroy() end)
		end
		table.clear(spawned)
	end

	local function setAttributes()
		local char = lplr.Character
		local trim, effect, rank, color = TrimType.Value, TrimEffect.Value, rankIndex(), colorIndex()
		for _, target in {lplr, char} do
			if target then
				pcall(function()
					target:SetAttribute('ArmorTrimType', trim)
					target:SetAttribute('ArmorTrimEffectType', effect)
					target:SetAttribute('ArmorTrimEffectRank', rank)
					target:SetAttribute('ArmorTrimColor', color)
				end)
			end
		end
	end

	local function refreshEffects()
		local char = lplr.Character
		local ctrl = getController()
		if not char or not ctrl then return end
		for _, acc in char:GetChildren() do
			if acc:IsA('Accessory') and ANCHORS[acc.Name] then
				if not pcall(function() ctrl:attachArmorTrim(lplr, acc) end) then
					pcall(function() ctrl:attachArmorTrim(acc, lplr) end)
				end
			end
		end
	end

	local function spawnAccessories()
		clearAccessories()
		local char = lplr.Character
		local hum = char and char:FindFirstChildOfClass('Humanoid')
		if not hum then return end
		local folder = RS:FindFirstChild('Assets')
		folder = folder and folder:FindFirstChild('ArmorTrims')
		folder = folder and folder:FindFirstChild(TrimType.Value)
		if not folder then return end

		local col = colorValue()
		local mat = rankMaterial()

		local owned = {}
		for _, existing in char:GetChildren() do
			if existing:IsA('Accessory') and ANCHORS[existing.Name] then
				owned[existing.Name] = true
			end
		end

		for _, acc in folder:GetChildren() do
			if acc:IsA('Accessory') and ANCHORS[acc.Name] and not owned[acc.Name] then
				local clone = acc:Clone()
				for _, d in clone:GetDescendants() do
					if d:IsA('BasePart') then
						d.Color = col
						d.Material = mat
						d.CanCollide = false
						d.CanQuery = false
						d.CanTouch = false
						d.Massless = true
					elseif d:IsA('TouchTransmitter') then
						pcall(function() d:Destroy() end)
					end
				end
				if not pcall(function() hum:AddAccessory(clone) end) then
					local handle = clone:FindFirstChild('Handle')
					local anchor = char:FindFirstChild(ANCHORS[acc.Name])
					if handle and anchor then
						handle.CFrame = anchor.CFrame
						local weld = Instance.new('WeldConstraint')
						weld.Part0 = anchor
						weld.Part1 = handle
						weld.Parent = handle
						clone.Parent = char
					else
						clone:Destroy()
						clone = nil
					end
				end
				if clone then
					table.insert(spawned, clone)
				end
			end
		end
	end

	local function applyAll()
		if not (ArmorTrims and ArmorTrims.Enabled) then return end
		applyToken = applyToken + 1
		local token = applyToken
		task.spawn(function()
			task.wait(0.1)
			if token ~= applyToken then return end
			if not ArmorTrims.Enabled then return end
			setAttributes()
			spawnAccessories()
			refreshEffects()
		end)
	end

	ArmorTrims = vape.Categories.Render:CreateModule({
		Name = 'ArmorTrims',
		Function = function(callback)
			if callback then
				applyAll()
				ArmorTrims:Clean(lplr.CharacterAdded:Connect(function(char)
					task.spawn(function()
						char:WaitForChild('Humanoid', 10)
						task.wait(1)
						if ArmorTrims.Enabled then applyAll() end
					end)
				end))
				ArmorTrims:Clean(clearAccessories)
			else
				applyToken = applyToken + 1
				clearAccessories()
				local char = lplr.Character
				for _, target in {lplr, char} do
					if target then
						pcall(function()
							target:SetAttribute('ArmorTrimEffectType', nil)
							target:SetAttribute('ArmorTrimEffectRank', nil)
						end)
					end
				end
			end
		end,
		Tooltip = 'gives ur armor a trim with effects, only u can see it'
	})

	vape:Clean(clearAccessories)

	TrimType = ArmorTrims:CreateDropdown({
		Name = 'Trim Type',
		List = TRIMS,
		Default = 'trim_1',
		Function = function() applyAll() end
	})
	TrimEffect = ArmorTrims:CreateDropdown({
		Name = 'Effect',
		List = EFFECTS,
		Default = 'void',
		Function = function() applyAll() end,
		Tooltip = 'the particles on ur trim'
	})
	TrimRank = ArmorTrims:CreateDropdown({
		Name = 'Rank',
		List = RANKS,
		Default = 'T7',
		Function = function() applyAll() end,
		Tooltip = 'higher tier looks crazier, T7 is neon'
	})
	TrimColor = ArmorTrims:CreateDropdown({
		Name = 'Color',
		List = COLORS,
		Default = 'RED',
		Function = function() applyAll() end
	})
end)

run(function()
    local BlockSelectorColor
    local Fill
    local Outline
    local RunService = game:GetService("RunService")
    local updateConnection
    local boxes = {}

    local function applyBox(box)
        box.Color3 = Color3.fromHSV(Outline.Hue, Outline.Sat, Outline.Value)
        box.Transparency = 1 - Outline.Opacity
        box.SurfaceColor3 = Color3.fromHSV(Fill.Hue, Fill.Sat, Fill.Value)
        box.SurfaceTransparency = 1 - Fill.Opacity
    end

    local function UpdateAllBoxes()
        for box in pairs(boxes) do
            if box.Parent then
                applyBox(box)
            else
                boxes[box] = nil
            end
        end
    end

    BlockSelectorColor = vape.Categories.Render:CreateModule({
        Name = 'BlockSelectorColor',
        Function = function(callback)
            if callback then
                table.clear(boxes)
                scanDescendants(workspace, function(box)
                    if box:IsA("SelectionBox") then
                        boxes[box] = true
                    end
                end, BlockSelectorColor)
                BlockSelectorColor:Clean(workspace.DescendantAdded:Connect(function(v)
                    if v:IsA("SelectionBox") then
                        boxes[v] = true
                        applyBox(v)
                    end
                end))
                BlockSelectorColor:Clean(workspace.DescendantRemoving:Connect(function(v)
                    if v:IsA("SelectionBox") then
                        boxes[v] = nil
                    end
                end))
                updateConnection = RunService.RenderStepped:Connect(UpdateAllBoxes)
            else
                if updateConnection then
                    updateConnection:Disconnect()
                    updateConnection = nil
                end
                table.clear(boxes)
            end
        end,
        Tooltip = 'change your block placement outline color'
    })

    Fill = BlockSelectorColor:CreateColorSlider({
        Name = 'Overlay Color',
        DefaultOpacity = 0.5
    })
    Outline = BlockSelectorColor:CreateColorSlider({
        Name = 'Outline Color',
        DefaultOpacity = 1
    })
end)

run(function()
	local NameTags
	local StreamProof
	local Targets
	local Color
	local Background
	local DisplayName
	local Health
	local HealthColorToggle
	local HealthColorFull
	local HealthColorMid
	local HealthColorLow
	local Distance
	local Equipment
	local Loot
	local Potions
	local potionDefs = {
		{name = 'PotSerpent', attr = 'StatusEffect_serpents_touch_potion', item = 'serpents_touch_potion', timed = true},
		{name = 'PotFury', attr = 'StatusEffect_fury_potion', item = 'fury_potion', timed = true},
		{name = 'PotInvis', attr = 'StatusEffect_invisibility', item = 'invisibility_potion', timed = true},
		{name = 'PotJump', attr = 'JumpBoost', item = 'jump_potion', timed = false},
		{name = 'PotPie', attr = 'SpeedPieBuff', item = 'speed_pie', timed = false}
	}
	local function potionActive(char, def)
		if not char then return false end
		local val = char:GetAttribute(def.attr)
		if val == nil then return false end
		if def.timed then
			return type(val) == 'number' and val > workspace:GetServerTimeNow()
		end
		return val ~= false and val ~= 0
	end
	local Boss
	local ShowKits
	local KitTracker
	local Rank
	local DeviceIcon
	local GloopIndicator
	local Enchant
	local Scale
	local FontOption
	local Teammates
	local DistanceCheck
	local DistanceLimit
	local Strings, Sizes, Reference = {}, {}, {}
	local bossNametags = {}
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local methodused
	local Removed
	local lastUpdate = {}
	local kitCache = {}
	local lootCache = {}
	local equipmentCache = {}
	local enchantCache = {}
	local healthCache = {}
	local charCache = {}
	local enchantConnections = {}
	local gloopConnections = {}
	local kitTrackerConnections = {}
	local billboardCache = {}
	local tick = tick
	local math_floor = math.floor
	local math_round = math.round
	local math_clamp = math.clamp
	local math_huge = math.huge
	local string_format = string.format
	local vector2new = Vector2.new
	local vector3new = Vector3.new
	local color3fromHSV = Color3.fromHSV
	local color3new = Color3.new
	
	local function getTeamColor(ent)
		local plr = ent and ent.Player
		if not plr then return nil end
		local theirTeam = tonumber(plr:GetAttribute('Team'))
		if not theirTeam then return nil end
		local myTeam = tonumber(lplr:GetAttribute('Team'))
		if myTeam and myTeam == theirTeam then return nil end
		local entry = getgenv().aeroTeamColors[theirTeam]
		return entry and entry.color
	end
	local udim2fromOffset = UDim2.fromOffset

	local function getChainsawImage(level)
		if not level or level < 1 or level > 5 then return '' end
		local success, img = pcall(function()
			local imageModule = require(game:GetService("ReplicatedStorage"):WaitForChild("TS"):WaitForChild("image"):WaitForChild("image-id"))
			local ids = {
				[1] = imageModule.BedwarsImageId.WOOD_TINKER_MECH,
				[2] = imageModule.BedwarsImageId.IRON_TINKER_MECH,
				[3] = imageModule.BedwarsImageId.DIAMOND_TINKER_MECH,
				[4] = imageModule.BedwarsImageId.EMERALD_TINKER_MECH,
				[5] = imageModule.BedwarsImageId.VOID_TINKER_MECH,
			}
			return ids[level]
		end)
		if success and img then
			return img
		end
		local fallback = {
			[1] = "rbxassetid://1234567890", 
			[2] = "rbxassetid://1234567891", 
			[3] = "rbxassetid://1234567892", 
			[4] = "rbxassetid://1234567893", 
			[5] = "rbxassetid://1234567894", 
		}
		return fallback[level] or ''
	end

	local function getHealthColor(ent)
		local ratio = math.clamp((ent.Health or 0) / (ent.MaxHealth and ent.MaxHealth > 0 and ent.MaxHealth or 1), 0, 1)
		if HealthColorToggle and HealthColorToggle.Enabled then
			local fullC = Color3.fromHSV(HealthColorFull.Hue, HealthColorFull.Sat, HealthColorFull.Value)
			local midC  = Color3.fromHSV(HealthColorMid.Hue,  HealthColorMid.Sat,  HealthColorMid.Value)
			local lowC  = Color3.fromHSV(HealthColorLow.Hue,  HealthColorLow.Sat,  HealthColorLow.Value)
			if ratio >= 0.5 then
				return fullC:Lerp(midC, (1 - ratio) / 0.5)
			else
				return midC:Lerp(lowC, (0.5 - ratio) / 0.5)
			end
		else
			if ratio >= 0.7 then
				return Color3.fromRGB(0, 255, 0)
			elseif ratio >= 0.3 then
				return Color3.fromRGB(255, 255, 0)
			elseif ratio >= 0.1 then
				return Color3.fromRGB(255, 165, 0)
			else
				return Color3.fromRGB(255, 0, 0)
			end
		end
	end

	local function getHealthColorStr(ent)
		local c = getHealthColor(ent)
		return 'rgb('..math.floor(c.R*255)..','..math.floor(c.G*255)..','..math.floor(c.B*255)..')'
	end

	local enchantImageMap = nil
	local function buildEnchantMap()
		if enchantImageMap then return enchantImageMap end
		enchantImageMap = {}
		task.spawn(function()
			if vape.ThreadFix then setthreadidentity(8) end
			local ok, meta = pcall(function()
				return require(game:GetService('ReplicatedStorage').TS.enchant['enchant-meta'])
			end)
			if not ok or not meta then return end
			for _, subMeta in pairs({meta.EnchantMeta, meta.ToolEnchantMeta, meta.ArmorEnchantMeta}) do
				if type(subMeta) == 'table' then
					for _, v in pairs(subMeta) do
						if type(v) == 'table' and v.statusEffect and v.image then
							enchantImageMap[v.statusEffect] = v.image
						end
					end
				end
			end
		end)
		return enchantImageMap
	end

	local function getActiveEnchantImage(char)
		if not char then return '' end
		local map = buildEnchantMap()
		for attr, val in pairs(char:GetAttributes()) do
			if attr:sub(1, 13) == 'StatusEffect_' and type(val) == 'number' and val < 0 then
				local effectName = attr:sub(14)
				if not effectName:find('stacks') then
					local img = map[effectName]
					if img and img ~= '' then return img end
				end
			end
		end
		return ''
	end

	local function getBossDisplayName(ent)
		if not ent.NPC or not ent.Character then return nil end
		local char = ent.Character
		if char:GetAttribute("BossType") == "Bhaa" then
			return "Bhaa"
		end
		if char.Name:lower() == "bhaa" then
			return "Bhaa"
		end
		if char:FindFirstChild("BhaaModel") or char:FindFirstChild("BhaaHead") then
			return "Bhaa"
		end
		if char.Name == "Titan" then
			return "Titan"
		end
		return nil
	end

	local function shouldShow(ent)
		if not Targets.Players.Enabled and ent.Player then return false end
		local isBoss = ent.NPC and ent.Character and getBossDisplayName(ent) ~= nil
		if isBoss then
			if not Boss.Enabled then return false end
		else
			if not Targets.NPCs.Enabled and ent.NPC then return false end
			if Teammates.Enabled and (ent.Player and lplr:GetAttribute('Team') and ent.Player:GetAttribute('Team') == lplr:GetAttribute('Team')) and (not ent.Friend) then return false end
		end
		return true
	end

	local Added = {
		Normal = function(ent)
			if Reference[ent] then Removed.Normal(ent) end
			for oldEnt in Reference do
				if oldEnt ~= ent and (not oldEnt.Character or not oldEnt.Character.Parent or (oldEnt.Player and ent.Player and oldEnt.Player == ent.Player)) then
					Removed.Normal(oldEnt)
				end
			end
			if not Targets.Players.Enabled and ent.Player then return end
			local bossDisplayName = ent.NPC and ent.Character and getBossDisplayName(ent) or nil
			local isBoss = bossDisplayName ~= nil
			if isBoss then
				if not Boss.Enabled then return end
			else
				if not Targets.NPCs.Enabled and ent.NPC then return end
				if Teammates.Enabled and (ent.Player and lplr:GetAttribute('Team') and ent.Player:GetAttribute('Team') == lplr:GetAttribute('Team')) and (not ent.Friend) then return end
			end

			local entityName = bossDisplayName or (ent.Player and nil) or ent.Character.Name
			Strings[ent] = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or entityName

			if Health.Enabled then
				local colorStr = getHealthColorStr(ent)
				Strings[ent] = Strings[ent]..' <font color="'..colorStr..'">'..math.round(ent.Health)..'</font>'
			end

			if Distance.Enabled then
				Strings[ent] = '[%s] ' .. Strings[ent]
			end
			local textSize = 14 * Scale.Value
			local fontFace = FontOption.Value
			local size = getfontsize(removeTags(Strings[ent]), textSize, fontFace, vector2new(100000, 100000))
			local nametag = Instance.new('TextLabel')
			nametag.Name = ent.Player and ent.Player.Name or ent.Character.Name
			nametag.Size = udim2fromOffset(size.X + 8, size.Y + 7)
			nametag.AnchorPoint = vector2new(0.5, 1)
			nametag.BackgroundColor3 = color3new()
			nametag.BackgroundTransparency = Background.Value
			nametag.BorderSizePixel = 0
			nametag.Visible = false
			nametag.Text = Strings[ent]
			nametag.TextColor3 = getTeamColor(ent) or color3fromHSV(Color.Hue, Color.Sat, Color.Value)
			nametag.RichText = true
			nametag.TextSize = textSize
			nametag.FontFace = fontFace
			nametag.Parent = Folder

			if KitTracker and KitTracker.Enabled and ent.Player then
			  pcall(function()
				local vkRoman = {'I','II','III','IV','V'}
				local ktLabel = Instance.new('TextLabel')
				ktLabel.Name = 'KitTrackerLabel'
				ktLabel.BackgroundTransparency = 1
				ktLabel.TextScaled = false
				ktLabel.TextSize = 13
				ktLabel.FontFace = FontOption.Value
				ktLabel.RichText = true
				ktLabel.AnchorPoint = Vector2.new(1, 0.5)
				ktLabel.Position = UDim2.new(0, -2, 0.5, 0)
				ktLabel.Size = UDim2.new(0, 40, 1, 0)
				ktLabel.ZIndex = 2
				ktLabel.Parent = nametag

				local stroke = Instance.new('UIStroke')
				stroke.Color = Color3.new(0, 0, 0)
				stroke.Thickness = 1.5
				stroke.Parent = ktLabel

				local ktEventsBound = false
				local function updateKtLabel()
					if not KitTracker.Enabled then ktLabel.Text = '' return end
					local playerKit = ent.Player:GetAttribute('PlayingAsKits') or 'none'
					if playerKit == 'void_knight' then
						local tier = ent.Player:GetAttribute('VoidKnightTier') or 0
						if tier > 0 then
							ktLabel.Text = '【' .. (vkRoman[tier] or tostring(tier)) .. '】'
							ktLabel.TextColor3 = Color3.fromRGB(180, 80, 255)
						else
							ktLabel.Text = ''
						end
					elseif playerKit == 'block_kicker' then
						local char = ent.Character
						local count = (char and char:GetAttribute('BlockKickerKit_BlockCount')) or ent.Player:GetAttribute('BlockKickerKit_BlockCount') or 0
						ktLabel.Text = count > 0 and '【' .. tostring(count) .. '】' or ''
						ktLabel.TextColor3 = Color3.fromRGB(100, 210, 100)
					elseif playerKit == 'elk_master' then
						local char = ent.Character
						local isMounted = char and char:FindFirstChild('elk') ~= nil
						if isMounted then
							ktLabel.Text = '【🦌】'
							ktLabel.TextColor3 = Color3.fromRGB(160, 220, 80)
						else
							ktLabel.Text = ''
						end
					elseif playerKit == 'summoner' then
						local tier = ent.Player:GetAttribute("Summoner_ClawLevel") or 0
						if not tier or tier == 0 then ktLabel.Text = '' return end
						ktLabel.Text = '【 ' .. (vkRoman[tier] or tostring(tier)) .. ' 】'
						ktLabel.TextColor3 = Color3.fromRGB(191, 0, 255)
					elseif playerKit == 'paladin' then
						ktLabel.Text = '【 🪽 】'
						ktLabel.TextColor3 = Color3.fromRGB(255, 242, 150)
					elseif playerKit == 'davey' then
						local tier = ent.Character and ent.Character:GetAttribute('StatusEffect_powdered_stacks') or 0
						if tier > 0 then
							ktLabel.Text = '【 ' .. tostring(tier) .. ' 】'
						else
							ktLabel.Text = ''
						end
						ktLabel.TextColor3 = Color3.fromRGB(255, 105, 130)
					elseif playerKit == 'airbender' then
						ktLabel.Text = ''
						ktLabel.TextColor3 = Color3.fromRGB(255, 200, 80)
						if not ktEventsBound then
							ktEventsBound = true
							if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
							task.spawn(function()
								table.insert(kitTrackerConnections[ent], bedwars.Client:Get("Airbender_UseTornadoFromServer"):Connect(function(p12)
									if not KitTracker.Enabled then ktLabel.Text = '' return end
									if p12.tornadoData.owner == ent.Player then
										ktLabel.Text = '【 🌪️ 】'
									end
								end))
								table.insert(kitTrackerConnections[ent], bedwars.Client:Get("Airbender_EndTornadoFromServer"):Connect(function(p13)
									if not KitTracker.Enabled then ktLabel.Text = '' return end
									if p13.tornadoData.owner == ent.Player then
										ktLabel.Text = ''
									end
								end))
							end)
						end
					elseif playerKit == 'hatter' then
						ktLabel.Text = ''
						ktLabel.TextColor3 = Color3.fromRGB(45, 15, 80)
						if not ktEventsBound then
							ktEventsBound = true
							if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
							task.spawn(function()
								if vape.ThreadFix then setthreadidentity(8) end
								table.insert(kitTrackerConnections[ent], bedwars.Client:OnEvent("HatterUseTeleport", function(p14)
									if vape.ThreadFix then setthreadidentity(8) end
									if not KitTracker.Enabled then ktLabel.Text = '' return end
									local dtc = p14.arriveTime - workspace:GetServerTimeNow() + 0.05
									if p14.hatterPlayer == ent.Player then
										ktLabel.Text = '【 🎩 】'
										task.wait(dtc)
										if vape.ThreadFix then setthreadidentity(8) end
										ktLabel.Text = ''
									else
										ktLabel.Text = ''
									end
								end))
							end)
						end
					elseif playerKit == 'black_market_trader' then
						ktLabel.Text = ''
						ktLabel.TextColor3 = Color3.fromRGB(30, 10, 60)
						if not ktEventsBound then
							ktEventsBound = true
							if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
							task.spawn(function()
								table.insert(kitTrackerConnections[ent], bedwars.Client:Get("BlackMarketPlaceShop"):Connect(function(p15)
									if not KitTracker.Enabled then ktLabel.Text = '' return end
									if p15.shopOwnerUserId == ent.Player.UserId then
										ktLabel.Text = '【 🏪 】'
									end
								end))
								local worldFolder = getWorldFolder()
								local blocks = worldFolder:WaitForChild("Blocks", math.huge)
								table.insert(kitTrackerConnections[ent], blocks.ChildAdded:Connect(function(obj)
									if obj.Name == 'black_market_shop' and obj:GetAttribute('PlacedByUserId') == ent.Player.UserId then
									local billboard = Instance.new('BillboardGui')
									billboard.Parent = obj
									billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
									billboard.Size = UDim2.fromOffset(36, 36)
									billboard.AlwaysOnTop = true
									billboard.ClipsDescendants = false
									local blur = addBlur(billboard)
									blur.Visible = Background.Enabled
									local image = Instance.new('ImageLabel')
									image.Size = UDim2.fromOffset(36, 36)
									image.Position = UDim2.fromScale(0.5, 0.5)
									image.AnchorPoint = Vector2.new(0.5, 0.5)
									image.BackgroundColor3 = Color3.fromHSV(0, 0, 0)
									image.BackgroundTransparency = 0.85
									image.BorderSizePixel = 0
									image.Image = bedwars.getIcon({itemType = 'shadow_coin'}, true)
									image.Parent = billboard
									local uicorner = Instance.new('UICorner')
									uicorner.CornerRadius = UDim.new(0, 4)
									uicorner.Parent = image
									if not billboardCache[ent] then billboardCache[ent] = {} end
									table.insert(billboardCache[ent], billboard)
									end
								end))
							end)
						end
					elseif playerKit == 'wizard' then
						ktLabel.TextColor3 = Color3.fromRGB(100, 70, 220)
						local function getStaffTier(v)
							local inv = store.inventories[v] and store.inventories[v].items or {}
							for _, item in inv do
								local nowstr = string.lower(item.itemType or '')
								if string.find(nowstr, 'wizard_staff') then
									return tonumber(item.itemType:sub(#item.itemType, #item.itemType)) or 1
								end
							end
						end
						local tier = getStaffTier(ent.Player)
						if not tier or tier < 0 then ktLabel.Text = '' return end
						ktLabel.Text = '【' .. (vkRoman[tier] or tostring(tier)) .. '】'
					elseif playerKit == 'aery' then
						ktLabel.TextColor3 = Color3.fromRGB(130, 210, 255)
						local stacks = ent.Player:GetAttribute('AeryStacks') or 0
						ktLabel.Text = '【' .. tostring(stacks) .. '】'
					elseif playerKit == 'mimic' then
						ktLabel.TextColor3 = Color3.fromRGB(180, 210, 60)
						ktLabel.Text = ''
						if not ktEventsBound then
							ktEventsBound = true
							if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
							task.spawn(function()
								if vape.ThreadFix then setthreadidentity(8) end
								table.insert(kitTrackerConnections[ent], bedwars.Client:OnEvent("ValidatedMimicBlock", function(p14)
									if not KitTracker.Enabled then ktLabel.Text = '' return end
									if vape.ThreadFix then setthreadidentity(8) end
									if p14.player == ent.Player then
										ktLabel.Text = '【 👔 】'
									else
										ktLabel.Text = ''
									end
								end))
								local val = workspace:FindFirstChild('DisguisedPlayerBlock_' .. ent.Player.UserId) ~= nil
								if val then ktLabel.Text = '【 👔 】' end
							end)
						end
					elseif playerKit == 'disruptor' then
						ktLabel.TextColor3 = Color3.fromRGB(40, 180, 140)
						if not ktEventsBound then
							ktEventsBound = true
							if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
							task.spawn(function()
								local worldFolder = getWorldFolder()
								local blocks = worldFolder:WaitForChild("Blocks", math.huge)
								table.insert(kitTrackerConnections[ent], blocks.ChildAdded:Connect(function(obj)
									if obj.Name == 'satellite_dish' and obj:GetAttribute('PlacedByUserId') == ent.Player.UserId then
									local billboard = Instance.new('BillboardGui')
									billboard.Parent = obj
									billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
									billboard.Size = UDim2.fromOffset(36, 36)
									billboard.AlwaysOnTop = true
									billboard.ClipsDescendants = false
									local blur = addBlur(billboard)
									blur.Visible = Background.Enabled
									local image = Instance.new('ImageLabel')
									image.Size = UDim2.fromOffset(36, 36)
									image.Position = UDim2.fromScale(0.5, 0.5)
									image.AnchorPoint = Vector2.new(0.5, 0.5)
									image.BackgroundColor3 = Color3.fromHSV(0, 0, 0)
									image.BackgroundTransparency = 0.85
									image.BorderSizePixel = 0
									image.Image = bedwars.getIcon({itemType = 'satellite_dish'}, true)
									image.Parent = billboard
									local uicorner = Instance.new('UICorner')
									uicorner.CornerRadius = UDim.new(0, 4)
									uicorner.Parent = image
									if not billboardCache[ent] then billboardCache[ent] = {} end
									table.insert(billboardCache[ent], billboard)
									end
								end))
							end)
						end
						ktLabel.Text = '【 ' .. (ent.Player:GetAttribute('DisruptorTarget') and ent.Player:GetAttribute('DisruptorTarget') .. ' Team' or 'Unknown Team') .. ' 】'
					elseif playerKit == 'winter_lady' then
						ktLabel.TextColor3 = Color3.fromRGB(200, 225, 255)
						local function getWandTier(v)
							local inv = store.inventories[v] and store.inventories[v].items or {}
							for _, item in inv do
								local nowstr = string.lower(item.itemType or '')
								if string.find(nowstr, 'frost_staff') then
									return tonumber(item.itemType:sub(#item.itemType, #item.itemType)) or 1
								end
							end
						end
						local tier = getWandTier(ent.Player)
						if not tier or tier < 0 then ktLabel.Text = '' return end
						ktLabel.Text = '【' .. (vkRoman[tier] or tostring(tier)) .. '】'
					elseif playerKit == 'warrior' then
						ktLabel.TextColor3 = Color3.fromRGB(220, 60, 60)
						local grit = ent.Player:GetAttribute('Grit') or 0
						ktLabel.Text = '【 ' .. tostring(grit) .. '/100】'
					else
						ktLabel.Text = ''
					end
				end

				updateKtLabel()

				if ent.Character then
					if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
					table.insert(kitTrackerConnections[ent], ent.Character.AttributeChanged:Connect(function(attr)
						if attr == 'BlockKickerKit_BlockCount' or attr == 'StatusEffect_powdered_stacks' then
							updateKtLabel()
						end
					end))
					table.insert(kitTrackerConnections[ent], ent.Player.AttributeChanged:Connect(function(attr)
						if attr == 'DisruptorTarget' or attr == 'DisruptorActivation'
						or attr == 'AeryStacks' or attr == 'VoidKnightTier'
						or attr == 'PaladinStartTime' or attr == 'Summoner_ClawLevel'
						or attr == 'Grit' then
							updateKtLabel()
						end
					end))
					table.insert(kitTrackerConnections[ent], ent.Character.ChildAdded:Connect(function(child)
						if child.Name == 'elk' or string.find(child.Name, "wizard_staff") or string.find(child.Name, "frost_staff") then
							updateKtLabel()
						end
					end))
					table.insert(kitTrackerConnections[ent], ent.Character.ChildRemoved:Connect(function(child)
						if child and (child.Name == 'elk' or string.find(child.Name, "wizard_staff") or string.find(child.Name, "frost_staff")) then
							updateKtLabel()
						end
					end))
					table.insert(kitTrackerConnections[ent], workspace.ChildAdded:Connect(function(child)
						if child and child.Name:sub(1, 20) == 'DisguisedPlayerBlock' then
							updateKtLabel()
						end
					end))
					table.insert(kitTrackerConnections[ent], workspace.ChildRemoved:Connect(function(child)
						if child and child.Name:sub(1, 20) == 'DisguisedPlayerBlock' then
							updateKtLabel()
						end
					end))
				end
				if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
				table.insert(kitTrackerConnections[ent], ent.Player:GetAttributeChangedSignal('PlayingAsKits'):Connect(updateKtLabel))
			  end)
			end

			if Potions.Enabled then
				local baseX = Loot.Enabled and 45 or -39
				for i, def in potionDefs do
					local iconSc = Scale.Value
					local Icon = Instance.new('ImageLabel')
					Icon.Name = def.name
					Icon.Size = udim2fromOffset(26 * iconSc, 26 * iconSc)
					Icon.Position = udim2fromOffset((baseX + ((i - 1) * 28)) * iconSc, (Equipment.Enabled and -58 or -30) * iconSc)
					Icon.BackgroundTransparency = 1
					Icon.Image = ''
					Icon.Parent = nametag
				end
			end

			if Loot.Enabled then
				local lootDefs = { LootIron = 'iron', LootDiamond = 'diamond', LootEmerald = 'emerald' }
				for i, v in { 'LootIron', 'LootDiamond', 'LootEmerald' } do
					local iconSc = Scale.Value
					local Icon = Instance.new('ImageLabel')
					Icon.Name = v
					Icon.Size = udim2fromOffset(26 * iconSc, 26 * iconSc)
					Icon.Position = udim2fromOffset((-39 + ((i - 1) * 28)) * iconSc, (Equipment.Enabled and -58 or -30) * iconSc)
					Icon.BackgroundTransparency = 1
					Icon.Image = ''
					Icon.Parent = nametag
					local amt = Instance.new('TextLabel')
					amt.Name = 'Amt'
					amt.AnchorPoint = Vector2.new(0, 1)
					amt.Position = UDim2.new(0, 0, 1, 0)
					amt.Size = UDim2.new(0, 26 * iconSc, 0, 10 * iconSc)
					amt.BackgroundTransparency = 1
					amt.Text = ''
					amt.TextColor3 = Color3.fromRGB(255, 255, 255)
					amt.TextStrokeTransparency = 0.4
					amt.TextSize = 9 * iconSc
					amt.Font = Enum.Font.GothamBold
					amt.TextXAlignment = Enum.TextXAlignment.Center
					amt.TextYAlignment = Enum.TextYAlignment.Bottom
					amt.Parent = Icon
				end

				local function applyLootIcons()
					if not (NameTags.Enabled and Loot.Enabled and nametag and nametag.Parent and ent.Player) then return end
					local inventory = store.inventories[ent.Player]
					if not inventory and bedwars.getInventory then
						inventory = bedwars.getInventory(ent.Player)
						if inventory then store.inventories[ent.Player] = inventory end
					end
					if not inventory then return end
					local counts = { iron = 0, diamond = 0, emerald = 0 }
					for _, it in inventory.items or {} do
						if counts[it.itemType] ~= nil then
							counts[it.itemType] = counts[it.itemType] + (it.amount or 0)
						end
					end
					for slot, nm in lootDefs do
						local ic = nametag:FindFirstChild(slot)
						if ic then
							local c = counts[nm]
							if c > 0 then
								ic.Image = bedwars.getIcon({ itemType = nm }, true)
								if ic.Amt then ic.Amt.Text = tostring(c) end
							else
								ic.Image = ''
								if ic.Amt then ic.Amt.Text = '' end
							end
						end
					end
				end

				applyLootIcons()
				if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
				table.insert(kitTrackerConnections[ent], vapeEvents.InventoryChanged.Event:Connect(function()
					if nametag and nametag.Parent then applyLootIcons() end
				end))
			end

			if Equipment.Enabled then
				for i, v in { 'Hand', 'Helmet', 'Chestplate', 'Boots' } do
					local Icon = Instance.new('ImageLabel')
					Icon.Name = v
					local iconSc = Scale.Value
					Icon.Size = udim2fromOffset(30 * iconSc, 30 * iconSc)
					Icon.Position = udim2fromOffset((-60 + (i * 30)) * iconSc, -30 * iconSc)
					Icon.BackgroundTransparency = 1
					Icon.Image = ''
					Icon.Parent = nametag
					if v == 'Hand' then
						local amt = Instance.new('TextLabel')
						amt.Name = 'Amt'
						amt.AnchorPoint = Vector2.new(0, 1)
						amt.Position = UDim2.new(0, 0, 1, 0)
						amt.Size = UDim2.new(0, 14 * iconSc, 0, 10 * iconSc)
						amt.BackgroundTransparency = 1
						amt.Text = ''
						amt.TextColor3 = Color3.fromRGB(255, 255, 255)
						amt.TextStrokeTransparency = 0.4
						amt.TextSize = 10 * iconSc
						amt.Font = Enum.Font.GothamBold
						amt.TextXAlignment = Enum.TextXAlignment.Left
						amt.TextYAlignment = Enum.TextYAlignment.Bottom
						amt.Parent = Icon
					end
				end

				local equipRetry = nil
				local function applyEquipmentIcons(attempt)
					if not ent.Player then return end
					if not (NameTags.Enabled and Equipment.Enabled and nametag and nametag.Parent) then return end
					local inventory = store.inventories[ent.Player]
					if not inventory and bedwars.getInventory then
						inventory = bedwars.getInventory(ent.Player)
						if inventory then store.inventories[ent.Player] = inventory end
					end
					if not inventory then
						if equipRetry then task.cancel(equipRetry) end
						local wait = math.min(0.1 * (attempt or 1), 1)
						equipRetry = task.delay(wait, function()
							equipRetry = nil
							applyEquipmentIcons((attempt or 1) + 1)
						end)
						return
					end
					local handItem = inventory.hand
					local kit = ent.Player:GetAttribute('PlayingAsKits')
					local handImage = bedwars.getIcon(handItem or { itemType = '' }, true)
					if nametag.Hand then
						nametag.Hand.Image = handImage
						if nametag.Hand.Amt then
							local a = handItem and handItem.amount
							nametag.Hand.Amt.Text = (a and a > 1) and tostring(a) or ''
						end
					end
					if nametag.Helmet then nametag.Helmet.Image = bedwars.getIcon(inventory.armor and inventory.armor[4] or { itemType = '' }, true) end
					local chestImage = bedwars.getIcon(inventory.armor and inventory.armor[5] or { itemType = '' }, true)
					if kit == 'tinker' then
						local level = ent.Player:GetAttribute('TinkerMachineLevel')
						if level and level >= 1 and level <= 5 then
							local mechImg = getChainsawImage(level)
							if mechImg and mechImg ~= '' then
								chestImage = mechImg
							end
						end
					end
					if nametag.Chestplate then nametag.Chestplate.Image = chestImage end
					if nametag.Boots then nametag.Boots.Image = bedwars.getIcon(inventory.armor and inventory.armor[6] or { itemType = '' }, true) end
				end
				applyEquipmentIcons()
				if not kitTrackerConnections[ent] then kitTrackerConnections[ent] = {} end
				table.insert(kitTrackerConnections[ent], vapeEvents.InventoryChanged.Event:Connect(function()
					if nametag and nametag.Parent then
						applyEquipmentIcons()
					end
				end))

				if ent.Player then
					local attrConn = ent.Player:GetAttributeChangedSignal('TinkerMachineLevel'):Connect(function()
						if Equipment.Enabled and nametag and nametag.Parent then
							applyEquipmentIcons()
						end
					end)
					if not kitTrackerConnections[ent] then
						kitTrackerConnections[ent] = {}
					end
					table.insert(kitTrackerConnections[ent], attrConn)
				end
			end

			if ShowKits.Enabled and ent.Player then
				local kitIcon = Instance.new('ImageLabel')
				kitIcon.Name = 'KitIcon'
				kitIcon.Size = udim2fromOffset(30, 30)
				kitIcon.AnchorPoint = vector2new(0.5, 0)
				kitIcon.BackgroundTransparency = 1
				kitIcon.Image = ''

				if Equipment.Enabled then
					kitIcon.Position = udim2fromOffset(110, -30)
				else
					kitIcon.Position = UDim2.new(0.5, 0, 0, -35)
				end

				kitIcon.Parent = nametag

				local kit = ent.Player:GetAttribute('PlayingAsKits')
				if kit then
					local kitImage = kitImageIds[kit:lower()]
					kitIcon.Image = kitImage or kitImageIds["none"]
					kitCache[ent] = kitImage or kitImageIds["none"]
				else
					kitIcon.Image = kitImageIds["none"]
					kitCache[ent] = kitImageIds["none"]
				end
			end

			if DeviceIcon and DeviceIcon.Enabled and ent.Player then
				local function getPlayerDevice(plr)
					local val = plr:GetAttribute('UserInputType') or 'Unknown'
					if not val then return 'Unknown' end
					val = val:upper()
					if val == 'MOBILE' then return 'Mobile'
					elseif val == 'GAMEPAD' or val == 'CONTROLLER' then return 'Controller'
					else return 'PC' end
				end
				local deviceType = getPlayerDevice(ent.Player)
				if deviceType then
					local deviceEmoji = {Mobile = '📱', PC = '🖥', Controller = '🎮', Unknown = '❔'}
					local deviceLabel = Instance.new('TextLabel')
					deviceLabel.Name = 'DeviceIcon'
					deviceLabel.Size = udim2fromOffset(22, 22)
					deviceLabel.AnchorPoint = vector2new(0, 0)
					deviceLabel.Position = UDim2.new(1, 10, 0, -1)
					deviceLabel.BackgroundTransparency = 1
					deviceLabel.BorderSizePixel = 0
					deviceLabel.Text = deviceEmoji[deviceType] or ''
					deviceLabel.RichText = false
					deviceLabel.TextScaled = false
					deviceLabel.TextSize = 16
					deviceLabel.FontFace = Font.fromEnum(Enum.Font.Arial)
					deviceLabel.TextColor3 = Color3.new(1, 1, 1)
					deviceLabel.Parent = nametag
				end
			end

			if Rank.Enabled and ent.Player then
				local rankIcon = Instance.new('ImageLabel')
				rankIcon.Name = 'RankIcon'
				rankIcon.Size = udim2fromOffset(30, 30)
				rankIcon.Position = UDim2.new(1, (DeviceIcon and DeviceIcon.Enabled and 42 or 10), 0, -4)
				rankIcon.BackgroundTransparency = 1
				rankIcon.Image = ''
				rankIcon.Parent = nametag

				task.spawn(function()
					task.wait(math.random() * 0.5)
					if not NameTags.Enabled then return end
					if vape.ThreadFix then setthreadidentity(8) end
					local plr = playersService:GetPlayerFromCharacter(ent.Character)
					if not plr then return end
					if not rankIcon or not rankIcon.Parent then return end

					local ok, success, data = pcall(function()
						return bedwars.Client:Get(remotes.Ranks):CallServerAsync({ plr.UserId }):await()
					end)

					if vape.ThreadFix then setthreadidentity(8) end

					if ok and success and type(data) == "table" then
						local division = data[1] and data[1].rankDivision
						if division and bedwars.RankMeta and bedwars.RankMeta[division] then
							if rankIcon and rankIcon.Parent then
								rankIcon.Image = bedwars.RankMeta[division].image
							end
						end
					end
				end)
			end

			if GloopIndicator and GloopIndicator.Enabled and ent.Character then
				local gloopIcon = Instance.new('ImageLabel')
				gloopIcon.Name = 'GloopIcon'
				gloopIcon.Size = udim2fromOffset(24, 24)
				gloopIcon.BackgroundTransparency = 1
				gloopIcon.Image = bedwars.getIcon({itemType = 'glue_projectile'}, true)
				gloopIcon.Visible = false
				if Rank.Enabled and DeviceIcon and DeviceIcon.Enabled then
					gloopIcon.Position = UDim2.new(1, 74, 0, -2)
				elseif Rank.Enabled or (DeviceIcon and DeviceIcon.Enabled) then
					gloopIcon.Position = UDim2.new(1, 42, 0, -2)
				else
					gloopIcon.Position = UDim2.new(1, 10, 0, -2)
				end
				gloopIcon.Parent = nametag
				local gloopTimer = nil
				if gloopConnections[ent] then gloopConnections[ent]:Disconnect() end
				gloopConnections[ent] = ent.Character.AttributeChanged:Connect(function(attr)
					if attr ~= 'GlueSlow' then return end
					local val = ent.Character:GetAttribute('GlueSlow')
					if val ~= nil and val ~= 0 then
						gloopIcon.Visible = true
						if gloopTimer then task.cancel(gloopTimer) end
						gloopTimer = task.delay(10, function()
							gloopIcon.Visible = false
							gloopTimer = nil
						end)
					end
				end)
			end

			if Enchant.Enabled and ent.Player and ent.Character then
				local Icon = Instance.new('ImageLabel')
				Icon.Name = 'EnchantIcon'
				Icon.Size = udim2fromOffset(30, 30)
				Icon.Position = udim2fromOffset(-30, -4)
				Icon.BackgroundTransparency = 1
				Icon.Image = getActiveEnchantImage(ent.Character)
				Icon.Parent = nametag
				enchantCache[ent] = Icon.Image
				if enchantConnections[ent] then enchantConnections[ent]:Disconnect() end
				enchantConnections[ent] = ent.Character.AttributeChanged:Connect(function(attr)
					if attr:sub(1, 13) ~= 'StatusEffect_' then return end
					local val = ent.Character:GetAttribute(attr)
					if type(val) ~= 'number' then return end
					local newImage = getActiveEnchantImage(ent.Character)
					if enchantCache[ent] ~= newImage then
						Icon.Image = newImage
						enchantCache[ent] = newImage
					end
				end)
			end

			Reference[ent] = nametag
			charCache[ent] = ent.Character
			lastUpdate[ent] = 0
		end,

		Drawing = function(ent)
			if not shouldShow(ent) then return end
			if Reference[ent] then return end

			local bg = Drawing.new('Square')
			bg.Filled = true
			bg.Thickness = 0
			bg.Color = color3new(0, 0, 0)
			bg.Transparency = 1 - Background.Value
			bg.ZIndex = 1
			bg.Visible = false

			local text = Drawing.new('Text')
			text.Center = false
			text.Outline = true
			text.OutlineColor = color3new(0, 0, 0)
			text.Size = math_round(14 * Scale.Value)
			text.Font = 2
			text.Color = getTeamColor(ent) or color3fromHSV(Color.Hue, Color.Sat, Color.Value)
			text.ZIndex = 2
			text.Visible = false

			local baseName = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or (getBossDisplayName(ent) or (ent.Character and ent.Character.Name)) or ''
			local built = baseName
			if Health.Enabled then built = built .. ' ' .. math_round(ent.Health) end
			if ShowKits.Enabled and ent.Player then
				local kit = ent.Player:GetAttribute('PlayingAsKits')
				if kit then
					built = built .. ' (' .. (kit:gsub('_', ' '):gsub('^%l', string.upper)) .. ')'
				end
			end
			if Distance.Enabled then
				Strings[ent] = '[%s] ' .. built
			else
				Strings[ent] = built
				text.Text = built
				bg.Size = vector2new(text.TextBounds.X + 8, text.TextBounds.Y + 7)
			end

			Reference[ent] = {Text = text, BG = bg}
			charCache[ent] = ent.Character
		end
	}

	Removed = {
		Normal = function(ent)
			local v = Reference[ent]
			if v then
				Reference[ent] = nil
				Strings[ent] = nil
				Sizes[ent] = nil
				lastUpdate[ent] = nil
				kitCache = {}
				equipmentCache = {}
				lootCache = {}
				enchantCache = {}
				healthCache[ent] = nil
				charCache[ent] = nil
				if enchantConnections[ent] then
					enchantConnections[ent]:Disconnect()
					enchantConnections[ent] = nil
				end
				if gloopConnections[ent] then
					gloopConnections[ent]:Disconnect()
					gloopConnections[ent] = nil
				end
				if kitTrackerConnections[ent] then
					for _, c in ipairs(kitTrackerConnections[ent]) do
						pcall(function() c:Disconnect() end)
					end
					kitTrackerConnections[ent] = nil
				end
				if billboardCache[ent] then
					for _, b in ipairs(billboardCache[ent]) do
						pcall(function() b:Destroy() end)
					end
					billboardCache[ent] = nil
				end
				v:Destroy()
			end
		end,
		Drawing = function(ent)
			local v = Reference[ent]
			if v then
				Reference[ent] = nil
				Strings[ent] = nil
				Sizes[ent] = nil
				lastUpdate[ent] = nil
				kitCache[ent] = nil
				healthCache[ent] = nil
				if enchantConnections[ent] then
					enchantConnections[ent]:Disconnect()
					enchantConnections[ent] = nil
				end
				if gloopConnections[ent] then
					gloopConnections[ent]:Disconnect()
					gloopConnections[ent] = nil
				end
				if billboardCache[ent] then
					for _, b in ipairs(billboardCache[ent]) do
						pcall(function() b:Destroy() end)
					end
					billboardCache[ent] = nil
				end
				for _, obj in v do
					pcall(function()
						obj.Visible = false
						obj:Remove()
					end)
				end
			end
		end
	}

	local Updated = {
		Normal = function(ent)
			local nametag = Reference[ent]
			if not nametag then return end

			local now = tick()
			if lastUpdate[ent] and (now - lastUpdate[ent]) < 0.2 then return end
			lastUpdate[ent] = now

			Sizes[ent] = nil
			local bossDisplayName = ent.NPC and ent.Character and getBossDisplayName(ent) or nil
			local entityName = bossDisplayName or (ent.Player and nil) or ent.Character.Name
			Strings[ent] = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or entityName

			if Health.Enabled then
				local colorStr = getHealthColorStr(ent)
				Strings[ent] = Strings[ent]..' <font color="'..colorStr..'">'..math.round(ent.Health)..'</font>'
			end

			if Distance.Enabled then
				Strings[ent] = '[%s] ' .. Strings[ent]
			end

			if Equipment.Enabled and ent.Player then
				if not store.inventories[ent.Player] and bedwars.getInventory then
					local fetched = bedwars.getInventory(ent.Player)
					if fetched then store.inventories[ent.Player] = fetched end
				end
				local inventory = store.inventories[ent.Player] or {hand = nil, armor = {}}
				local handItem = inventory.hand
				local kit = ent.Player:GetAttribute('PlayingAsKits')
				local handImage = bedwars.getIcon(handItem or { itemType = '' }, true)
				local chestImage = bedwars.getIcon(inventory.armor and inventory.armor[5] or { itemType = '' }, true)
				if kit == 'tinker' then
					local level = ent.Player:GetAttribute('TinkerMachineLevel')
					if level and level >= 1 and level <= 5 then
						local mechImg = getChainsawImage(level)
						if mechImg and mechImg ~= '' then
							chestImage = mechImg
						end
					end
				end
				local currentEquip = {
					tostring(handItem and handItem.itemType or ''),
					tostring((inventory.armor and inventory.armor[4] and inventory.armor[4].itemType) or ''),
					tostring((inventory.armor and inventory.armor[5] and inventory.armor[5].itemType) or ''),
					tostring((inventory.armor and inventory.armor[6] and inventory.armor[6].itemType) or '')
				}
				local equipKey = table.concat(currentEquip, "|")
				if equipmentCache[ent] ~= equipKey then
					equipmentCache[ent] = equipKey
					if nametag.Hand then
						nametag.Hand.Image = handImage
						if nametag.Hand.Amt then
							local a = handItem and handItem.amount
							nametag.Hand.Amt.Text = (a and a > 1) and tostring(a) or ''
						end
					end
					if nametag.Helmet then
						nametag.Helmet.Image = bedwars.getIcon(inventory.armor and inventory.armor[4] or { itemType = '' }, true)
					end
					if nametag.Chestplate then
						nametag.Chestplate.Image = chestImage
					end
					if nametag.Boots then
						nametag.Boots.Image = bedwars.getIcon(inventory.armor and inventory.armor[6] or { itemType = '' }, true)
					end
				end
			end

			local size = getfontsize(removeTags(Strings[ent]), nametag.TextSize, nametag.FontFace, vector2new(100000, 100000))
			nametag.Size = udim2fromOffset(size.X + 8, size.Y + 7)
			nametag.Text = Strings[ent]
			nametag.TextColor3 = getTeamColor(ent) or color3fromHSV(Color.Hue, Color.Sat, Color.Value)
		end,

		Drawing = function(ent)
			local nametag = Reference[ent]
			if nametag then
				if vape.ThreadFix then setthreadidentity(8) end
				Sizes[ent] = nil
				local bossDisplayName = ent.NPC and ent.Character and getBossDisplayName(ent) or nil
				local entityName = bossDisplayName or (ent.Player and nil) or ent.Character.Name
				Strings[ent] = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or entityName

				if Health.Enabled then
					Strings[ent] = Strings[ent]..' '..math.round(ent.Health)
					nametag.Text.Color = getHealthColor(ent)
				end

				if Distance.Enabled then
					Strings[ent] = '[%s] ' .. Strings[ent]
					nametag.Text.Text = entitylib.isAlive and string_format(Strings[ent], math_floor((entitylib.character.RootPart.Position - ent.RootPart.Position).Magnitude)) or Strings[ent]
				else
					nametag.Text.Text = Strings[ent]
				end

				if ShowKits.Enabled and ent.Player then
					local kit = ent.Player:GetAttribute('PlayingAsKits')
					if kit then
						local kitName = kit:gsub("_", " "):gsub("^%l", string.upper)
						nametag.Text.Text = nametag.Text.Text .. ' (' .. kitName .. ')'
					end
				end

				nametag.BG.Size = vector2new(nametag.Text.TextBounds.X + 8, nametag.Text.TextBounds.Y + 7)
				if not Health.Enabled then
					nametag.Text.Color = getTeamColor(ent) or color3fromHSV(Color.Hue, Color.Sat, Color.Value)
				end
			end
		end
	}

	local ColorFunc = {
		Normal = function(hue, sat, val)
			local color = color3fromHSV(hue, sat, val)
			for i, v in Reference do
				v.TextColor3 = getTeamColor(i) or color
			end
		end,
		Drawing = function(hue, sat, val)
			local color = color3fromHSV(hue, sat, val)
			for i, v in Reference do
				if not Health.Enabled then
					v.Text.Color = getTeamColor(i) or color
				end
			end
		end
	}

	local frameCounter = 0
	Loop = {
		Normal = function()
			frameCounter = frameCounter + 1
			local skipVisCheck = frameCounter % 2 ~= 0
			local updateDistText = frameCounter % 3 == 0
			local updateEquipment = frameCounter % 30 == 0
			local forceEquip = frameCounter % 150 == 0
			local updateKit = frameCounter % 30 == 0
			local updateDistanceText = frameCounter % 6 == 0
			local myPos = entitylib.isAlive and entitylib.character.RootPart and entitylib.character.RootPart.Position

			for ent, nametag in Reference do
				if not ent.RootPart or not ent.Character or not ent.Character.Parent or (ent.Health ~= nil and ent.Health <= 0) then
					nametag.Visible = false
					continue
				end
				if DistanceCheck.Enabled then
					local distance = myPos and (myPos - ent.RootPart.Position).Magnitude or math_huge
					if distance < DistanceLimit.ValueMin or distance > DistanceLimit.ValueMax then
						nametag.Visible = false
						continue
					end
				end

				local headPos, headVis = gameCamera:WorldToViewportPoint(ent.RootPart.Position + vector3new(0, ent.HipHeight + 1, 0))
				if not skipVisCheck then
					nametag.Visible = headVis
				end
				if not nametag.Visible or not headVis then continue end
				nametag.Position = udim2fromOffset(headPos.X, headPos.Y)

				if Health.Enabled then
					local hp = math_round(ent.Health)
					if healthCache[ent] ~= hp and (frameCounter % 2 == 0) then
						healthCache[ent] = hp
						local baseName = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or (getBossDisplayName(ent) or (ent.Character and ent.Character.Name)) or ''
						Strings[ent] = baseName..' <font color="'..getHealthColorStr(ent)..'">'..hp..'</font>'
						if Distance.Enabled then
							Strings[ent] = '[%s] ' .. Strings[ent]
						else
							nametag.Text = Strings[ent]
							local size = getfontsize(removeTags(nametag.Text), nametag.TextSize, nametag.FontFace, vector2new(100000, 100000))
							nametag.Size = udim2fromOffset(size.X + 8, size.Y + 7)
						end
						Sizes[ent] = nil
					end
				end

				if Distance.Enabled and updateDistText then
					local mag = myPos and math_floor((myPos - ent.RootPart.Position).Magnitude) or 0
					if Sizes[ent] ~= mag then
						nametag.Text = string_format(Strings[ent], mag)
						if updateDistanceText then
							local size = getfontsize(removeTags(nametag.Text), nametag.TextSize, nametag.FontFace, vector2new(100000, 100000))
							nametag.Size = udim2fromOffset(size.X + 8, size.Y + 7)
						end
						Sizes[ent] = mag
					end
				end

				if Equipment.Enabled and (updateEquipment or forceEquip) then
					if ent.Player and not store.inventories[ent.Player] and bedwars.getInventory then
						local fetched = bedwars.getInventory(ent.Player)
						if fetched then store.inventories[ent.Player] = fetched end
					end
					if ent.Player and store.inventories[ent.Player] then
						local inventory = store.inventories[ent.Player]
						local handItem = inventory.hand
						local currentEquip = {
							(handItem and handItem.itemType) or '',
							(inventory.armor and inventory.armor[4] and inventory.armor[4].itemType) or '',
							(inventory.armor and inventory.armor[5] and inventory.armor[5].itemType) or '',
							(inventory.armor and inventory.armor[6] and inventory.armor[6].itemType) or ''
						}
						local equipKey = table.concat(currentEquip, "|")
						local iconsBlank = nametag.Hand and (nametag.Hand.Image == '' or nametag.Hand.Image == nil)
						if equipmentCache[ent] ~= equipKey or forceEquip or (iconsBlank and equipKey ~= "|||") then
							local kit = ent.Player:GetAttribute('PlayingAsKits')
							local handImage = bedwars.getIcon(handItem or { itemType = '' }, true)
							local chestImage = bedwars.getIcon(inventory.armor and inventory.armor[5] or { itemType = '' }, true)
							if kit == 'tinker' then
								local level = ent.Player:GetAttribute('TinkerMachineLevel')
								if level and level >= 1 and level <= 5 then
									local mechImg = getChainsawImage(level)
									if mechImg and mechImg ~= '' then
										chestImage = mechImg
									end
								end
							end
							equipmentCache[ent] = equipKey
							if nametag.Hand then
								nametag.Hand.Image = handImage
								if nametag.Hand.Amt then
									local a = handItem and handItem.amount
									nametag.Hand.Amt.Text = (a and a > 1) and tostring(a) or ''
								end
							end
							if nametag.Helmet then
								nametag.Helmet.Image = bedwars.getIcon(inventory.armor and inventory.armor[4] or { itemType = '' }, true)
							end
							if nametag.Chestplate then
								nametag.Chestplate.Image = chestImage
							end
							if nametag.Boots then
								nametag.Boots.Image = bedwars.getIcon(inventory.armor and inventory.armor[6] or { itemType = '' }, true)
							end
						end
					elseif ent.Player then
						equipmentCache[ent] = nil
					end
				end

				if Loot.Enabled and (updateEquipment or forceEquip) and ent.Player and nametag.LootIron then
					local inv = store.inventories[ent.Player]
					if not inv and bedwars.getInventory then
						inv = bedwars.getInventory(ent.Player)
						if inv then store.inventories[ent.Player] = inv end
					end
					if inv and inv.items then
						local counts = { iron = 0, diamond = 0, emerald = 0 }
						for _, it in inv.items do
							local c = counts[it.itemType]
							if c then
								counts[it.itemType] = c + (it.amount or 0)
							end
						end
						local lootKey = counts.iron .. '|' .. counts.diamond .. '|' .. counts.emerald
						if lootCache[ent] ~= lootKey or forceEquip then
							lootCache[ent] = lootKey
							for slot, nm in { LootIron = 'iron', LootDiamond = 'diamond', LootEmerald = 'emerald' } do
								local ic = nametag:FindFirstChild(slot)
								if ic then
									local c = counts[nm]
									if c > 0 then
										ic.Image = bedwars.getIcon({ itemType = nm }, true)
										if ic.Amt then ic.Amt.Text = tostring(c) end
									else
										ic.Image = ''
										if ic.Amt then ic.Amt.Text = '' end
									end
								end
							end
						end
					end
				end

				if Potions.Enabled and (updateEquipment or forceEquip) then
					local char = ent.Character
					for _, def in potionDefs do
						local ic = nametag:FindFirstChild(def.name)
						if ic then
							local want = potionActive(char, def) and bedwars.getIcon({itemType = def.item}, true) or ''
							if ic.Image ~= want then
								ic.Image = want
							end
						end
					end
				end

				if ShowKits.Enabled and updateKit then
					local kitIcon = nametag:FindFirstChild('KitIcon')
					if kitIcon and ent.Player then
						local kit = ent.Player:GetAttribute('PlayingAsKits')
						local meta = bedwars.BedwarsKitMeta and kit and bedwars.BedwarsKitMeta[kit]
						local newKitImage = (meta and meta.renderImage) or kitImageIds[kit] or kitImageIds['none']
						if kitCache[ent] ~= newKitImage then
							kitIcon.Image = newKitImage
							kitCache[ent] = newKitImage
						end
					end
				end
			end
		end,

		Drawing = function()
			frameCounter = frameCounter + 1
			local skipFrame = frameCounter % 2 ~= 0

			for ent, nametag in Reference do
				if not ent.RootPart or (ent.Health ~= nil and ent.Health <= 0) then
					if not nametag:GetAttribute('DeadHidden') then
						nametag:SetAttribute('DeadHidden', true)
						for _, child in nametag:GetChildren() do
							if child:IsA('GuiObject') then child.Visible = false end
						end
					end
					continue
				end
				if nametag:GetAttribute('DeadHidden') then
					nametag:SetAttribute('DeadHidden', nil)
					for _, child in nametag:GetChildren() do
						if child:IsA('GuiObject') then child.Visible = true end
					end
				end
				if DistanceCheck.Enabled then
					local distance = entitylib.isAlive and (entitylib.character.RootPart.Position - ent.RootPart.Position).Magnitude or math_huge
					if distance < DistanceLimit.ValueMin or distance > DistanceLimit.ValueMax then
						nametag.Text.Visible = false
						nametag.BG.Visible = false
						continue
					end
				end

				local headPos, headVis = gameCamera:WorldToViewportPoint(ent.RootPart.Position + vector3new(0, ent.HipHeight + 1, 0))
				nametag.Text.Visible = headVis
				nametag.BG.Visible = headVis
				if not headVis then continue end
				if skipFrame then continue end

				if Health.Enabled then
					nametag.Text.Color = getHealthColor(ent)
					local hp = math_round(ent.Health)
					if healthCache[ent] ~= hp then
						healthCache[ent] = hp
						local baseName = ent.Player and (DisplayName.Enabled and ent.Player.DisplayName or ent.Player.Name) or (getBossDisplayName(ent) or (ent.Character and ent.Character.Name)) or ''
						local built = baseName..' '..hp
						if ShowKits.Enabled and ent.Player then
							local kit = ent.Player:GetAttribute('PlayingAsKits')
							if kit then
								built = built..' ('..(kit:gsub('_', ' '):gsub('^%l', string.upper))..')'
							end
						end
						if Distance.Enabled then
							Strings[ent] = '[%s] '..built
						else
							Strings[ent] = built
							nametag.Text.Text = built
							nametag.BG.Size = vector2new(nametag.Text.TextBounds.X + 8, nametag.Text.TextBounds.Y + 7)
						end
						Sizes[ent] = nil
					end
				end

				if Distance.Enabled then
					local mag = entitylib.isAlive and math_floor((entitylib.character.RootPart.Position - ent.RootPart.Position).Magnitude) or 0
					if Sizes[ent] ~= mag then
						nametag.Text.Text = string_format(Strings[ent], mag)
						nametag.BG.Size = vector2new(nametag.Text.TextBounds.X + 8, nametag.Text.TextBounds.Y + 7)
						Sizes[ent] = mag
					end
				end

				nametag.BG.Position = vector2new(headPos.X - (nametag.BG.Size.X / 2), headPos.Y - nametag.BG.Size.Y)
				nametag.Text.Position = nametag.BG.Position + vector2new(4, 3)
			end
		end
	}

	NameTags = vape.Categories.Render:CreateModule({
		Name = 'NameTags',
		Function = function(callback)
			if callback then
				methodused = StreamProof.Enabled and 'Drawing' or 'Normal'
				frameCounter = 0

				if Removed[methodused] then
					NameTags:Clean(entitylib.Events.EntityRemoved:Connect(Removed[methodused]))
				end

				if Added[methodused] then
					for _, v in entitylib.List do
						if Reference[v] then Removed[methodused](v) end
						pcall(Added[methodused], v)
					end
					task.spawn(function()
						task.wait(1)
						if not NameTags.Enabled then return end
						for _, v in entitylib.List do
							if Reference[v] then Removed[methodused](v) end
							pcall(Added[methodused], v)
						end
					end)
					NameTags:Clean(entitylib.Events.EntityAdded:Connect(function(ent)
						task.spawn(function()
							task.wait(0.5)
							if not NameTags.Enabled then return end
							if not ent.Character or not ent.Character.Parent then
								if Reference[ent] then Removed[methodused](ent) end
								return
							end
							if Reference[ent] then Removed[methodused](ent) end
							pcall(Added[methodused], ent)
						end)
					end))
					NameTags:Clean(playersService.PlayerAdded:Connect(function(p)
						NameTags:Clean(p.CharacterAdded:Connect(function()
							task.delay(0.3, function()
								if not NameTags.Enabled then return end
								for _, v in entitylib.List do
									if v.Player == p and not Reference[v] then
										pcall(Added[methodused], v)
									end
								end
							end)
						end))
					end))
				end

				if Updated[methodused] then
					NameTags:Clean(entitylib.Events.EntityUpdated:Connect(function(ent)
						local show = shouldShow(ent)
						if show and not Reference[ent] then
							pcall(Added[methodused], ent)
							return
						elseif not show and Reference[ent] then
							Removed[methodused](ent)
							return
						end
						if Reference[ent] and charCache[ent] ~= ent.Character then
							Removed[methodused](ent)
							pcall(Added[methodused], ent)
							return
						end
						Updated[methodused](ent)
					end))
					for _, v in entitylib.List do
						pcall(Updated[methodused], v)
					end
				end

				if ColorFunc[methodused] then
					NameTags:Clean(vape.Categories.Friends.ColorUpdate.Event:Connect(function()
						ColorFunc[methodused](Color.Hue, Color.Sat, Color.Value)
					end))
				end

				if Loop[methodused] then
					NameTags:Clean(runService.RenderStepped:Connect(Loop[methodused]))
				end

				if Added[methodused] then
					task.spawn(function()
						while NameTags.Enabled do
							task.wait(0.5)
							if not NameTags.Enabled then break end
							for _, v in entitylib.List do
								if v.Character and v.Character.Parent and shouldShow(v) then
									if not Reference[v] then
										pcall(Added[methodused], v)
									elseif charCache[v] ~= v.Character then
										Removed[methodused](v)
										pcall(Added[methodused], v)
									end
								end
							end
						end
					end)
				end
			else
				if Removed[methodused] then
					for i in Reference do
						Removed[methodused](i)
					end
				end
				lastUpdate = {}
				kitCache = {}
				equipmentCache = {}
				enchantCache = {}
				healthCache = {}
				enchantConnections = {}
				gloopConnections = {}
				kitTrackerConnections = {}
				for _, list in pairs(billboardCache) do
					for _, b in ipairs(list) do
						pcall(function() b:Destroy() end)
					end
				end
				billboardCache = {}
			end
		end,
		Tooltip = 'renders nametags on entities through walls.'
	})

	Targets = NameTags:CreateTargets({
		Players = true,
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	FontOption = NameTags:CreateFont({
		Name = 'Font',
		Blacklist = 'Arial',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Color = NameTags:CreateColorSlider({
		Name = 'Player Color',
		Function = function(hue, sat, val)
			if NameTags.Enabled and ColorFunc[methodused] then
				ColorFunc[methodused](hue, sat, val)
			end
		end
	})

	Scale = NameTags:CreateSlider({
		Name = 'Scale',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end,
		Default = 1,
		Min = 0.1,
		Max = 1.5,
		Decimal = 10
	})

	Background = NameTags:CreateSlider({
		Name = 'Transparency',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end,
		Default = 0.5,
		Min = 0,
		Max = 1,
		Decimal = 10
	})

	Health = NameTags:CreateToggle({
		Name = 'Health',
		Function = function(callback)
			HealthColorToggle.Object.Visible = callback
			HealthColorFull.Object.Visible = callback and HealthColorToggle.Enabled
			HealthColorMid.Object.Visible = callback and HealthColorToggle.Enabled
			HealthColorLow.Object.Visible = callback and HealthColorToggle.Enabled
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	HealthColorToggle = NameTags:CreateToggle({
		Name = 'Custom Health Colors',
		Darker = true,
		Visible = false,
		Function = function(callback)
			HealthColorFull.Object.Visible = callback
			HealthColorMid.Object.Visible = callback
			HealthColorLow.Object.Visible = callback
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	HealthColorFull = NameTags:CreateColorSlider({
		Name = 'Full HP Color',
		Darker = true,
		Visible = false,
		DefaultHue = 0.4,
		DefaultSat = 0.89,
		DefaultValue = 0.75,
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	HealthColorMid = NameTags:CreateColorSlider({
		Name = 'Mid HP Color',
		Darker = true,
		Visible = false,
		DefaultHue = 0.15,
		DefaultSat = 0.89,
		DefaultValue = 0.75,
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	HealthColorLow = NameTags:CreateColorSlider({
		Name = 'Low HP Color',
		Darker = true,
		Visible = false,
		DefaultHue = 0,
		DefaultSat = 0.89,
		DefaultValue = 0.75,
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Distance = NameTags:CreateToggle({
		Name = 'Distance',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Loot = NameTags:CreateToggle({
		Name = 'Loot',
		Tooltip = 'shows how much iron/diamond/emerald they got',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})
	Potions = NameTags:CreateToggle({
		Name = 'Potions',
		Tooltip = 'shows if they popped serpent, fury, invis, jump or speed pie',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})
	Equipment = NameTags:CreateToggle({
		Name = 'Equipment',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	ShowKits = NameTags:CreateToggle({
		Name = 'Show Kits',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end,
	})

	KitTracker = NameTags:CreateToggle({
		Name = 'Kit Tracker',
		Tooltip = 'shows stats for certain kits on a player',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Rank = NameTags:CreateToggle({
		Name = 'Rank',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	DeviceIcon = NameTags:CreateToggle({
		Name = 'Device Icon',
		Tooltip = 'shows what device the player is using',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	GloopIndicator = NameTags:CreateToggle({
		Name = 'Gloop',
		Default = true,
		Tooltip = 'shows when a player is glooped and not glooped anymore',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Enchant = NameTags:CreateToggle({
		Name = 'Enchant',
		Tooltip = 'shows the player current enchant',
		Default = true,
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	DisplayName = NameTags:CreateToggle({
		Name = 'Use Displayname',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end,
		Default = true
	})

	Teammates = NameTags:CreateToggle({
		Name = 'Priority Only',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end,
		Default = true
	})

	StreamProof = NameTags:CreateToggle({
		Name = 'Stream Proof',
		Default = false,
		Tooltip = 'uses drawing api so its stream proof if u record within roblox (text only, no icons)',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	Boss = NameTags:CreateToggle({
		Name = 'Boss',
		Tooltip = 'nametags for titan and bhaa (really good if u use health too!!)',
		Function = function()
			if NameTags.Enabled then
				NameTags:Toggle()
				NameTags:Toggle()
			end
		end
	})

	DistanceCheck = NameTags:CreateToggle({
		Name = 'Distance Check',
		Function = function(callback)
			DistanceLimit.Object.Visible = callback
		end
	})

	DistanceLimit = NameTags:CreateTwoSlider({
		Name = 'Player Distance',
		Min = 0,
		Max = 256,
		DefaultMin = 0,
		DefaultMax = 64,
		Darker = true,
		Visible = false
	})

	task.defer(function()
		if DistanceLimit and DistanceLimit.Object then
			DistanceLimit.Object.Visible = false
		end
		if HealthColorToggle and HealthColorToggle.Object then
			HealthColorToggle.Object.Visible = false
		end
		if HealthColorFull and HealthColorFull.Object then
			HealthColorFull.Object.Visible = false
		end
		if HealthColorMid and HealthColorMid.Object then
			HealthColorMid.Object.Visible = false
		end
		if HealthColorLow and HealthColorLow.Object then
			HealthColorLow.Object.Visible = false
		end
	end)
end)

run(function()
	local GeneratorESP
	DiamondToggle = nil
	EmeraldToggle = nil
	TeamGenToggle = nil
	ShowOwnTeamGen = nil
	ShowEnemyTeamGen = nil
	local UIStyle
	local CompactDiamondToggle
	local CompactEmeraldToggle
	local CollectionService = collectionService
	local RunService = runService
	local Reference = {}
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local CompactFolder = Instance.new('Folder')
	CompactFolder.Parent = vape.gui
	local teamColors = getgenv().aeroTeamColors

	local generatorTypes = {
		diamond = {
			keywords = {'diamond'},
			color = Color3.fromRGB(85, 200, 255),
			icon = 'diamond',
			displayName = 'Diamond',
			isTeamGen = false
		},
		emerald = {
			keywords = {'emerald'},
			color = Color3.fromRGB(0, 255, 100),
			icon = 'emerald',
			displayName = 'Emerald',
			isTeamGen = false
		}
	}

	local compactUI = Instance.new('ScreenGui')
	compactUI.Name = 'GeneratorCompactUI'
	compactUI.Parent = vape.gui
	compactUI.Enabled = false
	compactUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	compactUI.DisplayOrder = 10
	compactUI.ResetOnSpawn = false

	local mainFrame = Instance.new('Frame')
	mainFrame.Name = 'MainFrame'
	mainFrame.Parent = compactUI
	mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
	mainFrame.BackgroundTransparency = 0.3
	mainFrame.BorderSizePixel = 0
	mainFrame.Position = UDim2.new(1, -8, 1, -8)
	mainFrame.Size = UDim2.new(0, 120, 0, 100)
	mainFrame.AnchorPoint = Vector2.new(1, 1)

	local uicorner = Instance.new('UICorner')
	uicorner.CornerRadius = UDim.new(0, 8)
	uicorner.Parent = mainFrame

	local title = Instance.new('TextLabel')
	title.Name = 'Title'
	title.Parent = mainFrame
	title.BackgroundTransparency = 1
	title.Size = UDim2.new(1, 0, 0, 25)
	title.Position = UDim2.new(0, 0, 0, 5)
	title.Text = "GEN ESP"
	title.TextColor3 = Color3.fromRGB(255, 255, 255)
	title.TextSize = 14
	title.Font = Enum.Font.GothamBold
	title.TextStrokeTransparency = 0.5
	title.TextStrokeColor3 = Color3.new(0, 0, 0)

	local diamondFrame = Instance.new('Frame')
	diamondFrame.Name = 'DiamondFrame'
	diamondFrame.Parent = mainFrame
	diamondFrame.BackgroundTransparency = 1
	diamondFrame.Size = UDim2.new(1, -20, 0, 25)
	diamondFrame.Position = UDim2.new(0, 10, 0, 35)

	local diamondIcon = Instance.new('ImageLabel')
	diamondIcon.Name = 'DiamondIcon'
	diamondIcon.Parent = diamondFrame
	diamondIcon.BackgroundTransparency = 1
	diamondIcon.Size = UDim2.new(0, 18, 0, 18)
	diamondIcon.Position = UDim2.new(0, 0, 0.5, -9)
	diamondIcon.Image = bedwars.getIcon({itemType = 'diamond'}, true)

	local diamondTimer = Instance.new('TextLabel')
	diamondTimer.Name = 'DiamondTimer'
	diamondTimer.Parent = diamondFrame
	diamondTimer.BackgroundTransparency = 1
	diamondTimer.Size = UDim2.new(1, -25, 1, 0)
	diamondTimer.Position = UDim2.new(0, 25, 0, 0)
	diamondTimer.Text = "00"
	diamondTimer.TextColor3 = Color3.fromRGB(85, 200, 255)
	diamondTimer.TextSize = 18
	diamondTimer.Font = Enum.Font.GothamBold
	diamondTimer.TextXAlignment = Enum.TextXAlignment.Left

	local emeraldFrame = Instance.new('Frame')
	emeraldFrame.Name = 'EmeraldFrame'
	emeraldFrame.Parent = mainFrame
	emeraldFrame.BackgroundTransparency = 1
	emeraldFrame.Size = UDim2.new(1, -20, 0, 25)
	emeraldFrame.Position = UDim2.new(0, 10, 0, 65)

	local emeraldIcon = Instance.new('ImageLabel')
	emeraldIcon.Name = 'EmeraldIcon'
	emeraldIcon.Parent = emeraldFrame
	emeraldIcon.BackgroundTransparency = 1
	emeraldIcon.Size = UDim2.new(0, 18, 0, 18)
	emeraldIcon.Position = UDim2.new(0, 0, 0.5, -9)
	emeraldIcon.Image = bedwars.getIcon({itemType = 'emerald'}, true)

	local emeraldTimer = Instance.new('TextLabel')
	emeraldTimer.Name = 'EmeraldTimer'
	emeraldTimer.Parent = emeraldFrame
	emeraldTimer.BackgroundTransparency = 1
	emeraldTimer.Size = UDim2.new(1, -25, 1, 0)
	emeraldTimer.Position = UDim2.new(0, 25, 0, 0)
	emeraldTimer.Text = "00"
	emeraldTimer.TextColor3 = Color3.fromRGB(0, 255, 100)
	emeraldTimer.TextSize = 18
	emeraldTimer.Font = Enum.Font.GothamBold
	emeraldTimer.TextXAlignment = Enum.TextXAlignment.Left

	local diamondTimes = {}
	local emeraldTimes = {}

	local function getMyTeamId()
		local myTeam = lplr:GetAttribute('Team')
		if myTeam == nil then return nil end
		return tonumber(myTeam)
	end

	local function getGeneratorTeamId(generatorId)
		local teamNum = string.match(generatorId, "^(%d+)_generator")
		if teamNum then
			return tonumber(teamNum)
		end
		return nil
	end

	local function isTeamGenerator(generatorId)
		return string.match(generatorId, "^%d+_generator") ~= nil
	end

	local function getGeneratorType(generatorId)
		local idLower = string.lower(generatorId)

		if isTeamGenerator(generatorId) then
			return 'teamgen', {
				color = Color3.fromRGB(200, 200, 200),
				icon = 'iron',
				displayName = 'Team Gen',
				isTeamGen = true
			}
		end

		for genType, config in pairs(generatorTypes) do
			for _, keyword in ipairs(config.keywords) do
				if idLower:find(keyword) then
					return genType, config
				end
			end
		end
		return nil, nil
	end

	local function isGeneratorEnabled(genType, teamId)
		if genType == 'diamond' then
			return DiamondToggle.Enabled
		elseif genType == 'emerald' then
			return EmeraldToggle.Enabled
		elseif genType == 'teamgen' then
			if not TeamGenToggle.Enabled then return false end
			local myTeamId = getMyTeamId()
			if not myTeamId or not teamId then return TeamGenToggle.Enabled end
			if teamId == myTeamId then
				return ShowOwnTeamGen.Enabled
			else
				return ShowEnemyTeamGen.Enabled
			end
		end
		return false
	end

	local function getProperIcon(iconType)
		local icon = bedwars.getIcon({itemType = iconType}, true)
		if not icon or icon == "" then return nil end
		return icon
	end

	local function getTierText(generatorAdornee)
		if not generatorAdornee then return nil end
		if generatorAdornee.Name ~= 'GeneratorAdornee' then return nil end
		local reactTree = generatorAdornee:FindFirstChild('RoactTree')
		if not reactTree then return nil end
		local teamApp = reactTree:FindFirstChild('TeamOreGeneratorApp')
		if not teamApp then return nil end
		local globalGen = teamApp:FindFirstChild('GlobalOreGenerator')
		if globalGen then
			for _, child in pairs(globalGen:GetDescendants()) do
				if child:IsA('TextLabel') then
					local text = child.Text
					if text:find("Tier") or text:match("^[IVX]+$") or text == "0" then
						return child
					end
				end
			end
		end
		local teamGenMain = teamApp:FindFirstChild('TeamGenMain')
		if teamGenMain then
			for _, child in pairs(teamGenMain:GetDescendants()) do
				if child:IsA('TextLabel') then
					local text = child.Text
					if text:find("Tier") or text:match("^[IVX]+$") or text == "0" then
						return child
					end
				end
			end
		end
		return nil
	end

	local function extractTierLevel(tierText)
		if not tierText or tierText == "" then return "0" end
		if tierText == "0" then return "0" end
		local tierMatch = tierText:match("Tier%s+([IVX]+)")
		if tierMatch then return tierMatch end
		if tierText:match("^[IVX]+$") then return tierText end
		local numTier = tierText:match("Tier%s+(%d+)")
		if numTier then
			local num = tonumber(numTier)
			if num == 0 then return "0"
			elseif num == 1 then return "I"
			elseif num == 2 then return "II"
			elseif num == 3 then return "III"
			end
		end
		return "0"
	end

	local function getCountdownText(generatorAdornee)
		if not generatorAdornee then return nil end
		if generatorAdornee.Name ~= 'GeneratorAdornee' then return nil end
		local reactTree = generatorAdornee:FindFirstChild('RoactTree')
		if not reactTree then return nil end
		local teamApp = reactTree:FindFirstChild('TeamOreGeneratorApp')
		if not teamApp then return nil end
		local globalGen = teamApp:FindFirstChild('GlobalOreGenerator')
		if not globalGen then return nil end
		local countdown = globalGen:FindFirstChild('Countdown')
		if not countdown then return nil end
		local textLabel = countdown:FindFirstChild('Text')
		if not textLabel then
			if countdown:IsA('TextLabel') then return countdown end
			return nil
		end
		return textLabel
	end

	local function extractSecondsFromText(text)
		if not text or text == "" then return 0 end
		local seconds = text:match("%[(%d+)%]")
		if seconds then return tonumber(seconds) or 0 end
		local justNumber = text:match("(%d+)")
		if justNumber then return tonumber(justNumber) or 0 end
		return 0
	end

	local function getResourceCount(position, resourceType)
		local count = 0
		for _, drop in pairs(CollectionService:GetTagged('ItemDrop')) do
			if drop:FindFirstChild('Handle') then
				local dropName = drop.Name:lower()
				if dropName:find(resourceType) then
					local dist = (drop.Handle.Position - position).Magnitude
					if dist <= 10 then
						local amount = drop:GetAttribute('Amount') or 1
						count = count + amount
					end
				end
			end
		end
		return count
	end

	local CompactGenerators = {}

	local function rebuildCompactGenerators()
		table.clear(CompactGenerators)
		scanDescendants(workspace, function(obj)
			if obj.Name == 'GeneratorAdornee' then
				local ok, generatorId = pcall(function() return obj:GetAttribute('Id') end)
				if ok and generatorId and type(generatorId) == 'string' and generatorId ~= '' then
					local genType = getGeneratorType(generatorId)
					if genType == 'diamond' or genType == 'emerald' then
						table.insert(CompactGenerators, {obj = obj, genType = genType})
					end
				end
			end
		end)
	end

	local function updateCompactUI()
		if not GeneratorESP.Enabled or UIStyle.Value ~= 'Compact' then
			compactUI.Enabled = false
			return
		end
		compactUI.Enabled = true
		local bestDiamondTime = math.huge
		local bestEmeraldTime = math.huge
		for i = #CompactGenerators, 1, -1 do
			local entry = CompactGenerators[i]
			if not entry.obj or not entry.obj.Parent then
				table.remove(CompactGenerators, i)
				continue
			end
			local countdownText = getCountdownText(entry.obj)
			if countdownText and countdownText.Text then
				local timeLeft = extractSecondsFromText(countdownText.Text)
				if entry.genType == 'diamond' and timeLeft > 0 and timeLeft < bestDiamondTime then
					bestDiamondTime = timeLeft
				elseif entry.genType == 'emerald' and timeLeft > 0 and timeLeft < bestEmeraldTime then
					bestEmeraldTime = timeLeft
				end
			end
		end
		local showDiamond = CompactDiamondToggle and CompactDiamondToggle.Enabled
		local showEmerald = CompactEmeraldToggle and CompactEmeraldToggle.Enabled

		if not showDiamond and not showEmerald then
			compactUI.Enabled = false
			return
		end

		diamondFrame.Visible = showDiamond
		emeraldFrame.Visible = showEmerald

		if showDiamond then
			diamondFrame.Position = UDim2.new(0, 10, 0, 35)
		end
		if showEmerald then
			emeraldFrame.Position = UDim2.new(0, 10, 0, showDiamond and 65 or 35)
		end

		diamondTimes[1] = bestDiamondTime ~= math.huge and bestDiamondTime or 0
		emeraldTimes[1] = bestEmeraldTime ~= math.huge and bestEmeraldTime or 0
		if bestDiamondTime == math.huge then
			diamondTimer.Text = "00"
		else
			diamondTimer.Text = string.format("%02d", bestDiamondTime)
			if bestDiamondTime <= 5 then
				diamondTimer.TextColor3 = Color3.fromRGB(255, 50, 50)
			elseif bestDiamondTime <= 10 then
				diamondTimer.TextColor3 = Color3.fromRGB(255, 165, 0)
			else
				diamondTimer.TextColor3 = Color3.fromRGB(85, 200, 255)
			end
		end
		if bestEmeraldTime == math.huge then
			emeraldTimer.Text = "00"
		else
			emeraldTimer.Text = string.format("%02d", bestEmeraldTime)
			if bestEmeraldTime <= 5 then
				emeraldTimer.TextColor3 = Color3.fromRGB(255, 50, 50)
			elseif bestEmeraldTime <= 10 then
				emeraldTimer.TextColor3 = Color3.fromRGB(255, 165, 0)
			else
				emeraldTimer.TextColor3 = Color3.fromRGB(0, 255, 100)
			end
		end
	end

	local function clearAllESP()
		Folder:ClearAllChildren()
		table.clear(Reference)
		compactUI.Enabled = false
	end

	local function createESP(generatorAdornee, genType, config, position, teamId)
		if not isGeneratorEnabled(genType, teamId) then return end
		if Reference[generatorAdornee] then return end

		if UIStyle.Value == 'Compact' then
			Reference[generatorAdornee] = {
				genType = genType,
				position = position,
				teamId = teamId,
				isTeamGen = config.isTeamGen
			}
			return
		end
		if not generatorAdornee or not generatorAdornee.Parent then return end

		local displayColor = config.color
		local teamName = nil
		if config.isTeamGen and teamId and teamColors[teamId] then
			displayColor = teamColors[teamId].color
			teamName = teamColors[teamId].name
		end

		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'generator-esp-' .. genType
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = generatorAdornee
		billboard.Enabled = true  

		if config.isTeamGen then
			billboard.Size = UDim2.fromOffset(180, 55)
			billboard.StudsOffsetWorldSpace = Vector3.new(0, 5, 0)
		else
			billboard.Size = UDim2.fromOffset(80, 30)
			billboard.StudsOffsetWorldSpace = Vector3.new(0, 4, 0)
		end

		local blur = pcall(function()
			return addBlur(billboard)
		end) and blur or nil
		if blur then
			blur.Visible = true
		end

		if config.isTeamGen and teamName then
			local dot = Instance.new('Frame')
			dot.Name = 'TeamDot'
			dot.Parent = billboard
			dot.Size = UDim2.fromOffset(8, 8)
			dot.Position = UDim2.new(0, 10, 0, 5)
			dot.BackgroundColor3 = displayColor
			dot.BorderSizePixel = 0
			local dotCorner = Instance.new('UICorner')
			dotCorner.CornerRadius = UDim.new(1, 0)
			dotCorner.Parent = dot

			local teamLabel = Instance.new('TextLabel')
			teamLabel.Name = 'TeamLabel'
			teamLabel.Parent = billboard
			teamLabel.BackgroundTransparency = 1
			teamLabel.Size = UDim2.new(1, 0, 0, 18)
			teamLabel.Position = UDim2.new(0, 0, 0, 0)
			teamLabel.Text = teamName
			teamLabel.TextColor3 = displayColor
			teamLabel.TextSize = 13
			teamLabel.Font = Enum.Font.GothamBold
			teamLabel.TextStrokeTransparency = 0.4
			teamLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
			teamLabel.TextXAlignment = Enum.TextXAlignment.Center
		end

		local frame = Instance.new('Frame')
		frame.Size = config.isTeamGen and UDim2.new(1, 0, 0, 35) or UDim2.fromScale(1, 1)
		frame.Position = config.isTeamGen and UDim2.new(0, 0, 0, 20) or UDim2.new(0, 0, 0, 0)
		frame.BackgroundColor3 = Color3.new(0, 0, 0)
		frame.BackgroundTransparency = 0.3
		frame.BorderSizePixel = 0
		frame.Parent = billboard

		if config.isTeamGen and teamId and teamColors[teamId] then
			local stripe = Instance.new('Frame')
			stripe.Name = 'TeamStripe'
			stripe.Parent = frame
			stripe.Size = UDim2.new(0, 3, 1, 0)
			stripe.Position = UDim2.new(0, 0, 0, 0)
			stripe.BackgroundColor3 = displayColor
			stripe.BorderSizePixel = 0
			local stripeCorner = Instance.new('UICorner')
			stripeCorner.CornerRadius = UDim.new(0, 3)
			stripeCorner.Parent = stripe
		end

		local uicorner2 = Instance.new('UICorner')
		uicorner2.CornerRadius = UDim.new(0, 6)
		uicorner2.Parent = frame

		if config.isTeamGen then
			local tierLabel = Instance.new('TextLabel')
			tierLabel.Name = 'Tier'
			tierLabel.Size = UDim2.new(0, 25, 1, 0)
			tierLabel.Position = UDim2.new(0, 8, 0, 0)
			tierLabel.BackgroundTransparency = 1
			tierLabel.Text = "0"
			tierLabel.TextColor3 = Color3.fromRGB(255, 255, 100)
			tierLabel.TextSize = 16
			tierLabel.Font = Enum.Font.GothamBold
			tierLabel.TextStrokeTransparency = 0.5
			tierLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
			tierLabel.Parent = frame

			local resources = {
				{name = 'iron',    color = Color3.fromRGB(200, 200, 200), icon = 'iron',    xOffset = 35},
				{name = 'diamond', color = Color3.fromRGB(85, 200, 255),  icon = 'diamond', xOffset = 85},
				{name = 'emerald', color = Color3.fromRGB(0, 255, 100),   icon = 'emerald', xOffset = 135}
			}

			local resourceLabels = {}
			for _, resource in ipairs(resources) do
				local iconImage = getProperIcon(resource.icon)
				if iconImage then
					local image = Instance.new('ImageLabel')
					image.Size = UDim2.fromOffset(18, 18)
					image.Position = UDim2.new(0, resource.xOffset, 0.5, 0)
					image.AnchorPoint = Vector2.new(0, 0.5)
					image.BackgroundTransparency = 1
					image.Image = iconImage
					image.Parent = frame
				end
				local countLabel = Instance.new('TextLabel')
				countLabel.Name = resource.name .. '_count'
				countLabel.Size = UDim2.new(0, 25, 1, 0)
				countLabel.Position = UDim2.new(0, resource.xOffset + 20, 0, 0)
				countLabel.BackgroundTransparency = 1
				countLabel.Text = "0"
				countLabel.TextColor3 = resource.color
				countLabel.TextSize = 16
				countLabel.Font = Enum.Font.GothamBold
				countLabel.TextStrokeTransparency = 0.5
				countLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
				countLabel.TextXAlignment = Enum.TextXAlignment.Left
				countLabel.Parent = frame
				resourceLabels[resource.name] = countLabel
			end

			Reference[generatorAdornee] = {
				billboard = billboard,
				tierLabel = tierLabel,
				ironLabel = resourceLabels.iron,
				diamondLabel = resourceLabels.diamond,
				emeraldLabel = resourceLabels.emerald,
				genType = genType,
				position = position,
				teamId = teamId,
				isTeamGen = true
			}
		else
			local iconImage = getProperIcon(config.icon)
			if iconImage then
				local image = Instance.new('ImageLabel')
				image.Size = UDim2.fromOffset(20, 20)
				image.Position = UDim2.new(0, 5, 0.5, 0)
				image.AnchorPoint = Vector2.new(0, 0.5)
				image.BackgroundTransparency = 1
				image.Image = iconImage
				image.Parent = frame
			end
			local timerLabel = Instance.new('TextLabel')
			timerLabel.Name = 'Timer'
			timerLabel.Size = UDim2.new(0, 30, 1, 0)
			timerLabel.Position = UDim2.new(0.5, 0, 0, 0)
			timerLabel.AnchorPoint = Vector2.new(0.5, 0)
			timerLabel.BackgroundTransparency = 1
			timerLabel.Text = "00"
			timerLabel.TextColor3 = displayColor
			timerLabel.TextSize = 18
			timerLabel.Font = Enum.Font.GothamBold
			timerLabel.TextStrokeTransparency = 0.5
			timerLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
			timerLabel.Parent = frame
			local amountLabel = Instance.new('TextLabel')
			amountLabel.Name = 'Amount'
			amountLabel.Size = UDim2.new(0, 20, 1, 0)
			amountLabel.Position = UDim2.new(1, -20, 0, 0)
			amountLabel.BackgroundTransparency = 1
			amountLabel.Text = "0"
			amountLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
			amountLabel.TextSize = 16
			amountLabel.Font = Enum.Font.GothamBold
			amountLabel.TextStrokeTransparency = 0.5
			amountLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
			amountLabel.Parent = frame
			Reference[generatorAdornee] = {
				billboard = billboard,
				timerLabel = timerLabel,
				amountLabel = amountLabel,
				genType = genType,
				position = position,
				teamId = teamId,
				isTeamGen = false
			}
		end
		billboard.Enabled = true
	end

	local function updateESP(generatorAdornee)
		local ref = Reference[generatorAdornee]
		if not ref then return end
		if UIStyle.Value == 'Compact' then return end

		if ref.isTeamGen then
			if ref.tierLabel then
				local tierTextLabel = getTierText(generatorAdornee)
				if tierTextLabel and tierTextLabel.Text then
					ref.tierLabel.Text = extractTierLevel(tierTextLabel.Text)
				else
					ref.tierLabel.Text = "0"
				end
			end
			if ref.ironLabel then
				ref.ironLabel.Text = tostring(getResourceCount(ref.position, 'iron'))
			end
			if ref.diamondLabel then
				ref.diamondLabel.Text = tostring(getResourceCount(ref.position, 'diamond'))
			end
			if ref.emeraldLabel then
				ref.emeraldLabel.Text = tostring(getResourceCount(ref.position, 'emerald'))
			end
		else
			local countdownText = getCountdownText(generatorAdornee)
			if countdownText and countdownText.Text then
				local timeLeft = extractSecondsFromText(countdownText.Text)
				if ref.timerLabel then
					ref.timerLabel.Text = string.format("%02d", timeLeft)
					if timeLeft <= 5 then
						ref.timerLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
					elseif timeLeft <= 10 then
						ref.timerLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
					else
						ref.timerLabel.TextColor3 = generatorTypes[ref.genType].color
					end
				end
			else
				if ref.timerLabel then
					ref.timerLabel.Text = "00"
					ref.timerLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
				end
			end
			if ref.amountLabel then
				ref.amountLabel.Text = tostring(getResourceCount(ref.position, ref.genType))
			end
		end
	end

	local function processGeneratorAdornee(obj)
		if obj.Name ~= 'GeneratorAdornee' then return end
		local ok, generatorId = pcall(function() return obj:GetAttribute('Id') end)
		if not ok then return end
		if generatorId == nil then return end
		if type(generatorId) ~= 'string' then return end
		if generatorId == '' then return end

		local position = obj:GetPivot().Position
		local genType, config = getGeneratorType(generatorId)
		if not genType or not config then return end

		local teamId = getGeneratorTeamId(generatorId)
		if isGeneratorEnabled(genType, teamId) then
			createESP(obj, genType, config, position, teamId)
		end
	end

	local function findAllGenerators()
		scanDescendants(workspace, function(obj)
			pcall(processGeneratorAdornee, obj)
		end)
	end

	local function refreshESP()
		clearAllESP()
		if GeneratorESP.Enabled then
			findAllGenerators()
		end
	end

	local updateTimer = 0

	GeneratorESP = vape.Categories.Render:CreateModule({
		Name = 'GeneratorESP',
		Function = function(callback)
			if callback then
				findAllGenerators()
				rebuildCompactGenerators()

				GeneratorESP:Clean(workspace.DescendantAdded:Connect(function(obj)
					if not GeneratorESP.Enabled then return end
					task.wait(0.2)
					pcall(processGeneratorAdornee, obj)
					if obj.Name == 'GeneratorAdornee' then
						rebuildCompactGenerators()
					end
				end))

				GeneratorESP:Clean(runService.Heartbeat:Connect(function(dt)
					if not GeneratorESP.Enabled then return end
					updateTimer = updateTimer + dt
					if updateTimer < 0.2 then return end
					updateTimer = 0
					for generatorAdornee, ref in pairs(Reference) do
						if generatorAdornee and generatorAdornee.Parent then
							updateESP(generatorAdornee)
						else
							if ref.billboard then ref.billboard:Destroy() end
							Reference[generatorAdornee] = nil
						end
					end
					updateCompactUI()
				end))

				GeneratorESP:Clean(workspace.DescendantRemoving:Connect(function(obj)
					if not GeneratorESP.Enabled then return end
					if Reference[obj] then
						if Reference[obj].billboard then Reference[obj].billboard:Destroy() end
						Reference[obj] = nil
					end
				end))
			else
				clearAllESP()
			end
		end,
		Tooltip = 'esp for generators showing timer and items count'
	})

	UIStyle = GeneratorESP:CreateDropdown({
		Name = 'UI Style',
		List = {'Original', 'Compact'},
		Default = 'Original',
		Function = function(val)
			local isOriginal = val == 'Original'
			if DiamondToggle then DiamondToggle.Object.Visible = isOriginal end
			if EmeraldToggle then EmeraldToggle.Object.Visible = isOriginal end
			if TeamGenToggle then TeamGenToggle.Object.Visible = isOriginal end
			if ShowOwnTeamGen then ShowOwnTeamGen.Object.Visible = isOriginal and TeamGenToggle.Enabled end
			if ShowEnemyTeamGen then ShowEnemyTeamGen.Object.Visible = isOriginal and TeamGenToggle.Enabled end
			if CompactDiamondToggle then CompactDiamondToggle.Object.Visible = not isOriginal end
			if CompactEmeraldToggle then CompactEmeraldToggle.Object.Visible = not isOriginal end
			refreshESP()
		end,
		Tooltip = 'pick between the norm billboard esp on generators or a ui with timers for generators '
	})

	DiamondToggle = GeneratorESP:CreateToggle({
		Name = 'Diamond',
		Function = function() refreshESP() end,
		Default = false,
		Visible = true
	})

	EmeraldToggle = GeneratorESP:CreateToggle({
		Name = 'Emerald',
		Function = function() refreshESP() end,
		Default = false,
		Visible = true
	})

	CompactDiamondToggle = GeneratorESP:CreateToggle({
		Name = 'Compact Diamond',
		Default = false,
		Visible = false,
		Function = function()
			refreshESP()
		end
	})

	CompactEmeraldToggle = GeneratorESP:CreateToggle({
		Name = 'Compact Emerald',
		Default = false,
		Visible = false,
		Function = function()
			refreshESP()
		end
	})

	TeamGenToggle = GeneratorESP:CreateToggle({
		Name = 'Team Generators',
		Function = function(callback)
			if ShowOwnTeamGen then ShowOwnTeamGen.Object.Visible = callback end
			if ShowEnemyTeamGen then ShowEnemyTeamGen.Object.Visible = callback end
			refreshESP()
		end,
		Default = true
	})

	ShowOwnTeamGen = GeneratorESP:CreateToggle({
		Name = 'Show Own Team',
		Function = function() refreshESP() end,
		Default = false,
		Visible = true
	})

	ShowEnemyTeamGen = GeneratorESP:CreateToggle({
		Name = 'Show Enemy Teams',
		Function = function() refreshESP() end,
		Default = true,
		Visible = true
	})
end)

run(function()
	local SkinChanger
	local ItemSkinDropdown
	local SkinTypeDropdown

	local RS = game:GetService('ReplicatedStorage')
	local LocalPlayer = lplr
	local RESKIN_NAME = 'LOCAL_ITEM_RESKIN'

	local state = {
		itemSkin = 'Victorious Lyla',
		skinType = 'Nightmare',
	}

	local ok1, ItemType = pcall(function()
		return require(RS.TS.item['item-type']).ItemType
	end)
	if not ok1 then ItemType = {} end

	local ok2, ItemSkinType = pcall(function()
		return require(RS.TS.games.bedwars['item-skin']['item-skin-types']).ItemSkinType
	end)
	if not ok2 then ItemSkinType = {} end

	local KitSkinCtrl
	pcall(function()
		local KC = require(RS.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
		KitSkinCtrl = KC.Controllers.KitSkinController
	end)

	local BOW_ROT = CFrame.Angles(0, math.rad(-90), 0)
	local CROSSBOW_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(-360), 0)
	local LUNAR_CROSSBOW_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, -190, math.rad(-180))
	local VICTORIOUS_ARCHER_BOW_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, -52, math.rad(90))
	local VICTORIOUS_ARCHER_CROSSBOW_ROT = CFrame.new(0.00, 0.00, 0.00) * CFrame.Angles(math.rad(0), math.rad(80), math.rad(0.00))
	local VICTORIOUS_ARCHER_HEADHUNTER_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(0), 0)
	local HEADHUNTER_ROT = CFrame.new(0.4, 0, 0) * CFrame.Angles(0, math.rad(360), 0)
	local AXE_ROT = CFrame.new(0, 0, -0.4) * CFrame.Angles(0, math.rad(90), 0)
	local PICKAXE_ROT = CFrame.new(0, 0, -0.1) * CFrame.Angles(0, math.rad(110), 0)
	local DARKHEART_PICKAXE_ROT = CFrame.new(0, 0, -0.1) * CFrame.Angles(0, math.rad(90), 0)
	local LASSO_ROT = CFrame.Angles(0, math.rad(90), 0)
	local STAFF_ROT = CFrame.Angles(0, math.rad(90), 0)
	local PIXEL_SWORD_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local PIXEL_SWORD_SHORT_ROT = CFrame.new(0, -0.6, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local BALLOON_SWORD_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local SWORD_ROT = CFrame.new(0, -1.7, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local HEARTBEAM_SWORD_ROT = CFrame.new(0, -1.2, 0) * CFrame.Angles(0, math.rad(0), 0)
	local RABBIT_BEAM_SWORD_ROT = CFrame.new(0, -1.5, 0) * CFrame.Angles(0, math.rad(0), 0)
	local LIFE_BOW_ROT = CFrame.Angles(0, math.rad(-20), 0)
	local DAO_ROT = CFrame.new(0, -1.7, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local VIC_ROT = CFrame.new(0, -1.9, 0) * CFrame.Angles(0, math.rad(360), 0)
	local HEXED_DAO_ROT = CFrame.new(0.00, 0.00, 0.00) * CFrame.Angles(math.rad(180.00), math.rad(-4.00), math.rad(0.00))
	local SNOW_DAO_ROT = CFrame.new(-0.2, -0.9, 0) * CFrame.Angles(0, math.rad(-180), 0)
	local HARPOON_ROT = CFrame.new(0, -1.4, -0.15) * CFrame.Angles(0, math.rad(180), 0)
	local TRIDENT_ROT = CFrame.new(0, 0.5, 0.05) * CFrame.Angles(0, math.rad(180), 0)
	local LYLA_BOW_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(30, -30, 183.56)
	local LYLA_CROSSBOW_ROT = CFrame.Angles(math.rad(0), math.rad(180), math.rad(0))
	local LYLA_HEADHUNTER_ROT = CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(0), 0)
	local LYLA_FROST_CROSSBOW_ROT = CFrame.new(0.00, 0.00, 0.00) * CFrame.Angles(math.rad(180.00), math.rad(178.00), math.rad(0.00))

	local CANNON_HAND_SCALE = 0.34
	local CANNON_PLACED_OFFSET = CFrame.new(0, -1.0, 0)
	local CANNON_TOOL_NAME = "cannon"

	local CANNON_SKIN_NAMES = {
		["Victorious Cannon"] = {
			Gold = "cannon_gold_victorious",
			Platinum = "cannon_platinum_victorious",
			Diamond = "cannon_diamond_victorious",
			Emerald = "cannon_emerald_victorious",
			Nightmare = "cannon_nightmare_victorious",
		},
		["Ghost Cannon"] = { Default = "cannon_ghost" },
		["Deep Sea Cannon"] = { Default = "cannon_deepsea" },
	}

	local CANNON_SOUND_NAMES = {
		Gold = "CANNON_FIRE_VICTORIOUS_NIGHTMARE",
		Platinum = "CANNON_FIRE_VICTORIOUS_NIGHTMARE",
		Diamond = "CANNON_FIRE_VICTORIOUS_DIAMOND",
		Emerald = "CANNON_FIRE_VICTORIOUS_EMERALD",
		Nightmare = "CANNON_FIRE_VICTORIOUS_NIGHTMARE",
	}

	local SKIN_OFFSETS = {
		["nightmare_victorious_flower_bow"] = LYLA_BOW_ROT,
		["emerald_victorious_flower_bow"] = LYLA_BOW_ROT,
		["diamond_victorious_flower_bow"] = LYLA_BOW_ROT,
		["platinum_victorious_flower_bow"] = LYLA_BOW_ROT,
		["gold_victorious_flower_bow"] = LYLA_BOW_ROT,
		["nightmare_victorious_flower_crossbow"] = LYLA_CROSSBOW_ROT,
		["emerald_victorious_flower_crossbow"] = LYLA_CROSSBOW_ROT,
		["diamond_victorious_flower_crossbow"] = LYLA_CROSSBOW_ROT,
		["platinum_victorious_flower_crossbow"] = LYLA_CROSSBOW_ROT,
		["gold_victorious_flower_crossbow"] = LYLA_CROSSBOW_ROT,
		["nightmare_victorious_flower_headhunter"] = LYLA_HEADHUNTER_ROT,
		["emerald_victorious_flower_headhunter"] = LYLA_HEADHUNTER_ROT,
		["diamond_victorious_flower_headhunter"] = LYLA_HEADHUNTER_ROT,
		["platinum_victorious_flower_headhunter"] = LYLA_HEADHUNTER_ROT,
		["gold_victorious_flower_headhunter"] = LYLA_HEADHUNTER_ROT,
		["tactical_headhunter_victorious_nightmare"] = VICTORIOUS_ARCHER_HEADHUNTER_ROT,
		["tactical_headhunter_victorious_emerald"] = VICTORIOUS_ARCHER_HEADHUNTER_ROT,
		["tactical_headhunter_victorious_diamond"] = VICTORIOUS_ARCHER_HEADHUNTER_ROT,
		["tactical_headhunter_victorious_platinum"] = VICTORIOUS_ARCHER_HEADHUNTER_ROT,
		["tactical_headhunter_victorious_gold"] = VICTORIOUS_ARCHER_HEADHUNTER_ROT,
		["flower_bow_frost_queen"] = BOW_ROT,
		["tactical_crossbow_lunar_dragon"] = LUNAR_CROSSBOW_ROT,
		["life_bow_mummy"] = LIFE_BOW_ROT,
		["flower_headhunter_frost_queen"] = HEADHUNTER_ROT,
		["flower_crossbow_frost_queen"] = LYLA_FROST_CROSSBOW_ROT,
		["wood_sword_darkvalentine"] = SWORD_ROT,
		["stone_sword_darkvalentine"] = SWORD_ROT,
		["iron_sword_darkvalentine"] = SWORD_ROT,
		["diamond_sword_darkvalentine"] = SWORD_ROT,
		["emerald_sword_darkvalentine"] = SWORD_ROT,
		["wood_sword_heartbeam"] = HEARTBEAM_SWORD_ROT,
		["stone_sword_heartbeam"] = HEARTBEAM_SWORD_ROT,
		["iron_sword_heartbeam"] = HEARTBEAM_SWORD_ROT,
		["diamond_sword_heartbeam"] = HEARTBEAM_SWORD_ROT,
		["emerald_sword_heartbeam"] = HEARTBEAM_SWORD_ROT,
		["rabbit_beam_wood_sword"] = RABBIT_BEAM_SWORD_ROT,
		["rabbit_beam_stone_sword"] = RABBIT_BEAM_SWORD_ROT,
		["rabbit_beam_iron_sword"] = RABBIT_BEAM_SWORD_ROT,
		["rabbit_beam_diamond_sword"] = RABBIT_BEAM_SWORD_ROT,
		["rabbit_beam_emerald_sword"] = RABBIT_BEAM_SWORD_ROT,
		["wood_bow_victorious_nightmare"] = VICTORIOUS_ARCHER_BOW_ROT,
		["wood_bow_victorious_emerald"] = VICTORIOUS_ARCHER_BOW_ROT,
		["wood_bow_victorious_diamond"] = VICTORIOUS_ARCHER_BOW_ROT,
		["wood_bow_victorious_platinum"] = VICTORIOUS_ARCHER_BOW_ROT,
		["wood_bow_victorious_gold"] = VICTORIOUS_ARCHER_BOW_ROT,
		["tactical_crossbow_victorious_nightmare"] = VICTORIOUS_ARCHER_CROSSBOW_ROT,
		["tactical_crossbow_victorious_emerald"] = VICTORIOUS_ARCHER_CROSSBOW_ROT,
		["tactical_crossbow_victorious_diamond"] = VICTORIOUS_ARCHER_CROSSBOW_ROT,
		["tactical_crossbow_victorious_platinum"] = VICTORIOUS_ARCHER_CROSSBOW_ROT,
		["tactical_crossbow_victorious_gold"] = VICTORIOUS_ARCHER_CROSSBOW_ROT,
		["life_crossbow_mummy"] = CROSSBOW_ROT,
		["life_headhunter_mummy"] = HEADHUNTER_ROT,
		["victorious_gold_triton"] = TRIDENT_ROT,
		["victorious_platinum_triton"] = TRIDENT_ROT,
		["victorious_diamond_triton"] = TRIDENT_ROT,
		["victorious_emerald_triton"] = TRIDENT_ROT,
		["victorious_nightmare_triton"] = TRIDENT_ROT,
		["demon_triton"] = HARPOON_ROT,
		["lasso_mummy"] = LASSO_ROT,
		["lasso_wrangler_reindeer_lassy"] = LASSO_ROT,
		["lasso_lifeguard"] = LASSO_ROT,
		["wood_axe_darkvalentine"] = AXE_ROT,
		["stone_axe_darkvalentine"] = AXE_ROT,
		["iron_axe_darkvalentine"] = AXE_ROT,
		["diamond_axe_darkvalentine"] = AXE_ROT,
		["wood_axe_valentine"] = AXE_ROT,
		["stone_axe_valentine"] = AXE_ROT,
		["iron_axe_valentine"] = AXE_ROT,
		["diamond_axe_valentine"] = AXE_ROT,
		["wood_pickaxe_darkvalentine"] = DARKHEART_PICKAXE_ROT,
		["stone_pickaxe_darkvalentine"] = DARKHEART_PICKAXE_ROT,
		["iron_pickaxe_darkvalentine"] = DARKHEART_PICKAXE_ROT,
		["diamond_pickaxe_darkvalentine"] = DARKHEART_PICKAXE_ROT,
		["wood_pickaxe_valentine"] = PICKAXE_ROT,
		["stone_pickaxe_valentine"] = PICKAXE_ROT,
		["iron_pickaxe_valentine"] = PICKAXE_ROT,
		["diamond_pickaxe_valentine"] = PICKAXE_ROT,
		["gold_victorious_wizard_staff"] = STAFF_ROT,
		["gold_victorious_wizard_staff_2"] = STAFF_ROT,
		["gold_victorious_wizard_staff_3"] = STAFF_ROT,
		["platinum_victorious_wizard_staff"] = STAFF_ROT,
		["platinum_victorious_wizard_staff_2"] = STAFF_ROT,
		["platinum_victorious_wizard_staff_3"] = STAFF_ROT,
		["diamond_victorious_wizard_staff"] = STAFF_ROT,
		["diamond_victorious_wizard_staff_2"] = STAFF_ROT,
		["diamond_victorious_wizard_staff_3"] = STAFF_ROT,
		["emerald_victorious_wizard_staff"] = STAFF_ROT,
		["emerald_victorious_wizard_staff_2"] = STAFF_ROT,
		["emerald_victorious_wizard_staff_3"] = STAFF_ROT,
		["nightmare_victorious_wizard_staff"] = STAFF_ROT,
		["nightmare_victorious_wizard_staff_2"] = STAFF_ROT,
		["nightmare_victorious_wizard_staff_3"] = STAFF_ROT,
		["wood_dao_victorious"] = VIC_ROT,
		["stone_dao_victorious"] = VIC_ROT,
		["iron_dao_victorious"] = VIC_ROT,
		["diamond_dao_victorious"] = VIC_ROT,
		["emerald_dao_victorious"] = VIC_ROT,
		["wood_dao_cursed"] = HEXED_DAO_ROT,
		["stone_dao_cursed"] = HEXED_DAO_ROT,
		["iron_dao_cursed"] = HEXED_DAO_ROT,
		["diamond_dao_cursed"] = HEXED_DAO_ROT,
		["emerald_dao_cursed"] = HEXED_DAO_ROT,
		["wood_dao_tiger"] = DAO_ROT,
		["stone_dao_tiger"] = DAO_ROT,
		["iron_dao_tiger"] = DAO_ROT,
		["diamond_dao_tiger"] = DAO_ROT,
		["emerald_dao_tiger"] = DAO_ROT,
		["wood_dao_snow_rabbit"] = SNOW_DAO_ROT,
		["stone_dao_snow_rabbit"] = SNOW_DAO_ROT,
		["iron_dao_snow_rabbit"] = SNOW_DAO_ROT,
		["diamond_dao_snow_rabbit"] = SNOW_DAO_ROT,
		["emerald_dao_snow_rabbit"] = SNOW_DAO_ROT,
		["wood_sword_pixel"] = PIXEL_SWORD_ROT,
		["stone_sword_pixel"] = PIXEL_SWORD_ROT,
		["iron_sword_pixel"] = PIXEL_SWORD_ROT,
		["diamond_sword_pixel"] = PIXEL_SWORD_ROT,
		["emerald_sword_pixel"] = PIXEL_SWORD_ROT,
		["wood_sword_short_pixel"] = PIXEL_SWORD_SHORT_ROT,
		["stone_sword_short_pixel"] = PIXEL_SWORD_SHORT_ROT,
		["iron_sword_short_pixel"] = PIXEL_SWORD_SHORT_ROT,
		["diamond_sword_short_pixel"] = PIXEL_SWORD_SHORT_ROT,
		["emerald_sword_short_pixel"] = PIXEL_SWORD_SHORT_ROT,
		["balloon_wood_sword"] = BALLOON_SWORD_ROT,
		["balloon_stone_sword"] = BALLOON_SWORD_ROT,
		["balloon_iron_sword"] = BALLOON_SWORD_ROT,
		["balloon_diamond_sword"] = BALLOON_SWORD_ROT,
		["balloon_emerald_sword"] = BALLOON_SWORD_ROT,
	}

	local KIT_SKIN_MAP = {
		["Victorious Lyla"] = { Gold = "gold_victorious_lyla", Platinum = "platinum_victorious_lyla", Diamond = "diamond_victorious_lyla", Emerald = "emerald_victorious_lyla", Nightmare = "nightmare_victorious_lyla" },
		["Frost Queen Lyla"] = { Default = "flower_bee_frost_queen" },
		["Victorious Archer"] = { Gold = "archer_victorious_gold", Platinum = "archer_victorious_platinum", Diamond = "archer_victorious_diamond", Emerald = "archer_victorious_emerald", Nightmare = "archer_victorious_nightmare" },
		["Lunar Dragon Archer"] = { Default = "archer_lunar_dragon" },
		["Victorious Yuzi"] = { Default = "yuzi_victorious" },
		["Hexed Yuzi"] = { Default = "dasher_cursed" },
		["Tiger Yuzi"] = { Default = "dasher_tiger" },
		["Snow Rabbit Yuzi"] = { Default = "dasher_snow_rabbit" },
		["Victorious Zeno"] = { Gold = "gold_victorious_wizard", Platinum = "platinum_victorious_wizard", Diamond = "diamond_victorious_wizard", Emerald = "emerald_victorious_wizard", Nightmare = "nightmare_victorious_wizard" },
		["Victorious Triton"] = { Gold = "victorious_gold_triton", Platinum = "victorious_platinum_triton", Diamond = "victorious_diamond_triton", Emerald = "victorious_emerald_triton", Nightmare = "victorious_nightmare_triton" },
		["Demon Triton"] = { Default = "demon_triton" },
		["Mummy Life Bow"] = { Default = "mummy_nazar" },
		["Mummy Lasso"] = { Default = "cowgirl_mummy" },
		["Victorious Cannon"] = { Gold = "gold_victorious_davey", Platinum = "platinum_victorious_davey", Diamond = "diamond_victorious_davey", Emerald = "emerald_victorious_davey", Nightmare = "nightmare_victorious_davey" },
		["Ghost Cannon"] = { Default = "davey_ghost" },
		["Deep Sea Cannon"] = { Default = "davey_deepsea" },
	}

	local STORE_SKIN_MAP = {
		["Balloon Swords"] = function() return { { ItemType.WOOD_SWORD, ItemSkinType.BALLOON_WOOD_SWORD }, { ItemType.STONE_SWORD, ItemSkinType.BALLOON_STONE_SWORD }, { ItemType.IRON_SWORD, ItemSkinType.BALLOON_IRON_SWORD }, { ItemType.DIAMOND_SWORD, ItemSkinType.BALLOON_DIAMOND_SWORD }, { ItemType.EMERALD_SWORD, ItemSkinType.BALLOON_EMERALD_SWORD } } end,
		["Banana Swords"] = function() return { { ItemType.WOOD_SWORD, ItemSkinType.BANANA_WOOD_SWORD }, { ItemType.STONE_SWORD, ItemSkinType.BANANA_STONE_SWORD }, { ItemType.IRON_SWORD, ItemSkinType.BANANA_IRON_SWORD }, { ItemType.DIAMOND_SWORD, ItemSkinType.BANANA_DIAMOND_SWORD }, { ItemType.EMERALD_SWORD, ItemSkinType.BANANA_EMERALD_SWORD } } end,
		["Heartbeam Swords"] = function() return { { ItemType.WOOD_SWORD, ItemSkinType.HEARTBEAM_WOOD_SWORD }, { ItemType.STONE_SWORD, ItemSkinType.HEARTBEAM_STONE_SWORD }, { ItemType.IRON_SWORD, ItemSkinType.HEARTBEAM_IRON_SWORD }, { ItemType.DIAMOND_SWORD, ItemSkinType.HEARTBEAM_DIAMOND_SWORD }, { ItemType.EMERALD_SWORD, ItemSkinType.HEARTBEAM_EMERALD_SWORD } } end,
		["Valentine Set"] = function() return { { ItemType.WOOD_SWORD, ItemSkinType.VALENTINE_WOOD_SWORD }, { ItemType.STONE_SWORD, ItemSkinType.VALENTINE_STONE_SWORD }, { ItemType.IRON_SWORD, ItemSkinType.VALENTINE_IRON_SWORD }, { ItemType.DIAMOND_SWORD, ItemSkinType.VALENTINE_DIAMOND_SWORD }, { ItemType.EMERALD_SWORD, ItemSkinType.VALENTINE_EMERALD_SWORD }, { ItemType.WOOD_PICKAXE, ItemSkinType.VALENTINE_WOOD_PICKAXE }, { ItemType.STONE_PICKAXE, ItemSkinType.VALENTINE_STONE_PICKAXE }, { ItemType.IRON_PICKAXE, ItemSkinType.VALENTINE_IRON_PICKAXE }, { ItemType.DIAMOND_PICKAXE, ItemSkinType.VALENTINE_DIAMOND_PICKAXE }, { ItemType.WOOD_AXE, ItemSkinType.VALENTINE_WOOD_AXE }, { ItemType.STONE_AXE, ItemSkinType.VALENTINE_STONE_AXE }, { ItemType.IRON_AXE, ItemSkinType.VALENTINE_IRON_AXE }, { ItemType.DIAMOND_AXE, ItemSkinType.VALENTINE_DIAMOND_AXE } } end,
		["Darkheart Set"] = function() return { { ItemType.WOOD_SWORD, ItemSkinType.DARKVALENTINE_WOOD_SWORD }, { ItemType.STONE_SWORD, ItemSkinType.DARKVALENTINE_STONE_SWORD }, { ItemType.IRON_SWORD, ItemSkinType.DARKVALENTINE_IRON_SWORD }, { ItemType.DIAMOND_SWORD, ItemSkinType.DARKVALENTINE_DIAMOND_SWORD }, { ItemType.EMERALD_SWORD, ItemSkinType.DARKVALENTINE_EMERALD_SWORD }, { ItemType.WOOD_PICKAXE, ItemSkinType.DARKVALENTINE_WOOD_PICKAXE }, { ItemType.STONE_PICKAXE, ItemSkinType.DARKVALENTINE_STONE_PICKAXE }, { ItemType.IRON_PICKAXE, ItemSkinType.DARKVALENTINE_IRON_PICKAXE }, { ItemType.DIAMOND_PICKAXE, ItemSkinType.DARKVALENTINE_DIAMOND_PICKAXE }, { ItemType.WOOD_AXE, ItemSkinType.DARKVALENTINE_WOOD_AXE }, { ItemType.STONE_AXE, ItemSkinType.DARKVALENTINE_STONE_AXE }, { ItemType.IRON_AXE, ItemSkinType.DARKVALENTINE_IRON_AXE }, { ItemType.DIAMOND_AXE, ItemSkinType.DARKVALENTINE_DIAMOND_AXE } } end,
		["Mummy Life Bow"] = function() return { { ItemType.LIFE_BOW, ItemSkinType.LIFE_BOW_MUMMY }, { ItemType.LIFE_CROSSBOW, ItemSkinType.LIFE_CROSSBOW_MUMMY }, { ItemType.LIFE_HEADHUNTER, ItemSkinType.LIFE_HEADHUNTER_MUMMY } } end,
		["Mummy Lasso"] = function() return { { ItemType.LASSO, ItemSkinType.LASSO_MUMMY } } end,
	}

	local function yuziDaoMap(suffix)
		return {
			wood_dao = "wood_dao_" .. suffix,
			stone_dao = "stone_dao_" .. suffix,
			iron_dao = "iron_dao_" .. suffix,
			diamond_dao = "diamond_dao_" .. suffix,
			emerald_dao = "emerald_dao_" .. suffix,
		}
	end

	local SKIN_DATA = {
		["Victorious Lyla"] = function(t)
			local lt = t:lower()
			return {
				flower_bow = lt .. "_victorious_flower_bow",
				flower_crossbow = lt .. "_victorious_flower_crossbow",
				flower_headhunter = lt .. "_victorious_flower_headhunter",
			}
		end,
		["Frost Queen Lyla"] = function()
			return {
				flower_bow = "flower_bow_frost_queen",
				flower_crossbow = "flower_crossbow_frost_queen",
				flower_headhunter = "flower_headhunter_frost_queen",
			}
		end,
		["Victorious Archer"] = function(t)
			local lt = t:lower()
			return {
				wood_bow = "wood_bow_victorious_" .. lt,
				tactical_crossbow = "tactical_crossbow_victorious_" .. lt,
				tactical_headhunter = "tactical_headhunter_victorious_" .. lt,
			}
		end,
		["Lunar Dragon Archer"] = function()
			return {
				wood_bow = "wood_bow_lunar_dragon",
				tactical_crossbow = "tactical_crossbow_lunar_dragon",
				tactical_headhunter = "tactical_headhunter_lunar_dragon",
			}
		end,
		["Victorious Triton"] = function(t)
			return { harpoon = "victorious_" .. t:lower() .. "_triton" }
		end,
		["Demon Triton"] = function() return { harpoon = "demon_triton" } end,
		["Victorious Yuzi"] = function() return yuziDaoMap("victorious") end,
		["Hexed Yuzi"] = function() return yuziDaoMap("cursed") end,
		["Tiger Yuzi"] = function() return yuziDaoMap("tiger") end,
		["Snow Rabbit Yuzi"] = function() return yuziDaoMap("snow_rabbit") end,
		["Victorious Zeno"] = function(t)
			local lt = t:lower()
			return {
				wizard_staff = lt .. "_victorious_wizard_staff",
				wizard_staff_2 = lt .. "_victorious_wizard_staff_2",
				wizard_staff_3 = lt .. "_victorious_wizard_staff_3",
			}
		end,
		["Balloon Swords"] = function() return { wood_sword = "balloon_wood_sword", stone_sword = "balloon_stone_sword", iron_sword = "balloon_iron_sword", diamond_sword = "balloon_diamond_sword", emerald_sword = "balloon_emerald_sword" } end,
		["Banana Swords"] = function() return { wood_sword = "banana_wood_sword", stone_sword = "banana_stone_sword", iron_sword = "banana_iron_sword", diamond_sword = "banana_diamond_sword", emerald_sword = "banana_emerald_sword" } end,
		["Valentine Set"] = function() return { wood_sword = "wood_sword_valentine", stone_sword = "stone_sword_valentine", iron_sword = "iron_sword_valentine", diamond_sword = "diamond_sword_valentine", emerald_sword = "emerald_sword_valentine", wood_pickaxe = "wood_pickaxe_valentine", stone_pickaxe = "stone_pickaxe_valentine", iron_pickaxe = "iron_pickaxe_valentine", diamond_pickaxe = "diamond_pickaxe_valentine", wood_axe = "wood_axe_valentine", stone_axe = "stone_axe_valentine", iron_axe = "iron_axe_valentine", diamond_axe = "diamond_axe_valentine" } end,
		["Darkheart Set"] = function() return { wood_sword = "wood_sword_darkvalentine", stone_sword = "stone_sword_darkvalentine", iron_sword = "iron_sword_darkvalentine", diamond_sword = "diamond_sword_darkvalentine", emerald_sword = "emerald_sword_darkvalentine", wood_pickaxe = "wood_pickaxe_darkvalentine", stone_pickaxe = "stone_pickaxe_darkvalentine", iron_pickaxe = "iron_pickaxe_darkvalentine", diamond_pickaxe = "diamond_pickaxe_darkvalentine", wood_axe = "wood_axe_darkvalentine", stone_axe = "stone_axe_darkvalentine", iron_axe = "iron_axe_darkvalentine", diamond_axe = "diamond_axe_darkvalentine" } end,
		["Heartbeam Swords"] = function() return { wood_sword = "wood_sword_heartbeam", stone_sword = "stone_sword_heartbeam", iron_sword = "iron_sword_heartbeam", diamond_sword = "diamond_sword_heartbeam", emerald_sword = "emerald_sword_heartbeam" } end,
		["Rabbit Beam Sword"] = function() return { wood_sword = "rabbit_beam_wood_sword", stone_sword = "rabbit_beam_stone_sword", iron_sword = "rabbit_beam_iron_sword", diamond_sword = "rabbit_beam_diamond_sword", emerald_sword = "rabbit_beam_emerald_sword" } end,
		["Valentine Axes"] = function() return { wood_axe = "wood_axe_valentine", stone_axe = "stone_axe_valentine", iron_axe = "iron_axe_valentine", diamond_axe = "diamond_axe_valentine" } end,
		["Mummy Lasso"] = function() return { lasso = "lasso_mummy" } end,
		["Mummy Life Bow"] = function() return { life_bow = "life_bow_mummy", life_crossbow = "life_crossbow_mummy", life_headhunter = "life_headhunter_mummy" } end,
		["Pixel Swords"] = function() return { wood_sword = "wood_sword_pixel", stone_sword = "stone_sword_pixel", iron_sword = "iron_sword_pixel", diamond_sword = "diamond_sword_pixel", emerald_sword = "emerald_sword_pixel" } end,
		["Pixel Swords Short"] = function() return { wood_sword = "wood_sword_short_pixel", stone_sword = "stone_sword_short_pixel", iron_sword = "iron_sword_short_pixel", diamond_sword = "diamond_sword_short_pixel", emerald_sword = "emerald_sword_short_pixel" } end,
	}

	local TIERED_SKINS = {
		["Victorious Lyla"] = true,
		["Victorious Archer"] = true,
		["Victorious Zeno"] = true,
		["Victorious Triton"] = true,
		["Victorious Cannon"] = true,
	}

	local active = {}
	local hideList = {}
	local hookConns = {}
	local savedStoreSkins = {}
	local renderConn
	local oldGetKitSkin
	local oldFireCannon
	local oldLaunchSelf
	local soundsHooked = false
	local refreshVersion = 0

	local function normalizeName(s)
		return s:lower():gsub('[_%s%-]', '')
	end

	local function isCannonSkin()
		return CANNON_SKIN_NAMES[state.itemSkin] ~= nil
	end

	local function getCannonSkinName()
		local tbl = CANNON_SKIN_NAMES[state.itemSkin]
		if not tbl then return nil end
		return tbl[state.skinType] or tbl.Default
	end

	local function getCurrentMappings()
		local fn = SKIN_DATA[state.itemSkin]
		if not fn then return {} end
		local ok, res = pcall(fn, state.skinType)
		if ok and type(res) == 'table' then return res end
		return {}
	end

	local function getKitSkinValue()
		local m = KIT_SKIN_MAP[state.itemSkin]
		if not m then return nil end
		return m[state.skinType] or m.Default
	end

	local function getStoreSkins()
		local fn = STORE_SKIN_MAP[state.itemSkin]
		if not fn then return {} end
		local ok, res = pcall(fn)
		if ok and type(res) == 'table' then return res end
		return {}
	end

	local function itemsFolder()
		return RS:FindFirstChild('Items')
	end

	local function blocksFolder()
		local assets = RS:FindFirstChild('Assets')
		return assets and assets:FindFirstChild('Blocks')
	end

	local function firstBasePart(root)
		if root:IsA('BasePart') then return root end
		for _, d in root:GetDescendants() do
			if d:IsA('BasePart') then return d end
		end
		return nil
	end

	local function setNoCollide(model)
		local function strip(p)
			p.CanCollide = false
			p.CanTouch = false
			p.CanQuery = false
			p.Massless = true
			p.Anchored = false
		end
		if model:IsA('BasePart') then strip(model) end
		for _, d in model:GetDescendants() do
			if d:IsA('BasePart') then strip(d) end
		end
	end

	local function weldAllTo(anchor, container)
		for _, d in container:GetDescendants() do
			if d:IsA('BasePart') and d ~= anchor then
				local wc = Instance.new('WeldConstraint')
				wc.Part0 = anchor
				wc.Part1 = d
				wc.Parent = anchor
			end
		end
	end

	local function getRigScale(root)
		local node = root
		local hum
		for _ = 1, 6 do
			if not node or node == workspace then break end
			hum = node:FindFirstChildOfClass('Humanoid')
			if hum then break end
			node = node.Parent
		end
		if not hum then return 1 end
		local total, count = 0, 0
		for _, name in {'BodyHeightScale', 'BodyWidthScale', 'BodyDepthScale'} do
			local v = hum:FindFirstChild(name)
			if v and v:IsA('NumberValue') and v.Value > 0 then
				total = total + v.Value
				count = count + 1
			end
		end
		if count == 0 then return 1 end
		return math.clamp(total / count, 0.3, 3)
	end

	local function startRenderLoop()
		if renderConn then return end
		renderConn = runService.RenderStepped:Connect(function()
			for i = #hideList, 1, -1 do
				local p = hideList[i]
				if p and p.Parent then
					p.LocalTransparencyModifier = 1
				else
					table.remove(hideList, i)
				end
			end
		end)
	end

	local function stopRenderLoop()
		if renderConn then
			pcall(function() renderConn:Disconnect() end)
			renderConn = nil
		end
	end

	local function hideDescendant(entry, d)
		if d:IsA('BasePart') then
			entry.parts[#entry.parts + 1] = {d, d.Transparency}
			d.Transparency = 1
			d.LocalTransparencyModifier = 1
			hideList[#hideList + 1] = d
		elseif d:IsA('Decal') or d:IsA('Texture') then
			entry.decals[#entry.decals + 1] = {d, d.Transparency}
			d.Transparency = 1
		end
	end

	local function hideOriginal(entry, root)
		for _, d in root:GetDescendants() do
			if not d:IsDescendantOf(entry.clone) then
				hideDescendant(entry, d)
			end
		end
	end

	local function detach(root)
		local entry = active[root]
		if not entry then return end
		active[root] = nil
		for _, c in entry.conns do
			pcall(function() c:Disconnect() end)
		end
		if entry.clone then
			pcall(function() entry.clone:Destroy() end)
		end
		for _, rec in entry.parts do
			local p = rec[1]
			local idx = table.find(hideList, p)
			if idx then table.remove(hideList, idx) end
			pcall(function()
				if p.Parent then
					p.Transparency = rec[2]
					p.LocalTransparencyModifier = 0
				end
			end)
		end
		for _, rec in entry.decals do
			local d = rec[1]
			pcall(function()
				if d.Parent then d.Transparency = rec[2] end
			end)
		end
	end

	local function detachAll()
		for root in active do
			detach(root)
		end
		table.clear(active)
		table.clear(hideList)
	end

	local function attachReskin(root, skinName, source, opts)
		if not root or not root.Parent then return end
		if active[root] or not source then return end
		opts = opts or {}

		local origHandle = root:FindFirstChild('Handle')
		if not (origHandle and origHandle:IsA('BasePart')) then
			origHandle = firstBasePart(root)
		end
		if not origHandle then return end

		local clone = source:Clone()
		clone.Name = RESKIN_NAME
		for _, d in clone:GetDescendants() do
			if d:IsA('LuaSourceContainer') then
				pcall(function() d:Destroy() end)
			end
		end
		setNoCollide(clone)

		local anchor
		local alignByPivot = false
		if clone:IsA('BasePart') then
			anchor = clone
		else
			local handle = clone:FindFirstChild('Handle')
			if handle and handle:IsA('BasePart') then
				anchor = handle
			else
				alignByPivot = true
				anchor = clone:IsA('Model') and clone.PrimaryPart or nil
				anchor = anchor or firstBasePart(clone)
			end
		end
		if not anchor then
			pcall(function() clone:Destroy() end)
			return
		end
		if clone:IsA('Model') and not clone.PrimaryPart then
			pcall(function() clone.PrimaryPart = anchor end)
		end

		local scale = (opts.scale or 1) * getRigScale(root)
		if clone:IsA('Model') and math.abs(scale - 1) > 0.001 then
			pcall(function() clone:ScaleTo(scale) end)
		end

		local entry = {clone = clone, parts = {}, decals = {}, conns = {}}
		active[root] = entry
		clone.Parent = root

		local offset = (SKIN_OFFSETS[skinName] or CFrame.identity) * (opts.offset or CFrame.identity)
		local target = origHandle.CFrame * offset
		pcall(function()
			if not clone:IsA('Model') then
				anchor.CFrame = target
			elseif alignByPivot then
				clone:PivotTo(target)
			else
				clone:PivotTo(target * anchor.CFrame:ToObjectSpace(clone:GetPivot()))
			end
		end)

		weldAllTo(anchor, clone)

		local weld = Instance.new('WeldConstraint')
		weld.Part0 = origHandle
		weld.Part1 = anchor
		weld.Parent = anchor

		hideOriginal(entry, root)
		startRenderLoop()

		entry.conns[#entry.conns + 1] = root.DescendantAdded:Connect(function(d)
			if entry.clone and entry.clone.Parent and d:IsDescendantOf(entry.clone) then return end
			hideDescendant(entry, d)
		end)
		entry.conns[#entry.conns + 1] = root.AncestryChanged:Connect(function(_, parent)
			if not parent then detach(root) end
		end)
	end

	local function track(conn)
		hookConns[#hookConns + 1] = conn
		return conn
	end

	local function clearHooks()
		for _, c in hookConns do
			pcall(function() c:Disconnect() end)
		end
		table.clear(hookConns)
	end

	local function deferCall(fn, ...)
		local args = table.pack(...)
		task.spawn(function()
			task.wait()
			fn(table.unpack(args, 1, args.n))
		end)
	end

	local function skinNameForChild(childName)
		local mappings = getCurrentMappings()
		local skinName = mappings[childName:lower()]
		if not skinName then
			local norm = normalizeName(childName)
			for k, v in mappings do
				if normalizeName(k) == norm then
					skinName = v
					break
				end
			end
		end
		return skinName
	end

	local function applyItem(child)
		if not (SkinChanger and SkinChanger.Enabled) then return end
		if isCannonSkin() or not child or not child.Parent then return end
		local skinName = skinNameForChild(child.Name)
		if not skinName then return end
		local folder = itemsFolder()
		if not folder then return end
		attachReskin(child, skinName, folder:FindFirstChild(skinName))
	end

	local function applyCannon(child, placed)
		if not (SkinChanger and SkinChanger.Enabled) then return end
		if not isCannonSkin() or not child or not child.Parent then return end
		if child.Name ~= CANNON_TOOL_NAME then return end
		local skinName = getCannonSkinName()
		if not skinName then return end
		local folder = blocksFolder()
		if not folder then return end
		attachReskin(child, skinName, folder:FindFirstChild(skinName), {
			offset = placed and CANNON_PLACED_OFFSET or CFrame.identity,
			scale = placed and 1 or CANNON_HAND_SCALE,
		})
	end

	local function applyProjectile(inst)
		if not (SkinChanger and SkinChanger.Enabled) then return end
		if isCannonSkin() or not inst or not inst.Parent then return end
		if not inst:IsA('Model') or inst.Name ~= 'harpoon_projectile' then return end
		local shooter = inst:GetAttribute('ProjectileShooter')
		if not shooter or tostring(shooter) ~= tostring(LocalPlayer.UserId) then return end
		local skinName = getCurrentMappings().harpoon
		if not skinName then return end
		local folder = itemsFolder()
		if not folder then return end
		attachReskin(inst, skinName, folder:FindFirstChild(skinName))
	end

	local function hookItemContainer(container)
		if not container then return end
		for _, child in container:GetChildren() do
			deferCall(applyItem, child)
		end
		track(container.ChildAdded:Connect(function(child)
			deferCall(applyItem, child)
		end))
	end

	local function hookCannonContainer(container, placed)
		if not container then return end
		for _, child in container:GetChildren() do
			deferCall(applyCannon, child, placed)
		end
		track(container.ChildAdded:Connect(function(child)
			deferCall(applyCannon, child, placed)
		end))
	end

	local function hookViewmodel(vm)
		if not vm then return end
		if isCannonSkin() then
			hookCannonContainer(vm, false)
		else
			hookItemContainer(vm)
		end
	end

	local function hookCamera(cam)
		if not cam then return end
		local vm = cam:FindFirstChild('Viewmodel')
		if vm then hookViewmodel(vm) end
		track(cam.ChildAdded:Connect(function(child)
			if child.Name == 'Viewmodel' then
				deferCall(hookViewmodel, child)
			end
		end))
	end

	local function hookWorldCannons()
		local map = workspace:FindFirstChild('Map')
		if not map then return end
		local worlds = map:FindFirstChild('Worlds')
		if not worlds then return end
		local function hookWorld(world)
			local blocks = world:FindFirstChild('Blocks')
			if blocks then hookCannonContainer(blocks, true) end
		end
		for _, world in worlds:GetChildren() do
			hookWorld(world)
		end
		track(worlds.ChildAdded:Connect(function(world)
			deferCall(hookWorld, world)
		end))
	end

	local function hookProjectiles()
		track(workspace.DescendantAdded:Connect(function(inst)
			if inst.Name == 'harpoon_projectile' then
				deferCall(applyProjectile, inst)
			end
		end))
	end

	local function applyKitSkinHook()
		if not KitSkinCtrl then return end
		local val = getKitSkinValue()
		if not val then return end
		if not oldGetKitSkin then oldGetKitSkin = KitSkinCtrl.getKitSkin end
		local base = oldGetKitSkin
		KitSkinCtrl.getKitSkin = function(self, char)
			if char == LocalPlayer.Character then return val end
			return base(self, char)
		end
	end

	local function removeKitSkinHook()
		if KitSkinCtrl and oldGetKitSkin then
			KitSkinCtrl.getKitSkin = oldGetKitSkin
			oldGetKitSkin = nil
		end
	end

	local function applyStoreSkins()
		if not (bedwars and bedwars.Store) then return end
		local skins = getStoreSkins()
		if #skins == 0 then return end
		local ok, st = pcall(function() return bedwars.Store:getState() end)
		if not ok then return end
		local selected = st and st.Locker and st.Locker.selectedItemSkins
		savedStoreSkins = {}
		for _, pair in skins do
			if pair[1] and pair[2] then
				savedStoreSkins[#savedStoreSkins + 1] = {pair[1], selected and selected[pair[1]]}
				pcall(function()
					bedwars.Store:dispatch({type = 'LockerSetItemSkin', itemType = pair[1], itemSkin = pair[2]})
				end)
			end
		end
	end

	local function clearStoreSkins()
		if #savedStoreSkins == 0 then return end
		if bedwars and bedwars.Store then
			for _, saved in savedStoreSkins do
				pcall(function()
					bedwars.Store:dispatch({type = 'LockerSetItemSkin', itemType = saved[1], itemSkin = saved[2]})
				end)
			end
		end
		savedStoreSkins = {}
	end

	local function hookCannonSounds()
		if soundsHooked then return end
		if not (bedwars and bedwars.CannonHandController) then return end
		soundsHooked = true
		oldFireCannon = bedwars.CannonHandController.fireCannon
		oldLaunchSelf = bedwars.CannonHandController.launchSelf

		local function replaceSound()
			pcall(function()
				local pool = workspace:FindFirstChild('SoundPool')
				if pool then
					for _, v in pool:GetChildren() do
						if v:IsA('Sound') and v.SoundId == 'rbxassetid://7121064180' then
							v:Destroy()
						end
					end
				end
				local key = CANNON_SOUND_NAMES[state.skinType] or CANNON_SOUND_NAMES.Nightmare
				if bedwars.SoundManager and bedwars.SoundList and bedwars.SoundList[key] then
					bedwars.SoundManager:playSound(bedwars.SoundList[key])
				end
			end)
		end

		if oldFireCannon then
			bedwars.CannonHandController.fireCannon = function(...)
				replaceSound()
				return oldFireCannon(...)
			end
		end
		if oldLaunchSelf then
			bedwars.CannonHandController.launchSelf = function(...)
				replaceSound()
				return oldLaunchSelf(...)
			end
		end
	end

	local function unhookCannonSounds()
		if soundsHooked and bedwars and bedwars.CannonHandController then
			if oldFireCannon then bedwars.CannonHandController.fireCannon = oldFireCannon end
			if oldLaunchSelf then bedwars.CannonHandController.launchSelf = oldLaunchSelf end
		end
		oldFireCannon = nil
		oldLaunchSelf = nil
		soundsHooked = false
	end

	local function hookCharacter(char)
		if not char then return end
		local backpack = LocalPlayer:FindFirstChildOfClass('Backpack')
		if isCannonSkin() then
			hookCannonContainer(char, false)
			hookCannonContainer(backpack, false)
		else
			hookItemContainer(char)
			hookItemContainer(backpack)
		end
	end

	local function setup()
		if not (SkinChanger and SkinChanger.Enabled) then return end
		applyKitSkinHook()
		hookCamera(workspace.CurrentCamera)
		track(workspace:GetPropertyChangedSignal('CurrentCamera'):Connect(function()
			if SkinChanger.Enabled then hookCamera(workspace.CurrentCamera) end
		end))
		if isCannonSkin() then
			hookCannonSounds()
			hookWorldCannons()
		else
			applyStoreSkins()
			hookProjectiles()
		end
		hookCharacter(LocalPlayer.Character)
		track(LocalPlayer.CharacterAdded:Connect(function(char)
			task.spawn(function()
				task.wait(0.25)
				if not SkinChanger.Enabled then return end
				applyKitSkinHook()
				hookCharacter(char)
			end)
		end))
	end

	local function teardown()
		clearHooks()
		detachAll()
		stopRenderLoop()
		removeKitSkinHook()
		clearStoreSkins()
		unhookCannonSounds()
	end

	local function refresh()
		if not (SkinChanger and SkinChanger.Enabled) then return end
		refreshVersion = refreshVersion + 1
		local version = refreshVersion
		teardown()
		task.defer(function()
			if version ~= refreshVersion then return end
			if SkinChanger.Enabled then setup() end
		end)
	end

	local function syncSkinTypeVisibility()
		if SkinTypeDropdown and SkinTypeDropdown.Object then
			SkinTypeDropdown.Object.Visible = TIERED_SKINS[state.itemSkin] == true
		end
	end

	local skinNames = {}
	local seenNames = {}
	for name in SKIN_DATA do
		if not seenNames[name] then
			seenNames[name] = true
			skinNames[#skinNames + 1] = name
		end
	end
	for name in CANNON_SKIN_NAMES do
		if not seenNames[name] then
			seenNames[name] = true
			skinNames[#skinNames + 1] = name
		end
	end
	table.sort(skinNames)

	SkinChanger = vape.Categories.Render:CreateModule({
		Name = 'SkinChanger',
		Function = function(enabled)
			if enabled then
				setup()
			else
				teardown()
			end
		end,
		Tooltip = 'gives ur items a diff skin, only u can see it so dont trip'
	})

	vape:Clean(function() teardown() end)

	ItemSkinDropdown = SkinChanger:CreateDropdown({
		Name = 'Item Skin',
		List = skinNames,
		Default = state.itemSkin,
		Function = function(val)
			state.itemSkin = val
			if TIERED_SKINS[val] then
				state.skinType = SkinTypeDropdown and SkinTypeDropdown.Value or state.skinType
			else
				state.skinType = 'Default'
			end
			syncSkinTypeVisibility()
			refresh()
		end,
	})

	SkinTypeDropdown = SkinChanger:CreateDropdown({
		Name = 'Skin Type',
		List = {'Gold', 'Platinum', 'Diamond', 'Emerald', 'Nightmare', 'Default'},
		Default = state.skinType,
		Visible = false,
		Function = function(val)
			if not TIERED_SKINS[state.itemSkin] then return end
			state.skinType = val
			refresh()
		end,
	})

	task.spawn(function()
		for _ = 1, 24 do
			syncSkinTypeVisibility()
			task.wait(0.25)
		end
	end)
end)

run(function()
	local InventoryESP
	local TeamCheck
	local Kits
	local Whitelist
	local Tools
	local Weapon
	local Loot
	local PersonalChest
	local Filter
	local FilterMode
	local FilterRange
	local FilterPlayers
	local warnedNames = {}
	local gui
	local rows = {}
	local thumbCache = {}
	local ICON_SIZE = 18
	local MAX_PER_ROW = 8
	local function teamColor(plr)
		local t = tonumber(plr:GetAttribute('Team'))
		local entry = t and getgenv().aeroTeamColors[t]
		return entry and entry.color or Color3.fromRGB(255, 255, 255)
	end
	local PLACEHOLDER = 'rbxassetid://6031075931' 
	local lootNames = {iron = true, gold = true, diamond = true, emerald = true}

	local function classify(itemType)
		if lootNames[itemType] or itemType:find('iron') or itemType:find('diamond') or itemType:find('emerald') or itemType:find('gold') then
			return 'loot'
		end
		local meta = bedwars.ItemMeta[itemType]
		if meta then
			if meta.sword or meta.projectile or meta.projectileSource or itemType:find('bow') or itemType:find('sword') then
				return 'weapon'
			end
			if meta.breakBlock or itemType:find('pickaxe') or itemType:find('axe') or itemType:find('shear') then
				return 'tool'
			end
		end
		return 'etc'
	end

	local function passesWhitelist(itemType)
		if not Whitelist.Enabled then return true end
		local c = classify(itemType)
		if c == 'tool' then return Tools.Enabled end
		if c == 'weapon' then return Weapon.Enabled end
		if c == 'loot' then return Loot.Enabled end
		return false 
	end

	local function makeGui()
		gui = Instance.new('ScreenGui')
		gui.Name = 'InventoryESP'
		gui.ResetOnSpawn = false
		gui.IgnoreGuiInset = true
		gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
		gui.DisplayOrder = 50
		pcall(function() gui.Parent = gethui and gethui() or game:GetService('CoreGui') end)
		if not gui.Parent then gui.Parent = lplr:WaitForChild('PlayerGui') end

		local holder = Instance.new('Frame')
		holder.Name = 'Holder'
		holder.AnchorPoint = Vector2.new(1, 0.5)
		holder.Position = UDim2.new(1, -2, 0.5, 0)
		holder.Size = UDim2.new(0, 190, 0.9, 0)
		holder.BackgroundTransparency = 1
		holder.Parent = gui

		local list = Instance.new('UIListLayout')
		list.FillDirection = Enum.FillDirection.Vertical
		list.HorizontalAlignment = Enum.HorizontalAlignment.Left
		list.VerticalAlignment = Enum.VerticalAlignment.Center
		list.Padding = UDim.new(0, 4)
		list.SortOrder = Enum.SortOrder.Name
		list.Parent = holder
	end

	local function getThumb(userId)
		if not thumbCache[userId] then
			thumbCache[userId] = 'rbxthumb://type=AvatarHeadShot&id=' .. userId .. '&w=150&h=150'
		end
		return thumbCache[userId]
	end

	local function invFolder(plr, personal)
		local inv = replicatedStorage:FindFirstChild('Inventories')
		if not inv then return nil end
		return inv:FindFirstChild(personal and (plr.Name .. '_personal') or plr.Name)
	end

	local function makeSection(parent, titleText)
		local wrap = Instance.new('Frame')
		wrap.Name = titleText
		wrap.BackgroundTransparency = 1
		wrap.Size = UDim2.new(1, 0, 0, 0)
		wrap.AutomaticSize = Enum.AutomaticSize.Y
		wrap.Parent = parent

		local wl = Instance.new('UIListLayout')
		wl.Padding = UDim.new(0, 2)
		wl.SortOrder = Enum.SortOrder.LayoutOrder
		wl.Parent = wrap

		local title = Instance.new('TextLabel')
		title.Name = 'Title'
		title.LayoutOrder = 0
		title.BackgroundTransparency = 1
		title.Size = UDim2.new(1, 0, 0, 12)
		title.Text = titleText
		title.TextColor3 = Color3.fromRGB(160, 200, 255)
		title.TextXAlignment = Enum.TextXAlignment.Left
		title.TextSize = 11
		title.Font = Enum.Font.GothamBold
		title.Parent = wrap

		local grid = Instance.new('Frame')
		grid.Name = 'Grid'
		grid.LayoutOrder = 1
		grid.BackgroundTransparency = 1
		grid.Size = UDim2.new(1, 0, 0, 0)
		grid.AutomaticSize = Enum.AutomaticSize.Y
		grid.Parent = wrap

		local gl = Instance.new('UIGridLayout')
		gl.CellSize = UDim2.new(0, ICON_SIZE, 0, ICON_SIZE)
		gl.CellPadding = UDim2.new(0, 3, 0, 3)
		gl.FillDirectionMaxCells = MAX_PER_ROW
		gl.SortOrder = Enum.SortOrder.LayoutOrder
		gl.Parent = grid

		return {wrap = wrap, grid = grid, title = title, icons = {}}
	end

	local function buildRow(plr)
		local frame = Instance.new('Frame')
		frame.Name = plr.Name
		frame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
		frame.BackgroundTransparency = 0.25
		frame.BorderSizePixel = 0
		frame.Size = UDim2.new(1, 0, 0, 44)
		frame.AutomaticSize = Enum.AutomaticSize.Y
		frame.Parent = gui.Holder
		local corner = Instance.new('UICorner')
		corner.CornerRadius = UDim.new(0, 8)
		corner.Parent = frame
		local stroke = Instance.new('UIStroke')
		stroke.Thickness = 1
		stroke.Color = Color3.fromRGB(60, 60, 70)
		stroke.Transparency = 0.3
		stroke.Parent = frame
		local pad = Instance.new('UIPadding')
		pad.PaddingLeft = UDim.new(0, 4)
		pad.PaddingRight = UDim.new(0, 4)
		pad.PaddingTop = UDim.new(0, 3)
		pad.PaddingBottom = UDim.new(0, 3)
		pad.Parent = frame
		local left = Instance.new('Frame')
		left.Name = 'Left'
		left.BackgroundTransparency = 1
		left.Size = UDim2.new(0, 30, 1, 0)
		left.Parent = frame
		local name = Instance.new('TextLabel')
		name.Name = 'PName'
		name.BackgroundTransparency = 1
		name.Position = UDim2.new(0, 0, 0, 0)
		name.Size = UDim2.new(1, 0, 0, 12)
		name.Text = plr.DisplayName or plr.Name
		name.TextColor3 = teamColor(plr)
		name.TextXAlignment = Enum.TextXAlignment.Left
		name.TextSize = 11
		name.Font = Enum.Font.GothamBold
		name.TextTruncate = Enum.TextTruncate.AtEnd
		name.ZIndex = 3
		name.Parent = frame
		local face = Instance.new('ImageLabel')
		face.Name = 'Face'
		face.AnchorPoint = Vector2.new(0.5, 0)
		face.BackgroundColor3 = Color3.fromRGB(40, 40, 48)
		face.Size = UDim2.new(0, 24, 0, 24)
		face.Position = UDim2.new(0.5, 0, 0, 16) 
		face.Image = getThumb(plr.UserId)
		face.Parent = left
		local fc = Instance.new('UICorner')
		fc.CornerRadius = UDim.new(1, 0)
		fc.Parent = face
		local fs = Instance.new('UIStroke')
		fs.Thickness = 1.5
		fs.Color = teamColor(plr)
		fs.Parent = face
		local right = Instance.new('Frame')
		right.Name = 'Right'
		right.BackgroundTransparency = 1
		right.Position = UDim2.new(0, 34, 0, 14)
		right.Size = UDim2.new(1, -34, 0, 0)
		right.AutomaticSize = Enum.AutomaticSize.Y
		right.Parent = frame
		local rl = Instance.new('UIListLayout')
		rl.Padding = UDim.new(0, 4)
		rl.SortOrder = Enum.SortOrder.LayoutOrder
		rl.Parent = right

		local kiticon = Instance.new('ImageLabel')
		kiticon.Name = 'KitIcon'
		kiticon.AnchorPoint = Vector2.new(1, 0)
		kiticon.Position = UDim2.new(1, -2, 0, 0)
		kiticon.Size = UDim2.new(0, 20, 0, 20)
		kiticon.BackgroundTransparency = 1
		kiticon.ScaleType = Enum.ScaleType.Fit
		kiticon.Visible = false
		kiticon.ZIndex = 4
		kiticon.Parent = frame

		rows[plr] = {
			frame = frame,
			pname = name,
			pstroke = fs,
			kiticon = kiticon,
			inv = makeSection(right, 'Inventory'),
			personal = makeSection(right, 'Personal')
		}
		rows[plr].inv.wrap.LayoutOrder = 0
		rows[plr].personal.wrap.LayoutOrder = 1
		return rows[plr]
	end

	local function updateSection(section, folder, personalMode)
		local seen = {}
		local order = 0
		if folder then
			for _, item in folder:GetChildren() do
				local itemType = item.Name
				if personalMode or passesWhitelist(itemType) then
					seen[itemType] = true
					order += 1
					local icon = section.icons[itemType]
					if not icon then
						icon = Instance.new('ImageLabel')
						icon.Name = itemType
						icon.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
						icon.BackgroundTransparency = 0.2
						icon.ClipsDescendants = true
						icon.Parent = section.grid
						local ic = Instance.new('UICorner')
						ic.CornerRadius = UDim.new(0, 4)
						ic.Parent = icon
						local amt = Instance.new('TextLabel')
						amt.Name = 'Amt'
						amt.BackgroundTransparency = 1
						amt.Size = UDim2.new(1, 0, 1, 0)
						amt.Position = UDim2.new(0, 0, 0, 2)
						amt.TextColor3 = Color3.fromRGB(255, 255, 255)
						amt.TextStrokeTransparency = 0.2
						amt.TextXAlignment = Enum.TextXAlignment.Right
						amt.TextYAlignment = Enum.TextYAlignment.Bottom
						amt.TextSize = 9
						amt.TextScaled = false
						amt.Font = Enum.Font.GothamBold
						amt.Parent = icon
						local img = PLACEHOLDER
						pcall(function()
							local got = bedwars.getIcon({itemType = itemType}, true)
							if got and got ~= '' then img = got end
						end)
						icon.Image = img
						section.icons[itemType] = icon
					end
					icon.LayoutOrder = order
					local count = item:GetAttribute('Amount') or item:GetAttribute('amount') or item:GetAttribute('Count')
					icon.Amt.Text = (count and count > 1) and tostring(count) or ''
				end
			end
		end
		for iname, icon in section.icons do
			if not seen[iname] then
				icon:Destroy()
				section.icons[iname] = nil
			end
		end
		section.wrap.Visible = order > 0
	end

	local function passesFilter(plr)
		if not Filter or not Filter.Enabled then return true end
		if FilterMode.Value == 'Range' then
			local myRoot = entitylib.character and entitylib.character.RootPart
			local theirRoot = plr.Character and plr.Character:FindFirstChild('HumanoidRootPart')
			if not myRoot or not theirRoot then return false end
			return (theirRoot.Position - myRoot.Position).Magnitude <= FilterRange.Value
		end
		if FilterMode.Value == 'Players' then
			local list = FilterPlayers.ListEnabled
			if #list == 0 then return false end
			local lname = plr.Name:lower()
			local ldisp = plr.DisplayName:lower()
			for _, v in list do
				local lv = tostring(v):lower()
				if lv == lname or lv == ldisp then return true end
			end
			return false
		end
		return true
	end

	local function removeRow(plr)
		if rows[plr] then
			rows[plr].frame:Destroy()
			rows[plr] = nil
		end
	end

	InventoryESP = vape.Categories.Render:CreateModule({
		Name = 'InventoryESP',
		Function = function(callback)
			if callback then
				makeGui()
				repeat
					local myTeam = lplr:GetAttribute('Team')
					for _, plr in playersService:GetPlayers() do
						local skip = false
						if TeamCheck.Enabled and (plr == lplr or (plr:GetAttribute('Team') and plr:GetAttribute('Team') == myTeam)) then
							skip = true
						end
						if not skip and plr ~= lplr and not passesFilter(plr) then
							skip = true
						end
						if skip then
							removeRow(plr)
						else
							local liveInv = invFolder(plr, false)
							local personalInv = PersonalChest.Enabled and invFolder(plr, true) or nil
							local anything = (liveInv and #liveInv:GetChildren() > 0) or (personalInv and #personalInv:GetChildren() > 0)
							if anything then
								local row = rows[plr] or buildRow(plr)
								local col = teamColor(plr)
								row.pname.TextColor3 = col
								row.pstroke.Color = col
								if Kits.Enabled then
									local kit = plr:GetAttribute('PlayingAsKits')
									local kitImage = kit and kitImageIds[kit:lower()]
									row.kiticon.Image = kitImage or kitImageIds['none']
									row.kiticon.Visible = true
								else
									row.kiticon.Visible = false
								end
								updateSection(row.inv, liveInv, false)
								updateSection(row.personal, personalInv, true)
								row.personal.wrap.Visible = PersonalChest.Enabled and row.personal.wrap.Visible
							else
								removeRow(plr)
							end
						end
					end
					for plr in rows do
						if not plr.Parent then removeRow(plr) end
					end
					task.wait(0.5)
				until not InventoryESP.Enabled
			else
				for plr in rows do removeRow(plr) end
				rows = {}
				if gui then gui:Destroy() gui = nil end
			end
		end,
		Tooltip = 'shows what everyone has in their inventory (n chest) on the side gng'
	})

	Whitelist = InventoryESP:CreateToggle({
		Name = 'Whitelist',
		Tooltip = 'only show the categories u pick below',
		Function = function(callback)
			if Tools then Tools.Object.Visible = callback end
			if Weapon then Weapon.Object.Visible = callback end
			if Loot then Loot.Object.Visible = callback end
		end
	})
	Tools = InventoryESP:CreateToggle({
		Name = 'Tools',
		Default = true,
		Darker = true,
		Visible = false,
		Tooltip = 'pickaxes, axes, shears'
	})
	Weapon = InventoryESP:CreateToggle({
		Name = 'Weapon',
		Default = true,
		Darker = true,
		Visible = false,
		Tooltip = 'swords n projectiles'
	})
	Loot = InventoryESP:CreateToggle({
		Name = 'Loot',
		Default = true,
		Darker = true,
		Visible = false,
		Tooltip = 'iron, gold, diamond, emerald'
	})
	PersonalChest = InventoryESP:CreateToggle({
		Name = 'Personal Chest',
		Tooltip = 'also show their personal chest items'
	})
	TeamCheck = InventoryESP:CreateToggle({
		Name = 'Team check',
		Default = true,
		Tooltip = 'dont show ppl on ur team'
	})

	Kits = InventoryESP:CreateToggle({
		Name = 'Kits',
		Default = false,
		Tooltip = 'puts the kit icon next to each persons name'
	})

	local function syncFilterVisibility()
		local on = Filter and Filter.Enabled
		if FilterMode then FilterMode.Object.Visible = on end
		if FilterRange then FilterRange.Object.Visible = on and FilterMode.Value == 'Range' end
		if FilterPlayers then FilterPlayers.Object.Visible = on and FilterMode.Value == 'Players' end
	end

	Filter = InventoryESP:CreateToggle({
		Name = 'Filter',
		Default = false,
		Tooltip = 'only show certain ppl instead of everyone',
		Function = function()
			syncFilterVisibility()
		end
	})

	FilterMode = InventoryESP:CreateDropdown({
		Name = 'Filter Mode',
		List = {'Range', 'Players'},
		Default = 'Range',
		Darker = true,
		Visible = false,
		Tooltip = 'range = ppl close to u, players = only the ppl u type in',
		Function = function()
			syncFilterVisibility()
		end
	})

	FilterRange = InventoryESP:CreateSlider({
		Name = 'Filter Range',
		Min = 1,
		Max = 30,
		Default = 20,
		Darker = true,
		Visible = false,
		Tooltip = 'only show ppl this close to u, they disappear when they leave',
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})

	FilterPlayers = InventoryESP:CreateTextList({
		Name = 'Filter Players',
		Darker = true,
		Visible = false,
		Placeholder = 'username...',
		Tooltip = 'only show the ppl u add here',
		Function = function(list)
			for _, v in list do
				local lv = tostring(v):lower()
				if not warnedNames[lv] then
					local found = false
					for _, p in playersService:GetPlayers() do
						if p.Name:lower() == lv or p.DisplayName:lower() == lv then
							found = true
							break
						end
					end
					if not found then
						warnedNames[lv] = true
						notif('InventoryESP', tostring(v)..' aint in this game gng', 4)
					end
				end
			end
		end
	})

	task.defer(syncFilterVisibility)
end)

run(function()
    local DamageTexts = {Enabled = false}
	local Color
    local connection
	local Fonts
	local customMSG
	local DamageMessages = {
		'fuck',
		'shit',
		'damn',
		'hell',
		'bitch',
		'ass',
		'piss',
		'cock',
		'motherfucker',
		'fucker',
		'shitter',
		'bastard',
		'asshole',
		'get fucked',
		'eat shit',
		'suck it',
		'die',
		'kill',
		'smash',
		'crush',
		'break',
		'wreck',
		'destroy',
		'annihilate',
		'obliterate',
		'decimate',
		'massacre',
		'slaughter',
		'fuck you',
		'shithead',
		'dick',
		'pussy',
		'twat',
		'wanker',
		'bugger',
		'bloody hell',
		'goddamn',
		'holy shit',
		'what the fuck',
		'oh fuck',
		'fucking hell',
		'shitty',
		'crappy',
		'lousy',
		'motherfucking',
		'goddamn shit',
		'fuckface',
		'dipshit',
		'dumbshit',
		'jackass',
		'prick',
		'cocksucker',
		'asshat',
		'buttfuck',
		'fucknut',
		'shitbag',
		'hellfire',
		'fuckshit',
		'damnass',
		'piss off',
		'screw you',
		'bastard shit',
		'fucktard',
		'shitfuck',
		'cunt',
	    'bitchass nigga',
		'nigga',
		'die pls',
		'desire is some ass',
	}
	
	local RGBColors = {
		Color3.fromRGB(255, 255, 255), 
		Color3.fromRGB(0, 0, 0),       
		Color3.fromRGB(128, 0, 128), 
		Color3.fromRGB(0, 0, 255),  
		Color3.fromRGB(75, 0, 130),    
		Color3.fromRGB(0, 100, 255), 
		Color3.fromRGB(200, 200, 255),
		Color3.fromRGB(30, 30, 30)  
	}
	
	local function randomizer(tbl)
	    if not typeof(tbl) == "table" then return end
	    local index = math.random(1,#tbl)
	    local value = tbl[index]
	    return value,index
	end
	local font  = 'Arial'
    DamageTexts = vape.Categories.Render:CreateModule({
        Name = "DamageTexts",
        Function = function(call)
			if call then
				DamageTexts:Clean(workspace.DescendantAdded:Connect(function(part)
				    if part.Name == "DamageIndicatorPart" and part:IsA("BasePart") then
				        for i, v in part:GetDescendants() do
				            if v:IsA("TextLabel") then
				                local txt = randomizer(DamageMessages)
				                local clr = randomizer(RGBColors)
								if customMSG.Enabled then
				                	v.Text = txt
								end
								if Color.Enabled then
				              	  	v.TextColor3 = clr
								end
				            end
				        end
				    end
				end))
			else

			end
        end,
        Tooltip = "Customizes Damage Affects"
    })
	customMSG = DamageTexts:CreateToggle({
		Name = "Custom Messages",
		Default = true
	})
	Color = DamageTexts:CreateToggle({
		Name = "Custom Colors",
		Default = true
	})
	Fonts = DamageTexts:CreateFont({
		Name = 'Font',
		Function = function(val)
			font = val
		end
	})
end)

run(function()
	local PotESP
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local tracked = {}
	local function getSandIcon()
		local ok, icon = pcall(function() return bedwars.getIcon({itemType = 'sand'}, true) end)
		return ok and icon or 'rbxassetid://0'
	end
	local function createESP(obj)
		if tracked[obj] then return end
		if obj.Name:lower() ~= 'desert_pot' then return end
		local bill = Instance.new('BillboardGui')
		bill.Size = UDim2.fromOffset(40, 40)
		bill.Adornee = obj
		bill.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		bill.AlwaysOnTop = true
		bill.Parent = Folder
		local frame = Instance.new('Frame')
		frame.Size = UDim2.new(1, 0, 1, 0)
		frame.BackgroundTransparency = 0.4
		frame.BackgroundColor3 = Color3.new(0, 0, 0)
		frame.BorderSizePixel = 0
		frame.Parent = bill
		local uic = Instance.new('UICorner')
		uic.CornerRadius = UDim.new(0, 4)
		uic.Parent = frame
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.new(1, -6, 1, -6)
		image.Position = UDim2.new(0.5, 0, 0.5, 0)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundTransparency = 1
		image.Image = getSandIcon()
		image.Parent = frame
		tracked[obj] = bill
	end
	local function removeESP(obj)
		if tracked[obj] then
			tracked[obj]:Destroy()
			tracked[obj] = nil
		end
	end
	local function scanAll()
		scanDescendants(workspace, function(obj)
			createESP(obj)
		end)
	end
	local function cleanup()
		for _, bill in pairs(tracked) do
			bill:Destroy()
		end
		table.clear(tracked)
	end
	PotESP = vape.Categories.Render:CreateModule({
		Name = 'PotESP',
		Function = function(callback)
			if callback then
				scanAll()
				PotESP:Clean(workspace.DescendantAdded:Connect(function(obj)
					task.wait(0.1)
					if obj.Name:lower() == 'desert_pot' then
						createESP(obj)
					end
				end))
				PotESP:Clean(workspace.DescendantRemoving:Connect(function(obj)
					if obj.Name:lower() == 'desert_pot' then
						removeESP(obj)
					end
				end))
			else
				cleanup()
			end
		end,
		Tooltip = 'shows a sand block icon above each desert pot'
	})
end)

run(function()
	local OreESP
	local Background
	local Color
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local tracked = {}
	local function getIronIcon()
		local ok, icon = pcall(function() return bedwars.getIcon({itemType = 'iron'}, true) end)
		return ok and icon or 'rbxassetid://0'
	end
	local function createESP(obj)
		if tracked[obj] then return end
		if obj.Name:lower() ~= 'iron_ore_mesh_block' then return end
		local bill = Instance.new('BillboardGui')
		bill.Size = UDim2.fromOffset(40, 40)
		bill.Adornee = obj
		bill.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		bill.AlwaysOnTop = true
		bill.Parent = Folder
		local frame = Instance.new('Frame')
		frame.Size = UDim2.new(1, 0, 1, 0)
		frame.BackgroundTransparency = Background.Enabled and 0.4 or 1
		frame.BackgroundColor3 = Color3.new(0, 0, 0)
		frame.BorderSizePixel = 0
		frame.Parent = bill
		local uic = Instance.new('UICorner')
		uic.CornerRadius = UDim.new(0, 4)
		uic.Parent = frame
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.new(1, -6, 1, -6)
		image.Position = UDim2.new(0.5, 0, 0.5, 0)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundTransparency = 1
		image.Image = getIronIcon()
		image.Parent = frame
		tracked[obj] = {Billboard = bill, Frame = frame}
	end
	local function removeESP(obj)
		if tracked[obj] then
			tracked[obj].Billboard:Destroy()
			tracked[obj] = nil
		end
	end
	local function scanAll()
		scanDescendants(workspace, function(obj)
			createESP(obj)
		end)
	end
	local function cleanup()
		for _, v in pairs(tracked) do
			v.Billboard:Destroy()
		end
		table.clear(tracked)
	end
	OreESP = vape.Categories.Render:CreateModule({
		Name = 'OreESP',
		Function = function(callback)
			if callback then
				scanAll()
				OreESP:Clean(workspace.DescendantAdded:Connect(function(obj)
					task.wait(0.1)
					if obj.Name:lower() == 'iron_ore_mesh_block' then
						createESP(obj)
					end
				end))
				OreESP:Clean(workspace.DescendantRemoving:Connect(function(obj)
					if obj.Name:lower() == 'iron_ore_mesh_block' then
						removeESP(obj)
					end
				end))
			else
				cleanup()
			end
		end,
		Tooltip = 'puts an icon above iron ore blocks so u can find em? - idea from piston the goat'
	})
	Background = OreESP:CreateToggle({
		Name = 'Background',
		Function = function(callback)
			if Color and Color.Object then Color.Object.Visible = callback end
			for _, v in pairs(tracked) do
				v.Frame.BackgroundTransparency = callback and 0.4 or 1
			end
		end,
		Default = true
	})
	Color = OreESP:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.6,
		Function = function(hue, sat, val, opacity)
			for _, v in pairs(tracked) do
				v.Frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
				v.Frame.BackgroundTransparency = 1 - opacity
			end
		end,
		Darker = true
	})
	task.defer(function()
		if Color and Color.Object then
			Color.Object.Visible = Background.Enabled
		end
	end)
end)

--[[
	Utility Mobule
]]

run(function()
    local moduleData = {
        Connection = nil,
        CurrentDuration = 1,
        CachedPrompts = {}
    }
    
    local function updatePrompt(prompt, duration)
        if prompt and prompt:IsA("ProximityPrompt") then
            prompt.HoldDuration = duration
        end
    end
    
    local function updateAllPrompts(duration)
        for prompt in pairs(moduleData.CachedPrompts) do
            if prompt and prompt.Parent then
                prompt.HoldDuration = duration
            else
                moduleData.CachedPrompts[prompt] = nil
            end
        end
    end
    
    local function cacheExistingPrompts()
        moduleData.CachedPrompts = {}
        
        scanDescendants(workspace, function(descendant)
            if descendant:IsA("ProximityPrompt") then
                moduleData.CachedPrompts[descendant] = true
                descendant.HoldDuration = moduleData.CurrentDuration
            end
        end)
    end
    
	ProximityPromptDuration = vape.Categories.Utility:CreateModule({
		Name = 'ProximityPromptDuration',
		Function = function(callback)
			if callback then
				cacheExistingPrompts()
				ProximityPromptDuration:Clean(workspace.DescendantAdded:Connect(function(descendant)
					if descendant:IsA("ProximityPrompt") then
						moduleData.CachedPrompts[descendant] = true
						descendant.HoldDuration = moduleData.CurrentDuration
					end
				end))
			else
				moduleData.CachedPrompts = {}
			end
		end,
		Tooltip = 'customize proximity prompts'
	})
    
    local ProximityDurationSlider = ProximityPromptDuration:CreateSlider({
        Name = 'Duration',
        Min = 0,
        Max = 10,
        Default = 1,
        Decimal = 100,
        Suffix = 's',
        Function = function(value)
            moduleData.CurrentDuration = value
            if ProximityPromptDuration.Enabled then
                updateAllPrompts(value)
            end
        end
    })
end)

run(function()
	local AutoShoot
	local Targets
	local Check
	local Projectiles
	local UseSophia
	local UseWhim
	local ArrowCharge
	local SwitchDelay
	
	local FireDelays = {}
	
	local function getEntity()
		local selfpos = entitylib.character.RootPart.Position
		local plrs = entitylib.AllPosition({
			Origin = selfpos,
			Part = 'RootPart',
			Range = 22,
			Players = Targets.Players.Enabled,
			NPCs = Targets.NPCs.Enabled,
			Wallcheck = Targets.Walls.Enabled,
			Limit = 10
		})
		if #plrs > 0 then
			for _, ent in plrs do
				local localfacing = entitylib.character.RootPart.CFrame.LookVector * Vector3.new(1, 0, 1)
				local delta = (ent.RootPart.Position - selfpos) * Vector3.new(1, 0, 1)
				local angle = localfacing.Magnitude > 0 and delta.Magnitude > 0 and math.acos(math.clamp(localfacing.Unit:Dot(delta.Unit), -1, 1)) or 0
				if angle > (math.rad(120) / 2) then continue end
				return ent
			end
		end
		return nil
	end
	
	AutoShoot = vape.Categories.Utility:CreateModule({
		Name = 'AutoShoot',
		Function = function(callback)
			if callback then
				repeat
					if entitylib.isAlive and store.hand.toolType == 'sword' and (tick() - bedwars.SwordController.lastSwing) < 0.2 then
						local hotbar = store.hand.tool and getHotbar(store.hand.tool) or nil
						for _, data in getProjectiles(Projectiles.ListEnabled, UseSophia.Enabled, UseWhim.Enabled) do
							local item, ammo, projectile, itemMeta = unpack(data)
							if (FireDelays[item.itemType] or 0) < tick() then
								local ent = getEntity()
								if (not Check.Enabled or ent) and hotbarSwitch(getHotbar(item.tool)) then
									task.wait(store.ping.total or 0)
									local meta = bedwars.ProjectileMeta[projectile]
									local projSpeed, gravity = meta.launchVelocity, meta.gravitationalAcceleration or 196.2
									local calc = ent and prediction.SolveTrajectory(entitylib.character.RootPart.Position, projSpeed, gravity, ent.RootPart.Position, ent.RootPart.Velocity, workspace.Gravity, ent.HipHeight, ent.Jumping and 42.6 or nil, rayCheck, ent.Humanoid.FloorMaterial == Enum.Material.Air or math.abs(ent.RootPart.Velocity.Y) > 0.01, ent.RootPart.Position, ent.RootPart, nil, true) or nil
									if calc then
										local shootPosition = (CFrame.new(entitylib.character.RootPart.Position, calc) * CFrame.new(Vector3.new(-bedwars.BowConstantsTable.RelX, -bedwars.BowConstantsTable.RelY, -bedwars.BowConstantsTable.RelZ))).Position
										local aim = prediction.SolveTrajectory(shootPosition, projSpeed, gravity, ent.RootPart.Position, ent.RootPart.Velocity, workspace.Gravity, ent.HipHeight, ent.Jumping and 42.6 or nil, rayCheck, ent.Humanoid.FloorMaterial == Enum.Material.Air or math.abs(ent.RootPart.Velocity.Y) > 0.01, ent.RootPart.Position, ent.RootPart, nil, true) or calc
										local dir, id = CFrame.lookAt(shootPosition, aim).LookVector, httpService:GenerateGUID(true)
										bedwars.Handler:Get('ProjectileFire'):Fire('CallServerAsync',
											item.tool,
											ammo,
											projectile,
											shootPosition,
											entitylib.character.RootPart.Position,
											dir * projSpeed,
											id,
											{
												drawDurationSeconds = 1,
												shotId = httpService:GenerateGUID(false),
											},
											workspace:GetServerTimeNow() - 0.045
										):andThen(function(res)
											if res then
												res.Parent = replicatedStorage
											end
										end)
										prediction.trackShot(ent.RootPart)
										FireDelays[item.itemType] = tick() + (itemMeta.fireDelaySec + FireRate:GetRandomValue())
										task.wait(SwitchDelay.Value)
									end
								end
							end
						end
						hotbarSwitch(hotbar)
					end
					task.wait(0.1)
				until not AutoShoot.Enabled
			end
		end,
		Tooltip = 'Automatically crossbow macro\'s'
	})
	Targets = AutoShoot:CreateTargets({Players = true})
	Check = AutoShoot:CreateToggle({
		Name = 'Target check',
		Default = true,
		Function = function(callback)
			if Targets.Object then
				Targets.Object.Visible = callback
			end
		end
	})
	Projectiles = AutoShoot:CreateTextList({
		Name = 'Projectile Type',
		Default = {'arrow', 'fireball'}
	})
	UseSophia = AutoShoot:CreateToggle({
		Name = 'Sophia',
	})
	UseWhim = AutoShoot:CreateToggle({
		Name = 'Whim',
	})
	FireRate = AutoShoot:CreateTwoSlider({
		Name = 'Fire Rate',
		Min = 0,
		Max = 1,
		DefaultMin = 0.05,
		DefaultMax = 0.12,
		Decimal = 100
	})
	SwitchDelay = AutoShoot:CreateSlider({
		Name = 'Switch Delay',
		Min = 0,
		Max = 1,
		Decimal = 100,
		Suffix = 's',
		Default = 0.02
	})
end)

run(function()
	local BedAlarm
	local Types
	local Distance
	local UpdateTick
	local HighlightEnemies
	local SoundVolume
	local ShowAlarm

	local function getBed()
		if not (entitylib.isAlive and lplr.Character) then return nil end
		local id = lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId')
		for _, v in collectionService:GetTagged('bed') do
			if tonumber(id) == tonumber(v:GetAttribute('TeamID') or v:GetAttribute('Team') or v:GetAttribute('TeamId')) then
				return v
			end
		end
		return nil
	end

	local function createAlarm(bedpos)
		if not ShowAlarm.Enabled then return end
		if not bedpos then return end
		if store.BedAlarm[lplr] then return end

		if bedwars.BedAlarmController then
			local suc, res = pcall(function()
				local oldthread = 0
				if vape.ThreadFix then 
					oldthread = getthreadidentity()
					setthreadidentity(8) 
				end

				local myTeam = lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId') or -1
				bedwars.BedAlarmController:getOrCreateBedAlarmModel(myTeam, bedpos)

				if vape.ThreadFix then 
					setthreadidentity(oldthread) 
				end
			end)

			if suc then
				store.BedAlarm[lplr] = true
			else
				vape:CreateNotification("BedAlarm", `Creating Alarm issue: {res}`, 16, 'alert')
			end
		end
	end

	local function AlarmAffects(bedpos)
		if not ShowAlarm.Enabled or not bedpos then return end
		if not store.BedAlarm[lplr] then return end

		local myTeam = lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId') or -1
		pcall(function()
			bedwars.BedAlarmController:triggerBedAlarmModel({bedPosition = bedpos, teamId = myTeam})
		end)
	end

	local function removeAlarm()
		if not store.BedAlarm[lplr] then return end
		
		local myTeam = lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId') or -1
		local alarm = bedwars.BedAlarmController.bedAlarmModelMap[myTeam]
		if alarm then
			alarm:Destroy()
			bedwars.BedAlarmController.bedAlarmModelMap[myTeam] = nil
		end
		store.BedAlarm[lplr] = nil
	end

	local function createHighlight(ent)
		if store.BedAlarmHighlightedEnimes[ent] then return end
		
		local character = ent.character or ent.Character
		if not character then return end

		local highlight = Instance.new("Highlight")
		highlight.Name = "BedAlarmHighlight"
		highlight.Adornee = character
		highlight.FillColor = Color3.fromRGB(255, 80, 80)
		highlight.OutlineColor = Color3.fromRGB(255, 100, 100)
		highlight.FillTransparency = 0.6
		highlight.OutlineTransparency = 0
		highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
		highlight.Parent = character
		
		store.BedAlarmHighlightedEnimes[ent] = highlight
	end

	local function clearAllHighlights()
		for _, highlight in pairs(store.BedAlarmHighlightedEnimes) do
			if highlight and highlight.Parent then highlight:Destroy() end
		end
		table.clear(store.BedAlarmHighlightedEnimes)
	end

	local function removeHighlight(ent)
		local hl = store.BedAlarmHighlightedEnimes[ent]
		if hl then
			hl:Destroy()
			store.BedAlarmHighlightedEnimes[ent] = nil
		end
	end

	BedAlarm = vape.Categories.Utility:CreateModule({
		Name = "BedAlarm",
		Function = function(callback)
			if callback then
				store.BedAlarmNotifyTick = 0
				store.BedAlarmSoundTick = 0
				store.BedAlarmIsTrigged = false

				local bed = getBed()
				local bedpos = bed and bed:GetPivot().Position or Vector3.zero

				if ShowAlarm.Enabled then
					createAlarm(bedpos)
				end

				local _bedAlarmSearch = {Origin = bedpos, Range = Distance.Value, Part = 'RootPart', Players = true, IgnoreLocal = true}
				repeat
					if bedpos then
						_bedAlarmSearch.Origin = bedpos
						_bedAlarmSearch.Range = Distance.Value
						local entity = entitylib.EntityPosition(_bedAlarmSearch)

						if entity then
							store.BedAlarmIsTrigged = true

							if ShowAlarm.Enabled then
								createAlarm(bedpos)
							end

							if os.time() >= store.BedAlarmNotifyTick then
								store.BedAlarmNotifyTick = os.time() + UpdateTick.Value

								AlarmAffects(bedpos)

								local msg = '[Bed Alarm]: An intruder is near your bed!'
								if Types.Value == 'Vape' then
									vape:CreateNotification("BedAlarm", msg, UpdateTick.Value + 1)
								else
									pcall(function()
										bedwars.NotificationController:sendInfoNotification({ message = msg })
									end)
								end
							end

							if os.time() >= store.BedAlarmSoundTick then
								store.BedAlarmSoundTick = os.time() + 1.2

								local distance = (bedpos - entity.RootPart.Position).Magnitude
								local soundId = distance >= 30 and bedwars.SoundList.BED_ALARM_TRIGGERED_FAR or bedwars.SoundList.BED_ALARM

								pcall(function()
									bedwars.SoundManager:playSound(soundId, {
										volumeMultiplier = SoundVolume.Value
									})
								end)
							end

							if HighlightEnemies.Enabled then
								createHighlight(entity)
							end
						else
							store.BedAlarmIsTrigged = false
						end
					end

					for ent, _ in pairs(store.BedAlarmHighlightedEnimes) do
						if not entity or ent ~= entity then
							removeHighlight(ent)
						end
					end

					task.wait(1/60)
				until not BedAlarm.Enabled

			else
				store.BedAlarmIsTrigged = false
				clearAllHighlights()
				removeAlarm()
			end
		end
	})

	Distance = BedAlarm:CreateSlider({Name = 'Distance', Min = 10, Max = 100, Default = 64, Suffix = " studs"})
	
	Types = BedAlarm:CreateDropdown({
		Name = 'Notification Type',
		List = {'Vape','Bedwars'},
		Default = 'Bedwars'
	})
	
	UpdateTick = BedAlarm:CreateSlider({
		Name = "Update Tick",
		Min = 0.5,
		Max = 8,
		Decimal = 5,
		Default = 3,
		Suffix = 's'
	})
	
	SoundVolume = BedAlarm:CreateSlider({
		Name = "Volume Multiplier",
		Min = 0.1,
		Max = 3,
		Default = 1.5,
		Decimal = 5,
	})
	
	HighlightEnemies = BedAlarm:CreateToggle({
		Name = 'Highlight Enemies',
		Default = true,
		Function = function(v)
			if not v then clearAllHighlights() end
		end
	})
	
	ShowAlarm = BedAlarm:CreateToggle({
		Name = "Show Alarm Model",
		Default = false,
		Function = function(v)
			local bed = getBed()
			local pos = bed and bed:GetPivot().Position or Vector3.zero
			if v then
				createAlarm(pos)
			else
				removeAlarm()
			end
		end
	})
end)

run(function()
	local AutoPlay
	local Random
	
	local function isEveryoneDead()
		return #bedwars.Store:getState().Party.members <= 0
	end
	
	local function joinQueue()
		if not bedwars.Store:getState().Game.customMatch and bedwars.Store:getState().Party.leader.userId == lplr.UserId and bedwars.Store:getState().Party.queueState == 0 then
			if Random.Enabled then
				local listofmodes = {}
				for i, v in bedwars.QueueMeta do
					if not v.disabled and not v.voiceChatOnly and not v.rankCategory then 
						table.insert(listofmodes, i) 
					end
				end
				bedwars.QueueController:joinQueue(listofmodes[math.random(1, #listofmodes)])
			else
				bedwars.QueueController:joinQueue(store.queueType)
			end
		end
	end
	
	AutoPlay = vape.Categories.Utility:CreateModule({
		Name = 'AutoPlay',
		Function = function(callback)
			if callback then
				AutoPlay:Clean(vapeEvents.EntityDeathEvent.Event:Connect(function(deathTable)
					if deathTable.finalKill and deathTable.entityInstance == lplr.Character and isEveryoneDead() and store.matchState ~= 2 then
						joinQueue()
					end
				end))
				AutoPlay:Clean(vapeEvents.MatchEndEvent.Event:Connect(function()
					task.wait(1)
					joinQueue()
				end))
			end
		end,
		Tooltip = 'auto queues after the match ends.'
	})
	Random = AutoPlay:CreateToggle({
		Name = 'Random',
	})
end)

run(function()
	local AutoVoidDrop
	local OwlCheck
	local PearlCheck
	local AutoReset
	local pearlLastInHandTickVoid = 0
	local DropToggles = {
		iron = nil,
		diamond = nil,
		emerald = nil,
		gold = nil
	}
	local cachedLowestPoint
	
	AutoVoidDrop = vape.Categories.Utility:CreateModule({
		Name = 'AutoVoidDrop',
		Function = function(callback)
			if callback then
				repeat task.wait() until store.matchState ~= 0 or (not AutoVoidDrop.Enabled)
				if not AutoVoidDrop.Enabled then return end

				cachedLowestPoint = math.huge
				for _, v in pairs(store.blocks) do
					local point = (v.Position.Y - (v.Size.Y / 2)) - 75
					if point < cachedLowestPoint then
						cachedLowestPoint = point
					end
				end

				repeat
					if entitylib.isAlive then
						local root = entitylib.character.RootPart

						local handItem = store.inventory and store.inventory.inventory and store.inventory.inventory.hand
						if handItem and handItem.itemType == 'telepearl' then
							pearlLastInHandTickVoid = tick()
						end

						if root.Position.Y < cachedLowestPoint and (lplr.Character:GetAttribute('InflatedBalloons') or 0) <= 0 and not getItem('balloon') then
							local pearlBlock = PearlCheck.Enabled and (tick() - pearlLastInHandTickVoid) < 4
							if (not OwlCheck.Enabled or not root:FindFirstChild('OwlLiftForce')) and not pearlBlock then
								for _, item in pairs(store.inventory.inventory.items) do
									if item and item.tool then
										local dropped = bedwars.Client:Get(remotes.DropItem):CallServer({
											item = item.tool,
											amount = item.amount
										})
										if dropped then
											dropped:SetAttribute('ClientDropTime', tick() + 100)
										end
									end
								end
								if AutoReset.Enabled then
									local hum = root.Parent:FindFirstChildOfClass('Humanoid')
									if hum then
										hum.Health = 0
									end
								end
								break
							end
						end
					end

					task.wait(0.1)
				until not AutoVoidDrop.Enabled
			end
		end,
		Tooltip = 'drops resources when you fall into the void'
	})
	
	OwlCheck = AutoVoidDrop:CreateToggle({
		Name = 'Owl check',
		Default = true,
		Tooltip = 'doesnt drop items if being picked up by an owl'
	})
	PearlCheck = AutoVoidDrop:CreateToggle({
		Name = 'Pearl check',
		Default = false,
		Tooltip = 'does not drop if holding a pearl or recently threw one (4 sec cooldown)'
	})
	AutoReset = AutoVoidDrop:CreateToggle({
		Name = 'Auto Reset',
		Default = false,
		Tooltip = 'resets ur character right after dropping all the loot gng'
	})
	DropToggles.iron = AutoVoidDrop:CreateToggle({
		Name = 'Drop Iron',
		Default = true
	})
	DropToggles.diamond = AutoVoidDrop:CreateToggle({
		Name = 'Drop Diamond',
		Default = true
	})
	DropToggles.emerald = AutoVoidDrop:CreateToggle({
		Name = 'Drop Emerald',
		Default = true
	})
	DropToggles.gold = AutoVoidDrop:CreateToggle({
		Name = 'Drop Gold',
		Default = true
	})
end)
	
run(function()
	local PickupRange
	local Range
	local Lower
	local Network
	local PickupDelay
	local lastPickupTime = 0
	
	PickupRange = vape.Categories.Utility:CreateModule({
		Name = 'PickupRange',
		Function = function(callback)
			if callback then
				local items = collection('ItemDrop', PickupRange)
				local rangeSquared = Range.Value * Range.Value
				
				repeat
					if entitylib.isAlive then
						local localPosition = entitylib.character.RootPart.Position
						local humanoidHealth = entitylib.character.Humanoid.Health
						local currentTime = tick()
						local pickupDelaySeconds = PickupDelay.Value / 1000
						rangeSquared = Range.Value * Range.Value

						for _, v in pairs(items) do
							if (currentTime - (v:GetAttribute('ClientDropTime') or 0)) < 2 then continue end
							if (currentTime - lastPickupTime) < pickupDelaySeconds then continue end

							local offset = v.Position - localPosition
							local distanceSquared = offset.X * offset.X + offset.Y * offset.Y + offset.Z * offset.Z

							if distanceSquared <= rangeSquared then
								if Lower.Enabled and (localPosition.Y - v.Position.Y) < (entitylib.character.HipHeight - 1) then continue end

								bedwars.Client:Get(remotes.PickupItem):CallServerAsync({
									itemDrop = v
								}):andThen(function(suc)
									if suc then
										lastPickupTime = tick()
										if bedwars.SoundList then
											bedwars.SoundManager:playSound(bedwars.SoundList.PICKUP_ITEM_DROP)
											local itemMeta = bedwars.ItemMeta[v.Name]
											if itemMeta then
												local sound = itemMeta.pickUpOverlaySound
												if sound then
													bedwars.SoundManager:playSound(sound, {
														position = v.Position,
														volumeMultiplier = 0.9
													})
												end
											end
										end
									end
								end)
							end
						end
					end
					task.wait(0.1)
				until not PickupRange.Enabled
			else
				lastPickupTime = 0
			end
		end,
		Tooltip = 'Picks up items from a farther distance'
	})

	Range = PickupRange:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 10,
		Default = 10,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	PickupDelay = PickupRange:CreateSlider({
		Name = 'Pickup Delay',
		Min = 0,
		Max = 500,
		Default = 0,
		Suffix = 'ms'
	})
	Lower = PickupRange:CreateToggle({
		Name = 'Feet Check'
	})
end)

run(function()
	local Scaffold
	local Expand
	local Tower
	local Downwards
	local Diagonal
	local LimitItem
	local Mouse
	local PlaceDelay
	local adjacent, lastpos = {}, Vector3.zero
	local lastPlaceTime = 0
	
	for x = -3, 3, 3 do
		for y = -3, 3, 3 do
			for z = -3, 3, 3 do
				local vec = Vector3.new(x, y, z)
				if vec ~= Vector3.zero then
					table.insert(adjacent, vec)
				end
			end
		end
	end
	
	local function checkAdjacent(pos)
		for _, v in adjacent do
			if getPlacedBlock(pos + v) then
				return true
			end
		end
		return false
	end
	
	local function getScaffoldBlock()
		if store.hand.toolType == 'block' then
			return store.hand.tool.Name, store.hand.amount
		elseif (not LimitItem.Enabled) then
			local isHoldingSwordOrTool = store.hand.toolType == 'sword' or (store.hand.tool and bedwars.ItemMeta[store.hand.tool.Name].sword)
			if not isHoldingSwordOrTool then
				local wool, amount = getWool()
				if wool then
					return wool, amount
				else
					for _, item in store.inventory.inventory.items do
						if bedwars.ItemMeta[item.itemType].block then
							return item.itemType, item.amount
						end
					end
				end
			end
		end
	
		return nil, 0
	end
	
	Scaffold = vape.Categories.Utility:CreateModule({
		Name = 'Scaffold',
		Function = function(callback)
			if callback then
				lastPlaceTime = 0
				repeat
					if entitylib.isAlive then
						local wool, amount = getScaffoldBlock()

						if Mouse.Enabled then
							if not inputService:IsMouseButtonPressed(0) then
								wool = nil
							end
						end

						if wool then
							local root = entitylib.character.RootPart
							if Tower.Enabled and inputService:IsKeyDown(Enum.KeyCode.Space) and (not inputService:GetFocusedTextBox()) then
								root.Velocity = Vector3.new(root.Velocity.X, 38, root.Velocity.Z)
							end

							for i = Expand.Value, 1, -1 do
								local currentpos = roundPos(root.Position - Vector3.new(0, entitylib.character.HipHeight + (Downwards.Enabled and inputService:IsKeyDown(Enum.KeyCode.LeftShift) and 4.5 or 1.5), 0) + entitylib.character.Humanoid.MoveDirection * (i * 3))
								if Diagonal.Enabled then
									if math.abs(math.round(math.deg(math.atan2(-entitylib.character.Humanoid.MoveDirection.X, -entitylib.character.Humanoid.MoveDirection.Z)) / 45) * 45) % 90 == 45 then
										local dt = (lastpos - currentpos)
										if ((dt.X == 0 and dt.Z ~= 0) or (dt.X ~= 0 and dt.Z == 0)) and ((lastpos - root.Position) * Vector3.new(1, 0, 1)).Magnitude < 2.5 then
											currentpos = lastpos
										end
									end
								end

								local block, blockpos = getPlacedBlock(currentpos)
								if not block then
									if tick() - lastPlaceTime >= (PlaceDelay.Value / 1000) then
										blockpos = checkAdjacent(blockpos * 3) and blockpos * 3 or blockProximity(currentpos)
										if blockpos then
											task.spawn(bedwars.placeBlock, blockpos, wool, false)
											lastPlaceTime = tick()
										end
									end
								end
								lastpos = currentpos
							end
						end
					end

					task.wait(0.03)
				until not Scaffold.Enabled
			else
				lastPlaceTime = 0
			end
		end,
		Tooltip = 'Helps you make bridges/scaffold walk.'
	})
	Expand = Scaffold:CreateSlider({
		Name = 'Expand',
		Min = 1,
		Max = 6
	})
	Tower = Scaffold:CreateToggle({
		Name = 'Tower',
		Default = true
	})
	Downwards = Scaffold:CreateToggle({
		Name = 'Downwards',
		Default = true
	})
	Diagonal = Scaffold:CreateToggle({
		Name = 'Diagonal',
		Default = true
	})
	LimitItem = Scaffold:CreateToggle({Name = 'Limit to items'})
	Mouse = Scaffold:CreateToggle({Name = 'Require mouse down'})
	PlaceDelay = Scaffold:CreateSlider({
		Name = 'Place Delay',
		Min = 0,
		Max = 200,
		Default = 0,
		Suffix = "ms"
	})
end)

run(function()
	local AutoBuy
	local ItemShop
	local TeamUpgrade
	local WrenShop
	local GUICheck
	local BuyArmorToggle
	local BuyAxeToggle
	local BuyPickaxeToggle
	local BuyProjectileToggle
	local BuySerpentToggle
	local BuyJumpToggle
	local BuyShieldToggle
	local BreakSpeedToggle
	local ArmorUpgradeToggle
	local DamageToggle
	local DiamondGenToggle
	local TeamGenToggle
	local BedBarrierToggle

	local purchaseRemote
	local function getPurchaseRemote()
		if not purchaseRemote then
			purchaseRemote = game:GetService("ReplicatedStorage").rbxts_include.node_modules["@rbxts"].net.out._NetManaged.BedwarsPurchaseItem
		end
		return purchaseRemote
	end

	local function getResourceCount(currency)
		local item = getItem(currency)
		return item and item.amount or 0
	end

	local function playerOwns(itemType)
		for _, item in store.inventory.inventory.items do
			if item.itemType == itemType then return true end
		end
		if store.inventory.inventory.armor then
			for _, v in pairs(store.inventory.inventory.armor) do
				if type(v) == 'table' and v.itemType == itemType then return true end
			end
		end
		return false
	end

	local function isNearShop(checkType)
		if not GUICheck.Enabled then return true end
		local _, items, upgrades = getShopNPC()
		if checkType == 'item' then return items end
		if checkType == 'upgrade' then return upgrades end
		return false
	end

	local function buyItem(shopItem, shopId)
		pcall(function()
			getPurchaseRemote():InvokeServer({shopItem = shopItem, shopId = shopId})
		end)
	end

	local function getShopData(itemType)
		if not bedwars.Shop then return nil end
		local ok, res = pcall(function()
			return bedwars.Shop.getShopItem(itemType, lplr)
		end)
		return ok and res or nil
	end

	local armorTiers = {
		'emerald_chestplate','emerald_leggings','emerald_boots',
		'diamond_chestplate','diamond_leggings','diamond_boots',
		'iron_chestplate','iron_leggings','iron_boots',
		'leather_chestplate',
	}
	local axeTiers = {'emerald_axe','diamond_axe','iron_axe','stone_axe','wood_axe'}
	local pickaxeTiers = {'emerald_pickaxe','diamond_pickaxe','iron_pickaxe','stone_pickaxe','wood_pickaxe'}

	local function buyBestTier(tierList, shopId)
		for _, itemType in ipairs(tierList) do
			if playerOwns(itemType) then break end
			local data = getShopData(itemType)
			if data then
				if getResourceCount(data.currency or 'iron') >= (data.price or math.huge) then
					buyItem(data, shopId)
					break
				end
			end
		end
	end

	local function buyProjectile(shopId)
		local em = getResourceCount('emerald')
		local ir = getResourceCount('iron')
		local ownsAny = playerOwns('headhunter') or playerOwns('wood_crossbow') or playerOwns('wood_bow')
		if ownsAny then return end

		if em >= 24 then
			buyItem({
				lockAfterPurchase = true, itemType = "headhunter", price = 24,
				currency = "emerald", amount = 1,
				disabledInQueue = {"tnt_wars","bedwars_og_to4"}, category = "Combat",
				spawnWithItems = {"headhunter"},
				ignoredByKit = {"archer","flower_bee","falconer","nazar"}
			}, shopId)
		elseif em >= 7 then
			buyItem({
				disabledInQueue = {"tnt_wars","bedwars_og_to4"},
				itemType = "wood_crossbow", price = 7,
				superiorItems = {"headhunter"}, currency = "emerald",
				category = "Combat", lockAfterPurchase = true,
				ignoredByKit = {"archer","flower_bee","falconer","nazar"},
				spawnWithItems = {"wood_crossbow"}, amount = 1
			}, shopId)
		elseif ir >= 24 then
			buyItem({
				ignoredByKit = {"flower_bee","falconer","nazar"},
				itemType = "wood_bow", price = 24,
				superiorItems = {"wood_crossbow","tactical_crossbow"},
				currency = "iron", category = "Combat", lockAfterPurchase = true,
				spawnWithItems = {"wood_bow"}, amount = 1
			}, shopId)
		end
	end

	local function getNearestShopId()
		if not entitylib.isAlive then return nil end
		local localPosition = entitylib.character.RootPart.Position
		local id
		for _, v in store.shop do
			if v.Shop and v.RootPart and (v.RootPart.Position - localPosition).Magnitude <= 20 then
				id = v.Id
			end
		end
		return id
	end

	local function buyPotion(itemType)
		local shopId = getNearestShopId()
		if not shopId then return end
		local ok, item = pcall(function()
			return bedwars.Shop.getShopItem(itemType, lplr, {shopId = shopId})
		end)
		if not ok or not item then return end
		if getResourceCount(item.currency or 'iron') < (item.price or math.huge) then return end
		buyItem(item, shopId)
	end

	local upgradeIds = {
		BreakSpeed = 'BREAK_SPEED',
		Armor      = 'ARMOR',
		Damage     = 'DAMAGE',
		DiamondGen = 'DIAMOND_GENERATOR',
		TeamGen    = 'TEAM_GENERATOR',
	}

	local upgradeRemote = replicatedStorage:WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("RequestPurchaseTeamUpgrade")

	local function buyTeamUpgrade(upgradeType)
		if not upgradeType then return end
		pcall(function()
			upgradeRemote:InvokeServer(upgradeType)
		end)
	end

	local lastBedBarrierBuy = 0
	local BED_BARRIER_DURATION = 180

	local bedUpgradeRemote = replicatedStorage:WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("RequestPurchaseBedTeamUpgrade")

	local function buyBedBarrier()
		local now = tick()
		if now - lastBedBarrierBuy < BED_BARRIER_DURATION then return end
		local ok = pcall(function()
			bedUpgradeRemote:InvokeServer("bed_shield")
			bedUpgradeRemote:InvokeServer("bed_alarm")
		end)
		if ok then
			lastBedBarrierBuy = now
		end
	end

	AutoBuy = vape.Categories.Utility:CreateModule({
		Name = 'AutoBuy',
		Function = function(callback)
			if callback then
				task.spawn(function()
					repeat task.wait() until store.shopLoaded or not AutoBuy.Enabled
					if not AutoBuy.Enabled then return end
					repeat
						task.wait(0.5)
						if not entitylib.isAlive then continue end
						if WrenShop.Enabled then
							if BuySerpentToggle.Enabled then buyPotion('serpents_touch_potion') end
							if BuyJumpToggle.Enabled then buyPotion('jump_potion') end
							if BuyShieldToggle.Enabled then buyPotion('mini_shield') end
						end
						if ItemShop.Enabled and isNearShop('item') then
							local sid = "1_item_shop"
							if BuyArmorToggle.Enabled then buyBestTier(armorTiers, sid) end
							if BuyAxeToggle.Enabled then buyBestTier(axeTiers, sid) end
							if BuyPickaxeToggle.Enabled then buyBestTier(pickaxeTiers, sid) end
							if BuyProjectileToggle.Enabled then buyProjectile(sid) end
						end
						if TeamUpgrade.Enabled and isNearShop('upgrade') then
							if BreakSpeedToggle.Enabled   then buyTeamUpgrade(upgradeIds.BreakSpeed) end
							if ArmorUpgradeToggle.Enabled then buyTeamUpgrade(upgradeIds.Armor) end
							if DamageToggle.Enabled        then buyTeamUpgrade(upgradeIds.Damage) end
							if DiamondGenToggle.Enabled    then buyTeamUpgrade(upgradeIds.DiamondGen) end
							if TeamGenToggle.Enabled       then buyTeamUpgrade(upgradeIds.TeamGen) end
							if BedBarrierToggle.Enabled then buyBedBarrier() end
						end
					until not AutoBuy.Enabled
				end)
			end
		end,
		Tooltip = 'auto buys from the shops u turn on when ur near them'
	})

	ItemShop = AutoBuy:CreateToggle({
		Name = 'Item Shop',
		Default = true,
		Tooltip = 'buys gear from the item shop when ur near it',
		Function = function(v)
			if BuyArmorToggle     then BuyArmorToggle.Object.Visible     = v end
			if BuyAxeToggle       then BuyAxeToggle.Object.Visible       = v end
			if BuyPickaxeToggle   then BuyPickaxeToggle.Object.Visible   = v end
			if BuyProjectileToggle then BuyProjectileToggle.Object.Visible = v end
		end
	})

	TeamUpgrade = AutoBuy:CreateToggle({
		Name = 'Team Upgrade',
		Default = false,
		Tooltip = 'buys team upgrades when ur near the upgrade shop',
		Function = function(v)
			if BreakSpeedToggle   then BreakSpeedToggle.Object.Visible   = v end
			if ArmorUpgradeToggle then ArmorUpgradeToggle.Object.Visible = v end
			if DamageToggle       then DamageToggle.Object.Visible       = v end
			if DiamondGenToggle   then DiamondGenToggle.Object.Visible   = v end
			if TeamGenToggle      then TeamGenToggle.Object.Visible      = v end
			if BedBarrierToggle   then BedBarrierToggle.Object.Visible   = v end
		end
	})

	WrenShop = AutoBuy:CreateToggle({
		Name = 'Wren Shop',
		Default = false,
		Tooltip = 'buys potions from the wren shop gng',
		Function = function(v)
			if BuySerpentToggle then BuySerpentToggle.Object.Visible = v end
			if BuyJumpToggle    then BuyJumpToggle.Object.Visible    = v end
			if BuyShieldToggle  then BuyShieldToggle.Object.Visible  = v end
		end
	})

	GUICheck = AutoBuy:CreateToggle({
		Name = 'GUI Check',
		Default = true,
		Tooltip = 'only buys when ur near the shop'
	})

	BuyArmorToggle     = AutoBuy:CreateToggle({Name = 'Buy Armor',      Default = true, Darker = true})
	BuyAxeToggle       = AutoBuy:CreateToggle({Name = 'Buy Axe',        Default = false, Darker = true})
	BuyPickaxeToggle   = AutoBuy:CreateToggle({Name = 'Buy Pickaxe',    Default = false, Darker = true})
	BuyProjectileToggle = AutoBuy:CreateToggle({Name = 'Buy Projectile', Default = false, Darker = true})
	BuySerpentToggle   = AutoBuy:CreateToggle({Name = 'Buy Serpent Potion',     Default = false, Darker = true})
	BuyJumpToggle      = AutoBuy:CreateToggle({Name = 'Buy Jump Potion',        Default = false, Darker = true})
	BuyShieldToggle    = AutoBuy:CreateToggle({Name = 'Buy Shield Potion',      Default = false, Darker = true})
	BreakSpeedToggle   = AutoBuy:CreateToggle({Name = 'Break Speed',  Default = false, Darker = true})
	ArmorUpgradeToggle = AutoBuy:CreateToggle({Name = 'Armor',        Default = false, Darker = true})
	DamageToggle       = AutoBuy:CreateToggle({Name = 'Damage',       Default = false, Darker = true})
	DiamondGenToggle   = AutoBuy:CreateToggle({Name = 'Diamond Gen',  Default = false, Darker = true})
	TeamGenToggle      = AutoBuy:CreateToggle({Name = 'Team Gen',     Default = false, Darker = true})
	BedBarrierToggle   = AutoBuy:CreateToggle({Name = 'Bed Barrier',  Default = false, Darker = true})

	task.defer(function()
		if BuySerpentToggle   and BuySerpentToggle.Object   then BuySerpentToggle.Object.Visible   = WrenShop.Enabled end
		if BuyJumpToggle      and BuyJumpToggle.Object      then BuyJumpToggle.Object.Visible      = WrenShop.Enabled end
		if BuyShieldToggle    and BuyShieldToggle.Object    then BuyShieldToggle.Object.Visible    = WrenShop.Enabled end
		if BreakSpeedToggle   and BreakSpeedToggle.Object   then BreakSpeedToggle.Object.Visible   = TeamUpgrade.Enabled end
		if ArmorUpgradeToggle and ArmorUpgradeToggle.Object then ArmorUpgradeToggle.Object.Visible = TeamUpgrade.Enabled end
		if DamageToggle       and DamageToggle.Object       then DamageToggle.Object.Visible       = TeamUpgrade.Enabled end
		if DiamondGenToggle   and DiamondGenToggle.Object   then DiamondGenToggle.Object.Visible   = TeamUpgrade.Enabled end
		if TeamGenToggle      and TeamGenToggle.Object      then TeamGenToggle.Object.Visible      = TeamUpgrade.Enabled end
		if BedBarrierToggle   and BedBarrierToggle.Object   then BedBarrierToggle.Object.Visible   = TeamUpgrade.Enabled end
	end)
end)

run(function()
	local ViewMatchHistory

	ViewMatchHistory = vape.Categories.Utility:CreateModule({
		Name = "ViewMatchHistory",
		Function = function(callback)
			if callback then
				ViewMatchHistory:Toggle(false)
				local d = nil
				bedwars.MatchHistroyController:requestMatchHistory(lplr.Name):andThen(function(Data)
					if Data then
						bedwars.AppController:openApp({app = bedwars.MatchHistroyApp,appId = "MatchHistoryApp",},Data)
					end
				end)
			else
				return
			end
		end,
	})																								
end)

run(function()
    local KitRender
    local Players = playersService
    local player = Players.LocalPlayer
    local PlayerGui = player:WaitForChild("PlayerGui")
    local activeLoops = {}
    local updateDebounce = {}
    local retryThread = nil

    local function waitForChild(start, ...)
        local parent = start
        for _, v in {...} do
            local deadline = tick() + 10
            local child
            repeat
                child = parent and parent:FindFirstChild(v)
                if not child then task.wait(0.1) end
            until child or not KitRender.Enabled or tick() >= deadline
            parent = child
            if not parent then
                break
            end
        end
        return parent
    end

    local function createkitrender(plr)
        local icon = Instance.new("ImageLabel")
        icon.Name = "aerov4KitRender" 
        icon.AnchorPoint = Vector2.new(1, 0.5)
        icon.BackgroundTransparency = 1
        icon.Position = UDim2.new(1.05, 0, 0.5, 0)
        icon.Size = UDim2.new(1.5, 0, 1.5, 0)
        icon.SizeConstraint = Enum.SizeConstraint.RelativeYY
        icon.ImageTransparency = 0.4
        icon.ScaleType = Enum.ScaleType.Crop
        icon.ZIndex = 5
        local uar = Instance.new("UIAspectRatioConstraint")
        uar.AspectRatio = 1
        uar.AspectType = Enum.AspectType.FitWithinMaxSize
        uar.DominantAxis = Enum.DominantAxis.Width
        uar.Parent = icon
        local kit = plr:GetAttribute("PlayingAsKits")
        local meta = bedwars.BedwarsKitMeta and (bedwars.BedwarsKitMeta[kit] or bedwars.BedwarsKitMeta.none)
        local newImage = (meta and meta.renderImage) or kitImageIds[kit] or kitImageIds["none"]
        icon.Image = newImage
        local levelLabel = Instance.new("TextLabel")
        levelLabel.Name = "aerov4KitLevel"
        levelLabel.AnchorPoint = Vector2.new(1, 1)
        levelLabel.Position = UDim2.new(1, -2, 1, -2)
        levelLabel.Size = UDim2.new(0.5, 0, 0.5, 0)
        levelLabel.BackgroundTransparency = 1
        levelLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        levelLabel.TextSize = 14
        levelLabel.Font = Enum.Font.GothamBold
        levelLabel.TextStrokeTransparency = 0.2
        levelLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
        levelLabel.TextXAlignment = Enum.TextXAlignment.Right
        levelLabel.TextYAlignment = Enum.TextYAlignment.Bottom
        levelLabel.ZIndex = 6
        local level = plr:GetAttribute("PlayerLevel") or 0
        levelLabel.Text = "[" .. tostring(level) .. "]"
        levelLabel.Parent = icon

        return {icon = icon, levelLabel = levelLabel}
    end

    local function removeallkitrenders()
        for key, _ in pairs(activeLoops) do
            activeLoops[key] = nil
        end
        table.clear(updateDebounce)
        
        if retryThread then
            task.cancel(retryThread)
            retryThread = nil
        end
        
        for _, v in ipairs(PlayerGui:GetDescendants()) do
            if v:IsA("ImageLabel") and v.Name == "aerov4KitRender" then  
                v:Destroy()
            end
        end
    end

    local function refreshicon(data, plr)
        if not data or not data.icon or not data.icon.Parent then return end
        local kit = plr:GetAttribute("PlayingAsKits")
        local meta = bedwars.BedwarsKitMeta and (bedwars.BedwarsKitMeta[kit] or bedwars.BedwarsKitMeta.none)
        local newImage = (meta and meta.renderImage) or kitImageIds[kit] or kitImageIds["none"]
        if data.icon.Image ~= newImage then
            data.icon.Image = newImage
        end

        if data.levelLabel and data.levelLabel.Parent then
            local level = plr:GetAttribute("PlayerLevel") or 0
            local newText = "[" .. tostring(level) .. "]"
            if data.levelLabel.Text ~= newText then
                data.levelLabel.Text = newText
            end
        end
    end

    local function findPlayer(label, container)
        local render = container:FindFirstChild("PlayerRender", true)
        if render and render:IsA("ImageLabel") and render.Image then
            local userId = string.match(render.Image, "id=(%d+)")
            if userId then
                local plr = Players:GetPlayerByUserId(tonumber(userId))
                if plr then return plr end
            end
        end
        local text = label.Text
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr.Name == text or plr.DisplayName == text or plr:GetAttribute("DisguiseDisplayName") == text then
                return plr
            end
            local smName = nil
            pcall(function()
                smName = bedwars.KnitClient.Controllers.StreamerModeController:getDisplayName(plr)
            end)
            if smName and smName == text then
                return plr
            end
        end
    end

    local function renderRow(v)
        if not KitRender.Enabled or not v.Parent then return end
        local label = v:FindFirstChild("PlayerName", true)
        if not label then return end
        task.spawn(function()
            local playerFound = findPlayer(label, v)
            if not playerFound then
                task.wait(0.5)
                playerFound = findPlayer(label, v)
            end
            if not playerFound then return end
            if not playerFound:GetAttribute("PlayingAsKits") then
                task.wait(1)
                if not playerFound:GetAttribute("PlayingAsKits") then return end
            end
            local myTeam = lplr:GetAttribute('Team')
            local theirTeam = playerFound:GetAttribute('Team')
            if not myTeam or not theirTeam or myTeam == theirTeam then return end
            
            local card = v:FindFirstChild("1") and v["1"]:FindFirstChild("MatchDraftPlayerCard")
            if not card then return end
            
            local data = card:FindFirstChild("aerov4KitRender") and {icon = card:FindFirstChild("aerov4KitRender"), levelLabel = card:FindFirstChild("aerov4KitRender"):FindFirstChild("aerov4KitLevel")}
            if not data or not data.icon then
                data = createkitrender(playerFound)
                data.icon.Parent = card
            end
            
            local loopKey = playerFound.UserId
            if activeLoops[loopKey] then
                activeLoops[loopKey] = nil
            end
            activeLoops[loopKey] = data
            refreshicon(data, playerFound)

            local kitConn = playerFound:GetAttributeChangedSignal("PlayingAsKits"):Connect(function()
                if playerFound and data.icon and data.icon.Parent then
                    refreshicon(data, playerFound)
                end
            end)
            local levelConn = playerFound:GetAttributeChangedSignal("PlayerLevel"):Connect(function()
                if playerFound and data.icon and data.icon.Parent then
                    refreshicon(data, playerFound)
                end
            end)
            KitRender:Clean(kitConn)
            KitRender:Clean(levelConn)

            task.spawn(function()
                while activeLoops[loopKey] and KitRender.Enabled do
                    if not v or not v.Parent then
                        break
                    end
                    task.wait(0.5)
                end
                kitConn:Disconnect()
                levelConn:Disconnect()
                activeLoops[loopKey] = nil
                updateDebounce[loopKey] = nil
            end)
        end)
    end

    local activeConnections = {}
    local kitLabels = {}
    local squadUpdateDebounce = {}
    local processedPlayers = {}

    local function createKitLabel(parent, kitImage, plr)
        if kitLabels[parent] then kitLabels[parent]:Destroy() end
        local kitLabel = Instance.new("ImageLabel")
        kitLabel.Name = "aerov4KitIcon"
        kitLabel.Size = UDim2.new(1, 0, 1, 0)
        kitLabel.Position = UDim2.new(1.1, 0, 0, 0)
        kitLabel.BackgroundTransparency = 1
        kitLabel.Image = kitImage
        kitLabel.Parent = parent
        local levelLabel = Instance.new("TextLabel")
        levelLabel.Name = "aerov4KitLevel"
        levelLabel.AnchorPoint = Vector2.new(1, 1)
        levelLabel.Position = UDim2.new(1, -2, 1, -2)
        levelLabel.Size = UDim2.new(0.5, 0, 0.5, 0)
        levelLabel.BackgroundTransparency = 1
        levelLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        levelLabel.TextSize = 12
        levelLabel.Font = Enum.Font.GothamBold
        levelLabel.TextStrokeTransparency = 0.2
        levelLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
        levelLabel.TextXAlignment = Enum.TextXAlignment.Right
        levelLabel.TextYAlignment = Enum.TextYAlignment.Bottom
        local level = plr and plr:GetAttribute("PlayerLevel") or 0
        levelLabel.Text = "[" .. tostring(level) .. "]"
        levelLabel.Parent = kitLabel
        kitLabels[parent] = {icon = kitLabel, levelLabel = levelLabel}
        return kitLabels[parent]
    end

    local function setupSquadsKitRender(obj)
        if obj.Name == "PlayerRender" and obj.Parent and obj.Parent.Parent and obj.Parent.Parent.Parent and obj.Parent.Parent.Parent.Parent and obj.Parent.Parent.Parent.Parent.Parent and obj.Parent.Parent.Parent.Parent.Parent.Name == "MatchDraftTeamCardRow" then
            local Rank = obj.Parent:FindFirstChild('3')
            if not Rank then return end
            local userId = string.match(obj.Image, "id=(%d+)")
            if not userId then return end
            local plr = playersService:GetPlayerByUserId(tonumber(userId))
            if not plr then return end
            local myTeam = lplr:GetAttribute('Team')
            local theirTeam = plr:GetAttribute('Team')
            if not myTeam or not theirTeam or myTeam == theirTeam then return end
            local loopKey = plr.UserId
            processedPlayers[loopKey] = true
            if activeConnections[loopKey] then activeConnections[loopKey]:Disconnect() activeConnections[loopKey] = nil end
            local function updateKit()
                if not KitRender.Enabled then return end
                if not Rank or not Rank.Parent then
                    if activeConnections[loopKey] then activeConnections[loopKey]:Disconnect() activeConnections[loopKey] = nil end
                    if kitLabels[Rank] then kitLabels[Rank] = nil end
                    return
                end
                local kitName = plr:GetAttribute("PlayingAsKits") or "none"
                local render = bedwars.BedwarsKitMeta[kitName] or bedwars.BedwarsKitMeta.none
                local data = kitLabels[Rank]
                if data then
                    if data.icon then data.icon.Image = render.renderImage end
                    if data.levelLabel then
                        local level = plr:GetAttribute("PlayerLevel") or 0
                        data.levelLabel.Text = "[" .. tostring(level) .. "]"
                    end
                else
                    data = createKitLabel(Rank, render.renderImage, plr)
                end
            end
            updateKit()
            local connection = plr:GetAttributeChangedSignal("PlayingAsKits"):Connect(function()
                local t = tick()
                if not squadUpdateDebounce[loopKey] or (t - squadUpdateDebounce[loopKey]) >= 0.1 then
                    squadUpdateDebounce[loopKey] = t
                    updateKit()
                end
            end)
            local levelConn = plr:GetAttributeChangedSignal("PlayerLevel"):Connect(function()
                updateKit()
            end)
            activeConnections[loopKey] = connection
            KitRender:Clean(connection)
            KitRender:Clean(levelConn)
        end
    end

    local function setupSquadsRender()
        local teams = lplr.PlayerGui:FindFirstChild("MatchDraftApp")
        if not teams then return false end
        task.wait(0.5)
        for _, obj in teams:GetDescendants() do
            if KitRender.Enabled then task.spawn(function() setupSquadsKitRender(obj) end) end
        end
        KitRender:Clean(teams.DescendantAdded:Connect(function(obj)
            if KitRender.Enabled then task.wait(0.1) setupSquadsKitRender(obj) end
        end))
        return true
    end

    local function removeSquadsRender()
        for key, connection in pairs(activeConnections) do
            if connection then connection:Disconnect() end
            activeConnections[key] = nil
        end
        for parent, data in pairs(kitLabels) do
            if data and data.icon then data.icon:Destroy() end
            kitLabels[parent] = nil
        end
        table.clear(squadUpdateDebounce)
        table.clear(processedPlayers)
    end

    local function setupKitRender()
        local draftApp = waitForChild(PlayerGui, "MatchDraftApp")
        if not draftApp or not KitRender.Enabled then return false end

        local bodyContainer = waitForChild(draftApp, "DraftAppBackground", "1", "BodyContainer")
        if not bodyContainer or not KitRender.Enabled then return false end

        for i = 1, 2 do
            local column = waitForChild(bodyContainer, "Team" .. i .. "Column")
            if column then
                KitRender:Clean(column.ChildAdded:Connect(renderRow))
                for _, v in ipairs(column:GetChildren()) do
                    task.spawn(renderRow, v)
                end
            end
        end

        KitRender:Clean(draftApp.AncestryChanged:Connect(function()
            if not draftApp.Parent then
                removeallkitrenders()
            end
        end))

        return true
    end

    KitRender = vape.Categories.Utility:CreateModule({
        Name = "KitRender",
        Tooltip = "renders everyone kit during banning(for 5v5 or Squads)",
        Function = function(callback)
			if callback then
				local draftApp = lplr.PlayerGui:FindFirstChild("MatchDraftApp")
				if draftApp then
					setupKitRender()
				else
					retryThread = task.spawn(function()
						while KitRender.Enabled and not lplr.PlayerGui:FindFirstChild("MatchDraftApp") do
							task.wait(0.5)
						end
						if not KitRender.Enabled then return end
						local newDraftApp = lplr.PlayerGui:FindFirstChild("MatchDraftApp")
						if newDraftApp then
							setupKitRender()
						end
						retryThread = nil
					end)
				end
			else
				removeallkitrenders()
			end
		end
    })
end)

run(function()
	local StaffDetector
	local Mode
	local Clans
	local Party
	local Profile
	local Users
	local AlertDuration
	local blacklistedclans = {'gg', 'gg2', 'DV', 'DV2', 'nwr'}
	local blacklisteduserids = {1502104539, 3826146717, 4531785383, 1049767300, 4926350670, 653085195, 184655415, 2752307430, 5087196317, 5744061325, 1536265275}
	local apiModNames = {}
	local teamNameMap = { [1] = 'Blue', [2] = 'Orange', [3] = 'Pink', [4] = 'Yellow' }
	local joined = {}
	local detectedPlayers = {}
	local processing = {}
	for _, name in ipairs({'chasemaser','IllIlllIIIllllIIIll','7SlyR','DoordashRP','OrionYeets','lIllllllllllIllIIlll','AUW345678','the2ndtestaccount','IllIIIIlllIlllIIIIIl','IllIIIIlllIlllIlIIII','ProSurferGamer1','22willow_place','celisnix','G_TP56','celisnix_3','nwrkr','lIIlIlIllllllIIlI','IllIIIIIIlllllIIlIlI','GorillaWithASuit','liilliilliiiliill','IllIllIllllIIIIIIlIl','Typhoon_Kang','VictoryForLife2468','IlIIIlllllIIIIlIIIIl','Erin_Ireland22','IIIIllIIIIIIlllIlIII','IIIIIlllIlllllIlII','Ghostwxstaken','wvwvwvwwvwvvwvw','lllIIllIllIIllIllII','TheAwkwardSponge','TotallyKoaIa','YT_GoraPlays','LegendaryToragon','appleapplelllllll','Yo_johnny67','llIIllIIllllIlIl','PoopFarm_1','llIIIllIIIIIllllIII','Lemon01204','HugeMudOtter','AGameMasterHD','krustykrab204','kevinchuey','IllIIIIlIllIlIIIlI','YoZevStar','pzlican','Deevicus','Blackprincess1','yhpro1230','eple_147','whoisdv2_erin','whoisdv4_erin','whoisdv3_erin','VicForLife14','SleeplessSoulmate','Jsquire07','DVwastaken','pxIican','devzebu','FunFamilyKids177','Artan3333','3MEWMTS5LJCB','Zengoulen','GloriousConfigB2','heywasupsir','c6chisa'}) do
		apiModNames[name:lower()] = true
	end
	local listsLoaded = true

	getgenv()._onnation_staffCounts = {spec=0, mod=0, impossible=0}
	local function refreshStaffCounts()
		local c = {spec=0, mod=0, impossible=0}
		for _, data in pairs(detectedPlayers) do
			local ct = data.checktype
			if ct == 'spectator' then c.spec += 1
			elseif ct == 'impossible_join' then c.impossible += 1
			elseif ct == 'unverified' then c.impossible += 1
			else c.mod += 1 end
		end
		getgenv()._aerov4_staffCounts = c
		vapeEvents.StaffCountUpdate:Fire()
	end

	local function staffFunction(plr, checktype)
		if detectedPlayers[plr.UserId] then return end
		if not vape.Loaded then repeat task.wait() until vape.Loaded end
		local duration = AlertDuration.Value
		local playerName = plr.Name
		local playerId = plr.UserId
		detectedPlayers[playerId] = {name=playerName, checktype=checktype, detectedTime=tick()}
		notif('StaffDetector', 'Staff Detected (' .. checktype .. '): ' .. playerName .. ' (' .. playerId .. ')', duration, 'alert')
		whitelist.customtags[playerName] = {{text='GAME STAFF', color=Color3.new(1,0,0)}}
		local isClanCheck = checktype:find('clan')
		if Party.Enabled and not isClanCheck then pcall(bedwars.PartyController.leaveParty) end
		local modeValue = Mode.Value
		if modeValue == 'Uninject' then
			task.spawn(function() vape:Uninject() end)
			game:GetService('StarterGui'):SetCore('SendNotification', {Title='StaffDetector',Text='Staff Detected ('..checktype..')\n'..playerName..' ('..playerId..')',Duration=duration})
		elseif modeValue == 'Requeue' then
			pcall(bedwars.QueueController.leaveQueue)
			bedwars.QueueController:joinQueue(store.queueType)
		elseif modeValue == 'Profile' then
			if checktype == 'known_mod' or checktype == 'blacklisted_user' then
				vape.Save = function() end
				if vape.Profile ~= Profile.Value then vape:Load(true, Profile.Value) end
			end
		elseif modeValue == 'AutoConfig' then
			local safe = {AutoClicker=true,Reach=true,Sprint=true,HitFix=true,StaffDetector=true}
			vape.Save = function() end
			for i, v in vape.Modules do
				if not (safe[i] or v.Category == 'Render') then
					if v.Enabled then v:Toggle() end
					v:SetBind('')
				end
			end
		end
		refreshStaffCounts()
	end

	local function playerAdded(plr)
		joined[plr.UserId] = plr.Name
		if plr == lplr then return end
		if processing[plr.UserId] then return end
		processing[plr.UserId] = true

		if not listsLoaded then
			local t = tick()
			repeat task.wait(0.1) until listsLoaded or (tick()-t > 3)
		end

		if table.find(blacklisteduserids, plr.UserId) or (Users and table.find(Users.ListEnabled, tostring(plr.UserId))) then
			staffFunction(plr, 'blacklisted_user')
			processing[plr.UserId] = nil
			return
		end

		if apiModNames[plr.Name:lower()] then
			staffFunction(plr, 'known_mod')
			processing[plr.UserId] = nil
			return
		end

		local function spectatorFunction(plr)
			if detectedPlayers[plr.UserId] then return end
			if not vape.Loaded then repeat task.wait() until vape.Loaded end
			detectedPlayers[plr.UserId] = {name=plr.Name, checktype='spectator', detectedTime=tick()}
			notif('StaffDetector', 'Spectator: '..plr.Name..' ('..tostring(plr.UserId)..') [has friend(s) in server]', AlertDuration.Value, 'warning')
			refreshStaffCounts()
		end

		local function unverifiedFunction(plr, failed, checked)
			if detectedPlayers[plr.UserId] then return end
			if not vape.Loaded then repeat task.wait() until vape.Loaded end
			detectedPlayers[plr.UserId] = {name=plr.Name, checktype='unverified', detectedTime=tick()}
			notif('StaffDetector', 'cant check '..plr.Name..' ('..tostring(plr.UserId)..') friends list gng, could be staff. couldnt check '..tostring(failed)..'/'..tostring(checked)..' ppl', AlertDuration.Value, 'warning')
			refreshStaffCounts()
		end

		local function checkJoin()
			if not plr:GetAttribute('Team') and plr:GetAttribute('Spectator') then
				local hasFriend = false
				local failed = 0
				local checked = 0
				for _, sp in ipairs(playersService:GetPlayers()) do
					if sp ~= plr then
						checked = checked + 1
						local ok, res = pcall(function() return plr:IsFriendsWith(sp.UserId) end)
						if not ok then failed = failed + 1 end
						if ok and res then hasFriend = true break end
					end
				end
				if hasFriend then
					spectatorFunction(plr)
				elseif failed > 0 or checked == 0 then
					unverifiedFunction(plr, failed, checked)
				else
					staffFunction(plr, 'impossible_join')
				end
				return true
			end
			return false
		end

		local spectatorConnection
		spectatorConnection = plr:GetAttributeChangedSignal('Spectator'):Connect(function()
			if checkJoin() then spectatorConnection:Disconnect() processing[plr.UserId] = nil end
		end)
		StaffDetector:Clean(spectatorConnection)

		if checkJoin() then processing[plr.UserId] = nil return end

		if Clans.Enabled then
			local function checkClanTag()
				local clanTag = plr:GetAttribute('ClanTag')
				if clanTag and table.find(blacklistedclans, clanTag) then
					staffFunction(plr, 'blacklisted_clan_' .. clanTag:lower())
				end
			end
			if plr:GetAttribute('ClanTag') then
				checkClanTag()
			else
				local clanConnection
				clanConnection = plr:GetAttributeChangedSignal('ClanTag'):Connect(function()
					clanConnection:Disconnect()
					checkClanTag()
				end)
				StaffDetector:Clean(clanConnection)
				task.delay(5, function() if clanConnection then clanConnection:Disconnect() end end)
			end
		end

		processing[plr.UserId] = nil
	end

	local function playerRemoving(plr)
		local userId = plr.UserId
		joined[userId] = nil
		processing[userId] = nil
		if detectedPlayers[userId] then
			local data = detectedPlayers[userId]
			notif('StaffDetector', data.name .. ' (' .. data.checktype .. ') has left the server', AlertDuration.Value, 'warning')
			if whitelist.customtags[data.name] then whitelist.customtags[data.name] = nil end
			detectedPlayers[userId] = nil
			refreshStaffCounts()
		end
	end

	StaffDetector = vape.Categories.Utility:CreateModule({
		Name = 'StaffDetector',
		Function = function(callback)
			if callback then
				StaffDetector:Clean(playersService.PlayerAdded:Connect(playerAdded))
				StaffDetector:Clean(playersService.PlayerRemoving:Connect(playerRemoving))
				for _, v in playersService:GetPlayers() do task.spawn(playerAdded, v) end
			else
				table.clear(joined) table.clear(processing) table.clear(detectedPlayers)
				refreshStaffCounts()
			end
		end,
		Tooltip = 'detects people with staff role and etc'
	})

	Mode = StaffDetector:CreateDropdown({Name='Mode',List={'Uninject','Profile','Requeue','AutoConfig','Notify'},Function=function(val) if Profile.Object then Profile.Object.Visible = val=='Profile' end end})
	AlertDuration = StaffDetector:CreateSlider({Name='Alert Duration',Min=5,Max=120,Default=60,Suffix='s',})
	Clans = StaffDetector:CreateToggle({Name='Blacklist clans',Default=true})
	ClosetDetect = StaffDetector:CreateToggle({Name='Closet Cheaters',Default=true,Tooltip='tells u if a known closet cheater is in ur game so u know who to watch out for gng'})
	Party = StaffDetector:CreateToggle({Name='Leave party'})
	Profile = StaffDetector:CreateTextBox({Name='Profile',Default='default',Darker=true,Visible=false})
	Users = StaffDetector:CreateTextList({Name='Users',Placeholder='player (userid)',Function=function() end})
	task.defer(function() if Profile and Profile.Object then Profile.Object.Visible = (Mode.Value=='Profile') end end)
end)

run(function()
	TrapDisabler = vape.Categories.Utility:CreateModule({
		Name = 'TrapDisabler',
		Tooltip = 'disables Snap Traps'
	})
end)

run(function()
    local BedAssist = {Enabled = false}
    local bedassistrange = {Value = 30}
    local bedassistsmoothness = {Value = 6}
    local bedassistangle = {Value = 70}
    local bedassistfirstperson = {Enabled = false}
    local bedassistshopcheck = {Enabled = false}
	local bedassisthandcheck = {Enabled = false}
	local bedassistlowestblock = {Enabled = false}
	
	local function getBedAimSpeed(speedVal, dt)
		local baseSpeed = 0.01
		local multiplier = 1.35
		local speed = baseSpeed * (multiplier ^ speedVal)
		return math.min(speed, 0.95) * (dt * 60)
	end

	local function checkHand()
		return isHoldingPickaxe() or isHoldingItem({'axe'})
	end

    local camera = gameCamera
    local beds = {}
    local Connections = {}

    local function isFirstPerson()
        if not (lplr.Character and lplr.Character:FindFirstChild("Head")) then return false end
        return (lplr.Character.Head.Position - camera.CFrame.Position).Magnitude < 2
    end

    local function getClosestEnemyBed(playerPos)
        local closestBed = nil
        local closestDistance = bedassistrange.Value
        local lowestY = math.huge

        for _, bed in pairs(beds) do
            if not bed.Parent then continue end

            if tostring(bed:GetAttribute("TeamId")) == tostring(lplr:GetAttribute("Team")) then
                continue
            end

            if bed:GetAttribute("BedShieldEndTime") and bed:GetAttribute("BedShieldEndTime") > workspace:GetServerTimeNow() then
                continue
            end

            local distance = (playerPos - bed.Position).Magnitude
            if distance > bedassistrange.Value then continue end

            local delta = (bed.Position - playerPos)
            local char = lplr.Character
            local localfacing = Vector3.new(1, 0, 0)
            if char and char:FindFirstChild("HumanoidRootPart") then
                localfacing = char.HumanoidRootPart.CFrame.LookVector * Vector3.new(1, 0, 1)
                if localfacing.Magnitude < 0.001 then
                    localfacing = camera.CFrame.LookVector * Vector3.new(1, 0, 1)
                end
                localfacing = localfacing.Unit
            else
                localfacing = camera.CFrame.LookVector * Vector3.new(1, 0, 1)
                if localfacing.Magnitude > 0.001 then
                    localfacing = localfacing.Unit
                else
                    localfacing = Vector3.new(1, 0, 0)
                end
            end

            local flatDelta = delta * Vector3.new(1, 0, 1)
            local angle = math.huge
            if flatDelta.Magnitude > 0.1 then
                angle = math.acos(math.clamp(localfacing:Dot(flatDelta.Unit), -1, 1))
            else
                angle = 0
            end

            if angle <= math.rad(bedassistangle.Value) then
                if bedassistlowestblock.Enabled then
                    if bed.Position.Y < lowestY then
                        lowestY = bed.Position.Y
                        closestBed = bed
                    end
                else
                    if distance < closestDistance then
                        closestDistance = distance
                        closestBed = bed
                    end
                end
            end
        end

        return closestBed
    end

    BedAssist = vape.Categories.Utility:CreateModule({
        Name = "BedAssist",
        Function = function(callback)
            if callback then
                table.clear(beds)
                for _, bed in ipairs(collectionService:GetTagged("bed")) do
                    table.insert(beds, bed)
                end

                table.insert(Connections, collectionService:GetInstanceAddedSignal("bed"):Connect(function(bed)
                    table.insert(beds, bed)
                end))

                table.insert(Connections, collectionService:GetInstanceRemovedSignal("bed"):Connect(function(bed)
                    local i = table.find(beds, bed)
                    if i then
                        table.remove(beds, i)
                    end
                end))

                local heartbeatConn
                heartbeatConn = runService.Heartbeat:Connect(function(dt)
                    if not BedAssist.Enabled then
                        heartbeatConn:Disconnect()
                        camera.CameraType = Enum.CameraType.Custom
                        return
                    end
                    if not lplr.Character or not lplr.Character:FindFirstChild("HumanoidRootPart") then
                        return
                    end
					if bedassisthandcheck.Enabled and not checkHand() then 
						return
					end
                    if bedassistfirstperson.Enabled and not isFirstPerson() then
                        return
                    end
                    if bedassistshopcheck.Enabled then
                        local isShop = lplr:FindFirstChild("PlayerGui") and lplr.PlayerGui:FindFirstChild("ItemShop")
                        if isShop then return end
                    end

                    if #beds == 0 then return end

                    local playerPos = lplr.Character.HumanoidRootPart.Position
                    local closestBed = getClosestEnemyBed(playerPos)

                    if closestBed then
                        local bedPos = closestBed.Position
                        local currentCFrame = camera.CFrame
                        local targetCFrame = CFrame.lookAt(currentCFrame.Position, bedPos)
                        local lerpAmount = bedassistsmoothness.Value / 15
                        camera.CFrame = currentCFrame:Lerp(targetCFrame, math.min(getBedAimSpeed(bedassistsmoothness.Value, dt), 0.95))
                    end
                end)
                table.insert(Connections, heartbeatConn)
            else
                for _, v in pairs(Connections) do
                    pcall(function()
                        v:Disconnect()
                    end)
                end
                Connections = {}
                table.clear(beds)
                camera.CameraType = Enum.CameraType.Custom
            end
        end,
        Tooltip = "aa for beds lol"
    })

    bedassistrange = BedAssist:CreateSlider({
        Name = "Assist Range",
        Min = 10,
        Max = 100,
        Function = function(val) end,
        Default = 30,
        Suffix = function(val) 
            return val == 1 and "stud" or "studs" 
        end
    })

    bedassistsmoothness = BedAssist:CreateSlider({
        Name = "Aim Speed",
        Min = 1,
        Max = 20,
        Function = function(val) end,
        Default = 6
    })

    bedassistangle = BedAssist:CreateSlider({
        Name = "Max Angle",
        Min = 10,
        Max = 360,
        Function = function(val) end,
        Default = 70
    })

    bedassistfirstperson = BedAssist:CreateToggle({
        Name = "First Person Only",
        Function = function() end,
        Default = false,
    })

    bedassistshopcheck = BedAssist:CreateToggle({
        Name = "Shop Check",
        Function = function() end,
        Default = false,
    })

	bedassisthandcheck = BedAssist:CreateToggle({
		Name = "Hand Check",
		Function = function() end,
		Default = true,
	})

	bedassistlowestblock = BedAssist:CreateToggle({
		Name = "Target Lowest Block",
		Function = function() end,
		Default = false,
	})
end)

run(function()
	local UIS = game:GetService('UserInputService')
	local CustomCursor = {Enabled = false}
	local mouseDropdown = {Value = 'Arrow'}
	local mouseIcons = {
		['CS:GO'] = 'rbxassetid://14789879068',
		['Old Roblox Mouse'] = 'rbxassetid://13546344315',
		['dx9ware'] = 'rbxassetid://12233942144',
		['Aimbot'] = 'rbxassetid://8680062686',
		['Triangle'] = 'rbxassetid://14790304072',
		['Arrow'] = 'rbxassetid://14790316561'
	}
	local customMouseIcon = {Enabled = false}
	local customIcon = {Value = ''}
	CustomCursor = vape.Categories.Utility:CreateModule({
		Name = 'CustomCursor',
		Tooltip = 'changes your cursor\'s image.',
		Function = function(callback)
			if callback then
				local function applyIcon()
					if customMouseIcon.Enabled then
						UIS.MouseIcon = 'rbxassetid://' .. customIcon.Value
					else
						UIS.MouseIcon = mouseIcons[mouseDropdown.Value]
					end
				end
				applyIcon()
				CustomCursor:Clean(UIS:GetPropertyChangedSignal('MouseIcon'):Connect(function()
					if CustomCursor.Enabled then
						applyIcon()
					end
				end))
				local playerGui = lplr:FindFirstChildOfClass('PlayerGui')
				if playerGui then
					for _, obj in ipairs(playerGui:GetDescendants()) do
						pcall(function()
							if obj:IsA('GuiButton') and obj.MouseIcon ~= '' then
								obj.MouseIcon = ''
							end
						end)
					end
					CustomCursor:Clean(playerGui.DescendantAdded:Connect(function(obj)
						pcall(function()
							if obj:IsA('GuiButton') then
								obj.MouseIcon = ''
							end
						end)
					end))
				end
				CustomCursor:Clean(runService.Heartbeat:Connect(applyIcon))
			else
				UIS.MouseIcon = ''
				task.wait()
				UIS.MouseIcon = ''
			end
		end
	})
	mouseDropdown = CustomCursor:CreateDropdown({
		Name = 'Mouse Icon',
		List = {
			'CS:GO',
			'Old Roblox Mouse',
			'dx9ware',
			'Aimbot',
			'Triangle',
			'Arrow'
		},
		Function = function() end
	})
	customMouseIcon = CustomCursor:CreateToggle({
		Name = 'Custom Icon',
		Function = function(callback) end
	})
	customIcon = CustomCursor:CreateTextBox({
		Name = 'Custom Mouse Icon',
		TempText = 'Image ID (not decal)',
		FocusLost = function(enter) 
			if CustomCursor.Enabled then 
				CustomCursor:Toggle(false)
				CustomCursor:Toggle(false)
			end
		end
	})
end)

run(function()
	local StaffHUD
	local ShowSpec
	local ShowMod
	local ShowImpossible
	local STAFF_GROUP_ID = 5774246
	local STAFF_MIN_RANK = 100

	local rowDefs = {
		{key='spec', label='Spec', color=Color3.fromRGB(100,180,255), order=1},
		{key='mod', label='Mod', color=Color3.fromRGB(255,60,60), order=3},
		{key='impossible', label='Impossible', color=Color3.fromRGB(200,50,255), order=4},
	}

	local tracked  = {}
	local counts   = {spec=0, closet=0, mod=0, impossible=0}
	local watchers = {}

	local apiClosetNames = {}
	local apiModNames = {}
	local listsLoaded = false

	local function loadLists()
		for _, name in ipairs({'chasemaser','IllIlllIIIllllIIIll','7SlyR','DoordashRP','OrionYeets','lIllllllllllIllIIlll','AUW345678','the2ndtestaccount','IllIIIIlllIlllIIIIIl','IllIIIIlllIlllIlIIII','ProSurferGamer1','22willow_place','celisnix','G_TP56','celisnix_3','nwrkr','lIIlIlIllllllIIlI','IllIIIIIIlllllIIlIlI','GorillaWithASuit','liilliilliiiliill','IllIllIllllIIIIIIlIl','Typhoon_Kang','VictoryForLife2468','IlIIIlllllIIIIlIIIIl','Erin_Ireland22','IIIIllIIIIIIlllIlIII','IIIIIlllIlllllIlII','Ghostwxstaken','wvwvwvwwvwvvwvw','lllIIllIllIIllIllII','TheAwkwardSponge','TotallyKoaIa','YT_GoraPlays','LegendaryToragon','appleapplelllllll','Yo_johnny67','llIIllIIllllIlIl','PoopFarm_1','llIIIllIIIIIllllIII','Lemon01204','HugeMudOtter','AGameMasterHD','krustykrab204','kevinchuey','IllIIIIlIllIlIIIlI','YoZevStar','pzlican','Deevicus','Blackprincess1','yhpro1230','eple_147','whoisdv2_erin','whoisdv4_erin','whoisdv3_erin','VicForLife14','SleeplessSoulmate','Jsquire07','DVwastaken','pxIican','devzebu','FunFamilyKids177','Artan3333','3MEWMTS5LJCB','Zengoulen','GloriousConfigB2','heywasupsir','c6chisa'}) do
			apiModNames[name:lower()] = true
		end
		listsLoaded = true
	end

	local gui = Instance.new('ScreenGui')
	gui.Name = 'StaffHUD'
	gui.ResetOnSpawn = false
	gui.DisplayOrder = 15
	gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	gui.Parent = vape.gui
	gui.Enabled = false

	local frame = Instance.new('Frame')
	frame.Name = 'Container'
	frame.Parent = gui
	frame.BackgroundColor3 = Color3.fromRGB(15,15,15)
	frame.BackgroundTransparency = 0.3
	frame.BorderSizePixel = 0
	frame.AnchorPoint = Vector2.new(1,1)
	frame.Position = UDim2.new(1,-8,1,-8)
	frame.Size = UDim2.new(0,110,0,14)
	frame.AutomaticSize = Enum.AutomaticSize.Y

	local uicorner = Instance.new('UICorner')
	uicorner.CornerRadius = UDim.new(0,6)
	uicorner.Parent = frame

	local pad = Instance.new('UIPadding')
	pad.PaddingLeft=UDim.new(0,6) pad.PaddingRight=UDim.new(0,6)
	pad.PaddingTop=UDim.new(0,4)  pad.PaddingBottom=UDim.new(0,4)
	pad.Parent = frame

	local layout = Instance.new('UIListLayout')
	layout.SortOrder = Enum.SortOrder.LayoutOrder
	layout.Padding = UDim.new(0,2)
	layout.Parent = frame

	local rowObjects = {}
	for _, r in rowDefs do
		local lbl = Instance.new('TextLabel')
		lbl.Name = r.key
		lbl.Parent = frame
		lbl.BackgroundTransparency = 1
		lbl.Size = UDim2.new(1,0,0,13)
		lbl.TextColor3 = r.color
		lbl.TextSize = 11
		lbl.Font = Enum.Font.GothamBold
		lbl.TextXAlignment = Enum.TextXAlignment.Left
		lbl.TextStrokeTransparency = 0.4
		lbl.TextStrokeColor3 = Color3.new(0,0,0)
		lbl.LayoutOrder = r.order
		lbl.Visible = false
		rowObjects[r.key] = lbl
	end

	local function updateDisplay()
		if not StaffHUD or not StaffHUD.Enabled then gui.Enabled = false return end
		local toggleMap = {spec=ShowSpec,mod=ShowMod,impossible=ShowImpossible}
		local anyVisible = false
		for _, r in rowDefs do
			local show = toggleMap[r.key] and toggleMap[r.key].Enabled
			rowObjects[r.key].Text = r.label .. ': ' .. (counts[r.key] or 0)
			rowObjects[r.key].Visible = show
			if show then anyVisible = true end
		end
		gui.Enabled = anyVisible
	end

	local function setTracked(userId, newCat)
		local old = tracked[userId]
		if old == newCat then return end
		if old then counts[old] = math.max(0,(counts[old] or 1)-1) end
		if newCat then
			tracked[userId] = newCat
			counts[newCat] = (counts[newCat] or 0) + 1
		else
			tracked[userId] = nil
		end
		updateDisplay()
	end

	local function removePlayer(userId)
		setTracked(userId, nil)
		if watchers[userId] then
			for _, c in ipairs(watchers[userId]) do pcall(function() c:Disconnect() end) end
			watchers[userId] = nil
		end
	end

	local function hasFriendInServer(plr)
		for _, other in ipairs(playersService:GetPlayers()) do
			if other ~= plr then
				local ok, res = pcall(function() return plr:IsFriendsWith(other.UserId) end)
				if ok and res then return true end
			end
		end
		return false
	end

	local function recheckSpec(plr)
		if not StaffHUD or not StaffHUD.Enabled then return end
		local cat = tracked[plr.UserId]
		if cat == 'closet' or cat == 'mod' then return end
		if plr:GetAttribute('Spectator') == true then
			task.spawn(function()
				local hasFriend = hasFriendInServer(plr)
				setTracked(plr.UserId, hasFriend and 'spec' or 'impossible')
			end)
		else
			if cat == 'spec' or cat == 'impossible' then
				setTracked(plr.UserId, nil)
			end
		end
	end

	local function watchPlayer(plr)
		if plr == lplr or watchers[plr.UserId] then return end
		local conns = {}
		table.insert(conns, plr:GetAttributeChangedSignal('Spectator'):Connect(function() recheckSpec(plr) end))
		table.insert(conns, plr:GetAttributeChangedSignal('Team'):Connect(function() recheckSpec(plr) end))
		watchers[plr.UserId] = conns
	end

	local function classifyPlayer(plr)
		if plr == lplr then return end
		local lowerName = plr.Name:lower()
		if apiModNames[lowerName] then
			setTracked(plr.UserId, 'mod')
			watchPlayer(plr)
			return
		end

		watchPlayer(plr)
		recheckSpec(plr)
	end

	local function cleanAll()
		for _, conns in pairs(watchers) do
			for _, c in ipairs(conns) do pcall(function() c:Disconnect() end) end
		end
		table.clear(watchers)
		table.clear(tracked)
		counts = {spec=0, closet=0, mod=0, impossible=0}
	end

	StaffHUD = vape.Categories.Utility:CreateModule({
		Name = 'StaffHUD',
		Function = function(callback)
			if callback then
				cleanAll()
				loadLists()
				task.spawn(function()
					local t = tick()
					repeat task.wait(0.1) until listsLoaded or (tick() - t > 5)
					for _, plr in ipairs(playersService:GetPlayers()) do
						classifyPlayer(plr)
					end
				end)
				StaffHUD:Clean(playersService.PlayerAdded:Connect(function(plr)
					classifyPlayer(plr)
				end))
				StaffHUD:Clean(playersService.PlayerRemoving:Connect(function(plr)
					removePlayer(plr.UserId)
				end))
				StaffHUD:Clean(runService.Heartbeat:Connect(function()
					local compactUI = vape.gui:FindFirstChild('GeneratorCompactUI')
					frame.Position = (compactUI and compactUI.Enabled) and UDim2.new(1, -136, 1, -8) or UDim2.new(1, -8, 1, -8)
				end))
				updateDisplay()
			else
				cleanAll()
				table.clear(apiClosetNames)
				table.clear(apiModNames)
				listsLoaded = false
				gui.Enabled = false
			end
		end,
		Tooltip = 'little counter ui for staff'
	})

	ShowSpec = StaffHUD:CreateToggle({Name='Spectators', Default=true, Function=function() updateDisplay() end})
	ShowMod = StaffHUD:CreateToggle({Name='Mods', Default=true, Function=function() updateDisplay() end})
	ShowImpossible = StaffHUD:CreateToggle({Name='Impossible Joins',Default=true, Function=function() updateDisplay() end})

	vape:Clean(function()
		cleanAll()
		pcall(function() gui:Destroy() end)
	end)
end)

run(function()
	local InviteExploit
	local invitedPlayers = {}
	local partyMembers = {}
	local inviteRemote
	local partyEventConnection
	local storeChangedConnection

	local function getInviteRemote()
		if not inviteRemote then
			inviteRemote = game:GetService("ReplicatedStorage")
				:WaitForChild("events-@easy-games/lobby:shared/event/lobby-events@getEvents.Events")
				:WaitForChild("inviteToParty")
		end
		return inviteRemote
	end

	local function invitePlayer(player)
		if player == lplr then return end
		if invitedPlayers[player.UserId] then return end
		invitedPlayers[player.UserId] = true
		task.spawn(function()
			local success, err = pcall(function()
				getInviteRemote():FireServer({ player = player })
			end)
			if not success then
				invitedPlayers[player.UserId] = nil
			end
		end)
	end

	local function inviteAllPlayers()
		for _, player in ipairs(playersService:GetPlayers()) do
			invitePlayer(player)
		end
	end

	local function getPartyMemberNames()
		local state = bedwars.Store:getState()
		local party = state and state.Party
		if not party or not party.members then return {} end
		local names = {}
		for _, member in ipairs(party.members) do
			table.insert(names, member.name or member.displayName or "Unknown")
		end
		return names
	end

	local function checkPartyChanges()
		local currentMembers = getPartyMemberNames()
		local oldMembers = partyMembers
		for _, name in ipairs(currentMembers) do
			if not table.find(oldMembers, name) then
				notif("InviteExploit", name .. " joined your party", 3)
			end
		end
		for _, name in ipairs(oldMembers) do
			if not table.find(currentMembers, name) then
				notif("InviteExploit", name .. " left your party", 3)
			end
		end
		partyMembers = currentMembers
	end

	InviteExploit = vape.Categories.Utility:CreateModule({
		Name = 'InviteExploit',
		Function = function(callback)
			if callback then
				invitedPlayers = {}
				partyMembers = {}
				inviteAllPlayers()
				storeChangedConnection = bedwars.Store.changed:connect(function(newState, oldState)
					if newState.Party ~= oldState.Party then
						checkPartyChanges()
					end
				end)
				local event = game:GetService("ReplicatedStorage")
					["events-@easy-games/lobby:shared/event/lobby-events@getEvents.Events"]
					.partyInfoEvent
				partyEventConnection = event.OnClientEvent:Connect(function(data)
					checkPartyChanges()
				end)
				task.wait(0.5)
				checkPartyChanges()
				InviteExploit:Clean(function()
					if storeChangedConnection then
						storeChangedConnection:disconnect()
						storeChangedConnection = nil
					end
					if partyEventConnection then
						partyEventConnection:Disconnect()
						partyEventConnection = nil
					end
					invitedPlayers = {}
					partyMembers = {}
				end)
			else
				if storeChangedConnection then
					storeChangedConnection:disconnect()
					storeChangedConnection = nil
				end
				if partyEventConnection then
					partyEventConnection:Disconnect()
					partyEventConnection = nil
				end
				invitedPlayers = {}
				partyMembers = {}
			end
		end,
		Tooltip = 'invites everyone in the server and notifies you when they join/leave your party'
	})
end)

run(function()
	local NetworkTP
	NetworkTP = vape.Categories.Utility:CreateModule({
		Name = 'NetworkTP',
		Function = function(callback)
			if callback then
				local items = collection('ItemDrop', NetworkTP)
				repeat
					if entitylib.isAlive then
						local localPosition = entitylib.character.RootPart.Position
						local humanoidHealth = entitylib.character.Humanoid.Health
						local currentTime = tick()

						for _, v in pairs(items) do
							local dropTime = v:GetAttribute('ClientDropTime')
							if not dropTime then continue end
							if (currentTime - dropTime) < 2 then continue end
							if isnetworkowner(v) and humanoidHealth > 0 then
								v.CFrame = CFrame.new(localPosition - Vector3.new(0, 3, 0))
							end
						end
					end
					task.wait(0.1)
				until not NetworkTP.Enabled
			end
		end,
		Tooltip = 'network ownership teleports item drops to you'
	})
end)

run(function()
    local AntiSuffo
    
    AntiSuffo = vape.Categories.Utility:CreateModule({
    	Name = 'AntiSuffo',
    	Function = function(call)
    		if call then
    			repeat
    				if entitylib.isAlive then
    					if
    						getPlacedBlock(entitylib.character.RootPart.Position)
    						and (
    							getPlacedBlock(entitylib.character.RootPart.Position + Vector3.new(0, 2, 0))
    							and getPlacedBlock(entitylib.character.RootPart.Position - Vector3.new(0, 2, 0))
    						)
    					then
    						entitylib.character.RootPart.CFrame += Vector3.new(0, 0.5, 0)
    						if entitylib.character.RootPart.AssemblyLinearVelocity.Y < -1 then
    							entitylib.character.RootPart.AssemblyLinearVelocity = Vector3.zero
    						end
    					end
    				end
    				task.wait()
    			until not AntiSuffo.Enabled
    		end
    	end,
    	Tooltip = 'no more suffocating in blocks',
    })
end)

run(function()
	local BedPatcher
	local LimitItem
	local RangeSlider
	local StrongestBlock
	local AutoSwitch
	local runService = game:GetService('RunService')
	local watchSet = {}
	local watchList = {}
	local watchKeys = {}
	local lastEnemyNear = {}
	local heartbeatConn
	local lastEnemyScan = 0
	local function posKey(pos)
		return (pos.X + 8192) * 16777216 + (pos.Y + 8192) * 4096 + (pos.Z + 8192)
	end
	local function getMyBed()
		local myTeam = lplr:GetAttribute('Team')
		if not myTeam then return nil end
		for _, bed in pairs(collectionService:GetTagged('bed')) do
			if bed:GetAttribute('Team'..myTeam..'NoBreak') then
				return bed
			end
		end
		return nil
	end
	local function getPlaceableBlock(useStrongest)
		local best, bestHealth
		for _, item in pairs(store.inventory.inventory.items) do
			local meta = bedwars.ItemMeta[item.itemType]
			if meta and meta.block and not meta.block.seeThrough then
				if useStrongest then
					local health = meta.block.health or 0
					if not best or health > bestHealth then
						best = item.itemType
						bestHealth = health
					end
				else
					return item.itemType
				end
			end
		end
		return best or 'wool_white'
	end
	local function getSwitchBlock()
		local bestSlot, bestType, bestHealth
		local woolSlot, woolType
		local anySlot, anyType
		for i, v in store.inventory.hotbar do
			local item = v.item
			if item then
				local meta = bedwars.ItemMeta[item.itemType]
				if meta and meta.block and not meta.block.seeThrough then
					if not anySlot then
						anySlot = i - 1
						anyType = item.itemType
					end
					if not woolSlot and string.find(item.itemType, 'wool') then
						woolSlot = i - 1
						woolType = item.itemType
					end
					local health = meta.block.health or 0
					if not bestSlot or health > bestHealth then
						bestSlot = i - 1
						bestType = item.itemType
						bestHealth = health
					end
				end
			end
		end
		if StrongestBlock and StrongestBlock.Enabled and bestSlot then
			return bestSlot, bestType
		end
		if woolSlot then
			return woolSlot, woolType
		end
		return anySlot, anyType
	end
	local function addWatch(pos)
		if #watchList >= 400 then return end
		local key = posKey(pos)
		if not watchSet[key] then
			watchSet[key] = true
			watchList[#watchList + 1] = pos
			watchKeys[#watchKeys + 1] = key
		end
	end
	local function scanCoveredPositions(center)
		for x = -9, 9, 3 do
			for y = -9, 9, 3 do
				for z = -9, 9, 3 do
					local pos = center + Vector3.new(x, y, z)
					if getPlacedBlock(pos) then
						addWatch(pos)
					end
				end
			end
		end
	end
	local function inRange(bed)
		local char = lplr.Character
		local root = char and char:FindFirstChild('HumanoidRootPart')
		if not root then return false end
		local bedPos
		if bed:IsA('Model') then
			local cf = bed:GetBoundingBox()
			bedPos = cf.Position
		else
			bedPos = bed.CFrame.Position
		end
		return (root.Position - bedPos).Magnitude <= RangeSlider.Value
	end
	BedPatcher = vape.Categories.Utility:CreateModule({
		Name = 'BedPatcher',
		Function = function(callback)
			if callback then
				local bed = getMyBed()
				if not bed then return end
				local myTeam = lplr:GetAttribute('Team')
				local center = bedwars.BlockController:getBlockPosition(bed.Position) * 3
				watchSet = {}
				watchList = {}
				watchKeys = {}
				lastEnemyNear = {}
				lastEnemyScan = 0
				scanCoveredPositions(center)
				heartbeatConn = runService.Heartbeat:Connect(function()
					if not BedPatcher.Enabled then return end
					local now = tick()
					if now - lastEnemyScan < 0.1 then return end
					lastEnemyScan = now
					if not inRange(bed) then return end
					local roots = {}
					for _, plr in pairs(playersService:GetPlayers()) do
						if plr ~= lplr and plr:GetAttribute('Team') ~= myTeam then
							local char = plr.Character
							local root = char and char:FindFirstChild('HumanoidRootPart')
							if root then roots[#roots + 1] = root.Position end
						end
					end
					if #roots == 0 then return end
					for i = 1, #watchList do
						local pos = watchList[i]
						for j = 1, #roots do
							local d = roots[j] - pos
							if d:Dot(d) <= 100 then
								lastEnemyNear[watchKeys[i]] = now
								break
							end
						end
					end
				end)
				task.spawn(function()
					repeat
						if inRange(bed) then
							local i = 1
							while i <= #watchList do
								if not BedPatcher.Enabled then break end
								local pos = watchList[i]
								local key = watchKeys[i]
								if not getPlacedBlock(pos) then
									local seenAt = lastEnemyNear[key]
									if seenAt and tick() - seenAt <= 1 then
										if AutoSwitch and AutoSwitch.Enabled then
											local slot, switchType = getSwitchBlock()
											if not slot or not switchType then break end
											hotbarSwitch(slot)
											bedwars.placeBlock(pos, switchType, false)
										else
											if LimitItem and LimitItem.Enabled then
												if not store.hand or store.hand.toolType ~= 'block' then break end
												if StrongestBlock and StrongestBlock.Enabled then
													local held = store.hand.tool and store.hand.tool.Name
													if held ~= getPlaceableBlock(true) then break end
												end
											end
											local blockType = getPlaceableBlock(StrongestBlock and StrongestBlock.Enabled)
											if blockType then
												bedwars.placeBlock(pos, blockType, false)
											end
										end
									end
								else
									local dist = (pos - center).Magnitude
									if dist <= 12 then
										for x = -3, 3, 3 do
											for y = -3, 3, 3 do
												for z = -3, 3, 3 do
													local neighbor = pos + Vector3.new(x, y, z)
													if getPlacedBlock(neighbor) then
														addWatch(neighbor)
													end
												end
											end
										end
									end
								end
								i = i + 1
							end
						end
						task.wait(0.03)
					until not BedPatcher.Enabled
				end)
			else
				if heartbeatConn then
					heartbeatConn:Disconnect()
					heartbeatConn = nil
				end
				watchSet = {}
				watchList = {}
				watchKeys = {}
				lastEnemyNear = {}
			end
		end,
		Tooltip = 'places block when an enemy breaks a block'
	})
	LimitItem = BedPatcher:CreateToggle({
		Name = 'Limit to items',
		Default = false,
	})
	StrongestBlock = BedPatcher:CreateToggle({
		Name = 'Use strongest block',
		Default = false,
	})
	AutoSwitch = BedPatcher:CreateToggle({
		Name = 'Auto Switch',
		Default = false,
		Function = function(callback)
			if LimitItem and LimitItem.Object then
				LimitItem.Object.Visible = not callback
			end
		end,
	})
	RangeSlider = BedPatcher:CreateSlider({
		Name = 'Range',
		Min = 5,
		Max = 30,
		Default = 30,
	})
end)

run(function()
	local ShopTierBypass
	local tiered, nexttier = {}, {}
	local originalGetShop
	local originalClientGet
	local shopItemsTracked = {}
	local tierChains = {}
	local knownTiers = {
		{'wood_pickaxe', 'stone_pickaxe', 'iron_pickaxe', 'diamond_pickaxe'},
		{'wood_axe', 'stone_axe', 'iron_axe', 'diamond_axe'},
		{'leather_chestplate', 'iron_chestplate', 'diamond_chestplate', 'emerald_chestplate'},
	}
	
	local function applyBypassToItem(item)
		if item and type(item) == "table" then
			if not tiered[item] then 
				tiered[item] = item.tiered 
			end
			if not nexttier[item] then 
				nexttier[item] = item.nextTier 
			end
			item.nextTier = nil
			item.tiered = nil
			shopItemsTracked[item] = true
		end
	end
	
	local function applyBypassToTable(tbl)
		if tbl and type(tbl) == "table" then
			for _, item in pairs(tbl) do
				if type(item) == "table" then
					applyBypassToItem(item)
				end
			end
		end
	end
	
	local function hasItemOwned(itemType)
		if getItem(itemType) then return true end
		local armor = store.inventory.inventory.armor
		if armor then
			for _, a in armor do
				if type(a) == 'table' and a.itemType == itemType then
					return true
				end
			end
		end
		return false
	end

	local function isDowngrade(itemType)
		local chain = tierChains[itemType]
		if not chain then return false end
		local myIndex = chain.index[itemType]
		for i = myIndex + 1, #chain.items do
			if hasItemOwned(chain.items[i]) then return true end
		end
		return false
	end

	local function shouldBlock(itemType)
		if not tierChains[itemType] then return false end
		if hasItemOwned(itemType) then return true end
		if isDowngrade(itemType) then return true end
		return false
	end

	local function getShopController()
		local success, result = pcall(function()
			local RuntimeLib = require(game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("RuntimeLib"))
			if RuntimeLib then
				return RuntimeLib.import(script, game:GetService("ReplicatedStorage"), "TS", "games", "bedwars", "shop", "bedwars-shop")
			end
		end)
		
		if success then
			return result
		end
		
		local shopModule = game:GetService("ReplicatedStorage"):FindFirstChild("TS"):FindFirstChild("games"):FindFirstChild("bedwars"):FindFirstChild("shop"):FindFirstChild("bedwars-shop")
		if shopModule and shopModule:IsA("ModuleScript") then
			return require(shopModule)
		end
		
		return nil
	end
	
	ShopTierBypass = vape.Categories.Utility:CreateModule({
		Name = 'ShopTierBypass',
		Function = function(callback)
			if callback then
				local function collectAndBypass()
					local itemsSeen = {}
					if bedwars.Shop and bedwars.Shop.ShopItems then
						for _, v in pairs(bedwars.Shop.ShopItems) do
							itemsSeen[v] = true
						end
					end
					if bedwars.ShopItems then
						for _, v in pairs(bedwars.ShopItems) do
							itemsSeen[v] = true
						end
					end
					
					local shopController = getShopController()
					if shopController and shopController.BedwarsShop and shopController.BedwarsShop.getShop then
						local shopTable = shopController.BedwarsShop.getShop()
						if type(shopTable) == "table" then
							for _, v in pairs(shopTable) do
								itemsSeen[v] = true
							end
						end
					end
					for item, _ in pairs(itemsSeen) do
						applyBypassToItem(item)
					end

					local itemsByType = {}
					for item, _ in pairs(itemsSeen) do
						if item.itemType then
							itemsByType[item.itemType] = item
						end
					end
					for _, group in knownTiers do
						local chain = {items = {}, index = {}}
						for _, it in group do
							if itemsByType[it] then
								table.insert(chain.items, it)
								chain.index[it] = #chain.items
							end
						end
						if #chain.items > 1 then
							for _, it in chain.items do
								tierChains[it] = chain
							end
						end
					end
				end
				collectAndBypass()
				if bedwars.Shop and bedwars.Shop.getShop and not originalGetShop then
					originalGetShop = bedwars.Shop.getShop
					bedwars.Shop.getShop = function(...)
						local result = originalGetShop(...)
						if type(result) == "table" then
							applyBypassToTable(result)
						end
						return result
					end
				end
				
				if not originalClientGet then
					originalClientGet = bedwars.Client.Get
					local wrapperCache = setmetatable({}, {__mode = 'kv'})
					bedwars.Client.Get = function(self, name, ...)
						local remote = originalClientGet(self, name, ...)
						if name == 'BedwarsPurchaseItem' then
							local cached = wrapperCache[remote]
							if cached then return cached end
							local wrapper = {}
							wrapperCache[remote] = wrapper
							setmetatable(wrapper, {
								__index = function(_, key)
									if key == 'CallServerAsync' then
										return function(_, data, ...)
											if data and data.shopItem and data.shopItem.itemType then
												local itemType = data.shopItem.itemType
												if shouldBlock(itemType) then
													local displayName = bedwars.ItemMeta[itemType] and bedwars.ItemMeta[itemType].displayName or itemType
													if isDowngrade(itemType) then
														notif('ShopTierBypass', 'prevented yo ass from downgrading to '..displayName, 3, 'alert')
													else
														notif('ShopTierBypass', 'u alr got this item lmao: ', 3, 'alert')
													end
													return {andThen = function() end}
												end
											end
											return remote:CallServerAsync(data, ...)
										end
									end
									return remote[key]
								end
							})
							return wrapper
						end
						return remote
					end
				end

				local shopController = getShopController()
				if shopController and shopController.BedwarsShop and shopController.BedwarsShop.getShop then
					if not tiered["shopControllerHooked"] then
						tiered["shopControllerHooked"] = true
						local originalControllerGetShop = shopController.BedwarsShop.getShop
						shopController.BedwarsShop.getShop = function(...)
							local result = originalControllerGetShop(...)
							if type(result) == "table" then
								applyBypassToTable(result)
							end
							return result
						end
					end
				end
			else
				for item, _ in pairs(shopItemsTracked) do
					if item and type(item) == "table" then
						if tiered[item] ~= nil then
							item.tiered = tiered[item]
						end
						if nexttier[item] ~= nil then
							item.nextTier = nexttier[item]
						end
					end
				end
				
				if tiered["shopControllerHooked"] then
					tiered["shopControllerHooked"] = nil
				end
				
				if originalGetShop then
					bedwars.Shop.getShop = originalGetShop
					originalGetShop = nil
				end

				if originalClientGet then
					bedwars.Client.Get = originalClientGet
					originalClientGet = nil
				end
				
				table.clear(tiered)
				table.clear(nexttier)
				table.clear(shopItemsTracked)
				table.clear(tierChains)
			end
		end,
		Tooltip = 'lets u buy shit without buying the other tiers'
	})

	vape:Clean(function()
		if originalGetShop then
			pcall(function() bedwars.Shop.getShop = originalGetShop end)
			originalGetShop = nil
		end
		if originalClientGet then
			pcall(function() bedwars.Client.Get = originalClientGet end)
			originalClientGet = nil
		end
	end)
end)

run(function()
    local AutoPearl
    local Limit
    
    local rayCheck = RaycastParams.new()
    rayCheck.RespectCanCollide = true
    rayCheck.FilterType = Enum.RaycastFilterType.Include
    local groundCheck = RaycastParams.new()
    groundCheck.RespectCanCollide = true
    groundCheck.FilterType = Enum.RaycastFilterType.Include
    local projectileRemote = {InvokeServer = function(self, ...) end}
    task.spawn(function()
    	projectileRemote = bedwars.Client:Get(remotes.FireProjectile).instance
    end)
    
    local function firePearl(pos, spot, item)
    	for _, v in store.selfProjectiles or {} do
    		if v.Name == 'telepearl' then
    			return
    		end
    	end

    	local originalSlot = store.inventory.hotbarSlot
    	local pearlSlot = getHotbar(item.tool)
    	if not pearlSlot then
    		switchItem(item.tool)
    	else
    		if hotbarSwitch(pearlSlot) then
    			task.wait(0.05)
    		end
    	end
    
    	local meta = bedwars.ProjectileMeta.telepearl
    	local calc = prediction.SolveTrajectory(pos, meta.launchVelocity, meta.gravitationalAcceleration, spot, Vector3.zero, workspace.Gravity, 0, 0)
    
    	if calc then
    		local dir = CFrame.lookAt(pos, calc).LookVector * meta.launchVelocity
    		local projectile = bedwars.ProjectileController:createLocalProjectile(meta, 'telepearl', 'telepearl', pos, nil, dir, {drawDurationSeconds = 1})
    		local res = projectileRemote:InvokeServer(
    			item.tool,
    			'telepearl',
    			'telepearl',
    			pos,
    			pos,
    			dir,
    			httpService:GenerateGUID(true),
    			{ 
                    drawDurationSeconds = 1, 
                    shotId = httpService:GenerateGUID(false) 
                },
    			workspace:GetServerTimeNow() - 0.045
    		)
    		if res then
    			pcall(function()
    				res.Parent = replicatedStorage
    			end)
    		end
    	end
    
    	if pearlSlot then
    		task.wait(0.05)
    		local swordSlot
    		for i, hv in store.inventory.hotbar do
    			if hv and hv.item and hv.item.itemType then
    				local hm = bedwars.ItemMeta[hv.item.itemType]
    				if hm and hm.sword then
    					swordSlot = i - 1
    					break
    				end
    			end
    		end
    		hotbarSwitch(swordSlot or originalSlot)
    	end
    end
    
    local function findNearGround(origin)
    	for _, v in {Vector3.new(1, 0, 0), Vector3.new(0, 0, 1), Vector3.new(-1, 0, 0), Vector3.new(0, 0, -1)} do
    		for i = 1, 24 do
    			local ray = workspace:Raycast((origin.Position + (Vector3.yAxis * 3)) + (v * i), Vector3.new(0, -60, 0), groundCheck)
    			if ray then
    				return ray.Position + Vector3.new(0, 2, 0)
    			end
    		end
    	end
    	return nil
    end
    
    AutoPearl = vape.Categories.Utility:CreateModule({
    	Name = 'AutoPearl',
    	Function = function(callback)
    		if callback then
    			local check, lasty
    			repeat
    				if entitylib.isAlive and (not Limit.Enabled or store.hand.tool and store.hand.tool.Name == 'telepearl') then
    					local root = entitylib.character.RootPart
    					local pearl = getItem('telepearl')
    					local mapFolder = workspace:FindFirstChild('Map') or workspace
    					rayCheck.FilterDescendantsInstances = {mapFolder}
    					rayCheck.CollisionGroup = root.CollisionGroup
    					groundCheck.FilterDescendantsInstances = {mapFolder}
    					groundCheck.CollisionGroup = 'Default'
    
    					if entitylib.character.Humanoid.FloorMaterial ~= Enum.Material.Air then
    						lasty = root.CFrame
    					end
    
    					if pearl and root.Velocity.Y < -100 and not workspace:Raycast(root.Position, Vector3.new(0, -200, 0), rayCheck) then
    						if not check then
    							check = true
    							local ground = findNearGround(root.CFrame + Vector3.new(0, 40, 0)) or findNearGround(lasty and lasty + Vector3.new(0, 5, 0) or root.CFrame)
    							if ground then
    								firePearl(root.Position, ground, pearl)
    							end
    						end
    					else
    						check = false
    					end
    				end
    				task.wait(0.1)
    			until not AutoPearl.Enabled
    		end
    	end,
    	Tooltip = 'auto throws yo pearl when falling in the void'
    })
    
    Limit = AutoPearl:CreateToggle({
    	Name = 'Limit to pearl',
    	Tooltip = 'only throws when ur already holding a pearl'
    })
end)

--[[
	World Modules
]]

run(function()
	local Protect
	local Target
	local LimitItem
	local protectOffsets = {
		Vector3.new(3, 0, 0),
		Vector3.new(-3, 0, 0),
		Vector3.new(0, 0, 3),
		Vector3.new(0, 0, -3),
		Vector3.new(0, 3, 0),
		Vector3.new(0, -3, 0),
	}

	local function getMyBeehive()
		for _, v in collectionService:GetTagged('beehive') do
			if v:GetAttribute('PlacedByUserId') == lplr.UserId then
				return v
			end
		end
	end

	local function getTargetItem()
		if Target.Value == 'Beehive' then
			return getMyBeehive()
		end
	end

	local function getProtectBlock()
		if LimitItem.Enabled then
			if store.hand.toolType == 'block' then
				return store.hand.tool.Name
			end
			return nil
		end
		if StrongestBlock.Enabled then
			local best, bestHealth
			for _, item in store.inventory.inventory.items do
				local meta = bedwars.ItemMeta[item.itemType]
				if meta and meta.block and (item.amount or 0) > 0 then
					local health = meta.block.health or 0
					if not bestHealth or health > bestHealth then
						best = item.itemType
						bestHealth = health
					end
				end
			end
			return best
		end
		return getScaffoldBlockForModule(LimitItem)
	end

	Protect = vape.Categories.World:CreateModule({
		Name = 'Protect',
		Function = function(callback)
			if callback then
				repeat
					local item = getTargetItem()
					if item then
						local basePos = bedwars.BlockController:getBlockPosition(item.Position) * 3
						for _, offset in protectOffsets do
							if not Protect.Enabled then break end
							local pos = basePos + offset
							if not getPlacedBlock(pos) then
								local block = getProtectBlock()
								if block then
									task.spawn(bedwars.placeBlock, pos, block, false)
									task.wait(0.05)
								end
							end
						end
					end
					task.wait(0.5)
				until not Protect.Enabled
			end
		end,
		Tooltip = 'boxes ur hive in so nobody can break it gng - more coming soon ig'
	})

	Target = Protect:CreateDropdown({
		Name = 'Target',
		List = {'Beehive'},
		Default = 'Beehive'
	})
	LimitItem = Protect:CreateToggle({Name = 'Limit to items'})
	StrongestBlock = Protect:CreateToggle({Name = 'Use Strongest Block'})
end)

run(function()
	local a = {Enabled = false}
	a = vape.Categories.World:CreateModule({
		Name = "LeaveParty",
		Function = function(call)
			if call then
				a:Toggle(false)
				game:GetService("ReplicatedStorage"):WaitForChild("events-@easy-games/lobby:shared/event/lobby-events@getEvents.Events"):WaitForChild("leaveParty"):FireServer()
			end
		end
	})
end)

run(function()
    local anim
    local asset
    local trackingConnection
    local lastPosition
    local NightmareEmote
    local cachedRootPart
    local cachedHumanoid
    local lastValidationCheck = 0
    
    NightmareEmote = vape.Categories.World:CreateModule({
        Name = "NightmareEmote",
        Function = function(call)
            if call then
                local l__GameQueryUtil__8
                if (not shared.CheatEngineMode) then 
                    l__GameQueryUtil__8 = require(game:GetService("ReplicatedStorage")['rbxts_include']['node_modules']['@easy-games']['game-core'].out).GameQueryUtil 
                else
                    local backup = {}; function backup:setQueryIgnored() end; l__GameQueryUtil__8 = backup;
                end
                local l__TweenService__9 = tweenService
                local player = playersService.LocalPlayer
                local character = player.Character
                
                if not character then 
                    NightmareEmote:Toggle() 
                    return 
                end
                
                local humanoid = character:WaitForChild("Humanoid")
                local rootPart = character.PrimaryPart or character:FindFirstChild("HumanoidRootPart")
                
                if not rootPart then 
                    NightmareEmote:Toggle() 
                    return 
                end
                
                cachedRootPart = rootPart
                cachedHumanoid = humanoid
                lastPosition = rootPart.Position
                lastValidationCheck = 0
                
                local v10 = game:GetService("ReplicatedStorage"):WaitForChild("Assets"):WaitForChild("Effects"):WaitForChild("NightmareEmote"):Clone()
                asset = v10
                v10.Parent = game.Workspace
                
                local descendants = v10:GetDescendants()
                for _, part in ipairs(descendants) do
                    if part:IsA("BasePart") then
                        l__GameQueryUtil__8:setQueryIgnored(part, true)
                        part.CanCollide = false
                        part.Anchored = true
                    end
                end
                
                local l__Outer__15 = v10:FindFirstChild("Outer")
                if l__Outer__15 then
                    l__TweenService__9:Create(l__Outer__15, TweenInfo.new(1.5, Enum.EasingStyle.Linear, Enum.EasingDirection.Out, -1), {
                        Orientation = l__Outer__15.Orientation + Vector3.new(0, 360, 0)
                    }):Play()
                end
                
                local l__Middle__16 = v10:FindFirstChild("Middle")
                if l__Middle__16 then
                    l__TweenService__9:Create(l__Middle__16, TweenInfo.new(12.5, Enum.EasingStyle.Linear, Enum.EasingDirection.Out, -1), {
                        Orientation = l__Middle__16.Orientation + Vector3.new(0, -360, 0)
                    }):Play()
                end
                
                anim = Instance.new("Animation")
                anim.AnimationId = "rbxassetid://9191822700"
                anim = humanoid:LoadAnimation(anim)
                anim:Play()
                
                local movementThresholdSq = 0.1 * 0.1
                
                trackingConnection = runService.RenderStepped:Connect(function()
                    if not asset or not asset.Parent then 
                        if trackingConnection then
                            trackingConnection:Disconnect()
                        end
                        return 
                    end
                    
                    local currentTime = tick()
                    
                    if (currentTime - lastValidationCheck) > 0.5 then
                        if not character or not character.Parent then
                            asset:Destroy()
                            asset = nil
                            if trackingConnection then
                                trackingConnection:Disconnect()
                            end
                            NightmareEmote:Toggle()
                            return
                        end
                        
                        if not cachedRootPart or not cachedRootPart.Parent then
                            cachedRootPart = character.PrimaryPart or character:FindFirstChild("HumanoidRootPart")
                        end
                        
                        if not cachedHumanoid or not cachedHumanoid.Parent then
                            cachedHumanoid = character:FindFirstChildOfClass("Humanoid")
                        end
                        
                        if not cachedRootPart or not cachedHumanoid or cachedHumanoid.Health <= 0 then
                            asset:Destroy()
                            asset = nil
                            if trackingConnection then
                                trackingConnection:Disconnect()
                            end
                            NightmareEmote:Toggle()
                            return
                        end
                        
                        lastValidationCheck = currentTime
                    end
                    
                    if lastPosition and cachedRootPart then
                        local currentPosition = cachedRootPart.Position
                        local dx = currentPosition.X - lastPosition.X
                        local dy = currentPosition.Y - lastPosition.Y
                        local dz = currentPosition.Z - lastPosition.Z
                        local distanceMovedSq = dx * dx + dy * dy + dz * dz
                        
                        if distanceMovedSq > movementThresholdSq then
                            asset:Destroy()
                            asset = nil
                            if trackingConnection then
                                trackingConnection:Disconnect()
                            end
                            NightmareEmote:Toggle()
                            return
                        end
                        
                        lastPosition = currentPosition
                    end
                    
                    if cachedRootPart then
                        v10:SetPrimaryPartCFrame(cachedRootPart.CFrame * CFrame.new(0, -3, 0))
                    end
                end)
                
                NightmareEmote:Clean(trackingConnection)
                
            else 
                if trackingConnection then
                    trackingConnection:Disconnect()
                    trackingConnection = nil
                end
                
                if anim then 
                    anim:Stop()
                    anim = nil
                end
                
                if asset then
                    asset:Destroy() 
                    asset = nil
                end
                
                lastPosition = nil
                cachedRootPart = nil
                cachedHumanoid = nil
                lastValidationCheck = 0
            end
        end
    })
end)

run(function()
    local AutoCounter
    local tntCount
    local LimitItem
    local AutoPlaceToggle
    local HighlightToggle

    local alltntBlocks = {}
    local counteredtnt = {}
    local tntHighlights = {}
    local autoCounterPlacing = false

    local function addHighlight(tntBlock)
        if tntHighlights[tntBlock] or not tntBlock.Parent then return end
        local h = Instance.new('SelectionBox')
        h.Adornee = tntBlock
        h.Color3 = Color3.fromRGB(255, 50, 50)
        h.LineThickness = 0.05
        h.SurfaceTransparency = 0.6
        h.SurfaceColor3 = Color3.fromRGB(255, 50, 50)
        h.Parent = coreGui
        tntHighlights[tntBlock] = h
    end

    local function removeHighlight(tntBlock)
        if tntHighlights[tntBlock] then
            tntHighlights[tntBlock]:Destroy()
            tntHighlights[tntBlock] = nil
        end
    end

    local function clearAllHighlights()
        for _, h in pairs(tntHighlights) do
            h:Destroy()
        end
        table.clear(tntHighlights)
    end

    local function isEnemytnt(tntBlock)
        if not tntBlock or not tntBlock.Parent then return false end
        if tntBlock:GetAttribute("AutoCountertnt") then return false end

        local placerId = tntBlock:GetAttribute("PlacedByUserId")
        if not placerId then
            return true
        end

        if placerId == lplr.UserId then
            return false
        end
        local myTeam = lplr:GetAttribute('Team')
        if myTeam then
            for _, player in playersService:GetPlayers() do
                if player.UserId == placerId and player:GetAttribute('Team') == myTeam then
                    return false
                end
            end
        end

        return true
    end

    local function isHoldingtnt()
        return isHoldingItem({'tnt'})
    end

    AutoCounter = vape.Categories.World:CreateModule({
        Name = 'AutoCounter',
        Function = function(callback)
            if callback then
                table.clear(counteredtnt)

                local tntAddedConnection = workspace.DescendantAdded:Connect(function(obj)
                    if obj.Name == "tnt" and obj:IsA("Part") then
                        if autoCounterPlacing then
                            obj:SetAttribute("AutoCountertnt", true)
                        end
                        alltntBlocks[obj] = true

                        task.defer(function()
                            if HighlightToggle and HighlightToggle.Enabled and isEnemytnt(obj) then
                                addHighlight(obj)
                            end
                        end)

                        local ancestryConnection
                        ancestryConnection = obj.AncestryChanged:Connect(function()
                            if not obj.Parent then
                                alltntBlocks[obj] = nil
                                counteredtnt[obj] = nil
                                removeHighlight(obj)
                                local fixedPos = fixPosition(obj.Position)
                                local posKey = string.format("%.0f,%.0f,%.0f", fixedPos.X, fixedPos.Y, fixedPos.Z)
                                autoCounterPositions[posKey] = nil
                                if ancestryConnection then
                                    ancestryConnection:Disconnect()
                                end
                            end
                        end)
                    end
                end)
                AutoCounter:Clean(tntAddedConnection)

                scanDescendants(workspace, function(obj)
                    if obj.Name == "tnt" and obj:IsA("Part") and not alltntBlocks[obj] then
                        alltntBlocks[obj] = true
                    end
                end, AutoCounter)

                local horizontalSides = {}
                for _, side in ipairs(Enum.NormalId:GetEnumItems()) do
                    local sideVec = Vector3.fromNormalId(side)
                    if sideVec.Y == 0 then
                        table.insert(horizontalSides, sideVec)
                    end
                end

                repeat
                    if not entitylib.isAlive then
                        task.wait(0.1)
                        continue
                    end

                    if HighlightToggle and HighlightToggle.Enabled then
                        for tntBlock in pairs(alltntBlocks) do
                            if tntBlock.Parent and isEnemytnt(tntBlock) then
                                addHighlight(tntBlock)
                            end
                        end
                    else
                        clearAllHighlights()
                    end

                    if AutoPlaceToggle and AutoPlaceToggle.Enabled then
                        if LimitItem.Enabled and not isHoldingtnt() then
                            task.wait(0.1)
                            continue
                        end

                        if not getItem("tnt") then
                            task.wait(0.1)
                            continue
                        end

                        local myPosition = entitylib.character.RootPart.Position
                        local maxDistanceSq = 30 * 30

                        for tntBlock in pairs(alltntBlocks) do
                            if tntBlock.Parent and not counteredtnt[tntBlock] and isEnemytnt(tntBlock) then
                                local offset = tntBlock.Position - myPosition
                                local distanceSq = offset.X * offset.X + offset.Y * offset.Y + offset.Z * offset.Z

                                if distanceSq <= maxDistanceSq then
                                    local placedCount = 0
                                    local maxCount = tntCount.Value

                                    for _, sideVec in ipairs(horizontalSides) do
                                        if LimitItem.Enabled and not isHoldingtnt() then break end
                                        if placedCount >= maxCount then break end

                                        local placePos = fixPosition(tntBlock.Position + sideVec * 3.5)
                                        if not getPlacedBlock(placePos) and getItem("tnt") then
                                            if LimitItem.Enabled and not isHoldingtnt() then break end
                                            autoCounterPlacing = true
                                            bedwars.placeBlock(placePos, "tnt")
                                            autoCounterPlacing = false
                                            placedCount = placedCount + 1
                                            task.wait(0.05)
                                        end
                                    end

                                    counteredtnt[tntBlock] = true
                                    task.defer(function()
                                        if tntBlock.Parent then
                                            tntBlock.AncestryChanged:Wait()
                                        end
                                        counteredtnt[tntBlock] = nil
                                    end)
                                end
                            end
                        end
                    end

                    task.wait(0.1)
                until not AutoCounter.Enabled
            else
                table.clear(counteredtnt)
                clearAllHighlights()
            end
        end,
        Tooltip = 'Highlights and counters enemys tnt'
    })

    tntCount = AutoCounter:CreateSlider({
        Name = 'tnt Count',
        Min = 1,
        Max = 5,
        Default = 3
    })

    LimitItem = AutoCounter:CreateToggle({
        Name = 'Limit to tnt',
        Default = true,
    })

    AutoPlaceToggle = AutoCounter:CreateToggle({
        Name = 'AutoPlace',
        Default = true,
    })

    HighlightToggle = AutoCounter:CreateToggle({
        Name = 'Highlight',
        Default = true,
    })
end)
	
run(function()
	local AntiAfk

	AntiAfk = vape.Categories.World:CreateModule({
		Name = 'AntiAFK',
		Function = function(callback)
			if callback then
				pcall(function()
					for _, v in getconnections(lplr.Idled) do
						v:Disconnect()
					end
				end)

				pcall(function()
					for _, v in getconnections(runService.Heartbeat) do
						if type(v.Function) == 'function' then
							local constants = debug.getconstants(v.Function)
							if constants and table.find(constants, remotes.AfkStatus) then
								v:Disconnect()
							end
						end
					end
				end)

				pcall(function()
					local afkRemote = bedwars.Client:Get(remotes.AfkStatus)
					if afkRemote then
						afkRemote:SendToServer({
							afk = false
						})
					end
				end)
			end
		end,
	})
end)

run(function()
    local AutoBuildUp
    local LimitItem
    
    local function getScaffoldBlock()
        return getScaffoldBlockForModule(LimitItem)
    end

    local function canPlaceAtPosition(blockpos)
        if not checkFaceAdjacent(blockpos) then
            return false
        end
        
        local checkBelow = blockpos - Vector3.new(0, 3, 0)
        local hasSupport = false
        
        for i = 1, 10 do
            if getPlacedBlock(checkBelow) then
                hasSupport = true
                break
            end
            checkBelow = checkBelow - Vector3.new(0, 3, 0)
        end
        
        return hasSupport or hasFaceBelowOrSide(blockpos)
    end
    
    AutoBuildUp = vape.Categories.World:CreateModule({
        Name = 'AutoBuildUp',
        Function = function(callback)
            
            if callback then
                repeat
                    if entitylib.isAlive then
                        local wool = getScaffoldBlock()
                        
                        if wool then
                            local root = entitylib.character.RootPart
                            
                            local hum = entitylib.character.Humanoid
                            local jumping = inputService:IsKeyDown(Enum.KeyCode.Space) or (hum and hum.Jump)
                            if jumping and (not inputService:GetFocusedTextBox()) then
                                local currentpos = roundPos(root.Position - Vector3.new(0, entitylib.character.HipHeight + 1.5, 0))
                                
                                local block, blockpos = getPlacedBlock(currentpos)
                                if not block then
                                    blockpos = blockpos * 3
                                    
                                    if hasFaceBelowOrSide(blockpos) then
                                        if canPlaceAtPosition(blockpos) then
                                            task.spawn(bedwars.placeBlock, blockpos, wool, false)
                                        end
                                    else
                                        local nearestBlock = blockProximity(currentpos)
                                        if nearestBlock and canPlaceAtPosition(nearestBlock) then
                                            task.spawn(bedwars.placeBlock, nearestBlock, wool, false)
                                        end
                                    end
                                end
                            end
                        end
                    end
                    
                    task.wait(0.03)
                until not AutoBuildUp.Enabled
            end
        end,
    })
    LimitItem = AutoBuildUp:CreateToggle({
        Name = 'Limit to items',
        Default = false,
    })
end)

run(function()
    local AutoTool
    local old, event
    
    local function switchHotbarItem(block)
        if block and not block:GetAttribute('NoBreak') and not block:GetAttribute('Team'..(lplr:GetAttribute('Team') or 0)..'NoBreak') then
            local meta = bedwars.ItemMeta and bedwars.ItemMeta[block.Name]
			if not meta or not meta.block then return end
			local tool, slot = store.tools and store.tools[meta.block.breakType], nil
            if tool then
                for i, v in store.inventory.hotbar do
                    if v.item and v.item.itemType == tool.itemType then slot = i - 1 break end
                end
    
                if hotbarSwitch(slot) then
                    if inputService:IsMouseButtonPressed(0) then 
                        event:Fire() 
                    end
                    return true
                end
            end
        end
    end
    
    AutoTool = vape.Categories.World:CreateModule({
        Name = 'AutoTool',
        Function = function(callback)
            if callback then
                event = Instance.new('BindableEvent')
                AutoTool:Clean(event)
                AutoTool:Clean(event.Event:Connect(function()
                    contextActionService:CallFunction('block-break', Enum.UserInputState.Begin, newproxy(true))
                end))
                registerHitBlockPatch('AutoTool', function(self, maid, raycastparams, ...)
                    local ok, block = pcall(function()
                        return self.clientManager:getBlockSelector():getMouseInfo(1, {ray = raycastparams})
                    end)
                    if not ok then return nil end
                    local inst = block and block.target and block.target.blockInstance or nil
                    local switched = false
                    pcall(function() switched = switchHotbarItem(inst) == true end)
                    if switched then return false end
                    return nil
                end)
            else
                unregisterHitBlockPatch('AutoTool')
                old = nil
            end
        end,
        Tooltip = 'auto selects the correct tool'
    })
end)
	
run(function()
	local ChestSteal
	local Range
	local Open
	local Skywars
	local DelayToggle
	local DelaySlider
	local TeamFilter
	local Delays = {}
	
	local function isTeamChest(chest)
		if not TeamFilter.Enabled then return false end
		local myTeam = tostring(lplr:GetAttribute('Team') or '')
		local myBed = nil
		for _, bed in collectionService:GetTagged('bed') do
			if not bed:IsA('BasePart') then continue end
			local bedTeam = tostring(bed:GetAttribute('Team') or bed:GetAttribute('TeamId') or '')
			if bedTeam == myTeam then myBed = bed break end
		end
		if not myBed then return false end
		local dist = (chest.Position - myBed.Position).Magnitude
		return dist <= 60
	end
	
	local function lootChest(chest)
		chest = chest and chest.Value or nil
		if not chest then return end
		
		local chestitems = chest and chest:GetChildren() or {}
		if #chestitems > 1 and (Delays[chest] or 0) < tick() then
			Delays[chest] = tick() + (DelayToggle.Enabled and DelaySlider.Value or 0.2)
			bedwars.Client:GetNamespace('Inventory'):Get('SetObservedChest'):SendToServer(chest)
	
			for _, v in chestitems do
				if v:IsA('Accessory') then
					if DelayToggle.Enabled then
						task.wait(DelaySlider.Value / #chestitems) 
					end
					
					task.spawn(function()
						pcall(function()
							bedwars.Client:GetNamespace('Inventory'):Get('ChestGetItem'):CallServer(chest, v)
						end)
					end)
				end
			end
	
			bedwars.Client:GetNamespace('Inventory'):Get('SetObservedChest'):SendToServer(nil)
		end
	end
	
	ChestSteal = vape.Categories.World:CreateModule({
		Name = 'ChestSteal',
		Function = function(callback)
			if callback then
				local chests = collection('chest', ChestSteal)
				repeat task.wait() until store.queueType ~= 'bedwars_test'
				if (not Skywars.Enabled) or store.queueType:find('skywars') then
					repeat
						if entitylib.isAlive and store.matchState ~= 2 then
							if Open.Enabled then
								if bedwars.AppController:isAppOpen('ChestApp') then
									lootChest(lplr.Character:FindFirstChild('ObservedChestFolder'))
								end
							else
								local localPosition = entitylib.character.RootPart.Position
								for _, v in chests do
									if (localPosition - v.Position).Magnitude <= Range.Value then
										if isTeamChest(v) then continue end
										lootChest(v:FindFirstChild('ChestFolderValue'))
									end
								end
							end
						end
						task.wait(0.1)
					until not ChestSteal.Enabled
				end
			end
		end,
		Tooltip = 'takes items from near chests'
	})
	Range = ChestSteal:CreateSlider({
		Name = 'Range',
		Min = 0,
		Max = 18,
		Default = 18,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
	Open = ChestSteal:CreateToggle({Name = 'GUI Check'})
	Skywars = ChestSteal:CreateToggle({
		Name = 'Only Skywars',
		Function = function()
			if ChestSteal.Enabled then
				ChestSteal:Toggle()
				ChestSteal:Toggle()
			end
		end,
		Default = true
	})
	TeamFilter = ChestSteal:CreateToggle({
		Name = 'Team Check',
		Default = false
	})
	DelayToggle = ChestSteal:CreateToggle({
		Name = 'Delay',
		Function = function(callback)
			DelaySlider.Object.Visible = callback
			if ChestSteal.Enabled then
				ChestSteal:Toggle()
				ChestSteal:Toggle()
			end
		end
	})
    DelaySlider = ChestSteal:CreateSlider({
        Name = 'Delay Time',
        Min = 0.1,
        Max = 5,
        Default = 1,
        Decimal = 10,
        Suffix = 's',
        Visible = false
    })

    task.defer(function()
        if DelaySlider and DelaySlider.Object then
            DelaySlider.Object.Visible = false  
        end
    end)
end)

run(function()
	local FPSBoost
	local Kill
	local Visualizer
	local effects, util = {}, {}
	local originalAddGameNametag
	local nametagHooked = false
	
	FPSBoost = vape.Categories.World:CreateModule({
		Name = 'FPSBoost',
		Function = function(callback)
			if callback then
				if Kill.Enabled then
					for i, v in bedwars.KillEffectController.killEffects do
						if not i:find('Custom') then
							effects[i] = v
							bedwars.KillEffectController.killEffects[i] = {
								new = function() 
									return {
										onKill = function() end, 
										isPlayDefaultKillEffect = function() 
											return true 
										end
									} 
								end
							}
						end
					end
				end

			if Visualizer.Enabled then
				local keepKeys = {'beam', 'Beam', 'projectile', 'Projectile', 'draw', 'Draw', 'line', 'Line', 'ray', 'Ray', 'arc', 'Arc'}
				for i, v in bedwars.VisualizerUtils do
					local keep = false
					for _, k in keepKeys do
						if tostring(i):lower():find(k:lower()) then
							keep = true
							break
						end
					end
					if not keep then
						util[i] = v
						bedwars.VisualizerUtils[i] = function() end
					end
				end
			end

			else
				for i, v in effects do 
					bedwars.KillEffectController.killEffects[i] = v 
				end
				
				for i, v in util do 
					bedwars.VisualizerUtils[i] = v 
				end
				
				if nametagHooked and originalAddGameNametag then
					bedwars.NametagController.addGameNametag = originalAddGameNametag
					nametagHooked = false
				end
				
				table.clear(effects)
				table.clear(util)
			end
		end,
		Tooltip = 'improves fps - well tries'
	})
	
	Kill = FPSBoost:CreateToggle({
		Name = 'Kill Effects',
		Function = function()
			if FPSBoost.Enabled then
				FPSBoost:Toggle()
				FPSBoost:Toggle()
			end
		end,
		Default = true
	})
	
	Visualizer = FPSBoost:CreateToggle({
		Name = 'Visualizer',
		Function = function()
			if FPSBoost.Enabled then
				FPSBoost:Toggle()
				FPSBoost:Toggle()
			end
		end,
		Default = true
	})
end)

run(function()
	local ShadowRemover
	local connections = {}
	local originalShadows = {}
	local processedShadows = {}
	
	local function removeShadow(obj)
		if obj:IsA("BasePart") and not processedShadows[obj] then
			if not originalShadows[obj] then
				originalShadows[obj] = obj.CastShadow
			end
			obj.CastShadow = false
			processedShadows[obj] = true
		end
	end
	
	ShadowRemover = vape.Categories.World:CreateModule({
		Name = 'ShadowRemover',
		Function = function(callback)
			if callback then
				scanDescendants(workspace, removeShadow, ShadowRemover)
				
				local conn = workspace.DescendantAdded:Connect(function(obj)
					if ShadowRemover.Enabled then
						removeShadow(obj)
					end
				end)
				table.insert(connections, conn)
			else
				for obj, shadow in pairs(originalShadows) do
					if obj and obj.Parent then
						pcall(function()
							obj.CastShadow = shadow
						end)
					end
				end
				
				for _, conn in connections do
					conn:Disconnect()
				end
				table.clear(connections)
				table.clear(originalShadows)
				table.clear(processedShadows)
			end
		end,
	})
end)

run(function()
	local RemoveNeon = {Enabled = false}
	local neonConnection
	local safetyLoop
	local originalMaterials = {}
	local processedParts = {}
	local lastCleanup = 0
	
	local function cleanupDeadReferences()
		local count = 0
		for obj, _ in pairs(originalMaterials) do
			if not obj or not obj.Parent then
				originalMaterials[obj] = nil
				processedParts[obj] = nil
			end
			count = count + 1
			if count % 100 == 0 then
				task.wait()
			end
		end
	end
	
	local function removeNeonFromPart(obj)
		if obj:IsA("BasePart") then
			if obj.Material == Enum.Material.Neon then
				if not originalMaterials[obj] then
					originalMaterials[obj] = {
						Material = obj.Material,
						Reflectance = obj.Reflectance
					}
				end
				pcall(function()
					obj.Material = Enum.Material.Plastic
					obj.Reflectance = 0
				end)
			end
		end
	end
	
	local function restoreNeon()
		for obj, data in pairs(originalMaterials) do
			if obj and obj.Parent then
				pcall(function()
					obj.Material = data.Material
					obj.Reflectance = data.Reflectance
				end)
			end
		end
		table.clear(originalMaterials)
		table.clear(processedParts)
	end
	
	local function batchProcessParts(parts, batchSize)
		local count = 0
		for i, part in ipairs(parts) do
			if part and part.Parent then
				removeNeonFromPart(part)
				count = count + 1
			end
			if i % batchSize == 0 then
				task.wait()
			end
		end
		return count
	end
	
	RemoveNeon = vape.Categories.World:CreateModule({
		Name = 'RemoveNeon',
		Function = function(callback)
			if callback then
				task.spawn(function()
					local allParts = {}
					scanDescendants(workspace, function(v)
						if v:IsA("BasePart") then
							removeNeonFromPart(v)
						end
					end, RemoveNeon)
				end)
				
				neonConnection = workspace.DescendantAdded:Connect(function(obj)
					if RemoveNeon.Enabled then
						removeNeonFromPart(obj)
					end
				end)
				
				safetyLoop = task.spawn(function()
					while RemoveNeon.Enabled do
						task.wait(30)
						if RemoveNeon.Enabled then
							cleanupDeadReferences()
						end
					end
				end)
			else
				if neonConnection then
					neonConnection:Disconnect()
					neonConnection = nil
				end
				if safetyLoop then
					task.cancel(safetyLoop)
					safetyLoop = nil
				end
				restoreNeon()
			end
		end,
	})
end)

run(function()
	local PotatoMode
	local Mode
	local originalProperties = {}
	local blockMonitorConnections = {}
	local processedBlocks = {}

	local blockColors = {
		["wool_white"] = Color3.fromRGB(255, 255, 255),
		["wool_red"] = Color3.fromRGB(255, 50, 50),
		["wool_green"] = Color3.fromRGB(50, 255, 50),
		["wool_blue"] = Color3.fromRGB(50, 100, 255),
		["wool_yellow"] = Color3.fromRGB(255, 255, 50),
		["wool_orange"] = Color3.fromRGB(255, 150, 50),
		["wool_purple"] = Color3.fromRGB(180, 50, 255),
		["wool_pink"] = Color3.fromRGB(255, 100, 200),
		["wool_black"] = Color3.fromRGB(50, 50, 50),
		["wool_cyan"] = Color3.fromRGB(50, 255, 255),
		["wool_magenta"] = Color3.fromRGB(255, 50, 150),
		["wool_lime"] = Color3.fromRGB(150, 255, 50),
		["wool_brown"] = Color3.fromRGB(150, 75, 0),
		["wool_light_blue"] = Color3.fromRGB(100, 200, 255),
		["wool_gray"] = Color3.fromRGB(150, 150, 150),
		["wool_builder"] = Color3.fromRGB(200, 200, 200),
		["wool_shear"] = Color3.fromRGB(200, 200, 200),
		["wool"] = Color3.fromRGB(200, 200, 200),
		["clay_white"] = Color3.fromRGB(255, 255, 255),
		["clay_black"] = Color3.fromRGB(50, 50, 50),
		["clay_blue"] = Color3.fromRGB(50, 100, 255),
		["clay_dark_brown"] = Color3.fromRGB(100, 60, 30),
		["clay_dark_green"] = Color3.fromRGB(30, 100, 30),
		["clay_gray"] = Color3.fromRGB(150, 150, 150),
		["clay_green"] = Color3.fromRGB(50, 255, 50),
		["clay_light_brown"] = Color3.fromRGB(200, 170, 120),
		["clay_light_green"] = Color3.fromRGB(150, 255, 150),
		["clay_orange"] = Color3.fromRGB(255, 150, 50),
		["clay_pink"] = Color3.fromRGB(255, 100, 200),
		["clay_purple"] = Color3.fromRGB(180, 50, 255),
		["clay_red"] = Color3.fromRGB(255, 50, 50),
		["clay_tan"] = Color3.fromRGB(210, 180, 140),
		["clay_yellow"] = Color3.fromRGB(255, 255, 50),
		["clay"] = Color3.fromRGB(220, 180, 140),
		["wood_plank_spruce"] = Color3.fromRGB(222, 184, 135),
		["wood_plank_birch"] = Color3.fromRGB(230, 220, 190),
		["wood_plank_maple"] = Color3.fromRGB(200, 140, 90),
		["wood_plank_oak"] = Color3.fromRGB(180, 140, 100),
		["wood_plank_oak_builder"] = Color3.fromRGB(180, 140, 100),
		["oak_log"] = Color3.fromRGB(120, 90, 60),
		["birch_log"] = Color3.fromRGB(220, 210, 180),
		["spruce_log"] = Color3.fromRGB(100, 75, 50),
		["hickory_log"] = Color3.fromRGB(140, 100, 60),
		["wood"] = Color3.fromRGB(180, 140, 100),
		["stone"] = Color3.fromRGB(150, 150, 150),
		["stone_brick"] = Color3.fromRGB(140, 140, 140),
		["stone_brick_builder"] = Color3.fromRGB(140, 140, 140),
		["stone_slab"] = Color3.fromRGB(160, 160, 160),
		["stone_pillar"] = Color3.fromRGB(160, 160, 160),
		["stone_tiles"] = Color3.fromRGB(160, 160, 160),
		["stone_player_block"] = Color3.fromRGB(150, 150, 150),
		["andesite"] = Color3.fromRGB(150, 150, 150),
		["andesite_polished"] = Color3.fromRGB(160, 160, 160),
		["diorite"] = Color3.fromRGB(220, 220, 220),
		["diorite_polished"] = Color3.fromRGB(230, 230, 230),
		["granite"] = Color3.fromRGB(180, 100, 80),
		["granite_polished"] = Color3.fromRGB(190, 110, 90),
		["cobblestone"] = Color3.fromRGB(150, 150, 150),
		["limestone"] = Color3.fromRGB(210, 200, 180),
		["marble"] = Color3.fromRGB(235, 235, 235),
		["marble_pillar"] = Color3.fromRGB(235, 235, 235),
		["slate_brick"] = Color3.fromRGB(90, 95, 100),
		["slate_tiles"] = Color3.fromRGB(90, 95, 100),
		["volatile_stone"] = Color3.fromRGB(150, 150, 150),
		["obsidian"] = Color3.fromRGB(50, 30, 80),
		["bedrock"] = Color3.fromRGB(80, 80, 80),
		["tnt"] = Color3.fromRGB(255, 50, 50),
		["sandstone"] = Color3.fromRGB(220, 200, 150),
		["sandstone_polished"] = Color3.fromRGB(225, 205, 155),
		["sandstone_smooth"] = Color3.fromRGB(225, 205, 155),
		["red_sandstone"] = Color3.fromRGB(190, 110, 60),
		["red_sandstone_polished"] = Color3.fromRGB(195, 115, 65),
		["red_sandstone_smooth"] = Color3.fromRGB(195, 115, 65),
		["sand"] = Color3.fromRGB(220, 200, 150),
		["red_sand"] = Color3.fromRGB(190, 110, 60),
		["glass"] = Color3.fromRGB(200, 230, 230),
		["magic_glass"] = Color3.fromRGB(150, 200, 255),
		["diamond"] = Color3.fromRGB(100, 220, 220),
		["diamond_block"] = Color3.fromRGB(100, 220, 220),
		["diamond_ore"] = Color3.fromRGB(100, 220, 220),
		["emerald"] = Color3.fromRGB(50, 255, 50),
		["emerald_block"] = Color3.fromRGB(50, 255, 50),
		["emerald_ore"] = Color3.fromRGB(50, 255, 50),
		["iron"] = Color3.fromRGB(220, 220, 220),
		["iron_block"] = Color3.fromRGB(220, 220, 220),
		["iron_ore"] = Color3.fromRGB(200, 200, 200),
		["iron_ore_mesh_block"] = Color3.fromRGB(200, 200, 200),
		["gold"] = Color3.fromRGB(255, 215, 0),
		["gold_block"] = Color3.fromRGB(255, 215, 0),
		["copper_block"] = Color3.fromRGB(184, 115, 51),
		["steel_block"] = Color3.fromRGB(170, 170, 180),
		["galactite"] = Color3.fromRGB(100, 80, 160),
		["galactite_brick"] = Color3.fromRGB(100, 80, 160),
		["crystal_ore"] = Color3.fromRGB(180, 150, 255),
		["guilded_iron"] = Color3.fromRGB(230, 200, 100),
		["ceramic"] = Color3.fromRGB(230, 140, 60),
		["aquamarine_lantern"] = Color3.fromRGB(80, 220, 200),
		["barrel"] = Color3.fromRGB(140, 95, 55),
		["bookshelf"] = Color3.fromRGB(150, 100, 60),
		["brick"] = Color3.fromRGB(160, 80, 60),
		["grass"] = Color3.fromRGB(50, 255, 50),
		["moss_block"] = Color3.fromRGB(50, 150, 50),
		["dirt"] = Color3.fromRGB(120, 80, 50),
		["void_dirt"] = Color3.fromRGB(60, 40, 25),
		["void_grass"] = Color3.fromRGB(25, 80, 25),
		["ice"] = Color3.fromRGB(180, 220, 255),
		["snow"] = Color3.fromRGB(255, 255, 255),
		["snow_pile"] = Color3.fromRGB(255, 255, 255),
		["glowstone"] = Color3.fromRGB(255, 240, 150),
		["magma_block"] = Color3.fromRGB(200, 80, 30),
		["slime_block"] = Color3.fromRGB(120, 230, 120),
		["gum_block"] = Color3.fromRGB(255, 150, 200),
		["invisible_block"] = Color3.fromRGB(255, 255, 255),
		["void_block"] = Color3.fromRGB(20, 20, 20),
		["smoke_block"] = Color3.fromRGB(130, 130, 130),
		["cotton_candy_block"] = Color3.fromRGB(255, 200, 230),
		["cotton_candy_block_blue"] = Color3.fromRGB(150, 200, 255),
		["cotton_candy_block_orange"] = Color3.fromRGB(255, 180, 120),
		["cotton_candy_block_pink"] = Color3.fromRGB(255, 150, 200),
		["cotton_candy_block_yellow"] = Color3.fromRGB(255, 240, 150),
		["blue_tile"] = Color3.fromRGB(50, 100, 255),
		["concrete_green"] = Color3.fromRGB(50, 180, 50),
		["concrete"] = Color3.fromRGB(180, 180, 180),
		["bed"] = Color3.fromRGB(200, 50, 50),
		["og_bed"] = Color3.fromRGB(200, 50, 50),
		["royale_bed"] = Color3.fromRGB(200, 50, 50),
		["fake_bed"] = Color3.fromRGB(200, 50, 50),
		["barrier"] = Color3.fromRGB(255, 0, 0),
		["ladder"] = Color3.fromRGB(150, 100, 50),
		["vine_ladder"] = Color3.fromRGB(60, 120, 60),
		["scaffold"] = Color3.fromRGB(180, 140, 100),
		["christmas_scaffold"] = Color3.fromRGB(180, 140, 100),
		["drawbridge"] = Color3.fromRGB(150, 110, 70),
		["christmas_drawbridge"] = Color3.fromRGB(150, 110, 70),
		["haybale"] = Color3.fromRGB(230, 200, 100),
		["purple_hay_bale"] = Color3.fromRGB(180, 50, 255),
	}

	local cachedColors = {}

	local function getBlockColor(blockName)
		if cachedColors[blockName] then
			return cachedColors[blockName]
		end

		if blockColors[blockName] then
			cachedColors[blockName] = blockColors[blockName]
			return blockColors[blockName]
		end

		local lowerName = blockName:lower()

		if blockColors[lowerName] then
			cachedColors[blockName] = blockColors[lowerName]
			return blockColors[lowerName]
		end

		if lowerName:find("wool", 1, true) then
			for key, color in pairs(blockColors) do
				if key:find("wool", 1, true) and lowerName:find(key, 1, true) then
					cachedColors[blockName] = color
					return color
				end
			end
			cachedColors[blockName] = blockColors["wool"]
			return blockColors["wool"]
		end

		if lowerName:find("clay", 1, true) then
			for key, color in pairs(blockColors) do
				if key:find("clay", 1, true) and lowerName:find(key, 1, true) then
					cachedColors[blockName] = color
					return color
				end
			end
			cachedColors[blockName] = blockColors["clay"]
			return blockColors["clay"]
		end

		for name, color in pairs(blockColors) do
			if lowerName:find(name, 1, true) then
				cachedColors[blockName] = color
				return color
			end
		end

		local defaultColor = Color3.fromRGB(150, 150, 150)
		cachedColors[blockName] = defaultColor
		return defaultColor
	end

	local function cleanupDeadReferences()
		for block, _ in pairs(originalProperties) do
			if not block or not block.Parent then
				originalProperties[block] = nil
				processedBlocks[block] = nil
			end
		end
	end

	local function simplifyBlock(block)
		if not block or not block.Parent or processedBlocks[block] then return end

		if not originalProperties[block] then
			originalProperties[block] = {
				Material = block.Material,
				Color = block.Color,
				TextureID = block:IsA("MeshPart") and block.TextureID or nil,
				Textures = {}
			}

			for _, child in block:GetChildren() do
				if child:IsA("Texture") or child:IsA("Decal") then
					table.insert(originalProperties[block].Textures, {
						Class = child.ClassName,
						Texture = child.Texture,
						StudsPerTileU = child:IsA("Texture") and child.StudsPerTileU or nil,
						StudsPerTileV = child:IsA("Texture") and child.StudsPerTileV or nil,
						Face = child.Face,
						Transparency = child.Transparency,
						Color3 = child:IsA("Decal") and child.Color3 or nil
					})
				end
			end
		end

		block.Material = Enum.Material.SmoothPlastic
		block.Color = getBlockColor(block.Name)

		for _, child in block:GetChildren() do
			if child:IsA("Texture") or child:IsA("Decal") then
				child:Destroy()
			end
		end

		if block:IsA("MeshPart") and block.TextureID ~= "" then
			block.TextureID = ""
		end

		processedBlocks[block] = true
	end

	local function restoreBlock(block)
		if not block or not block.Parent then
			originalProperties[block] = nil
			processedBlocks[block] = nil
			return
		end

		local props = originalProperties[block]
		if not props then return end

		block.Material = props.Material or Enum.Material.Plastic
		block.Color = props.Color or Color3.fromRGB(255, 255, 255)

		if props.TextureID and block:IsA("MeshPart") then
			block.TextureID = props.TextureID
		end

		for _, textureProps in props.Textures do
			local newTexture
			if textureProps.Class == "Texture" then
				newTexture = Instance.new("Texture")
				newTexture.StudsPerTileU = textureProps.StudsPerTileU or 1
				newTexture.StudsPerTileV = textureProps.StudsPerTileV or 1
			else
				newTexture = Instance.new("Decal")
				newTexture.Color3 = textureProps.Color3 or Color3.fromRGB(255, 255, 255)
			end

			newTexture.Texture = textureProps.Texture or ""
			newTexture.Face = textureProps.Face or Enum.NormalId.Front
			newTexture.Transparency = textureProps.Transparency or 0
			newTexture.Parent = block
		end

		originalProperties[block] = nil
		processedBlocks[block] = nil
	end

	local function isTargetBlock(obj)
		if not obj:IsA("BasePart") then return false end

		local name = obj.Name

		if blockColors[name] then return true end

		local lowerName = name:lower()
		return lowerName:find("wool", 1, true) or
		       lowerName:find("clay", 1, true) or
		       lowerName:find("wood", 1, true) or
		       lowerName:find("log", 1, true) or
		       lowerName:find("stone", 1, true) or
		       lowerName:find("brick", 1, true) or
		       lowerName:find("glass", 1, true) or
		       lowerName:find("plank", 1, true) or
		       lowerName:find("bed", 1, true) or
		       lowerName:find("obsidian", 1, true) or
		       lowerName:find("sand", 1, true) or
		       lowerName:find("tnt", 1, true) or
		       lowerName:find("barrier", 1, true) or
		       lowerName:find("magic", 1, true) or
		       lowerName:find("concrete", 1, true) or
		       lowerName:find("diamond", 1, true) or
		       lowerName:find("emerald", 1, true) or
		       lowerName:find("iron", 1, true) or
		       lowerName:find("gold", 1, true) or
		       lowerName:find("copper", 1, true) or
		       lowerName:find("steel", 1, true) or
		       lowerName:find("ore", 1, true) or
		       lowerName:find("marble", 1, true) or
		       lowerName:find("slate", 1, true) or
		       lowerName:find("granite", 1, true) or
		       lowerName:find("andesite", 1, true) or
		       lowerName:find("diorite", 1, true) or
		       lowerName:find("grass", 1, true) or
		       lowerName:find("dirt", 1, true) or
		       lowerName:find("ice", 1, true) or
		       lowerName:find("snow", 1, true) or
		       lowerName:find("moss", 1, true) or
		       lowerName:find("slime", 1, true) or
		       lowerName:find("scaffold", 1, true) or
		       lowerName:find("ladder", 1, true) or
		       lowerName:find("tile", 1, true) or
		       lowerName:find("cotton_candy", 1, true) or
		       lowerName:find("_block", 1, true) or
		       obj:IsA("Seat")
	end

	local function processExistingBlocks(simplify)
		scanDescendants(workspace, function(obj)
			if isTargetBlock(obj) then
				if simplify then
					simplifyBlock(obj)
				else
					restoreBlock(obj)
				end
			end
		end)

		if not simplify then
			task.delay(2, cleanupDeadReferences)
		end
	end

	local function setupBlockMonitor(simplify)
		for _, conn in blockMonitorConnections do
			conn:Disconnect()
		end
		table.clear(blockMonitorConnections)

		if not simplify then return end

		local mainConn = workspace.DescendantAdded:Connect(function(descendant)
			if isTargetBlock(descendant) then
				task.defer(function()
					if descendant and descendant.Parent then
						simplifyBlock(descendant)
					end
				end)
			end
		end)

		table.insert(blockMonitorConnections, mainConn)

		local lastCleanup = 0
		local cleanupConn = runService.Heartbeat:Connect(function()
			local now = tick()
			if now - lastCleanup >= 5 then
				lastCleanup = now
				cleanupDeadReferences()
			end
		end)

		table.insert(blockMonitorConnections, cleanupConn)
	end

	local ntReference = {}
	local function ntRemember(obj, property)
		local props = ntReference[obj]
		if not props then
			props = {}
			ntReference[obj] = props
		end
		if props[property] == nil then
			props[property] = obj[property]
		end
	end
	local function ntStrip(obj)
		if obj:IsA('Decal') then
			ntRemember(obj, 'Transparency')
			obj.Transparency = 1
			return
		end
		if obj:IsA('SurfaceAppearance') then
			ntRemember(obj, 'Parent')
			obj.Parent = nil
			return
		end
		if obj:IsA('SpecialMesh') then
			ntRemember(obj, 'TextureId')
			obj.TextureId = ''
			return
		end
		if obj:IsA('BasePart') then
			if obj:IsA('MeshPart') then
				ntRemember(obj, 'TextureID')
				obj.TextureID = ''
			end
			ntRemember(obj, 'Material')
			obj.Material = Enum.Material.SmoothPlastic
		end
	end
	local function ntRestore()
		for i, v in ntReference do
			pcall(function()
				for property, value in v do
					i[property] = value
				end
			end)
		end
		table.clear(ntReference)
	end
	local function ntScan()
		local descendants = store.map:GetDescendants()
		for i, v in descendants do
			if not PotatoMode.Enabled or Mode.Value ~= 'No Texture' then return end
			ntStrip(v)
			if i % 500 == 0 then
				task.wait()
			end
		end
	end

	PotatoMode = vape.Categories.World:CreateModule({
		Name = 'PotatoMode',
		Function = function(callback)
			if callback then
				if Mode.Value == 'No Texture' then
					repeat task.wait() until store.map or not PotatoMode.Enabled
					if not PotatoMode.Enabled then return end
					PotatoMode:Clean(store.map.DescendantAdded:Connect(function(obj)
						task.defer(ntStrip, obj)
					end))
					ntScan()
				else
					processExistingBlocks(true)
					setupBlockMonitor(true)
				end
			else
				processExistingBlocks(false)
				for _, conn in blockMonitorConnections do
					conn:Disconnect()
				end
				table.clear(blockMonitorConnections)
				table.clear(cachedColors)
				cleanupDeadReferences()
				ntRestore()
			end
		end,
	})
	Mode = PotatoMode:CreateDropdown({
		Name = 'Mode',
		List = {'Original', 'No Texture'},
		Default = 'Original',
		Tooltip = 'Original = flat colored blocks, No Texture = strips everything off the map',
		Function = function(val)
			if PotatoMode.Enabled then
				PotatoMode:Toggle()
				PotatoMode:Toggle()
			end
		end
	})
end)

run(function()
	local Clutch
	local LimitItem

	local function getScaffoldBlock()
		return getScaffoldBlockForModule(LimitItem)
	end

	local lastPlace = 0

	Clutch = vape.Categories.World:CreateModule({
		Name = 'Clutch',
		Tooltip = 'clutchs for u via blocks',
		Function = function(callback)
			if callback then
				Clutch:Clean(runService.Heartbeat:Connect(function()
					if not Clutch.Enabled or not entitylib.isAlive then return end
					local root = entitylib.character.RootPart
					if not root then return end
					if root.Velocity.Y >= -50 then return end
					local wool = getScaffoldBlock()
					if not wool then return end
					local now = os.clock()
					if now - lastPlace < 0.05 then return end
					local target = roundPos(root.Position - Vector3.new(0, entitylib.character.HipHeight + 4.5, 0))
					if not getPlacedBlock(target) then
						local prox = blockProximity(target)
						bedwars.placeBlock(prox or target, wool, false)
						lastPlace = now
					end
				end))
			else
				lastPlace = 0
			end
		end,
	})

	LimitItem = Clutch:CreateToggle({
		Name = 'Limit to items',
		Default = false,
	})
end)

run(function()
	local PhaseMine
	local connections = {}
	local trackedParts = {}
	local lastWeaponState = nil
	local weaponCheckCounter = 0
	
	local function removeCollision(character)
		if not character then return end
		
		local charParts = trackedParts[character]
		if not charParts then
			charParts = {}
			trackedParts[character] = charParts
			
			for _, part in character:GetDescendants() do
				if part:IsA("BasePart") then
					table.insert(charParts, {part = part, origCollide = part.CanCollide, origQuery = part.CanQuery})
					part.CanCollide = false
					part.CanQuery = false
				end
			end
		else
			for _, entry in charParts do
				if entry.part and entry.part.Parent then
					entry.part.CanCollide = false
					entry.part.CanQuery = false
				end
			end
		end
	end
	
	local function restoreCollision(character)
		if not character then return end
		
		local charParts = trackedParts[character]
		if charParts then
			for _, entry in charParts do
				if entry.part and entry.part.Parent then
					entry.part.CanCollide = entry.origCollide
					entry.part.CanQuery = entry.origQuery
				end
			end
		end
	end

	local function updateAllCollisions(forceUpdate)
		weaponCheckCounter = weaponCheckCounter + 1
		local shouldCheck = forceUpdate or (weaponCheckCounter % 3 == 0)
		
		if not shouldCheck then return end
		
		local isWeaponEquipped = hasValidWeapon()
		
		if not forceUpdate and lastWeaponState == isWeaponEquipped then
			return
		end
		
		lastWeaponState = isWeaponEquipped
		
		for _, entity in entitylib.List do
			if entity.Character and entity.Character.Parent then
				if isWeaponEquipped then
					restoreCollision(entity.Character)
				else
					removeCollision(entity.Character)
				end
			end
		end
	end
	
	local motorParts = {}
	local function updateMotorParts()
		for _, entity in entitylib.List do
			if entity.Character then
				local charMotors = motorParts[entity.Character]
				
				if not charMotors then
					charMotors = {}
					motorParts[entity.Character] = charMotors
					
					for _, part in entity.Character:GetChildren() do
						if part:IsA("BasePart") and part.Name == "Part" and part:FindFirstChildOfClass("Motor6D") then
							table.insert(charMotors, part)
						end
					end
				end
				
				for _, part in charMotors do
					if part and part.Parent then
						part.CanCollide = false
					end
				end
			end
		end
	end
	
	local tracked = {}
	local descConnections = {}

	PhaseMine = vape.Categories.World:CreateModule({
		Name = 'PhaseMine',
		Function = function(callback)
			if callback then

				local function shouldRemoveCollision()
					local hand = store.hand
					if not hand or not hand.tool then return false end
					local meta = bedwars.ItemMeta[hand.tool.Name]
					if meta and (meta.block == true or meta.toolType == 'block') then return true end
					if hand.toolType == 'block' then return true end
					local name = hand.tool.Name:lower()
					if name:find("pickaxe") or name:find("axe") or name:find("shovel") or name:find("tool") then return true end
					if meta and (meta.miningPower or meta.isTool) then return true end
					return false
				end

				local function storeOriginal(char)
					if tracked[char] then return end
					local list = {}
					for _, part in ipairs(char:GetDescendants()) do
						if part:IsA("BasePart") then
							table.insert(list, {
								part = part,
								origCollide = part.CanCollide,
								origQuery = part.CanQuery
							})
						end
					end
					tracked[char] = list
				end

				local function applyToChar(char, remove)
					if not char then return end
					if char == lplr.Character then return end
					storeOriginal(char)
					local list = tracked[char]
					for _, entry in ipairs(list) do
						if entry.part and entry.part.Parent then
							entry.part.CanCollide = false
							entry.part.CanQuery = not remove
						end
					end
				end

				local function onPartAdded(char, part)
					if not PhaseMine.Enabled then return end
					if char == lplr.Character then return end
					if part:IsA("BasePart") then
						storeOriginal(char)
						local list = tracked[char]
						local found = false
						for _, entry in ipairs(list) do
							if entry.part == part then
								found = true
								break
							end
						end
						if not found then
							table.insert(list, {
								part = part,
								origCollide = part.CanCollide,
								origQuery = part.CanQuery
							})
						end
						local remove = shouldRemoveCollision()
						part.CanCollide = false
						part.CanQuery = not remove
					end
				end

				local function setupCharacter(char)
					if not char then return end
					if char == lplr.Character then return end
					if descConnections[char] then
						descConnections[char]:Disconnect()
						descConnections[char] = nil
					end
					applyToChar(char, shouldRemoveCollision())
					local conn = char.DescendantAdded:Connect(function(part)
						onPartAdded(char, part)
					end)
					descConnections[char] = conn
				end

				local function cleanupCharacter(char)
					if descConnections[char] then
						descConnections[char]:Disconnect()
						descConnections[char] = nil
					end
					tracked[char] = nil
				end

				local function applyToAll(remove)
					for _, entity in ipairs(entitylib.List) do
						if entity.Character then
							applyToChar(entity.Character, remove)
						end
					end
					for _, obj in ipairs(workspace:GetChildren()) do
						if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
							if obj ~= lplr.Character then
								applyToChar(obj, remove)
							end
						end
					end
				end

				local function updateCollision()
					if not PhaseMine.Enabled then return end
					local remove = shouldRemoveCollision()
					applyToAll(remove)
				end

				local function setupAllCharacters()
					for _, entity in ipairs(entitylib.List) do
						if entity.Character then
							setupCharacter(entity.Character)
						end
					end
					for _, obj in ipairs(workspace:GetChildren()) do
						if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
							if obj ~= lplr.Character then
								setupCharacter(obj)
							end
						end
					end
				end

				task.wait(0.2)
				setupAllCharacters()
				updateCollision()

				local lastState = shouldRemoveCollision()
				local heartbeat = runService.Heartbeat:Connect(function()
					if not PhaseMine.Enabled then return end
					local current = shouldRemoveCollision()
					if current ~= lastState then
						lastState = current
						updateCollision()
					end
				end)
				table.insert(connections, heartbeat)

				local entityAdded = entitylib.Events.EntityAdded:Connect(function(entity)
					if entity.Character then
						task.wait(0.1)
						setupCharacter(entity.Character)
					end
				end)
				table.insert(connections, entityAdded)

				local workspaceChildAdded = workspace.ChildAdded:Connect(function(obj)
					if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
						if obj ~= lplr.Character then
							task.wait(0.1)
							setupCharacter(obj)
						end
					end
				end)
				table.insert(connections, workspaceChildAdded)

				local entityRemoved = entitylib.Events.EntityRemoved:Connect(function(entity)
					if entity.Character then
						cleanupCharacter(entity.Character)
					end
				end)
				table.insert(connections, entityRemoved)

				local workspaceChildRemoved = workspace.ChildRemoved:Connect(function(obj)
					if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") then
						if obj ~= lplr.Character then
							cleanupCharacter(obj)
						end
					end
				end)
				table.insert(connections, workspaceChildRemoved)

				local localAdded = lplr.CharacterAdded:Connect(function(char)
					if PhaseMine.Enabled then
						if lplr.Character and lplr.Character ~= char then
							cleanupCharacter(lplr.Character)
						end
						task.wait(0.2)
						updateCollision()
						setupAllCharacters()
					end
				end)
				table.insert(connections, localAdded)

				if vapeEvents and vapeEvents.InventoryChanged then
					local invConn = vapeEvents.InventoryChanged.Event:Connect(function()
						if PhaseMine.Enabled then
							updateCollision()
						end
					end)
					table.insert(connections, invConn)
				end

			else
				for _, conn in ipairs(connections) do
					conn:Disconnect()
				end
				table.clear(connections)

				for char, conn in pairs(descConnections) do
					conn:Disconnect()
				end
				table.clear(descConnections)

				for char, list in pairs(tracked) do
					if char and char.Parent then
						for _, entry in ipairs(list) do
							if entry.part and entry.part.Parent then
								entry.part.CanCollide = entry.origCollide
								entry.part.CanQuery = entry.origQuery
							end
						end
					end
				end
				table.clear(tracked)
			end
		end,
		Tooltip = 'mine OR build through players and npcs'
	})
end)

run(function()
	local AntiEffects
	local AntiDizzy
	local AntiSleep
	local AntiTrap
	local old = nil
	local oldAdd = nil
	local sleepModifier = nil

	local function applyEffects()
		if AntiDizzy and AntiDizzy.Enabled then
			if not old then
				old = bedwars.ForestEnvironmentCollectibleEntityController.canPickupEntity
			end
			bedwars.ForestEnvironmentCollectibleEntityController.canPickupEntity = function(plr, mushroom)
				if plr and mushroom then
					local mush = mushroom:GetAttribute('MushroomType')
					if not mush then return old(plr, mushroom) end
					if mush == 'Dizzy' then return false end
					return true
				else
					return old(plr, mushroom)
				end
			end
			runService:BindToRenderStep('antieffects_dizzy', Enum.RenderPriority.Character.Value + 2, function()
				runService:UnbindFromRenderStep('dizzy-status')
			end)
		else
			if old then
				bedwars.ForestEnvironmentCollectibleEntityController.canPickupEntity = old
				old = nil
			end
			runService:UnbindFromRenderStep('antieffects_dizzy')
		end

		if AntiTrap and AntiTrap.Enabled then
			runService:BindToRenderStep('antieffects_trap', Enum.RenderPriority.Character.Value + 2, function()
				local char = lplr.Character
				if not char then return end
				local hum = char:FindFirstChildOfClass('Humanoid')
				if char:GetAttribute('SnapTrapMarked') then
					char:SetAttribute('SnapTrapMarked', nil)
				end
				if hum then
					if hum.JumpPower == 0 then hum.JumpPower = 50 end
					if hum.JumpHeight == 0 then hum.JumpHeight = 7.2 end
					if hum.WalkSpeed < 10 then hum.WalkSpeed = 16 end
				end
			end)
		else
			runService:UnbindFromRenderStep('antieffects_trap')
		end

		if AntiSleep and AntiSleep.Enabled then
			local char = lplr.Character
			if char then
				sleepModifier = bedwars.SprintController:getMovementStatusModifier()
				oldAdd = sleepModifier.addModifier
				sleepModifier.addModifier = function(self, tab)
					if tab and tab.moveSpeedMultiplier and tab.moveSpeedMultiplier <= 0 then
						return oldAdd(self, { moveSpeedMultiplier = 1 })
					end
					return oldAdd(self, tab)
				end
				runService:BindToRenderStep('antieffects_sleep', Enum.RenderPriority.Character.Value + 2, function()
					local stunTime = char:GetAttribute('StunnedUntilTime')
					local now = workspace:GetServerTimeNow()
					if stunTime and stunTime > now then
						char:SetAttribute('StunnedUntilTime', -1)
					end
					for mod, _ in sleepModifier.modifiers do
						if type(mod) == 'table' and (mod.moveSpeedMultiplier or 1) <= 0 then
							modifier:removeModifier(mod)
						end
					end
					local hum = char:FindFirstChildOfClass('Humanoid')
					if hum then
						if hum.JumpPower == 0 then hum.JumpPower = 50 end
						if hum.JumpHeight == 0 then hum.JumpHeight = 7.2 end
					end
				end)
			end
		else
			runService:UnbindFromRenderStep('antieffects_sleep')
			if sleepModifier and oldAdd then
				sleepModifier.addModifier = oldAdd
				oldAdd = nil
				sleepModifier = nil
			end
		end
	end

	AntiEffects = vape.Categories.World:CreateModule({
		Name = 'AntiEffects',
		Tooltip = 'blocks negative effects',
		Function = function(callback)
			if callback then
				applyEffects()
			else
				if old then
					bedwars.ForestEnvironmentCollectibleEntityController.canPickupEntity = old
					old = nil
				end
				runService:UnbindFromRenderStep('antieffects_dizzy')
				runService:UnbindFromRenderStep('antieffects_sleep')
				if sleepModifier and oldAdd then
					sleepModifier.addModifier = oldAdd
					oldAdd = nil
					sleepModifier = nil
				end
			end
		end
	})
	AntiDizzy = AntiEffects:CreateToggle({
		Name = 'Dizzy',
		Tooltip = 'blocks dizzy effects from mushrooms and toads',
		Default = true,
		Function = function()
			if AntiEffects.Enabled then applyEffects() end
		end
	})
	AntiSleep = AntiEffects:CreateToggle({
		Name = 'Sleep',
		Tooltip = 'blocks sleep splash potion effect',
		Default = true,
		Function = function()
			if AntiEffects.Enabled then applyEffects() end
		end
	})
	AntiTrap = AntiEffects:CreateToggle({
		Name = 'Trap',
		Tooltip = 'allows you to move when snapped by a trap',
		Default = true,
		Function = function()
			if AntiEffects.Enabled then applyEffects() end
		end
	})
end)

run(function()
	local AntiMagma

	AntiMagma = vape.Categories.World:CreateModule({
		Name = "AntiMagma",
		Tooltip = 'allows you not to be instantly killed by magma',
		Function = function(callback)
			if callback then
				local function disableMagmaPart(v)
					if not v:IsA("BasePart") then return end
					if not string.find(string.lower(v.Name), 'magma') then return end
					for _, stuff in v:GetDescendants() do
						if stuff:IsA("TouchTransmitter") then
							pcall(function() stuff:Destroy() end)
						end
					end
				end

				scanDescendants(workspace, disableMagmaPart, AntiMagma)

				AntiMagma:Clean(workspace.DescendantAdded:Connect(function(v)
					disableMagmaPart(v)
				end))
				local worldFolder = getWorldFolder()
				if not worldFolder then return end
				local blocks = worldFolder:WaitForChild("Blocks", 1)
				if not blocks then return end
				AntiMagma:Clean(blocks.ChildAdded:Connect(function(v)
					disableMagmaPart(v)
				end))
			end
		end
	})
end)

run(function()
    local BlockIn
    local rayCheck = RaycastParams.new()
    rayCheck.RespectCanCollide = true
    rayCheck.FilterType = Enum.RaycastFilterType.Exclude
    local PlaceSpeed
    local LimitItem
    local Blacklist

    local function isBlacklisted(itemType)
        return Blacklist and table.find(Blacklist.ListEnabled, itemType)
    end

    local function getBlocks()
        local blocks = {}

        if LimitItem and LimitItem.Enabled then
            local itemType = store.hand.toolType == 'block' and store.hand.tool and store.hand.tool.Name
            local meta = itemType and bedwars.ItemMeta[itemType]
            local block = meta and meta.block
            if block and not isBlacklisted(itemType) and (store.hand.amount or 0) > 0 then
                table.insert(blocks, { itemType, block.health or 0, store.hand.tool, store.hand.amount })
            end
            return blocks
        end

        for _, item in store.inventory.inventory.items do
            local itemType = item.itemType
            local meta = itemType and bedwars.ItemMeta[itemType]
            local block = meta and meta.block
            if block and not isBlacklisted(itemType) and (item.amount or 0) > 0 then
                table.insert(blocks, { itemType, block.health or 0, item.tool, item.amount })
            end
        end
        table.sort(blocks, function(a, b)
            return a[2] > b[2]
        end)
        return blocks
    end

    local function getPyramid()
        return {
            Vector3.new(0, 0, 3),
            Vector3.new(0, 3, 3),
            Vector3.new(-3, 0, 0),
            Vector3.new(-3, 3, 0),
            Vector3.new(0, 0, -3),
            Vector3.new(0, 3, -3),
            Vector3.new(3, 0, 0),
            Vector3.new(3, 3, 0),
            Vector3.new(3, 6, 0),
            Vector3.new(0, 6, 0),
        }
    end

    local function isMoving()
        if not entitylib.isAlive then return false end
        local vel = entitylib.character.RootPart.AssemblyLinearVelocity
        return Vector3.new(vel.X, 0, vel.Z).Magnitude > 2
    end

    local function doBuild()
        local basePos = bedwars.BlockController:getBlockPosition(entitylib.character.RootPart.Position) * 3
        rayCheck.FilterDescendantsInstances = { lplr.Character, gameCamera }
        local oldPlaceCPS = bedwars.SharedConstants.BLOCK_PLACE_CPS
        bedwars.SharedConstants.BLOCK_PLACE_CPS = 20
        local blocks = getBlocks()
        if #blocks < 1 then
            notif('BlockIn', 'Missing blocks', 4, 'warning')
        end
        local pattern = getPyramid()
        local aborted = false
        for attempt = 1, 4 do
            if not BlockIn.Enabled or not entitylib.isAlive then break end
            for _, block in blocks do
                if not BlockIn.Enabled or not entitylib.isAlive then break end
                if (block[4] or 0) <= 0 then continue end
                local switched = false
                for _, pos in pattern do
                    if not BlockIn.Enabled or not entitylib.isAlive then break end
                    if isMoving() then
                        aborted = true
                        break
                    end
                    local target = basePos + pos
                    if getPlacedBlock(target) then continue end
                    if not switched then
                        for index, v in store.inventory.hotbar do
                            if v.item and v.item.tool == block[3] and index ~= (store.inventory.hotbarSlot + 1) then
                                hotbarSwitch(index - 1)
                                break
                            end
                        end
                        switched = true
                    end
                    bedwars.placeBlock(target, block[1])
                    local delay = PlaceSpeed.Value
                    if delay > 0 then task.wait(delay) end
                end
                if aborted then break end
            end
            if aborted then break end
            local complete = true
            for _, pos in pattern do
                if not getPlacedBlock(basePos + pos) then
                    complete = false
                    break
                end
            end
            if complete then break end
            task.wait(0.08)
        end
        bedwars.SharedConstants.BLOCK_PLACE_CPS = oldPlaceCPS or 12
        return aborted
    end

    BlockIn = vape.Categories.World:CreateModule({
        Name = 'BlockIn',
        Function = function(callback)
            if callback then
                if not entitylib.isAlive then
                elseif LimitItem and LimitItem.Enabled and store.hand.toolType ~= 'block' then
                    notif('BlockIn', 'Hold a block first', 2, 'warning')
                else
                    doBuild()
                end
                if BlockIn.Enabled then
                    BlockIn:Toggle()
                end
            end
        end,
        Tooltip = 'auto places blocks around yo self'
    })

    PlaceSpeed = BlockIn:CreateSlider({
        Name = 'Placement Speed',
        Min = 0,
        Max = 0.5,
        Default = 0.07,
        Decimal = 100,
        Suffix = 'seconds',
    })
    LimitItem = BlockIn:CreateToggle({
        Name = 'Limit to items',
    })
    Blacklist = BlockIn:CreateTextList({
        Name = 'Blacklists',
        Placeholder = 'block',
        Default = {
            'cannon',
            'tnt',
            'siege_tnt',
        }
    })
end)

run(function()
    local AutoSuffo
    local Range
    local LimitItem
    local Targets

    local function fixPosition(pos)
        return bedwars.BlockController:getBlockPosition(pos) * 3
    end

    local ring = {
        Vector3.new(3, 0, 0), Vector3.new(-3, 0, 0),
        Vector3.new(0, 0, 3), Vector3.new(0, 0, -3),
    }

    local function getBlockItem()
        if store.hand.toolType == 'block' then
            return store.hand.tool.Name
        elseif not LimitItem.Enabled then
            return (getWool())
        end
        return nil
    end

    local function predict(part)
        local vel = part.AssemblyLinearVelocity or Vector3.zero
        return part.Position + vel * lplr:GetNetworkPing()
    end

    local function buildCells(ent)
        local root = predict(ent.RootPart)
        local head = predict(ent.Head)
        local cells = {}
        table.insert(cells, fixPosition(head))
        table.insert(cells, fixPosition(head + Vector3.new(0, 3, 0)))
        for _, o in ring do
            table.insert(cells, fixPosition(head + o))
            table.insert(cells, fixPosition(root + o))
        end
        return cells
    end

    AutoSuffo = vape.Categories.World:CreateModule({
        Name = 'AutoSuffo',
        Function = function(callback)
            if callback then
                repeat
                    local item = entitylib.isAlive and getBlockItem()

                    if item then
                        for _, ent in entitylib.AllPosition({
                            Part = 'RootPart',
                            Range = Range.Value,
                            Players = Targets.Players.Enabled,
                            NPCs = Targets.NPCs.Enabled,
                        }) do
                            if ent.Targetable and ent.Head then
                                for _, pos in buildCells(ent) do
                                    if not getPlacedBlock(pos) and checkFaceAdjacent(pos) then
                                        task.spawn(bedwars.placeBlock, pos, item)
                                    end
                                end
                            end
                        end
                    end

                    task.wait(0.05)
                until not AutoSuffo.Enabled
            end
        end,
    })
    Range = AutoSuffo:CreateSlider({
        Name = 'Range',
        Min = 1,
        Max = 20,
        Default = 14,
        Suffix = function(val)
            return val == 1 and 'stud' or 'studs'
        end
    })
    Targets = AutoSuffo:CreateTargets({Players = true})
    LimitItem = AutoSuffo:CreateToggle({
        Name = 'Limit to Items',
    })
end)

--[[
	Inventory Modules
]]

run(function()
	local AutoBank
	local UIToggle
	local GUICheck
	local UI
	local Chests
	local Items = {}
	local BankToggles = {
		iron = nil,
		diamond = nil,
		emerald = nil
	}
	local cachedChest
	local lastChestCheck = 0
	local lastHotbarUpdate = 0

	local function addItem(itemType, shop)
		local item = Instance.new('ImageLabel')
		item.Image = bedwars.getIcon({itemType = itemType}, true)
		item.Size = UDim2.fromOffset(32, 32)
		item.Name = itemType
		item.BackgroundTransparency = 1
		item.LayoutOrder = #UI:GetChildren()
		item.Parent = UI
		local itemtext = Instance.new('TextLabel')
		itemtext.Name = 'Amount'
		itemtext.Size = UDim2.fromScale(1, 1)
		itemtext.BackgroundTransparency = 1
		itemtext.Text = ''
		itemtext.TextColor3 = Color3.new(1, 1, 1)
		itemtext.TextSize = 16
		itemtext.TextStrokeTransparency = 0.3
		itemtext.Font = Enum.Font.Arial
		itemtext.Parent = item
		Items[itemType] = {Object = itemtext, Type = shop}
	end

	local function refreshBank(echest)
		for i, v in pairs(Items) do
			local item = echest:FindFirstChild(i)
			v.Object.Text = item and item:GetAttribute('Amount') or ''
		end
	end

	local function nearChest()
		if not entitylib.isAlive then return false end

		local pos = entitylib.character.RootPart.Position
		local maxDistanceSq = 22 * 22

		for _, chest in pairs(Chests) do
			if chest.Parent then
				local offset = chest.Position - pos
				local distanceSq = offset.X * offset.X + offset.Y * offset.Y + offset.Z * offset.Z
				if distanceSq < maxDistanceSq then
					return true
				end
			end
		end

		return false
	end

	local function isNearOwnBase()
		if not entitylib.isAlive then return false end
		local myTeam = tostring(lplr:GetAttribute('Team') or '')
		if myTeam == '' then return false end
		local myBed = nil
		for _, bed in collectionService:GetTagged('bed') do
			if not bed:IsA('BasePart') then continue end
			local bedTeam = tostring(bed:GetAttribute('Team') or bed:GetAttribute('TeamId') or '')
			if bedTeam == myTeam then myBed = bed break end
		end
		if not myBed then return false end
		local bedPos = myBed.Position
		local closestChest, closestDist = nil, math.huge
		for _, chest in pairs(Chests) do
			if chest.Parent then
				local dist = (chest.Position - bedPos).Magnitude
				if dist < closestDist then closestChest = chest closestDist = dist end
			end
		end
		if not closestChest then return false end
		local myPos = entitylib.character.RootPart.Position
		return (myPos - closestChest.Position).Magnitude <= 60
	end

	local function handleState()
		local currentTime = tick()

		if not cachedChest or not cachedChest.Parent or (currentTime - lastChestCheck) > 1 then
			cachedChest = replicatedStorage.Inventories:FindFirstChild(lplr.Name..'_personal')
			lastChestCheck = currentTime
		end

		if not cachedChest then return end

		if not nearChest() and not GUICheck.Enabled then
			return
		end

		local itemsToDeposit = {}
		for _, v in ipairs(store.inventory.inventory.items) do
			local itemInfo = Items[v.itemType]
			if itemInfo and BankToggles[v.itemType] and BankToggles[v.itemType].Enabled then
				table.insert(itemsToDeposit, v)
			end
		end

		if #itemsToDeposit > 0 then
			for _, v in ipairs(itemsToDeposit) do
				bedwars.Client:GetNamespace('Inventory'):Get('ChestGiveItem'):CallServer(cachedChest, v.tool)
			end
			task.defer(function()
				if cachedChest and cachedChest.Parent then
					refreshBank(cachedChest)
				end
			end)
		end
	end


	AutoBank = vape.Categories.Inventory:CreateModule({
		Name = 'AutoBank',
		Function = function(callback)
			if callback then
				Chests = collection('chest', AutoBank)
				cachedChest = nil
				lastChestCheck = 0
				lastHotbarUpdate = 0
				UI = Instance.new('Frame')
				UI.Size = UDim2.new(1, 0, 0, 44)
				UI.Position = UDim2.fromOffset(0, -256)
				UI.BackgroundTransparency = 1
				UI.Visible = UIToggle.Enabled
				UI.Parent = vape.gui
				AutoBank:Clean(UI)

				local Sort = Instance.new('UIListLayout')
				Sort.FillDirection = Enum.FillDirection.Horizontal
				Sort.HorizontalAlignment = Enum.HorizontalAlignment.Center
				Sort.SortOrder = Enum.SortOrder.LayoutOrder
				Sort.Parent = UI

				addItem('iron', true)
				addItem('diamond', false)
				addItem('emerald', true)
				addItem('serpents_touch_potion', false)
				addItem('jump_potion', false)
				addItem('mini_shield', false)

				local cachedHotbar
				local guiInset = guiService:GetGuiInset().Y

				repeat
					local currentTime = tick()

					if (currentTime - lastHotbarUpdate) > 0.5 then
						local playerGui = lplr.PlayerGui
						if playerGui then
							local hotbar = playerGui:FindFirstChild('hotbar')
							if hotbar then
								local container = hotbar['1']:FindFirstChild('HotbarHealthbarContainer')
								if container then
									cachedHotbar = container
									UI.Position = UDim2.fromOffset(0, (container.AbsolutePosition.Y + guiInset) - 40)
									lastHotbarUpdate = currentTime
								end
							end
						end
					end

					if not cachedChest or not cachedChest.Parent or (currentTime - lastChestCheck) > 1 then
						cachedChest = replicatedStorage.Inventories:FindFirstChild(lplr.Name .. '_personal')
						lastChestCheck = currentTime
					end
					if UIToggle.Enabled and UI and cachedChest then
						refreshBank(cachedChest)
					end

					local shouldBank = false

					if GUICheck.Enabled then
						shouldBank = bedwars.AppController:isAppOpen('ChestApp') or
						             bedwars.AppController:isAppOpen('BedwarsAppIds.CHEST_INVENTORY')
					else
						shouldBank = nearChest()
					end

					if shouldBank and not (TeamCheck and TeamCheck.Enabled and isNearOwnBase()) then
						handleState()
					end

					task.wait(0.1)
				until (not AutoBank.Enabled)
			else
				table.clear(Items)
				cachedChest = nil
			end
		end,
		Tooltip = 'automatically puts resources in pchest'
	})

	UIToggle = AutoBank:CreateToggle({
		Name = 'UI',
		Function = function(callback)
			if AutoBank.Enabled and UI then
				UI.Visible = callback
			end
		end,
		Default = true
	})

	GUICheck = AutoBank:CreateToggle({
		Name = 'GUI Check',
	})

	BankToggles.iron = AutoBank:CreateToggle({
		Name = 'Bank Iron',
		Default = true
	})

	BankToggles.diamond = AutoBank:CreateToggle({
		Name = 'Bank Diamond',
		Default = true
	})

	BankToggles.emerald = AutoBank:CreateToggle({
		Name = 'Bank Emerald',
		Default = true
	})

	BankToggles.serpents_touch_potion = AutoBank:CreateToggle({
		Name = 'Bank Serpent Potion',
		Default = false
	})

	BankToggles.jump_potion = AutoBank:CreateToggle({
		Name = 'Bank Jump Potion',
		Default = false
	})

	BankToggles.mini_shield = AutoBank:CreateToggle({
		Name = 'Bank Shield Potion',
		Default = false
	})

	TeamCheck = AutoBank:CreateToggle({
		Name = 'Team Check',
		Default = false
	})
end)
	
run(function()
	local AutoConsume
	local Health
	local SpeedPotion
	local Apple
	local ShieldPotion
	local SerpentsTouch
	local JumpPotion
	local LimitToItem
	local Delay
	local NoSlow
	local consuming = false
	local heldTrack
	local GoldenApple
	local GoldenAppleHealth
	local SpeedPie
	
	local function consumeCheck(attribute)
		if entitylib.isAlive then
			if SpeedPotion.Enabled and (not attribute or attribute == 'StatusEffect_speed') then
				local speedpotion = getItem('speed_potion')
				if speedpotion and (not lplr.Character:GetAttribute('StatusEffect_speed')) then
					task.spawn(function()
						for _ = 1, 4 do
							local result = false
							bedwars.Client:Get(remotes.ConsumeItem):CallServerAsync({item = speedpotion.tool}):andThen(function(r)
								result = r
							end):await()
							if result then break end
						end
					end)
				end
			end
	
			if Apple.Enabled and (not attribute or attribute:find('Health')) then
				if (lplr.Character:GetAttribute('Health') / lplr.Character:GetAttribute('MaxHealth')) <= (Health.Value / 100) then
					local inv = replicatedStorage:FindFirstChild('Inventories') and replicatedStorage.Inventories:FindFirstChild(lplr.Name)
					local appleItem = inv and (inv:FindFirstChild('orange') or inv:FindFirstChild('apple'))
					if appleItem then
						replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = appleItem})
					end
				end
			end

			if GoldenApple and GoldenApple.Enabled and (not attribute or attribute:find('Health')) then
				local gaHealth = GoldenAppleHealth and GoldenAppleHealth.Value or 50
				local currentHPPct = (lplr.Character:GetAttribute('Health') / lplr.Character:GetAttribute('MaxHealth') * 100)
				local inv = replicatedStorage:FindFirstChild('Inventories') and replicatedStorage.Inventories:FindFirstChild(lplr.Name)
				local gaItem = inv and inv:FindFirstChild('golden_apple')
				if currentHPPct <= gaHealth and gaItem then
					replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = gaItem})
				end
			end

			if SpeedPie and SpeedPie.Enabled then
				local speedPieActive = false
				local statusHud = lplr.PlayerGui:FindFirstChild('StatusEffectHudScreen')
				if statusHud then
					local hud = statusHud:FindFirstChild('StatusEffectHud')
					if hud then
						local speedPieFrame = hud:FindFirstChild('Speed Pie')
						if speedPieFrame then
							local timer = speedPieFrame:FindFirstChild('4')
							if timer and timer:IsA('TextLabel') then
								local val = tonumber(timer.Text)
								speedPieActive = val and val > 0
							end
						end
					end
				end
				if not speedPieActive then
					local inv = replicatedStorage:FindFirstChild('Inventories') and replicatedStorage.Inventories:FindFirstChild(lplr.Name)
					local pieItem = inv and inv:FindFirstChild('pie')
					if pieItem then
						replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = pieItem})
					end
				end
			end

			if ShieldPotion.Enabled and (not attribute or attribute:find('Shield')) then
				if (lplr.Character:GetAttribute('Shield_POTION') or 0) == 0 then
					local shield = getItem('big_shield') or getItem('mini_shield')
	
					if shield then
						replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = shield.tool})
					end
				end
			end
		end
	end
	
	local function heldConsume()
		if consuming then return end
		local tool = store.hand.tool
		if not tool then return end
		local item = getItem(tool.Name)
		if not item then return end
		local meta = bedwars.ItemMeta[item.itemType]
		if not meta or not meta.consumable then return end

		consuming = true
		local humanoid = lplr.Character and lplr.Character:FindFirstChildOfClass('Humanoid')
		local anim = Instance.new('Animation')
		anim.AnimationId = 'rbxassetid://4968114448'
		if humanoid then
			heldTrack = humanoid:LoadAnimation(anim)
			heldTrack:Play()
		end

		if NoSlow.Enabled and humanoid then
			local normalSpeed = humanoid.WalkSpeed
			task.spawn(function()
				while consuming and NoSlow.Enabled do
					humanoid.WalkSpeed = normalSpeed
					task.wait()
				end
			end)
		end

		task.wait(Delay.Value)

		if heldTrack then
			heldTrack:Stop()
			heldTrack = nil
		end

		if consuming and store.hand.tool and store.hand.tool.Name == tool.Name then
			local currentItem = getItem(tool.Name)
			if currentItem then
				replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = currentItem.tool})
			end
		end
		consuming = false
	end

	AutoConsume = vape.Categories.Inventory:CreateModule({
		Name = 'AutoConsume',
		Function = function(callback)
			if callback then
				local throttle = 0
				local throttledCheck = function()
					local now = tick()
					if now - throttle < 0.2 then return end
					throttle = now
					consumeCheck()
				end
				AutoConsume:Clean(vapeEvents.InventoryAmountChanged.Event:Connect(throttledCheck))
				AutoConsume:Clean(vapeEvents.AttributeChanged.Event:Connect(function(attribute)
					if attribute:find('Shield') or attribute:find('Health') then
						throttledCheck()
					end
				end))
				consumeCheck()
				task.spawn(function()
					local lastTool
					while AutoConsume.Enabled do
						if LimitToItem.Enabled then
							local tool = store.hand.tool
							if tool and tool.Name ~= lastTool then
								lastTool = tool.Name
								heldConsume()
							elseif not tool then
								lastTool = nil
							end
						end
						task.wait(0.2)
					end
				end)
				task.spawn(function()
					while AutoConsume.Enabled do
						task.wait(0.3)
						if SerpentsTouch.Enabled then
							local serpent = getItem('serpents_touch_potion')
							if serpent then
								replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = serpent.tool})
							end
						end
						if JumpPotion.Enabled then
							local jumppotion = getItem('jump_potion')
							if jumppotion then
								replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = jumppotion.tool})
							end
						end
						if ShieldPotion.Enabled and (lplr.Character:GetAttribute('Shield_POTION') or 0) == 0 then
							local shield = getItem('big_shield') or getItem('mini_shield')
							if shield then
								replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = shield.tool})
							end
						end
					end
				end)
				task.spawn(function()
					while AutoConsume.Enabled do
						task.wait(0.5)
						if not SpeedPie or not SpeedPie.Enabled then continue end
						local speedPieActive = false
						local statusHud = lplr.PlayerGui:FindFirstChild('StatusEffectHudScreen')
						if statusHud then
							local hud = statusHud:FindFirstChild('StatusEffectHud')
							if hud then
								local speedPieFrame = hud:FindFirstChild('Speed Pie')
								if speedPieFrame then
									local timer = speedPieFrame:FindFirstChild('4')
									if timer and timer:IsA('TextLabel') then
										local val = tonumber(timer.Text)
										speedPieActive = val and val > 0
									end
								end
							end
						end
						if not speedPieActive then
							local inv = replicatedStorage:FindFirstChild('Inventories') and replicatedStorage.Inventories:FindFirstChild(lplr.Name)
							local pieItem = inv and inv:FindFirstChild('pie')
							if pieItem then
								replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged.ConsumeItem:InvokeServer({item = pieItem})
							end
						end
					end
				end)
			end
		end,
		Tooltip = 'Automatically heals for you when health or shield is under threshold.'
	})
	SpeedPotion = AutoConsume:CreateToggle({
		Name = 'Speed Potions',
		Default = true
	})
	Apple = AutoConsume:CreateToggle({
		Name = 'Apple',
		Default = true,
		Function = function(callback)
			if Health then
				Health.Object.Visible = callback
			end
		end
	})
	Health = AutoConsume:CreateSlider({
		Name = 'Health Percent',
		Min = 1,
		Max = 99,
		Default = 70,
		Suffix = '%',
		Visible = false
	})
	ShieldPotion = AutoConsume:CreateToggle({
		Name = 'Shield Potions',
		Default = true
	})
	GoldenApple = AutoConsume:CreateToggle({
		Name = 'Golden Apple',
		Default = false,
		Function = function(callback)
			if GoldenAppleHealth then
				GoldenAppleHealth.Object.Visible = callback
			end
		end
	})
	GoldenAppleHealth = AutoConsume:CreateSlider({
		Name = 'Eat At HP%',
		Min = 1,
		Max = 99,
		Default = 50,
		Suffix = '%',
		Visible = false
	})
	SpeedPie = AutoConsume:CreateToggle({
		Name = 'Speed Pie',
		Default = false
	})
	SerpentsTouch = AutoConsume:CreateToggle({
		Name = 'Serpents Potion',
		Default = true,
		Tooltip = 'auto drinks ur serpents touch potion when u got one'
	})
	JumpPotion = AutoConsume:CreateToggle({
		Name = 'Jump Potion',
		Default = true,
		Tooltip = 'auto chugs jump potions for u'
	})
	LimitToItem = AutoConsume:CreateToggle({
		Name = 'Limit To Item',
		Default = false,
		Function = function(callback)
			if Delay then
				Delay.Object.Visible = callback
			end
		end,
		Tooltip = 'only consumes stuff when ur actually holding it, waits the delay first'
	})
	Delay = AutoConsume:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 3,
		Default = 1,
		Suffix = 's',
		Visible = false,
		Tooltip = 'how long to wait before it actually eats it'
	})
	NoSlow = AutoConsume:CreateToggle({
		Name = 'No Slow',
		Default = false,
		Tooltip = 'stops u from getting slowed down while eating/drinking stuff'
	})
end)

run(function()
	local Value
	local oldclickhold, oldshowprogress
	
	local FastConsume = vape.Categories.Inventory:CreateModule({
		Name = 'FastConsume',
		Function = function(callback)
			if callback then
				oldclickhold = bedwars.ClickHold.startClick
				oldshowprogress = bedwars.ClickHold.showProgress
				bedwars.ClickHold.startClick = function(self)
					self.startedClickTime = tick()
					local handle = self:showProgress()
					local clicktime = self.startedClickTime
					bedwars.RuntimeLib.Promise.defer(function()
						task.wait(self.durationSeconds * (Value.Value / 40))
						if handle == self.handle and clicktime == self.startedClickTime and self.closeOnComplete then
							self:hideProgress()
							if self.onComplete then self.onComplete() end
							if self.onPartialComplete then self.onPartialComplete(1) end
							self.startedClickTime = -1
						end
					end)
				end
	
				bedwars.ClickHold.showProgress = function(self)
					local roact = debug.getupvalue(oldshowprogress, 1)
					local countdown = roact.mount(roact.createElement('ScreenGui', {}, { roact.createElement('Frame', {
						[roact.Ref] = self.wrapperRef,
						Size = UDim2.new(),
						Position = UDim2.fromScale(0.5, 0.55),
						AnchorPoint = Vector2.new(0.5, 0),
						BackgroundColor3 = Color3.fromRGB(0, 0, 0),
						BackgroundTransparency = 0.8
					}, { roact.createElement('Frame', {
						[roact.Ref] = self.progressRef,
						Size = UDim2.fromScale(0, 1),
						BackgroundColor3 = Color3.new(1, 1, 1),
						BackgroundTransparency = 0.5
					}) }) }), lplr:FindFirstChild('PlayerGui'))
	
					self.handle = countdown
					local sizetween = tweenService:Create(self.wrapperRef:getValue(), TweenInfo.new(0.1), {
						Size = UDim2.fromScale(0.11, 0.005)
					})
					local countdowntween = tweenService:Create(self.progressRef:getValue(), TweenInfo.new(self.durationSeconds * (Value.Value / 40), Enum.EasingStyle.Linear), {
						Size = UDim2.fromScale(1, 1)
					})
	
					sizetween:Play()
					countdowntween:Play()
					table.insert(self.tweens, countdowntween)
					table.insert(self.tweens, sizetween)
					
					return countdown
				end
			else
				bedwars.ClickHold.startClick = oldclickhold
				bedwars.ClickHold.showProgress = oldshowprogress
				oldclickhold = nil
				oldshowprogress = nil
			end
		end,
		Tooltip = 'Use/Consume items quicker.'
	})
	Value = FastConsume:CreateSlider({
		Name = 'Multiplier',
		Min = 0,
		Max = 100
	})
end)
	
run(function()
	local FastDrop
	local DropDelay
	local ItemList
	local UseBind
	local BBind
	local CurrentBind = Enum.KeyCode.H

	local function getInputEnum(inputName)
		if string.find(inputName, "MouseButton") then
			return Enum.UserInputType[inputName]
		else
			return Enum.KeyCode[inputName]
		end
	end

	local function isInputDown(input)
		if typeof(input) == "EnumItem" then
			if input.EnumType == Enum.KeyCode then
				return inputService:IsKeyDown(input)
			elseif input.EnumType == Enum.UserInputType then
				return inputService:IsMouseButtonPressed(input)
			end
		end
		return false
	end

	FastDrop = vape.Categories.Inventory:CreateModule({
		Name = 'FastDrop',
		Function = function(callback)
			if callback then

				repeat
					if entitylib.isAlive and (not store.inventory.opened) and (isInputDown(CurrentBind)) and inputService:GetFocusedTextBox() == nil then
						if tick() - store.lastDropTime >= (DropDelay.Value / 1000) then
							local handItem = store.hand and store.hand.tool
							if handItem then
								local itemType = handItem.Name
								local listEnabled = ItemList.ListEnabled
								
								local shouldDrop = true
								if #listEnabled > 0 then
									shouldDrop = table.find(listEnabled, itemType) ~= nil
								end
								
								if shouldDrop then
									task.spawn(bedwars.ItemDropController.dropItemInHand)
									store.lastDropTime = tick()
								end
							end
							task.wait()
						else
							task.wait(0.01)
						end
					else
						task.wait(0.1)
					end
				until not FastDrop.Enabled
			else
				store.lastDropTime = tick() + DropDelay.Value
			end
		end,
		Tooltip = 'Drops items fast'
	})

	DropDelay = FastDrop:CreateSlider({
		Name = 'Drop Delay',
		Min = 0,
		Max = 500,
		Default = 0,
		Suffix = 'ms'
	})
	
	ItemList = FastDrop:CreateTextList({
		Name = 'Item Whitelist',
		Placeholder = 'Item name (e.g., wool_blue)',
	})
end)

run(function()
	local InfAutoBank
	local Iron
	local Emerald
	local Diamond
	local DropAnyItem
	local Chests
	local UIToggle
	local UI
	local Items = {}
	local cachedChest
	local lastChestCheck = 0
	local lastHotbarUpdate = 0
	local frozen = {}
	local busy = false
	local released = false
	local voidPos = Vector3.new(0, -250, 0)
	local pickupRemote = {InvokeServer = function() end}
	task.spawn(function()
		pickupRemote = replicatedStorage:WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged'):WaitForChild('PickupItemDrop')
	end)

	local function moveItem(obj, cf)
		if obj:IsA('BasePart') then
			obj.CFrame = cf
		elseif obj:IsA('Model') then
			obj:PivotTo(cf)
		end
	end

	local function freezeParts(obj)
		if obj:IsA('BasePart') then
			obj.Velocity = Vector3.zero
			obj.RotVelocity = Vector3.zero
			obj.CanCollide = false
			obj.Anchored = true
		end
		for _, part in obj:GetDescendants() do
			if part:IsA('BasePart') then
				part.Velocity = Vector3.zero
				part.RotVelocity = Vector3.zero
				part.CanCollide = false
				part.Anchored = true
			end
		end
	end

	local function unfreezeParts(obj)
		if obj:IsA('BasePart') then
			obj.CanCollide = true
			obj.Anchored = false
		end
		for _, part in obj:GetDescendants() do
			if part:IsA('BasePart') then
				part.CanCollide = true
				part.Anchored = false
			end
		end
	end

	local function nearChest()
		if entitylib.isAlive then
			local pos = entitylib.character.RootPart.Position
			for _, chest in Chests do
				if (chest.Position - pos).Magnitude < 8 then
					return true
				end
			end
		end
	end

	local function getDropType(name)
		local lower = name:lower()
		if lower:find('iron') then return 'iron' end
		if lower:find('diamond') then return 'diamond' end
		if lower:find('emerald') then return 'emerald' end
	end

	local function getDroppedAmount(itemType)
		local total = 0
		for _, item in frozen do
			if item and item.Parent and getDropType(item.Name) == itemType then
				total += item:GetAttribute('Amount') or 0
			end
		end
		return total
	end

	local function addItem(itemType)
		local container = Instance.new('Frame')
		container.Name = itemType
		container.Size = UDim2.fromOffset(32, 44)
		container.BackgroundTransparency = 1
		container.LayoutOrder = #UI:GetChildren()
		container.Parent = UI

		local item = Instance.new('ImageLabel')
		item.Image = bedwars.getIcon({itemType = itemType}, true)
		item.Size = UDim2.fromOffset(32, 32)
		item.BackgroundTransparency = 1
		item.Parent = container

		local itemtext = Instance.new('TextLabel')
		itemtext.Name = 'Amount'
		itemtext.Size = UDim2.fromScale(1, 1)
		itemtext.BackgroundTransparency = 1
		itemtext.Text = ''
		itemtext.TextColor3 = Color3.new(1, 1, 1)
		itemtext.TextSize = 16
		itemtext.TextStrokeTransparency = 0.3
		itemtext.Font = Enum.Font.Arial
		itemtext.Parent = item

		local droptext = Instance.new('TextLabel')
		droptext.Name = 'Dropped'
		droptext.Size = UDim2.new(1, 0, 0, 12)
		droptext.Position = UDim2.fromOffset(0, 32)
		droptext.BackgroundTransparency = 1
		droptext.Text = ''
		droptext.TextColor3 = Color3.fromRGB(255, 200, 80)
		droptext.TextSize = 12
		droptext.TextStrokeTransparency = 0.3
		droptext.Font = Enum.Font.Arial
		droptext.Parent = container

		Items[itemType] = {Banked = itemtext, Dropped = droptext}
	end

	local function refreshBank(echest)
		for i, v in pairs(Items) do
			local item = echest:FindFirstChild(i)
			v.Banked.Text = item and item:GetAttribute('Amount') or ''
			local dropped = getDroppedAmount(i)
			v.Dropped.Text = dropped > 0 and tostring(dropped) or ''
		end
	end

	local function cleanupFrozen()
		for i = #frozen, 1, -1 do
			if not frozen[i] or not frozen[i].Parent then
				table.remove(frozen, i)
			end
		end
	end

	local resourceOrder = {'iron', 'emerald', 'diamond'}
	local rotateIndex = 0

	local function getResourceTypes()
		local types = {}
		local n = #resourceOrder
		for i = 0, n - 1 do
			local kind = resourceOrder[((rotateIndex + i) % n) + 1]
			if kind == 'iron' and Iron.Enabled then
				table.insert(types, kind)
			elseif kind == 'emerald' and Emerald.Enabled then
				table.insert(types, kind)
			elseif kind == 'diamond' and Diamond.Enabled then
				table.insert(types, kind)
			end
		end
		rotateIndex = (rotateIndex + 1) % n
		return types
	end

	local function stashOne()
		if busy or not entitylib.isAlive then return end
		for _, itemType in getResourceTypes() do
			local item = getItem(itemType)
			if item then
				busy = true
				task.spawn(function()
					local caught
					local conn = collectionService:GetInstanceAddedSignal('ItemDrop'):Connect(function(v)
						if not caught then caught = v end
					end)

					local ok, dropped = pcall(function()
						return bedwars.Client:Get(remotes.DropItem):CallServer({
							item = item.tool,
							amount = item.amount
						})
					end)

					if not (ok and dropped) then
						task.wait()
						dropped = caught
					end
					conn:Disconnect()

					if dropped and dropped.Parent then
						pcall(function()
							moveItem(dropped, CFrame.new(voidPos))
							dropped:SetAttribute('ClientDropTime', tick() + 9999)
							freezeParts(dropped)
						end)
						table.insert(frozen, dropped)
					end
					task.wait(0.1)
					busy = false
				end)
				return
			end
		end
	end

	local function depositInventory()
		local chest = replicatedStorage.Inventories:FindFirstChild(lplr.Name..'_personal')
		if not chest then return end
		local snapshot = {}
		for _, v in store.inventory.inventory.items do
			if v.itemType == 'iron' or v.itemType == 'diamond' or v.itemType == 'emerald' then
				table.insert(snapshot, v.tool)
			end
		end
		for _, tool in snapshot do
			local success
			for attempt = 1, 3 do
				success = pcall(function()
					bedwars.Client:GetNamespace('Inventory'):Get('ChestGiveItem'):CallServer(chest, tool)
				end)
				if success then break end
				task.wait(0.15)
			end
		end
	end

	local function releaseAll()
		if released or not entitylib.isAlive then return end
		if #frozen == 0 then return end
		released = true
		local pos = entitylib.character.RootPart.Position
		local remaining = {}
		local total = #frozen
		local done = 0
		for i = 1, total do
			local item = frozen[i]
			task.spawn(function()
				local picked = true
				pcall(function()
					if item and item.Parent then
						unfreezeParts(item)
						moveItem(item, CFrame.new(pos - Vector3.new(0, 3, 0)))
						item:SetAttribute('ClientDropTime', 0)
						for _ = 1, 5 do
							pickupRemote:InvokeServer({itemDrop = item})
							if not item.Parent then break end
							task.wait(0.03)
						end
						if item.Parent then
							picked = false
							freezeParts(item)
							moveItem(item, CFrame.new(voidPos))
							item:SetAttribute('ClientDropTime', tick() + 9999)
						end
					end
				end)
				if not picked then
					table.insert(remaining, item)
				end
				done += 1
			end)
		end
		repeat task.wait() until done == total
		frozen = remaining
		released = false
	end

	local function maintainFrozen()
		for _, item in frozen do
			if item and item.Parent then
				pcall(function()
					moveItem(item, CFrame.new(voidPos))
					freezeParts(item)
				end)
			end
		end
	end

	local function fastPickupResources()
		if not entitylib.isAlive then return end
		local pos = entitylib.character.RootPart.Position
		local cache = workspace:FindFirstChild('ItemDropsCache')
		if not cache then return end
		for _, drop in cache:GetChildren() do
			local dpos
			if drop:IsA('Model') then
				dpos = drop:GetPivot().Position
			elseif drop:IsA('BasePart') then
				dpos = drop.Position
			end
			if dpos and (dpos - pos).Magnitude <= 20 then
				local name = drop.Name:lower()
				if name:find('iron') or name:find('emerald') or name:find('diamond') then
					pcall(function()
						pickupRemote:InvokeServer({itemDrop = drop})
					end)
				end
			end
		end
	end

	local function hookManualDrops()
		return collectionService:GetInstanceAddedSignal('ItemDrop'):Connect(function(v)
			if not InfAutoBank.Enabled then return end
			if not DropAnyItem.Enabled then return end
			if busy then return end
			if not v or not entitylib.isAlive then return end
			local pos = entitylib.character.RootPart.Position
			if (v.Position - pos).Magnitude > 15 then return end
			pcall(function()
				moveItem(v, CFrame.new(voidPos))
				v:SetAttribute('ClientDropTime', tick() + 9999)
				freezeParts(v)
			end)
			table.insert(frozen, v)
		end)
	end

	InfAutoBank = vape.Categories.Inventory:CreateModule({
		Name = 'InfAutoBank',
		Function = function(callback)
			if callback then
				Chests = collection('personal-chest', InfAutoBank)
				InfAutoBank:Clean(hookManualDrops())
				cachedChest = nil
				lastChestCheck = 0
				lastHotbarUpdate = 0
				UI = Instance.new('Frame')
				UI.Size = UDim2.new(1, 0, 0, 44)
				UI.Position = UDim2.fromOffset(0, -256)
				UI.BackgroundTransparency = 1
				UI.Visible = UIToggle.Enabled
				UI.Parent = vape.gui
				InfAutoBank:Clean(UI)

				local Sort = Instance.new('UIListLayout')
				Sort.FillDirection = Enum.FillDirection.Horizontal
				Sort.HorizontalAlignment = Enum.HorizontalAlignment.Center
				Sort.SortOrder = Enum.SortOrder.LayoutOrder
				Sort.Parent = UI

				addItem('iron')
				addItem('diamond')
				addItem('emerald')

				local guiInset = guiService:GetGuiInset().Y
				repeat
				pcall(function()
					local currentTime = tick()
					if (currentTime - lastHotbarUpdate) > 0.5 then
						local playerGui = lplr.PlayerGui
						if playerGui then
							local hotbar = playerGui:FindFirstChild('hotbar')
							if hotbar then
								local container = hotbar['1']:FindFirstChild('HotbarHealthbarContainer')
								if container then
									UI.Position = UDim2.fromOffset(0, (container.AbsolutePosition.Y + guiInset) - 56)
									lastHotbarUpdate = currentTime
								end
							end
						end
					end
					if not cachedChest or not cachedChest.Parent or (currentTime - lastChestCheck) > 1 then
						cachedChest = replicatedStorage.Inventories:FindFirstChild(lplr.Name..'_personal')
						lastChestCheck = currentTime
					end
					if UIToggle.Enabled and UI and cachedChest then
						refreshBank(cachedChest)
					end
					if entitylib.isAlive then
						cleanupFrozen()
						if nearChest() then
							fastPickupResources()
							releaseAll()
							depositInventory()
						else
							stashOne()
							maintainFrozen()
						end
					end
				end)
				task.wait(0.03)
			until not InfAutoBank.Enabled
				if entitylib.isAlive then
					local pos = entitylib.character.RootPart.Position
					for i = #frozen, 1, -1 do
						local item = frozen[i]
						pcall(function()
							if item and item.Parent then
								unfreezeParts(item)
								moveItem(item, CFrame.new(pos - Vector3.new(0, 3, 0)))
								item:SetAttribute('ClientDropTime', 0)
							end
						end)
						table.remove(frozen, i)
					end
				end
				busy = false
			end
		end,
		Tooltip = 'uses a weird method for inf autobank'
	})
	Iron = InfAutoBank:CreateToggle({
		Name = 'Iron',
		Default = true
	})
	Emerald = InfAutoBank:CreateToggle({
		Name = 'Emerald',
		Default = true
	})
	Diamond = InfAutoBank:CreateToggle({
		Name = 'Diamond',
		Default = true
	})
	DropAnyItem = InfAutoBank:CreateToggle({
		Name = 'Drop Any Item',
		Default = false,
		Tooltip = 'freezes stuff u manually drop too, not just iron/emerald/diamond'
	})
	UIToggle = InfAutoBank:CreateToggle({
		Name = 'UI',
		Function = function(callback)
			if InfAutoBank.Enabled and UI then
				UI.Visible = callback
			end
		end,
		Default = true
	})
end)

run(function()
	local AutoHotbar
	local Mode
	local Clear
	local List
	local Active
	
	local function CreateWindow(self)
		local selectedslot = 1
		local window = Instance.new('Frame')
		window.Name = 'HotbarGUI'
		window.Size = UDim2.fromOffset(660, 465)
		window.Position = UDim2.fromScale(0.5, 0.5)
		window.BackgroundColor3 = uipallet.Main
		window.AnchorPoint = Vector2.new(0.5, 0.5)
		window.Visible = false
		window.Parent = vape.gui.ScaledGui
		local title = Instance.new('TextLabel')
		title.Name = 'Title'
		title.Size = UDim2.new(1, -10, 0, 20)
		title.Position = UDim2.fromOffset(math.abs(title.Size.X.Offset), 12)
		title.BackgroundTransparency = 1
		title.Text = 'AutoHotbar'
		title.TextXAlignment = Enum.TextXAlignment.Left
		title.TextColor3 = uipallet.Text
		title.TextSize = 13
		title.FontFace = uipallet.Font
		title.Parent = window
		local divider = Instance.new('Frame')
		divider.Name = 'Divider'
		divider.Size = UDim2.new(1, 0, 0, 1)
		divider.Position = UDim2.fromOffset(0, 40)
		divider.BackgroundColor3 = color.Light(uipallet.Main, 0.04)
		divider.BorderSizePixel = 0
		divider.Parent = window
		addBlur(window)
		local modal = Instance.new('TextButton')
		modal.Text = ''
		modal.BackgroundTransparency = 1
		modal.Modal = true
		modal.Parent = window
		local corner = Instance.new('UICorner')
		corner.CornerRadius = UDim.new(0, 5)
		corner.Parent = window
		local close = Instance.new('ImageButton')
		close.Name = 'Close'
		close.Size = UDim2.fromOffset(24, 24)
		close.Position = UDim2.new(1, -35, 0, 9)
		close.BackgroundColor3 = Color3.new(1, 1, 1)
		close.BackgroundTransparency = 1
		close.Image = getcustomasset('aerov4/assets/new/close.png')
		close.ImageColor3 = color.Light(uipallet.Text, 0.2)
		close.ImageTransparency = 0.5
		close.AutoButtonColor = false
		close.Parent = window
		close.MouseEnter:Connect(function()
			close.ImageTransparency = 0.3
			tween:Tween(close, TweenInfo.new(0.2), {
				BackgroundTransparency = 0.6
			})
		end)
		close.MouseLeave:Connect(function()
			close.ImageTransparency = 0.5
			tween:Tween(close, TweenInfo.new(0.2), {
				BackgroundTransparency = 1
			})
		end)
		close.MouseButton1Click:Connect(function()
			window.Visible = false
			vape.gui.ScaledGui.ClickGui.Visible = true
		end)
		local closecorner = Instance.new('UICorner')
		closecorner.CornerRadius = UDim.new(1, 0)
		closecorner.Parent = close
		local bigslot = Instance.new('Frame')
		bigslot.Size = UDim2.fromOffset(110, 111)
		bigslot.Position = UDim2.fromOffset(11, 71)
		bigslot.BackgroundColor3 = color.Dark(uipallet.Main, 0.02)
		bigslot.Parent = window
		local bigslotcorner = Instance.new('UICorner')
		bigslotcorner.CornerRadius = UDim.new(0, 4)
		bigslotcorner.Parent = bigslot
		local bigslotstroke = Instance.new('UIStroke')
		bigslotstroke.Color = color.Light(uipallet.Main, 0.034)
		bigslotstroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
		bigslotstroke.Parent = bigslot
		local slotnum = Instance.new('TextLabel')
		slotnum.Size = UDim2.fromOffset(80, 20)
		slotnum.Position = UDim2.fromOffset(25, 200)
		slotnum.BackgroundTransparency = 1
		slotnum.Name = 'SlotNum'
		slotnum.Text = 'SLOT 1'
		slotnum.TextColor3 = color.Dark(uipallet.Text, 0.1)
		slotnum.TextSize = 12
		slotnum.FontFace = uipallet.Font
		slotnum.Parent = window
		for i = 1, 9 do
			local slotbkg = Instance.new('TextButton')
			slotbkg.Name = 'Slot'..i
			slotbkg.Size = UDim2.fromOffset(51, 52)
			slotbkg.Position = UDim2.fromOffset(89 + (i * 55), 382)
			slotbkg.BackgroundColor3 = color.Dark(uipallet.Main, 0.02)
			slotbkg.Text = ''
			slotbkg.AutoButtonColor = false
			slotbkg.Parent = window
			local slotimage = Instance.new('ImageLabel')
			slotimage.Size = UDim2.fromOffset(32, 32)
			slotimage.Position = UDim2.new(0.5, -16, 0.5, -16)
			slotimage.BackgroundTransparency = 1
			slotimage.Image = ''
			slotimage.Parent = slotbkg
			local slotcorner = Instance.new('UICorner')
			slotcorner.CornerRadius = UDim.new(0, 4)
			slotcorner.Parent = slotbkg
			local slotstroke = Instance.new('UIStroke')
			slotstroke.Color = color.Light(uipallet.Main, 0.04)
			slotstroke.Thickness = 2
			slotstroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
			slotstroke.Enabled = i == selectedslot
			slotstroke.Parent = slotbkg
			slotbkg.MouseEnter:Connect(function()
				slotbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.034)
			end)
			slotbkg.MouseLeave:Connect(function()
				slotbkg.BackgroundColor3 = color.Dark(uipallet.Main, 0.02)
			end)
			slotbkg.MouseButton1Click:Connect(function()
				window['Slot'..selectedslot].UIStroke.Enabled = false
				selectedslot = i
				slotstroke.Enabled = true
				slotnum.Text = 'SLOT '..selectedslot
			end)
			slotbkg.MouseButton2Click:Connect(function()
				local obj = self.Hotbars[self.Selected]
				if obj then
					window['Slot'..i].ImageLabel.Image = ''
					obj.Hotbar[tostring(i)] = nil
					obj.Object['Slot'..i].Image = '	'
				end
			end)
		end
		local searchbkg = Instance.new('Frame')
		searchbkg.Size = UDim2.fromOffset(496, 31)
		searchbkg.Position = UDim2.fromOffset(142, 80)
		searchbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.034)
		searchbkg.Parent = window
		local search = Instance.new('TextBox')
		search.Size = UDim2.new(1, -10, 0, 31)
		search.Position = UDim2.fromOffset(10, 0)
		search.BackgroundTransparency = 1
		search.Text = ''
		search.PlaceholderText = ''
		search.TextXAlignment = Enum.TextXAlignment.Left
		search.TextColor3 = uipallet.Text
		search.TextSize = 12
		search.FontFace = uipallet.Font
		search.ClearTextOnFocus = false
		search.Parent = searchbkg
		local searchcorner = Instance.new('UICorner')
		searchcorner.CornerRadius = UDim.new(0, 4)
		searchcorner.Parent = searchbkg
		local searchicon = Instance.new('ImageLabel')
		searchicon.Size = UDim2.fromOffset(14, 14)
		searchicon.Position = UDim2.new(1, -26, 0, 8)
		searchicon.BackgroundTransparency = 1
		searchicon.Image = getcustomasset('aerov4/assets/new/search.png')
		searchicon.ImageColor3 = color.Light(uipallet.Main, 0.37)
		searchicon.Parent = searchbkg
		local children = Instance.new('ScrollingFrame')
		children.Name = 'Children'
		children.Size = UDim2.fromOffset(500, 240)
		children.Position = UDim2.fromOffset(144, 122)
		children.BackgroundTransparency = 1
		children.BorderSizePixel = 0
		children.ScrollBarThickness = 2
		children.ScrollBarImageTransparency = 0.75
		children.CanvasSize = UDim2.new()
		children.Parent = window
		local windowlist = Instance.new('UIGridLayout')
		windowlist.SortOrder = Enum.SortOrder.LayoutOrder
		windowlist.FillDirectionMaxCells = 9
		windowlist.CellSize = UDim2.fromOffset(51, 52)
		windowlist.CellPadding = UDim2.fromOffset(4, 3)
		windowlist.Parent = children
		windowlist:GetPropertyChangedSignal('AbsoluteContentSize'):Connect(function()
			if vape.ThreadFix then
				setthreadidentity(8)
			end
			children.CanvasSize = UDim2.fromOffset(0, windowlist.AbsoluteContentSize.Y / vape.guiscale.Scale)
		end)
		table.insert(vape.Windows, window)
	
		local function createitem(id, image)
			local slotbkg = Instance.new('TextButton')
			slotbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.02)
			slotbkg.Text = ''
			slotbkg.AutoButtonColor = false
			slotbkg.Parent = children
			local slotimage = Instance.new('ImageLabel')
			slotimage.Size = UDim2.fromOffset(32, 32)
			slotimage.Position = UDim2.new(0.5, -16, 0.5, -16)
			slotimage.BackgroundTransparency = 1
			slotimage.Image = image
			slotimage.Parent = slotbkg
			local slotcorner = Instance.new('UICorner')
			slotcorner.CornerRadius = UDim.new(0, 4)
			slotcorner.Parent = slotbkg
			slotbkg.MouseEnter:Connect(function()
				slotbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.04)
			end)
			slotbkg.MouseLeave:Connect(function()
				slotbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.02)
			end)
			slotbkg.MouseButton1Click:Connect(function()
				local obj = self.Hotbars[self.Selected]
				if obj then
					window['Slot'..selectedslot].ImageLabel.Image = image
					obj.Hotbar[tostring(selectedslot)] = id
					obj.Object['Slot'..selectedslot].Image = image
				end
			end)
		end
	
		local function indexSearch(text)
			for _, v in children:GetChildren() do
				if v:IsA('TextButton') then
					v:ClearAllChildren()
					v:Destroy()
				end
			end
	
			if text == '' then
				for _, v in {'diamond_sword', 'diamond_pickaxe', 'diamond_axe', 'shears', 'wood_bow', 'wool_white', 'fireball', 'apple', 'iron', 'gold', 'diamond', 'emerald'} do
					createitem(v, bedwars.ItemMeta[v].image)
				end
				return
			end
	
			for i, v in bedwars.ItemMeta do
				if text:lower() == i:lower():sub(1, text:len()) then
					if not v.image then continue end
					createitem(i, v.image)
				end
			end
		end
	
		search:GetPropertyChangedSignal('Text'):Connect(function()
			indexSearch(search.Text)
		end)
		indexSearch('')
	
		return window
	end
	
	vape.Components.HotbarList = function(optionsettings, children, api)
		if vape.ThreadFix then
			setthreadidentity(8)
		end
		local optionapi = {
			Type = 'HotbarList',
			Hotbars = {},
			Selected = 1
		}
		local hotbarlist = Instance.new('TextButton')
		hotbarlist.Name = 'HotbarList'
		hotbarlist.Size = UDim2.fromOffset(220, 40)
		hotbarlist.BackgroundColor3 = optionsettings.Darker and (children.BackgroundColor3 == color.Dark(uipallet.Main, 0.02) and color.Dark(uipallet.Main, 0.04) or color.Dark(uipallet.Main, 0.02)) or children.BackgroundColor3
		hotbarlist.Text = ''
		hotbarlist.BorderSizePixel = 0
		hotbarlist.AutoButtonColor = false
		hotbarlist.Parent = children
		local textbkg = Instance.new('Frame')
		textbkg.Name = 'BKG'
		textbkg.Size = UDim2.new(1, -20, 0, 31)
		textbkg.Position = UDim2.fromOffset(10, 4)
		textbkg.BackgroundColor3 = color.Light(uipallet.Main, 0.034)
		textbkg.Parent = hotbarlist
		local textbkgcorner = Instance.new('UICorner')
		textbkgcorner.CornerRadius = UDim.new(0, 4)
		textbkgcorner.Parent = textbkg
		local textbutton = Instance.new('TextButton')
		textbutton.Name = 'HotbarList'
		textbutton.Size = UDim2.new(1, -2, 1, -2)
		textbutton.Position = UDim2.fromOffset(1, 1)
		textbutton.BackgroundColor3 = uipallet.Main
		textbutton.Text = ''
		textbutton.AutoButtonColor = false
		textbutton.Parent = textbkg
		textbutton.MouseEnter:Connect(function()
			tween:Tween(textbkg, TweenInfo.new(0.2), {
				BackgroundColor3 = color.Light(uipallet.Main, 0.14)
			})
		end)
		textbutton.MouseLeave:Connect(function()
			tween:Tween(textbkg, TweenInfo.new(0.2), {
				BackgroundColor3 = color.Light(uipallet.Main, 0.034)
			})
		end)
		local textbuttoncorner = Instance.new('UICorner')
		textbuttoncorner.CornerRadius = UDim.new(0, 4)
		textbuttoncorner.Parent = textbutton
		local textbuttonicon = Instance.new('ImageLabel')
		textbuttonicon.Size = UDim2.fromOffset(12, 12)
		textbuttonicon.Position = UDim2.fromScale(0.5, 0.5)
		textbuttonicon.AnchorPoint = Vector2.new(0.5, 0.5)
		textbuttonicon.BackgroundTransparency = 1
		textbuttonicon.Image = getcustomasset('aerov4/assets/new/add.png')
		textbuttonicon.ImageColor3 = Color3.fromHSV(0.46, 0.96, 0.52)
		textbuttonicon.Parent = textbutton
		local childrenlist = Instance.new('Frame')
		childrenlist.Size = UDim2.new(1, 0, 1, -40)
		childrenlist.Position = UDim2.fromOffset(0, 40)
		childrenlist.BackgroundTransparency = 1
		childrenlist.Parent = hotbarlist
		local windowlist = Instance.new('UIListLayout')
		windowlist.SortOrder = Enum.SortOrder.LayoutOrder
		windowlist.HorizontalAlignment = Enum.HorizontalAlignment.Center
		windowlist.Padding = UDim.new(0, 3)
		windowlist.Parent = childrenlist
		windowlist:GetPropertyChangedSignal('AbsoluteContentSize'):Connect(function()
			if vape.ThreadFix then
				setthreadidentity(8)
			end
			hotbarlist.Size = UDim2.fromOffset(220, math.min(43 + windowlist.AbsoluteContentSize.Y / vape.guiscale.Scale, 603))
		end)
		optionapi.Window = CreateWindow(optionapi)
	
		function optionapi:Save(savetab)
			local hotbars = {}
			for _, v in self.Hotbars do
				table.insert(hotbars, v.Hotbar)
			end
			savetab.HotbarList = {
				Selected = self.Selected,
				Hotbars = hotbars
			}
		end
	
		function optionapi:Load(savetab)
			for _, v in self.Hotbars do
				v.Object:ClearAllChildren()
				v.Object:Destroy()
				table.clear(v.Hotbar)
			end
			table.clear(self.Hotbars)
			for _, v in savetab.Hotbars do
				self:AddHotbar(v)
			end
			self.Selected = savetab.Selected or 1
		end
	
		textbutton.MouseButton1Click:Connect(function()
		optionapi:AddHotbar()
	end)
	function optionapi:AddHotbar(data)
		local hotbardata = {Hotbar = data or {}}
			table.insert(self.Hotbars, hotbardata)
			local hotbar = Instance.new('TextButton')
			hotbar.Size = UDim2.fromOffset(200, 27)
			hotbar.BackgroundColor3 = table.find(self.Hotbars, hotbardata) == self.Selected and color.Light(uipallet.Main, 0.034) or uipallet.Main
			hotbar.Text = ''
			hotbar.AutoButtonColor = false
			hotbar.Parent = childrenlist
			hotbardata.Object = hotbar
			local hotbarcorner = Instance.new('UICorner')
			hotbarcorner.CornerRadius = UDim.new(0, 4)
			hotbarcorner.Parent = hotbar
			for i = 1, 9 do
				local slot = Instance.new('ImageLabel')
				slot.Name = 'Slot'..i
				slot.Size = UDim2.fromOffset(17, 18)
				slot.Position = UDim2.fromOffset(-7 + (i * 18), 5)
				slot.BackgroundColor3 = color.Dark(uipallet.Main, 0.02)
				slot.Image = hotbardata.Hotbar[tostring(i)] and bedwars.getIcon({itemType = hotbardata.Hotbar[tostring(i)]}, true) or ''
				slot.BorderSizePixel = 0
				slot.Parent = hotbar
			end
			hotbar.MouseButton1Click:Connect(function()
				local ind = table.find(optionapi.Hotbars, hotbardata)
				if ind == optionapi.Selected then
					vape.gui.ScaledGui.ClickGui.Visible = false
					optionapi.Window.Visible = true
					for i = 1, 9 do
						optionapi.Window['Slot'..i].ImageLabel.Image = hotbardata.Hotbar[tostring(i)] and bedwars.getIcon({itemType = hotbardata.Hotbar[tostring(i)]}, true) or ''
					end
				else
					if optionapi.Hotbars[optionapi.Selected] then
						optionapi.Hotbars[optionapi.Selected].Object.BackgroundColor3 = uipallet.Main
					end
					hotbar.BackgroundColor3 = color.Light(uipallet.Main, 0.034)
					optionapi.Selected = ind
				end
			end)
			local close = Instance.new('ImageButton')
			close.Name = 'Close'
			close.Size = UDim2.fromOffset(16, 16)
			close.Position = UDim2.new(1, -23, 0, 6)
			close.BackgroundColor3 = Color3.new(1, 1, 1)
			close.BackgroundTransparency = 1
			close.Image = getcustomasset('aerov4/assets/new/closemini.png')
			close.ImageColor3 = color.Light(uipallet.Text, 0.2)
			close.ImageTransparency = 0.5
			close.AutoButtonColor = false
			close.Parent = hotbar
			local closecorner = Instance.new('UICorner')
			closecorner.CornerRadius = UDim.new(1, 0)
			closecorner.Parent = close
			close.MouseEnter:Connect(function()
				close.ImageTransparency = 0.3
				tween:Tween(close, TweenInfo.new(0.2), {
					BackgroundTransparency = 0.6
				})
			end)
			close.MouseLeave:Connect(function()
				close.ImageTransparency = 0.5
				tween:Tween(close, TweenInfo.new(0.2), {
					BackgroundTransparency = 1
				})
			end)
			close.MouseButton1Click:Connect(function()
				local ind = table.find(self.Hotbars, hotbardata)
				local obj = self.Hotbars[self.Selected]
				local obj2 = self.Hotbars[ind]
				if obj and obj2 then
					obj2.Object:ClearAllChildren()
					obj2.Object:Destroy()
					table.remove(self.Hotbars, ind)
					ind = table.find(self.Hotbars, obj)
					self.Selected = table.find(self.Hotbars, obj) or 1
				end
			end)
		end
	
		api.Options.HotbarList = optionapi
	
		return optionapi
	end
	
	local function getBlock()
		local clone = table.clone(store.inventory.inventory.items)
		table.sort(clone, function(a, b)
			return a.amount < b.amount
		end)
	
		for _, item in clone do
			local block = bedwars.ItemMeta[item.itemType].block
			if block and not block.seeThrough then
				return item
			end
		end
	end
	
	local function getCustomItem(v)
		if v == 'diamond_sword' then
			local sword = store.tools.sword
			v = sword and sword.itemType or 'wood_sword'
		elseif v == 'diamond_pickaxe' then
			local pickaxe = store.tools.stone
			v = pickaxe and pickaxe.itemType or 'wood_pickaxe'
		elseif v == 'diamond_axe' then
			local axe = store.tools.wood
			v = axe and axe.itemType or 'wood_axe'
		elseif v == 'wood_bow' then
			local bow = getBow()
			v = bow and bow.itemType or 'wood_bow'
		elseif v == 'wool_white' then
			local block = getBlock()
			v = block and block.itemType or 'wool_white'
		end
	
		return v
	end
	
	local function findItemInTable(tab, item)
		for slot, v in tab do
			if item.itemType == getCustomItem(v) then
				return tonumber(slot)
			end
		end
	end
	
	local function findInHotbar(item)
		for i, v in store.inventory.hotbar do
			if v.item and v.item.itemType == item.itemType then
				return i - 1, v.item
			end
		end
	end
	
	local function findInInventory(item)
		for _, v in store.inventory.inventory.items do
			if v.itemType == item.itemType then
				return v
			end
		end
	end
	
	local function dispatch(...)
		bedwars.Store:dispatch(...)
		vapeEvents.InventoryChanged.Event:Wait()
	end
	
	local function sortCallback()
		if Active then return end
		Active = true
		local items = (List.Hotbars[List.Selected] and List.Hotbars[List.Selected].Hotbar or {})
	
		for _, v in store.inventory.inventory.items do
			local slot = findItemInTable(items, v)
			if slot then
				local olditem = store.inventory.hotbar[slot]
				if olditem.item and olditem.item.itemType == v.itemType then continue end
				if olditem.item then
					dispatch({
						type = 'InventoryRemoveFromHotbar',
						slot = slot - 1
					})
				end
	
				local newslot = findInHotbar(v)
				if newslot then
					dispatch({
						type = 'InventoryRemoveFromHotbar',
						slot = newslot
					})
					if olditem.item then
						dispatch({
							type = 'InventoryAddToHotbar',
							item = findInInventory(olditem.item),
							slot = newslot
						})
					end
				end
	
				dispatch({
					type = 'InventoryAddToHotbar',
					item = findInInventory(v),
					slot = slot - 1
				})
			elseif Clear.Enabled then
				local newslot = findInHotbar(v)
				if newslot then
				   	dispatch({
						type = 'InventoryRemoveFromHotbar',
						slot = newslot
					})
				end
			end
		end
	
		Active = false
	end
	
	AutoHotbar = vape.Categories.Inventory:CreateModule({
		Name = 'AutoHotbar',
		Function = function(callback)
			if callback then
				task.spawn(sortCallback)
				if Mode.Value == 'On Key' then
					AutoHotbar:Toggle()
					return
				end
	
				AutoHotbar:Clean(vapeEvents.InventoryAmountChanged.Event:Connect(sortCallback))
			end
		end,
		Tooltip = 'arranges your hotbar based off what u want'
	})
	Mode = AutoHotbar:CreateDropdown({
		Name = 'Activation',
		List = {'Toggle', 'On Key'},
		Function = function()
			if AutoHotbar.Enabled then
				AutoHotbar:Toggle()
				AutoHotbar:Toggle()
			end
		end
	})
	Clear = AutoHotbar:CreateToggle({Name = 'Clear Hotbar'})
	List = AutoHotbar:CreateHotbarList({})
end)

--[[
	Minigames Modules
]]

run(function()
    local BedPlates
    local Background
    local TeamColor
    local Color = {}
    local Reference = {}
    local BlockCache = {}
	local LayerCounter
	local LayerColor
	local UIStyle
	local PositionDropdown
	local IgnoreTeam
    local Folder = Instance.new('Folder')
    Folder.Parent = vape.gui

    local compactUI = Instance.new('ScreenGui')
    compactUI.Name = 'BedPlatesCompactUI'
    compactUI.Parent = vape.gui
    compactUI.Enabled = false
    compactUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    compactUI.DisplayOrder = 10
    compactUI.ResetOnSpawn = false
    local compactMainFrame = Instance.new('Frame')
    compactMainFrame.Name = 'MainFrame'
    compactMainFrame.Parent = compactUI
    compactMainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    compactMainFrame.BackgroundTransparency = 0.3
    compactMainFrame.BorderSizePixel = 0
    compactMainFrame.Position = UDim2.new(1, -8, 1, -8)
    compactMainFrame.Size = UDim2.new(0, 150, 0, 0)
    compactMainFrame.AutomaticSize = Enum.AutomaticSize.Y
    compactMainFrame.AnchorPoint = Vector2.new(1, 1)
    local compactCorner = Instance.new('UICorner')
    compactCorner.CornerRadius = UDim.new(0, 8)
    compactCorner.Parent = compactMainFrame
    local compactListLayout = Instance.new('UIListLayout')
    compactListLayout.Padding = UDim.new(0, 4)
    compactListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    compactListLayout.Parent = compactMainFrame
    local compactPadding = Instance.new('UIPadding')
    compactPadding.PaddingTop = UDim.new(0, 6)
    compactPadding.PaddingBottom = UDim.new(0, 6)
    compactPadding.PaddingLeft = UDim.new(0, 6)
    compactPadding.PaddingRight = UDim.new(0, 6)
    compactPadding.Parent = compactMainFrame
    
	local teamColors = getgenv().aeroTeamColors
    
    local function getBedTeamColor(bed)
        local teamId = bed:GetAttribute('TeamID') or bed:GetAttribute('Team') or bed:GetAttribute('TeamId')
        teamId = tonumber(teamId)
        if teamId and teamColors[teamId] then
            return teamColors[teamId]
        end
        return {name = 'Enemy', color = Color3.new(1, 1, 1)}
    end
    
    local function isMyBed(bed)
        local myTeam = lplr.Character and (lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId'))
        local bedTeam = bed:GetAttribute('TeamID') or bed:GetAttribute('Team') or bed:GetAttribute('TeamId')
        if not myTeam or not bedTeam then return false end
        return tonumber(myTeam) == tonumber(bedTeam)
    end
    
	local function updateLayerTextColor()
		for _, ref in pairs(Reference) do
			for _, img in ref.billboard.Frame:GetChildren() do
				if img:IsA('ImageLabel') then
					local txt = img:FindFirstChild('Amount')
					if txt then
						txt.TextColor3 = LayerColor and Color3.fromHSV(LayerColor.Hue, LayerColor.Sat, LayerColor.Value) or Color3.fromRGB(250,250,250)
					end
				end
			end
		end
	end

    local function scanSide(self, start, tab, dirsHit)
		local checkDirs = {
			Vector3.new(3,0,0), Vector3.new(-3,0,0),
			Vector3.new(0,0,3), Vector3.new(0,0,-3),
			Vector3.new(3,0,3), Vector3.new(3,0,-3),
			Vector3.new(-3,0,3), Vector3.new(-3,0,-3),
			Vector3.new(0,3,0),
		}
		for _, side in ipairs(checkDirs) do
			for i = 1, 15 do
				local block = getPlacedBlock(start + (side * i))
				if not block or block == self or block.Name == 'bed' then break end
				if not block:GetAttribute('NoBreak') then
					tab[block.Name] = math.max(tab[block.Name] or 0, i)
					dirsHit[block.Name] = dirsHit[block.Name] or {}
					dirsHit[block.Name][tostring(side)] = true
				end
			end
		end
    end

	local function getBlockHealth(blck)
		local meta = bedwars.ItemMeta[blck]
		if not meta then  return 0 end
		local blockmeta = meta.block
		if not blockmeta then  return 0 end
		return blockmeta.health or 0
	end
    
    local BedTotals = {}

    local function getBedLayers(bed)
		local start = bedwars.BlockController:getBlockPosition(bed.Position) * 3
		local layers = {}
		local dirsHit = {}
		local founded = {}
		scanSide(bed, start, layers, dirsHit)
		scanSide(bed, start + Vector3.new(0,0,3), layers, dirsHit)
		for blocks, amount in layers do 
			local coverage = 0
			for _ in pairs(dirsHit[blocks] or {}) do
				coverage = coverage + 1
			end
			if coverage >= 5 then
				table.insert(founded, {blocks, amount})
			end
		end
		table.sort(founded, function(a,b)
			local healthA, healthB = getBlockHealth(a[1]), getBlockHealth(b[1])			
			return healthA == healthB and a[1] < b[1] or healthA > healthB
		end)
		return founded
    end

    local function renderLayers(frame, founded)
		for _, obj in frame:GetChildren() do
			if obj and (obj:IsA("ImageLabel") and obj.Name ~= 'Blur') then
				obj:Destroy()
			end
		end
		for _, data in founded do
			local block, amt = data[1], data[2]
			local image = Instance.new('ImageLabel')
			image.Size = UDim2.fromOffset(32,32)
			image.BackgroundTransparency = 1
			image.Image = bedwars.getIcon({itemType=block}, true)
			image.Parent = frame
			if amt >= 1 and (not LayerCounter or LayerCounter.Enabled) then
				local txt = Instance.new('TextLabel')
				txt.Name = 'Amount'
				txt.Size = UDim2.fromScale(1,1)
				txt.BackgroundTransparency = 1
				local newamt = amt
				txt.Text = tostring(newamt)
				txt.TextColor3 = LayerColor and Color3.fromHSV(LayerColor.Hue, LayerColor.Sat, LayerColor.Value) or Color3.fromRGB(250,250,250)
				txt.TextSize = 24
				txt.TextStrokeTransparency = 0.3
				txt.Font = Enum.Font.Arial
				txt.Parent = image
			end
		end
    end

    local function refreshAdornee(v)
		local bed = v.Adornee
		local founded = getBedLayers(bed)
		local total = 0
		for _, data in founded do total = math.max(total, data[2]) end
		BedTotals[bed] = total
		local mine = isMyBed(bed)
		v.Enabled = #founded > 0 and UIStyle.Value == 'Original' and not mine
		renderLayers(v.Frame, founded)
		if Reference[bed] and Reference[bed].compactRow then
			renderLayers(Reference[bed].compactRow.iconsFrame, founded)
			Reference[bed].compactRow.countLabel.Text = tostring(total)
			Reference[bed].compactRow.frame.Visible = #founded > 0 and not (mine and IgnoreTeam and IgnoreTeam.Enabled)
		end
    end
    
    local function Added(v)
        if Reference[v] then return end
        local _bpUserId = v:GetAttribute('PlacedByUserId')
        if _bpUserId then
            local _bpOk, _bpOwner = pcall(function() return playersService:GetPlayerByUserId(_bpUserId) end)
        end
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = Folder
        billboard.Name = 'bed'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(36, TeamColor.Enabled and 50 or 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = v
        billboard.Enabled = UIStyle.Value == 'Original'
        
        local blur = addBlur(billboard)
        blur.Visible = Background.Enabled
        
        local frame = Instance.new('Frame')
        frame.Size = TeamColor.Enabled and UDim2.new(1, 0, 1, -16) or UDim2.fromScale(1, 1)
        frame.Position = TeamColor.Enabled and UDim2.new(0, 0, 0, 16) or UDim2.new(0, 0, 0, 0)
        frame.BackgroundColor3 = TeamColor.Enabled and getBedTeamColor(v).color or Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
        frame.BackgroundTransparency = 1 - (Background.Enabled and (TeamColor.Enabled and 0.5 or Color.Opacity) or 0)
        frame.Parent = billboard

        local teamLabel = Instance.new('TextLabel')
        teamLabel.Name = 'TeamLabel'
        teamLabel.Size = UDim2.new(1, 0, 0, 14)
        teamLabel.Position = UDim2.new(0, 0, 0, 0)
        teamLabel.BackgroundTransparency = 1
        teamLabel.Text = getBedTeamColor(v).name
        teamLabel.TextColor3 = getBedTeamColor(v).color
        teamLabel.TextSize = 13
        teamLabel.Font = Enum.Font.GothamBold
        teamLabel.TextStrokeTransparency = 0.4
        teamLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
        teamLabel.Visible = TeamColor.Enabled
        teamLabel.Parent = billboard
        
        local layout = Instance.new('UIListLayout')
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.VerticalAlignment = Enum.VerticalAlignment.Center
        layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        layout:GetPropertyChangedSignal('AbsoluteContentSize'):Connect(function()
            billboard.Size = UDim2.fromOffset(math.max(layout.AbsoluteContentSize.X + 4, 36), TeamColor.Enabled and 50 or 36)
        end)
        layout.Parent = frame
        
        local corner = Instance.new('UICorner')
        corner.CornerRadius = UDim.new(0, 4)
        corner.Parent = frame

        local rowFrame = Instance.new('Frame')
        rowFrame.BackgroundTransparency = 1
        rowFrame.Size = UDim2.new(1, 0, 0, 50)
        rowFrame.LayoutOrder = #compactMainFrame:GetChildren()
        rowFrame.Visible = false
        rowFrame.Parent = compactMainFrame

        local rowTeamLabel = Instance.new('TextLabel')
        rowTeamLabel.Size = UDim2.new(1, 0, 0, 14)
        rowTeamLabel.BackgroundTransparency = 1
        rowTeamLabel.Text = getBedTeamColor(v).name
        rowTeamLabel.TextColor3 = getBedTeamColor(v).color
        rowTeamLabel.TextSize = 13
        rowTeamLabel.Font = Enum.Font.GothamBold
        rowTeamLabel.TextStrokeTransparency = 0.4
        rowTeamLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
        rowTeamLabel.Parent = rowFrame

        local rowBG = Instance.new('Frame')
        rowBG.Position = UDim2.new(0, 0, 0, 16)
        rowBG.Size = UDim2.new(1, 0, 0, 34)
        rowBG.BackgroundColor3 = Color3.new(0, 0, 0)
        rowBG.BackgroundTransparency = 0.3
        rowBG.Parent = rowFrame

        local rowCorner = Instance.new('UICorner')
        rowCorner.CornerRadius = UDim.new(0, 4)
        rowCorner.Parent = rowBG

        local rowIcons = Instance.new('Frame')
        rowIcons.BackgroundTransparency = 1
        rowIcons.Size = UDim2.new(1, -30, 1, 0)
        rowIcons.Parent = rowBG

        local rowIconLayout = Instance.new('UIListLayout')
        rowIconLayout.FillDirection = Enum.FillDirection.Horizontal
        rowIconLayout.Padding = UDim.new(0, 2)
        rowIconLayout.VerticalAlignment = Enum.VerticalAlignment.Center
        rowIconLayout.Parent = rowIcons

        local rowCount = Instance.new('TextLabel')
        rowCount.Size = UDim2.new(0, 28, 1, 0)
        rowCount.Position = UDim2.new(1, -28, 0, 0)
        rowCount.BackgroundTransparency = 1
        rowCount.Text = "0"
        rowCount.TextColor3 = Color3.fromRGB(255, 255, 255)
        rowCount.TextSize = 14
        rowCount.Font = Enum.Font.GothamBold
        rowCount.Parent = rowBG

        Reference[v] = {
            billboard = billboard,
            compactRow = {
                frame = rowFrame,
                iconsFrame = rowIcons,
                countLabel = rowCount
            }
        }
        BlockCache[v] = ""
        refreshAdornee(billboard)
    end
    
    local _refreshNearPending = false
    local function refreshNear(data)
        if _refreshNearPending then return end
        _refreshNearPending = true
        task.defer(function()
            _refreshNearPending = false
            local blockPos = data.blockRef.blockPosition * 3
            local maxDistanceSq = 30 * 30
            for bed, ref in pairs(Reference) do
                if bed.Parent then
                    local offset = blockPos - bed.Position
                    local distanceSq = offset.X * offset.X + offset.Y * offset.Y + offset.Z * offset.Z
                    if distanceSq <= maxDistanceSq then
                        refreshAdornee(ref.billboard)
                    end
                end
            end
        end)
    end

    local currentCorner = 'Bottom Right'

    local function updateCompactPosition()
        local genUI = vape.gui:FindFirstChild('GeneratorCompactUI')
        local offset = 8
        if genUI and genUI.Enabled and currentCorner == 'Bottom Right' then
            offset = offset + 128
        end
        local invHost = (gethui and gethui()) or game:GetService('CoreGui')
        if invHost:FindFirstChild('InventoryESP') and (currentCorner == 'Bottom Right' or currentCorner == 'Top Right') then
            offset = offset + 200
        end
        if currentCorner == 'Top Right' then
            compactMainFrame.AnchorPoint = Vector2.new(1, 0)
            compactMainFrame.Position = UDim2.new(1, -offset, 0, 8)
        elseif currentCorner == 'Top Left' then
            compactMainFrame.AnchorPoint = Vector2.new(0, 0)
            compactMainFrame.Position = UDim2.new(0, 8, 0, 8)
        elseif currentCorner == 'Bottom Left' then
            compactMainFrame.AnchorPoint = Vector2.new(0, 1)
            compactMainFrame.Position = UDim2.new(0, 8, 1, -8)
        else
            compactMainFrame.AnchorPoint = Vector2.new(1, 1)
            compactMainFrame.Position = UDim2.new(1, -offset, 1, -8)
        end
    end
    
    BedPlates = vape.Categories.Minigames:CreateModule({
        Name = 'BedPlates',
        Function = function(callback)
            if callback then
                table.clear(BlockCache)
                compactUI.Enabled = UIStyle.Value == 'Compact'
                
                local tagged = collectionService:GetTagged('bed')
                for _, v in ipairs(tagged) do 
                    Added(v)
                end
                
                BedPlates:Clean(vapeEvents.PlaceBlockEvent.Event:Connect(refreshNear))
                BedPlates:Clean(vapeEvents.BreakBlockEvent.Event:Connect(refreshNear))
                BedPlates:Clean(collectionService:GetInstanceAddedSignal('bed'):Connect(Added))

                local worldFolder = getWorldFolder()
                local blocksFolder = worldFolder and worldFolder:FindFirstChild('Blocks')
                if blocksFolder then
                    local function onBlockChange(obj)
                        if not (obj:IsA('BasePart')) then return end
                        refreshNear({blockRef = {blockPosition = obj.Position / 3}})
                    end
                    BedPlates:Clean(blocksFolder.ChildAdded:Connect(onBlockChange))
                    BedPlates:Clean(blocksFolder.ChildRemoved:Connect(onBlockChange))
                end
                BedPlates:Clean(collectionService:GetInstanceRemovedSignal('bed'):Connect(function(v)
                    if Reference[v] then
                        Reference[v].billboard:Destroy()
                        Reference[v].compactRow.frame:Destroy()
                        Reference[v] = nil
                        BlockCache[v] = nil
                    end
                end))
                BedPlates:Clean(runService.Heartbeat:Connect(updateCompactPosition))
                BedPlates:Clean(runService.Heartbeat:Connect(function()
                    if not TeamColor.Enabled and Background.Enabled then
                        local col = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
                        for bed, ref in pairs(Reference) do
                            ref.billboard.Frame.BackgroundColor3 = col
                        end
                    end
                    if LayerCounter.Enabled then
                        updateLayerTextColor()
                    end
                end))
            else
                for _, v in pairs(Reference) do
                    v.billboard:Destroy()
                    v.compactRow.frame:Destroy()
                end
                table.clear(Reference)
                table.clear(BlockCache)
                compactUI.Enabled = false
            end
        end,
        Tooltip = 'shows enemys bed defence'
    })

    UIStyle = BedPlates:CreateDropdown({
        Name = 'UI Style',
        List = {'Original', 'Compact'},
        Default = 'Original',
        Function = function(val)
            local isOriginal = val == 'Original'
            compactUI.Enabled = BedPlates.Enabled and not isOriginal
            for bed, ref in pairs(Reference) do
                ref.billboard.Enabled = isOriginal
                refreshAdornee(ref.billboard)
            end
            if PositionDropdown.Object then
                PositionDropdown.Object.Visible = not isOriginal
            end
            if IgnoreTeam and IgnoreTeam.Object then
                IgnoreTeam.Object.Visible = not isOriginal
            end
        end,
        Tooltip = 'pick between the floating world esp or a corner panel'
    })

    PositionDropdown = BedPlates:CreateDropdown({
        Name = 'Position',
        List = {'Top Right', 'Top Left', 'Bottom Left', 'Bottom Right'},
        Default = 'Bottom Right',
        Function = function(val)
            currentCorner = val
            updateCompactPosition()
        end,
        Tooltip = 'pick w corner u want the panel chillin in'
    })

    task.defer(function()
        if PositionDropdown and PositionDropdown.Object then
            PositionDropdown.Object.Visible = (UIStyle.Value == 'Compact')
        end
        if IgnoreTeam and IgnoreTeam.Object then
            IgnoreTeam.Object.Visible = (UIStyle.Value == 'Compact')
        end
    end)
    
    IgnoreTeam = BedPlates:CreateToggle({
        Name = 'Ignore Team',
        Default = false,
        Function = function(callback)
            for bed, ref in pairs(Reference) do
                refreshAdornee(ref.billboard)
            end
        end,
        Tooltip = 'wont show ur own team bed on the compact panel'
    })
    
    Background = BedPlates:CreateToggle({
        Name = 'Background',
        Function = function(callback)
            if Color.Object then 
                Color.Object.Visible = callback and not TeamColor.Enabled
            end
            for _, v in pairs(Reference) do
                v.Frame.BackgroundTransparency = 1 - (callback and (TeamColor.Enabled and 0.5 or Color.Opacity) or 0)
                local blur = v:FindFirstChild('Blur')
                if blur then
                    blur.Visible = callback
                end
            end
        end,
        Default = true
    })

    Color = BedPlates:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for bed, v in pairs(Reference) do
                if not TeamColor.Enabled then
                    v.Frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                end
                if Background.Enabled and not TeamColor.Enabled then
                    v.Frame.BackgroundTransparency = 1 - opacity
                end
            end
        end,
        Visible = false,
        Darker = true
    })
    
    TeamColor = BedPlates:CreateToggle({
        Name = 'Team Color',
        Default = true,
        Function = function(callback)
            if Color.Object then
                Color.Object.Visible = Background.Enabled and not callback
            end
            for bed, billboard in pairs(Reference) do
                billboard.Frame.BackgroundColor3 = callback and getBedTeamColor(bed).color or Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
                billboard.Frame.BackgroundTransparency = 1 - (Background.Enabled and (callback and 0.5 or Color.Opacity) or 0)
            end
        end
    })

	LayerCounter = BedPlates:CreateToggle({
		Name = 'Layer Counter',
		Function = function(callback)
			if LayerColor and LayerColor.Object then
				LayerColor.Object.Visible = callback
			end
			for _, billboard in pairs(Reference) do
				refreshAdornee(billboard)
			end
		end,
		Default = true
	})
	LayerColor = BedPlates:CreateColorSlider({
		Name = 'Counter Text Color',
		DefaultSat = 1,
		DefaultValue = 1,
		Function = function()
			updateLayerTextColor()
		end,
		Visible = LayerCounter.Enabled
	})	
end)

run(function()
	local AutoHonor
	local Delay
	local honoredusers = {}
	local maxhonors = 2
	
	local function getTeammates()
		local teammates = {}
		local nonteammates = {}
		local myTeam = lplr.Team
		
		for i, plr in playersService:GetPlayers() do
			if plr ~= lplr then
				if plr.Team == myTeam then
					table.insert(teammates, plr)
				else
					table.insert(nonteammates, plr)
				end
			end
		end
		return teammates, nonteammates
	end
	
	local function honorPlayers()
		if #honoredusers >= maxhonors then return end
		
		local teammates, nonteammates = getTeammates()
		
		if #teammates > 0 and #honoredusers < maxhonors then
			local randomTeammate = teammates[math.random(1, #teammates)]
			if not honoredusers[randomTeammate.UserId] then
				task.wait(Delay.Value)
				bedwars.HonorController:honorPlayer(randomTeammate.UserId)
				honoredusers[randomTeammate.UserId] = true
			end
		end
		
		if #nonteammates > 0 and #honoredusers < maxhonors then
			local randomEnemy = nonteammates[math.random(1, #nonteammates)]
			if not honoredusers[randomEnemy.UserId] then
				task.wait(Delay.Value)
				bedwars.HonorController:honorPlayer(randomEnemy.UserId)
				honoredusers[randomEnemy.UserId] = true
			end
		end
	end
	
	AutoHonor = vape.Categories.Minigames:CreateModule({
		Name = "AutoHonor",
		Function = function(callback)
			if callback then
				AutoHonor:Clean(vapeEvents.EntityDeathEvent.Event:Connect(function(deathTable)
					if deathTable.finalKill and deathTable.entityInstance == lplr.Character and isEveryoneDead() and store.matchState ~= 2 then
						honorPlayers()
					end
				end))
				AutoHonor:Clean(vapeEvents.MatchEndEvent.Event:Connect(function(...)
					honorPlayers()
				end))
			else
				table.clear(honoredusers)
			end
		end
	})
	Delay = AutoHonor:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Decimal = 100,
		Default = 0.05
	})
end)

run(function()
	local Breaker
	local TargetMode
	local Mode
	local Range
	local BreakSpeed
	local UpdateRate
	local Bed
	local BedCheck
	local LuckyBlock
	local IronOre
	local Tesla
	local Hive
	local Pinata
	local Crops
	local Effect
	local CustomHealth = {}
	local Animation
	local SelfBreak
	local LimitItem
	local AutoTool
	local MouseDown
	local Snow
	local YetiBreaker
	local RagnarBreaker
	local ShowPath
	local BlockHighlight
	local BreakerHighlightColor
	local BreakerAngle
	local BreakerDebug
	local _dbgLast = {}
	local function dbg(key, ...)
		if not (BreakerDebug and BreakerDebug.Enabled) then return end
		if tick() - (_dbgLast[key] or 0) < 0.5 then return end
		_dbgLast[key] = tick()
		print('[BRK]', key, ...)
	end

	local blockHighlightInstance
	local parts = {}
	local lastYetiUse = 0
	local cachedTeammates = {}
	local cachedTeammatesTime = 0
	local breakabilityCache = {}
	local BREAK_CACHE_TTL = 0.5

	local legitRoute = {}
	local legitTarget = nil
	local legitAnchor = nil
	local legitPlanTime = 0
	local legitLastHit = 0

	local _hbMounted = nil
	local _hbPart = nil
	local _hbProgressRef = nil
	local _hbBlock = nil

	local function screenPoint()
		if inputService.TouchEnabled then
			return gameCamera.ViewportSize / 2
		end
		return inputService:GetMouseLocation()
	end

	local function frontPoint()
		local root = entitylib.character and entitylib.character.RootPart
		if not root then return nil end
		local look = gameCamera.CFrame.LookVector * Vector3.new(1, 0, 1)
		if look.Magnitude < 0.01 then return root.Position end
		return root.Position + look.Unit * 6
	end

	local function cleanupHealthbar()
		if _hbMounted then
			pcall(bedwars.Roact.unmount, _hbMounted)
			_hbMounted = nil
		end
		if _hbPart then
			pcall(function() _hbPart:Destroy() end)
			_hbPart = nil
		end
		_hbProgressRef = nil
		_hbBlock = nil
		local stray = workspace:FindFirstChild('AeroBreakerHB')
		while stray do
			pcall(function() stray:Destroy() end)
			stray = workspace:FindFirstChild('AeroBreakerHB')
		end
	end

	local function customHealthbar(self, blockRef, health, maxHealth, changeHealth, block)
		if not Breaker or not Breaker.Enabled then return end
		if not block or not block.Parent then
			cleanupHealthbar()
			return
		end
		if block:GetAttribute('NoHealthbar') then return end
		if health <= 0 then
			cleanupHealthbar()
			return
		end

		if _hbBlock ~= block then
			cleanupHealthbar()
			_hbBlock = block
			local create = bedwars.Roact.createElement
			local percent = math.clamp(health / maxHealth, 0, 1)
			_hbProgressRef = bedwars.Roact.createRef()
			local part = Instance.new('Part')
			part.Name = 'AeroBreakerHB'
			part.Size = Vector3.one
			part.CFrame = CFrame.new(bedwars.BlockController:getWorldPosition(blockRef.blockPosition))
			part.Transparency = 1
			part.Anchored = true
			part.CanCollide = false
			part.Parent = workspace
			_hbPart = part
			bedwars.QueryUtil:setQueryIgnored(part, true)

			_hbMounted = bedwars.Roact.mount(create('BillboardGui', {
				Size = UDim2.fromOffset(249, 102),
				StudsOffset = Vector3.new(0, 2.5, 0),
				Adornee = part,
				MaxDistance = 40,
				AlwaysOnTop = true
			}, {
				create('Frame', {
					Size = UDim2.fromOffset(160, 50),
					Position = UDim2.fromOffset(44, 32),
					BackgroundColor3 = Color3.new(),
					BackgroundTransparency = 0.5
				}, {
					create('UICorner', {CornerRadius = UDim.new(0, 5)}),
					create('ImageLabel', {
						Size = UDim2.new(1, 89, 1, 52),
						Position = UDim2.fromOffset(-48, -31),
						BackgroundTransparency = 1,
						Image = getcustomasset('aerov4/assets/new/blur.png'),
						ScaleType = Enum.ScaleType.Slice,
						SliceCenter = Rect.new(52, 31, 261, 502)
					}),
					create('TextLabel', {
						Size = UDim2.fromOffset(145, 14),
						Position = UDim2.fromOffset(13, 12),
						BackgroundTransparency = 1,
						Text = (bedwars.ItemMeta[block.Name] and bedwars.ItemMeta[block.Name].displayName) or block.Name,
						TextXAlignment = Enum.TextXAlignment.Left,
						TextYAlignment = Enum.TextYAlignment.Top,
						TextColor3 = Color3.new(),
						TextScaled = true,
						Font = Enum.Font.Arial
					}),
					create('TextLabel', {
						Size = UDim2.fromOffset(145, 14),
						Position = UDim2.fromOffset(12, 11),
						BackgroundTransparency = 1,
						Text = (bedwars.ItemMeta[block.Name] and bedwars.ItemMeta[block.Name].displayName) or block.Name,
						TextXAlignment = Enum.TextXAlignment.Left,
						TextYAlignment = Enum.TextYAlignment.Top,
						TextColor3 = color.Dark(uipallet.Text, 0.16),
						TextScaled = true,
						Font = Enum.Font.Arial
					}),
					create('Frame', {
						Size = UDim2.fromOffset(138, 4),
						Position = UDim2.fromOffset(12, 32),
						BackgroundColor3 = uipallet.Main
					}, {
						create('UICorner', {CornerRadius = UDim.new(1, 0)}),
						create('Frame', {
							[bedwars.Roact.Ref] = _hbProgressRef,
							Size = UDim2.fromScale(percent, 1),
							BackgroundColor3 = Color3.fromHSV(math.clamp(percent / 2.5, 0, 1), 0.89, 0.75)
						}, {create('UICorner', {CornerRadius = UDim.new(1, 0)})})
					})
				})
			}), part)
		end

		local newpercent = math.clamp((health - changeHealth) / maxHealth, 0, 1)
		if _hbProgressRef and _hbProgressRef:getValue() then
			tweenService:Create(_hbProgressRef:getValue(), TweenInfo.new(0.3), {
				Size = UDim2.fromScale(newpercent, 1),
				BackgroundColor3 = Color3.fromHSV(math.clamp(newpercent / 2.5, 0, 1), 0.89, 0.75)
			}):Play()
		end

		if newpercent <= 0 then
			cleanupHealthbar()
		end
	end

	local function cachedIsBreakable(v)
		local now = tick()
		local cached = breakabilityCache[v]
		if cached and (now - cached.t) < BREAK_CACHE_TTL then
			return cached.v
		end
		local ok, result = pcall(function()
			local blockPos = bedwars.BlockController:getBlockPosition(v.Position)
			return bedwars.BlockController:isBlockBreakable({blockPosition = blockPos}, lplr)
		end)
		local val = ok and result or false
		breakabilityCache[v] = {v = val, t = now}
		return val
	end

	local function rebuildTeammateCache()
		local now = tick()
		if now - cachedTeammatesTime < 2 then return end
		cachedTeammatesTime = now
		table.clear(cachedTeammates)
		local localTeam = lplr:GetAttribute('Team') or lplr.Team
		if not localTeam then return end
		for _, player in playersService:GetPlayers() do
			local pt = player:GetAttribute('Team') or player.Team
			if pt == localTeam then
				cachedTeammates[player.UserId] = true
			end
		end
	end

	local function isSameTeam(userId)
		if not userId or userId == 0 then return false end
		rebuildTeammateCache()
		return cachedTeammates[userId] == true
	end

	local function myTeamId()
		return lplr:GetAttribute('Team')
			or (lplr.Character and (lplr.Character:GetAttribute('Team') or lplr.Character:GetAttribute('TeamId')))
	end

	local function passesChecks(v)
		if v:GetAttribute('NoBreak') then return false end

		if (v:GetAttribute('BedShieldEndTime') or 0) > workspace:GetServerTimeNow() then
			return false
		end

		local blockTeam = v:GetAttribute('Team') or v:GetAttribute('TeamId')
		local mineNow = myTeamId()
		if mineNow and v:GetAttribute('Team' .. tostring(mineNow) .. 'NoBreak') then
			return false
		end

		if not SelfBreak.Enabled then
			local mine = myTeamId()
			if blockTeam and mine and tonumber(blockTeam) == tonumber(mine) then
				return false
			end
			if v:GetAttribute('PlacedByUserId') == lplr.UserId then
				return false
			end
			if isSameTeam(v:GetAttribute('PlacedByUserId')) then
				return false
			end
		end

		if LimitItem.Enabled then
			local hand = store.hand and store.hand.tool
			local hmeta = hand and bedwars.ItemMeta[hand.Name]
			if not (hmeta and hmeta.breakBlock) then return false end
		end

		return true
	end

	local function ensureParts(count)
		if not (Breaker and Breaker.Enabled) then return end
		while #parts < count do
			local part = Instance.new('Part')
			part.Anchored = true
			part.CanQuery = false
			part.CanCollide = false
			part.Transparency = 1
			part.Position = Vector3.zero
			part.Parent = gameCamera
			local adorn = Instance.new('BoxHandleAdornment')
			adorn.Size = Vector3.one
			adorn.AlwaysOnTop = true
			adorn.ZIndex = 1
			adorn.Transparency = 0.5
			adorn.Adornee = part
			adorn.Parent = part
			table.insert(parts, part)
		end
	end

	local function hideParts()
		for _, p in parts do
			p.Position = Vector3.zero
		end
	end

	local function destroyParts()
		for _, p in parts do
			pcall(function()
				p:ClearAllChildren()
				p:Destroy()
			end)
		end
		table.clear(parts)
	end

	local function highlightColor(fallback)
		if BreakerHighlightColor then
			return Color3.fromHSV(BreakerHighlightColor.Hue, BreakerHighlightColor.Sat, BreakerHighlightColor.Value)
		end
		return fallback
	end

	local function setHighlight(block)
		if not (BlockHighlight and BlockHighlight.Enabled) then
			if blockHighlightInstance then blockHighlightInstance.Adornee = nil end
			return
		end
		if not blockHighlightInstance then
			if not (Breaker and Breaker.Enabled) then return end
			blockHighlightInstance = Instance.new('BoxHandleAdornment')
			blockHighlightInstance.AlwaysOnTop = true
			blockHighlightInstance.ZIndex = 10
			blockHighlightInstance.Transparency = 0.3
			blockHighlightInstance.Parent = gameCamera
		end
		blockHighlightInstance.Color3 = highlightColor(Color3.fromRGB(255, 255, 0))
		if block and block.Parent then
			blockHighlightInstance.Size = block.Size + Vector3.new(0.05, 0.05, 0.05)
			blockHighlightInstance.Adornee = block
		else
			blockHighlightInstance.Adornee = nil
		end
	end

	local function clearVisuals()
		hideParts()
		setHighlight(nil)
	end

	local function clearLegit()
		table.clear(legitRoute)
		legitTarget = nil
		legitAnchor = nil
	end

	local function cleanupAll()
		table.clear(breakabilityCache)
		clearLegit()
		if blockHighlightInstance then
			pcall(function() blockHighlightInstance:Destroy() end)
			blockHighlightInstance = nil
		end
		destroyParts()
		cleanupHealthbar()
	end

	local function useKitAbilities()
		if RagnarBreaker and RagnarBreaker.Enabled and store.equippedKit == 'berserker' then
			pcall(function()
				if bedwars.AbilityController and bedwars.AbilityController:canUseAbility('berserker_rage') then
					replicatedStorage:WaitForChild('events-@easy-games/game-core:shared/game-core-networking@getEvents.Events'):WaitForChild('useAbility'):FireServer('berserker_rage')
				end
			end)
		end
		if YetiBreaker and YetiBreaker.Enabled and store.equippedKit == 'yeti' and (tick() - lastYetiUse) > 1 then
			lastYetiUse = tick()
			task.spawn(function()
				pcall(function()
					if bedwars.AbilityController and bedwars.AbilityController:canUseAbility('yeti_glacial_roar') then
						replicatedStorage:WaitForChild('events-@easy-games/game-core:shared/game-core-networking@getEvents.Events'):WaitForChild('useAbility'):FireServer('yeti_glacial_roar')
					end
				end)
			end)
		end
	end

	local function holdingCorrectTool(v)
		if AutoTool.Enabled then return true end
		local blockMeta = bedwars.ItemMeta[v.Name]
		local breaktype = v.Name == 'gumdrop_bounce_pad' and 'stone' or (blockMeta and blockMeta.block and blockMeta.block.breakType)
		if not breaktype then return true end
		local correctTool = store.tools[breaktype]
		if not correctTool then return true end
		local hand = store.hand and store.hand.tool
		return hand ~= nil and hand.Name == correctTool.tool.Name
	end

	local function drawPath(target, path, endpos)
		if not (ShowPath and ShowPath.Enabled) or not path or not target then
			hideParts()
			return
		end
		local currentnode = target
		for _, part in parts do
			part.Position = currentnode or Vector3.zero
			if currentnode then
				part.BoxHandleAdornment.Color3 = currentnode == endpos and Color3.new(1, 0.2, 0.2)
					or currentnode == target and Color3.new(0.2, 0.2, 1)
					or Color3.new(0.2, 1, 0.2)
			end
			currentnode = path[currentnode]
		end
	end

	local function drawRoute(activePos)
		if not (ShowPath and ShowPath.Enabled) then
			hideParts()
			return
		end
		for i, part in parts do
			local node = legitRoute[i]
			part.Position = node or Vector3.zero
			if node then
				part.BoxHandleAdornment.Color3 = node == activePos and Color3.new(1, 0.2, 0.2) or Color3.new(0.2, 1, 0.2)
			end
		end
	end

	local damageRemote
	task.spawn(function()
		pcall(function()
			damageRemote = replicatedStorage
				:WaitForChild('rbxts_include'):WaitForChild('node_modules')
				:WaitForChild('@easy-games'):WaitForChild('block-engine')
				:WaitForChild('node_modules'):WaitForChild('@rbxts')
				:WaitForChild('net'):WaitForChild('out')
				:WaitForChild('_NetManaged'):WaitForChild('DamageBlock')
		end)
	end)

	local function rawBreak(block)
		if not damageRemote or not block or not block.Parent then return false end
		local bp = bedwars.BlockController:getBlockPosition(block.Position)
		task.spawn(function()
			local ok, res = pcall(function()
				return damageRemote:InvokeServer({
					blockRef = {blockPosition = bp},
					hitPosition = block.Position + Vector3.new(0, block.Size.Y / 2, 0),
					hitNormal = Vector3.yAxis
				})
			end)
			dbg('raw', block.Name, tostring(bp), 'ok', ok, 'res', tostring(res))
		end)
		return true
	end

	local ignoreList = {}
	local cursorParams = RaycastParams.new()
	cursorParams.FilterType = Enum.RaycastFilterType.Exclude

	local function cursorBlock()
		local sp = screenPoint()
		local unit = gameCamera:ViewportPointToRay(sp.X, sp.Y, 0)
		table.clear(ignoreList)
		if lplr.Character then
			table.insert(ignoreList, lplr.Character)
		end
		for _, plr in playersService:GetPlayers() do
			if plr.Character then
				table.insert(ignoreList, plr.Character)
			end
		end
		for _, ent in entitylib.List do
			if ent.Character then
				table.insert(ignoreList, ent.Character)
			end
		end
		cursorParams.FilterDescendantsInstances = ignoreList
		local result = workspace:Raycast(unit.Origin, unit.Direction * 999, cursorParams)
		if not result then return nil, nil end
		local inst = result.Instance
		return (inst and inst:IsA('BasePart')) and inst or nil, result.Position
	end

	local function containedWorldPositions(block)
		local out = {}
		local ok, handler = pcall(function()
			return bedwars.BlockController:getHandlerRegistry():getHandler(block.Name)
		end)
		if ok and handler then
			local ok2, cp = pcall(function() return handler:getContainedPositions(block) end)
			if ok2 and cp then
				for _, v in cp do
					table.insert(out, v * 3)
				end
			end
		end
		if #out == 0 then
			table.insert(out, bedwars.BlockController:getBlockPosition(block.Position) * 3)
		end
		return out
	end

	local function buildLegitRoute(target)
		local anchor = frontPoint()
		if not anchor then return false end

		local bestPos, bestPath, bestStart, bestScore = nil, nil, nil, math.huge
		for _, startPos in containedWorldPositions(target) do
			local ok, pos, cost, path = pcall(bedwars.calculatePath, target, startPos, BreakerAngle.Value, anchor)
			if ok and pos and cost then
				local score = cost + (pos - anchor).Magnitude * 0.1
				if score < bestScore then
					bestPos, bestPath, bestStart, bestScore = pos, path, startPos, score
				end
			end
		end
		if not bestPos then return false end

		table.clear(legitRoute)
		local cur = bestPos
		local guard = 0
		while cur and guard < 128 do
			guard = guard + 1
			table.insert(legitRoute, cur)
			if cur == bestStart then break end
			cur = bestPath and bestPath[cur]
		end

		legitAnchor = anchor
		legitTarget = target
		legitPlanTime = tick()
		return #legitRoute > 0
	end

	Breaker = vape.Categories.Minigames:CreateModule({
		Name = 'Breaker',
		Function = function(callback)
			if callback then
				local beds = {}
				local function bedAdd(obj)
					if obj.Name ~= 'bed' or not obj:IsA('BasePart') then return end
					if table.find(beds, obj) then return end
					table.insert(beds, obj)
				end
				local function bedRemove(obj)
					local i = table.find(beds, obj)
					if i then table.remove(beds, i) end
				end
				for _, obj in workspace:GetChildren() do
					bedAdd(obj)
				end
				Breaker:Clean(workspace.ChildAdded:Connect(bedAdd))
				Breaker:Clean(workspace.ChildRemoved:Connect(bedRemove))
				task.spawn(function()
					while Breaker.Enabled do
						task.wait(1)
						for i = #beds, 1, -1 do
							if not beds[i] or not beds[i].Parent then
								table.remove(beds, i)
							end
						end
						for _, obj in workspace:GetChildren() do
							bedAdd(obj)
						end
					end
				end)
				local luckyblock = collection('LuckyBlock', Breaker)
				local ironores = collection('iron_ore_mesh_block', Breaker)

				local trackedSpecial = {tesla_trap = {}, beehive = {}, pinata = {}, carrot = {}, melon = {}, pumpkin = {}, snow_pile = {}}
				local _trackedNames = {tesla_trap = true, beehive = true, pinata = true, carrot = true, melon = true, pumpkin = true, snow_pile = true}

				local function trackAdd(obj)
					if not _trackedNames[obj.Name] then return end
					local t = trackedSpecial[obj.Name]
					if not t then return end
					if obj:IsA('BasePart') then
						table.insert(t, obj)
					elseif obj:IsA('Model') then
						local part = obj.PrimaryPart or obj:FindFirstChildWhichIsA('BasePart')
						if part then table.insert(t, part) end
					end
				end

				local function trackRemove(obj)
					if not _trackedNames[obj.Name] then return end
					local t = trackedSpecial[obj.Name]
					if t then
						local part = obj
						if obj:IsA('Model') then
							part = obj.PrimaryPart or obj:FindFirstChildWhichIsA('BasePart')
						end
						if part then
							local i = table.find(t, part)
							if i then table.remove(t, i) end
						end
					end
					breakabilityCache[obj] = nil
				end

				ensureParts(8)
				scanDescendants(workspace, trackAdd, Breaker)
				Breaker:Clean(workspace.DescendantAdded:Connect(trackAdd))
				Breaker:Clean(workspace.DescendantRemoving:Connect(trackRemove))

				local function getBlockHealth(block)
					return block:GetAttribute('Health')
						or (bedwars.ItemMeta[block.Name] and bedwars.ItemMeta[block.Name].block and bedwars.ItemMeta[block.Name].block.health)
						or 0
				end

				local function candidateLists()
					local lists = {}
					if Bed.Enabled then
						dbg('bedlist size', #beds)
						table.insert(lists, beds)
					end
					if Tesla and Tesla.Enabled then table.insert(lists, trackedSpecial.tesla_trap) end
					if Hive and Hive.Enabled then table.insert(lists, trackedSpecial.beehive) end
					if LuckyBlock.Enabled then table.insert(lists, luckyblock) end
					if IronOre.Enabled then table.insert(lists, ironores) end
					if Snow and Snow.Enabled then table.insert(lists, trackedSpecial.snow_pile) end
					if Pinata and Pinata.Enabled then table.insert(lists, trackedSpecial.pinata) end
					if Crops and Crops.Enabled then
						table.insert(lists, trackedSpecial.carrot)
						table.insert(lists, trackedSpecial.melon)
						table.insert(lists, trackedSpecial.pumpkin)
					end
					return lists
				end

				local function valid(v, localPosition)
					if not v or not v.Parent then return false end
					local d = (v.Position - localPosition).Magnitude
					if d >= Range.Value then
						if v.Name == 'bed' then dbg('bed OUT OF RANGE', math.floor(d), 'max', Range.Value) end
						return false
					end
					if not passesChecks(v) then
						if v.Name == 'bed' then dbg('bed FAILED passesChecks') end
						return false
					end
					if not cachedIsBreakable(v) then
						if v.Name == 'bed' then dbg('bed FAILED isBreakable') end
						return false
					end
					if v.Name == 'bed' then dbg('bed VALID at', math.floor(d)) end
					return true
				end

				local function pickBest(tab, localPosition)
					if not tab then return nil end
					local best, bestValue = nil, math.huge
					for i = 1, #tab do
						local v = tab[i]
						if v and valid(v, localPosition) then
							local value = (TargetMode.Value == 'Health') and getBlockHealth(v) or (v.Position - localPosition).Magnitude
							if value < bestValue then
								best, bestValue = v, value
							end
						end
					end
					return best
				end

				local function pickClosestAny(localPosition)
					local best, bestDist = nil, math.huge
					for _, tab in candidateLists() do
						for i = 1, #tab do
							local v = tab[i]
							if v and valid(v, localPosition) then
								local dist = (v.Position - localPosition).Magnitude
								if dist < bestDist then
									best, bestDist = v, dist
								end
							end
						end
					end
					return best
				end

				local function runNormal(localPosition)
					local found = false
					for _, tab in candidateLists() do
						local best = pickBest(tab, localPosition)
						if best then
							found = true
							dbg('picked', best.Name, 'toolOK', holdingCorrectTool(best), 'hand', store.hand and store.hand.tool and store.hand.tool.Name)
							if holdingCorrectTool(best) then
								useKitAbilities()
								setHighlight(best)
								rawBreak(best)
								local breakWait = BreakSpeed.Value
								local target, path, endpos = bedwars.breakBlock(
									best,
									Effect.Enabled,
									Animation.Enabled,
									CustomHealth.Enabled and customHealthbar or nil,
									AutoTool.Enabled,
									BreakerAngle.Value
								)
								dbg('breakBlock ->', best.Name, 'pos', tostring(target), 'health', best:GetAttribute('Health'))
								if BedCheck and BedCheck.Enabled and best.Name == 'bed' and target and (target - best.Position).Magnitude < 3 then
									breakWait = math.max(breakWait, 0.3)
								end
								drawPath(target, path, endpos)
								task.wait(breakWait)
								return true
							end
						end
					end
					return found
				end

				local function runLegit(localPosition)
					local target = pickClosestAny(localPosition)
					if not target then
						clearLegit()
						return false
					end

					local anchor = frontPoint()
					local stale = legitTarget ~= target
						or legitTarget and not legitTarget.Parent
						or not legitAnchor
						or not anchor
						or #legitRoute == 0
						or (anchor - legitAnchor).Magnitude > 6
						or (tick() - legitPlanTime) > 2

					local frontPos, frontBlock = nil, nil
					local function findFront()
						frontPos, frontBlock = nil, nil
						for _, pos in legitRoute do
							local blk = getPlacedBlock(pos)
							if blk then
								frontPos, frontBlock = pos, blk
								break
							end
						end
					end

					if not stale then
						findFront()
						if not frontBlock then stale = true end
					end
					if stale then
						if not buildLegitRoute(target) then
							clearVisuals()
							return false
						end
						findFront()
					end
					if not frontBlock then
						clearVisuals()
						return false
					end

					local raw, hitPos = cursorBlock()
					local aimed = nil
					if raw and raw.Parent then
						if raw == frontBlock or raw:IsDescendantOf(frontBlock) then
							aimed = frontBlock
						elseif frontBlock:IsA('BasePart') and raw:IsDescendantOf(frontBlock.Parent) and raw.Name == frontBlock.Name then
							aimed = frontBlock
						end
					end
					if not aimed and hitPos and (hitPos - frontBlock.Position).Magnitude <= 4.5 then
						aimed = frontBlock
					end

					drawRoute(aimed and frontPos or nil)

					if not aimed or (aimed.Position - localPosition).Magnitude > Range.Value then
						setHighlight(nil)
						return true
					end

					setHighlight(aimed)
					if (tick() - legitLastHit) >= BreakSpeed.Value then
						legitLastHit = tick()
						if holdingCorrectTool(aimed) then
							useKitAbilities()
							bedwars.breakBlock(
								aimed,
								Effect.Enabled,
								Animation.Enabled,
								CustomHealth.Enabled and customHealthbar or nil,
								AutoTool.Enabled,
								BreakerAngle.Value,
								frontPos
							)
						end
					end
					return true
				end

				repeat
					task.wait(1 / UpdateRate.Value)
					if not Breaker.Enabled then break end

					if _hbBlock and (not _hbBlock.Parent or (_hbBlock:GetAttribute('Health') or 0) <= 0) then
						cleanupHealthbar()
					end
					if blockHighlightInstance and blockHighlightInstance.Adornee then
						local a = blockHighlightInstance.Adornee
						if not a.Parent or (a:GetAttribute('Health') or 1) <= 0 then
							blockHighlightInstance.Adornee = nil
							hideParts()
						end
					end
					if legitTarget and not legitTarget.Parent then
						clearLegit()
						hideParts()
					end

					if entitylib.isAlive then
						if MouseDown.Enabled and not inputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
							clearVisuals()
							continue
						end
						local localPosition = entitylib.character.RootPart.Position
						local acted
						dbg('mode', Mode and Mode.Value, 'range', Range.Value, 'selfbreak', SelfBreak.Enabled)
						if Mode and Mode.Value == 'Legit' then
							acted = runLegit(localPosition)
						else
							acted = runNormal(localPosition)
						end
						if not acted then
							clearVisuals()
						end
					else
						clearVisuals()
					end
				until not Breaker.Enabled
			else
				cleanupAll()
			end
		end,
		Tooltip = 'oh my god nuke the BEDD NOOOO'
	})

	vape:Clean(cleanupAll)

	TargetMode = Breaker:CreateDropdown({
		Name = 'Target Mode',
		List = {'Distance', 'Health'},
		Default = 'Distance',
		Tooltip = 'distance picks the closest block, health picks the weakest one'
	})
	Mode = Breaker:CreateDropdown({
		Name = 'Mode',
		List = {'Normal', 'Legit'},
		Default = 'Normal',
		Tooltip = 'normal digs to the block on its own, legit only breaks what ur cursor is on',
		Function = function(val)
			if TargetMode and TargetMode.Object then
				TargetMode.Object.Visible = val ~= 'Legit'
			end
			clearLegit()
		end
	})
	Range = Breaker:CreateSlider({
		Name = 'Break range',
		Min = 1,
		Max = 30,
		Default = 30,
		Tooltip = 'how far away a block can be for u to hit it'
	})
	BreakerAngle = Breaker:CreateSlider({
		Name = 'Break Angle',
		Min = 0,
		Max = 360,
		Default = 360,
		Tooltip = 'only digs thru blocks inside this cone in front of ur cam'
	})
	BreakSpeed = Breaker:CreateSlider({
		Name = 'Break speed',
		Min = 0,
		Max = 0.3,
		Default = 0.25,
		Decimal = 100,
		Tooltip = 'wait between each hit, lower is faster'
	})
	UpdateRate = Breaker:CreateSlider({
		Name = 'Update rate',
		Min = 1,
		Max = 120,
		Default = 60,
		Tooltip = 'how often it re checks for blocks, leave it high'
	})
	Bed = Breaker:CreateToggle({
		Name = 'Break Bed',
		Default = true,
		Function = function(callback)
			if BedCheck and BedCheck.Object then
				BedCheck.Object.Visible = callback
			end
		end
	})
	BedCheck = Breaker:CreateToggle({
		Name = 'Bed Check',
		Default = false,
		Darker = true,
		Tooltip = 'slows down to normal speed once ur actually on the bed'
	})
	LuckyBlock = Breaker:CreateToggle({
		Name = 'Break Lucky Block',
		Default = true
	})
	IronOre = Breaker:CreateToggle({
		Name = 'Break Iron Ore',
		Default = true
	})
	Snow = Breaker:CreateToggle({
		Name = 'Break Snow',
		Default = false
	})
	Tesla = Breaker:CreateToggle({
		Name = 'Break Tesla',
		Default = true
	})
	Hive = Breaker:CreateToggle({
		Name = 'Break Hive',
		Default = true
	})
	Pinata = Breaker:CreateToggle({
		Name = 'Break Pinata',
		Default = false
	})
	Crops = Breaker:CreateToggle({
		Name = 'Break Crops',
		Default = false,
		Tooltip = 'breaks farmer cletus crops (carrot and etc)'
	})
	Effect = Breaker:CreateToggle({
		Name = 'Show Healthbar & Effects',
		Default = true,
		Function = function(callback)
			if CustomHealth and CustomHealth.Object then
				CustomHealth.Object.Visible = callback
			end
		end
	})
	CustomHealth = Breaker:CreateToggle({
		Name = 'Custom Healthbar',
		Default = true,
		Darker = true
	})
	Animation = Breaker:CreateToggle({
		Name = 'Animation',
		Tooltip = 'plays the swing animation while u dig'
	})
	SelfBreak = Breaker:CreateToggle({
		Name = 'Self Break',
		Tooltip = 'lets it break ur own bed and blocks u or ur team placed'
	})
	AutoTool = Breaker:CreateToggle({
		Name = 'Auto Tool',
		Default = true,
		Tooltip = 'swaps to the right tool on its own, off means it waits till ur holdin it'
	})
	LimitItem = Breaker:CreateToggle({
		Name = 'Limit to items',
		Tooltip = 'only works while ur holdin sum that can break blocks'
	})
	MouseDown = Breaker:CreateToggle({
		Name = 'Require Mouse Down',
		Tooltip = 'only digs while u hold left click'
	})
	YetiBreaker = Breaker:CreateToggle({
		Name = 'Yeti Breaker',
		Tooltip = 'pops the yeti roar whenever ur nuking'
	})
	RagnarBreaker = Breaker:CreateToggle({
		Name = 'Ragnar',
		Tooltip = 'pops the ragnar rage whenever ur nuking'
	})
	ShowPath = Breaker:CreateToggle({
		Name = 'Show Path',
		Default = true,
		Tooltip = 'shows u the blocks its diggin thru'
	})
	BlockHighlight = Breaker:CreateToggle({
		Name = 'Block Highlight',
		Default = false,
		Function = function(callback)
			if BreakerHighlightColor and BreakerHighlightColor.Object then
				BreakerHighlightColor.Object.Visible = callback
			end
			if not callback and blockHighlightInstance then
				blockHighlightInstance.Adornee = nil
			end
		end,
		Tooltip = 'boxes the block its hittin rn'
	})
	BreakerHighlightColor = Breaker:CreateColorSlider({
		Name = 'Highlight Color',
		Darker = true,
		Visible = false
	})
	BreakerDebug = Breaker:CreateToggle({
		Name = 'Debug',
		Default = false,
		Tooltip = 'prints what breaker is doing to console'
	})

	task.defer(function()
		if CustomHealth and CustomHealth.Object and Effect then
			CustomHealth.Object.Visible = Effect.Enabled
		end
		if BedCheck and BedCheck.Object and Bed then
			BedCheck.Object.Visible = Bed.Enabled
		end
		if BreakerHighlightColor and BreakerHighlightColor.Object and BlockHighlight then
			BreakerHighlightColor.Object.Visible = BlockHighlight.Enabled
		end
		if TargetMode and TargetMode.Object and Mode then
			TargetMode.Object.Visible = Mode.Value ~= 'Legit'
		end
	end)
end)

--[[
	Legit Modules
]]

run(function()
	local HitColor
	local Color
	local done = {}
	
	HitColor = vape.Categories.Legit:CreateModule({
		Name = 'HitColor',
		Function = function(callback)
			if callback then
				local function hookHighlight(v)
					local highlight = v.Character and v.Character:FindFirstChild('_DamageHighlight_')
					if highlight and not done[highlight] then
						highlight.FillColor = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
						highlight.FillTransparency = Color.Opacity
						done[highlight] = true
					end
				end
				for _, v in entitylib.List do hookHighlight(v) end
				HitColor:Clean(entitylib.Events.EntityAdded:Connect(hookHighlight))
			else
				for highlight in pairs(done) do
					if highlight and highlight.Parent then
						pcall(function()
							highlight.FillColor = Color3.new(1, 0, 0)
							highlight.FillTransparency = 0.4
						end)
					end
				end
				table.clear(done)
			end
		end,
		Tooltip = 'customize hit color'
	})
	Color = HitColor:CreateColorSlider({
		Name = 'Color',
		DefaultOpacity = 0.4
	})
end)

run(function()
	local Hitfix 

    HitFix = vape.Categories.Legit:CreateModule({
        Name = 'HitFix',
        Function = function(callback)
            local func = bedwars.SwordController.swingSwordAtMouse
            if not func then return end
            local foundIndex = nil
            pcall(function()
                for i = 1, 50 do
                    local constant = debug.getconstant(func, i)
                    if constant == 'Raycast' or constant == 'raycast' then
                        foundIndex = i
                        break
                    end
                end
            end)
            
            if foundIndex then
                pcall(function()
                    debug.setconstant(func, foundIndex, callback and 'raycast' or 'Raycast')
                end)
            end
            
            pcall(function()
                debug.setupvalue(func, 4, callback and bedwars.QueryUtil or workspace)
            end)
        end,
    })
end)

run(function()
	local Interface
	local HotbarOpenInventory = require(lplr.PlayerScripts.TS.controllers.global.hotbar.ui['hotbar-open-inventory']).HotbarOpenInventory
	local HotbarHealthbar = require(lplr.PlayerScripts.TS.controllers.global.hotbar.ui.healthbar['hotbar-healthbar']).HotbarHealthbar
	local HotbarApp = getRoactRender(require(lplr.PlayerScripts.TS.controllers.global.hotbar.ui['hotbar-app']).HotbarApp.render)
	local old, new = {}, {}
	
	vape:Clean(function()
		for _, v in new do
			table.clear(v)
		end
		for _, v in old do
			table.clear(v)
		end
		table.clear(new)
		table.clear(old)
	end)
	
	local function modifyconstant(func, ind, val)
		if not func then return end
		if not old[func] then old[func] = {} end
		if not new[func] then new[func] = {} end
		if not old[func][ind] then
			old[func][ind] = debug.getconstant(func, ind)
		end
		if typeof(old[func][ind]) ~= typeof(val) then return end
		new[func][ind] = val
	
		if Interface.Enabled then
			if val then
				debug.setconstant(func, ind, val)
			else
				debug.setconstant(func, ind, old[func][ind])
				old[func][ind] = nil
			end
		end
	end
	
	Interface = vape.Categories.Legit:CreateModule({
		Name = 'Interface',
		Function = function(callback)
			for i, v in (callback and new or old) do
				for i2, v2 in v do
					debug.setconstant(i, i2, v2)
				end
			end
		end,
		Tooltip = 'Customize bedwars UI'
	})
	local fontitems = {'LuckiestGuy'}
	for _, v in Enum.Font:GetEnumItems() do
		if v.Name ~= 'LuckiestGuy' then
			table.insert(fontitems, v.Name)
		end
	end
	Interface:CreateDropdown({
		Name = 'Health Font',
		List = fontitems,
		Function = function(val)
			modifyconstant(HotbarHealthbar.render, 77, val)
		end
	})
	Interface:CreateColorSlider({
		Name = 'Health Color',
		Function = function(hue, sat, val)
			modifyconstant(HotbarHealthbar.render, 16, tonumber(Color3.fromHSV(hue, sat, val):ToHex(), 16))
			if Interface.Enabled then
				local hotbar = lplr.PlayerGui:FindFirstChild('hotbar')
				hotbar = hotbar and hotbar:FindFirstChild('HealthbarProgressWrapper', true)
				if hotbar then
					hotbar['1'].BackgroundColor3 = Color3.fromHSV(hue, sat, val)
				end
			end
		end
	})
	Interface:CreateColorSlider({
		Name = 'Hotbar Color',
		DefaultOpacity = 0.8,
		Function = function(hue, sat, val, opacity)
			local func = oldinvrender or HotbarOpenInventory.render
			modifyconstant(debug.getupvalue(HotbarApp, 23).render, 51, tonumber(Color3.fromHSV(hue, sat, val):ToHex(), 16))
			modifyconstant(debug.getupvalue(HotbarApp, 23).render, 58, tonumber(Color3.fromHSV(hue, sat, math.clamp(val > 0.5 and val - 0.2 or val + 0.2, 0, 1)):ToHex(), 16))
			modifyconstant(debug.getupvalue(HotbarApp, 23).render, 54, 1 - opacity)
			modifyconstant(debug.getupvalue(HotbarApp, 23).render, 55, math.clamp(1.2 - opacity, 0, 1))
			modifyconstant(func, 31, tonumber(Color3.fromHSV(hue, sat, val):ToHex(), 16))
			modifyconstant(func, 32, math.clamp(1.2 - opacity, 0, 1))
			modifyconstant(func, 34, tonumber(Color3.fromHSV(hue, sat, math.clamp(val > 0.5 and val - 0.2 or val + 0.2, 0, 1)):ToHex(), 16))
		end
	})
end)
	
run(function()
	local KillEffect
	local Mode
	local List
	local NameToId = {}
	
	local killeffects = {
		Gravity = function(_, _, char, _)
			char:BreakJoints()
			local highlight = char:FindFirstChildWhichIsA('Highlight')
			local nametag = char:FindFirstChild('Nametag', true)
			if highlight then
				highlight:Destroy()
			end
			if nametag then
				nametag:Destroy()
			end
	
			task.spawn(function()
				local partvelo = {}
				for _, v in char:GetDescendants() do
					if v:IsA('BasePart') then
						partvelo[v.Name] = v.Velocity
					end
				end
				char.Archivable = true
				local clone = char:Clone()
				clone.Humanoid.Health = 100
				clone.Parent = workspace
				game:GetService('Debris'):AddItem(clone, 30)
				char:Destroy()
				task.wait(0.01)
				clone.Humanoid:ChangeState(Enum.HumanoidStateType.Dead)
				clone:BreakJoints()
				task.wait(0.01)
				for _, v in clone:GetDescendants() do
					if v:IsA('BasePart') then
						local bodyforce = Instance.new('BodyForce')
						bodyforce.Force = Vector3.new(0, (workspace.Gravity - 10) * v:GetMass(), 0)
						bodyforce.Parent = v
						v.CanCollide = true
						v.Velocity = partvelo[v.Name] or Vector3.zero
					end
				end
			end)
		end,
		Lightning = function(_, _, char, _)
			char:BreakJoints()
			local highlight = char:FindFirstChildWhichIsA('Highlight')
			if highlight then
				highlight:Destroy()
			end
			local startpos = 1125
			local startcf = char.PrimaryPart.CFrame.p - Vector3.new(0, 8, 0)
			local newpos = Vector3.new((math.random(1, 10) - 5) * 2, startpos, (math.random(1, 10) - 5) * 2)
	
			for i = startpos - 75, 0, -75 do
				local newpos2 = Vector3.new((math.random(1, 10) - 5) * 2, i, (math.random(1, 10) - 5) * 2)
				if i == 0 then
					newpos2 = Vector3.zero
				end
				local part = Instance.new('Part')
				part.Size = Vector3.new(1.5, 1.5, 77)
				part.Material = Enum.Material.SmoothPlastic
				part.Anchored = true
				part.Material = Enum.Material.Neon
				part.CanCollide = false
				part.CFrame = CFrame.new(startcf + newpos + ((newpos2 - newpos) * 0.5), startcf + newpos2)
				part.Parent = workspace
				local part2 = part:Clone()
				part2.Size = Vector3.new(3, 3, 78)
				part2.Color = Color3.new(0.7, 0.7, 0.7)
				part2.Transparency = 0.7
				part2.Material = Enum.Material.SmoothPlastic
				part2.Parent = workspace
				game:GetService('Debris'):AddItem(part, 0.5)
				game:GetService('Debris'):AddItem(part2, 0.5)
				bedwars.QueryUtil:setQueryIgnored(part, true)
				bedwars.QueryUtil:setQueryIgnored(part2, true)
				if i == 0 then
					local soundpart = Instance.new('Part')
					soundpart.Transparency = 1
					soundpart.Anchored = true
					soundpart.Size = Vector3.zero
					soundpart.Position = startcf
					soundpart.Parent = workspace
					bedwars.QueryUtil:setQueryIgnored(soundpart, true)
					local sound = Instance.new('Sound')
					sound.SoundId = 'rbxassetid://6993372814'
					sound.Volume = 2
					sound.Pitch = 0.5 + (math.random(1, 3) / 10)
					sound.Parent = soundpart
					sound:Play()
					sound.Ended:Connect(function()
						soundpart:Destroy()
					end)
				end
				newpos = newpos2
			end
		end,
		Delete = function(_, _, char, _)
			char:Destroy()
		end
	}
	
	KillEffect = vape.Categories.Legit:CreateModule({
		Name = 'KillEffect',
		Function = function(callback)
			if callback then
				for i, v in killeffects do
					bedwars.KillEffectController.killEffects['Custom'..i] = {
						new = function()
							return {
								onKill = v,
								isPlayDefaultKillEffect = function()
									return false
								end
							}
						end
					}
				end
				KillEffect:Clean(lplr:GetAttributeChangedSignal('KillEffectType'):Connect(function()
					lplr:SetAttribute('KillEffectType', Mode.Value == 'Bedwars' and NameToId[List.Value] or 'Custom'..Mode.Value)
				end))
				lplr:SetAttribute('KillEffectType', Mode.Value == 'Bedwars' and NameToId[List.Value] or 'Custom'..Mode.Value)
			else
				for i in killeffects do
					bedwars.KillEffectController.killEffects['Custom'..i] = nil
				end
				lplr:SetAttribute('KillEffectType', 'default')
			end
		end,
		Tooltip = 'Custom final kill effects'
	})
	local modes = {'Bedwars'}
	for i in killeffects do
		table.insert(modes, i)
	end
	Mode = KillEffect:CreateDropdown({
		Name = 'Mode',
		List = modes,
		Function = function(val)
			List.Object.Visible = val == 'Bedwars'
			if KillEffect.Enabled then
				lplr:SetAttribute('KillEffectType', val == 'Bedwars' and NameToId[List.Value] or 'Custom'..val)
			end
		end
	})
	local KillEffectName = {}
	for i, v in bedwars.KillEffectMeta do
		table.insert(KillEffectName, v.name)
		NameToId[v.name] = i
	end
	table.sort(KillEffectName)
    List = KillEffect:CreateDropdown({
        Name = 'Bedwars',
        List = KillEffectName,
        Function = function(val)
            if KillEffect.Enabled then
                lplr:SetAttribute('KillEffectType', NameToId[val])
            end
        end,
        Darker = true
    })

    task.defer(function()
        if List and List.Object then
            List.Object.Visible = (Mode.Value == 'Bedwars')
        end
    end)
end)
	
run(function()
    local WinEffect
    local List
    local NameToId = {}
    
    WinEffect = vape.Categories.Legit:CreateModule({
        Name = "WinEffect",
        Function = function(callback)
            if callback then
                WinEffect:Clean(vapeEvents.MatchEndEvent.Event:Connect(function()
                    local remote = bedwars.Client:Get(remotes.WinEffectTriggered).instance
                    local payload = {
                        winEffectType = NameToId[List.Value],
                        winningPlayer = lplr
                    }
                    local ok = pcall(function()
                        remote.OnClientEvent:Fire(payload)
                    end)
                    if not ok then
                        for _, v in getconnections(remote.OnClientEvent) do
                            local fn = v.Function or v.Callback or v[1]
                            if fn then pcall(fn, payload) end
                        end
                    end
                end))
            end
        end,
        Tooltip = "select any clientside win effect"
    })
    
    local WinEffectName = {}
    for i, v in bedwars.WinEffectMeta do
        table.insert(WinEffectName, v.name)
        NameToId[v.name] = i
    end
    table.sort(WinEffectName)
    
    List = WinEffect:CreateDropdown({
        Name = "Effects",
        List = WinEffectName
    })
end)

run(function()
	local DamageIndicator
	local FontOption
	local Color
	local Size
	local Anchor
	local Stroke

	local fn = bedwars.DamageIndicator

	local suc, tab = pcall(function()
		return debug.getupvalue(fn, 2)
	end)
	tab = suc and tab or {}

	local fontIndices = {}
	local strokeIndex
	do
		local ok, consts = pcall(debug.getconstants, fn)
		if ok and consts then
			for i, v in consts do
				if typeof(v) == 'EnumItem' and v.EnumType == Enum.Font then
					table.insert(fontIndices, i)
				elseif v == 'Thickness' or v == 'Enabled' then
					strokeIndex = i
				end
			end
		end
	end
	if #fontIndices == 0 then fontIndices = {86} end
	if not strokeIndex then strokeIndex = 119 end

	local oldvalues = {}
	local oldfonts = {}

	local function applyFont(fontName)
		local font = Enum.Font[fontName]
		if not font then return end
		for _, i in fontIndices do
			pcall(debug.setconstant, fn, i, font)
		end
	end

	local function applyStroke(on)
		pcall(debug.setconstant, fn, strokeIndex, on and 'Thickness' or 'Enabled')
		tab.strokeThickness = on and 1 or false
	end
	
	DamageIndicator = vape.Categories.Legit:CreateModule({
		Name = 'DamageIndicator',
		Function = function(callback)
			if callback then
				oldvalues = table.clone(tab)
				oldfonts = {}
				for _, i in fontIndices do
					oldfonts[i] = debug.getconstant(fn, i)
				end
				applyFont(FontOption.Value)
				applyStroke(Stroke.Enabled)
				tab.textSize = Size.Value
				tab.blowUpSize = Size.Value
				tab.blowUpDuration = 0
				tab.baseColor = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
				tab.blowUpCompleteDuration = 0
				tab.anchoredDuration = Anchor.Value
			else
				for i, v in oldvalues do
					tab[i] = v
				end
				for i, v in oldfonts do
					pcall(debug.setconstant, fn, i, v)
				end
				pcall(debug.setconstant, fn, strokeIndex, 'Thickness')
			end
		end,
		Tooltip = 'customize the damage indicator'
	})
	local fontitems = {'GothamBlack'}
	for _, v in Enum.Font:GetEnumItems() do
		if v.Name ~= 'GothamBlack' then
			table.insert(fontitems, v.Name)
		end
	end
	FontOption = DamageIndicator:CreateDropdown({
		Name = 'Font',
		List = fontitems,
		Function = function(val)
			if DamageIndicator.Enabled then
				applyFont(val)
			end
		end
	})
	Color = DamageIndicator:CreateColorSlider({
		Name = 'Color',
		DefaultHue = 0,
		Function = function(hue, sat, val)
			if DamageIndicator.Enabled then
				tab.baseColor = Color3.fromHSV(hue, sat, val)
			end
		end
	})
	Size = DamageIndicator:CreateSlider({
		Name = 'Size',
		Min = 1,
		Max = 32,
		Default = 32,
		Function = function(val)
			if DamageIndicator.Enabled then
				tab.textSize = val
				tab.blowUpSize = val
			end
		end
	})
	Anchor = DamageIndicator:CreateSlider({
		Name = 'Anchor',
		Min = 0,
		Max = 1,
		Decimal = 10,
		Function = function(val)
			if DamageIndicator.Enabled then
				tab.anchoredDuration = val
			end
		end
	})
	Stroke = DamageIndicator:CreateToggle({
		Name = 'Stroke',
		Function = function(callback)
			if DamageIndicator.Enabled then
				applyStroke(callback)
			end
		end
	})
end)

run(function()
    local DisableStreamer
    local old = {}
    local oldLevels = {}
    local oldUsernames = {}
    local levelMap = {}
    local updaterThread = nil

    DisableStreamer = vape.Categories.Legit:CreateModule({
        Name = 'DisableStreamer',
        Function = function(callback)
            if callback then
                for _, plrs in playersService:GetPlayers() do
                    if plrs == lplr then continue end
                    
                    local disguiseName = plrs:GetAttribute("DisguiseDisplayName")
                    if disguiseName and disguiseName ~= "" then
                        old[plrs] = disguiseName
                        plrs:SetAttribute("DisguiseDisplayName", "")
                    end

                    local disguiseUsername = plrs:GetAttribute("DisguiseUsername")
                    if disguiseUsername and disguiseUsername ~= "" then
                        oldUsernames[plrs] = disguiseUsername
                        plrs:SetAttribute("DisguiseUsername", "")
                    end

                    local playerLevel = plrs:GetAttribute("PlayerLevel")
                    if playerLevel then
                        oldLevels[plrs] = playerLevel
                        levelMap[plrs.Name] = playerLevel
                    end
                end

                pcall(function()
                    bedwars.StreamerModeController:updateNametags(true)
                end)

                local running = true
                updaterThread = task.spawn(function()
                    while running do
                        local tabList = lplr.PlayerGui:FindFirstChild("TabListScreenGui")
                        if tabList then
                            for _, desc in tabList:GetDescendants() do
                                if desc:IsA("TextLabel") and desc.Name == "PlayerLevel" then
                                    local text = desc.Text
                                    if text:find("%[%?%]") then
                                        local siblingName = desc.Parent:FindFirstChild("PlayerName")
                                        if siblingName then
                                            local currentNameText = siblingName.Text
                                            local cleanName = currentNameText:gsub("^@", ""):match("^%s*(.-)%s*$")
                                            
                                            local matchedPlayer = nil
                                            for _, plr in playersService:GetPlayers() do
                                                if plr == lplr then continue end
                                                if cleanName == plr.Name or cleanName == plr.DisplayName then
                                                    matchedPlayer = plr
                                                    break
                                                end
                                            end
                                            
                                            if matchedPlayer then
                                                local realLevel = levelMap[matchedPlayer.Name]
                                                if realLevel then
                                                    desc.Text = '<font color="rgb(255,255,255)">[' .. tostring(realLevel) .. ']</font>'
                                                end
                                            end
                                        end
                                    end
                                end
                            end
                        end
                        task.wait(0.3)
                    end
                end)

            else
                if updaterThread then
                    task.cancel(updaterThread)
                    updaterThread = nil
                end
                
                for _, plrs in playersService:GetPlayers() do
                    if plrs == lplr then continue end
                    
                    if old[plrs] then
                        plrs:SetAttribute("DisguiseDisplayName", old[plrs])
                        old[plrs] = nil
                    end
                    
                    if oldUsernames[plrs] then
                        plrs:SetAttribute("DisguiseUsername", oldUsernames[plrs])
                        oldUsernames[plrs] = nil
                    end
                    
                    if oldLevels[plrs] then
                        plrs:SetAttribute("PlayerLevel", oldLevels[plrs])
                        oldLevels[plrs] = nil
                    end
                end
                
                pcall(function()
                    bedwars.StreamerModeController:updateNametags(true)
                end)
                
                task.spawn(function()
                    local tabList = lplr.PlayerGui:FindFirstChild("TabListScreenGui")
                    if tabList then
                        tabList.Enabled = false
                        task.wait()
                        tabList.Enabled = true
                    end
                end)
            end
        end,
        Tooltip = 'sisable people streamer mode'
    })
end)

run(function()
	local WhiteHits
	WhiteHits = vape.Categories.Legit:CreateModule({
		Name = "WhiteHits",
		Function = function(callback)
			if callback then
				local function killHighlight(obj)
					if obj and obj.Name == '_DamageHighlight_' then
						pcall(function()
							obj.Enabled = false
							obj:Destroy()
						end)
					end
				end
				local function sweep(model)
					if not model then return end
					for _, obj in model:GetDescendants() do
						killHighlight(obj)
					end
				end
				WhiteHits:Clean(workspace.DescendantAdded:Connect(killHighlight))
				repeat
					for _, plr in playersService:GetPlayers() do
						sweep(plr.Character)
					end
					for i, v in entitylib.List do 
						sweep(v.Character)
					end
					task.wait(0.1)
				until not WhiteHits.Enabled
			end
		end,
		Tooltip = 'stops that red flash when ppl get hit so hits stay "white"'
	})
end)

--[[
	Kits Modules
]]

run(function()
    local DronePhase
    local modified = {}

    DronePhase = vape.Categories.Kits:CreateModule({
        Name = 'DronePhase',
        Function = function(callback)
            if callback then
                local lastDroneScan = 0
                DronePhase:Clean(runService.Stepped:Connect(function()
                    local now = os.clock()
                    if now - lastDroneScan < 0.25 then return end
                    lastDroneScan = now
                    local myDrone
                    for _, drone in collectionService:GetTagged('Drone') do
                        if drone:GetAttribute('PlayerUserId') == lplr.UserId then
                            myDrone = drone
                            break
                        end
                    end

                    for part in modified do
                        if not myDrone or not part:IsDescendantOf(myDrone) then
                            modified[part] = nil
                            part.CanCollide = true
                        end
                    end

                    if myDrone then
                        for _, part in myDrone:GetDescendants() do
                            if part:IsA('BasePart') and part.CanCollide then
                                modified[part] = true
                                part.CanCollide = false
                            end
                        end
                    end
                end))
            else
                for part in modified do
                    part.CanCollide = true
                end
                table.clear(modified)
            end
        end,
        Tooltip = 'turns off collision for your drone - allowing you to phase through walls :D',
    })
end)

run(function()
    local AutoMetal
    local CollectionToggle
    local LimitToItem
    local Animation
    local CollectionDelay
    local DelaySlider
    local RangeSlider
    local ESPToggle
    local ESPNotify
    local ESPBackground
    local ESPColor
    local HoldingCheck
    local DistanceCheck
    local DistanceLimit
    local Folder = Instance.new('Folder')
    Folder.Parent = vape.gui
    local Reference = {}
    local lastNotification = 0
    local notificationPending = false
    local spawnQueue = {}
    local notificationCooldown = 1
    local collectionActive = false
    local collectedMetals = {}
    local animationDebounce = {}

    local function isHoldingAutoMetal()
        if not store.hand or not store.hand.tool then return false end
        return store.hand.tool.Name == 'metal_detector'
    end

    local function sendNotification(count)
        notif("Metal ESP", string.format("%d metals spawned", count), 3)
    end

    local function processSpawnQueue()
        if #spawnQueue == 0 then return end
        local currentTime = tick()
        local remaining = notificationCooldown - (currentTime - lastNotification)
        if remaining <= 0 then
            sendNotification(#spawnQueue)
            lastNotification = currentTime
            spawnQueue = {}
            notificationPending = false
        elseif not notificationPending then
            notificationPending = true
            task.delay(remaining, function()
                if #spawnQueue > 0 then
                    sendNotification(#spawnQueue)
                    lastNotification = tick()
                    spawnQueue = {}
                end
                notificationPending = false
            end)
        end
    end

    local function getProperImage()
        return bedwars.getIcon({itemType = 'iron'}, true)
    end

    local function Added(v)
        if Reference[v] then return end
        local _bpUserId = v:GetAttribute('PlacedByUserId')
        if _bpUserId then
            local _bpOk, _bpOwner = pcall(function() return playersService:GetPlayerByUserId(_bpUserId) end)
        end
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = Folder
        billboard.Name = 'hidden-metal'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(36, 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = v
        
        local blur = addBlur(billboard)
        blur.Visible = ESPBackground.Enabled
        
        local image = Instance.new('ImageLabel')
        image.Size = UDim2.fromOffset(36, 36)
        image.Position = UDim2.fromScale(0.5, 0.5)
        image.AnchorPoint = Vector2.new(0.5, 0.5)
        image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
        image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
        image.BorderSizePixel = 0
        image.Image = getProperImage()
        image.Parent = billboard
        
        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 4)
        uicorner.Parent = image
        
        Reference[v] = billboard
        
        if ESPNotify.Enabled then
            table.insert(spawnQueue, {item = 'metal', time = tick()})
            processSpawnQueue()
        end
    end

    local function Removed(v)
        if Reference[v] then
            Reference[v]:Destroy()
            Reference[v] = nil
        end
    end

    local function setupESP()
        for _, v in collectionService:GetTagged('hidden-metal') do
            if v:IsA("Model") and v.PrimaryPart then
                Added(v.PrimaryPart)
            end
        end

        AutoMetal:Clean(collectionService:GetInstanceAddedSignal('hidden-metal'):Connect(function(v)
            if v:IsA("Model") and v.PrimaryPart then
                Added(v.PrimaryPart)
            end
        end))

        AutoMetal:Clean(collectionService:GetInstanceRemovedSignal('hidden-metal'):Connect(function(v)
            if v.PrimaryPart then
                Removed(v.PrimaryPart)
            end
        end))

        local _mdLastUpdate = 0
        AutoMetal:Clean(runService.RenderStepped:Connect(function()
            if not ESPToggle.Enabled then return end
            local _now = tick()
            if _now - _mdLastUpdate < 0.1 then return end
            _mdLastUpdate = _now
            
            for v, billboard in pairs(Reference) do
                if not v or not v.Parent then
                    Removed(v)
                    continue
                end

                local shouldShow = true

                if HoldingCheck.Enabled and not isHoldingAutoMetal() then
                    shouldShow = false
                end

                if shouldShow and DistanceCheck.Enabled and entitylib.isAlive then
                    local distance = (entitylib.character.RootPart.Position - v.Position).Magnitude
                    if distance < DistanceLimit.ValueMin or distance > DistanceLimit.ValueMax then
                        shouldShow = false
                    end
                end

                billboard.Enabled = shouldShow
            end
        end))
    end

    local function collectMetal(metalModel)
        local metalId = metalModel:GetAttribute('Id')
        if not metalId then return false end
        if collectedMetals[metalId] then return false end

        collectedMetals[metalId] = true

        local success = pcall(function()
            bedwars.Client:Get(remotes.CollectCollectableEntity).instance:FireServer({ id = metalId })
        end)

        if Animation.Enabled then
            local currentTick = tick()
            if not animationDebounce[metalId] or (currentTick - animationDebounce[metalId]) >= 0.5 then
                animationDebounce[metalId] = currentTick
                pcall(function()
                    bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.SHOVEL_DIG)
                    bedwars.SoundManager:playSound(bedwars.SoundList.SNAP_TRAP_CONSUME_MARK)
                end)
            end
        end

        task.delay(2, function()
            collectedMetals[metalId] = nil
            animationDebounce[metalId] = nil
        end)
        
        return success
    end

    local function startAutoCollect()
        if collectionActive then return end
        collectionActive = true
        
        task.spawn(function()
            while AutoMetal.Enabled and CollectionToggle.Enabled and collectionActive do
                if not entitylib.isAlive then 
                    task.wait(0.5)
                    continue 
                end
                
                if LimitToItem.Enabled and not isHoldingAutoMetal() then 
                    task.wait(0.5)
                    continue 
                end
                
                local localPosition = entitylib.character.RootPart.Position
                local range = RangeSlider.Value
                local collectedThisCycle = false
								
				for _, v in collectionService:GetTagged('hidden-metal') do
					if not AutoMetal.Enabled or not CollectionToggle.Enabled or not collectionActive then 
						break 
					end
					
					if v:IsA("Model") and v.PrimaryPart then
						local distance = (localPosition - v.PrimaryPart.Position).Magnitude
						
						if distance <= range then
							if CollectionDelay.Enabled and DelaySlider.Value > 0 then
								task.wait(DelaySlider.Value)
							end
							if collectMetal(v) then
								collectedThisCycle = true
								if Animation.Enabled then
									task.wait(0.15)
								end
							end
						end
					end
				end
                
                task.wait(collectedThisCycle and (Animation.Enabled and 0.3 or 0.05) or 0.5)
            end
            
            collectionActive = false
        end)
    end

    local function stopAutoCollect()
        collectionActive = false
        table.clear(collectedMetals)
        table.clear(animationDebounce)
    end

    AutoMetal = vape.Categories.Kits:CreateModule({
        Name = 'AutoMetal',
        Function = function(callback)
            if callback then
                if ESPToggle.Enabled then 
                    setupESP() 
                end
                if CollectionToggle.Enabled then
                    startAutoCollect()
                end
            else
                stopAutoCollect()
                Folder:ClearAllChildren()
                table.clear(Reference)
                spawnQueue = {}
                lastNotification = 0
                notificationPending = false
            end
        end,
        Tooltip = 'automatically collects metal loot and esp'
    })
    
    CollectionToggle = AutoMetal:CreateToggle({
        Name = 'Auto Collect',
        Default = true,
        Function = function(callback)
            if LimitToItem and LimitToItem.Object then LimitToItem.Object.Visible = callback end
            if Animation and Animation.Object then Animation.Object.Visible = callback end
            if CollectionDelay and CollectionDelay.Object then CollectionDelay.Object.Visible = callback end
            if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = callback end
            if DelaySlider and DelaySlider.Object then
                DelaySlider.Object.Visible = callback and CollectionDelay and CollectionDelay.Enabled
            end
            
            if AutoMetal.Enabled then
                if callback then
                    startAutoCollect()
                else
                    stopAutoCollect()
                end
            end
        end
    })
    
    LimitToItem = AutoMetal:CreateToggle({
        Name = 'Limit to Items',
        Default = true,
    })
    
    Animation = AutoMetal:CreateToggle({
        Name = 'Animation',
        Default = true,
    })
    
    CollectionDelay = AutoMetal:CreateToggle({
        Name = 'Collection Delay',
        Default = false,
        Function = function(callback)
            if DelaySlider and DelaySlider.Object then
                DelaySlider.Object.Visible = callback
            end
        end
    })
    
    DelaySlider = AutoMetal:CreateSlider({
        Name = 'Delay',
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = 's',
        Visible = false,
    })
    
    RangeSlider = AutoMetal:CreateSlider({
        Name = 'Range',
        Min = 1, 
        Max = 10,
        Default = 10,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    ESPToggle = AutoMetal:CreateToggle({
        Name = 'Metal ESP',
        Default = false,
        Function = function(callback)
            if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = callback end
            if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            if HoldingCheck and HoldingCheck.Object then HoldingCheck.Object.Visible = callback end
            if DistanceCheck and DistanceCheck.Object then DistanceCheck.Object.Visible = callback end
            if DistanceLimit and DistanceLimit.Object then
                DistanceLimit.Object.Visible = (callback and DistanceCheck.Enabled)
            end

            if not callback then
                if ESPColor and ESPColor.Object then
                    ESPColor.Object.Visible = false
                end
                if DistanceLimit and DistanceLimit.Object then
                    DistanceLimit.Object.Visible = false
                end
            else
                if ESPBackground and ESPBackground.Enabled then
                    if ESPColor and ESPColor.Object then
                        ESPColor.Object.Visible = true
                    end
                end
                if DistanceCheck and DistanceCheck.Enabled then
                    if DistanceLimit and DistanceLimit.Object then
                        DistanceLimit.Object.Visible = true
                    end
                end
            end
            
            if AutoMetal.Enabled then
                if callback then setupESP() else
                    Folder:ClearAllChildren()
                    table.clear(Reference)
                end
            end
        end
    })
    
    ESPNotify = AutoMetal:CreateToggle({
        Name = 'Notify',
        Default = false,
    })
    
    ESPBackground = AutoMetal:CreateToggle({
        Name = 'Background',
        Default = true,
        Function = function(callback)
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    local blur = v:FindFirstChild("Blur")
                    if blur then blur.Visible = callback end
                    v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
                end
            end
        end
    })
    
    ESPColor = AutoMetal:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                    v.ImageLabel.BackgroundTransparency = 1 - opacity
                end
            end
        end,
        Darker = true
    })
    
    HoldingCheck = AutoMetal:CreateToggle({
        Name = 'Holding Detector',
        Default = false,
    })
    
    DistanceCheck = AutoMetal:CreateToggle({
        Name = 'Distance Check',
        Default = false,
        Function = function(callback)
            if DistanceLimit and DistanceLimit.Object then
                DistanceLimit.Object.Visible = callback
            end
        end
    })
    
    DistanceLimit = AutoMetal:CreateTwoSlider({
        Name = 'Metal Distance',
        Min = 0,
        Max = 256,
        DefaultMin = 0,
        DefaultMax = 64,
        Darker = true,
    })

    task.defer(function()
        if DelaySlider and DelaySlider.Object then
            DelaySlider.Object.Visible = CollectionDelay.Enabled  
        end
        local espOn = ESPToggle and ESPToggle.Enabled
        if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = espOn end
        if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
        if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (espOn and ESPBackground.Enabled) end
        if HoldingCheck and HoldingCheck.Object then HoldingCheck.Object.Visible = espOn end
        if DistanceCheck and DistanceCheck.Object then DistanceCheck.Object.Visible = espOn end
        if DistanceLimit and DistanceLimit.Object then DistanceLimit.Object.Visible = (espOn and DistanceCheck.Enabled) end
    end)
end)

run(function()
	local AutoBekzat
	local Targets
	local Range
	local Delay
	local useAbilityEvent
	local sendFalconRemote
	local falconOut = false
	local lastSend = 0
	local lastStrike = 0

	task.spawn(function()
		task.wait()
		pcall(function()
			useAbilityEvent = replicatedStorage:WaitForChild('events-@easy-games/game-core:shared/game-core-networking@getEvents.Events'):WaitForChild('useAbility')
		end)
		pcall(function()
			sendFalconRemote = replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged:WaitForChild('SendFalconRequested')
		end)
	end)

	local function sendFalcon(pos)
		if useAbilityEvent then
			pcall(function() useAbilityEvent:FireServer('SEND_FALCON', {target = pos}) end)
		end
	end

	local function strikeFalcon(pos)
		if sendFalconRemote then
			pcall(function() sendFalconRemote:FireServer({strikeZoneEpicenter = pos}) end)
		end
	end

	local function recallFalcon()
		if useAbilityEvent then
			pcall(function() useAbilityEvent:FireServer('RECALL_FALCON') end)
		end
	end

	AutoBekzat = vape.Categories.Kits:CreateModule({
		Name = 'AutoBekzat',
		Function = function(callback)
			if callback then
				falconOut = false
				lastSend = 0
				lastStrike = 0
				AutoBekzat:Clean(runService.Heartbeat:Connect(function()
					if not entitylib.isAlive then return end
					local target = entitylib.AllPosition({
						Range = Range.Value,
						Part = 'RootPart',
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Limit = 1,
						Sort = sortmethods['Distance']
					})[1]

					local now = tick()
					if target and target.Character and target.RootPart then
						local exactPos = target.Character:GetPivot().Position
						if not falconOut then
							if (now - lastSend) >= 5 then
								sendFalcon(exactPos)
								falconOut = true
								lastSend = now
								lastStrike = 0
							end
						else
							if (now - lastStrike) >= Delay.Value then
								strikeFalcon(exactPos)
								lastStrike = now
							end
						end
					else
						if falconOut then
							recallFalcon()
							falconOut = false
						end
					end
				end))
			else
				if falconOut then
					recallFalcon()
					falconOut = false
				end
			end
		end,
		Tooltip = 'auto yeets ur falcon at enemies n keeps the strike right on em so it hits instant, calls it back when no ones around gng'
	})

	Targets = AutoBekzat:CreateTargets({
		Players = true,
		NPCs = true
	})

	Range = AutoBekzat:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 300,
		Default = 150,
		Suffix = 'studs'
	})

	Delay = AutoBekzat:CreateSlider({
		Name = 'Strike Delay',
		Min = 0.1,
		Max = 5,
		Default = 0.5,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoMarina
	local Range
	
	AutoMarina = vape.Categories.Kits:CreateModule({
		Name = 'AutoMarina',
		Function = function(call)
			if call then
				local jellies = collection('jellyfish', AutoMarina, function(tab, obj)
					task.delay(0, function()
						if obj:GetAttribute('PlacedByUserId') == lplr.UserId then
							table.insert(tab, obj)
						end
					end)
				end)
				repeat
					if entitylib.isAlive and bedwars.AbilityController:canUseAbility('electrify_jellyfish') then
						for _, v in jellies do
							if v.PrimaryPart then
								if entitylib.EntityPosition({
									Origin = v.PrimaryPart.Position,
									Range = Range.Value,
									Part = 'RootPart',
									Players = true
								}) then
									bedwars.AbilityController:useAbility('electrify_jellyfish')
									break
								end
							end
						end
					end
					task.wait(0.1)
				until not AutoMarina.Enabled
			end
		end,
		Tooltip = 'auto uses ability when players are near yo jelly'
	})
	
	Range = AutoMarina:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 65,
		Default = 50,
		Suffix = function(val)
			return val <= 1 and 'stud' or 'studs'
		end,
	})
end)

run(function()
	local CannonSpeed
	local Value
	
	CannonSpeed = vape.Categories.Kits:CreateModule({
	    Name = 'CannonSpeed',
	    Function = function(callback)
	        debug.setconstant(bedwars.CannonHandController.launchSelf, 15, callback and Value.Value or 200)
	    end,
	    Tooltip = 'go faster with cannon'
	})
	
	Value = CannonSpeed:CreateSlider({
	    Name = 'Speed',
	    Min = 1,
	    Max = 400,
	    Default = 200,
	    Function = function(val)
	        if CannonSpeed.Enabled then
	            debug.setconstant(bedwars.CannonHandController.launchSelf, 15, val)
	        end
	    end,
	    Suffix = function(val)
	        return val <= 1 and 'stud' or 'studs'
	    end
	})
end)

run(function()
	local InfStamina
	local dismountRemote

	task.spawn(function()
		local net = replicatedStorage:WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged')
		dismountRemote = net:WaitForChild('CatWallClimbDismount')
	end)

	InfStamina = vape.Categories.Kits:CreateModule({
		Name = 'InfStamina',
		Function = function(callback)
			if callback then
				InfStamina:Clean(task.spawn(function()
					repeat
						if entitylib.isAlive and dismountRemote then
							pcall(function()
								dismountRemote:FireServer({direction = vector.create(-0, -0, -1)})
							end)
						end
						task.wait(0.25)
					until not InfStamina.Enabled
				end))
			end
		end,
		Tooltip = 'infinite stamina with yamini kit'
	})
end)

run(function()
	local AutoAdetunde
	local PriorityList
	local DelaySlider
	local upgradeNames = {
		'Strength',
		'Speed',
		'Shield',
		'Strength > Speed > Shield',
		'Strength > Shield > Speed',
		'Speed > Strength > Shield',
		'Speed > Shield > Strength',
		'Shield > Strength > Speed',
		'Shield > Speed > Strength'
	}
	local upgradeKeys = {Strength = 'strength', Speed = 'speed', Shield = 'shield'}
	local upgradeIndex = 1
	local upgradeRemote

	task.spawn(function()
		upgradeRemote = game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("UpgradeFrostyHammer")
	end)

	AutoAdetunde = vape.Categories.Kits:CreateModule({
		Name = 'AutoAdetunde',
		Function = function(callback)
			if callback then
				AutoAdetunde:Clean(task.spawn(function()
					repeat
						if upgradeRemote and entitylib.isAlive and store.hand and store.hand.tool and store.hand.tool.Name == 'frosty_hammer' then
							local val = PriorityList.Value
							local order = {}
							for part in tostring(val):gmatch('[^>]+') do
								local trimmed = part:match('^%s*(.-)%s*$')
								if upgradeKeys[trimmed] then
									table.insert(order, upgradeKeys[trimmed])
								end
							end
							if #order > 0 then
								if upgradeIndex > #order then upgradeIndex = 1 end
								local key = order[upgradeIndex]
								pcall(function()
									upgradeRemote:InvokeServer(key)
								end)
								upgradeIndex = upgradeIndex % #order + 1
							end
						end
						task.wait(math.max(DelaySlider.Value, 0.3))
					until not AutoAdetunde.Enabled
				end))
			end
		end,
		Tooltip = 'auto upgrades the pick u chose when u got the crystals'
	})

	PriorityList = AutoAdetunde:CreateDropdown({
		Name = 'Priority',
		List = upgradeNames,
		Default = 'Strength',
	})

	DelaySlider = AutoAdetunde:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.3,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
    local AutoHeph
    local AutoRepair
    local AttackOnRepair

    local COOLDOWN = 6
    local ABILITY_NAMES = {
        'hephaestus_self_repair', 'tinker_self_repair',
        'hephaestus_repair', 'tinker_repair',
        'HEPHAESTUS_SELF_REPAIR', 'TINKER_SELF_REPAIR',
        'HEPHAESTUS_REPAIR', 'TINKER_REPAIR',
        'self_repair', 'SELF_REPAIR',
        'shield_repair', 'SHIELD_REPAIR',
        'kit_repair', 'KIT_REPAIR',
        'forge_repair', 'FORGE_REPAIR',
    }

    local repairingUntil = 0

    local function getKitShield(char)
        local best = 0
        for name, val in char:GetAttributes() do
            if name:find('Shield') and type(val) == 'number' and val > best and name ~= 'Shield_POTION' then
                best = val
            end
        end
        return best
    end

    local function discoverRepairAbilities()
        local found = {}
        pcall(function()
            local ac = bedwars.AbilityController
            for _, v in next, ac do
                if type(v) ~= 'table' then continue end
                for k in next, v do
                    if type(k) == 'string' and k:lower():find('repair') and not table.find(found, k) then
                        table.insert(found, k)
                    end
                end
            end
            for _, fn in {ac.canUseAbility, ac.useAbility, ac.getAbilityCooldown, ac.registerAbility} do
                if type(fn) ~= 'function' then continue end
                for i = 1, 40 do
                    local ok, _, val = pcall(debug.getupvalue, fn, i)
                    if not ok then break end
                    if type(val) ~= 'table' then continue end
                    for k in next, val do
                        if type(k) == 'string' and k:lower():find('repair') and not table.find(found, k) then
                            table.insert(found, k)
                        end
                    end
                end
            end
        end)
        return found
    end

    local function getCharAbilityName()
        local char = lplr.Character
        if char then
            for _, obj in {char, lplr} do
                for attrName, attrVal in obj:GetAttributes() do
                    if type(attrVal) == 'string' and attrVal:lower():find('repair') then
                        return attrVal
                    end
                end
            end
        end
        return nil
    end

    local function fireRepair()
        local ac = bedwars.AbilityController
        local names = discoverRepairAbilities()
        local charAbility = getCharAbilityName()
        if charAbility and not table.find(names, charAbility) then
            table.insert(names, 1, charAbility)
        end
        for _, n in ABILITY_NAMES do
            if not table.find(names, n) then table.insert(names, n) end
        end
        for _, name in names do
            local ok, canUse = pcall(function() return ac:canUseAbility(name) end)
            if ok and canUse then
                pcall(function() ac:useAbility(name) end)
                return true
            end
        end
        return false
    end

    local weaponConfig = nil
    pcall(function()
        local swc = bedwars.SwordController
        for _, fn in {swc.sendServerRequest, swc.swingSwordAtMouse, swc.swingSwordInRegion, swc.isClickingTooFast} do
            if type(fn) ~= 'function' then continue end
            for i = 1, 40 do
                local ok2, weapon, ok3
                local ok, _, val = pcall(debug.getupvalue, fn, i)
                if not ok then break end
                if type(val) ~= 'table' then continue end
                ok2, weapon = pcall(function() return val.Weapon end)
                if not ok2 or type(weapon) ~= 'table' then continue end
                ok3 = pcall(function() return weapon.respectAttackOverride end)
                if ok3 then weaponConfig = val return end
            end
        end
    end)

    local oldIsClickingTooFast = bedwars.SwordController.isClickingTooFast
    local syncConn = nil
    local stunConn = nil
    local charConn = nil

    local function attackUnlocked()
        if not AttackOnRepair or not AttackOnRepair.Enabled then return false end
        return tick() < repairingUntil
    end

    local function connectStunClear(char)
        if stunConn then stunConn:Disconnect() stunConn = nil end
        if not char then return end
        stunConn = char:GetAttributeChangedSignal('StunnedUntilTime'):Connect(function()
            if not attackUnlocked() then return end
            local val = char:GetAttribute('StunnedUntilTime')
            if val and val > workspace:GetServerTimeNow() then
                pcall(function() char:SetAttribute('StunnedUntilTime', 0) end)
            end
        end)
    end

    AutoHeph = vape.Categories.Kits:CreateModule({
        Name = 'AutoHeph',
        Function = function(callback)
            if callback then
                repairingUntil = 0

                bedwars.SwordController.isClickingTooFast = function(self, ...)
                    if attackUnlocked() then
                        if weaponConfig then
                            pcall(function() weaponConfig.Weapon.respectAttackOverride = false end)
                        end
                        return false
                    end
                    return oldIsClickingTooFast(self, ...)
                end

                connectStunClear(lplr.Character)
                charConn = lplr.CharacterAdded:Connect(function(char)
                    connectStunClear(char)
                end)

                pcall(function()
                    local sync = bedwars.ClientSync
                    if sync and type(sync.SwordSwing) == 'table' and type(sync.SwordSwing.setPriority) == 'function' then
                        syncConn = sync.SwordSwing:setPriority(1):Connect(function()
                            if attackUnlocked() and weaponConfig then
                                pcall(function() weaponConfig.Weapon.respectAttackOverride = false end)
                            end
                        end)
                    end
                end)

                local maxShield = 0
                local lastFired = 0
                local prevAlive = false
                local stableVal = -1
                local stableCount = 0

                repeat
                    local alive, char, current, now, cooldownLeft, needsRepair, healPoint
                    task.wait(0.1)

                    if not AutoRepair or not AutoRepair.Enabled then
                        prevAlive = false
                        continue
                    end

                    alive = entitylib.isAlive
                    if not alive then
                        maxShield = 0
                        lastFired = 0
                        prevAlive = false
                        continue
                    end

                    char = lplr.Character
                    if not char then continue end

                    current = getKitShield(char)

                    if current == stableVal then
                        stableCount = stableCount + 1
                    else
                        stableVal = current
                        stableCount = 1
                    end

                    if not prevAlive and current > 0 then
                        maxShield = current
                    elseif stableCount >= 2 and current > maxShield then
                        maxShield = current
                    end
                    prevAlive = true

                    now = tick()
                    cooldownLeft = math.max(0, COOLDOWN - (now - lastFired))
                    healPoint = maxShield * 0.5
                    needsRepair = maxShield > 0 and current <= healPoint

                    if cooldownLeft <= 0 and needsRepair then
                        if fireRepair() then
                            repairingUntil = now + 2
                            lastFired = now
                        end
                    end

                until not AutoHeph.Enabled
            else
                repairingUntil = 0
                bedwars.SwordController.isClickingTooFast = oldIsClickingTooFast
                if stunConn then stunConn:Disconnect() stunConn = nil end
                if charConn then charConn:Disconnect() charConn = nil end
                if syncConn then
                    pcall(function() syncConn:Disconnect() end)
                    syncConn = nil
                end
            end
        end,
        Tooltip = 'fixes ur heph shield by itself when it gets low so u dont gotta think about it'
    })

    AutoRepair = AutoHeph:CreateToggle({
        Name = 'Auto Repair',
        Default = true,
        Tooltip = 'turns the auto fixing on. leave this on gng'
    })

    AttackOnRepair = AutoHeph:CreateToggle({
        Name = 'Attack on Repair',
        Default = true,
        Tooltip = 'lets u keep swingin while ur shield is fixing instead of getting frozen'
    })
end)

run(function()
    local AutoWhisper
    local PlayerDropdown
    local AutoHeal
    local AutoHealSlider
    local AutoFly
    local LimitToItem
    local RefreshButton
    local running = false
    local healRunning = false
    local flyRunning = false
    local currentTarget = nil
    local currentMountedPlayer = nil
    local fallCheckTimer = 0
    local hasActivatedFly = false
    
    local function isHoldingOwlOrb()
        if not entitylib.isAlive then return false end
        
        local inventory = store.inventory
        if inventory and inventory.inventory and inventory.inventory.hand then
            local handItem = inventory.inventory.hand
            if handItem and handItem.itemType == "owl_orb" then
                return true
            end
        end
        return false
    end
    
    local function getMountedPlayer()
        local owlTarget = lplr:GetAttribute('OwlTarget')
        if owlTarget then
            return playersService:GetPlayerByUserId(owlTarget)
        end
        return nil
    end
    
    local function mountBirdToPlayer(targetPlayer)
        if not targetPlayer or not targetPlayer.Character then return false end
        
        if LimitToItem.Enabled and not isHoldingOwlOrb() then
            return false
        end
        
        local success = false
        pcall(function()
            local result = bedwars.Client:Get(remotes.SummonOwl).instance:InvokeServer(targetPlayer)
            
            if result then
            task.wait(0.05)
            
            pcall(function()
    			bedwars.Client:Get(remotes.UseAbility).instance:FireServer("SUMMON_OWL")
			end)
                
                currentMountedPlayer = targetPlayer
                success = true
            end
        end)
        
        return success
    end
    
    local function demountOwl()
        pcall(function()
            bedwars.Client:Get(remotes.UseAbility).instance:FireServer("DEACTIVE_OWL")
            
            task.wait(0.05)
            
            bedwars.Client:Get(remotes.RemoveOwl).instance:FireServer()
        end)
        
        currentMountedPlayer = nil
    end
    
    local function healTarget()
        pcall(function()
            replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility"):FireServer("OWL_HEAL")
        end)
    end
    
    local function isFalling(player)
        if not player or not player.Character or not player.Character.PrimaryPart then
            return false
        end
        
        local velocity = player.Character.PrimaryPart.AssemblyLinearVelocity.Y
        return velocity < -20
    end
    
	local voidRayParams = RaycastParams.new()
	voidRayParams.FilterType = Enum.RaycastFilterType.Blacklist
	voidRayParams.RespectCanCollide = true

	local function isAboveVoid(player)
		if not player or not player.Character or not player.Character.PrimaryPart then
			return false
		end
		
		local rayOrigin = player.Character.PrimaryPart.Position
		local rayDirection = Vector3.new(0, -1000, 0)
		
		voidRayParams.FilterDescendantsInstances = {player.Character, gameCamera}
		
		local rayResult = workspace:Raycast(rayOrigin, rayDirection, voidRayParams)
		
		if not rayResult then
			return true
		end
		
		return rayResult.Distance > 200
	end
    
    local function activateFly()
        pcall(function()
            replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility"):FireServer("OWL_LIFT")
            
            hasActivatedFly = true
            task.spawn(function()
                task.wait(85)
                hasActivatedFly = false
            end)
        end)
    end
    
    AutoWhisper = vape.Categories.Kits:CreateModule({
        Name = "AutoWhisper",
        Function = function(callback)
            running = callback
            healRunning = callback
            flyRunning = callback
            
            if callback then
                task.spawn(function()
                    while running do
                        if LimitToItem.Enabled and not isHoldingOwlOrb() then
                            task.wait(0.2)
                            continue
                        end
                        
                        local targetPlayer = playersService:FindFirstChild(PlayerDropdown.Value)
                        if targetPlayer then
                            currentTarget = targetPlayer
                            
                            local mountedTo = getMountedPlayer()
                            
                            if mountedTo ~= targetPlayer then
                                if mountedTo and mountedTo ~= targetPlayer then
                                    demountOwl()
                                    task.wait(0.3)
                                end
                                
                                if not mountedTo or mountedTo ~= targetPlayer then
                                    local success = mountBirdToPlayer(targetPlayer)
                                    if not success then
                                        task.wait(0.5)
                                    else
                                        task.wait(1)
                                    end
                                end
                            else
                                task.wait(0.5)
                            end
                        else
                            task.wait(0.5)
                        end
                    end
                end)
                
                if AutoHeal.Enabled then
                    task.spawn(function()
                        while healRunning and AutoHeal.Enabled do
                            if currentTarget then
                                local health, maxHealth = getPlayerHealth(currentTarget)
                                if health and maxHealth and maxHealth > 0 then
                                    local healthPercent = (health / maxHealth) * 100
                                    if healthPercent < AutoHealSlider.Value and healthPercent < 90 then
                                        healTarget()
                                        task.wait(8.5)
                                    end
                                end
                            end
                            
                            task.wait(0.5)
                        end
                    end)
                end
                
                if AutoFly.Enabled then
                    task.spawn(function()
                        while flyRunning and AutoFly.Enabled do
                            if currentTarget and not hasActivatedFly then
                                if isFalling(currentTarget) and isAboveVoid(currentTarget) then
                                    fallCheckTimer = fallCheckTimer + 0.1
                                    
                                    if fallCheckTimer >= 0.5 then
                                        activateFly()
                                        fallCheckTimer = 0
                                    end
                                else
                                    fallCheckTimer = 0
                                end
                            else
                                fallCheckTimer = 0
                            end
                            
                            task.wait(0.1)
                        end
                    end)
                end
                
                AutoWhisper:Clean(playersService.PlayerAdded:Connect(function()
                    task.wait(0.5)
                    local newList = getTeammates(true)
                    if PlayerDropdown then
                        PlayerDropdown:Change(newList)
                        
                        if #newList > 0 then
                            if not PlayerDropdown.Value or PlayerDropdown.Value == "" or not table.find(newList, PlayerDropdown.Value) then
                                PlayerDropdown:SetValue(newList[1])
                            end
                        end
                    end
                end))
                
                AutoWhisper:Clean(playersService.PlayerRemoving:Connect(function(player)
                    task.wait(0.5)
                    local newList = getTeammates(true)
                    if PlayerDropdown then
                        PlayerDropdown:Change(newList)
                        
                        if #newList > 0 then
                            if not PlayerDropdown.Value or PlayerDropdown.Value == "" or not table.find(newList, PlayerDropdown.Value) then
                                PlayerDropdown:SetValue(newList[1])
                            end
                        end
                    end
                    
                    if currentTarget == player then
                        currentTarget = nil
                        currentMountedPlayer = nil
                    end
                end))
                
                AutoWhisper:Clean(lplr:GetAttributeChangedSignal('Team'):Connect(function()
                    task.wait(0.5)
                    local newList = getTeammates(true)
                    if PlayerDropdown then
                        PlayerDropdown:Change(newList)
                        
                        if #newList > 0 then
                            if not PlayerDropdown.Value or PlayerDropdown.Value == "" or not table.find(newList, PlayerDropdown.Value) then
                                PlayerDropdown:SetValue(newList[1])
                            end
                        end
                    end
                    currentTarget = nil
                    currentMountedPlayer = nil
                    hasActivatedFly = false
                end))
                
            else
                running = false
                healRunning = false
                flyRunning = false
                currentTarget = nil
                currentMountedPlayer = nil
                hasActivatedFly = false
                fallCheckTimer = 0
            end
        end,
    })
    
    PlayerDropdown = AutoWhisper:CreateDropdown({
        Name = "Bird Target",
        List = {},
        Function = function(val)
            if val then
                local targetPlayer = playersService:FindFirstChild(val)
                if targetPlayer then
                    currentTarget = targetPlayer
                end
            end
        end,
    })
    RefreshButton = AutoWhisper:CreateButton({
        Name = "Refresh Teammates",
        Function = function()
            task.spawn(function()
                local newList = getTeammates(true)
                
                if PlayerDropdown then
                    pcall(function()
                        PlayerDropdown:Change(newList)
                        
                        if #newList > 0 then
                            if not PlayerDropdown.Value or PlayerDropdown.Value == "" or not table.find(newList, PlayerDropdown.Value) then
                                PlayerDropdown:SetValue(newList[1])
                            else
                                PlayerDropdown:SetValue(PlayerDropdown.Value)
                            end
                        end
                    end)
                end
                
                notif("AutoWhisper", string.format("teammate list (%d teammates)", #newList), 2)
            end)
        end,
    })
    
    LimitToItem = AutoWhisper:CreateToggle({
        Name = "Limit to Owl Orb",
        Default = true,
        Function = function(val)
        end,
    })

    AutoFly = AutoWhisper:CreateToggle({
        Name = "Auto Fly",
        Default = true,
        Function = function(val)
            if AutoWhisper.Enabled then
                if val then
                    flyRunning = true
                    hasActivatedFly = false
                    fallCheckTimer = 0
                    
                    task.spawn(function()
                        while flyRunning and AutoFly.Enabled do
                            if currentTarget and not hasActivatedFly then
                                if isFalling(currentTarget) and isAboveVoid(currentTarget) then
                                    fallCheckTimer = fallCheckTimer + 0.1
                                    
                                    if fallCheckTimer >= 0.5 then
                                        activateFly()
                                        fallCheckTimer = 0
                                    end
                                else
                                    fallCheckTimer = 0
                                end
                            else
                                fallCheckTimer = 0
                            end
                            
                            task.wait(0.1)
                        end
                    end)
                else
                    flyRunning = false
                    hasActivatedFly = false
                    fallCheckTimer = 0
                end
            end
        end,
    })
    
    AutoHeal = AutoWhisper:CreateToggle({
        Name = "Auto Heal",
        Default = true,
        Function = function(val)
            if AutoHealSlider and AutoHealSlider.Object then
                AutoHealSlider.Object.Visible = val
            end
            
            if AutoWhisper.Enabled then
                if val then
                    healRunning = true
                    task.spawn(function()
                        while healRunning and AutoHeal.Enabled do
                            if currentTarget then
                                local health, maxHealth = getPlayerHealth(currentTarget)
                                if not (health and maxHealth and maxHealth > 0) then task.wait(0.5) continue end
                                local healthPercent = (health / maxHealth) * 100
                                if healthPercent < AutoHealSlider.Value and healthPercent < 90 then
                                    healTarget()
                                    task.wait(8.5)
                                end
                            end
                            
                            task.wait(0.5)
                        end
                    end)
                else
                    healRunning = false
                end
            end
        end,
    })
    
    AutoHealSlider = AutoWhisper:CreateSlider({
        Name = "Heal Threshold",
        Min = 1,
        Max = 100,
        Default = 50,
        Suffix = "%",
    })
end)

run(function()
	local CogsworthExploit
	local remote = game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("GatherBotCollectItems")

	local bots = {}
	local conns = {}

	local function isBot(obj)
		return obj.Name == 'GatherBotBasic' or obj.Name == 'GatherBotPro'
	end

	local function trackBot(obj)
		if bots[obj] then return end
		bots[obj] = true
		local conn
		conn = obj.AncestryChanged:Connect(function()
			if not obj.Parent then
				bots[obj] = nil
				if conns[obj] then conns[obj]:Disconnect() conns[obj] = nil end
			end
		end)
		conns[obj] = conn
	end

	CogsworthExploit = vape.Categories.Kits:CreateModule({
		Name = 'CogsworthExploit',
		Tooltip = 'auto collects loot from every gather bot in the server not just yours',
		Function = function(callback)
			if callback then
				table.clear(bots)
				scanDescendants(workspace, function(obj)
					if isBot(obj) then trackBot(obj) end
				end, CogsworthExploit)
				local addedConn = workspace.DescendantAdded:Connect(function(obj)
					if isBot(obj) then trackBot(obj) end
				end)
				CogsworthExploit:Clean(addedConn)

				repeat
					for obj, _ in bots do
						if not CogsworthExploit.Enabled then break end
						pcall(function()
							remote:FireServer({
								player = lplr,
								gatherBot = obj
							})
						end)
					end
					task.wait(0.5)
				until not CogsworthExploit.Enabled

				for _, conn in conns do conn:Disconnect() end
				table.clear(conns)
				table.clear(bots)
			end
		end
	})
end)

run(function()
	local AutoZola
	local Range
	local Delay
	local soulLinkRemote
	local useAbilityRemote

	local function getRemotes()
		if not soulLinkRemote then
			pcall(function()
				soulLinkRemote = replicatedStorage:WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("AttemptSoulLink")
			end)
		end
		if not useAbilityRemote then
			pcall(function()
				useAbilityRemote = replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility")
			end)
		end
	end

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Range and Delay
			getRemotes()
			local linkedTargets = {}
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local plr = entitylib.EntityPosition({
					Range = Range.Value,
					Part = "RootPart",
					Players = true,
					NPCs = true,
					Wallcheck = false
				})
				local linkCount = 0
				for _ in linkedTargets do linkCount += 1 end
				if plr and plr.Character and linkCount < 8 and soulLinkRemote then
					local char = plr.Character
					if not linkedTargets[char] then
						if useAbilityRemote then
							pcall(function() useAbilityRemote:FireServer("soul_link") end)
						end
						task.wait(0.1)
						local ok, result = pcall(function()
							return soulLinkRemote:InvokeServer(char)
						end)
						if ok and result and result.result then
							linkedTargets[char] = true
							char.AncestryChanged:Connect(function()
								if not char.Parent then
									linkedTargets[char] = nil
								end
							end)
						end
					end
				end
				task.wait(Delay.Value)
			until not AutoZola.Enabled
			linkedTargets = {}
		end)
	end

	AutoZola = vape.Categories.Kits:CreateModule({
		Name = 'AutoZola',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto soul links enemies for zola'
	})

	Range = AutoZola:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 60,
		Default = 50
	})
	Delay = AutoZola:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.3,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoWarrior
	local Targets
	local Range
	local Delay
	local useAbilityRemote

	local function getAbilityRemote()
		if not useAbilityRemote then
			pcall(function()
				useAbilityRemote = replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility")
			end)
		end
		return useAbilityRemote
	end

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local plr = entitylib.EntityPosition({
					Range = Range.Value,
					Part = 'RootPart',
					Players = Targets.Players.Enabled,
					NPCs = Targets.NPCs.Enabled,
					Wallcheck = Targets.Walls.Enabled,
					Sort = sortmethods.Distance
				})
				local grit = lplr:GetAttribute('Grit') or 0
				if plr and grit >= 100 then
					local remote = getAbilityRemote()
					if remote then
						pcall(function() remote:FireServer('warrior_strike') end)
					end
				end
				task.wait(Delay.Value)
			until not AutoWarrior.Enabled
		end)
	end

	AutoWarrior = vape.Categories.Kits:CreateModule({
		Name = 'AutoWarrior',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto strikes enemies when ur grit is full'
	})

	Targets = AutoWarrior:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoWarrior:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 20
	})
	Delay = AutoWarrior:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local AutoLasso
	local Targets
	local Range
	local Angle
	local LimitToLasso
	local ArrowCharge

	local projectileRemote = {InvokeServer = function() end}
	task.spawn(function()
		projectileRemote = bedwars.Client:Get(remotes.FireProjectile).instance
	end)
	local handGunRemote = {InvokeServer = function() end}
	task.spawn(function()
		handGunRemote = game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("HandGunFireRequest")
	end)

	local rayCheck = RaycastParams.new()
	local lastShot = 0
	local autoLassoLoop

	local function doLasso()
		if not entitylib.isAlive then return end
		if LimitToLasso.Enabled then
			if not store.hand or not store.hand.tool or store.hand.tool.Name ~= 'lasso' then
				return
			end
		end

		if (tick() - lastShot) < FireRate.Value then return end

		local lasso = getItem('lasso')
		if not lasso then return end

		local ent = entitylib.EntityPosition({
			Range = Range.Value,
			Part = 'RootPart',
			Wallcheck = Targets.Walls.Enabled,
			Players = Targets.Players.Enabled,
			NPCs = Targets.NPCs.Enabled,
			Sort = sortmethods.Distance
		})
		if not ent then return end

		local selfpos = entitylib.character.RootPart.Position
		local localfacing = gameCamera.CFrame.LookVector * Vector3.new(1, 0, 1)
		local delta = (ent.RootPart.Position - selfpos) * Vector3.new(1, 0, 1)
		if delta.Magnitude <= 0.001 then return end
		if math.acos(math.clamp(localfacing:Dot(delta.Unit), -1, 1)) > (math.rad(Angle.Value) / 2) then return end

		local calc = prediction.SolveTrajectory(selfpos, 200, 135, ent.RootPart.Position, ent.RootPart.Velocity, workspace.Gravity, ent.Humanoid.HipHeight or 2, nil, rayCheck)
		if not calc then return end

		switchItem(lasso.tool, 0.05)

		local res = projectileRemote:InvokeServer(
			lasso.tool,
			'lasso',
			'lasso',
			selfpos,
			selfpos,
			CFrame.lookAt(selfpos, calc).LookVector * 200,
			httpService:GenerateGUID(true),
			{
				drawDurationSeconds = 1,
				shotId = httpService:GenerateGUID(false)
			},
			workspace:GetServerTimeNow() - 0.045
		)
		if res then
			lastShot = tick()
		end
	end

	local function startAutoLassoLoop()
		if autoLassoLoop then return end
		autoLassoLoop = task.spawn(function()
			while AutoLasso and AutoLasso.Enabled do
				doLasso()
				task.wait(0.05)
			end
			autoLassoLoop = nil
		end)
	end

	local function stopAutoLassoLoop()
		if autoLassoLoop then
			task.cancel(autoLassoLoop)
			autoLassoLoop = nil
		end
	end

	AutoLasso = vape.Categories.Kits:CreateModule({
		Name = 'AutoLasso',
		Function = function(callback)
			if callback then
				startAutoLassoLoop()
			else
				stopAutoLassoLoop()
			end
		end
	})

	Targets = AutoLasso:CreateTargets({Players = true})
	Range = AutoLasso:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 60,
		Default = 60,
		Suffix = function(val)
			return val <= 1 and 'stud' or 'studs'
		end
	})
	Angle = AutoLasso:CreateSlider({
		Name = 'Max angle',
		Min = 1,
		Max = 360,
		Default = 120
	})
	FireRate = AutoLasso:CreateSlider({
		Name = 'Fire rate',
		Min = 0,
		Max = 15,
		Default = 10.5,
		Decimal = 10,
		Suffix = 's'
	})
	LimitToLasso = AutoLasso:CreateToggle({
		Name = 'Limit to Lasso',
		Default = false,
	})
end)

run(function()
	local InfiniteSigrid
	local Kit = 'elk_master'
	local running = false

	InfiniteSigrid = vape.Categories.Kits:CreateModule({
		Name = 'InfiniteSigrid',
		Tooltip = 'ride yo shitty ass elk forever',
		Function = function(call)
			if call then
				if running then return end
				running = true
				task.spawn(function()
					repeat
						if entitylib.isAlive then
							if store.equippedKit == Kit then
								local Mount = bedwars.Client:Get('ElkKitMounted')
								if Mount then
									Mount:SendToServer()
								end
							end
						end
						task.wait(0.1)
					until not InfiniteSigrid.Enabled or not running
					running = false
				end)
			else
				running = false
			end
		end
	})
end)

run(function()
	local AutoFortify

	local function getOwnBedPosition()
		local playerTeam = lplr:GetAttribute('Team')
		if not playerTeam then return nil end
		local tagged = collectionService:GetTagged('bed')
		for _, bed in ipairs(tagged) do
			if bed:GetAttribute('Team'..playerTeam..'NoBreak') then
				return bed.Position
			end
		end
		return nil
	end

	local function fortifyAround(start)
		for x = -3, 3 do
			for y = -3, 3 do
				for z = -3, 3 do
					local offset = Vector3.new(x * 3, y * 3, z * 3)
					local block, roundedPos = getPlacedBlock(start + offset)
					if block and block.Name ~= 'bed' then
						pcall(function()
							game:GetService("ReplicatedStorage"):WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("FortifyBlock"):FireServer(roundedPos)
						end)
					end
				end
			end
		end
	end

	AutoFortify = vape.Categories.Kits:CreateModule({
		Name = 'AutoFortify',
		Function = function(callback)
			if not callback then return end
			task.spawn(function()
				repeat
					local bedPos = getOwnBedPosition()
					if bedPos then
						fortifyAround(bedPos)
						fortifyAround(bedPos + Vector3.new(0,0,3))
					end
					task.wait(2)
				until not AutoFortify or not AutoFortify.Enabled
			end)
		end,
		Tooltip = 'auto fortifies blocks around your bed from anywhere'
	})
end)

run(function()
	local MinerSpy
	local IgnoreTeammatesMiner

	MinerSpy = vape.Categories.Kits:CreateModule({
		Name = "MinerSpy",
		Tooltip = 'notifies when players get petrified or mined by a miner',
		Function = function(callback)
			if callback then
				MinerSpy:Clean(bedwars.Client:OnEvent("Petrified", function(tbl)
					if not MinerSpy.Enabled then return end
					local victim = playersService:GetPlayerFromCharacter(tbl.to)
					local killer = playersService:GetPlayerFromCharacter(tbl.killer)
					if not victim then return end

					if IgnoreTeammatesMiner.Enabled then
						if victim.Team == lplr.Team then return end
					end

					local victimName = victim.Name
					victimName = victimName:sub(1, 1):upper() .. victimName:sub(2)

					local killerText = ''
					if killer then
						local killerName = killer.Name
						killerName = killerName:sub(1, 1):upper() .. killerName:sub(2)
						killerText = ' by ' .. killerName
					end

					notif("MinerSpy", victimName .. " was petrified" .. killerText, 8)
				end))

				MinerSpy:Clean(bedwars.Client:OnEvent("PetrifiedPlayerDestroyed", function(tbl)
					if not MinerSpy.Enabled then return end
					local destroyer = tbl.destroyer

					local destroyerText = 'Unknown'
					if destroyer then
						local destroyerName = destroyer.Name
						destroyerText = destroyerName:sub(1, 1):upper() .. destroyerName:sub(2)
						if IgnoreTeammatesMiner.Enabled and destroyer.Team == lplr.Team then return end
					end

					local rewardText = ''
					if tbl.rewards then
						local totals = {}
						local order = {}
						for _, reward in ipairs(tbl.rewards) do
							local item = tostring(reward.itemType) or 'unknown'
							local amount = tonumber(reward.amount) or 0
							if not totals[item] then
								totals[item] = 0
								table.insert(order, item)
							end
							totals[item] = totals[item] + amount
						end
						local parts = {}
						for _, item in ipairs(order) do
							table.insert(parts, item .. ' x' .. totals[item])
						end
						if #parts > 0 then
							rewardText = ' | Loot: ' .. table.concat(parts, ', ')
						end
					end

					notif("MinerSpy", destroyerText .. " mined a petrified player" .. rewardText, 8)
				end))
			end
		end
	})
	IgnoreTeammatesMiner = MinerSpy:CreateToggle({Name='Ignore Teammates',Default=true})
end)

run(function()
	local AutoVulcan
	local Targets
	local ArrowCharge
	local turretfiretime = {}
	local netman = game:GetService("ReplicatedStorage").rbxts_include.node_modules["@rbxts"].net.out._NetManaged
	local turretTargets = {}
	local Sort
	local AimTurretRemote = netman:FindFirstChild("AimTurret")
	local PFR = netman:FindFirstChild("ProjectileFire")
	local turretthread = {}
	local Distance
	local PHR = netman:FindFirstChild("ProjectileHit")
	local RTR = netman:FindFirstChild("RaycastTurret")
	local LimitTablet

	local function tableFind(tab, val)
		for _, v in ipairs(tab) do
			if v == val then return true end
		end
		return false
	end

	local function isHoldingTablet()
		local hand = store.hand
		return hand and hand.tool and hand.tool.Name and hand.tool.Name:find('tablet') ~= nil
	end

	local function getOwnedTurrets()
		local turrets = {}
		for _, block in ipairs(store.blocks or {}) do
			if block.Name == "camera_turret"
				and block:GetAttribute("PlacedByUserId") == lplr.UserId
				and block.Parent
			then
				table.insert(turrets, block)
			end
		end
		return turrets
	end

	local function isTargetValid(ent, turretPos)
		if not ent or not ent.Character then return false end
		if not ent.RootPart or not ent.RootPart.Parent then return false end
		if (ent.Character:GetAttribute("Health") or 0) <= 0 then return false end
		if (turretPos - ent.RootPart.Position).Magnitude > (Distance and Distance.Value or 1000) then return false end
		return true
	end

	local function getTarget(turret)
		local turretPos = turret.Position
		local locked = turretTargets[turret]
		if isTargetValid(locked, turretPos) then
			return locked
		end

		local ent = entitylib.EntityPosition({
			Origin = turretPos,
			Range = Distance and Distance.Value or 1000,
			Part = "RootPart",
			Players = Targets and Targets.Players.Enabled or true,
			NPCs = Targets and Targets.NPCs.Enabled or true,
			Sort = sortmethods and Sort and sortmethods[Sort.Value] or nil
		})

		turretTargets[turret] = ent
		return ent
	end

	local turretBlockPosCache = {}

	local function fireTurret(turret)
		if not turret or not turret.Parent then return end
		if LimitTablet and LimitTablet.Enabled and not isHoldingTablet() then return end

		local turretPos = turret.Position
		local blockPos = turretBlockPosCache[turret]
		if not blockPos then
			blockPos = bedwars.BlockController:getBlockPosition(turretPos)
			turretBlockPosCache[turret] = blockPos
		end
		local ent = getTarget(turret)
		if not ent or not ent.RootPart then return end

		local char = ent.Character
		local torso = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") or ent.RootPart
		local targetVel = ent.RootPart.AssemblyLinearVelocity or Vector3.zero
		local travel = (torso.Position - turretPos).Magnitude / 300
		local targetPos = torso.Position + targetVel * travel

		local lookDir = targetPos - turretPos
		if lookDir.Magnitude < 0.1 then return end
		lookDir = lookDir.Unit

		local flatMag = Vector3.new(lookDir.X, 0, lookDir.Z).Magnitude
		local yaw = math.atan2(-lookDir.X, -lookDir.Z)
		local pitch = math.atan2(lookDir.Y, flatMag > 0 and flatMag or 0.001)

		if AimTurretRemote then
			pcall(function()
				AimTurretRemote:FireServer({
					angleX = math.deg(yaw),
					angleY = math.deg(pitch),
					turretBlockPos = blockPos,
				})
			end)
		end

		local now = tick()
		if (now - (turretfiretime[turret] or 0)) < (FireRate and FireRate.Value or 0.05) then return end
		turretfiretime[turret] = now

		local fireId = httpService:GenerateGUID(false):sub(1, 8)
		local shotId = httpService:GenerateGUID(false):sub(1, 8)
		local targetChar = ent.Character
		local shootOrigin = turretPos + lookDir * 2 + Vector3.new(0, 1, 0)
		local velocity = lookDir * 300

		task.spawn(function()
			local firedId
			local myKit = lplr.Character and lplr.Character:GetAttribute('Kit')
			if PFR and myKit ~= 'block-kicker' then
				pcall(function()
					firedId = PFR:InvokeServer(
						turret, nil, "turretBullet",
						shootOrigin, turretPos, velocity,
						fireId,
						{ shotId = shotId, drawDurationSec = 0 },
						workspace:GetServerTimeNow() - 0.045
					)
				end)
			elseif RTR then
				pcall(function()
					local targetDir = (targetPos - turretPos).Unit * 500
					RTR:FireServer(turret, turretPos + Vector3.new(0, 1, 0), targetDir, blockPos)
				end)
			end

			if not firedId then
				turretTargets[turret] = nil
				return
			end

			task.wait(0.05)
			if not targetChar or not targetChar.Parent then return end
			if (targetChar:GetAttribute("Health") or 0) <= 0 then return end

			if PHR then
				pcall(function() PHR:FireServer(firedId, targetChar) end)
				task.wait(0.02)
				if targetChar and targetChar.Parent and (targetChar:GetAttribute("Health") or 0) > 0 then
					pcall(function() PHR:FireServer(firedId, targetChar) end)
				end
			end
		end)
	end

	local function startTurretLoop(turret)
		if turretthread[turret] then return end

		turretthread[turret] = task.spawn(function()
			while AutoVulcan.Enabled and turret and turret.Parent do
				if entitylib.isAlive then
					pcall(fireTurret, turret)
				end
				task.wait(0.05)
			end
			turretthread[turret] = nil
			turretTargets[turret] = nil
			turretfiretime[turret] = nil
		end)
	end

	local function stopAllLoops()
		for _, thread in pairs(turretthread) do
			pcall(task.cancel, thread)
		end
		table.clear(turretthread)
		table.clear(turretTargets)
		table.clear(turretfiretime)
		table.clear(turretBlockPosCache)
	end

	AutoVulcan = vape.Categories.Kits:CreateModule({
		Name = 'AutoVulcan',
		Function = function(callback)
			if callback then
				AutoVulcan:Clean(task.spawn(function()
					while AutoVulcan.Enabled do
						local turrets = getOwnedTurrets()
						for _, turret in ipairs(turrets) do
							startTurretLoop(turret)
						end
						for turret, thread in pairs(turretthread) do
							if not turret.Parent then
								pcall(task.cancel, thread)
								turretthread[turret] = nil
								turretTargets[turret] = nil
								turretfiretime[turret] = nil
							end
						end
						task.wait(1)
					end
				end))
				AutoVulcan:Clean(stopAllLoops)
			else
				stopAllLoops()
			end
		end,
	})

	Targets = AutoVulcan:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = false,
	})

	FireRate = AutoVulcan:CreateSlider({
		Name = 'Fire Rate',
		Min = 0,
		Max = 3,
		Default = 0,
		Tooltip = 'Shots per second',
	})

	Distance = AutoVulcan:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 1000,
		Default = 1000,
		Suffix = function(val) return val == 1 and 'stud' or 'studs' end,
	})

	Sort = AutoVulcan:CreateDropdown({
		Name = 'Target mode',
		List = getSortList({'Distance', 'Health', 'Cursor'}),
		Default = 'Distance',
	})

	LimitTablet = AutoVulcan:CreateToggle({
		Name = 'Limit to tablet',
		Default = false,
		Tooltip = 'only works when u holdin the tablet'
	})
end)

run(function()
	local InstantJade
	local old = nil
	
    InstantJade = vape.Categories.Kits:CreateModule({
		Name = "InstantJade",
		Tooltip = "no cooldown on the jade hammer",
		Function = function(callback)
			if callback then
				if old then return end
				old = bedwars.CooldownController.isOnCooldown
				bedwars.CooldownController.isOnCooldown = function(self, id)
					if id ~= nil and string.find(tostring(id):lower(), 'jade', 1, true) then
						return false
					end
					if type(old) ~= 'function' then return false end
					return old(self, id)
				end
			else
				if old then
					bedwars.CooldownController.isOnCooldown = old
					old = nil
				end
			end
		end
	})
end)

run(function()
	local InstantRegent
	local old = nil
	
	InstantRegent = vape.Categories.Kits:CreateModule({
		Name = "InstantRegent",
		Tooltip = "no cooldown on the void axe spam",
		Function = function(callback)
			if callback then
				if old then return end
				old = bedwars.CooldownController.isOnCooldown
				bedwars.CooldownController.isOnCooldown = function(self, id)
					if id ~= nil and string.find(tostring(id):lower(), 'void_axe', 1, true) then
						return false
					end
					if type(old) ~= 'function' then return false end
					return old(self, id)
				end
			else
				if old then
					bedwars.CooldownController.isOnCooldown = old
					old = nil
				end
			end
		end
	})
end)

run(function()
	local YuziDasher
	local ImpulseSlider
	local JumpHeightSlider
	local CurrentKeybind = Enum.KeyCode.Q

	local canDash = true

	local function PerformDash()
		if not canDash then return end
		if not entitylib.isAlive then return end

		local heldItem = store.hand.tool
		if not heldItem or not (heldItem.Name:find("dao") or heldItem.Name:find("yuzi") or heldItem.Name == "jade_hammer" or heldItem.Name == "void_axe") then return end

		local abilityId = (heldItem.Name == "jade_hammer" and "JADE_HAMMER_JUMP") or (heldItem.Name == "void_axe" and "VOID_AXE_JUMP") or nil

		local character = lplr.Character
		if not (character and character.PrimaryPart) then return end

		canDash = false

		task.spawn(function()
			local originalJumpHeight = character.Humanoid.JumpHeight

			pcall(function() character:SetAttribute('CanDash', 0) end)

			local lookVector = gameCamera.CFrame.LookVector
			local origin = character.PrimaryPart.Position

			pcall(function()
				local n = game:GetService("ReplicatedStorage"):FindFirstChild("rbxts_include")
				if n then n = n:FindFirstChild("node_modules") end
				if n then n = n:FindFirstChild("@rbxts") end
				if n then n = n:FindFirstChild("net") end
				if n then n = n:FindFirstChild("out") end
				if n then n = n:FindFirstChild("_NetManaged") end
				if n then n = n:FindFirstChild("SwordSwingMiss") end
				if n then n:FireServer({ weapon = heldItem, chargeRatio = 0 }) end
			end)

			task.wait(0.05)

			local useDash = abilityId == nil
			local canUse = useDash and bedwars.AbilityController:canUseAbility('dash') or (abilityId and bedwars.AbilityController:canUseAbility(abilityId))

			if canUse then
				if useDash then
					bedwars.AbilityController:useAbility('dash', nil, {
						direction = lookVector,
						origin = origin,
						weapon = heldItem.Name
					})
				else
					bedwars.AbilityController:useAbility(abilityId)
				end

				pcall(function()
					bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.DAO_DASH)
				end)

				pcall(function()
					local hrp = character.HumanoidRootPart
					local mass = hrp.AssemblyMass or 5
					hrp:ApplyImpulse(lookVector.Unit * Vector3.new(1, 0, 1) * mass * ImpulseSlider.Value)
					character.Humanoid.JumpHeight = JumpHeightSlider.Value
					character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
				end)

				task.delay(0.5, function()
					if character and character.Humanoid then
						pcall(function()
							character.Humanoid.JumpHeight = originalJumpHeight
							if bedwars.JumpHeightController then
								bedwars.JumpHeightController:setJumpHeight(game:GetService("StarterPlayer").CharacterJumpHeight)
							end
						end)
					end
				end)
			end

			task.wait(0.3)
			canDash = true
		end)
	end

	YuziDasher = vape.Categories.Kits:CreateModule({
		Name = 'YuziDasher',
		Function = function(callback)
			if callback then
				YuziDasher:Clean(inputService.InputBegan:Connect(function(input, gameProcessed)
					if gameProcessed then return end
					if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == CurrentKeybind then
						PerformDash()
					end
				end))
			else
				canDash = true
			end
		end,
	})

	local keybindOptions = {
		"Q", "E", "R", "F", "G", "X", "Z", "V", "B",
		"LeftAlt", "LeftControl", "LeftShift", "RightAlt", "RightControl", "RightShift",
		"Space", "CapsLock", "Tab"
	}

	YuziDasher:CreateDropdown({
		Name = 'Keybind',
		List = keybindOptions,
		Default = "Q",
		Function = function(value)
			CurrentKeybind = Enum.KeyCode[value]
		end
	})

	ImpulseSlider = YuziDasher:CreateSlider({
		Name = 'Impulse Multiplier',
		Min = 10,
		Max = 500,
		Default = 100,
		Tooltip = 'Controls dash speed'
	})

	JumpHeightSlider = YuziDasher:CreateSlider({
		Name = 'Jump Height',
		Min = 0,
		Max = 50,
		Default = 10,
	})
end)

run(function()
	local InfKrystal
	local KeepMomentum
	InfKrystal = vape.Categories.Kits:CreateModule({
		Name = 'InfKrystal',
		Function = function(callback)
			if callback then
				runService:BindToRenderStep('InfiniteKrystalMovement', Enum.RenderPriority.Character.Value + 2, function()
					if store.equippedKit ~= 'glacial_skater' and store.equippedKit ~= 'krystal' then return end
					if not entitylib.isAlive then return end
					if not bedwars.GlacialSkaterController then return end
					store.lastKrystalUpdateCheck = tick()
					pcall(function()
						bedwars.GlacialSkaterController:updateMomentum(100, "newValue")
					end)
				end)
			else
				runService:UnbindFromRenderStep('InfiniteKrystalMovement')
				store.lastKrystalUpdateCheck = tick() + 0.015
				if not (KeepMomentum and KeepMomentum.Enabled) then
					local suc, res = pcall(function()
						bedwars.GlacialSkaterController:updateMomentum(0, "newValue")
					end)
				end
			end
		end
	})
	KeepMomentum = InfKrystal:CreateToggle({
		Name = 'Keep Momentum',
		Tooltip = 'when u turn off inf krystal ur bar stays full n drains normal instead of dropping to 0',
		Default = false
	})
end)

run(function()
	local KaidaKillaura	
	local Targets
	local AttackRange
	local UpdateRate
	local MouseDown
	local GUICheck
	local ShowAnimation
	local AutoAbility
	local AbilityDistance
	local SwingDuringAbility
	local lastAttackTime = 0
	local lastAbilityTime = 0
	local attackCooldown = 0.55
	local abilityCooldown = 22
	local isChargingAbility = false
	manualCharging = false
	local currentTarget = nil
	local AutoStopAbility
	local SummonerKitController = nil
	local function getSummonerController()
		if SummonerKitController then return SummonerKitController end
		pcall(function()
			SummonerKitController = bedwars.KnitClient.Controllers.SummonerKitController
		end)
		return SummonerKitController
	end

	local function isActuallyCharging()
		if isChargingAbility then return true end
		if manualCharging then return true end
		local result = false
		pcall(function()
			local btns = lplr.PlayerGui
				:FindFirstChild("ActionBarScreenGui")
				and lplr.PlayerGui.ActionBarScreenGui:FindFirstChild("ActionBar")
				and lplr.PlayerGui.ActionBarScreenGui.ActionBar:FindFirstChild("AbilityButtons")
			if btns and btns:FindFirstChild("summoner_finish_charging") then
				result = true
			end
		end)
		return result
	end

	local function getSpellLevel()
		local level = 1
		pcall(function()
			local util = require(game:GetService("ReplicatedStorage").TS.games.bedwars.kit.kits.summoner['summoner-kit-util'])
			local result = util.summoner_getPlayerSpellLevel(lplr)
			if result then level = result end
		end)
		return level
	end

	local function getCastTime(level)
		local castTime = 2
		pcall(function()
			local util = require(game:GetService("ReplicatedStorage").TS.games.bedwars.kit.kits.summoner['summoner-kit-util'])
			local result = util.summoner_getTotalCastTimeRequired(level)
			if result then castTime = result end
		end)
		return castTime
	end

	local function fireUseAbility(abilityName)
		pcall(function()
			game:GetService("ReplicatedStorage")
				:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events")
				:WaitForChild("useAbility"):FireServer(abilityName)
		end)
	end

	local function doAutoAbility()
		if isChargingAbility then return end
		isChargingAbility = true

		pcall(function()
			local remote = game:GetService("ReplicatedStorage")
				:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events")
				:WaitForChild("useAbility")

			remote:FireServer(unpack({"summoner_start_charging"}))

			if AutoStopAbility.Enabled then
				task.wait(0.5)
				remote:FireServer(unpack({"summoner_finish_charging"}))
			else
				local level = getSpellLevel()
				local castTime = getCastTime(level)
				task.wait(math.max(castTime, 0.5))
				if isChargingAbility then
					remote:FireServer(unpack({"summoner_finish_charging"}))
					if currentTarget and currentTarget.RootPart then
						local myPos = entitylib.character.RootPart.Position
						local shootDir = CFrame.lookAt(myPos, currentTarget.RootPart.Position).LookVector
						local localPosition = myPos + shootDir * math.max((myPos - currentTarget.RootPart.Position).Magnitude - 16, 0)
						bedwars.Client:Get(remotes.SummonerClawAttack):SendToServer({
							position = localPosition,
							direction = shootDir,
							clientTime = workspace:GetServerTimeNow()
						})
					end
				end
			end
		end)

		lastAbilityTime = tick()
		isChargingAbility = false
	end

	local function getPlayerClawLevel()
		local handItem = lplr.Character and lplr.Character:FindFirstChild('HandInvItem')
		if handItem and handItem.Value then
			local itemType = handItem.Value.Name
			if itemType == 'summoner_claw_1' then return 1 end
			if itemType == 'summoner_claw_2' then return 2 end
			if itemType == 'summoner_claw_3' then return 3 end
			if itemType == 'summoner_claw_4' then return 4 end
		end
		if store and store.inventory and store.inventory.hotbar then
			for _, v in pairs(store.inventory.hotbar) do
				if v.item then
					local itemType = v.item.itemType
					if itemType == 'summoner_claw_1' then return 1 end
					if itemType == 'summoner_claw_2' then return 2 end
					if itemType == 'summoner_claw_3' then return 3 end
					if itemType == 'summoner_claw_4' then return 4 end
				end
			end
		end
		return 1
	end

	KaidaKillaura = vape.Categories.Kits:CreateModule({
		Name = 'AutoKaida',
		Function = function(callback)
			if callback then
				lastAttackTime = 0
				if tridentRemote then
					if tridentSpamThread then task.cancel(tridentSpamThread) end
					tridentSpamThread = task.spawn(function()
						while Killaura.Enabled do
							pcall(function() tridentRemote:InvokeServer() end)
							task.wait(0.25)
						end
					end)
					Killaura:Clean(function()
						if tridentSpamThread then task.cancel(tridentSpamThread) end
						tridentSpamThread = nil
					end)
				end
				lastAbilityTime = 0
				isChargingAbility = false
				manualCharging = false   
				pcall(function()
					local abilityButtons = lplr.PlayerGui
						:WaitForChild("ActionBarScreenGui", 10)
						:WaitForChild("ActionBar", 10)
						:WaitForChild("AbilityButtons", 10)

					KaidaKillaura:Clean(abilityButtons.ChildRemoved:Connect(function(child)
						if child.Name == "summoner_start_charging" then
							manualCharging = true
						end
						if child.Name == "summoner_finish_charging" then
							manualCharging = false
						end
					end))

					KaidaKillaura:Clean(abilityButtons.ChildAdded:Connect(function(child)
						if child.Name == "summoner_start_charging" then
							manualCharging = false
						end
					end))

					if abilityButtons:FindFirstChild("summoner_finish_charging") then
						manualCharging = true
					end
				end)

				repeat
					if not entitylib.isAlive then
						task.wait(0.1)
						continue
					end

					if GUICheck.Enabled then
						if bedwars.AppController:isLayerOpen(bedwars.UILayers.MAIN) then
							task.wait(0.1)
							continue
						end
					end

					local handItem = lplr.Character:FindFirstChild('HandInvItem')
					local hasClaw = handItem and handItem.Value and handItem.Value.Name:find('summoner_claw') ~= nil

					if MouseDown.Enabled then
						if not inputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
							task.wait(1.2)
							continue
						end
					end

					local plr = nil
					do
						local bestDot = -math.huge
						local camCF = workspace.CurrentCamera.CFrame
						local myPos = entitylib.character.RootPart.Position
						for _, ent in ipairs(entitylib.List) do
							local validType = (Targets.Players.Enabled and ent.Player) or (Targets.NPCs.Enabled and ent.NPC)
							if validType and ent.Targetable and ent.RootPart and ent.Health > 0 then
								local dist = (myPos - ent.RootPart.Position).Magnitude
								if dist <= AttackRange.Value then
									local toEnt = (ent.RootPart.Position - camCF.Position).Unit
									local dot = camCF.LookVector:Dot(toEnt)
									if dot <= 0 then continue end
									if dot > bestDot then
										bestDot = dot
										plr = ent
									end
								end
							end
						end
					end

					if plr and plr.Health > 0 then
						local localPosition = entitylib.character.RootPart.Position
						local targetDistance = (localPosition - plr.RootPart.Position).Magnitude
						local now = tick()

						if AutoAbility.Enabled and targetDistance <= AbilityDistance.Value * 1.25 then
							if not isChargingAbility and (now - lastAbilityTime) >= abilityCooldown then
								currentTarget = plr
								task.spawn(doAutoAbility)
							end
						end

						if not SwingDuringAbility.Enabled then
							local active = false
							for _, v in workspace:QueryDescendants('#Summoner_SummonCircle') do
								local pivot = v:FindFirstChild('Pivot')
								if pivot and math.floor(pivot.Position.X) == math.floor(entitylib.character.RootPart.Position.X) and math.floor(pivot.Position.Z) == math.floor(entitylib.character.RootPart.Position.Z) then
									active = true
									break
								end
							end
							if active then
								task.wait()
								continue
							end
						end

						if not SwingDuringAbility.Enabled and isChargingAbility then
							task.wait(0.05)
							continue
						end

						if hasClaw then
							local charging = isActuallyCharging()

							if not SwingDuringAbility.Enabled and charging then
								task.wait(0.05)
								continue
							end

							if (now - lastAttackTime) >= attackCooldown and targetDistance <= AttackRange.Value then
								local shootDir = CFrame.lookAt(localPosition, plr.RootPart.Position).LookVector
								localPosition += shootDir * math.max((localPosition - plr.RootPart.Position).Magnitude - 16, 0)
								lastAttackTime = now

								if ShowAnimation.Enabled then
									task.spawn(function()
										pcall(function()
											local clawLevel = getPlayerClawLevel()
											bedwars.AnimationUtil:playAnimation(lplr, bedwars.GameAnimationUtil:getAssetId(bedwars.AnimationType.SUMMONER_CHARACTER_SWIPE), {
												looped = false
											})
											local clawModel = replicatedStorage.Assets.Misc.Kaida.Summoner_DragonClaw:Clone()
											local clawColors = {
												Color3.fromRGB(75, 75, 75),
												Color3.fromRGB(255, 255, 255),
												Color3.fromRGB(43, 229, 229),
												Color3.fromRGB(49, 229, 94)
											}
											local nailMesh = clawModel:FindFirstChild("dragon_claw_nail_mesh")
											if nailMesh and nailMesh:IsA("MeshPart") then
												nailMesh.Color = clawColors[clawLevel] or clawColors[1]
											end
											if bedwars.KnightClient and bedwars.KnightClient.Controllers.SummonerKitSkinController then
												if bedwars.KnightClient.Controllers.SummonerKitSkinController:isPrismaticSkin(lplr) then
													bedwars.KnightClient.Controllers.SummonerKitSkinController:applyClawRGB(clawModel)
												end
											end
											clawModel.Parent = workspace
											local camera = workspace.CurrentCamera
											if camera and (camera.CFrame.Position - entitylib.character.RootPart.Position).Magnitude < 1 then
												for _, part in clawModel:GetDescendants() do
													if part:IsA('MeshPart') then
														part.Transparency = 0.6
													end
												end
											end
											local rootPart = entitylib.character.RootPart
											local Unit = Vector3.new(shootDir.X, 0, shootDir.Z).Unit
											local startPos = rootPart.Position + Unit:Cross(Vector3.new(0, 1, 0)).Unit * -1 * 5 + Unit * 6
											local direction = (startPos + shootDir * 13 - startPos).Unit
											local cframe = CFrame.new(startPos, startPos + direction)
											clawModel:PivotTo(cframe)
											clawModel.PrimaryPart.Anchored = true
											local portalConn = nil
											if clawModel:FindFirstChild("Portal1") then
												portalConn = runService.Heartbeat:Connect(function()
													if not clawModel or not clawModel.Parent then
														portalConn:Disconnect()
														portalConn = nil
														return
													end
													local foreArmCF = clawModel.RootPart.root.fore_arm.TransformedWorldCFrame
													if clawModel.Portal1 then
														clawModel.Portal1:PivotTo(foreArmCF)
													end
													if clawModel.Portal2 then
														clawModel.Portal2:PivotTo(foreArmCF * CFrame.Angles(math.pi, 0, 0))
													end
												end)
											end
											if clawModel:FindFirstChild('AnimationController') then
												local animator = clawModel.AnimationController:FindFirstChildOfClass('Animator')
												if animator then
													bedwars.AnimationUtil:playAnimation(animator, bedwars.GameAnimationUtil:getAssetId(bedwars.AnimationType.SUMMONER_CLAW_ATTACK), {
														looped = false,
														speed = 1
													})
												end
											end
											pcall(function()
												local sounds = {
													bedwars.SoundList.SUMMONER_CLAW_ATTACK_1,
													bedwars.SoundList.SUMMONER_CLAW_ATTACK_2,
													bedwars.SoundList.SUMMONER_CLAW_ATTACK_3,
													bedwars.SoundList.SUMMONER_CLAW_ATTACK_4
												}
												bedwars.SoundManager:playSound(sounds[math.random(1, #sounds)], {
													position = rootPart.Position
												})
											end)
											task.wait(0.5)
											if portalConn then
												portalConn:Disconnect()
												portalConn = nil
											end
											clawModel:Destroy()
										end)
									end)
								end

								bedwars.Client:Get(remotes.SummonerClawAttack):SendToServer({
									position = localPosition,
									direction = shootDir,
									clientTime = workspace:GetServerTimeNow()
								})
							end
						end
					else
						if isChargingAbility then
							isChargingAbility = false
							fireUseAbility("summoner_finish_charging")
						end
					end

					task.wait(1 / UpdateRate.Value)
				until not KaidaKillaura.Enabled

				isChargingAbility = false
			end
		end,
		Tooltip = 'auto attacks players with kaida claw'
	})

	Targets = KaidaKillaura:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})

	AttackRange = KaidaKillaura:CreateSlider({
		Name = 'Attack Range',
		Min = 1,
		Max = 32,
		Default = 22,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})

	UpdateRate = KaidaKillaura:CreateSlider({
		Name = 'Update Rate',
		Min = 1,
		Max = 120,
		Default = 60,
		Suffix = 'hz'
	})

	MouseDown = KaidaKillaura:CreateToggle({
		Name = 'Require Mouse Down',
	})

	GUICheck = KaidaKillaura:CreateToggle({
		Name = 'GUI Check'
	})

	ShowAnimation = KaidaKillaura:CreateToggle({
		Name = 'Show Animation',
		Default = true
	})

	SwingDuringAbility = KaidaKillaura:CreateToggle({
		Name = 'Swing During Ability',
		Default = true,
	})

	AutoAbility = KaidaKillaura:CreateToggle({
		Name = 'Auto Ability',
		Default = false,
		Function = function(callback)
			if not callback then
				isChargingAbility = false
			end
			AbilityDistance.Object.Visible = callback
			AutoStopAbility.Object.Visible = callback
		end
	})

	AbilityDistance = KaidaKillaura:CreateSlider({
		Name = 'Ability Distance',
		Min = 3,
		Max = 15,
		Default = 6,
		Visible = false,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})

	AutoStopAbility = KaidaKillaura:CreateToggle({
		Name = 'Auto Stop Ability',
		Default = true,
		Visible = false,
	})

	task.defer(function()
		if AbilityDistance and AbilityDistance.Object then
			AbilityDistance.Object.Visible = false   
		end
	end)
end)

run(function()
	local TurretRecall
	local HPThreshold
	local recalled = {}
	local takeBackRemote = game:GetService('ReplicatedStorage'):WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged'):WaitForChild('TakeBackCameraTurret')

	local function getMaxHealth(turret)
		local ok, result = pcall(function()
			return bedwars.ItemMeta[turret.Name].block.health
		end)
		return (ok and result) or turret:GetAttribute('MaxHealth') or 50
	end

	local function getCurrentHealth(turret)
		local blockpos = bedwars.BlockController:getBlockPosition(turret.Position)
		local blockdata = bedwars.BlockController:getStore():getBlockData(blockpos)
		return (blockdata and (blockdata:GetAttribute('1') or blockdata:GetAttribute('Health'))) or turret:GetAttribute('Health') or 0
	end

	local function recallTurret(turret)
		if recalled[turret] then return end
		recalled[turret] = true
		local blockPos = bedwars.BlockController:getBlockPosition(turret.Position)
		pcall(function()
			takeBackRemote:FireServer({turretBlockPos = blockPos})
		end)
	end

	local function watchTurret(turret)
		if not (TurretRecall and TurretRecall.Enabled) then return end
		if turret:GetAttribute('PlacedByUserId') ~= lplr.UserId then return end
		local maxHp = getMaxHealth(turret)
		task.spawn(function()
			while turret and turret.Parent do
				task.wait(0.1)
				if not TurretRecall.Enabled then continue end
				local hp = getCurrentHealth(turret)
				local threshold = (HPThreshold.Value / 100) * maxHp
				if hp <= threshold then
					recallTurret(turret)
					break
				end
			end
		end)
		turret.AncestryChanged:Connect(function()
			if not turret.Parent then
				recalled[turret] = nil
			end
		end)
	end

	collectionService:GetInstanceAddedSignal('Turret'):Connect(watchTurret)
	for _, v in collectionService:GetTagged('Turret') do
		watchTurret(v)
	end

	TurretRecall = vape.Categories.Kits:CreateModule({
		Name = 'TurretRecall',
		Tooltip = 'picks up your turrent based off hp threshold'
	})
	HPThreshold = TurretRecall:CreateSlider({
		Name = 'HP Threshold',
		Min = 1,
		Max = 50,
		Default = 20,
		Suffix = function(v) return '%' end
	})
end)

run(function()
	local TerraAssist
	local TATargets
	local TAFOV
	local TARange
	local TASort
	local terraOldNC

	TerraAssist = vape.Categories.Kits:CreateModule({
		Name = 'TerraAssist',
		Tooltip = 'pa for terra',
		Function = function(callback)
			if callback then
				local tryBlockKick
				pcall(function()
					tryBlockKick = replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged:FindFirstChild('TryBlockKick')
				end)
				if not tryBlockKick then
					notif('TerraAssist', 'terra kick remote not found', 3, 'warning')
					if TerraAssist.Enabled then TerraAssist:Toggle() end
					return
				end

				local terraAim = nil

				local targetThread = task.spawn(function()
					while TerraAssist.Enabled do
						local aim = nil
						local root = entitylib.isAlive and entitylib.character and entitylib.character.RootPart
						if root then
							local origin = root.Position
							local ent = entitylib.EntityMouse({
								Part = 'RootPart',
								Range = TAFOV.Value,
								Players = TATargets.Players.Enabled,
								NPCs = TATargets.NPCs and TATargets.NPCs.Enabled or false,
								Wallcheck = TATargets.Walls.Enabled,
								Origin = origin,
							}) or entitylib.EntityPosition({
								Origin = origin,
								Range = TARange.Value,
								Part = 'RootPart',
								Players = TATargets.Players.Enabled,
								NPCs = TATargets.NPCs and TATargets.NPCs.Enabled or false,
								Wallcheck = TATargets.Walls.Enabled,
								Sort = sortmethods[TASort.Value],
							})
							if ent and ent.RootPart then
								local targetPos = ent.RootPart.Position
								local targetVel = ent.RootPart.AssemblyLinearVelocity or ent.RootPart.Velocity or Vector3.zero
								local dist = (targetPos - origin).Magnitude
								local travelTime = math.clamp(dist / 40, 0, 0.4)
								local predictedPos = targetPos + targetVel * travelTime
								local aimPoint = predictedPos + Vector3.new(0, 3, 0)
								local dir = aimPoint - origin
								if dir.Magnitude > 0 then
									dir = dir.Unit
									aim = {
										direction = dir,
										originPosition = origin + Vector3.new(0, 2, 0) + dir * 2,
									}
								end
							end
						end
						terraAim = aim
						task.wait()
					end
				end)

				terraOldNC = hookmetamethod(game, '__namecall', newcclosure(function(self, ...)
					if self == tryBlockKick and getnamecallmethod() == 'FireServer' then
						if TerraAssist.Enabled and terraAim then
							local argsTable = ...
							if type(argsTable) == 'table' then
								argsTable.direction = terraAim.direction
								argsTable.originPosition = terraAim.originPosition
							end
						end
					end
					return terraOldNC(self, ...)
				end))

				TerraAssist:Clean(function()
					if terraOldNC then
						hookmetamethod(game, '__namecall', terraOldNC)
						terraOldNC = nil
					end
				end)
				TerraAssist:Clean(function()
					if targetThread then
						task.cancel(targetThread)
						targetThread = nil
					end
				end)
			end
		end,
	})

	TATargets = TerraAssist:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = false,
	})

	TAFOV = TerraAssist:CreateSlider({
		Name = 'FOV',
		Min = 1,
		Max = 800,
		Default = 200,
	})

	TARange = TerraAssist:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 100,
		Default = 60,
		Suffix = function(val) return val == 1 and 'stud' or 'studs' end,
	})

	TASort = TerraAssist:CreateDropdown({
		Name = 'Sort',
		List = getSortList({'Distance', 'Cursor'}),
	})
end)

run(function()
	local SigridDisabler
	local elkCtrl = nil
	local modMaid = nil
	local sentMount = false

	local function getElk()
		local ok, Knit = pcall(function()
			return require(game.ReplicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
		end)
		if not ok or not Knit then return nil end
		return Knit.Controllers.ElkMasterController
	end

	SigridDisabler = vape.Categories.Kits:CreateModule({
		Name = 'Sigrid Disabler',
		Function = function(callback)
			if callback then
				elkCtrl = getElk()
				if not elkCtrl then
					notif('Sigrid Disabler', 'u aint on sigrid gng', 5, 'alert')
					task.defer(function()
						if SigridDisabler.Enabled then
							SigridDisabler:Toggle()
						end
					end)
					return
				end

				sentMount = false
				pcall(function()
					local Knit = require(game.ReplicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
					local Maid = require(game.ReplicatedStorage.rbxts_include.node_modules['@rbxts'].maid.Maid)
					modMaid = Maid.new()
					modMaid:GiveTask(Knit.Controllers.SprintController:getMovementStatusModifier():addModifier({
						blockSprint = false,
						moveSpeedMultiplier = 1.5
					}))
					modMaid:GiveTask(Knit.Controllers.JumpHeightController:getJumpModifier():addModifier({
						jumpHeightMultiplier = 1.6
					}))
				end)

				local lastForce = 0
				SigridDisabler:Clean(runService.Heartbeat:Connect(function()
					if not elkCtrl or not SigridDisabler.Enabled then return end
					if (tick() - lastForce) < 0.25 then return end
					lastForce = tick()
					pcall(function()
						elkCtrl.isMounted = true
						if elkCtrl.summonAbilityMaid then
							elkCtrl.summonAbilityMaid:DoCleaning()
							elkCtrl.summonAbilityMaid = nil
						end
						local Knit = require(game.ReplicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
						Knit.Controllers.BlockPlacementController:enableBlockPlacer()
					end)
				end))

				task.spawn(function()
					repeat
						if entitylib.isAlive and store.equippedKit == 'elk_master' then
							if not sentMount then
								sentMount = true
								pcall(function()
									bedwars.Client:Get('ElkKitMounted'):SendToServer()
								end)
							end
						else
							sentMount = false
						end
						task.wait(0.5)
					until not SigridDisabler.Enabled
				end)
			else
				sentMount = false
				if modMaid then
					pcall(function() modMaid:DoCleaning() end)
					modMaid = nil
				end
				if elkCtrl then
					pcall(function() elkCtrl.isMounted = false end)
					pcall(function()
						if elkCtrl.summonAbilityMaid then
							elkCtrl.summonAbilityMaid:DoCleaning()
							elkCtrl.summonAbilityMaid = nil
						end
						if elkCtrl.playerModifierMaid then
							elkCtrl.playerModifierMaid:DoCleaning()
							elkCtrl.playerModifierMaid = nil
						end
					end)
					pcall(function()
						local Knit = require(game.ReplicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
						Knit.Controllers.SwordController:toggleSwordSwing(false)
						Knit.Controllers.BlockPlacementController:enableBlockPlacer()
					end)
					pcall(function()
						local MountType = require(game.ReplicatedStorage.TS.mount['mount-type']).MountType
						bedwars.Client:Get('Dismount'):SendToServer({mountType = MountType.ELK})
					end)
					pcall(function()
						bedwars.Client:Get('Dismount'):SendToServer({mountType = 'elk'})
					end)
				end
				elkCtrl = nil
			end
		end
	})
end)

run(function()
	local KrystalDisabler
	local oldMomentum = nil
	local krystalCtrl = nil

	local function setupKrystal()
		local ok, Knit = pcall(function()
			return require(game.ReplicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
		end)
		if not ok or not Knit or not Knit.Controllers.GlacialSkaterController then return false end
		local Client = require(game.ReplicatedStorage.TS.remotes).default.Client
		krystalCtrl = { GlacialSkaterController = Knit.Controllers.GlacialSkaterController }
		oldMomentum = krystalCtrl.GlacialSkaterController.updateMomentum
		krystalCtrl.GlacialSkaterController.updateMomentum = function(self, ...)
			self.momentum = 9e9
			self.lastMomentumReport = 9e9
			pcall(function()
				Client:Get('MomentumUpdate'):SendToServer({ momentumValue = 9e9 })
			end)
		end
		return true
	end

	KrystalDisabler = vape.Categories.Kits:CreateModule({
		Name = 'KrystalDisabler',
		Function = function(callback)
			if callback then
				local ok = setupKrystal()
				if not ok then
					notif("KrystalDisabler", "Not on Krystal kit!", 5, 'alert')
					KrystalDisabler:Toggle()
					return
				end
				KrystalDisabler:Clean(runService.RenderStepped:Connect(function()
					if not krystalCtrl then return end
					pcall(function()
						krystalCtrl.GlacialSkaterController:updateMomentum(9e9, 'newValue')
					end)
				end))
			else
				if krystalCtrl and oldMomentum then
					krystalCtrl.GlacialSkaterController.updateMomentum = oldMomentum
					oldMomentum = nil
				end
				krystalCtrl = nil
			end
		end
	})
end)

run(function()
	local OwlAura
	local OwlTargets
	local RangeSlider
	local PROJECTILE_SPEED = 220
	local SHOOT_COOLDOWN = 0.87
	local lastOwlShot = 0

	local function getOwlModel()
		return workspace:FindFirstChild("ClientOwl")
	end

	local function getBulletOrigin(owl)
		local primary = owl and owl.PrimaryPart
		if not primary then return nil end
		local bo = primary:FindFirstChild("bulletOrigin")
		return bo and bo.WorldPosition or primary.Position
	end

	local function fireOwlProjectile(fromPos, targetPos)
		local owl = getOwlModel()
		if not owl then return end
		local owlHandle = owl:FindFirstChild("Handle") or owl.PrimaryPart
		if not owlHandle then return end

		local rbxts = replicatedStorage:FindFirstChild("rbxts_include")
		local netManaged = rbxts
			and rbxts:FindFirstChild("node_modules")
			and rbxts.node_modules:FindFirstChild("@rbxts")
			and rbxts.node_modules["@rbxts"]:FindFirstChild("net")
			and rbxts.node_modules["@rbxts"].net:FindFirstChild("out")
			and rbxts.node_modules["@rbxts"].net.out:FindFirstChild("_NetManaged")
		if not netManaged then return end

		local owlAiming = netManaged:FindFirstChild("OwlAiming")
		local owlFire = netManaged:FindFirstChild("OwlFireProjectile")
		if not owlAiming or not owlFire then return end

		local direction = (targetPos - fromPos).Unit * PROJECTILE_SPEED
		local refId = httpService:GenerateGUID(false):sub(1, 8):upper()

		owlAiming:FireServer({ owl = owlHandle, starting = true })
		task.wait(0.05)
		owlAiming:FireServer({ owl = owlHandle, starting = false })
		owlFire:FireServer({
			fromPosition = fromPos,
			direction = direction,
			offset = nil,
			ProjectileRefId = refId,
			initialVelocity = direction
		})
	end

	OwlAura = vape.Categories.Kits:CreateModule({
		Name = 'OwlAura',
		Function = function(callback)
			if callback then
				lastOwlShot = 0
				OwlAura:Clean(runService.Heartbeat:Connect(function()
					if tick() - lastOwlShot < SHOOT_COOLDOWN then return end
					if not entitylib.isAlive then return end

					local owl = getOwlModel()
					local myRoot = lplr.Character and lplr.Character:FindFirstChild("HumanoidRootPart")
					if not myRoot then return end
					local fromPos = owl and getBulletOrigin(owl) or myRoot.Position

					local ent = entitylib.EntityPosition({
						Range = RangeSlider and RangeSlider.Value or 40,
						Part = 'RootPart',
						Wallcheck = OwlTargets and OwlTargets.Walls and OwlTargets.Walls.Enabled,
						Players = OwlTargets and OwlTargets.Players and OwlTargets.Players.Enabled,
						NPCs = OwlTargets and OwlTargets.NPCs and OwlTargets.NPCs.Enabled,
					})

					if not ent or not ent.RootPart then return end

					local rootPos = ent.RootPart.Position
					local vel = ent.RootPart.AssemblyLinearVelocity or ent.RootPart.Velocity or Vector3.zero
					local dist = (fromPos - rootPos).Magnitude
					local predictedPos = rootPos + vel * (dist / PROJECTILE_SPEED)

					lastOwlShot = tick()
					fireOwlProjectile(fromPos, predictedPos)
				end))
			end
		end
	})

	OwlTargets = OwlAura:CreateTargets({
		Players = true,
		NPCs = false,
		Walls = true
	})
	RangeSlider = OwlAura:CreateSlider({
		Name = 'Range',
		Min = 10,
		Max = 60,
		Default = 40,
		Suffix = function(val)
			return val == 1 and 'stud' or 'studs'
		end
	})
end)

run(function()
	local AutoVanessa
	local oldGetChargeTime
	local lastChargeTime = 0
	
	AutoVanessa = vape.Categories.Kits:CreateModule({
		Name = 'AutoVanessa',
		Function = function(callback)
			if callback then
				task.spawn(function()
					repeat task.wait() until bedwars.TripleShotProjectileController
					
					if bedwars.TripleShotProjectileController then
						oldGetChargeTime = bedwars.TripleShotProjectileController.getChargeTime
						
						bedwars.TripleShotProjectileController.getChargeTime = function(self)
							return 0
						end
						
						bedwars.TripleShotProjectileController.overchargeStartTime = tick()
					end
				end)
			else
				if oldGetChargeTime and bedwars.TripleShotProjectileController then
					bedwars.TripleShotProjectileController.getChargeTime = oldGetChargeTime
				end
				lastChargeTime = 0
			end
		end,
		Tooltip = 'auto charges Vanessa triple shot'
	})
end)

run(function()
    local AutoLani
    local PlayerDropdown
    local RefreshButton
    local DelaySlider
    local AutoBuyToggle
    local GUICheck
    local DelayBuySlider
    local LimitItems
	local HandCheck
    local TargetModeDropdown
    local HealthActivationToggle
    local HealthThresholdSlider
    local TeammateHealthToggle
    local TeammateHealthSlider
    local running = false
    local buyRunning = false
    local buyLoopThread = nil

    local function isHoldingScepter()
        if not entitylib.isAlive then return false end
        local hand = store.hand and store.hand.tool
        if hand and (hand.Name == 'scepter' or hand.itemType == 'scepter') then
            return true
        end
        local inv = store.inventory
        if inv and inv.hand and inv.hand.itemType == 'scepter' then
            return true
        end
        return false
    end

    local function isPlayerAlive(player)
        if not player or not player.Character then return false end
        local humanoid = player.Character:FindFirstChild("Humanoid")
        return humanoid and humanoid.Health > 0
    end

    local function isPlayerInVoid(player)
        if not player or not player.Character then return true end
        local rootPart = player.Character:FindFirstChild("HumanoidRootPart")
        if rootPart then return rootPart.Position.Y < 0 end
        return true
    end

    local function getTargetPlayer()
        local myTeam = lplr:GetAttribute('Team')
        if not myTeam then return nil end
        local mode = TargetModeDropdown.Value

        if mode == "Specific Player" then
            local targetName = PlayerDropdown.Value
            if not targetName or targetName == "" or targetName == "None" then return nil end
            local targetPlayer = playersService:FindFirstChild(targetName)
            if targetPlayer and targetPlayer:GetAttribute('Team') == myTeam then
                if isPlayerAlive(targetPlayer) and not isPlayerInVoid(targetPlayer) then
                    return targetPlayer
                end
            end
            return nil

        elseif mode == "Lowest Health" then
            local lowestHealth = math.huge
            local lowestPlayer = nil
            for _, player in playersService:GetPlayers() do
                if player ~= lplr and player:GetAttribute('Team') == myTeam then
                    if isPlayerAlive(player) and not isPlayerInVoid(player) then
                        local hp = getPlayerHealthPercent(player)
                        if hp < lowestHealth and hp > 0 then
                            lowestHealth = hp
                            lowestPlayer = player
                        end
                    end
                end
            end
            return lowestPlayer

        elseif mode == "Closest" then
            if not entitylib.isAlive then return nil end
            local myPos = entitylib.character.RootPart.Position
            local closestDist = math.huge
            local closestPlayer = nil
            for _, player in playersService:GetPlayers() do
                if player ~= lplr and player:GetAttribute('Team') == myTeam then
                    if isPlayerAlive(player) and not isPlayerInVoid(player) then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            local dist = (player.Character.HumanoidRootPart.Position - myPos).Magnitude
                            if dist < closestDist then
                                closestDist = dist
                                closestPlayer = player
                            end
                        end
                    end
                end
            end
            return closestPlayer

        elseif mode == "Furthest" then
            if not entitylib.isAlive then return nil end
            local myPos = entitylib.character.RootPart.Position
            local furthestDist = 0
            local furthestPlayer = nil
            for _, player in playersService:GetPlayers() do
                if player ~= lplr and player:GetAttribute('Team') == myTeam then
                    if isPlayerAlive(player) and not isPlayerInVoid(player) then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            local dist = (player.Character.HumanoidRootPart.Position - myPos).Magnitude
                            if dist > furthestDist then
                                furthestDist = dist
                                furthestPlayer = player
                            end
                        end
                    end
                end
            end
            return furthestPlayer

        elseif mode == "Random" then
            local valid = {}
            for _, player in playersService:GetPlayers() do
                if player ~= lplr and player:GetAttribute('Team') == myTeam then
                    if isPlayerAlive(player) and not isPlayerInVoid(player) then
                        table.insert(valid, player)
                    end
                end
            end
            if #valid > 0 then return valid[math.random(1, #valid)] end
            return nil
        end

        return nil
    end

    local function shouldActivateByHealth()
        if not HealthActivationToggle.Enabled then return true end
        if not entitylib.isAlive then return false end
        local myHp = getPlayerHealthPercent(lplr)
        if myHp <= HealthThresholdSlider.Value then return true end
        if TeammateHealthToggle.Enabled then
            local target = getTargetPlayer()
            if target then
                local targetHp = getPlayerHealthPercent(target)
                if targetHp <= TeammateHealthSlider.Value then return true end
            end
        end
        return false
    end

    local function buyScepter()
        pcall(function()
            bedwars.Client:Get(remotes.BedwarsPurchaseItem).instance:InvokeServer({
                shopItem = {
                    currency = "iron",
                    itemType = "scepter",
                    amount = 1,
                    price = 45,
                    category = "Combat",
                    requiresKit = {"paladin"},
                    lockAfterPurchase = true
                },
                shopId = "1_item_shop"
            })
        end)
    end

    local function startBuyLoop()
        if buyLoopThread then
            task.cancel(buyLoopThread)
            buyLoopThread = nil
        end
        buyRunning = true
        buyLoopThread = task.spawn(function()
            while buyRunning and AutoBuyToggle.Enabled and AutoLani.Enabled do
                local canBuy = GUICheck.Enabled
                    and bedwars.AppController:isAppOpen('BedwarsItemShopApp')
                    or (not GUICheck.Enabled and getShopNPC())
                if canBuy then
                    buyScepter()
                end
                task.wait(DelayBuySlider.Value)
            end
            buyLoopThread = nil
        end)
    end

    local function stopBuyLoop()
        buyRunning = false
        if buyLoopThread then
            task.cancel(buyLoopThread)
            buyLoopThread = nil
        end
    end

    AutoLani = vape.Categories.Kits:CreateModule({
        Name = "AutoLani",
        Function = function(callback)
            running = callback
            if callback then
                task.spawn(function()
                    AutoLani:Clean(lplr:GetAttributeChangedSignal("PaladinStartTime"):Connect(function()
                        if not running then return end
                        if not shouldActivateByHealth() then return end
                        if LimitItems.Enabled and not isHoldingScepter() then
                            notif("AutoLani", "bru u aint even holding the scepter", 3)
                            return
                        end

                        if DelaySlider.Value > 0 then
                            task.wait(DelaySlider.Value)
                        end

                        local targetPlayer = getTargetPlayer()
                        if not targetPlayer then
                            notif("AutoLani", "no teammate to tp to gng", 2)
                            return
                        end

                        local sent = false
                        for i = 1, 3 do
                            local ok = pcall(function()
                                bedwars.Client:Get(remotes.PaladinAbilityRequest):SendToServer({
                                    target = targetPlayer,
                                    player = targetPlayer
                                })
                            end)
                            if ok then
                                sent = true
                                break
                            end
                            task.wait(0.05)
                        end

                        if sent then
                            pcall(function()
                                bedwars.AbilityController:useAbility('PALADIN_ABILITY')
                            end)
                            notif("AutoLani", "tp'd to " .. targetPlayer.Name .. " dont die lol", 2)
                        else
                            notif("AutoLani", "tp failed gng", 2)
                        end
                    end))
                end)

                if AutoBuyToggle.Enabled then startBuyLoop() end

                AutoLani:Clean(playersService.PlayerAdded:Connect(function()
                    task.wait(0.5)
                    if PlayerDropdown and type(PlayerDropdown.SetList) == 'function' then PlayerDropdown:SetList(getTeammates(true)) end
                end))
                AutoLani:Clean(playersService.PlayerRemoving:Connect(function()
                    task.wait(0.5)
                    if PlayerDropdown and type(PlayerDropdown.SetList) == 'function' then PlayerDropdown:SetList(getTeammates(true)) end
                end))
                AutoLani:Clean(lplr:GetAttributeChangedSignal('Team'):Connect(function()
                    task.wait(1)
                    if PlayerDropdown and type(PlayerDropdown.SetList) == 'function' then PlayerDropdown:SetList(getTeammates(true)) end
                end))
            else
                running = false
                stopBuyLoop()
            end
        end,
        Tooltip = "auto tp to teammates w paladin scepter"
    })

    TargetModeDropdown = AutoLani:CreateDropdown({
        Name = "Target Mode",
        List = {"Specific Player", "Lowest Health", "Closest", "Furthest", "Random"},
        Default = "Closest",
        Function = function(val)
            if PlayerDropdown then
                PlayerDropdown.Object.Visible = (val == "Specific Player")
            end
        end,
        Tooltip = "who to tp to"
    })

    local function teammateListWithNone()
        local list = {"None"}
        for _, name in ipairs(getTeammates(true)) do
            table.insert(list, name)
        end
        return list
    end

    PlayerDropdown = AutoLani:CreateDropdown({
        Name = "Teammate",
        List = teammateListWithNone(),
        Tooltip = "pick ur teammate"
    })

    RefreshButton = AutoLani:CreateButton({
        Name = "Refresh Teammates",
        Function = function()
            task.spawn(function()
                local newNames = getTeammates(true)
                local newList = {"None"}
                for _, name in ipairs(newNames) do
                    table.insert(newList, name)
                end
                if PlayerDropdown then
                    pcall(function()
                        PlayerDropdown:Change(newList)
                        if #newList > 1 then
                            if not PlayerDropdown.Value or PlayerDropdown.Value == "" or not table.find(newList, PlayerDropdown.Value) then
                                PlayerDropdown:SetValue(newList[2] or "None")
                            else
                                PlayerDropdown:SetValue(PlayerDropdown.Value)
                            end
                        end
                    end)
                end
                notif("AutoLani", #newList > 0 and "refreshed, got " .. #newList .. " teammates" or "no teammates found", 2)
            end)
        end,
        Tooltip = "refresh the teammate list"
    })

    DelaySlider = AutoLani:CreateSlider({
        Name = "Teleport Delay",
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = "s",
        Tooltip = "delay before tping"
    })

    LimitItems = AutoLani:CreateToggle({
        Name = "Limit to Scepter",
        Default = true,
        Tooltip = "only tp when u holdin the scepter"
    })

    HealthActivationToggle = AutoLani:CreateToggle({
        Name = "Health Activation",
        Default = false,
        Function = function(val)
            if HealthThresholdSlider then HealthThresholdSlider.Object.Visible = val end
            if TeammateHealthToggle then TeammateHealthToggle.Object.Visible = val end

            if not val then
                if TeammateHealthSlider and TeammateHealthSlider.Object then
                    TeammateHealthSlider.Object.Visible = false
                end
            else
                if TeammateHealthToggle and TeammateHealthToggle.Enabled then
                    if TeammateHealthSlider and TeammateHealthSlider.Object then
                        TeammateHealthSlider.Object.Visible = true
                    end
                end
            end
        end,
        Tooltip = "only use ability based on hp"
    })

    HealthThresholdSlider = AutoLani:CreateSlider({
        Name = "Self Health %",
        Min = 1,
        Max = 100,
        Default = 50,
        Suffix = "%",
        Tooltip = "use ability when ur hp is below this",
        Visible = false
    })

    TeammateHealthToggle = AutoLani:CreateToggle({
        Name = "Teammate Health Check",
        Default = false,
        Function = function(val)
            if TeammateHealthSlider then TeammateHealthSlider.Object.Visible = val end
        end,
        Tooltip = "also check teammate hp",
        Visible = false
    })

    TeammateHealthSlider = AutoLani:CreateSlider({
        Name = "Teammate Health %",
        Min = 1,
        Max = 100,
        Default = 30,
        Suffix = "%",
        Tooltip = "use ability when teammate hp is below this",
        Visible = false
    })

    AutoBuyToggle = AutoLani:CreateToggle({
        Name = "Auto Buy Scepter",
        Default = false,
        Function = function(val)
            if GUICheck then GUICheck.Object.Visible = val end
            if DelayBuySlider then DelayBuySlider.Object.Visible = val end
            if val and AutoLani.Enabled then
                startBuyLoop()
            else
                stopBuyLoop()
            end
        end,
        Tooltip = "auto cop scepters from shop"
    })

    GUICheck = AutoLani:CreateToggle({
        Name = "GUI Check",
        Default = false,
        Tooltip = "only buy when shop is open",
        Visible = false
    })

    DelayBuySlider = AutoLani:CreateSlider({
        Name = "Buy Delay",
        Min = 0.1,
        Max = 2,
        Default = 0.3,
        Decimal = 10,
        Suffix = "s",
        Tooltip = "delay between buys",
        Visible = false
    })

    task.defer(function()
        if PlayerDropdown and PlayerDropdown.Object then
            PlayerDropdown.Object.Visible = true
        end
        if HealthThresholdSlider and HealthThresholdSlider.Object then
            HealthThresholdSlider.Object.Visible = false
        end
        if TeammateHealthToggle and TeammateHealthToggle.Object then
            TeammateHealthToggle.Object.Visible = false
        end
        if TeammateHealthSlider and TeammateHealthSlider.Object then
            TeammateHealthSlider.Object.Visible = false
        end
        if GUICheck and GUICheck.Object then GUICheck.Object.Visible = false end
        if DelayBuySlider and DelayBuySlider.Object then DelayBuySlider.Object.Visible = false end
    end)
end)

run(function()
	local AntiLasso
	local Chance
	local watching = setmetatable({}, {__mode = 'k'})
	
	local function Added(ent)
		if watching[ent] then return end
	
		watching[ent] = ent.ChildAdded:Connect(function(v)
			if not AntiLasso.Enabled then return end
			if v:IsA('Accessory') and v:FindFirstChild('Rope') and Random.new(os.clock()):NextNumber(1, 100) < Chance.Value then
				ent.PrimaryPart.Anchored = true
				v.Destroying:Once(function()
					task.wait(0.5)
					ent.PrimaryPart.Anchored = false
				end)
			end
		end)
	end
	
	AntiLasso = vape.Categories.Kits:CreateModule({
		Name = 'AntiLasso',
		Function = function(callback)
			if callback then
				AntiLasso:Clean(entitylib.Events.LocalAdded:Connect(function(ent)
					task.delay(1, function()
						Added(ent.Character)
					end)
				end))
				if entitylib.isAlive then
					Added(lplr.Character)
				end
			else
				local char = lplr.Character
				local root = char and char.PrimaryPart
				if root then
					pcall(function() root.Anchored = false end)
				end
			end
		end,
		Tooltip = 'stops u gettin yanked by the lasso'
	})
	
	Chance = AntiLasso:CreateSlider({
		Name = 'Chance',
		Min = 0,
		Max = 100,
		Default = 100,
		Suffix = '%'
	})
end)

run(function()
	local GrimReaperFix
	GrimReaperFix = vape.Categories.Kits:CreateModule({
		Name = 'GrimReaperFix',
		Function = function(callback)
			if callback then
				GrimReaperFix:Clean(runService.Heartbeat:Connect(function()
					if not entitylib.isAlive then return end
					local humanoid = entitylib.character.Humanoid
					if humanoid.HipHeight > 2.1 then
						humanoid.HipHeight = 2.05
					end
				end))
			end
		end,
		Tooltip = 'fixes grim height (prevents being too tall)'
	})
end)

run(function()
	local AutoEmber
	local Targets
	local Range
	local SpinCooldown
	local HoldRange = 32
	local old = 0
	local isCharging = false
	local chargeAnim, FpChargeAnim = nil,nil
	AutoEmber = vape.Categories.Kits:CreateModule({
		Name = 'AutoEmber',
		Tooltip = 'automatically uses the ember ability',
		Function = function(call)
			if call then
				repeat
					if not entitylib.isAlive and isCharging then
						isCharging = false
						bedwars.HellSaberController.animationMaid:DoCleaning()
						chargeAnim = nil
						FpChargeAnim = nil
					end
					if entitylib.isAlive then 
						local tool = getItem('infernal_saber') 
						if tool then
							local ent = entitylib.EntityPosition({
								Range = HoldRange,
								Players = Targets.Players.Enabled,
								NPCs = Targets.NPCs.Enabled,
								Part = 'RootPart'
							}) 

							if not ent then
								if isCharging then
									isCharging = false
									bedwars.HellSaberController.animationMaid:DoCleaning()
									chargeAnim = nil
									FpChargeAnim = nil
									task.wait(0.3)
									continue
								end
							end

							if ent then
								if not isCharging then
									isCharging = true
									bedwars.HellSaberController:playChargeSound(lplr)
									local animer = lplr.Character
									if animer ~= nil then
										animer = animer:FindFirstChild("Humanoid")
										if animer ~= nil then
											animer = animer:FindFirstChild("Animator")
										end
									end
									if not animer then
										isCharging = false
										task.wait(0.1)
										continue
									end
									chargeAnim = animer:LoadAnimation(bedwars.GameAnimationUtil:getAnimation(bedwars.AnimationType.INFERNO_SWORD_CHARGE))
									chargeAnim:Play()
									chargeAnim:AdjustSpeed(1.83)
									chargeAnim:GetMarkerReachedSignal("end"):Connect(function()
										local newChargeAnim = chargeAnim
										if newChargeAnim ~= nil then
											newChargeAnim:AdjustSpeed(0)
										end
									end)
									FpChargeAnim = bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_INFERNO_SWORD_CHARGE)
									if FpChargeAnim then
										FpChargeAnim:GetMarkerReachedSignal("end"):Connect(function()
											local newFpChargeAnim = FpChargeAnim
											if newFpChargeAnim ~= nil then
												newFpChargeAnim:AdjustSpeed(0)
											end
										end)
									end
									bedwars.HellSaberController.animationMaid:GiveTask(function()
										local MaidCA1 = chargeAnim
										if MaidCA1 ~= nil then
											MaidCA1:Stop()
										end
										local MaidCA2 = chargeAnim
										if MaidCA2 ~= nil then
											MaidCA2:Destroy()
										end
										local MaidFCA1 = FpChargeAnim
										if MaidFCA1 ~= nil then
											MaidFCA1:Stop()
										end
										local MaidFCA2 = FpChargeAnim
										if MaidFCA2 ~= nil then
											MaidFCA2:Destroy()
										end
									end)
								end
								local DeltaPos = (ent.RootPart.Position - lplr.Character.HumanoidRootPart.Position).Magnitude
								if DeltaPos <= Range.Value then
									if (os.clock() - old) >= SpinCooldown.Value then
										bedwars.HellSaberController.animationMaid:DoCleaning()
										switchItem(tool)
										bedwars.Client:Get('HellBladeRelease'):SendToServer({
											chargeTime = 1 + tick() - (0.045 + (math.random() - math.random())), 
											weapon = tool,
											player = lplr
										})
										old = os.clock()
										bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_INFERNO_SWORD_SPIN)										
										isCharging = false
										
									end
								end
							end
						end
					end
					task.wait(0.03)
				until not AutoEmber.Enabled
				isCharging = false
				bedwars.HellSaberController.animationMaid:DoCleaning()
				chargeAnim = nil
				FpChargeAnim = nil 
			end
		end
	})
	Targets = AutoEmber:CreateTargets({
		Players = true,
		NPCs = false
	})
	SpinCooldown = AutoEmber:CreateSlider({
		Name = 'Spin Cooldown',
		Min = 0,
		Max = 4,
		Default = 1.12,
		Decimal = 100,
		Tooltip = 'how long it waits b4 spinning again. keep it above 0.2 or u finna get banned'
	})
	Range = AutoEmber:CreateSlider({
		Name = 'Release Range',
		Min = 1,
		Max = 22,
		Default = 22,
		Suffix = function(val)
			return val <= 1 and 'stud' or 'studs'
		end
	})
end)

run(function()
    local AutoUma
    local rangeSlider
    local limitToStaff
    local playAnim
    local autoSummon
    local useHeal
    local useAttack
    local targetDrops
    local allowDiamond
    local allowEmerald

    local function findStaff()
        if limitToStaff.Enabled then
            local held = store.hand.tool
            if held and held.Name == 'spirit_staff' then
                return held, getHotbar(held)
            end
            return nil
        end

        for slot, item in pairs(store.inventory.inventory.items) do
            if item.itemType == 'spirit_staff' then
                switchItem(item, 0)
                return item, slot
            end
        end
    end

    local function getClosestDrop(origin, drops)
        local closest, shortest = nil, rangeSlider.Value + 1

        for _, drop in pairs(drops) do
            local valid = (drop.Name == 'emerald' and allowEmerald.Enabled) or (drop.Name == 'diamond' and allowDiamond.Enabled)

            if valid then
                local dist = (origin - drop.Position).Magnitude

                if dist <= shortest and not entitylib.Wallcheck(origin, drop.Position, {gameCamera, lplr.Character, drop}) then
                    closest = drop
                    shortest = dist
                end
            end
        end

        return closest
    end

    local function fireSpirit(staff, origin, targetPos, hasAttackSpirit)
        local shootFrom = origin + Vector3.new(0, 2, 0)

        local predicted = targetPos + Vector3.new(0,(origin - targetPos).Magnitude / 5, 0)

        local direction = CFrame.lookAt(origin, predicted).LookVector * 100

        local _spiritRemote = bedwars.Client:Get(remotes.FireProjectile).instance
        if _spiritRemote then
            _spiritRemote:InvokeServer(staff, nil, hasAttackSpirit and 'attack_spirit' or 'heal_spirit',shootFrom,origin,direction,httpService:GenerateGUID(),{drawDurationSeconds = 1, shotId = httpService:GenerateGUID(false)},workspace:GetServerTimeNow() - 0.045)
        end

        if playAnim.Enabled then
            bedwars.GameAnimationUtil:playAnimation(lplr.Character, bedwars.AnimationType.WIZARD_BALL_CAST )
            bedwars.SoundManager:playSound(bedwars.SoundList.SPIRIT_SUMMONER_CHANGE_AFFINITY,{})
        end
    end

	AutoUma = vape.Categories.Kits:CreateModule({
        Name = 'AutoUma',
        Tooltip = 'auto uses spirit staff usage for drops',
        Function = function(enabled)
            if not enabled then return end
            repeat
                local drops = collection('ItemDrop', AutoUma)
                local staff = findStaff()

                if staff and targetDrops.Enabled then
                    local atkCount = lplr:GetAttribute('ReadySummonedAttackSpirits') or 0
                    local healCount = lplr:GetAttribute('ReadySummonedHealSpirits') or 0

                    if autoSummon.Enabled and getItem('summon_stone') then
                        if useAttack.Enabled and atkCount < 1 then
                            bedwars.AbilityController:useAbility('summon_attack_spirit')
                        end

                        if useHeal.Enabled and healCount < 1 then
                            bedwars.AbilityController:useAbility('summon_heal_spirit')
                        end
                    end

                    if (atkCount + healCount) > 0 then
                        local root = entitylib.character.RootPart
                        if root then
                            local pos = root.Position
                            local target = getClosestDrop(pos, drops)

                            if target then
                                fireSpirit(staff, pos, target.Position, atkCount > 0)
                                task.wait(1.5)
                            end
                        end
                    end
                end

                task.wait(0.1)
            until not AutoUma.Enabled
        end
    })

    rangeSlider = AutoUma:CreateSlider({
        Name = 'Range',
        Min = 1,
        Max = 80,
        Default = 50,
        Decimal = 5,
        Suffix = function(v)
            return v == 1 and 'stud' or 'studs'
        end
    })

    playAnim = AutoUma:CreateToggle({
        Name = 'Play Animation',
        Default = true
    })

    limitToStaff = AutoUma:CreateToggle({
        Name = 'Only When Holding Staff',
        Default = true
    })

    autoSummon = AutoUma:CreateToggle({
        Name = 'Auto Summon',
        Function = function(state)
            pcall(function()
                useAttack.Object.Visible = state
                useHeal.Object.Visible = state
            end)
        end
    })

    useHeal = AutoUma:CreateToggle({
        Name = 'Heal Spirit',
        Default = true,
        Visible = false,
        Darker = true
    })

    useAttack = AutoUma:CreateToggle({
        Name = 'Attack Spirit',
        Default = true,
        Visible = false,
        Darker = true
    })

    targetDrops = AutoUma:CreateToggle({
        Name = 'Target Drops',
        Default = true,
        Function = function(state)
            pcall(function()
                allowEmerald.Object.Visible = state
                allowDiamond.Object.Visible = state
            end)
        end
    })

    allowEmerald = AutoUma:CreateToggle({
        Name = 'Emerald',
        Default = true,
        Darker = true
    })

    allowDiamond = AutoUma:CreateToggle({
        Name = 'Diamond',
        Default = true,
        Darker = true
    })
end)

run(function()
    local Caitlyn
    local MethodDropdown
    local LowHealthSlider
    local ExecuteRangeSlider
    local HitRangeSlider
    local ProximityRangeSlider
    local connections = {}
    local Players = playersService
    local lplr = Players.LocalPlayer
    local currentTarget = nil
    local lastHitTime = 0
    local lastContractSelect = 0
    
    local function selectContract(targetPlayer)
        if not entitylib.isAlive then return false end
        if tick() - lastContractSelect < 0.1 then return false end
        
        local storeState = bedwars.Store:getState()
        local activeContract = storeState.Kit.activeContract
        local availableContracts = storeState.Kit.availableContracts or {}
        
        if activeContract then return false end
        if #availableContracts == 0 then return false end
        
        for _, contract in pairs(availableContracts) do
            if contract.target == targetPlayer then
                bedwars.Client:Get(remotes.BloodAssassinSelectContract):SendToServer({
                    contractId = contract.id
                })
                lastContractSelect = tick()
                return true
            end
        end
        return false
    end
    
    local function executeOnLowHealth()
        if not currentTarget or tick() - lastHitTime > 3 then
            currentTarget = nil
            return
        end
        
        if not currentTarget.Character then return end
        
        local humanoid = currentTarget.Character:FindFirstChild("Humanoid")
        local rootPart = currentTarget.Character:FindFirstChild("HumanoidRootPart")
        
        if humanoid and rootPart and lplr.Character and lplr.Character:FindFirstChild("HumanoidRootPart") then
            local health = humanoid.Health
            local distance = (lplr.Character.HumanoidRootPart.Position - rootPart.Position).Magnitude
            
            if health > 0 and health <= LowHealthSlider.Value and distance <= ExecuteRangeSlider.Value then
                selectContract(currentTarget)
            end
        end
    end
    
    local function contractOnHit()
        if not currentTarget or tick() - lastHitTime > 0.5 then
            currentTarget = nil
            return
        end
        
        if not currentTarget.Character then return end
        
        local rootPart = currentTarget.Character:FindFirstChild("HumanoidRootPart")
        
        if rootPart and lplr.Character and lplr.Character:FindFirstChild("HumanoidRootPart") then
            local distance = (lplr.Character.HumanoidRootPart.Position - rootPart.Position).Magnitude
            
            if distance <= HitRangeSlider.Value then
                selectContract(currentTarget)
            end
        end
    end
    
    local function proximityContract()
        if not entitylib.isAlive then return end
        
        local myRoot = lplr.Character and lplr.Character:FindFirstChild("HumanoidRootPart")
        if not myRoot then return end
        
        local closestPlayer = nil
        local closestDistance = ProximityRangeSlider.Value
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= lplr and player.Character then
                local theirRoot = player.Character:FindFirstChild("HumanoidRootPart")
                local humanoid = player.Character:FindFirstChild("Humanoid")
                
                if theirRoot and humanoid and humanoid.Health > 0 then
                    local distance = (myRoot.Position - theirRoot.Position).Magnitude
                    
                    if distance < closestDistance then
                        closestDistance = distance
                        closestPlayer = player
                    end
                end
            end
        end
        
        if closestPlayer then
            selectContract(closestPlayer)
        end
    end
    
    Caitlyn = vape.Categories.Kits:CreateModule({
        Name = 'AutoCaitlyn',
        Function = function(callback)
            if callback then
                local damageConnection = vapeEvents.EntityDamageEvent.Event:Connect(function(damageTable)
                    if not entitylib.isAlive then return end
                    
                    local attacker = playersService:GetPlayerFromCharacter(damageTable.fromEntity)
                    local victim = playersService:GetPlayerFromCharacter(damageTable.entityInstance)
                
                    if attacker == lplr and victim and victim ~= lplr then
                        currentTarget = victim
                        lastHitTime = tick()
                    end
                end)
                table.insert(connections, damageConnection)
                
                task.spawn(function()
                    repeat
                        if entitylib.isAlive then
                            local method = MethodDropdown.Value
                            
                            if method == "Low HP" then
                                executeOnLowHealth()
                            elseif method == "Contract on Hit" then
                                contractOnHit()
                            elseif method == "Proximity Select" then
                                proximityContract()
                            end
                        end
                        task.wait(0.1)
                    until not Caitlyn.Enabled
                end)
            else
                for _, conn in pairs(connections) do
                    if typeof(conn) == "RBXScriptConnection" then
                        conn:Disconnect()
                    end
                end
                table.clear(connections)
                
                currentTarget = nil
                lastHitTime = 0
            end
        end,
        Tooltip = 'auto select contracts for cait based off varius situation'
    })
    
    MethodDropdown = Caitlyn:CreateDropdown({
        Name = 'Method',
        List = {"Low HP", "Contract on Hit", "Proximity Select"},
        Default = "Low HP",
        Tooltip = 'selection methods',
        Function = function(value)
            LowHealthSlider.Object.Visible = (value == "Low HP")
            ExecuteRangeSlider.Object.Visible = (value == "Low HP")
            HitRangeSlider.Object.Visible = (value == "Contract on Hit")
            ProximityRangeSlider.Object.Visible = (value == "Proximity Select")
        end
    })
    
    LowHealthSlider = Caitlyn:CreateSlider({
        Name = 'Select HP',
        Min = 10,
        Max = 100,
        Default = 30,
    })
    
    ExecuteRangeSlider = Caitlyn:CreateSlider({
        Name = 'Select Range',
        Min = 5,
        Max = 50,
        Default = 20,
        Suffix = ' studs',
    })
    
    HitRangeSlider = Caitlyn:CreateSlider({
        Name = 'Hit Range',
        Min = 10,
        Max = 200,
        Default = 100,
        Suffix = ' studs',
    })
    
    ProximityRangeSlider = Caitlyn:CreateSlider({
        Name = 'Proximity Range',
        Min = 10,
        Max = 200,
        Default = 50,
        Suffix = ' studs',
    })
    
    LowHealthSlider.Object.Visible = true
    ExecuteRangeSlider.Object.Visible = true
    HitRangeSlider.Object.Visible = false
    ProximityRangeSlider.Object.Visible = false
end)

run(function()
	local FishermanESP
	local FishNames = {
		fish_iron = "Iron Fish",
		fish_diamond = "Diamond Fish",
		fish_emerald = "Emerald Fish",
		fish_special = "Special Fish",
		fish_gold = "Gold Fish",
	}
	
	FishermanESP = vape.Categories.Kits:CreateModule({
		Name = "FishermanESP",
		Tooltip = 'shows what fish you are catching before the minigame starts',
		Function = function(callback)		
			if callback then		
				local exp = bedwars.Client:WaitFor(remotes.FishFound):expect()
				FishermanESP:Clean(exp:Connect(function(p24)
					local scl = p24.dropData
					if scl and scl.fishModel then
						local ftype = tostring(scl.fishModel) or 'fish_iron'
						local allDrops = scl.drops
						local lootParts = {}
						if allDrops then
							local totals = {}
							local order = {}
							for _, drop in ipairs(allDrops) do
								local item = tostring(drop.itemType) or 'unknown'
								local amount = tonumber(drop.amount) or 0
								if not totals[item] then
									totals[item] = 0
									table.insert(order, item)
								end
								totals[item] = totals[item] + amount
							end
							for _, item in ipairs(order) do
								table.insert(lootParts, item .. ' x' .. math.ceil(totals[item] * 1.4))
							end
						end
						local lootText = #lootParts > 0 and table.concat(lootParts, ', ') or 'unknown'
						notif('FishermanESP', 'Your fish will be ' .. FishNames[ftype] .. ' | Loot: ' .. lootText, 12)
					end
				end))
			end
		end
	})
end)

run(function()
	local RavenTP
	
	RavenTP = vape.Categories.Kits:CreateModule({
		Name = 'RavenTP',
		Function = function(callback)
			if callback then
				RavenTP:Toggle()
				local plr = entitylib.EntityMouse({
					Range = 1000,
					Players = true,
					Part = 'RootPart'
				})
	
				if getItem('raven') and plr then
					bedwars.Client:Get(remotes.SpawnRaven):CallServerAsync():andThen(function(projectile)
						if projectile then
							local ravenPart = projectile:FindFirstChild("Root") or projectile:FindFirstChildWhichIsA("BasePart")
							
							if ravenPart then
								local bodyforce = Instance.new('BodyForce')
								bodyforce.Force = Vector3.new(0, ravenPart.AssemblyMass * workspace.Gravity, 0)
								bodyforce.Parent = ravenPart
		
								if plr then
									task.spawn(function()
										for _ = 1, 20 do
											if plr.RootPart and ravenPart then
												ravenPart.CFrame = CFrame.lookAlong(plr.RootPart.Position, gameCamera.CFrame.LookVector)
											end
											task.wait(0.05)
										end
									end)
									task.wait(0.3)
									bedwars.RavenController:detonateRaven()
								end
							end
						end
					end)
				end
			end
		end,
		Tooltip = 'spawns and teleports a raven to a player\nnear your mouse'
	})
end)

run(function()
    local Kaliyah
    local AutoPunch
    local RangeSlider
    local PunchDelay
    local DelaySlider
    local NoSlow
    local punchActive = false
    local punchDebounce = {}

    local function getKaliyahTargets()
        local targets = {}
        if not entitylib.isAlive then return targets end
        
        local localPosition = entitylib.character.RootPart.Position
        local range = RangeSlider.Value
        
        for _, v in collectionService:GetTagged('KaliyahPunchInteraction') do
            if v:IsA("Model") and v.PrimaryPart then
                local distance = (localPosition - v.PrimaryPart.Position).Magnitude
                if distance <= range then
                    table.insert(targets, v)
                end
            end
        end
        
        return targets
    end

    local function punchTarget(target)
        local targetId = target:GetAttribute('Id') or tostring(target)
        
        if punchDebounce[targetId] then return false end
        punchDebounce[targetId] = true
        
        local character = lplr.Character
        if not character or not character.PrimaryPart then 
            punchDebounce[targetId] = nil
            return false 
        end
        
        pcall(function()
            bedwars.DragonSlayerController:deleteEmblem(target)
        end)
        
        local playerPos = character:GetPrimaryPartCFrame().Position
        local targetPos = target:GetPrimaryPartCFrame().Position * Vector3.new(1, 0, 1) + Vector3.new(0, playerPos.Y, 0)
        local lookAtCFrame = CFrame.new(playerPos, targetPos)
        
        character:PivotTo(lookAtCFrame)
        
        pcall(function()
            bedwars.DragonSlayerController:playPunchAnimation(lookAtCFrame - lookAtCFrame.Position)
        end)
        
        local success = pcall(function()
            bedwars.Client:Get(remotes.RequestDragonPunch):SendToServer({
                target = target
            })
        end)
        
        task.delay(3, function()
            punchDebounce[targetId] = nil
        end)
        
        return success
    end

    local function startAutoPunch()
        if punchActive then return end
        punchActive = true
        
        task.spawn(function()
            while Kaliyah.Enabled and AutoPunch.Enabled and punchActive do
                if not entitylib.isAlive then 
                    task.wait(0.5)
                    continue 
                end
                
                local targets = getKaliyahTargets()
                local punchedThisCycle = false
                
                for _, target in targets do
                    if not Kaliyah.Enabled or not AutoPunch.Enabled or not punchActive then 
                        break 
                    end
                    
                    if PunchDelay.Enabled and DelaySlider.Value > 0 then
                        task.wait(DelaySlider.Value)
                    end
                    
                    if punchTarget(target) then
                        punchedThisCycle = true
                        task.wait(0.2)
                    end
                end
                
                task.wait(punchedThisCycle and 0.5 or 0.3)
            end
            
            punchActive = false
        end)
    end

    local function stopAutoPunch()
        punchActive = false
        table.clear(punchDebounce)
    end

    local originalPlayPunchAnimation
    local function hookNoSlow()
        if not bedwars.DragonSlayerController then return end
        
        originalPlayPunchAnimation = bedwars.DragonSlayerController.playPunchAnimation
        
        bedwars.DragonSlayerController.playPunchAnimation = function(self, arg2)
            if NoSlow.Enabled then
                local any_import_result1_6_upvr = debug.getupvalue(originalPlayPunchAnimation, 1)
                local GameAnimationUtil_upvr = debug.getupvalue(originalPlayPunchAnimation, 2)
                local Players_upvr = debug.getupvalue(originalPlayPunchAnimation, 3)
                local AnimationType_upvr = debug.getupvalue(originalPlayPunchAnimation, 4)
                local KnitClient_upvr = debug.getupvalue(originalPlayPunchAnimation, 5)
                local RunService_upvr = debug.getupvalue(originalPlayPunchAnimation, 6)
                
                local any_new_result1_upvr_2 = any_import_result1_6_upvr.new()
                local any_playAnimation_result1_upvr_2 = GameAnimationUtil_upvr:playAnimation(Players_upvr.LocalPlayer, AnimationType_upvr.DRAGON_SLAYER_PUNCH)
                any_new_result1_upvr_2:GiveTask(function()
                    local var137 = any_playAnimation_result1_upvr_2
                    if var137 ~= nil then
                        var137:Stop()
                    end
                end)
                
                any_new_result1_upvr_2:GiveTask(RunService_upvr.Heartbeat:Connect(function()
                    local Character = Players_upvr.LocalPlayer.Character
                    local var141 = Character
                    if var141 ~= nil then
                        var141 = var141.PrimaryPart
                    end
                    if not var141 then
                        any_new_result1_upvr_2:DoCleaning()
                        return nil
                    end
                    Character:PivotTo(CFrame.new(Character:GetPrimaryPartCFrame().Position) * arg2)
                end))
                
                task.delay(0.46, function()
                    any_new_result1_upvr_2:DoCleaning()
                end)
                
                return any_new_result1_upvr_2
            else
                return originalPlayPunchAnimation(self, arg2)
            end
        end
    end

    local function unhookNoSlow()
        if originalPlayPunchAnimation and bedwars.DragonSlayerController then
            bedwars.DragonSlayerController.playPunchAnimation = originalPlayPunchAnimation
        end
    end

    Kaliyah = vape.Categories.Kits:CreateModule({
        Name = 'AutoKaliyah',
        Function = function(callback)
            if callback then
                if AutoPunch.Enabled then
                    startAutoPunch()
                end
                if NoSlow.Enabled then
                    hookNoSlow()
                end
            else
                stopAutoPunch()
                unhookNoSlow()
            end
        end,
        Tooltip = 'autokit with more features lmao'
    })
    
    AutoPunch = Kaliyah:CreateToggle({
        Name = 'Auto Punch',
        Default = false,
        Function = function(callback)
            if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = callback end
            if PunchDelay and PunchDelay.Object then PunchDelay.Object.Visible = callback end
            if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = (callback and PunchDelay.Enabled) end
            if not callback then
                if DelaySlider and DelaySlider.Object then
                    DelaySlider.Object.Visible = false
                end
            else
                if PunchDelay and PunchDelay.Enabled then
                    if DelaySlider and DelaySlider.Object then
                        DelaySlider.Object.Visible = true
                    end
                end
            end
            
            if Kaliyah.Enabled then
                if callback then
                    startAutoPunch()
                else
                    stopAutoPunch()
                end
            end
        end
    })
    
    RangeSlider = Kaliyah:CreateSlider({
        Name = 'Range',
        Min = 1, 
        Max = 100,
        Default = 18,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    PunchDelay = Kaliyah:CreateToggle({
        Name = 'Punch Delay',
        Default = false,
        Function = function(callback)
            if DelaySlider and DelaySlider.Object then
                DelaySlider.Object.Visible = callback
            end
        end
    })
    
    DelaySlider = Kaliyah:CreateSlider({
        Name = 'Delay',
        Min = 1,
        Max = 3,
        Default = 1,
        Decimal = 10,
        Suffix = 's',
    })
    
    NoSlow = Kaliyah:CreateToggle({
        Name = 'No Slow',
        Default = false,
        Function = function(callback)
            if Kaliyah.Enabled then
                if callback then
                    hookNoSlow()
                else
                    unhookNoSlow()
                end
            end
        end
    })

    task.defer(function()
        if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = false end
        if PunchDelay and PunchDelay.Object then PunchDelay.Object.Visible = false end
        if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = false end
    end)
end)

run(function()
    local Grove
    local NoSlow
    local NoSlowOnAbility
    local AutoWater
    local AutoWaterRange
    local AutoCollect
    local CollectRange
    local SpiritESP
    local ESPNotify
    local ESPBackground
    local ESPColor
    local DistanceCheck
    local DistanceLimit
    
    local Folder = Instance.new('Folder')
    Folder.Parent = vape.gui
    local Reference = {}
    local lastNotification = 0
    local spawnQueue = {}
    local notificationCooldown = 1
    local noSlowActive = false
    local autoWaterActive = false
    local autoCollectActive = false
    local originalDisableActionsOnCharge
    local originalCheckForPickup
    
    local function sendNotification(count)
        notif("Spirit ESP", string.format("%d spirit orbs spawned", count), 3)
    end

    local function processSpawnQueue()
        if #spawnQueue > 0 then
            local currentTime = tick()
            if currentTime - lastNotification >= notificationCooldown then
                sendNotification(#spawnQueue)
                lastNotification = currentTime
                spawnQueue = {}
            else
                task.delay(notificationCooldown - (currentTime - lastNotification), function()
                    if #spawnQueue > 0 then
                        sendNotification(#spawnQueue)
                        spawnQueue = {}
                    end
                end)
            end
        end
    end

    local function getProperImage()
        return bedwars.getIcon({itemType = 'spirit'}, true)
    end

    local function Added(v)
        if Reference[v] then return end
        local _bpUserId = v:GetAttribute('PlacedByUserId')
        if _bpUserId then
            local _bpOk, _bpOwner = pcall(function() return playersService:GetPlayerByUserId(_bpUserId) end)
        end
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = Folder
        billboard.Name = 'spirit-energy'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(36, 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = v
        
        local blur = addBlur(billboard)
        blur.Visible = ESPBackground.Enabled
        
        local image = Instance.new('ImageLabel')
        image.Size = UDim2.fromOffset(36, 36)
        image.Position = UDim2.fromScale(0.5, 0.5)
        image.AnchorPoint = Vector2.new(0.5, 0.5)
        image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
        image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
        image.BorderSizePixel = 0
        image.Image = getProperImage()
        image.Parent = billboard
        
        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 4)
        uicorner.Parent = image
        
        Reference[v] = billboard
        
        if ESPNotify.Enabled then
            table.insert(spawnQueue, {item = 'spirit', time = tick()})
            processSpawnQueue()
        end
    end

    local function Removed(v)
        if Reference[v] then
            Reference[v]:Destroy()
            Reference[v] = nil
        end
    end

    local function setupESP()
        for _, v in workspace:GetChildren() do
            if v.Name == "SpiritGardenerEnergy" and v:IsA("Model") and v.PrimaryPart then
                Added(v.PrimaryPart)
            end
        end

        Grove:Clean(workspace.ChildAdded:Connect(function(v)
            if v.Name == "SpiritGardenerEnergy" and v:IsA("Model") then
                task.wait(0.1)
                if v.PrimaryPart then
                    Added(v.PrimaryPart)
                end
            end
        end))

        Grove:Clean(workspace.ChildRemoved:Connect(function(v)
            if v.Name == "SpiritGardenerEnergy" and v.PrimaryPart then
                Removed(v.PrimaryPart)
            end
        end))

        Grove:Clean(runService.RenderStepped:Connect(function()
            if not SpiritESP.Enabled then return end
            
            for v, billboard in pairs(Reference) do
                if not v or not v.Parent then
                    Removed(v)
                    continue
                end

                local shouldShow = true

                if shouldShow and DistanceCheck.Enabled and entitylib.isAlive then
                    local distance = (entitylib.character.RootPart.Position - v.Position).Magnitude
                    if distance < DistanceLimit.ValueMin or distance > DistanceLimit.ValueMax then
                        shouldShow = false
                    end
                end

                billboard.Enabled = shouldShow
            end
        end))
    end

    local function getNearbyFlowers()
        local flowers = {}
        if not entitylib.isAlive then return flowers end
        
        local localPosition = entitylib.character.RootPart.Position
        local range = AutoWaterRange.Value
        
        for _, v in collectionService:GetTagged('SpiritGardenerFlower') do
            if v:IsA("Model") and v.PrimaryPart then
                if v:GetAttribute("PlacedByUserId") == lplr.UserId then
                    local needsEnergy = not v:GetAttribute("HasFullyGrown")
                    if needsEnergy then
                        local distance = (localPosition - v.PrimaryPart.Position).Magnitude
                        if distance <= range then
                            table.insert(flowers, v)
                        end
                    end
                end
            end
        end
        
        return flowers
    end

    local function useWaterAbility()
        local success = pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility"):FireServer("spirit_gardener_water")
        end)
        return success
    end

    local function startAutoWater()
        if autoWaterActive then return end
        autoWaterActive = true
        
        task.spawn(function()
            while Grove.Enabled and AutoWater.Enabled and autoWaterActive do
                if not entitylib.isAlive then 
                    task.wait(0.5)
                    continue 
                end
                
                local flowers = getNearbyFlowers()
                
                if #flowers > 0 then
                    if useWaterAbility() then
                        task.wait(0.6) 
                    else
                        task.wait(0.3)
                    end
                else
                    task.wait(0.5)
                end
            end
            
            autoWaterActive = false
        end)
    end

    local function stopAutoWater()
        autoWaterActive = false
    end

    local function hookAutoCollect()
        if not bedwars.SpiritGardenerSeedController then return end
        
        originalCheckForPickup = bedwars.SpiritGardenerSeedController.checkForPickup
        
        bedwars.SpiritGardenerSeedController.checkForPickup = function(self)
            if not AutoCollect.Enabled then
                return originalCheckForPickup(self)
            end
            
            local Players = playersService
            local CollectionService = collectionService
            local Workspace = game:GetService("Workspace")
            
            local Character = Players.LocalPlayer.Character
            if not Character or not Character.PrimaryPart then
                return nil
            end
            
            local localPosition = Character.PrimaryPart.Position
            local range = CollectRange.Value
            
            local validTypes = self:validCollectableEntityTypes()
            
            for _, collectableType in validTypes do
                local tagged = CollectionService:GetTagged(collectableType)
                
                for _, orb in tagged do
                    local spawnTime = orb:GetAttribute("SpawnTime")
                    if spawnTime and (Workspace:GetServerTimeNow() - spawnTime) >= 1 then
                        local orbPosition = orb:GetPivot().Position
                        local distance = (localPosition - orbPosition).Magnitude
                        
                        if distance <= range then
                            self:collectEntity(Players.LocalPlayer, orb, collectableType)
                        end
                    end
                end
            end
        end
    end

    local function unhookAutoCollect()
        if originalCheckForPickup and bedwars.SpiritGardenerSeedController then
            bedwars.SpiritGardenerSeedController.checkForPickup = originalCheckForPickup
        end
    end

    local function startAutoCollect()
        if autoCollectActive then return end
        autoCollectActive = true
        
        hookAutoCollect()
        
        if bedwars.SpiritGardenerSeedController then
            pcall(function()
                bedwars.SpiritGardenerSeedController:listenToPickup()
            end)
        end
    end

    local function stopAutoCollect()
        autoCollectActive = false
        unhookAutoCollect()
    end

    local function hookNoSlow()
        if not bedwars.SpiritGardenerController then return end
        
        originalDisableActionsOnCharge = bedwars.SpiritGardenerController.disableActionsOnCharge
        
        bedwars.SpiritGardenerController.disableActionsOnCharge = function(self, maid, character)
            if not NoSlow.Enabled then
                return originalDisableActionsOnCharge(self, maid, character)
            end
            
            if NoSlowOnAbility.Enabled then
                local isLocalPlayer = character == lplr.Character
                if not isLocalPlayer then
                    return originalDisableActionsOnCharge(self, maid, character)
                end
            end
            
            if character == lplr.Character then
                local KnitClient = bedwars.KnitClient
                
                KnitClient.Controllers.SwordController:toggleSwordSwing(true)
                KnitClient.Controllers.BlockPlacementController:disableBlockPlacer()
                
                local ClientSyncEvents = debug.getupvalue(originalDisableActionsOnCharge, 3)
                local projectileConnection = ClientSyncEvents.BeginProjectileTargeting:connect(function(event)
                    event:setCancelled(true)
                    return nil
                end)
                
                local jumpModifier = KnitClient.Controllers.JumpHeightController:getJumpModifier():addModifier({
                    jumpHeightMultiplier = 0;
                })
                
                maid:GiveTask(function()
                    KnitClient.Controllers.SwordController:toggleSwordSwing(false)
                    KnitClient.Controllers.BlockPlacementController:enableBlockPlacer()
                    projectileConnection:Destroy()
                    jumpModifier.Destroy()
                end)
            end
        end
    end

    local function unhookNoSlow()
        if originalDisableActionsOnCharge and bedwars.SpiritGardenerController then
            bedwars.SpiritGardenerController.disableActionsOnCharge = originalDisableActionsOnCharge
        end
    end

    Grove = vape.Categories.Kits:CreateModule({
        Name = 'AutoGrove',
        Function = function(callback)
            if callback then
                if SpiritESP.Enabled then 
                    setupESP() 
                end
                
                if NoSlow.Enabled then
                    hookNoSlow()
                end
                
                if AutoWater.Enabled then
                    startAutoWater()
                end
                
                if AutoCollect.Enabled then
                    startAutoCollect()
                end
            else
                stopAutoWater()
                stopAutoCollect()
                unhookNoSlow()
                Folder:ClearAllChildren()
                table.clear(Reference)
                table.clear(spawnQueue)
                lastNotification = 0
            end
        end,
        Tooltip = 'cool features for grove '
    })
    
    NoSlow = Grove:CreateToggle({
        Name = 'No Slow',
        Default = false,
        Tooltip = 'Remove movement lock when using water ability',
        Function = function(callback)
            if NoSlowOnAbility and NoSlowOnAbility.Object then 
                NoSlowOnAbility.Object.Visible = callback 
            end
            
            if Grove.Enabled then
                if callback then
                    hookNoSlow()
                else
                    unhookNoSlow()
                end
            end
        end
    })
    
    NoSlowOnAbility = Grove:CreateToggle({
        Name = 'Only On Ability Use',
        Default = false,
        Tooltip = 'noslow down only works when u manually use the ability'
    })
    
    AutoWater = Grove:CreateToggle({
        Name = 'Auto Water',
        Default = false,
        Tooltip = 'uses water ability on nearby flowers',
        Function = function(callback)
            if AutoWaterRange and AutoWaterRange.Object then 
                AutoWaterRange.Object.Visible = callback 
            end
            
            if Grove.Enabled then
                if callback then
                    startAutoWater()
                else
                    stopAutoWater()
                end
            end
        end
    })
    
    AutoWaterRange = Grove:CreateSlider({
        Name = 'Water Range',
        Min = 1, 
        Max = 30,
        Default = 20,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    AutoCollect = Grove:CreateToggle({
        Name = 'Auto Collect',
        Default = false,
        Function = function(callback)
            if CollectRange and CollectRange.Object then 
                CollectRange.Object.Visible = callback 
            end
            
            if Grove.Enabled then
                if callback then
                    startAutoCollect()
                else
                    stopAutoCollect()
                end
            end
        end
    })
    
    CollectRange = Grove:CreateSlider({
        Name = 'Collect Range',
        Min = 5, 
        Max = 12,
        Default = 12,
        Decimal = 10,
        Suffix = ' studs',
    })
    
    SpiritESP = Grove:CreateToggle({
        Name = 'Spirit ESP',
        Default = false,
        Function = function(callback)
            if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = callback end
            if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            if DistanceCheck and DistanceCheck.Object then DistanceCheck.Object.Visible = callback end
            if DistanceLimit and DistanceLimit.Object then
                DistanceLimit.Object.Visible = (callback and DistanceCheck.Enabled)
            end

            if not callback then
                if ESPColor and ESPColor.Object then
                    ESPColor.Object.Visible = false
                end
                if DistanceLimit and DistanceLimit.Object then
                    DistanceLimit.Object.Visible = false
                end
            else
                if ESPBackground and ESPBackground.Enabled then
                    if ESPColor and ESPColor.Object then
                        ESPColor.Object.Visible = true
                    end
                end
                if DistanceCheck and DistanceCheck.Enabled then
                    if DistanceLimit and DistanceLimit.Object then
                        DistanceLimit.Object.Visible = true
                    end
                end
            end
            
            if Grove.Enabled then
                if callback then 
                    setupESP() 
                else
                    Folder:ClearAllChildren()
                    table.clear(Reference)
                end
            end
        end
    })
    
    ESPNotify = Grove:CreateToggle({
        Name = 'Notify',
        Default = false,
    })
    
    ESPBackground = Grove:CreateToggle({
        Name = 'Background',
        Default = true,
        Function = function(callback)
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    local blur = v:FindFirstChild("BlurEffect")
                    if blur then blur.Visible = callback end
                    v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
                end
            end
        end
    })
    
    ESPColor = Grove:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0.5,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                    v.ImageLabel.BackgroundTransparency = 1 - opacity
                end
            end
        end,
        Darker = true
    })
    
    DistanceCheck = Grove:CreateToggle({
        Name = 'Distance Check',
        Default = false,
        Function = function(callback)
            if DistanceLimit and DistanceLimit.Object then
                DistanceLimit.Object.Visible = callback
            end
        end
    })
    
    DistanceLimit = Grove:CreateTwoSlider({
        Name = 'Spirit Distance',
        Min = 0,
        Max = 256,
        DefaultMin = 0,
        DefaultMax = 64,
        Darker = true,
    })

    task.defer(function()
        if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = false end
        if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = false end
        if ESPColor and ESPColor.Object then ESPColor.Object.Visible = false end
        if DistanceCheck and DistanceCheck.Object then DistanceCheck.Object.Visible = false end
        if DistanceLimit and DistanceLimit.Object then DistanceLimit.Object.Visible = false end
        if AutoWaterRange and AutoWaterRange.Object then
            AutoWaterRange.Object.Visible = false
        end
        if CollectRange and CollectRange.Object then
            CollectRange.Object.Visible = false
        end
        if NoSlowOnAbility and NoSlowOnAbility.Object then
            NoSlowOnAbility.Object.Visible = false
        end
    end)
end)

run(function()
    local Lucia
    local AutoDepositToggle
    local RangeSlider
    local DelayToggle
    local DelaySlider
    local LuciaESPToggle
    local CandyESPToggle
    local IgnoreTeammatesESP
    local ESPBackground
    local ESPColor = {}
    local LuciaSpyToggle
    local IgnoreTeammatesSpy
    local DisplayNameToggle
    local Players = playersService
    local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local Reference = {}
	local collectedPinatas = {}
	local trackedPinatas = {}

    local function kitCollection(id, func, range, specific)
        repeat
            if entitylib.isAlive then
                local objs = type(id) == 'table' and id or collection(id, Lucia)
                local localPosition = entitylib.character.RootPart.Position
                for _, v in objs do
                    if not Lucia.Enabled then break end
                    local part = not v:IsA('Model') and v or v.PrimaryPart
                    if part and (part.Position - localPosition).Magnitude <= range then
                        local success, err = pcall(func, v)
                        if not success then
                            warn("lucia deposit error:", err)
                        end
                        if DelayToggle.Enabled then
                            task.wait(DelaySlider.Value)
                        else
                            task.wait(0.05)
                        end
                    end
                end
            end
            task.wait(0.1)
        until not Lucia.Enabled
    end

    local function isTeammateESP(pinataPart)
        if not IgnoreTeammatesESP.Enabled then return false end

        local placerId = pinataPart:GetAttribute("PlacedByUserId") or pinataPart:GetAttribute("PlacerId")
        if not placerId then
            local parent = pinataPart.Parent
            if parent then
                placerId = parent:GetAttribute("PlacedByUserId") or parent:GetAttribute("PlacerId")
            end
        end

        if placerId then
            if placerId == lplr.UserId then
                return true
            end

            local placer = playersService:GetPlayerByUserId(placerId)
            if placer and placer.Team == lplr.Team then
                return true
            end
        end

        return false
    end

    local function isTeammateSpy(pinataPart)
        if not IgnoreTeammatesSpy.Enabled then return false end

        local placerId = pinataPart:GetAttribute("PlacedByUserId") or pinataPart:GetAttribute("PlacerId")
        if not placerId then
            local parent = pinataPart.Parent
            if parent then
                placerId = parent:GetAttribute("PlacedByUserId") or parent:GetAttribute("PlacerId")
            end
        end

        if placerId then
            if placerId == lplr.UserId then
                return true
            end

            local placer = playersService:GetPlayerByUserId(placerId)
            if placer and placer.Team == lplr.Team then
                return true
            end
        end

        return false
    end

    local function getCandyAmount(pinataPart)
        local coins = pinataPart:GetAttribute("Coin")
        return coins or 0
    end

    local function getProperIcon(iconType)
        local icon = bedwars.getIcon({itemType = iconType}, true)
        if not icon or icon == "" then
            return nil
        end
        return icon
    end

    local function Added(pinataPart)
        if isTeammateESP(pinataPart) then
            return
        end

        if Reference[pinataPart] then return end

        local billboard = Instance.new('BillboardGui')
        billboard.Parent = Folder
        billboard.Name = 'pinata'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(CandyESPToggle.Enabled and 80 or 36, 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = pinataPart

        local blur = addBlur(billboard)
        blur.Visible = ESPBackground.Enabled

        local frame = Instance.new('Frame')
        frame.Size = UDim2.fromScale(1, 1)
        frame.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
        frame.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
        frame.BorderSizePixel = 0
        frame.Parent = billboard

        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 4)
        uicorner.Parent = frame

        local pinataIcon = getProperIcon('pinata')
        if pinataIcon then
            local image = Instance.new('ImageLabel')
            image.Name = 'PinataIcon'
            image.Size = UDim2.fromOffset(36, 36)
            image.Position = UDim2.new(0, 0, 0.5, 0)
            image.AnchorPoint = Vector2.new(0, 0.5)
            image.BackgroundTransparency = 1
            image.Image = pinataIcon
            image.Parent = frame
        end

        local candyAmount = nil
        local candyIcon = nil

        if CandyESPToggle.Enabled then
            candyAmount = Instance.new('TextLabel')
            candyAmount.Name = 'CandyAmount'
            candyAmount.Size = UDim2.fromOffset(25, 20)
            candyAmount.Position = UDim2.new(0, 40, 0.5, 0)
            candyAmount.AnchorPoint = Vector2.new(0, 0.5)
            candyAmount.BackgroundTransparency = 1
            candyAmount.Text = tostring(getCandyAmount(pinataPart))
            candyAmount.TextColor3 = Color3.fromRGB(255, 255, 255)
            candyAmount.TextSize = 16
            candyAmount.Font = Enum.Font.GothamBold
            candyAmount.TextStrokeTransparency = 0.5
            candyAmount.TextStrokeColor3 = Color3.new(0, 0, 0)
            candyAmount.Parent = frame

            local candyIconImage = getProperIcon('candy')
            if candyIconImage then
                candyIcon = Instance.new('ImageLabel')
                candyIcon.Name = 'CandyIcon'
                candyIcon.Size = UDim2.fromOffset(18, 18)
                candyIcon.Position = UDim2.new(0, 65, 0.5, 0)
                candyIcon.AnchorPoint = Vector2.new(0, 0.5)
                candyIcon.BackgroundTransparency = 1
                candyIcon.Image = candyIconImage
                candyIcon.Parent = frame
            end
        end

        Reference[pinataPart] = {
            billboard = billboard,
            frame = frame,
            candyAmount = candyAmount,
            candyIcon = candyIcon
        }
    end

    local function Removed(pinataPart)
        if Reference[pinataPart] then
            Reference[pinataPart].billboard:Destroy()
            Reference[pinataPart] = nil
        end
    end

    local function updateCandyDisplay(pinataPart)
        local ref = Reference[pinataPart]
        if not ref then return end

        if CandyESPToggle.Enabled then
            if not ref.candyAmount then
                ref.candyAmount = Instance.new('TextLabel')
                ref.candyAmount.Name = 'CandyAmount'
                ref.candyAmount.Size = UDim2.fromOffset(25, 20)
                ref.candyAmount.Position = UDim2.new(0, 40, 0.5, 0)
                ref.candyAmount.AnchorPoint = Vector2.new(0, 0.5)
                ref.candyAmount.BackgroundTransparency = 1
                ref.candyAmount.TextColor3 = Color3.fromRGB(255, 255, 255)
                ref.candyAmount.TextSize = 16
                ref.candyAmount.Font = Enum.Font.GothamBold
                ref.candyAmount.TextStrokeTransparency = 0.5
                ref.candyAmount.TextStrokeColor3 = Color3.new(0, 0, 0)
                ref.candyAmount.Parent = ref.frame

                local candyIconImage = getProperIcon('candy')
                if candyIconImage and not ref.candyIcon then
                    ref.candyIcon = Instance.new('ImageLabel')
                    ref.candyIcon.Name = 'CandyIcon'
                    ref.candyIcon.Size = UDim2.fromOffset(18, 18)
                    ref.candyIcon.Position = UDim2.new(0, 65, 0.5, 0)
                    ref.candyIcon.AnchorPoint = Vector2.new(0, 0.5)
                    ref.candyIcon.BackgroundTransparency = 1
                    ref.candyIcon.Image = candyIconImage
                    ref.candyIcon.Parent = ref.frame
                end

                ref.billboard.Size = UDim2.fromOffset(80, 36)
            end

            if ref.candyAmount then
                ref.candyAmount.Text = tostring(getCandyAmount(pinataPart))
            end
        else
            if ref.candyAmount then
                ref.candyAmount:Destroy()
                ref.candyAmount = nil
            end
            if ref.candyIcon then
                ref.candyIcon:Destroy()
                ref.candyIcon = nil
            end
            ref.billboard.Size = UDim2.fromOffset(36, 36)
        end
    end

    local function findExistingPinatas()
        scanDescendants(workspace, function(obj)
            if obj:IsA("BasePart") and obj.Name == "pinata" then
                if not Reference[obj] and not isTeammateESP(obj) then
                    Added(obj)
                end
            end
        end)
    end

    local function refreshESP()
        Folder:ClearAllChildren()
        table.clear(Reference)
        findExistingPinatas()
    end

    local function getLuciaPlayerName(player)
		return getPlayerName(player, DisplayNameToggle and DisplayNameToggle.Enabled)
	end

    local function getTeamName(player)
		return player.Team and player.Team.Name or "Unknown"
	end

    local function setupLuciaSpy()
        local util = require(game:GetService("ReplicatedStorage").TS.games.bedwars.kit.kits['piggy-bank']['piggy-bank-util']).PiggyBankUtil

        scanDescendants(workspace, function(obj)
            if obj:IsA("BasePart") and obj.Name == "pinata" then
                if not isTeammateSpy(obj) then
                    local placerId = obj:GetAttribute("PlacedByUserId") or obj:GetAttribute("PlacerId")

                    if placerId then
                        local placer = playersService:GetPlayerByUserId(placerId)
                        local initialCandy = getCandyAmount(obj)

                        trackedPinatas[obj] = {
                            player = placer,
                            lastCandy = initialCandy,
                            exists = true,
                            placedTime = tick()
                        }
                    end
                end
            end
        end)

        Lucia:Clean(workspace.DescendantAdded:Connect(function(obj)
            if not LuciaSpyToggle.Enabled then return end

            if obj:IsA("BasePart") and obj.Name == "pinata" then
                task.wait(0.2)

                if not isTeammateSpy(obj) then
                    local placerId = obj:GetAttribute("PlacedByUserId") or obj:GetAttribute("PlacerId")

                    if placerId then
                        local placer = playersService:GetPlayerByUserId(placerId)
                        local initialCandy = getCandyAmount(obj)

                        trackedPinatas[obj] = {
                            player = placer,
                            lastCandy = initialCandy,
                            exists = true,
                            placedTime = tick()
                        }
                    end
                end
            end
        end))

        Lucia:Clean(bedwars.Client:Get("PiggyBankPop"):Connect(function(self)
            if not LuciaSpyToggle.Enabled then return end
            local plr = self.awardedPlayer
            if not plr then return end
            if IgnoreTeammatesSpy.Enabled then
                if plr == lplr or (plr.Team and plr.Team == lplr.Team) then
                    return
                end
            end

            local rewards = util:getRewardsFromCoins(self.coins)
            local I, D, E = 0, 0, 0
            for _, reward in ipairs(rewards) do
                if reward.itemType == "iron" then
                    I = I + (reward.amount or 0)
                elseif reward.itemType == "diamond" then
                    D = D + (reward.amount or 0)
                elseif reward.itemType == "emerald" then
                    E = E + (reward.amount or 0)
                end
            end

            local playerName = getPlayerName(plr)
            local teamName = getTeamName(plr)
            local loot = string.format("%d irons, %d diamonds, %d emeralds", I, D, E)

            vape:CreateNotification(
                "Lucia Spy",
                string.format("%s (%s) opened their pinata and got %s", playerName, teamName, loot),
                8
            )

            for pinataPart, data in pairs(trackedPinatas) do
                if data.player and data.player.UserId == plr.UserId then
                    trackedPinatas[pinataPart] = nil
                end
            end
        end))

        local luciaSpyCounter = 0
        Lucia:Clean(RunService.Heartbeat:Connect(function()
            if not LuciaSpyToggle.Enabled then return end
            luciaSpyCounter = luciaSpyCounter + 1
            if luciaSpyCounter % 6 ~= 0 then return end
            local toRemove = {}
            for pinataPart, data in pairs(trackedPinatas) do
                if pinataPart and pinataPart.Parent then
                    local currentCandy = getCandyAmount(pinataPart)

                    if currentCandy ~= data.lastCandy then
                        local difference = currentCandy - data.lastCandy

                        if difference > 0 and data.player then
                                local playerName = getPlayerName(data.player)
                                local teamName = getTeamName(data.player)

                                vape:CreateNotification(
                                    "Lucia Spy",
                                    string.format("%s (%s) has just deposited %d candy and now has %d candy",
                                        playerName, teamName, difference, currentCandy),
                                    5
                                )
                            end
                            data.lastCandy = currentCandy
                        end
                else
                    if data.exists and data.player then
                        local timeSincePlaced = tick() - (data.placedTime or tick())

                        if timeSincePlaced > 2 then
                                local playerName = getPlayerName(data.player)
                                local teamName = getTeamName(data.player)

                                vape:CreateNotification(
                                    "Lucia Spy",
                                    string.format("%s (%s) has just broken their pinata with %d candy",
                                        playerName, teamName, data.lastCandy),
                                    5
                                )
                            end
                        end

                    table.insert(toRemove, pinataPart)
                end
            end

            for _, pinataPart in ipairs(toRemove) do
                trackedPinatas[pinataPart] = nil
            end
        end))
    end

    Lucia = vape.Categories.Kits:CreateModule({
        Name = 'AutoLucia',
        Function = function(callback)
            if callback then
                if LuciaESPToggle.Enabled then
                    findExistingPinatas()

                    Lucia:Clean(workspace.DescendantAdded:Connect(function(obj)
                        if Lucia.Enabled and obj:IsA("BasePart") and obj.Name == "pinata" then
                            task.wait(0.1)
                            if not isTeammateESP(obj) then
                                Added(obj)
                            end
                        end
                    end))

                    Lucia:Clean(workspace.DescendantRemoving:Connect(function(obj)
                        if obj:IsA("BasePart") and obj.Name == "pinata" and Reference[obj] then
                            Removed(obj)
                        end
                    end))

                    local luciaESPCounter = 0
                    Lucia:Clean(RunService.Heartbeat:Connect(function()
                        if not Lucia.Enabled or not LuciaESPToggle.Enabled then return end
                        luciaESPCounter = luciaESPCounter + 1
                        if luciaESPCounter % 6 ~= 0 then return end
                        for pinataPart, ref in pairs(Reference) do
                            if pinataPart and pinataPart.Parent then
                                updateCandyDisplay(pinataPart)
                            else
                                if ref.billboard then
                                    ref.billboard:Destroy()
                                end
                                Reference[pinataPart] = nil
                            end
                        end
                    end))
                end

                if AutoDepositToggle.Enabled then
                    task.spawn(function()
                        local r = RangeSlider.Value
                        kitCollection(lplr.Name .. ':pinata', function(v)
                            if getItem('candy') then
                                bedwars.Client:Get(remotes.DepositCoins):CallServer(v)
                            end
                        end, r, true)
                    end)
                end

                if LuciaSpyToggle.Enabled then
                    setupLuciaSpy()
                end
            else
                Folder:ClearAllChildren()
                table.clear(Reference)
                table.clear(collectedPinatas)
                table.clear(trackedPinatas)
            end
        end,
    })

    AutoDepositToggle = Lucia:CreateToggle({
        Name = 'Auto Deposit',
        Default = false,
        Function = function(callback)
            if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = callback end
            if DelayToggle and DelayToggle.Object then DelayToggle.Object.Visible = callback end
            if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = (callback and DelayToggle.Enabled) end

            if not callback then
                if DelaySlider and DelaySlider.Object then
                    DelaySlider.Object.Visible = false
                end
            else
                if DelayToggle and DelayToggle.Enabled then
                    if DelaySlider and DelaySlider.Object then
                        DelaySlider.Object.Visible = true
                    end
                end
            end
        end
    })

    RangeSlider = Lucia:CreateSlider({
        Name = 'Range',
        Min = 1,
        Max = 18,
        Default = 8,
        Suffix = ' studs',
        Visible = false
    })

    DelayToggle = Lucia:CreateToggle({
        Name = 'Delay',
        Default = false,
        Visible = false,
        Function = function(callback)
            if DelaySlider and DelaySlider.Object then
                DelaySlider.Object.Visible = callback
            end
        end
    })

    DelaySlider = Lucia:CreateSlider({
        Name = 'Delay Amount',
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = 's',
        Visible = false
    })

    LuciaESPToggle = Lucia:CreateToggle({
        Name = 'Pinata ESP',
        Function = function(callback)
            if CandyESPToggle and CandyESPToggle.Object then
                CandyESPToggle.Object.Visible = callback
            end
            if IgnoreTeammatesESP and IgnoreTeammatesESP.Object then
                IgnoreTeammatesESP.Object.Visible = callback
            end
            if ESPBackground and ESPBackground.Object then
                ESPBackground.Object.Visible = callback
            end
            if ESPColor and ESPColor.Object then
                ESPColor.Object.Visible = callback
            end

            if not callback then
                if ESPColor and ESPColor.Object then
                    ESPColor.Object.Visible = false
                end
            else
                if ESPBackground and ESPBackground.Enabled then
                    if ESPColor and ESPColor.Object then
                        ESPColor.Object.Visible = true
                    end
                end
            end

            if Lucia.Enabled then
                if callback then
                    findExistingPinatas()
                else
                    Folder:ClearAllChildren()
                    table.clear(Reference)
                end
            end
        end
    })

    CandyESPToggle = Lucia:CreateToggle({
        Name = 'Candy ESP',
        Visible = false,
        Tooltip = 'show amount of candy in pinatas',
        Function = function(callback)
            for pinataPart in pairs(Reference) do
                updateCandyDisplay(pinataPart)
            end
        end
    })

    IgnoreTeammatesESP = Lucia:CreateToggle({
        Name = 'Ignore Teammates',
        Visible = false,
        Function = function(callback)
            if Lucia.Enabled and LuciaESPToggle.Enabled then
                refreshESP()
            end
        end
    })

    ESPBackground = Lucia:CreateToggle({
        Name = 'Background',
        Visible = false,
        Function = function(callback)
            if ESPColor and ESPColor.Object then
                ESPColor.Object.Visible = callback
            end
            for _, ref in pairs(Reference) do
                if ref.frame then
                    ref.frame.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
                    if ref.billboard.Blur then
                        ref.billboard.Blur.Visible = callback
                    end
                end
            end
        end
    })

    ESPColor = Lucia:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Visible = false,
        Function = function(hue, sat, val, opacity)
            ESPColor.Hue = hue
            ESPColor.Sat = sat
            ESPColor.Value = val
            ESPColor.Opacity = opacity

            for _, ref in pairs(Reference) do
                if ref.frame then
                    ref.frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                    ref.frame.BackgroundTransparency = 1 - opacity
                end
            end
        end,
        Darker = true
    })

    LuciaSpyToggle = Lucia:CreateToggle({
        Name = 'Lucia Spy',
        Default = false,
        Tooltip = 'notifies you varius info for lucia kits for players (amount of loot and etc)',
        Function = function(callback)
            if IgnoreTeammatesSpy and IgnoreTeammatesSpy.Object then
                IgnoreTeammatesSpy.Object.Visible = callback
            end
            if DisplayNameToggle and DisplayNameToggle.Object then
                DisplayNameToggle.Object.Visible = callback
            end

            if Lucia.Enabled and callback then
                setupLuciaSpy()
            else
                table.clear(trackedPinatas)
            end
        end
    })

    IgnoreTeammatesSpy = Lucia:CreateToggle({
        Name = 'Ignore Teammates',
        Default = true,
        Visible = false
    })

    DisplayNameToggle = Lucia:CreateToggle({
        Name = 'Display Name',
        Default = false,
        Visible = false,
    })

    task.defer(function()
        if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = false end
        if DelayToggle and DelayToggle.Object then DelayToggle.Object.Visible = false end
        if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = false end
        if CandyESPToggle and CandyESPToggle.Object then CandyESPToggle.Object.Visible = false end
        if IgnoreTeammatesESP and IgnoreTeammatesESP.Object then IgnoreTeammatesESP.Object.Visible = false end
        if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = false end
        if ESPColor and ESPColor.Object then ESPColor.Object.Visible = false end
        if IgnoreTeammatesSpy and IgnoreTeammatesSpy.Object then IgnoreTeammatesSpy.Object.Visible = false end
        if DisplayNameToggle and DisplayNameToggle.Object then DisplayNameToggle.Object.Visible = false end
    end)
end)

run(function()
	local AutoHannah
	local Targets
	local Sort
	local Distance
	local Void
	local KATarget 

	AutoHannah = vape.Categories.Kits:CreateModule({
		Name = "AutoHannah",
		Tooltip = 'autokit with more features!!',
		Function = function(callback)
			if callback then
				task.spawn(function()
					local objs = collection('HannahExecuteInteraction', AutoHannah)

					while AutoHannah.Enabled do
						task.wait(0.1)
						if not entitylib.isAlive then continue end

						local localPosition = entitylib.character.RootPart.Position

						for _, v in objs do
							if not AutoHannah.Enabled then break end
							local part = not v:IsA('Model') and v or v.PrimaryPart
							if not part then continue end
							if (part.Position - localPosition).Magnitude > Distance.Value then continue end
							if Void.Enabled and isAboveVoid(part.Position) then continue end
							local success = bedwars.Client:Get(remotes.HannahPromptTrigger).instance:InvokeServer({
								user = lplr,
								victimEntity = v
							})
							if success then
								local icon = v:FindFirstChild('Hannah Execution Icon')
								if icon then icon:Destroy() end
							end
							task.wait(0.05)
						end
					end
				end)
			end
		end
	})

	Targets = AutoHannah:CreateTargets({
		Players = true,
		Walls = false,
		NPCs = false
	})
	local methods = {'Damage', 'Distance'}
	for i in sortmethods do
		if not table.find(methods, i) then
			table.insert(methods, i)
		end
	end
	Sort = AutoHannah:CreateDropdown({Name = 'Sort', List = methods})
	Distance = AutoHannah:CreateSlider({
		Name = "Distance",
		Min = 0,
		Max = 16,
		Default = 12,
		Suffix = 'studs'
	})
	Void = AutoHannah:CreateToggle({
		Name = 'Void',
		Tooltip = 'doesnt use ability if player is falling in the void',
		Default = true,
	})
	KATarget = AutoHannah:CreateToggle({
		Name = 'Use KA Target',
		Default = false,
	})
end)

run(function()
	local KitSpy
	local BeehiveToggle
	local BeehiveBackground
	local BeehiveColor
	local LuciaToggle
	local LuciaIgnoreTeammates
	local LuciaDisplayName
	local FishermanToggle
	local FishermanIgnoreTeammates
	local BountyHunterToggle
	local BountyTargetToggle
	local BountyLootToggle
	local huntTargets = {}
	local BeehiveFolder = Instance.new('Folder')
	BeehiveFolder.Parent = vape.gui
	local BeehiveReference = {}
	local trackedPinatas = {}
	local FishNames = {
		fish_iron = "Iron Fish",
		fish_diamond = "Diamond Fish",
		fish_emerald = "Emerald Fish",
		fish_special = "Special Fish",
		fish_gold = "Gold Fish",
	}

	local function addBlur(parent)
		local blur = Instance.new('ImageLabel')
		blur.Name = 'Blur'
		blur.Size = UDim2.new(1, 89, 1, 52)
		blur.Position = UDim2.fromOffset(-48, -31)
		blur.BackgroundTransparency = 1
		blur.Image = getcustomasset('aerov4/assets/new/blur.png')
		blur.ScaleType = Enum.ScaleType.Slice
		blur.SliceCenter = Rect.new(52, 31, 261, 502)
		blur.Parent = parent
		return blur
	end

	local function isMyBeehive(beehive)
		if not beehive then return false end
		local placedBy = beehive:GetAttribute("PlacedByUserId")
		return placedBy and placedBy == lplr.UserId
	end

	local function getBeehiveOwnerName(beehive)
		if not beehive then return "Unknown" end
		local placedBy = beehive:GetAttribute("PlacedByUserId")
		if not placedBy then return "Unknown" end
		local player = playersService:GetPlayerByUserId(placedBy)
		if player then return player.Name end
		return "Player"
	end

	local function getBeehiveOwner(beehive)
		if not beehive then return nil end
		local placedBy = beehive:GetAttribute("PlacedByUserId")
		if not placedBy then return nil end
		return playersService:GetPlayerByUserId(placedBy)
	end

	local function AddedBeehive(beehive)
		if isMyBeehive(beehive) then return end
		if BeehiveReference[beehive] then return end

		local level = beehive:GetAttribute("Level") or 0
		local ownerName = getBeehiveOwnerName(beehive)
		local owner = getBeehiveOwner(beehive)
		if not owner then return end

		local billboard = Instance.new('BillboardGui')
		billboard.Parent = BeehiveFolder
		billboard.Name = 'beehive-spy'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 4, 0)
		billboard.Size = UDim2.fromOffset(120, 40)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = beehive

		local blur = addBlur(billboard)
		blur.Visible = BeehiveBackground and BeehiveBackground.Enabled or true

		local hue, sat, val, opacity = 0, 0, 1, 0.5
		if BeehiveColor then
			hue, sat, val, opacity = BeehiveColor.Hue, BeehiveColor.Sat, BeehiveColor.Value, BeehiveColor.Opacity
		end

		local frame = Instance.new('Frame')
		frame.Size = UDim2.fromScale(1, 1)
		frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
		frame.BackgroundTransparency = 1 - ((BeehiveBackground and BeehiveBackground.Enabled or true) and opacity or 0)
		frame.BorderSizePixel = 0
		frame.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 6)
		uicorner.Parent = frame
		local nameLabel = Instance.new('TextLabel')
		nameLabel.Name = 'OwnerName'
		nameLabel.Size = UDim2.new(1, 0, 0.4, 0)
		nameLabel.Position = UDim2.new(0, 0, 0, -20)
		nameLabel.BackgroundTransparency = 1
		nameLabel.Text = ownerName
		nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
		nameLabel.TextSize = 12
		nameLabel.Font = Enum.Font.GothamBold
		nameLabel.TextStrokeTransparency = 0.5
		nameLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
		nameLabel.Parent = billboard
		local homeLabel = Instance.new('ImageLabel')
		homeLabel.Size = UDim2.fromOffset(20, 20)
		homeLabel.Position = UDim2.new(0, 5, 0.5, 0)
		homeLabel.AnchorPoint = Vector2.new(0, 0.5)
		homeLabel.BackgroundTransparency = 1
		homeLabel.BorderSizePixel = 0
		homeLabel.Image = bedwars.getIcon({itemType = 'beehive'}, true)
		homeLabel.Parent = frame
		local levelLabel = Instance.new('TextLabel')
		levelLabel.Name = 'Level'
		levelLabel.Size = UDim2.new(0, 25, 1, 0)
		levelLabel.Position = UDim2.new(1, -30, 0, 0)
		levelLabel.BackgroundTransparency = 1
		levelLabel.Text = tostring(level)
		levelLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
		levelLabel.TextSize = 16
		levelLabel.Font = Enum.Font.GothamBold
		levelLabel.TextStrokeTransparency = 0.5
		levelLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
		levelLabel.Parent = frame

		BeehiveReference[beehive] = {
			billboard = billboard,
			levelLabel = levelLabel,
			frame = frame,
		}

		KitSpy:Clean(beehive:GetAttributeChangedSignal("Level"):Connect(function()
			local ref = BeehiveReference[beehive]
			if ref and ref.levelLabel then
				ref.levelLabel.Text = tostring(beehive:GetAttribute("Level") or 0)
			end
		end))
	end

	local function RemovedBeehive(beehive)
		if BeehiveReference[beehive] then
			BeehiveReference[beehive].billboard:Destroy()
			BeehiveReference[beehive] = nil
		end
	end

	local function setupBeehiveSpy()
		for _, beehive in collectionService:GetTagged('beehive') do
			AddedBeehive(beehive)
		end

		KitSpy:Clean(collectionService:GetInstanceAddedSignal('beehive'):Connect(function(beehive)
			task.wait(0.1)
			AddedBeehive(beehive)
		end))

		KitSpy:Clean(collectionService:GetInstanceRemovedSignal('beehive'):Connect(function(beehive)
			RemovedBeehive(beehive)
		end))
	end

	local function cleanupBeehive()
		BeehiveFolder:ClearAllChildren()
		table.clear(BeehiveReference)
	end

	local function getPlayerName(player)
		if LuciaDisplayName and LuciaDisplayName.Enabled then
			return player.DisplayName ~= "" and player.DisplayName or player.Name
		end
		return player.Name
	end

	local function getTeamName(player)
		if player.Team then return player.Team.Name end
		return "Unknown"
	end

	local function getCandyAmount(pinataPart)
		return pinataPart:GetAttribute("Coin") or 0
	end

	local function isTeammateSpy(pinataPart)
		if not LuciaIgnoreTeammates or not LuciaIgnoreTeammates.Enabled then return false end
		local placerId = pinataPart:GetAttribute("PlacedByUserId") or pinataPart:GetAttribute("PlacerId")
		if not placerId then
			local parent = pinataPart.Parent
			if parent then
				placerId = parent:GetAttribute("PlacedByUserId") or parent:GetAttribute("PlacerId")
			end
		end
		if placerId then
			if placerId == lplr.UserId then return true end
			local placer = playersService:GetPlayerByUserId(placerId)
			if placer and placer.Team == lplr.Team then return true end
		end
		return false
	end

	local function setupLuciaSpy()
		local util = require(game:GetService("ReplicatedStorage").TS.games.bedwars.kit.kits['piggy-bank']['piggy-bank-util']).PiggyBankUtil
		for _, obj in pairs(workspace:GetDescendants()) do
			if obj:IsA("BasePart") and obj.Name == "pinata" then
				if not isTeammateSpy(obj) then
					local placerId = obj:GetAttribute("PlacedByUserId") or obj:GetAttribute("PlacerId")
					if placerId then
						local placer = playersService:GetPlayerByUserId(placerId)
						trackedPinatas[obj] = {
							player = placer,
							lastCandy = getCandyAmount(obj),
							exists = true,
							placedTime = tick()
						}
					end
				end
			end
		end

		KitSpy:Clean(workspace.DescendantAdded:Connect(function(obj)
			if not LuciaToggle.Enabled then return end
			if obj:IsA("BasePart") and obj.Name == "pinata" then
				task.wait(0.2)
				if not isTeammateSpy(obj) then
					local placerId = obj:GetAttribute("PlacedByUserId") or obj:GetAttribute("PlacerId")
					if placerId then
						local placer = playersService:GetPlayerByUserId(placerId)
						trackedPinatas[obj] = {
							player = placer,
							lastCandy = getCandyAmount(obj),
							exists = true,
							placedTime = tick()
						}
					end
				end
			end
		end))

		KitSpy:Clean(bedwars.Client:Get("PiggyBankPop"):Connect(function(self)
			if not LuciaToggle.Enabled then return end
			local plr = self.awardedPlayer
			if not plr then return end
			if LuciaIgnoreTeammates and LuciaIgnoreTeammates.Enabled then
				if plr == lplr or (plr.Team and plr.Team == lplr.Team) then return end
			end

			local rewards = util:getRewardsFromCoins(self.coins)
			local I, D, E = 0, 0, 0
			for _, reward in ipairs(rewards) do
				if reward.itemType == "iron" then
					I = I + (reward.amount or 0)
				elseif reward.itemType == "diamond" then
					D = D + (reward.amount or 0)
				elseif reward.itemType == "emerald" then
					E = E + (reward.amount or 0)
				end
			end

			local playerName = getPlayerName(plr)
			local teamName = getTeamName(plr)
			local loot = string.format("%d irons, %d diamonds, %d emeralds", I, D, E)

			notif("KitSpy", string.format("%s (%s) opened their pinata and got %s", playerName, teamName, loot), 8)

			for pinataPart, data in pairs(trackedPinatas) do
				if data.player and data.player.UserId == plr.UserId then
					trackedPinatas[pinataPart] = nil
				end
			end
		end))

		local counter = 0
		KitSpy:Clean(runService.Heartbeat:Connect(function()
			if not LuciaToggle.Enabled then return end
			counter = counter + 1
			if counter % 6 ~= 0 then return end

			local toRemove = {}
			for pinataPart, data in pairs(trackedPinatas) do
				if pinataPart and pinataPart.Parent then
					local currentCandy = getCandyAmount(pinataPart)
					if currentCandy ~= data.lastCandy then
						local difference = currentCandy - data.lastCandy
						if difference > 0 and data.player then
							local playerName = getPlayerName(data.player)
							local teamName = getTeamName(data.player)
							notif("KitSpy", string.format("%s (%s) deposited %d candy (now %d)", playerName, teamName, difference, currentCandy), 5)
						end
						data.lastCandy = currentCandy
					end
				else
					if data.exists and data.player then
						local timeSincePlaced = tick() - (data.placedTime or tick())
						if timeSincePlaced > 2 then
							local playerName = getPlayerName(data.player)
							local teamName = getTeamName(data.player)
							notif("KitSpy", string.format("%s (%s) broke their pinata (had %d candy)", playerName, teamName, data.lastCandy), 5)
						end
					end
					table.insert(toRemove, pinataPart)
				end
			end

			for _, pinataPart in ipairs(toRemove) do
				trackedPinatas[pinataPart] = nil
			end
		end))
	end

	local function cleanupLucia()
		table.clear(trackedPinatas)
	end

	local function setupBountyHunterSpy()
		bedwars.Client:WaitFor(remotes.BountyHunterTargetChanged):andThen(function(rbx)
			KitSpy:Clean(rbx:Connect(function(tbl)
				if not BountyHunterToggle.Enabled then return end
				local hunter = tbl.hunter
				if not hunter then return end
				huntTargets[hunter] = {targetUserId = tbl.newTargetUserId, reward = tbl.reward}
				if not BountyTargetToggle.Enabled then return end
				local hunterName = getPlayerName(hunter)
				local targetPlr = tbl.newTargetUserId and tbl.newTargetUserId ~= 0 and playersService:GetPlayerByUserId(tbl.newTargetUserId)
				if targetPlr then
					notif("KitSpy", hunterName .. " is huntin " .. getPlayerName(targetPlr), 8)
				else
					notif("KitSpy", hunterName .. " lost their bounty target", 8)
				end
			end))
		end)

		KitSpy:Clean(vapeEvents.EntityDeathEvent.Event:Connect(function(deathTable)
			if not BountyHunterToggle.Enabled or not BountyLootToggle.Enabled then return end
			local killer = playersService:GetPlayerFromCharacter(deathTable.fromEntity)
			local killed = playersService:GetPlayerFromCharacter(deathTable.entityInstance)
			if not killer or not killed then return end
			local info = huntTargets[killer]
			if not info or info.targetUserId ~= killed.UserId then return end
			local reward = info.reward
			local iron = reward and reward.iron or 0
			local emerald = reward and reward.emerald or 0
			local lootText = 'iron x' .. iron
			if emerald and emerald > 0 then
				lootText = lootText .. ', emerald x' .. emerald
			end
			notif("KitSpy", getPlayerName(killer) .. " bounty killed " .. getPlayerName(killed) .. " | Loot: " .. lootText, 8)
			huntTargets[killer] = nil
		end))
	end

	local function cleanupBountyHunter()
		table.clear(huntTargets)
	end

	local function setupFishermanSpy()
		bedwars.Client:WaitFor(remotes.FishCaught):andThen(function(rbx)
			KitSpy:Clean(rbx:Connect(function(tbl)
				if not FishermanToggle.Enabled then return end
				local char = tbl.catchingPlayer and tbl.catchingPlayer.Character
				if not char then return end
				local fish = tbl.dropData and tbl.dropData.fishModel
				local allDrops = tbl.dropData and tbl.dropData.drops
				local plrName = char.Name
				local str = plrName:sub(1, 1):upper() .. plrName:sub(2)
				local strfish = FishNames[tostring(fish)] or 'Unknown Fish'
				local lootText = ''
				if allDrops then
					local totals = {}
					local order = {}
					for _, drop in ipairs(allDrops) do
						local item = tostring(drop.itemType) or 'unknown'
						local amount = tonumber(drop.amount) or 0
						if not totals[item] then
							totals[item] = 0
							table.insert(order, item)
						end
						totals[item] = totals[item] + amount
					end
					local parts = {}
					for _, item in ipairs(order) do
						table.insert(parts, item .. ' x' .. math.ceil(totals[item] * 1.4))
					end
					if #parts > 0 then
						lootText = ' | Loot: ' .. table.concat(parts, ', ')
					end
				end
				if FishermanIgnoreTeammates and FishermanIgnoreTeammates.Enabled then
					local currentplr = playersService:GetPlayerFromCharacter(char)
					if currentplr and currentplr.Team == lplr.Team then return end
				end
				notif("KitSpy", str .. " caught a " .. strfish .. lootText, 8)
			end))
		end)
	end

	KitSpy = vape.Categories.Kits:CreateModule({
		Name = "KitSpy",
		Tooltip = "gives u info on stuff other ppl are doing with certain kits - piston inspired :D",
		Function = function(callback)
			if callback then
				if BeehiveToggle.Enabled then setupBeehiveSpy() end
				if LuciaToggle.Enabled then setupLuciaSpy() end
				if FishermanToggle.Enabled then setupFishermanSpy() end
				if BountyHunterToggle.Enabled then setupBountyHunterSpy() end
			else
				cleanupBeehive()
				cleanupLucia()
				cleanupBountyHunter()
			end
		end
	})

	BeehiveToggle = KitSpy:CreateToggle({
		Name = 'Beehive',
		Function = function(callback)
			if BeehiveBackground and BeehiveBackground.Object then BeehiveBackground.Object.Visible = callback end
			if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = callback and BeehiveBackground.Enabled end
			if KitSpy.Enabled then
				if callback then setupBeehiveSpy() else cleanupBeehive() end
			end
		end
	})

	BeehiveBackground = KitSpy:CreateToggle({
		Name = 'Beehive Background',
		Default = true,
		Darker = true,
		Visible = false,
		Function = function(callback)
			if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = callback end
			for _, ref in BeehiveReference do
				if ref and ref.frame then
					local opacity = BeehiveColor and BeehiveColor.Opacity or 0.5
					ref.frame.BackgroundTransparency = 1 - (callback and opacity or 0)
				end
				if ref and ref.billboard then
					local blur = ref.billboard:FindFirstChild("Blur")
					if blur then blur.Visible = callback end
				end
			end
		end
	})

	BeehiveColor = KitSpy:CreateColorSlider({
		Name = 'Beehive Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Darker = true,
		Visible = false,
		Function = function(hue, sat, val, opacity)
			for _, ref in BeehiveReference do
				if ref and ref.frame then
					ref.frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					ref.frame.BackgroundTransparency = 1 - (BeehiveBackground.Enabled and opacity or 0)
				end
			end
		end
	})

	LuciaToggle = KitSpy:CreateToggle({
		Name = 'Lucia',
		Function = function(callback)
			if LuciaIgnoreTeammates and LuciaIgnoreTeammates.Object then LuciaIgnoreTeammates.Object.Visible = callback end
			if LuciaDisplayName and LuciaDisplayName.Object then LuciaDisplayName.Object.Visible = callback end
			if KitSpy.Enabled then
				if callback then setupLuciaSpy() else cleanupLucia() end
			end
		end
	})

	LuciaIgnoreTeammates = KitSpy:CreateToggle({
		Name = 'Lucia Ignore Teammates',
		Default = true,
		Darker = true,
		Visible = false,
	})

	LuciaDisplayName = KitSpy:CreateToggle({
		Name = 'Lucia Display Name',
		Default = false,
		Darker = true,
		Visible = false,
	})

	FishermanToggle = KitSpy:CreateToggle({
		Name = 'Fisherman',
		Function = function(callback)
			if FishermanIgnoreTeammates and FishermanIgnoreTeammates.Object then FishermanIgnoreTeammates.Object.Visible = callback end
			if KitSpy.Enabled and callback then
				setupFishermanSpy()
			end
		end
	})

	FishermanIgnoreTeammates = KitSpy:CreateToggle({
		Name = 'Fisherman Ignore Teammates',
		Default = true,
		Darker = true,
		Visible = false,
	})

	BountyHunterToggle = KitSpy:CreateToggle({
		Name = 'Bounty Hunter',
		Tooltip = 'shows u who a bounty hunter is huntin and the loot they get when they get em',
		Function = function(callback)
			if BountyTargetToggle and BountyTargetToggle.Object then BountyTargetToggle.Object.Visible = callback end
			if BountyLootToggle and BountyLootToggle.Object then BountyLootToggle.Object.Visible = callback end
			if KitSpy.Enabled then
				if callback then setupBountyHunterSpy() else cleanupBountyHunter() end
			end
		end
	})

	BountyTargetToggle = KitSpy:CreateToggle({
		Name = 'Bounty Hunter Target',
		Default = true,
		Darker = true,
		Visible = false,
	})

	BountyLootToggle = KitSpy:CreateToggle({
		Name = 'Bounty Hunter Loot',
		Default = true,
		Darker = true,
		Visible = false,
	})
end)

run(function()
    local StarCollector
    local CollectionToggle
    local Animation
    local RangeSlider
    local ESPToggle
    local ESPNotify
    local ESPBackground
    local ESPColor
    local SwordCheck
    local Folder = Instance.new('Folder')
    Folder.Parent = vape.gui
    local Reference = {}
    local starCooldowns = {}
    local COOLDOWN_TIME = 0.5
    local lastNotification = 0
    local spawnQueue = {}
    local notificationCooldown = 1
    local collectionRunning = false

    local function sendNotification(count)
        notif("Star ESP", string.format("%d stars spawned", count), 3)
    end

    local function processSpawnQueue()
        if #spawnQueue > 0 then
            local currentTime = tick()
            if currentTime - lastNotification >= notificationCooldown then
                sendNotification(#spawnQueue)
                lastNotification = currentTime
                spawnQueue = {}
            else
                task.delay(notificationCooldown - (currentTime - lastNotification), function()
                    if #spawnQueue > 0 then
                        sendNotification(#spawnQueue)
                        spawnQueue = {}
                    end
                end)
            end
        end
    end

    local function getProperImage(v)
        local parent = v.Parent
        if parent and parent:IsA("Model") then
            local modelName = parent.Name
            if modelName == "CritStar" then
                return bedwars.getIcon({itemType = 'crit_star'}, true)
            elseif modelName == "VitalityStar" then
                return bedwars.getIcon({itemType = 'vitality_star'}, true)
            elseif modelName:find("vitality") or modelName:lower():find("vitality") then
                return bedwars.getIcon({itemType = 'vitality_star'}, true)
            elseif modelName:find("crit") or modelName:lower():find("crit") then
                return bedwars.getIcon({itemType = 'crit_star'}, true)
            end
        end
        return bedwars.getIcon({itemType = 'crit_star'}, true)
    end

    local function Added(v)
        if Reference[v] then return end
        local _bpUserId = v:GetAttribute('PlacedByUserId')
        if _bpUserId then
            local _bpOk, _bpOwner = pcall(function() return playersService:GetPlayerByUserId(_bpUserId) end)
        end
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = Folder
        billboard.Name = 'stars'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(36, 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = v
        
        local blur = addBlur(billboard)
        blur.Visible = ESPBackground.Enabled
        
        local image = Instance.new('ImageLabel')
        image.Size = UDim2.fromOffset(36, 36)
        image.Position = UDim2.fromScale(0.5, 0.5)
        image.AnchorPoint = Vector2.new(0.5, 0.5)
        image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
        image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
        image.BorderSizePixel = 0
        image.Image = getProperImage(v)
        image.Parent = billboard
        
        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 4)
        uicorner.Parent = image
        
        Reference[v] = billboard
        
        if ESPNotify.Enabled then
            table.insert(spawnQueue, {item = 'star', time = tick()})
            processSpawnQueue()
        end
    end

    local function Removed(v)
        if Reference[v] then
            Reference[v]:Destroy()
            Reference[v] = nil
        end
        starCooldowns[v] = nil
    end

    local function setupESP()
        for _, v in collectionService:GetTagged('stars') do
            if v:IsA("Model") and v.PrimaryPart then
                Added(v.PrimaryPart)
            end
        end

        StarCollector:Clean(collectionService:GetInstanceAddedSignal('stars'):Connect(function(v)
            if v:IsA("Model") and v.PrimaryPart then
                task.wait(0.1)
                Added(v.PrimaryPart)
            end
        end))

        StarCollector:Clean(collectionService:GetInstanceRemovedSignal('stars'):Connect(function(v)
            if v.PrimaryPart then
                Removed(v.PrimaryPart)
            end
        end))
        
        local _scLastUpdate = 0
        StarCollector:Clean(runService.RenderStepped:Connect(function()
            if not ESPToggle.Enabled then return end
            local _now = tick()
            if _now - _scLastUpdate < 0.1 then return end
            _scLastUpdate = _now
            
            for v, billboard in pairs(Reference) do
                if not v or not v.Parent then
                    Removed(v)
                    continue
                end

                local shouldShow = true

                if SwordCheck.Enabled and isSword() then
                    shouldShow = false
                end

                billboard.Enabled = shouldShow
            end
        end))
    end

    local function collectStar(star)
        if not star or not star.Parent then return end
        
        if Animation.Enabled and entitylib.isAlive then
            bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.PUNCH)
            bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_USE_ITEM)
        end
        
        bedwars.StarCollectorController:collectEntity(lplr, star, star.Name)
    end

	local function startCollection()
		collectionRunning = true
		task.spawn(function()
			while collectionRunning and StarCollector.Enabled and CollectionToggle.Enabled do
				if not entitylib.isAlive then
					task.wait(0.1)
					continue
				end

				local localPosition = entitylib.character.RootPart.Position
				local range = RangeSlider.Value
				local collected = false

				for _, v in collectionService:GetTagged('stars') do
					if not collectionRunning or not StarCollector.Enabled or not CollectionToggle.Enabled then
						break
					end

					if v:IsA("Model") and v.PrimaryPart then
						local starPos = v.PrimaryPart.Position
						local distance = (localPosition - starPos).Magnitude

						if distance <= range then
							local lastAttempt = starCooldowns[v]
							if lastAttempt and tick() - lastAttempt < COOLDOWN_TIME then
								continue
							end
							starCooldowns[v] = tick()
							collectStar(v)
							collected = true
							break
						end
					end
				end

				task.wait(collected and 0.1 or 0.2)
			end
			collectionRunning = false
		end)
	end

    StarCollector = vape.Categories.Kits:CreateModule({
        Name = 'AutoStar',
        Function = function(callback)
            if callback then
                if ESPToggle.Enabled then 
                    setupESP() 
                end
                
                if CollectionToggle.Enabled then
                    startCollection()
                end
            else
                collectionRunning = false
                Folder:ClearAllChildren()
                table.clear(Reference)
                table.clear(spawnQueue)
                table.clear(starCooldowns)
                lastNotification = 0
            end
        end,
    })
    
    CollectionToggle = StarCollector:CreateToggle({
        Name = 'Auto Collect',
        Default = true,
        Function = function(callback)
            if Animation and Animation.Object then Animation.Object.Visible = callback end
            if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = callback end
            
            if callback and StarCollector.Enabled then
                startCollection()
            else
                collectionRunning = false
            end
        end
    })
    
    Animation = StarCollector:CreateToggle({
        Name = 'Animation',
        Default = true,
    })
    
    RangeSlider = StarCollector:CreateSlider({
        Name = 'Range',
        Min = 1, 
        Max = 18,
        Default = 10,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    ESPToggle = StarCollector:CreateToggle({
        Name = 'Star ESP',
        Default = false,
        Function = function(callback)
            if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = callback end
            if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            if SwordCheck and SwordCheck.Object then SwordCheck.Object.Visible = callback end
            
            if StarCollector.Enabled then
                if callback then 
                    setupESP() 
                else
                    Folder:ClearAllChildren()
                    table.clear(Reference)
                end
            end
        end
    })
    
    ESPNotify = StarCollector:CreateToggle({
        Name = 'Notify',
        Default = false,
    })
    
    ESPBackground = StarCollector:CreateToggle({
        Name = 'Background',
        Default = true,
        Function = function(callback)
            if ESPColor and ESPColor.Object then ESPColor.Object.Visible = callback end
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
                    if v:FindFirstChild("Blur") then
                        v.Blur.Visible = callback
                    end
                end
            end
        end
    })
    
    ESPColor = StarCollector:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, v in Reference do
                if v and v:FindFirstChild("ImageLabel") then
                    v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                    v.ImageLabel.BackgroundTransparency = 1 - opacity
                end
            end
        end,
        Darker = true
    })
    SwordCheck = StarCollector:CreateToggle({
        Name = 'Sword Check',
        Default = false,
    })

    task.defer(function()
        local espOn = ESPToggle and ESPToggle.Enabled
        if ESPNotify and ESPNotify.Object then ESPNotify.Object.Visible = espOn end
        if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
        if ESPColor and ESPColor.Object then ESPColor.Object.Visible = espOn end
        if SwordCheck and SwordCheck.Object then SwordCheck.Object.Visible = espOn end
    end)
end)

run(function()
    local Melody
    local SelfHeal
    local TeammateHeal
    local RangeSlider
    local PlayerDropdown
    local healRunning = false
    local lastHealTime = 0
    local healCooldown = 1

    local function getItem(itemName)
        if not entitylib.isAlive then return false end
        
        local inventory = store.inventory
        if inventory and inventory.inventory and inventory.inventory.hand then
            local handItem = inventory.inventory.hand
            if handItem and handItem.itemType == itemName then
                return true
            end
        end
        return false
    end

    local function getLowestHealthTeammate()
        if not entitylib.isAlive then return nil end

        local localPosition = entitylib.character.RootPart.Position
        local range = RangeSlider.Value
        local picked = PlayerDropdown and PlayerDropdown.Value

        if picked and picked ~= 'None' then
            for _, v in entitylib.List do
                if v.Player and v.Player ~= lplr and v.Player.Name == picked then
                    local distance = (localPosition - v.RootPart.Position).Magnitude
                    if distance <= range and v.Health < v.MaxHealth then
                        return v
                    end
                    return nil
                end
            end
            return nil
        end

        local lowestHp = math.huge
        local targetEntity = nil

        for _, v in entitylib.List do
            if v.Player and v.Player ~= lplr and v.Player:GetAttribute('Team') == lplr:GetAttribute('Team') then
                local distance = (localPosition - v.RootPart.Position).Magnitude

                if distance <= range and v.Health < lowestHp and v.Health < v.MaxHealth then
                    lowestHp = v.Health
                    targetEntity = v
                end
            end
        end

        return targetEntity
    end

    local function shouldSelfHeal()
        if not entitylib.isAlive then return false end
        
        local currentHealth = lplr.Character:GetAttribute('Health') or 0
        local maxHealth = lplr.Character:GetAttribute('MaxHealth') or 100
        
        return currentHealth < maxHealth
    end

    local function performHeal(target)
        local currentTime = tick()
        if currentTime - lastHealTime < healCooldown then
            return false
        end
        
        if not getItem('guitar') then
            return false
        end
        
        bedwars.Client:Get(remotes.GuitarHeal):SendToServer({
            healTarget = target
        })
        
        lastHealTime = currentTime
        return true
    end

    local function startHealing()
        healRunning = true
        task.spawn(function()
            while healRunning and Melody.Enabled do
                if not entitylib.isAlive then
                    task.wait(0.1)
                    continue
                end
                
                if not getItem('guitar') then
                    task.wait(0.2)
                    continue
                end
                
                local healed = false
                
                if SelfHeal.Enabled and shouldSelfHeal() then
                    if performHeal(lplr.Character) then
                        healed = true
                    end
                end
                
                if not healed and TeammateHeal.Enabled then
                    local teammate = getLowestHealthTeammate()
                    if teammate then
                        if performHeal(teammate.Character) then
                            healed = true
                        end
                    end
                end
                
                task.wait(0.1)
            end
            healRunning = false
        end)
    end

    Melody = vape.Categories.Kits:CreateModule({
        Name = 'AutoMelody',
        Function = function(callback)
            if callback then
                lastHealTime = 0
                startHealing()
                local function refreshList()
                    if PlayerDropdown and type(PlayerDropdown.SetList) == 'function' then
                        local list = {'None'}
                        for _, name in ipairs(getTeammates(true)) do
                            table.insert(list, name)
                        end
                        PlayerDropdown:SetList(list)
                    end
                end
                refreshList()
                Melody:Clean(playersService.PlayerAdded:Connect(function() task.wait(0.5) refreshList() end))
                Melody:Clean(playersService.PlayerRemoving:Connect(function() task.wait(0.5) refreshList() end))
                Melody:Clean(lplr:GetAttributeChangedSignal('Team'):Connect(function() task.wait(1) refreshList() end))
            else
                healRunning = false
                lastHealTime = 0
            end
        end,
        Tooltip = 'auto heal yourself and teammates with guitar'
    })
    
    SelfHeal = Melody:CreateToggle({
        Name = 'Self Heal',
        Default = true,
    })
    
    TeammateHeal = Melody:CreateToggle({
        Name = 'Teammate Heal',
        Default = true,
        Function = function(callback)
            if RangeSlider and RangeSlider.Object then
                RangeSlider.Object.Visible = callback
            end
        end
    })
    
    RangeSlider = Melody:CreateSlider({
        Name = 'Range',
        Min = 1,
        Max = 51,
        Default = 30,
        Decimal = 1,
        Suffix = ' studs',
    })

    PlayerDropdown = Melody:CreateDropdown({
        Name = 'Target Teammate',
        List = {'None'},
        Default = 'None',
        Tooltip = 'pick who u wanna heal, None just heals whoevers lowest hp'
    })
end)

run(function()
	local AutoMilo
	local Targets
	local Range
	local Delay

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local localPosition = entitylib.character.RootPart.Position
				for _, v in entitylib.List do
					if not AutoMilo.Enabled then break end
					if v.Targetable and v.Character and v.Player and v.RootPart then
						if Targets.Players.Enabled then
							if (v.RootPart.Position - localPosition).Magnitude <= Range.Value then
								if collectionService:HasTag(v.Character, "MimicBLockPickPocketPlayer") then
									pcall(function()
										bedwars.Client:Get(remotes.MimicBlockPickPocketPlayer).instance:InvokeServer(v.Player)
									end)
									task.wait(Delay.Value)
								end
							end
						end
					end
				end
				task.wait(0.1)
			until not AutoMilo.Enabled
		end)
	end

	AutoMilo = vape.Categories.Kits:CreateModule({
		Name = 'AutoMilo',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto robs players close to u'
	})

	Targets = AutoMilo:CreateTargets({
		Players = true
	})
	Range = AutoMilo:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 50,
		Default = 30
	})
	Delay = AutoMilo:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.5,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoEvelynn
	local Range
	local Delay
	local collectionActive = false

	local function startLoop()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			repeat task.wait() until Range and Delay
			while AutoEvelynn.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = Range.Value
				for _, obj in collectionService:GetTagged('EvelynnSoul') do
					if not AutoEvelynn.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						pcall(function()
							bedwars.SpiritAssassinController:useSpirit(lplr, obj)
						end)
						if Delay.Value > 0 then
							task.wait(Delay.Value)
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	AutoEvelynn = vape.Categories.Kits:CreateModule({
		Name = 'AutoEvelynn',
		Function = function(callback)
			if callback then
				startLoop()
			else
				collectionActive = false
			end
		end,
		Tooltip = 'auto grabs the evelynn souls for u'
	})

	Range = AutoEvelynn:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 150,
		Default = 120
	})
	Delay = AutoEvelynn:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local AutoXurot
	local AutoBreath
	local Targets
	local Range
	local Delay
	local FastFly
	local breathActive = false
	local oldFlap
	local breathRemote

	local function getBreathRemote()
		if not breathRemote then
			pcall(function()
				breathRemote = replicatedStorage:WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("DragonBreath")
			end)
		end
		return breathRemote
	end

	local function installHook()
		if oldFlap then return end
		if not bedwars.VoidDragonController then return end
		oldFlap = bedwars.VoidDragonController.flapWings
		bedwars.VoidDragonController.flapWings = function(self, ...)
			local result = oldFlap(self, ...)
			if result ~= false and self.inDragonForm then
				local modifier = bedwars.SprintController:getMovementStatusModifier():addModifier({
					blockSprint = true,
					constantSpeedMultiplier = 2
				})
				self.SpeedMaid:GiveTask(modifier)
			end
			return result
		end
	end

	local function removeHook()
		if oldFlap and bedwars.VoidDragonController then
			bedwars.VoidDragonController.flapWings = oldFlap
		end
		oldFlap = nil
	end

	local function startBreath()
		if breathActive then return end
		breathActive = true
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			while AutoXurot.Enabled and AutoBreath.Enabled and breathActive do
				if entitylib.isAlive and bedwars.VoidDragonController and bedwars.VoidDragonController.inDragonForm then
					local target = entitylib.EntityPosition({
						Range = Range.Value,
						Part = 'RootPart',
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Wallcheck = Targets.Walls.Enabled,
						Sort = sortmethods.Distance
					})
					if target and target.RootPart then
						local remote = getBreathRemote()
						if remote then
							pcall(function()
								remote:FireServer({ player = lplr })
							end)
							task.wait(Delay.Value)
						end
					end
				end
				task.wait(0.1)
			end
			breathActive = false
		end)
	end

	AutoXurot = vape.Categories.Kits:CreateModule({
		Name = 'AutoXurot',
		Function = function(callback)
			if callback then
				if AutoBreath.Enabled then startBreath() end
				if FastFly.Enabled then installHook() end
			else
				breathActive = false
				removeHook()
			end
		end,
		Tooltip = 'auto dragon breath + faster flying'
	})

	AutoBreath = AutoXurot:CreateToggle({
		Name = 'Auto Breath',
		Tooltip = 'breathes on enemies while ur a dragon',
		Function = function(callback)
			if Targets and Targets.Object then Targets.Object.Visible = callback end
			if Range and Range.Object then Range.Object.Visible = callback end
			if Delay and Delay.Object then Delay.Object.Visible = callback end
			if AutoXurot.Enabled then
				if callback then startBreath() else breathActive = false end
			end
		end
	})
	Targets = AutoXurot:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoXurot:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 60,
		Default = 30,
		Visible = false
	})
	Delay = AutoXurot:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 1,
		Decimal = 10,
		Suffix = 's',
		Visible = false
	})

	FastFly = AutoXurot:CreateToggle({
		Name = 'Fast Fly',
		Tooltip = 'makes u fly way faster as a dragon',
		Function = function(callback)
			if AutoXurot.Enabled then
				if callback then installHook() else removeHook() end
			end
		end
	})

	task.defer(function()
		local breathOn = AutoBreath and AutoBreath.Enabled
		if Targets and Targets.Object then Targets.Object.Visible = breathOn end
		if Range and Range.Object then Range.Object.Visible = breathOn end
		if Delay and Delay.Object then Delay.Object.Visible = breathOn end
	end)
end)

run(function()
	local AutoMartin
	local Targets
	local Range

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				if bedwars.AbilityController:canUseAbility('cactus_fire') then
					local plr = entitylib.EntityPosition({
						Range = Range.Value,
						Part = 'RootPart',
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Wallcheck = Targets.Walls.Enabled,
						Sort = sortmethods.Distance
					})
					if plr then
						pcall(function() bedwars.AbilityController:useAbility('cactus_fire') end)
						task.wait(0.5)
					end
				end
				task.wait(0.1)
			until not AutoMartin.Enabled
		end)
	end

	AutoMartin = vape.Categories.Kits:CreateModule({
		Name = 'AutoMartin',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto shoots cactus fire at enemies close to u'
	})

	Targets = AutoMartin:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoMartin:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 18
	})
end)

run(function()
	local AutoFortuna
	local Targets
	local Range
	local Delay
	local useAbilityRemote

	local function getAbilityRemote()
		if not useAbilityRemote then
			pcall(function()
				useAbilityRemote = replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility")
			end)
		end
		return useAbilityRemote
	end

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				if bedwars.AbilityController:canUseAbility('CARD_THROW') then
					local plr = entitylib.EntityPosition({
						Range = Range.Value,
						Part = 'RootPart',
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Wallcheck = Targets.Walls.Enabled,
						Sort = sortmethods.Distance
					})
					if plr and plr.Character then
						local remote = getAbilityRemote()
						if remote then
							pcall(function() remote:FireServer("CARD_THROW") end)
							task.wait(0.1)
							pcall(function()
								bedwars.Client:Get(remotes.AttemptCardThrow).instance:FireServer({ targetEntityInstance = plr.Character })
							end)
							task.wait(Delay.Value)
						end
					end
				end
				task.wait(0.1)
			until not AutoFortuna.Enabled
		end)
	end

	AutoFortuna = vape.Categories.Kits:CreateModule({
		Name = 'AutoFortuna',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto throws cards at enemies for u'
	})

	Targets = AutoFortuna:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoFortuna:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 80,
		Default = 60
	})
	Delay = AutoFortuna:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.5,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoWhim
	local Range
	local collectionActive = false

	local function startLoop()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			repeat task.wait() until Range
			while AutoWhim.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = Range.Value
				for _, obj in collectionService:GetTagged('ElementTome') do
					if not AutoWhim.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						local secret = obj:GetAttribute('TomeSecret')
						if secret then
							pcall(function()
								bedwars.GameAnimationUtil:playAnimation(lplr, bedwars.AnimationType.PUNCH)
							end)
							pcall(function()
								bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_USE_ITEM)
							end)
							local ok, result = pcall(function()
								return bedwars.Client:Get(remotes.LearnElementTome).instance:InvokeServer({ secret = secret })
							end)
							if ok and result and result.success then
								pcall(function() obj:Destroy() end)
								task.wait(0.5)
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	AutoWhim = vape.Categories.Kits:CreateModule({
		Name = 'AutoWhim',
		Function = function(callback)
			if callback then
				startLoop()
			else
				collectionActive = false
			end
		end,
		Tooltip = 'auto learns the element tomes for u'
	})

	Range = AutoWhim:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 30,
		Default = 18
	})
end)

run(function()
	local AutoEldric
	local Range

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Range
			local lastTarget = nil
			repeat
				if not entitylib.isAlive then
					lastTarget = nil
					task.wait(0.1)
					continue
				end
				local range = Range.Value
				if store.hand.tool and store.hand.tool.Name == 'warlock_staff' then
					if lastTarget then
						local hum = lastTarget:FindFirstChildOfClass('Humanoid')
						local root = lastTarget:FindFirstChild('HumanoidRootPart')
						local inRange = root and (root.Position - entitylib.character.RootPart.Position).Magnitude <= range
						if not hum or hum.Health <= 0 or not lastTarget.Parent or not inRange then
							lastTarget = nil
						end
					end
					if not lastTarget then
						local plr = entitylib.EntityPosition({
							Range = range,
							Part = 'RootPart',
							Players = true,
							NPCs = true,
							Wallcheck = false
						})
						if plr and plr.Character then
							lastTarget = plr.Character
							pcall(function() bedwars.AbilityController:useAbility("WARLOCK_LINK") end)
							task.wait(0.1)
							pcall(function()
								bedwars.Client:Get(remotes.WarlockTarget):CallServer({ target = lastTarget })
							end)
						end
					end
				else
					lastTarget = nil
				end
				task.wait(0.3)
			until not AutoEldric.Enabled
		end)
	end

	AutoEldric = vape.Categories.Kits:CreateModule({
		Name = 'AutoEldric',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto links enemies with the warlock staff'
	})

	Range = AutoEldric:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 18,
		Default = 10
	})
end)

run(function()
    local autoZenoModule
    local targetSettings
    local targetSelectionMode
    local itemLimitToggle
    local shockwaveToggle
    local shockwaveRadius
    local lightningStrikeToggle
    local lightningStormToggle
    local attackRange
    local actionDelay

    local function fetchAttackItem()
        if itemLimitToggle.Enabled then
            local wizardStaff = (store.hand.tool and store.hand.tool.Name:find('wizard_staff')) and store.hand.tool or nil
            return wizardStaff, wizardStaff and getHotbar(wizardStaff) or nil, wizardStaff and (tonumber(wizardStaff.Name:sub(#wizardStaff.Name, #wizardStaff.Name)) or 1) or nil
        end

        if store.hand.tool and store.hand.tool.Name:find('wizard_staff') then
            local item = store.hand.tool
            return item, getHotbar(item), tonumber(item.Name:sub(#item.Name, #item.Name)) or 1
        end

        for index, item in pairs(store.inventory.inventory.items) do
            if item.itemType:find('wizard_staff') then
                switchItem(item, 0)
                return item, index, tonumber(item.itemType:sub(#item.itemType, #item.itemType)) or 1
            end
        end

        return nil
    end

    autoZenoModule = vape.Categories.Kits:CreateModule({
        Name = 'AutoZeno',
        Function = function(callback)
            if callback then
                repeat
                    if entitylib.isAlive then
                        local staff, _, level = fetchAttackItem()

                        if staff then
                            local playerPosition = entitylib.character.RootPart.Position
                            local nearestEntity = entitylib.EntityPosition({
                                Origin = playerPosition,
                                Range = (attackRange.Value < 6 and shockwaveToggle.Enabled and 7) or attackRange.Value,
                                Part = 'RootPart',
                                Players = targetSettings.Players.Enabled,
                                NPCs = targetSettings.NPCs.Enabled,
                                Sort = sortmethods[targetSelectionMode.Value]
                            })

                            if nearestEntity then
                                if shockwaveToggle.Enabled and level > 2 then
                                    if bedwars.AbilityController:canUseAbility('SHOCKWAVE') and (playerPosition - nearestEntity.RootPart.Position).Magnitude <= shockwaveRadius.Value then
                                        bedwars.AbilityController:useAbility('SHOCKWAVE', newproxy(true), {
                                            target = CFrame.lookAt(playerPosition, nearestEntity.RootPart.Position).LookVector
                                        })
                                        task.wait(actionDelay.Value)
                                    end
                                end

                                if lightningStrikeToggle.Enabled and bedwars.AbilityController:canUseAbility('LIGHTNING_STRIKE') then
                                    bedwars.AbilityController:useAbility('LIGHTNING_STRIKE', newproxy(true), {
                                        target = nearestEntity.RootPart.Position + ((nearestEntity.Humanoid.MoveDirection or Vector3.zero) * (1 + lplr:GetNetworkPing()))
                                    })
                                    task.wait(actionDelay.Value)
                                end

                                if lightningStormToggle.Enabled and level > 1 then
                                    if bedwars.AbilityController:canUseAbility('LIGHTNING_STORM') then
                                        bedwars.AbilityController:useAbility('LIGHTNING_STORM', newproxy(true), {
                                            target = nearestEntity.RootPart.Position + ((nearestEntity.Humanoid.MoveDirection or Vector3.zero) * (1 + lplr:GetNetworkPing()))
                                        })
                                        task.wait(actionDelay.Value)
                                    end
                                end
                            end
                        end
                    end
                    task.wait(0.1)
                until not autoZenoModule.Enabled
            end
        end,
    })

    targetSettings = autoZenoModule:CreateTargets({
        Players = true,
        NPCs = false
    })

    local availableSortingMethods = {'Damage', 'Distance'}
    for method in sortmethods do
        if not table.find(availableSortingMethods, method) then
            table.insert(availableSortingMethods, method)
        end
    end

    targetSelectionMode = autoZenoModule:CreateDropdown({
        Name = 'Target Mode',
        List = availableSortingMethods,
        Default = 'Distance'
    })

    itemLimitToggle = autoZenoModule:CreateToggle({
        Name = 'Limit to item',
        Default = true
    })

    lightningStrikeToggle = autoZenoModule:CreateToggle({
        Name = 'Use Lightning Strike',
        Default = true
    })

    lightningStormToggle = autoZenoModule:CreateToggle({
        Name = 'Use Lightning Storm'
    })

    shockwaveRadius = autoZenoModule:CreateSlider({
        Name = 'Shockwave Range',
        Visible = false,
        Darker = true,
        Min = 1,
        Max = 12,
        Suffix = function(value)
            return value > 1 and 'studs' or 'stud'
        end,
        Decimal = 5,
        Default = 12
    })

    shockwaveToggle = autoZenoModule:CreateToggle({
        Name = 'Auto Shockwave',
        Function = function(enabled)
            pcall(function()
                shockwaveRadius.Object.Visible = enabled
            end)
        end
    })

    attackRange = autoZenoModule:CreateSlider({
        Name = 'Attack Range',
        Min = 1,
        Max = 60,
        Default = 35,
        Suffix = function(value)
            return value > 1 and 'studs' or 'stud'
        end,
        Decimal = 5
    })

    actionDelay = autoZenoModule:CreateSlider({
        Name = 'Delay',
        Min = 0,
        Max = 10,
        Default = 0.5,
        Decimal = 5,
        Suffix = function(value)
            return value > 1 and 'secs' or 'sec'
        end
    })
end)

run(function()
    local AutoGinger
    local LimitToItem
    local BreakDelay
    local BreakDelaySlider
    local AutoSwitch
    local SwitchMode
    
    local Folder = Instance.new('Folder')
    Folder.Parent = vape.gui
    local lastBreakTime = 0
    local lastPlaceTime = 0
    local placeCheckConnection
    local justPlacedGumdrop = false
    local lastPlacedPosition = nil
    
    _G.gingerLock = _G.gingerLock or false
    
    local function getGumdropSlot()
        for i, v in store.inventory.hotbar do
            if v.item and v.item.itemType == "gumdrop_bounce_pad" then
                return i - 1
            end
        end
        return nil
    end
    
    local function getPredictedPosition()
        if not (lplr.Character and lplr.Character.PrimaryPart) then return nil end
        local root = lplr.Character.PrimaryPart
        local velocity = root.AssemblyLinearVelocity
        local horizontalVelocity = Vector3.new(velocity.X, 0, velocity.Z)
        local speed = horizontalVelocity.Magnitude
        if speed < 1 then return root.Position end
        local predictionTime = math.clamp(speed / 40, 0.15, 0.35)
        return root.Position + (horizontalVelocity * predictionTime)
    end
    
    local function forceBreak(block)
        if not block then return end
        lastBreakTime = tick()
        justPlacedGumdrop = false
        task.spawn(function()
            for i = 1, 6 do
                if not block or not block.Parent then return end
                pcall(bedwars.breakBlock, block, false, nil, true)
                task.wait(0.05)
            end
        end)
    end
    
    local function tryPlaceGumdrop()
        if not AutoSwitch.Enabled or _G.gingerLock then return end
        if not (lplr.Character and lplr.Character.PrimaryPart) then return end
        
        local inFirstPerson = isFirstPerson()
        if SwitchMode.Value == 'First Person' and not inFirstPerson then return end
        if SwitchMode.Value == 'Third Person' and inFirstPerson then return end
        
        local velocity = lplr.Character.PrimaryPart.AssemblyLinearVelocity.Y
        if velocity >= -5 then return end
        
        local gumdropSlot = getGumdropSlot()
        if not gumdropSlot then return end
        
        local root = lplr.Character.PrimaryPart
        local targetPos = getPredictedPosition() or root.Position
        local checkPos = targetPos - Vector3.new(0, 3, 0)
        local groundBlockPos = nil
        
        for i = 1, 16 do
            local testPos = checkPos - Vector3.new(0, 3 * (i - 1), 0)
            local block, blockpos = getPlacedBlock(roundPos(testPos))
            if block then
                groundBlockPos = blockpos * 3
                break
            end
        end
        
        if not groundBlockPos then return end
        
        local distanceToGround = root.Position.Y - groundBlockPos.Y
        if distanceToGround < 9 or distanceToGround > 18 then return end
        
        local placePos = groundBlockPos + Vector3.new(0, 3, 0)
        if lastPlacedPosition and (lastPlacedPosition - placePos).Magnitude < 1 then return end
        if getPlacedBlock(placePos) then return end
        
        _G.gingerLock = true
        
        if hotbarSwitch(gumdropSlot) then
            task.wait(0.03)
            local success = pcall(function()
                bedwars.placeBlock(placePos, "gumdrop_bounce_pad", false)
            end)
            
            if success then
                lastPlaceTime = tick()
                justPlacedGumdrop = true
                lastPlacedPosition = placePos
                
                task.wait(0.03)
                local pickaxeSlot = getPickaxeSlot()
                if pickaxeSlot then
                    hotbarSwitch(pickaxeSlot)
                    task.wait(0.08)
                    local placedBlock = getPlacedBlock(placePos)
                    if placedBlock and placedBlock.Name == "gumdrop_bounce_pad" then
                        forceBreak(placedBlock)
                    end
                end
            end
        end
        
        _G.gingerLock = false
    end
    
    AutoGinger = vape.Categories.Kits:CreateModule({
        Name = 'AutoGinger',
        Function = function(callback)
            if callback then
                local old = bedwars.LaunchPadController.attemptLaunch
                bedwars.LaunchPadController.attemptLaunch = function(...)
                    local res = {old(...)}
                    local self, block = ...
                    
                    if block:GetAttribute('PlacedByUserId') == lplr.UserId and
                       (block.Position - entitylib.character.RootPart.Position).Magnitude < 30 then

                        if LimitToItem.Enabled and not AutoSwitch.Enabled and not isHoldingPickaxe() then
                            return unpack(res)
                        end

                        local inFP = isFirstPerson()
                        local cameraAllowed = not AutoSwitch.Enabled or ((SwitchMode.Value ~= 'First Person' or inFP) and (SwitchMode.Value ~= 'Third Person' or not inFP))

                        if isHoldingPickaxe() then
                            local shouldBreak = true
                            if not AutoSwitch.Enabled and BreakDelay.Enabled and not justPlacedGumdrop then
                                if (tick() - lastBreakTime) < BreakDelaySlider.Value then
                                    shouldBreak = false
                                end
                            end
                            if shouldBreak then
                                forceBreak(block)
                            end
                        elseif AutoSwitch.Enabled and cameraAllowed and not _G.gingerLock then
                            local pickaxeSlot = getPickaxeSlot()
                            if pickaxeSlot then
                                _G.gingerLock = true
                                task.spawn(function()
                                    if hotbarSwitch(pickaxeSlot) then
                                        task.wait(0.03)
                                        forceBreak(block)
                                    end
                                    task.wait(0.2)
                                    _G.gingerLock = false
                                end)
                            end
                        elseif not LimitToItem.Enabled then
                            forceBreak(block)
                        end
                    end
                    
                    return unpack(res)
                end
                
				if AutoSwitch.Enabled then
                    if placeCheckConnection then
                        placeCheckConnection:Disconnect()
                        placeCheckConnection = nil
                    end
                    placeCheckConnection = runService.RenderStepped:Connect(function()
                        if not _G.gingerLock and entitylib.isAlive and tick() - lastPlaceTime > 0.15 then
                            tryPlaceGumdrop()
                        end
                    end)
                end
                
                AutoGinger:Clean(function()
                    bedwars.LaunchPadController.attemptLaunch = old
                    if placeCheckConnection then
                        placeCheckConnection:Disconnect()
                        placeCheckConnection = nil
                    end
                end)
            else
                lastBreakTime = 0
                lastPlaceTime = 0
                justPlacedGumdrop = false
                lastPlacedPosition = nil
                _G.gingerLock = false
                if placeCheckConnection then
                    placeCheckConnection:Disconnect()
                    placeCheckConnection = nil
                end
            end
        end,
        Tooltip = 'really just advanced autokit'
    })

    LimitToItem = AutoGinger:CreateToggle({
        Name = 'Limit to Pickaxe',
        Default = true,
    })
    
    BreakDelay = AutoGinger:CreateToggle({
        Name = 'Break Delay',
        Default = false,
        Function = function(callback)
            if BreakDelaySlider and BreakDelaySlider.Object then
                BreakDelaySlider.Object.Visible = callback and not AutoSwitch.Enabled
            end
        end,
    })
    
    BreakDelaySlider = AutoGinger:CreateSlider({
        Name = 'Delay',
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = 's',
        Visible = false,
    })
    
	AutoSwitch = AutoGinger:CreateToggle({
        Name = 'Auto-Switch',
        Default = false,
        Function = function(callback)
            if SwitchMode and SwitchMode.Object then SwitchMode.Object.Visible = callback end
            if BreakDelay and BreakDelay.Object then BreakDelay.Object.Visible = not callback end
            if BreakDelaySlider and BreakDelaySlider.Object then
                BreakDelaySlider.Object.Visible = (not callback) and BreakDelay.Enabled
            end
            if LimitToItem and LimitToItem.Object then LimitToItem.Object.Visible = not callback end

            if placeCheckConnection then
                placeCheckConnection:Disconnect()
                placeCheckConnection = nil
            end

            if callback and AutoGinger.Enabled then
                placeCheckConnection = runService.RenderStepped:Connect(function()
                    if not _G.gingerLock and entitylib.isAlive and tick() - lastPlaceTime > 0.15 then
                        tryPlaceGumdrop()
                    end
                end)
            end
        end,
    })
    
    SwitchMode = AutoGinger:CreateDropdown({
        Name = 'Camera Mode',
        List = {'Both', 'First Person', 'Third Person'},
        Default = 'Both',
        Visible = false,
    })
end)

run(function()
    local Beekeeper
    local isRealBee
    local CollectionToggle
	local LimitToNet
	local HiveLimit
	local function maxHiveLevel()
		return HiveLimit and HiveLimit.Value or 12
	end
    local maxedBeehives = {}
    local maxedNotificationSent = {}
    local CollectionDelay
    local DelaySlider
    local RangeSlider
    local ESPToggle
    local BeesESP
    local BeesNotify
    local BeesBackground
    local BeesColor
    local BeehiveESP
    local ShowOtherBeehives
    local BeehiveBackground
    local BeehiveColor
    local AutoDeposit
    local DepositDelay
    local DepositDelaySlider
    local DepositRange
    local ESPLimitToNet  
    local collectionRunning = false
    local depositRunning = false
    local BeesFolder = Instance.new('Folder')
    BeesFolder.Parent = vape.gui
    local BeehiveFolder = Instance.new('Folder')
    BeehiveFolder.Parent = vape.gui
    local BeesReference = {}
    local BeehiveReference = {}
    local lastNotification = 0
    local spawnQueue = {}
    local notificationCooldown = 1

    local function sendNotification(count)
        notif("Bee ESP", string.format("%d bees spawned", count), 3)
    end

    local function processSpawnQueue()
        if #spawnQueue > 0 then
            local currentTime = tick()
            if currentTime - lastNotification >= notificationCooldown then
                sendNotification(#spawnQueue)
                lastNotification = currentTime
                spawnQueue = {}
            else
                task.delay(notificationCooldown - (currentTime - lastNotification), function()
                    if #spawnQueue > 0 then
                        sendNotification(#spawnQueue)
                        spawnQueue = {}
                    end
                end)
            end
        end
    end

    local function getBeeIcon()
        return bedwars.getIcon({itemType = 'bee'}, true)
    end

    local function AddedBee(v)
        if BeesReference[v] then return end
        local model = v.Parent
        if model and not isRealBee(model) then
            return
        end
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = BeesFolder
        billboard.Name = 'bee'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
        billboard.Size = UDim2.fromOffset(36, 36)
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = v
        
        local blur = addBlur(billboard)
        blur.Visible = BeesBackground.Enabled
        
        local image = Instance.new('ImageLabel')
        image.Size = UDim2.fromOffset(36, 36)
        image.Position = UDim2.fromScale(0.5, 0.5)
        image.AnchorPoint = Vector2.new(0.5, 0.5)
        image.BackgroundColor3 = Color3.fromHSV(BeesColor.Hue, BeesColor.Sat, BeesColor.Value)
        image.BackgroundTransparency = 1 - (BeesBackground.Enabled and BeesColor.Opacity or 0)
        image.BorderSizePixel = 0
        image.Image = getBeeIcon()
        image.Parent = billboard
        
        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 4)
        uicorner.Parent = image
        
        BeesReference[v] = billboard
        
        if BeesNotify.Enabled then
            table.insert(spawnQueue, {item = 'bee', time = tick()})
            processSpawnQueue()
        end
    end

    local function RemovedBee(v)
        if BeesReference[v] then
            BeesReference[v]:Destroy()
            BeesReference[v] = nil
        end
    end

    local function isMyBeehive(beehive)
        if not beehive then return false end
        local placedBy = beehive:GetAttribute("PlacedByUserId")
        return placedBy and placedBy == lplr.UserId
    end
    
    local function getBeehiveOwnerName(beehive)
        if not beehive then return "Unknown" end
        local placedBy = beehive:GetAttribute("PlacedByUserId")
        if not placedBy then return "Unknown" end
        
        local player = game.Players:GetPlayerByUserId(placedBy)
        if player then
            return player.Name
        end
        
        return "Player"
    end

    local function AddedBeehive(beehive)
        local isOwn = isMyBeehive(beehive)
        
        if not isOwn and not (ShowOtherBeehives and ShowOtherBeehives.Enabled) then 
            return 
        end
        
        if BeehiveReference[beehive] then return end
        
        local level = beehive:GetAttribute("Level") or 0
        local isMaxed = level >= maxHiveLevel() and isOwn
        
        if isMaxed and isOwn then
            maxedBeehives[beehive] = true
        end
        
        local ownerName = isOwn and nil or getBeehiveOwnerName(beehive)
        local hasOwnerName = ownerName ~= nil
        
        local billboard = Instance.new('BillboardGui')
        billboard.Parent = BeehiveFolder
        billboard.Name = 'beehive-esp'
        billboard.StudsOffsetWorldSpace = Vector3.new(0, 4, 0)
        billboard.Size = isMaxed and UDim2.fromOffset(90, 40) or (hasOwnerName and UDim2.fromOffset(120, 40) or UDim2.fromOffset(80, 30))
        billboard.AlwaysOnTop = true
        billboard.ClipsDescendants = false
        billboard.Adornee = beehive
        
        local blur = addBlur(billboard)
        blur.Visible = BeehiveBackground.Enabled
        
        local frame = Instance.new('Frame')
        frame.Size = UDim2.fromScale(1, 1)
        frame.BackgroundColor3 = isMaxed and Color3.fromRGB(255, 50, 50) or Color3.fromHSV(BeehiveColor.Hue, BeehiveColor.Sat, BeehiveColor.Value)
        frame.BackgroundTransparency = 1 - (BeehiveBackground.Enabled and (isMaxed and 0.5 or BeehiveColor.Opacity) or 0)
        frame.BorderSizePixel = 0
        frame.Parent = billboard
        
        local uicorner = Instance.new('UICorner')
        uicorner.CornerRadius = UDim.new(0, 6)
        uicorner.Parent = frame
        
        if hasOwnerName then
            local nameLabel = Instance.new('TextLabel')
            nameLabel.Name = 'OwnerName'
            nameLabel.Size = UDim2.new(1, 0, 0.4, 0)
            nameLabel.Position = UDim2.new(0, 0, 0, -20)
            nameLabel.BackgroundTransparency = 1
            nameLabel.Text = ownerName
            nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            nameLabel.TextSize = 12
            nameLabel.Font = Enum.Font.GothamBold
            nameLabel.TextStrokeTransparency = 0.5
            nameLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
            nameLabel.Parent = billboard
        end
        
        local homeImage = Instance.new('ImageLabel')
		homeImage.Size = UDim2.fromOffset(20, 20)
		homeImage.Position = UDim2.new(0, 5, 0.5, 0)
		homeImage.AnchorPoint = Vector2.new(0, 0.5)
		homeImage.BackgroundTransparency = 1
		homeImage.BorderSizePixel = 0
		homeImage.Image = bedwars.getIcon({itemType = 'beehive'}, true)
		homeImage.Parent = frame
        
        local beeImage = Instance.new('ImageLabel')
        beeImage.Size = UDim2.fromOffset(18, 18)
        beeImage.Position = UDim2.new(0.5, -5, 0.5, 0)
        beeImage.AnchorPoint = Vector2.new(0, 0.5)
        beeImage.BackgroundTransparency = 1
        beeImage.Image = getBeeIcon()
        beeImage.Parent = frame
        
        local levelLabel = Instance.new('TextLabel')
        levelLabel.Name = 'Level'
        levelLabel.Size = UDim2.new(0, 25, 1, 0)
        levelLabel.Position = UDim2.new(1, -30, 0, 0)
        levelLabel.BackgroundTransparency = 1
        levelLabel.Text = tostring(level)
        levelLabel.TextColor3 = isMaxed and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 255, 255)
        levelLabel.TextSize = 16
        levelLabel.Font = Enum.Font.GothamBold
        levelLabel.TextStrokeTransparency = 0.5
        levelLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
        levelLabel.Parent = frame
        
        if isMaxed and isOwn then
            local maxText = Instance.new('TextLabel')
            maxText.Name = 'MaxText'
            maxText.Size = UDim2.new(1, 0, 0.4, 0)
            maxText.Position = UDim2.new(0, 0, 0, hasOwnerName and -40 or -20)
            maxText.BackgroundTransparency = 1
            maxText.Text = "MAX"
            maxText.TextColor3 = Color3.fromRGB(255, 50, 50)
            maxText.TextSize = 12
            maxText.Font = Enum.Font.GothamBold
            maxText.TextStrokeTransparency = 0.5
            maxText.TextStrokeColor3 = Color3.new(0, 0, 0)
            maxText.Parent = billboard
        end
        
        BeehiveReference[beehive] = {
            billboard = billboard,
            levelLabel = levelLabel,
            beehive = beehive,
            isMaxed = isMaxed,
            isOwn = isOwn
        }
        
        local function updateLevel()
            local level = beehive:GetAttribute("Level") or 0
            local isMaxed = level >= maxHiveLevel() and isOwn
            
            if isMaxed and isOwn then
                maxedBeehives[beehive] = true
                
                if not maxedNotificationSent[beehive] then
                    notif("Bee Keeper", "Beehive is full (MAX)", 3)
                    maxedNotificationSent[beehive] = true
                end
                
                if BeehiveReference[beehive] and BeehiveReference[beehive].billboard then
                    local maxText = BeehiveReference[beehive].billboard:FindFirstChild("MaxText")
                    if not maxText then
                        maxText = Instance.new('TextLabel')
                        maxText.Name = 'MaxText'
                        maxText.Size = UDim2.new(1, 0, 0.4, 0)
                        maxText.Position = UDim2.new(0, 0, 0, hasOwnerName and -40 or -20)
                        maxText.BackgroundTransparency = 1
                        maxText.Text = "MAX"
                        maxText.TextColor3 = Color3.fromRGB(255, 50, 50)
                        maxText.TextSize = 12
                        maxText.Font = Enum.Font.GothamBold
                        maxText.TextStrokeTransparency = 0.5
                        maxText.TextStrokeColor3 = Color3.new(0, 0, 0)
                        maxText.Parent = BeehiveReference[beehive].billboard
                    end
                    
                    local frame = BeehiveReference[beehive].billboard:FindFirstChild("Frame")
                    if frame then
                        frame.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
                        frame.BackgroundTransparency = 1 - (BeehiveBackground.Enabled and 0.5 or 0)
                    end
                end
            else
                if isOwn then
                    maxedBeehives[beehive] = nil
                    maxedNotificationSent[beehive] = nil
                end
                
                if BeehiveReference[beehive] and BeehiveReference[beehive].billboard then
                    local maxText = BeehiveReference[beehive].billboard:FindFirstChild("MaxText")
                    if maxText then
                        maxText:Destroy()
                    end
                    
                    local frame = BeehiveReference[beehive].billboard:FindFirstChild("Frame")
                    if frame then
                        frame.BackgroundColor3 = Color3.fromHSV(BeehiveColor.Hue, BeehiveColor.Sat, BeehiveColor.Value)
                        frame.BackgroundTransparency = 1 - (BeehiveBackground.Enabled and BeehiveColor.Opacity or 0)
                    end
                end
            end
            
            if BeehiveReference[beehive] and BeehiveReference[beehive].levelLabel then
                BeehiveReference[beehive].levelLabel.Text = tostring(level)
            end
            
            if BeehiveReference[beehive] then
                BeehiveReference[beehive].isMaxed = isMaxed
            end
        end
        
        updateLevel()
        
        if isOwn then
            Beekeeper:Clean(beehive:GetAttributeChangedSignal("Level"):Connect(updateLevel))
        else
            Beekeeper:Clean(beehive:GetAttributeChangedSignal("Level"):Connect(function()
                local level = beehive:GetAttribute("Level") or 0
                if BeehiveReference[beehive] and BeehiveReference[beehive].levelLabel then
                    BeehiveReference[beehive].levelLabel.Text = tostring(level)
                end
            end))
        end
    end


    local function RemovedBeehive(beehive)
        if BeehiveReference[beehive] then
            BeehiveReference[beehive].billboard:Destroy()
            BeehiveReference[beehive] = nil
        end
    end

    isRealBee = function(v)
        local beeId = v:GetAttribute('BeeId')
        return beeId ~= nil and beeId > -1
    end

    local function setupBeesESP()
        for _, tagName in {'bee', 'MeadowBee'} do
            for _, v in collectionService:GetTagged(tagName) do
                if v:IsA("Model") and v.PrimaryPart then
                    if isRealBee(v) then
                        AddedBee(v.PrimaryPart)
                    end
                end
            end

            Beekeeper:Clean(collectionService:GetInstanceAddedSignal(tagName):Connect(function(v)
                if v:IsA("Model") and v.PrimaryPart then
                    task.wait(0.1)
                    if isRealBee(v) then
                        AddedBee(v.PrimaryPart)
                    end
                end
            end))

            Beekeeper:Clean(collectionService:GetInstanceRemovedSignal(tagName):Connect(function(v)
                if v.PrimaryPart then
                    RemovedBee(v.PrimaryPart)
                end
            end))
        end
    end

    local function setupBeehiveESP()
        for _, beehive in collectionService:GetTagged('beehive') do
            AddedBeehive(beehive)
        end

        Beekeeper:Clean(collectionService:GetInstanceAddedSignal('beehive'):Connect(function(beehive)
            task.wait(0.1)
            AddedBeehive(beehive)
        end))

        Beekeeper:Clean(collectionService:GetInstanceRemovedSignal('beehive'):Connect(function(beehive)
            RemovedBeehive(beehive)
        end))
    end

    local function isHoldingBeeNet()
        if not store.hand or not store.hand.tool then return false end
        return store.hand.tool.Name == 'bee_net' or store.hand.tool.Name == 'bee-net'
    end

	local function kitCollection(id, func, range, specific)
		repeat
			if entitylib.isAlive then
				local objs = type(id) == 'table' and id or collection(id, Beekeeper)
				local localPosition = entitylib.character.RootPart.Position
				for _, v in objs do
					if not Beekeeper.Enabled then break end
					local part = not v:IsA('Model') and v or v.PrimaryPart
					if part and (part.Position - localPosition).Magnitude <= range then
						local success, err = pcall(func, v)
						task.wait(0.02)
					end
				end
			end
			task.wait(0.05)
		until not Beekeeper.Enabled
	end

    local collectionThreads = {}

    local function startCollection()
        collectionRunning = true
        for _, tagName in {'bee', 'MeadowBee'} do
            local thread = task.spawn(function()
                kitCollection(tagName, function(v)
                    if not isRealBee(v) then return end
                    if LimitToNet.Enabled and not isHoldingBeeNet() then return end
                    bedwars.Client:Get(remotes.BeePickup):SendToServer({beeId = v:GetAttribute('BeeId')})
                end, RangeSlider.Value, false)
            end)
            table.insert(collectionThreads, thread)
        end
        Beekeeper:Clean(function()
            for _, thread in collectionThreads do
                task.cancel(thread)
            end
            table.clear(collectionThreads)
        end)
    end

    local function startDeposit()
        depositRunning = true
        task.spawn(function()
            while depositRunning and Beekeeper.Enabled and AutoDeposit.Enabled do
                if not entitylib.isAlive then 
                    task.wait(0.1) 
                    continue 
                end
                
                local currentTool = store.hand and store.hand.tool
                if not currentTool or currentTool.Name ~= 'bee' then
                    task.wait(0.1)
                    continue
                end
                
                local localPosition = entitylib.character.RootPart.Position
                local range = DepositRange.Value
                local depositedThisCycle = false
                
                local availableBeehives = {}
                for _, beehive in collectionService:GetTagged('beehive') do
                    if isMyBeehive(beehive) and not maxedBeehives[beehive] then
                        local beehivePos = beehive.Position
                        local distance = (localPosition - beehivePos).Magnitude
                        
                        if distance <= range then
                            table.insert(availableBeehives, {
                                beehive = beehive,
                                distance = distance
                            })
                        end
                    end
                end
                
                table.sort(availableBeehives, function(a, b)
                    return a.distance < b.distance
                end)
                
                for _, beehiveData in ipairs(availableBeehives) do
                    if not depositRunning or not Beekeeper.Enabled or not AutoDeposit.Enabled then 
                        break 
                    end
                    local beehive = beehiveData.beehive
                    if maxedBeehives[beehive] then
                        continue
                    end
                    
                    local prompt = beehive:FindFirstChildOfClass("ProximityPrompt")
                    
                    if prompt and prompt.Enabled then
                        if DepositDelay.Enabled and DepositDelaySlider.Value > 0 then
                            local originalDuration = prompt.HoldDuration
                            prompt.HoldDuration = DepositDelaySlider.Value
                            
                            if fireproximityprompt then
                                fireproximityprompt(prompt)
                            else
                                prompt:InputHoldBegin()
                                task.wait(DepositDelaySlider.Value)
                                prompt:InputHoldEnd()
                            end
                            
                            task.wait(DepositDelaySlider.Value + 0.1)
                            prompt.HoldDuration = originalDuration
                        else
                            if fireproximityprompt then
                                fireproximityprompt(prompt)
                            else
                                prompt:InputHoldBegin()
                                prompt:InputHoldEnd()
                            end
                            task.wait(0.1)
                        end
                        
                        depositedThisCycle = true
                        break 
                    end
                end
                
                if not depositedThisCycle and #availableBeehives > 0 then
                    local allMaxed = true
                    for _, beehiveData in ipairs(availableBeehives) do
                        if not maxedBeehives[beehiveData.beehive] then
                            allMaxed = false
                            break
                        end
                    end
                    
                    if allMaxed then
                        notif("Bee Keeper", "All nearby beehives are full", 3)
                    end
                end
                
                task.wait(depositedThisCycle and 0.3 or 0.2)
            end
            depositRunning = false
        end)
    end

    Beekeeper = vape.Categories.Kits:CreateModule({
        Name = 'AutoBeekeeper',
        Function = function(callback)
            if callback then
                if ESPToggle.Enabled then
                    if BeesESP.Enabled then
                        setupBeesESP()
                    end
                    if BeehiveESP.Enabled then
                        setupBeehiveESP()
                    end
                end
                
                if CollectionToggle.Enabled then
                    startCollection()
                end
                
                if AutoDeposit.Enabled then
                    startDeposit()
                end
                
                local _bkLastUpdate = 0
                Beekeeper:Clean(runService.RenderStepped:Connect(function()
                    if not ESPToggle.Enabled then return end
                    local _now = tick()
                    if _now - _bkLastUpdate < 0.1 then return end
                    _bkLastUpdate = _now
                    
                    for v, billboard in pairs(BeesReference) do
                        if not v or not v.Parent then
                            RemovedBee(v)
                            continue
                        end

                        local shouldShow = true

                        if ESPLimitToNet.Enabled and not isHoldingBeeNet() then
                            shouldShow = false
                        end

                        billboard.Enabled = shouldShow
                    end
                    
                    for beehive, ref in pairs(BeehiveReference) do
                        if not beehive or not beehive.Parent then
                            RemovedBeehive(beehive)
                            continue
                        end

                        local shouldShow = true

                        if ESPLimitToNet.Enabled and not isHoldingBeeNet() then
                            shouldShow = false
                        end

                        if ref.billboard then
                            ref.billboard.Enabled = shouldShow
                        end
                    end
                end))
            else
                collectionRunning = false
                depositRunning = false
                BeesFolder:ClearAllChildren()
                BeehiveFolder:ClearAllChildren()
                table.clear(BeesReference)
                table.clear(BeehiveReference)
                table.clear(spawnQueue)
                lastNotification = 0
            end
        end,
    })
    
    CollectionToggle = Beekeeper:CreateToggle({
        Name = 'Auto Collect',
        Default = true,
        Function = function(callback)
            if LimitToNet and LimitToNet.Object then LimitToNet.Object.Visible = callback end
            if CollectionDelay and CollectionDelay.Object then CollectionDelay.Object.Visible = callback end
            if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = (callback and CollectionDelay.Enabled) end
            if RangeSlider and RangeSlider.Object then RangeSlider.Object.Visible = callback end
            
            if callback and Beekeeper.Enabled then
                startCollection()
            else
                collectionRunning = false
            end
        end
    })
    
    LimitToNet = Beekeeper:CreateToggle({
        Name = 'Limit to Net',
        Default = false,
    })
    
    CollectionDelay = Beekeeper:CreateToggle({
        Name = 'Collection Delay',
        Default = false,
        Function = function(callback)
            if DelaySlider and DelaySlider.Object then
                DelaySlider.Object.Visible = callback
            end
        end
    })
    
    DelaySlider = Beekeeper:CreateSlider({
        Name = 'Delay',
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = 's',
    })
    
    RangeSlider = Beekeeper:CreateSlider({
        Name = 'Range',
        Min = 1, 
        Max = 30,
        Default = 18,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    ESPToggle = Beekeeper:CreateToggle({
        Name = 'ESP',
        Default = true,
		Function = function(callback)
			if BeesESP and BeesESP.Object then BeesESP.Object.Visible = callback end
			if BeehiveESP and BeehiveESP.Object then BeehiveESP.Object.Visible = callback end
			if ESPLimitToNet and ESPLimitToNet.Object then ESPLimitToNet.Object.Visible = callback end

			if not callback then
				if BeesNotify and BeesNotify.Object then BeesNotify.Object.Visible = false end
				if BeesBackground and BeesBackground.Object then BeesBackground.Object.Visible = false end
				if BeesColor and BeesColor.Object then BeesColor.Object.Visible = false end
				if ShowOtherBeehives and ShowOtherBeehives.Object then ShowOtherBeehives.Object.Visible = false end
				if BeehiveBackground and BeehiveBackground.Object then BeehiveBackground.Object.Visible = false end
				if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = false end
			else
				if BeesESP and BeesESP.Enabled then
					if BeesNotify and BeesNotify.Object then BeesNotify.Object.Visible = true end
					if BeesBackground and BeesBackground.Object then BeesBackground.Object.Visible = true end
					if BeesColor and BeesColor.Object then BeesColor.Object.Visible = BeesBackground.Enabled end
				end
				if BeehiveESP and BeehiveESP.Enabled then
					if ShowOtherBeehives and ShowOtherBeehives.Object then ShowOtherBeehives.Object.Visible = true end
					if BeehiveBackground and BeehiveBackground.Object then BeehiveBackground.Object.Visible = true end
					if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = BeehiveBackground.Enabled end
				end
			end

			if Beekeeper.Enabled then
				if callback then
					if BeesESP.Enabled then setupBeesESP() end
					if BeehiveESP.Enabled then setupBeehiveESP() end
				else
					BeesFolder:ClearAllChildren()
					BeehiveFolder:ClearAllChildren()
					table.clear(BeesReference)
					table.clear(BeehiveReference)
				end
			end
		end
    })
    
    ESPLimitToNet = Beekeeper:CreateToggle({
        Name = 'Limit to Net',
        Default = false,
    })
    
    BeesESP = Beekeeper:CreateToggle({
        Name = 'Bees',
        Default = false,
        Function = function(callback)
            if BeesNotify and BeesNotify.Object then BeesNotify.Object.Visible = callback end
            if BeesBackground and BeesBackground.Object then BeesBackground.Object.Visible = callback end
            if BeesColor and BeesColor.Object then BeesColor.Object.Visible = callback end
            
            if Beekeeper.Enabled and ESPToggle.Enabled then
                if callback then setupBeesESP() else
                    BeesFolder:ClearAllChildren()
                    table.clear(BeesReference)
                end
            end
        end
    })
    
    BeesNotify = Beekeeper:CreateToggle({
        Name = 'Notify',
        Default = false,
    })
    
    BeesBackground = Beekeeper:CreateToggle({
        Name = 'Background',
        Default = true,
        Function = function(callback)
            if BeesColor and BeesColor.Object then BeesColor.Object.Visible = callback end
            for _, v in BeesReference do
                if v and v:FindFirstChild("ImageLabel") then
                    v.ImageLabel.BackgroundTransparency = 1 - (callback and BeesColor.Opacity or 0)
                    if v:FindFirstChild("Blur") then
                        v.Blur.Visible = callback
                    end
                end
            end
        end
    })
    
	BeesColor = Beekeeper:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Function = function(hue, sat, val, opacity)
			for _, v in BeesReference do
				if v and v:FindFirstChild("ImageLabel") then
					v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					v.ImageLabel.BackgroundTransparency = 1 - opacity
				end
			end
		end,
		Darker = true
	})
    
    BeehiveESP = Beekeeper:CreateToggle({
        Name = 'Beehives',
        Default = false,
        Function = function(callback)
            if ShowOtherBeehives and ShowOtherBeehives.Object then ShowOtherBeehives.Object.Visible = callback end
            if BeehiveBackground and BeehiveBackground.Object then BeehiveBackground.Object.Visible = callback end
            if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = callback end
            
            if Beekeeper.Enabled and ESPToggle.Enabled then
                if callback then setupBeehiveESP() else
                    BeehiveFolder:ClearAllChildren()
                    table.clear(BeehiveReference)
                end
            end
        end
    })
    
    ShowOtherBeehives = Beekeeper:CreateToggle({
        Name = 'Show Others',
        Default = false,
        Function = function(callback)
            if Beekeeper.Enabled and ESPToggle.Enabled and BeehiveESP.Enabled then
                BeehiveFolder:ClearAllChildren()
                table.clear(BeehiveReference)
                setupBeehiveESP()
            end
        end
    })
    
    BeehiveBackground = Beekeeper:CreateToggle({
        Name = 'Beehive Background',
        Default = true,
        Function = function(callback)
            if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = callback end
            for _, ref in BeehiveReference do
                if ref and ref.billboard then
                    local frame = ref.billboard:FindFirstChild("Frame")
                    if frame then
                        if ref.isMaxed and ref.isOwn then
                            frame.BackgroundTransparency = 1 - (callback and 0.5 or 0)
                        else
                            frame.BackgroundTransparency = 1 - (callback and BeehiveColor.Opacity or 0)
                        end
                    end
                    if ref.billboard:FindFirstChild("Blur") then
                        ref.billboard.Blur.Visible = callback
                    end
                end
            end
        end
    })
    
    BeehiveColor = Beekeeper:CreateColorSlider({
        Name = 'Beehive Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, ref in BeehiveReference do
                if ref and ref.billboard then
                    local frame = ref.billboard:FindFirstChild("Frame")
                    if frame and not (ref.isMaxed and ref.isOwn) then
                        frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                        frame.BackgroundTransparency = 1 - opacity
                    end
                end
            end
        end,
        Darker = true
    })
    
    AutoDeposit = Beekeeper:CreateToggle({
        Name = 'Auto Deposit',
        Default = false,
		Function = function(callback)
			if DepositDelay and DepositDelay.Object then DepositDelay.Object.Visible = callback end
			if DepositDelaySlider and DepositDelaySlider.Object then DepositDelaySlider.Object.Visible = (callback and DepositDelay.Enabled) end
			if DepositRange and DepositRange.Object then DepositRange.Object.Visible = callback end
			if HiveLimit and HiveLimit.Object then HiveLimit.Object.Visible = callback end
			
			if not callback then
				if DepositDelaySlider and DepositDelaySlider.Object then DepositDelaySlider.Object.Visible = false end
			end

			if callback and Beekeeper.Enabled then
				startDeposit()
			else
				depositRunning = false
			end
		end
    })
    
    DepositDelay = Beekeeper:CreateToggle({
        Name = 'Deposit Delay',
        Default = false,
        Function = function(callback)
            if DepositDelaySlider and DepositDelaySlider.Object then
                DepositDelaySlider.Object.Visible = callback
            end
        end
    })
    
    DepositDelaySlider = Beekeeper:CreateSlider({
        Name = 'Deposit Delay',
        Min = 0,
        Max = 2,
        Default = 0.5,
        Decimal = 10,
        Suffix = 's',
    })
    
    DepositRange = Beekeeper:CreateSlider({
        Name = 'Deposit Range',
        Min = 1,
        Max = 15,
        Default = 10,
        Decimal = 1,
        Suffix = ' studs',
    })
    
    HiveLimit = Beekeeper:CreateSlider({
        Name = 'Hive Limit',
        Min = 1,
        Max = 20,
        Default = 12,
        Decimal = 1,
        Suffix = ' bees',
        Function = function()
            table.clear(maxedBeehives)
            table.clear(maxedNotificationSent)
        end
    })
	task.defer(function()
		if HiveLimit and HiveLimit.Object then HiveLimit.Object.Visible = AutoDeposit.Enabled end
		if DelaySlider and DelaySlider.Object then DelaySlider.Object.Visible = CollectionDelay.Enabled end
		if not ESPToggle.Enabled or not BeesESP.Enabled then
			if BeesNotify and BeesNotify.Object then BeesNotify.Object.Visible = false end
			if BeesBackground and BeesBackground.Object then BeesBackground.Object.Visible = false end
			if BeesColor and BeesColor.Object then BeesColor.Object.Visible = false end
		else
			if BeesColor and BeesColor.Object then BeesColor.Object.Visible = BeesBackground.Enabled end
		end

		if not ESPToggle.Enabled or not BeehiveESP.Enabled then
			if ShowOtherBeehives and ShowOtherBeehives.Object then ShowOtherBeehives.Object.Visible = false end
			if BeehiveBackground and BeehiveBackground.Object then BeehiveBackground.Object.Visible = false end
			if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = false end
		else
			if BeehiveColor and BeehiveColor.Object then BeehiveColor.Object.Visible = BeehiveBackground.Enabled end
		end

		if AutoDeposit and not AutoDeposit.Enabled then
			if DepositDelay and DepositDelay.Object then DepositDelay.Object.Visible = false end
			if DepositDelaySlider and DepositDelaySlider.Object then DepositDelaySlider.Object.Visible = false end
			if DepositRange and DepositRange.Object then DepositRange.Object.Visible = false end
		end

		if DepositDelaySlider and DepositDelaySlider.Object then
			DepositDelaySlider.Object.Visible = (AutoDeposit.Enabled and DepositDelay.Enabled)
		end
	end)
end)

run(function()
	local AutoDrill
	local DrillESP
	local TeamCheck
	local ShowOwn
	local Background
	local BackgroundColor
	local AutoAttack
	local Targets
	local AttackRange
	local AttackDelay
	local SilentMount

	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui

	local Reference = {}
	local espConnections = {}
	local attackRunning = false
	local lastAttackTime = 0
	local mountedDrill = nil
	local drillRemote, attackRemote

	task.spawn(function()
		pcall(function()
			local net = replicatedStorage.rbxts_include.node_modules['@rbxts'].net.out._NetManaged
			drillRemote = net:WaitForChild('PlayerUseDrillController', 10)
			attackRemote = net:WaitForChild('DrillAttack', 10)
		end)
	end)

	local function isDrill(obj)
		return obj and obj:IsA('Model') and obj.Name == 'Drill'
	end

	local function getDrillHead(drill)
		return drill:FindFirstChild('Head') or drill.PrimaryPart or drill:FindFirstChildWhichIsA('BasePart')
	end

	local function isOwnDrill(drill)
		if not drill then return false end
		return drill:GetAttribute('PlacedByUserId') == lplr.UserId
	end

	local function isTeammateDrill(drill)
		local placerId = drill:GetAttribute('PlacedByUserId')
		if not placerId then return false end
		if placerId == lplr.UserId then return false end
		local placer = playersService:GetPlayerByUserId(placerId)
		return placer ~= nil and placer.Team == lplr.Team
	end

	local function shouldShowDrill(drill)
		if isOwnDrill(drill) then
			return ShowOwn == nil or ShowOwn.Enabled
		end
		if TeamCheck and TeamCheck.Enabled and isTeammateDrill(drill) then
			return false
		end
		return true
	end

	local drillCache = {}
	local drillCacheTime = 0
	local function getAllDrills()
		local now = os.clock()
		if now - drillCacheTime < 2 and #drillCache > 0 then
			return drillCache
		end
		drillCacheTime = now
		local found = {}
		for _, obj in workspace:GetDescendants() do
			if isDrill(obj) then
				table.insert(found, obj)
			end
		end
		drillCache = found
		return found
	end

	local function getDrillInfo(drill)
		local itemType = drill:GetAttribute('ItemType')
		local health = drill:GetAttribute('Health') or 0
		local maxHealth = drill:GetAttribute('MaxHealth') or 750
		local amount = 0
		if itemType then
			amount = drill:GetAttribute(itemType) or 0
		end
		return itemType, amount, health, maxHealth
	end

	local function getProperIcon(iconType)
		if not iconType or iconType == '' then return '' end
		local ok, icon = pcall(function()
			return bedwars.getIcon({itemType = iconType}, true)
		end)
		if not ok or not icon then return '' end
		return icon
	end

	local function updateESP(drill)
		local ref = Reference[drill]
		if not ref or not ref.billboard or not ref.billboard.Parent then return end

		local itemType, amount, health, maxHealth = getDrillInfo(drill)

		if ref.healthLabel then
			ref.healthLabel.Text = tostring(math.floor(health))
			local pct = maxHealth > 0 and (health / maxHealth) or 0
			if pct > 0.6 then
				ref.healthLabel.TextColor3 = Color3.fromRGB(80, 235, 80)
			elseif pct > 0.3 then
				ref.healthLabel.TextColor3 = Color3.fromRGB(255, 175, 60)
			else
				ref.healthLabel.TextColor3 = Color3.fromRGB(255, 70, 70)
			end
		end

		if ref.resourceImage then
			local icon = itemType and getProperIcon(itemType) or ''
			if ref.lastIcon ~= icon then
				ref.lastIcon = icon
				ref.resourceImage.Image = icon
			end
			ref.resourceImage.Visible = icon ~= ''
		end

		if ref.amountLabel then
			ref.amountLabel.Text = tostring(math.floor(amount))
		end

		if ref.ownLabel then
			ref.ownLabel.Visible = isOwnDrill(drill)
		end
	end

	local function removeESP(drill)
		local ref = Reference[drill]
		if not ref then return end
		Reference[drill] = nil
		if ref.attrConn then pcall(function() ref.attrConn:Disconnect() end) end
		if ref.billboard then pcall(function() ref.billboard:Destroy() end) end
	end

	local function createESP(drill)
		if Reference[drill] then return end
		if not shouldShowDrill(drill) then return end

		local head = getDrillHead(drill)
		if not head then return end

		local billboard = Instance.new('BillboardGui')
		billboard.Name = 'drill-esp'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(124, 26)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = head
		billboard.Parent = Folder

		local blur = addBlur(billboard)
		blur.Visible = Background.Enabled

		local frame = Instance.new('Frame')
		frame.Size = UDim2.fromScale(1, 1)
		frame.BackgroundColor3 = Color3.fromHSV(BackgroundColor.Hue, BackgroundColor.Sat, BackgroundColor.Value)
		frame.BackgroundTransparency = 1 - (Background.Enabled and (BackgroundColor.Opacity or 0.3) or 1)
		frame.BorderSizePixel = 0
		frame.Parent = billboard

		local corner = Instance.new('UICorner')
		corner.CornerRadius = UDim.new(0, 6)
		corner.Parent = frame

		local drillImage = Instance.new('ImageLabel')
		drillImage.Name = 'DrillIcon'
		drillImage.Size = UDim2.fromOffset(18, 18)
		drillImage.Position = UDim2.new(0, 4, 0.5, 0)
		drillImage.AnchorPoint = Vector2.new(0, 0.5)
		drillImage.BackgroundTransparency = 1
		drillImage.Image = getProperIcon('drill')
		drillImage.Parent = frame

		local healthLabel = Instance.new('TextLabel')
		healthLabel.Name = 'Health'
		healthLabel.Size = UDim2.new(0, 34, 1, 0)
		healthLabel.Position = UDim2.new(0, 25, 0, 0)
		healthLabel.BackgroundTransparency = 1
		healthLabel.Text = '0'
		healthLabel.TextColor3 = Color3.new(1, 1, 1)
		healthLabel.TextSize = 12
		healthLabel.Font = Enum.Font.GothamBold
		healthLabel.TextStrokeTransparency = 0.5
		healthLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
		healthLabel.TextXAlignment = Enum.TextXAlignment.Left
		healthLabel.Parent = frame

		local resourceImage = Instance.new('ImageLabel')
		resourceImage.Name = 'ResourceIcon'
		resourceImage.Size = UDim2.fromOffset(16, 16)
		resourceImage.Position = UDim2.new(0, 64, 0.5, 0)
		resourceImage.AnchorPoint = Vector2.new(0, 0.5)
		resourceImage.BackgroundTransparency = 1
		resourceImage.Image = ''
		resourceImage.Visible = false
		resourceImage.Parent = frame

		local amountLabel = Instance.new('TextLabel')
		amountLabel.Name = 'Amount'
		amountLabel.Size = UDim2.new(0, 30, 1, 0)
		amountLabel.Position = UDim2.new(0, 84, 0, 0)
		amountLabel.BackgroundTransparency = 1
		amountLabel.Text = '0'
		amountLabel.TextColor3 = Color3.new(1, 1, 1)
		amountLabel.TextSize = 12
		amountLabel.Font = Enum.Font.GothamBold
		amountLabel.TextStrokeTransparency = 0.5
		amountLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
		amountLabel.TextXAlignment = Enum.TextXAlignment.Left
		amountLabel.Parent = frame

		local ownLabel = Instance.new('TextLabel')
		ownLabel.Name = 'Own'
		ownLabel.Size = UDim2.fromOffset(12, 12)
		ownLabel.Position = UDim2.new(1, -14, 0, 1)
		ownLabel.BackgroundTransparency = 1
		ownLabel.Text = '★'
		ownLabel.TextColor3 = Color3.fromRGB(255, 220, 90)
		ownLabel.TextSize = 12
		ownLabel.Font = Enum.Font.GothamBold
		ownLabel.Visible = isOwnDrill(drill)
		ownLabel.Parent = frame

		Reference[drill] = {
			billboard = billboard,
			frame = frame,
			healthLabel = healthLabel,
			resourceImage = resourceImage,
			amountLabel = amountLabel,
			ownLabel = ownLabel,
			lastIcon = nil
		}

		Reference[drill].attrConn = drill.AttributeChanged:Connect(function()
			if Reference[drill] then updateESP(drill) end
		end)

		updateESP(drill)
	end

	local function refreshESP()
		for drill in Reference do
			removeESP(drill)
		end
		table.clear(Reference)
		if DrillESP and DrillESP.Enabled and AutoDrill and AutoDrill.Enabled then
			for _, drill in getAllDrills() do
				createESP(drill)
			end
		end
	end

	local function disconnectESP()
		for _, conn in espConnections do
			pcall(function() conn:Disconnect() end)
		end
		table.clear(espConnections)
		for drill in Reference do
			removeESP(drill)
		end
		table.clear(Reference)
	end

	local function setupESPConnections()
		disconnectESP()

		table.insert(espConnections, workspace.DescendantAdded:Connect(function(obj)
			if not (DrillESP and DrillESP.Enabled) then return end
			if not isDrill(obj) then return end
			task.spawn(function()
				task.wait(0.2)
				if DrillESP.Enabled and obj.Parent then
					createESP(obj)
				end
			end)
		end))

		table.insert(espConnections, workspace.DescendantRemoving:Connect(function(obj)
			if Reference[obj] then removeESP(obj) end
		end))

		local lastScan = 0
		table.insert(espConnections, runService.Heartbeat:Connect(function()
			if not (DrillESP and DrillESP.Enabled) then return end
			local now = tick()
			if now - lastScan < 0.5 then return end
			lastScan = now

			for drill, ref in Reference do
				if not drill.Parent then
					removeESP(drill)
				elseif not shouldShowDrill(drill) then
					removeESP(drill)
				else
					updateESP(drill)
				end
			end

			for _, drill in getAllDrills() do
				if not Reference[drill] and shouldShowDrill(drill) then
					createESP(drill)
				end
			end
		end))

		for _, drill in getAllDrills() do
			createESP(drill)
		end
	end

	local function releaseClientLock()
		pcall(function()
			local Knit = require(replicatedStorage.rbxts_include.node_modules['@easy-games'].knit.src).KnitClient
			local tablet = Knit.Controllers.DrillTabletController
			if tablet then
				if tablet.sessionMaid and tablet.sessionMaid.DoCleaning then
					tablet.sessionMaid:DoCleaning()
				end
				tablet.enabled = false
				tablet.lastAttackTime = 0
			end
			local item = Knit.Controllers.DrillItemController
			if item and item.sessionMaid and item.sessionMaid.DoCleaning then
				item.sessionMaid:DoCleaning()
			end
			if gameCamera then
				gameCamera.CameraSubject = entitylib.character and entitylib.character.Humanoid or gameCamera.CameraSubject
				gameCamera.CameraType = Enum.CameraType.Custom
			end
		end)
	end

	local lockedEnt = nil
	local lockedAt = 0

	local function targetStillGood(ent, drill)
		if not ent or not ent.RootPart or not ent.Character or not ent.Character.Parent then return false end
		if ent.Health ~= nil and ent.Health <= 0 then return false end
		local head = getDrillHead(drill)
		if not head then return false end
		return (head.Position - ent.RootPart.Position).Magnitude <= AttackRange.Value + 4
	end

	local function getTargetNearDrill(drill)
		local head = getDrillHead(drill)
		if not head then return nil end

		if lockedEnt and targetStillGood(lockedEnt, drill) then
			return lockedEnt
		end

		if lockedEnt and tick() - lockedAt < 0.4 then
			return nil
		end

		local list = entitylib.AllPosition({
			Origin = head.Position,
			Range = AttackRange.Value,
			Wallcheck = Targets.Walls.Enabled or nil,
			Part = 'RootPart',
			Players = Targets.Players.Enabled,
			NPCs = Targets.NPCs.Enabled,
			Limit = 1,
			Sort = sortmethods['Distance']
		})
		local pick = list and list[1] or nil
		if pick then
			lockedEnt = pick
			lockedAt = tick()
		else
			lockedEnt = nil
		end
		return pick
	end

	local function mountDrill(drill)
		if not drillRemote then return false end
		if mountedDrill == drill then return true end
		local ok = pcall(function()
			drillRemote:InvokeServer({drill = drill})
		end)
		if ok then
			mountedDrill = drill
			if SilentMount and SilentMount.Enabled then
				releaseClientLock()
			end
		end
		return ok
	end

	local function unmountDrill()
		if not drillRemote or not mountedDrill then return end
		pcall(function() drillRemote:InvokeServer({}) end)
		mountedDrill = nil
	end

	local function startAutoAttack()
		if attackRunning then return end
		attackRunning = true
		task.spawn(function()
			while attackRunning and AutoDrill.Enabled and AutoAttack.Enabled do
				local acted = false

				if entitylib.isAlive and attackRemote and tick() - lastAttackTime >= AttackDelay.Value then
					for _, drill in getAllDrills() do
						if isOwnDrill(drill) and drill.Parent then
							local ent = getTargetNearDrill(drill)
							if ent and ent.RootPart then
								local aimPos = ent.RootPart.Position
								pcall(function() drillRemote:InvokeServer({drill = drill}) end)
								if SilentMount and SilentMount.Enabled then
									releaseClientLock()
								end
								pcall(function()
									attackRemote:FireServer({targetPosition = aimPos})
								end)
								pcall(function() drillRemote:InvokeServer({}) end)
								if SilentMount and SilentMount.Enabled then
									releaseClientLock()
								end
								mountedDrill = nil
								lastAttackTime = tick()
								acted = true
								break
							end
						end
					end
				end

				task.wait(acted and 0.05 or 0.15)
			end
			attackRunning = false
			unmountDrill()
		end)
	end

	AutoDrill = vape.Categories.Kits:CreateModule({
		Name = 'AutoDrill',
		Tooltip = 'esp for drills n makes ur drill shoot people by itself',
		Function = function(callback)
			if callback then
				if DrillESP.Enabled then
					setupESPConnections()
				end
				if AutoAttack.Enabled then
					startAutoAttack()
				end
			else
				attackRunning = false
				unmountDrill()
				disconnectESP()
			end
		end
	})

	DrillESP = AutoDrill:CreateToggle({
		Name = 'Drill ESP',
		Default = false,
		Function = function(callback)
			if TeamCheck and TeamCheck.Object then TeamCheck.Object.Visible = callback end
			if ShowOwn and ShowOwn.Object then ShowOwn.Object.Visible = callback end
			if Background and Background.Object then Background.Object.Visible = callback end
			if BackgroundColor and BackgroundColor.Object then
				BackgroundColor.Object.Visible = callback and Background.Enabled
			end
			if not AutoDrill.Enabled then return end
			if callback then
				setupESPConnections()
			else
				disconnectESP()
			end
		end
	})

	TeamCheck = AutoDrill:CreateToggle({
		Name = 'Team Check',
		Default = true,
		Darker = true,
		Visible = false,
		Tooltip = 'hides drills placed by ur teammates (esp only)',
		Function = function()
			if AutoDrill.Enabled and DrillESP.Enabled then refreshESP() end
		end
	})

	ShowOwn = AutoDrill:CreateToggle({
		Name = 'Show Own Drill',
		Default = true,
		Darker = true,
		Visible = false,
		Function = function()
			if AutoDrill.Enabled and DrillESP.Enabled then refreshESP() end
		end
	})

	Background = AutoDrill:CreateToggle({
		Name = 'Background',
		Default = true,
		Darker = true,
		Visible = false,
		Function = function(callback)
			if BackgroundColor and BackgroundColor.Object then
				BackgroundColor.Object.Visible = callback and DrillESP.Enabled
			end
			for _, ref in Reference do
				if ref.frame then
					ref.frame.BackgroundTransparency = 1 - (callback and (BackgroundColor.Opacity or 0.3) or 1)
				end
				if ref.billboard then
					local blur = ref.billboard:FindFirstChild('Blur')
					if blur then blur.Visible = callback end
				end
			end
		end
	})

	BackgroundColor = AutoDrill:CreateColorSlider({
		Name = 'Background Color',
		Darker = true,
		Visible = false,
		DefaultValue = 0,
		DefaultOpacity = 0.3,
		Function = function(hue, sat, val, opacity)
			for _, ref in Reference do
				if ref.frame then
					ref.frame.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					ref.frame.BackgroundTransparency = 1 - (Background.Enabled and opacity or 1)
				end
			end
		end
	})

	AutoAttack = AutoDrill:CreateToggle({
		Name = 'Auto Attack',
		Default = false,
		Function = function(callback)
			if Targets and Targets.Object then Targets.Object.Visible = callback end
			if AttackRange and AttackRange.Object then AttackRange.Object.Visible = callback end
			if AttackDelay and AttackDelay.Object then AttackDelay.Object.Visible = callback end
			if SilentMount and SilentMount.Object then SilentMount.Object.Visible = callback end
			if callback then
				if AutoDrill.Enabled then startAutoAttack() end
			else
				attackRunning = false
				unmountDrill()
			end
		end
	})

	Targets = AutoDrill:CreateTargets({
		Players = true,
		NPCs = true
	})

	AttackRange = AutoDrill:CreateSlider({
		Name = 'Attack Range',
		Min = 5,
		Max = 60,
		Default = 30,
		Darker = true,
		Visible = false,
		Suffix = function(val) return val == 1 and 'stud' or 'studs' end
	})

	AttackDelay = AutoDrill:CreateSlider({
		Name = 'Attack Delay',
		Min = 0.1,
		Max = 2,
		Default = 0.3,
		Decimal = 100,
		Suffix = 's',
		Darker = true,
		Visible = false
	})

	SilentMount = AutoDrill:CreateToggle({
		Name = 'Silent Mount',
		Default = true,
		Darker = true,
		Visible = false,
		Tooltip = 'tries to keep u movin while da drill shoots'
	})

	task.defer(function()
		local espOn = DrillESP.Enabled
		local atkOn = AutoAttack.Enabled
		if TeamCheck.Object then TeamCheck.Object.Visible = espOn end
		if ShowOwn.Object then ShowOwn.Object.Visible = espOn end
		if Background.Object then Background.Object.Visible = espOn end
		if BackgroundColor.Object then BackgroundColor.Object.Visible = espOn and Background.Enabled end
		if Targets.Object then Targets.Object.Visible = atkOn end
		if AttackRange.Object then AttackRange.Object.Visible = atkOn end
		if AttackDelay.Object then AttackDelay.Object.Visible = atkOn end
		if SilentMount.Object then SilentMount.Object.Visible = atkOn end
	end)
end)

run(function()
	local MiloDisguse
	local Blocks
	local old
	MiloDisguse = vape.Categories.Kits:CreateModule({
		Name = "MiloDisguise",
		Tooltip = 'allows you to change to any block u want to hide as',
		Function = function(callback)
			if not callback then
				return
			end
			MiloDisguse:Toggle(false)
			local v88 = {
				["data"] = {
					["blockType"] = Blocks.Value or 'wool_red'
				}
			}

			bedwars.Client:Get(remotes.Mimic):SendToServer(v88)
		end
	})
	Blocks = MiloDisguse:CreateTextBox({
		Name = "Blocks",
		Default = 'wool_brown',
		Visible = true
	})
	
end)

run(function()
    local Fisherman
    local AutoMinigameToggle
    local CompleteDelaySlider
    local PullAnimationToggle
    local MinigameAnimationToggle
    local BlacklistOption
    local Blacklist
    local ESPToggle
	local AutoCastDelay
	local AutoCast
    local ESPNotifyToggle
    local Players = playersService
	local RunService = runService
	local lplr = Players.LocalPlayer
	local RandomizeToggle
    local RandomRange
	local waitTime
    local fishNames = {
		fish_iron = "Iron Fish",
		fish_diamond = "Diamond Fish",
		fish_gold = "Gold Fish",
		fish_special = "Special Fish",
		fish_emerald = "Emerald Fish",
	}

    local function buildMessage(fishModel, drops)
        local fishName = fishNames[fishModel] or fishModel

        if fishModel == "fish_special" then
            if drops and drops[1] then
                return "You caught a " .. fishName .. "! You will receive a " .. tostring(drops[1].itemType)
            else
                return "You caught a " .. fishName .. "! (special item incoming)"
            end
        end

        if drops and drops[1] then
            local drop = drops[1]
            return "You caught a " .. fishName .. "! Receiving " ..
                   tostring(drop.amount) .. "x " .. tostring(drop.itemType)
        end

        return "You caught a " .. fishName .. "!"
    end

    local notifQueue = {}
	local function safeNotif(title, msg, dur)
		table.insert(notifQueue, {title=title, message=msg, duration=dur or 5})
	end
	local heartbeatConn = nil

    local autoMinigameActive    = false
    local pullAnimationTrack    = nil
    local successAnimationTrack = nil
    local espOld                = nil

	local function getBait()
		for _, v in workspace:GetChildren() do
			if v.Name == "fisherman_bobber" and v:GetAttribute("ProjectileShooter") == lplr.UserId then
				return v
			end
		end

		return
	end

    local function stopAllAnimations()
        if pullAnimationTrack then
            pcall(function() pullAnimationTrack:Stop() end)
            pullAnimationTrack = nil
        end
        if successAnimationTrack then
            pcall(function() successAnimationTrack:Stop() end)
            successAnimationTrack = nil
        end
    end

    local function setupESP()
        if not bedwars or not bedwars.FishingMinigameController then
            warn("[AutoFisher] FishingMinigameController not found")
            return
        end
        if espOld then return end 
        espOld = bedwars.FishingMinigameController.startMinigame

        bedwars.FishingMinigameController.startMinigame = function(self, dropData, result)
            if ESPToggle.Enabled and ESPNotifyToggle.Enabled and dropData and dropData.fishModel then
                safeNotif("Fisherman ESP", buildMessage(dropData.fishModel, dropData.drops), 8)
            end
            return espOld(self, dropData, result)
        end

        Fisherman:Clean(function()
            if espOld then
                bedwars.FishingMinigameController.startMinigame = espOld
                espold = nil
                nsHooked = false
            end
        end)
    end

    local function cleanupESP()
        if espOld then
            bedwars.FishingMinigameController.startMinigame = espOld
            espOld = nil
        end
    end

    local function setupAutoMinigame()
        if not bedwars or not bedwars.FishingMinigameController then
            warn("[AutoFisher] FishingMinigameController not found")
            return
        end

        local old = bedwars.FishingMinigameController.startMinigame

        bedwars.FishingMinigameController.startMinigame = function(self, dropData, result)
            if not AutoMinigameToggle.Enabled then
                return old(self, dropData, result)
            end

            if BlacklistOption.Enabled and dropData and dropData.fishModel then
                if table.find(Blacklist.ListEnabled, dropData.fishModel) then
                    local hum = lplr.Character and lplr.Character:FindFirstChildOfClass("Humanoid")
                    if hum and hum:GetState() ~= Enum.HumanoidStateType.Jumping then
                        hum:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                    return old(self, dropData, result)
                end
            end

            autoMinigameActive = true
            stopAllAnimations()

            local waitTime = 0
            if RandomizeToggle and RandomizeToggle.Enabled then
                local min = RandomRange.ValueMin
                local max = RandomRange.ValueMax
                waitTime = min + (max - min) * math.random()
            else
                waitTime = CompleteDelaySlider.Value
            end

            task.spawn(function()
                if PullAnimationToggle.Enabled and waitTime > 0 then
                    local ok, track = pcall(function()
                        return bedwars.GameAnimationUtil:playAnimation(
                            lplr, bedwars.AnimationType.FISHING_ROD_PULLING
                        )
                    end)
                    if ok and track then pullAnimationTrack = track end
                end

                if waitTime > 0 then
                    task.wait(waitTime)
                end

                if pullAnimationTrack then
                    pcall(function() pullAnimationTrack:Stop() end)
                    pullAnimationTrack = nil
                end

                if MinigameAnimationToggle.Enabled then
                    local ok, track = pcall(function()
                        return bedwars.GameAnimationUtil:playAnimation(
                            lplr, bedwars.AnimationType.FISHING_ROD_CATCH_SUCCESS
                        )
                    end)
                    if ok and track then successAnimationTrack = track end
                end

                if result then
                    pcall(function() result({ win = true }) end)
                end

                task.wait(0.5)

                if successAnimationTrack then
                    pcall(function() successAnimationTrack:Stop() end)
                    successAnimationTrack = nil
                end

                autoMinigameActive = false
            end)
        end

        Fisherman:Clean(function()
            bedwars.FishingMinigameController.startMinigame = old
            stopAllAnimations()
        end)
    end

	local function setupAutoCast()
		task.spawn(function()
			repeat
				if entitylib.isAlive and AutoCast.Enabled and (store.hand.tool and store.hand.tool.Name == 'fishing_rod') then
					local position = workspace.CurrentCamera.ViewportSize / 2
					local ray = cloneref(lplr:GetMouse()).UnitRay

					if not getBait() and not workspace:Raycast(entitylib.character.Head.Position + (ray.Direction * 6), Vector3.new(0, -20, 0)) then
						task.wait(AutoCastDelay:GetRandomValue())

						for _, v in {true, false} do
							VirtualInputManager:SendMouseButtonEvent(position.X, position.Y, 0, v, game, 1)
							task.wait()
						end
						task.wait(0.5)
					end
				end
				task.wait(0.1)
			until not Fisherman.Enabled
		end)
	end

    Fisherman = vape.Categories.Kits:CreateModule({
		Name = "AutoFisher",
		Function = function(callback)
            if callback then
                if ESPToggle.Enabled then setupESP() end
                if AutoMinigameToggle.Enabled then setupAutoMinigame() end
                setupAutoCast()

                heartbeatConn = RunService.Heartbeat:Connect(function()
                    if #notifQueue == 0 then return end
                    local entry = table.remove(notifQueue, 1)
                    pcall(notif, entry.title, entry.message, entry.duration)
                end)
                Fisherman:Clean(heartbeatConn)

            else
                autoMinigameActive = false
                stopAllAnimations()
                cleanupESP()
                notifQueue = {} 
            end
        end
    })

    AutoMinigameToggle = Fisherman:CreateToggle({
		Name = "Auto Minigame",
		Default = false,
		Function = function(cv)
			if CompleteDelaySlider and CompleteDelaySlider.Object then
				CompleteDelaySlider.Object.Visible = cv and not (RandomizeToggle and RandomizeToggle.Enabled)
			end
			if PullAnimationToggle and PullAnimationToggle.Object then PullAnimationToggle.Object.Visible = cv end
			if MinigameAnimationToggle and MinigameAnimationToggle.Object then MinigameAnimationToggle.Object.Visible = cv end
			if RandomizeToggle and RandomizeToggle.Object then RandomizeToggle.Object.Visible = cv end
			if RandomRange and RandomRange.Object then RandomRange.Object.Visible = cv and RandomizeToggle.Enabled end
			if Fisherman.Enabled and cv then setupAutoMinigame() end
		end
	})

	CompleteDelaySlider = Fisherman:CreateSlider({
		Name = "Complete Delay",
		Min = 0,
		Max = 5,
		Default = 1,
		Decimal = 10,
		Suffix = "s",
		Visible = false,
	})

	RandomizeToggle = Fisherman:CreateToggle({
		Name = "Randomize Timing",
		Default = false,
		Function = function(cv)
			if RandomRange and RandomRange.Object then RandomRange.Object.Visible = cv end
			if CompleteDelaySlider and CompleteDelaySlider.Object then CompleteDelaySlider.Object.Visible = not cv end
		end
	})

	RandomRange = Fisherman:CreateTwoSlider({
		Name = "Random Delay Range",
		Min = 0.1,
		Max = 5,
		DefaultMin = 0.5,
		DefaultMax = 2,
		Decimal = 10,
		Visible = false,
	})

	PullAnimationToggle = Fisherman:CreateToggle({
		Name = "Pull Animation",
		Default = true,
		Visible = false,
	})

	MinigameAnimationToggle = Fisherman:CreateToggle({
		Name = "Success Animation",
		Default = true,
		Visible = false,
	})

	BlacklistOption = Fisherman:CreateToggle({
		Name = "Blacklist",
		Default = false,
		Function = function(cv)
			if Blacklist and Blacklist.Object then Blacklist.Object.Visible = cv end
		end
	})

	Blacklist = Fisherman:CreateTextList({
		Name = "Blacklist Fish",
		Default = {"fish_iron"}
	})

	AutoCast = Fisherman:CreateToggle({
		Name 	= "AutoCast",
		Default = false,
		Function = function(callback)
			if callback then
				if AutoCastDelay and AutoCastDelay.Object then AutoCastDelay.Object.Visible = callback end
				if AutoCast.Enabled and callback then setupAutoCast() end
			end
		end
	})

	AutoCastDelay = Fisherman:CreateTwoSlider({
		Name 	= "Cast Delay",
		Min 	= 0,
		Max 	= 5,
		Decimal = 5,
		DefaultMin = 0.3,
		DefaultMax = 1.2,
		Darker 	= true,
		Visible = AutoCast.Enabled
	})	

    ESPToggle = Fisherman:CreateToggle({
		Name = "Fisherman ESP",
		Default = false,
		Function = function(cv)
			if ESPNotifyToggle and ESPNotifyToggle.Object then ESPNotifyToggle.Object.Visible = cv end
			if Fisherman.Enabled then
				if cv then setupESP() else cleanupESP() end
			end
		end
	})

	ESPNotifyToggle = Fisherman:CreateToggle({
		Name = "Notify Loot",
		Default = true,
		Visible = false,
	})
end)

run(function()
	local CannonHandController = bedwars.CannonHandController
	local CannonController = bedwars.CannonController
	local oldLaunchSelf = CannonHandController.launchSelf
	local oldStopAiming = CannonController.stopAiming
	local oldStartAiming = CannonController.startAiming
	local AutoDavey
	local AutoDaveyAutojump
	local AutoDaveyAutoLaunch
	local AutoDaveyAutoBreak
	local AutoDaveyPickaxeCheck
	local AutoDaveyAutoSwitch
	local LaunchDelay
	local BreakDelay
	local isLaunching = false
	local didAutoLaunch = false

	local function getCannonSlot()
		for i, v in pairs(store.inventory.hotbar) do
			if v.item then
				local t = tostring(v.item.itemType):lower()
				if t:find("cannon") then
					return i - 1
				end
			end
		end
		return nil
	end

	local function hasWoodPickaxeOnly()
		local bestTier = 0
		for _, slot in pairs(store.inventory.hotbar) do
			if slot.item then
				local t = tostring(slot.item.itemType):lower()
				if t == "wood_pickaxe" and bestTier < 1 then
					bestTier = 1
				elseif (t:find("pickaxe") or t:find("drill")) and t ~= "wood_pickaxe" then
					bestTier = 2
				end
			end
		end
		return bestTier == 1
	end

	local function getNearestCannon()
		if not entitylib.isAlive then return nil end
		local nearest
		local nearestDist = math.huge
		for i, v in pairs(CannonController.getCannons()) do
			pcall(function()
				local dist = (v.Position - entitylib.character.RootPart.Position).Magnitude
				if dist < nearestDist and dist < 30 then
					nearestDist = dist
					nearest = v
				end
			end)
		end
		return nearest
	end

	local function findCannonModel(pos)
		local closest = nil
		local closestDist = 8
		for _, obj in store.blocks do
			if obj and obj:IsA("BasePart") and obj.Name == "cannon" then
				local dist = (obj.Position - pos).Magnitude
				if dist < closestDist then
					closestDist = dist
					closest = obj
				end
			end
		end
		return closest
	end

	local function doBreakCannon(cannon)
		if not entitylib.isAlive then return end
		if not cannon or not cannon.Parent then return end
		local block, blockpos = getPlacedBlock(cannon.Position)
		if block and block.Name == 'cannon' and
		   (entitylib.character.RootPart.Position - block.Position).Magnitude < 30 then
			pcall(bedwars.breakBlock, block, false, nil, true)
		else
			local directBlock = findCannonModel(cannon.Position)
			if directBlock and directBlock.Parent then
				pcall(bedwars.breakBlock, directBlock, false, nil, true)
			end
		end
	end

	local function firstHitCannon(cannon)
		if not entitylib.isAlive then return end
		if not cannon or not cannon.Parent then return end
		if hasWoodPickaxeOnly() then
			doBreakCannon(cannon)
		end
	end

	local function breakCannon(cannon, shootfunc)
		if not entitylib.isAlive then
			isLaunching = false
			return shootfunc()
		end

		local cannonSlot = nil

		if AutoDaveyAutoSwitch.Enabled and not isHoldingPickaxe() then
			local pickaxeSlot = getPickaxeSlot()
			if not pickaxeSlot then
				notif("AutoDavey", "No pickaxe found in hotbar!", 3)
				if AutoDaveyAutojump.Enabled and entitylib.isAlive and entitylib.character.Humanoid then
					entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
				end
				isLaunching = false
				return shootfunc()
			end
			cannonSlot = getCannonSlot()
			if hotbarSwitch(pickaxeSlot) then
				task.wait(0.05)
			end
		end

		if AutoDaveyPickaxeCheck.Enabled and not isHoldingPickaxe() then
			notif("AutoDavey", "You need to HOLD a pickaxe to break cannons!", 3)
			if AutoDaveyAutojump.Enabled and entitylib.isAlive and entitylib.character.Humanoid then
				entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			end
			isLaunching = false
			return shootfunc()
		end

		if BreakDelay.Value > 0 then
			task.wait(BreakDelay.Value)
		end

		local cannonRef = cannon
		if AutoDaveyAutojump.Enabled and entitylib.isAlive and entitylib.character.Humanoid then
			entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
		end
		shootfunc()
		isLaunching = false

		task.spawn(function()
			task.wait(0.05)
			doBreakCannon(cannonRef)
			task.wait(0.35)
			if cannonRef and cannonRef.Parent then
				doBreakCannon(cannonRef)
			end
			if AutoDaveyAutoSwitch.Enabled and cannonSlot then
				task.wait(0.05)
				hotbarSwitch(cannonSlot)
			end
		end)
	end

	AutoDavey = vape.Categories.Kits:CreateModule({
		Name = 'AutoDavey',
		Function = function(callback)
			if callback then
				CannonHandController.launchSelf = function(...)
					if isLaunching then
						isLaunching = false
						return oldLaunchSelf(...)
					end
					isLaunching = true
					if LaunchDelay.Value > 0 then
						task.wait(LaunchDelay.Value)
					end
					if AutoDaveyAutoBreak.Enabled then
						local cannon = getNearestCannon()
						if cannon then
							local args = {...}
							breakCannon(cannon, function()
								oldLaunchSelf(unpack(args))
							end)
							return
						else
							if AutoDaveyAutojump.Enabled and entitylib.isAlive and entitylib.character.Humanoid then
								entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
							end
							local res = oldLaunchSelf(...)
							isLaunching = false
							return res
						end
					else
						if AutoDaveyAutojump.Enabled and entitylib.isAlive and entitylib.character.Humanoid then
							entitylib.character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
						end
						local res = oldLaunchSelf(...)
						isLaunching = false
						return res
					end
				end

				local aimedCannon = nil
				CannonController.startAiming = function(...)
					didAutoLaunch = false
					isLaunching = false
					local result = oldStartAiming(...)
					aimedCannon = getNearestCannon()
					return result
				end

				CannonController.stopAiming = function(...)
					local cannon = aimedCannon or getNearestCannon()
					local result = oldStopAiming(...)
					aimedCannon = nil
					isLaunching = false
					if AutoDaveyAutoLaunch.Enabled and not didAutoLaunch then
						didAutoLaunch = true
						if AutoDaveyAutoBreak.Enabled and AutoDaveyPickaxeCheck.Enabled and not isHoldingPickaxe() and not AutoDaveyAutoSwitch.Enabled then
							notif("AutoDavey", "Hold a pickaxe to auto-break!", 3)
							return result
						end
						if cannon then
							task.spawn(function()
								pcall(CannonHandController.launchSelf, CannonHandController, cannon)
							end)
						end
					end
					return result
				end

				local firstHitDebounce = {}
				AutoDavey:Clean(workspace.DescendantAdded:Connect(function(obj)
					if not (obj:IsA("BasePart") and obj.Name == "cannon") then return end
					if firstHitDebounce[obj] then return end
					firstHitDebounce[obj] = true
					task.spawn(function()
						task.wait(0.5)
						if AutoDaveyAutoBreak.Enabled and hasWoodPickaxeOnly() then
							local dist = entitylib.isAlive
								and (entitylib.character.RootPart.Position - obj.Position).Magnitude
								or math.huge
							if dist < 20 and obj.Parent then
								firstHitCannon(obj)
							end
						end
						firstHitDebounce[obj] = nil
					end)
				end))
			else
				CannonHandController.launchSelf = oldLaunchSelf
				CannonController.stopAiming = oldStopAiming
				CannonController.startAiming = oldStartAiming
				isLaunching = false
				didAutoLaunch = false
			end
		end,
	})

	LaunchDelay = AutoDavey:CreateSlider({
		Name = 'Launch Delay',
		Min = 0,
		Max = 2,
		Default = 0,
		Decimal = 10,
		Suffix = 's',
	})
	BreakDelay = AutoDavey:CreateSlider({
		Name = 'Break Delay',
		Min = 0,
		Max = 2,
		Default = 0,
		Decimal = 10,
		Suffix = 's',
	})

	AutoDaveyAutojump = AutoDavey:CreateToggle({
		Name = 'Auto Jump',
		Default = true,
	})
	AutoDaveyAutoLaunch = AutoDavey:CreateToggle({
		Name = 'Auto Launch',
		Default = true,
		Tooltip = 'automatically lauches u after your done aiming'
	})
	AutoDaveyAutoBreak = AutoDavey:CreateToggle({
		Name = 'Auto Break',
		Default = true,
		Tooltip = 'auto breaks cannon when you lauch'
	})
	AutoDaveyPickaxeCheck = AutoDavey:CreateToggle({
		Name = 'Pickaxe Check',
		Default = true,
	})
	AutoDaveyAutoSwitch = AutoDavey:CreateToggle({
		Name = 'AutoSwitch Pickaxe',
		Default = false,
		Tooltip = 'switches to pickaxe when breaking thens switches back to cannon'
	})
end)

run(function()
	local AutoGompy
	local AutoCollect
	local CollectRange
	local CollectDelay
	local CollectLimit
	local GhostESP
	local ESPBackground
	local ESPColor
	local ESPLimitVacuum
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local Reference = {}
	local ghostIcon
	local collectionActive = false

	local collectRemote = replicatedStorage:WaitForChild("rbxts_include"):WaitForChild("node_modules"):WaitForChild("@rbxts"):WaitForChild("net"):WaitForChild("out"):WaitForChild("_NetManaged"):WaitForChild("CollectCollectableEntity")

	local function isHoldingVacuum()
		return isHoldingItem({'vacuum'})
	end

	local function getGhostIcon()
		if not ghostIcon then
			ghostIcon = bedwars.getIcon({itemType = 'ghost_orb'}, true)
		end
		return ghostIcon
	end

	local function Added(obj)
		if Reference[obj] then return end
		local part = not obj:IsA('Model') and obj or obj.PrimaryPart
		if not part then return end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'ghost'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = part
		local blur = addBlur(billboard)
		blur.Visible = ESPBackground.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(36, 36)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
		image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
		image.BorderSizePixel = 0
		image.Image = getGhostIcon()
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[obj] = billboard
	end

	local function Removed(obj)
		if Reference[obj] then
			Reference[obj]:Destroy()
			Reference[obj] = nil
		end
	end

	local function setupESP()
		for _, v in collectionService:GetTagged('ghost') do
			Added(v)
		end
		AutoGompy:Clean(collectionService:GetInstanceAddedSignal('ghost'):Connect(function(v)
			if GhostESP.Enabled then Added(v) end
		end))
		AutoGompy:Clean(collectionService:GetInstanceRemovedSignal('ghost'):Connect(function(v)
			Removed(v)
		end))
		local lastUpdate = 0
		AutoGompy:Clean(runService.RenderStepped:Connect(function()
			if not GhostESP.Enabled then return end
			local now = tick()
			if now - lastUpdate < 0.1 then return end
			lastUpdate = now
			for obj, billboard in pairs(Reference) do
				if not obj or not obj.Parent then
					Removed(obj)
					continue
				end
				local shouldShow = true
				if ESPLimitVacuum.Enabled and not isHoldingVacuum() then
					shouldShow = false
				end
				billboard.Enabled = shouldShow
			end
		end))
	end

	local function startAutoCollect()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			while AutoGompy.Enabled and AutoCollect.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				if CollectLimit.Enabled and not isHoldingVacuum() then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = CollectRange.Value
				for _, obj in collectionService:GetTagged('ghost') do
					if not AutoGompy.Enabled or not AutoCollect.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						local id = obj:GetAttribute('Id') or obj:GetAttribute('id')
							or obj:GetAttribute('CollectableId') or tonumber(obj.Name:match('%d+'))
						if id then
							pcall(function() collectRemote:FireServer({ id = id }) end)
							if CollectDelay.Value > 0 then
								task.wait(CollectDelay.Value)
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	local function stopAutoCollect()
		collectionActive = false
	end

	AutoGompy = vape.Categories.Kits:CreateModule({
		Name = 'AutoGompy',
		Function = function(callback)
			if callback then
				if GhostESP.Enabled then setupESP() end
				if AutoCollect.Enabled then startAutoCollect() end
			else
				stopAutoCollect()
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'ghost stuff for gompy fr'
	})

	AutoCollect = AutoGompy:CreateToggle({
		Name = 'Auto Collect',
		Tooltip = 'grabs the ghosts for u no cap',
		Function = function(callback)
			if CollectRange and CollectRange.Object then CollectRange.Object.Visible = callback end
			if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = callback end
			if CollectLimit and CollectLimit.Object then CollectLimit.Object.Visible = callback end
			if AutoGompy.Enabled then
				if callback then startAutoCollect() else stopAutoCollect() end
			end
		end
	})
	CollectRange = AutoGompy:CreateSlider({
		Name = 'Range',
		Tooltip = 'how far it grabs ghosts gng',
		Min = 10,
		Max = 200,
		Default = 50,
		Visible = false
	})
	CollectDelay = AutoGompy:CreateSlider({
		Name = 'Delay',
		Tooltip = 'wait time between grabs',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's',
		Visible = false
	})
	CollectLimit = AutoGompy:CreateToggle({
		Name = 'Limit to Vacuum',
		Tooltip = 'only grabs ghosts when ur holding the vacuum'
	})

	GhostESP = AutoGompy:CreateToggle({
		Name = 'Ghost ESP',
		Tooltip = 'shows u where the ghosts at',
		Function = function(callback)
			if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and ESPBackground and ESPBackground.Enabled) end
			if ESPLimitVacuum and ESPLimitVacuum.Object then ESPLimitVacuum.Object.Visible = callback end
			if AutoGompy.Enabled then
				if callback then
					setupESP()
				else
					Folder:ClearAllChildren()
					table.clear(Reference)
				end
			end
		end
	})
	ESPBackground = AutoGompy:CreateToggle({
		Name = 'Background',
		Tooltip = 'adds a lil box behind the icon',
		Default = true,
		Function = function(callback)
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and GhostESP and GhostESP.Enabled) end
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					local blur = v:FindFirstChild('Blur')
					if blur then blur.Visible = callback end
					v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
				end
			end
		end
	})
	ESPColor = AutoGompy:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Darker = true,
		Function = function(hue, sat, val, opacity)
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					v.ImageLabel.BackgroundTransparency = 1 - opacity
				end
			end
		end
	})
	ESPLimitVacuum = AutoGompy:CreateToggle({
		Name = 'ESP Limit to Vacuum',
		Tooltip = 'only shows ghosts when ur holding the vacuum'
	})

	task.defer(function()
		local collectOn = AutoCollect and AutoCollect.Enabled
		if CollectRange and CollectRange.Object then CollectRange.Object.Visible = collectOn end
		if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = collectOn end
		if CollectLimit and CollectLimit.Object then CollectLimit.Object.Visible = collectOn end
		local espOn = GhostESP and GhostESP.Enabled
		if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
		if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (espOn and ESPBackground.Enabled) end
		if ESPLimitVacuum and ESPLimitVacuum.Object then ESPLimitVacuum.Object.Visible = espOn end
	end)
end)

run(function()
	local AutoSilas
	local Damage
	local Targets
	local DamageRange
	local Heal
	local HealThreshold

	local useAbilityRemote = replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility")

	local function getCurrentAura()
		return lplr:GetAttribute("LeaderAuraType")
	end

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Damage and Heal and DamageRange and HealThreshold and Targets
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local hum = entitylib.character.Humanoid
				if Heal.Enabled and hum and hum.MaxHealth > 0 then
					local pct = (hum.Health / hum.MaxHealth) * 100
					if pct <= HealThreshold.Value then
						useAbilityRemote:FireServer("rebellion_shield")
						if getCurrentAura() ~= "healing" then
							useAbilityRemote:FireServer("rebellion_aura_swap")
						end
						task.wait(0.1)
						continue
					end
				end
				if Damage.Enabled then
					local plr = entitylib.EntityPosition({
						Range = DamageRange.Value,
						Part = "RootPart",
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Wallcheck = Targets.Walls.Enabled,
						Sort = sortmethods.Distance
					})
					if plr and getCurrentAura() ~= "damage" then
						useAbilityRemote:FireServer("rebellion_aura_swap")
						task.wait(0.1)
					end
				end
				task.wait(0.1)
			until not AutoSilas.Enabled
		end)
	end

	AutoSilas = vape.Categories.Kits:CreateModule({
		Name = 'AutoSilas',
		Function = function(callback)
			if callback then
				startLoop()
			end
		end,
		Tooltip = 'auto uses silas auras for u'
	})

	Damage = AutoSilas:CreateToggle({
		Name = 'Damage',
		Tooltip = 'swaps to damage aura when a enemy is close',
		Function = function(callback)
			if DamageRange and DamageRange.Object then DamageRange.Object.Visible = callback end
			if Targets and Targets.Object then Targets.Object.Visible = callback end
		end
	})
	Targets = AutoSilas:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	DamageRange = AutoSilas:CreateSlider({
		Name = 'Range',
		Tooltip = 'how close a enemy gotta be gng',
		Min = 1,
		Max = 50,
		Default = 14,
		Visible = false
	})

	Heal = AutoSilas:CreateToggle({
		Name = 'Heal',
		Tooltip = 'shields + heal aura when ur hp gets low',
		Function = function(callback)
			if HealThreshold and HealThreshold.Object then HealThreshold.Object.Visible = callback end
		end
	})
	HealThreshold = AutoSilas:CreateSlider({
		Name = 'HP Threshold',
		Tooltip = 'heals when ur hp drops under this',
		Min = 1,
		Max = 100,
		Default = 40,
		Suffix = '%',
		Visible = false
	})

	task.defer(function()
		if Targets and Targets.Object then Targets.Object.Visible = Damage and Damage.Enabled end
		if DamageRange and DamageRange.Object then DamageRange.Object.Visible = Damage and Damage.Enabled end
		if HealThreshold and HealThreshold.Object then HealThreshold.Object.Visible = Heal and Heal.Enabled end
	end)
end)

run(function()
	local AutoMiner
	local AutoCollect
	local CollectRange
	local CollectDelay
	local PetrifiedESP
	local ESPBackground
	local ESPColor
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local Reference = {}
	local rockIcon
	local collectionActive = false

	local function getRockIcon()
		if not rockIcon then
			rockIcon = bedwars.getIcon({itemType = 'large_rock'}, true)
		end
		return rockIcon
	end

	local function Added(obj)
		if Reference[obj] then return end
		local part = not obj:IsA('Model') and obj or obj.PrimaryPart
		if not part then return end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'petrified-player'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = part
		local blur = addBlur(billboard)
		blur.Visible = ESPBackground.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(36, 36)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
		image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
		image.BorderSizePixel = 0
		image.Image = getRockIcon()
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[obj] = billboard
	end

	local function Removed(obj)
		if Reference[obj] then
			Reference[obj]:Destroy()
			Reference[obj] = nil
		end
	end

	local function setupESP()
		for _, v in collectionService:GetTagged('petrified-player') do
			Added(v)
		end
		AutoMiner:Clean(collectionService:GetInstanceAddedSignal('petrified-player'):Connect(function(v)
			if PetrifiedESP.Enabled then Added(v) end
		end))
		AutoMiner:Clean(collectionService:GetInstanceRemovedSignal('petrified-player'):Connect(function(v)
			Removed(v)
		end))
		local lastUpdate = 0
		AutoMiner:Clean(runService.RenderStepped:Connect(function()
			if not PetrifiedESP.Enabled then return end
			local now = tick()
			if now - lastUpdate < 0.2 then return end
			lastUpdate = now
			for obj in pairs(Reference) do
				if not obj or not obj.Parent then
					Removed(obj)
				end
			end
		end))
	end

	local function startAutoCollect()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			while AutoMiner.Enabled and AutoCollect.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = CollectRange.Value
				for _, obj in collectionService:GetTagged('petrified-player') do
					if not AutoMiner.Enabled or not AutoCollect.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						local petrifyId = obj:GetAttribute('petrifyId') or obj:GetAttribute('PetrifyId')
						if petrifyId then
							pcall(function()
								bedwars.Client:Get(remotes.DestroyPetrifiedPlayer):SendToServer({ petrifyId = petrifyId })
							end)
							if CollectDelay.Value > 0 then
								task.wait(CollectDelay.Value)
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	local function stopAutoCollect()
		collectionActive = false
	end

	AutoMiner = vape.Categories.Kits:CreateModule({
		Name = 'AutoMiner',
		Function = function(callback)
			if callback then
				if PetrifiedESP.Enabled then setupESP() end
				if AutoCollect.Enabled then startAutoCollect() end
			else
				stopAutoCollect()
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'frees petrified players + esp for miner'
	})

	AutoCollect = AutoMiner:CreateToggle({
		Name = 'Auto Collect',
		Tooltip = 'auto frees the petrified players',
		Function = function(callback)
			if CollectRange and CollectRange.Object then CollectRange.Object.Visible = callback end
			if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = callback end
			if AutoMiner.Enabled then
				if callback then startAutoCollect() else stopAutoCollect() end
			end
		end
	})
	CollectRange = AutoMiner:CreateSlider({
		Name = 'Range',
		Tooltip = 'how far it collects em from',
		Min = 1,
		Max = 60,
		Default = 6,
		Suffix = 'm',
		Visible = false
	})
	CollectDelay = AutoMiner:CreateSlider({
		Name = 'Delay',
		Tooltip = 'wait time between collecting',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's',
		Visible = false
	})

	PetrifiedESP = AutoMiner:CreateToggle({
		Name = 'Petrified ESP',
		Tooltip = 'shows u where the petrified players at',
		Function = function(callback)
			if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and ESPBackground and ESPBackground.Enabled) end
			if AutoMiner.Enabled then
				if callback then
					setupESP()
				else
					Folder:ClearAllChildren()
					table.clear(Reference)
				end
			end
		end
	})
	ESPBackground = AutoMiner:CreateToggle({
		Name = 'Background',
		Default = true,
		Function = function(callback)
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and PetrifiedESP and PetrifiedESP.Enabled) end
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					local blur = v:FindFirstChild('Blur')
					if blur then blur.Visible = callback end
					v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
				end
			end
		end
	})
	ESPColor = AutoMiner:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Darker = true,
		Function = function(hue, sat, val, opacity)
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					v.ImageLabel.BackgroundTransparency = 1 - opacity
				end
			end
		end
	})

	task.defer(function()
		local collectOn = AutoCollect and AutoCollect.Enabled
		if CollectRange and CollectRange.Object then CollectRange.Object.Visible = collectOn end
		if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = collectOn end
		local espOn = PetrifiedESP and PetrifiedESP.Enabled
		if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
		if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (espOn and ESPBackground.Enabled) end
	end)
end)

run(function()
	local AutoLian
	local Targets
	local Range
	local Delay

	local dragonSwordFire = replicatedStorage:WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged'):WaitForChild('DragonSwordFire')

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local plr = entitylib.EntityPosition({
					Range = Range.Value,
					Part = 'RootPart',
					Players = Targets.Players.Enabled,
					NPCs = Targets.NPCs.Enabled,
					Wallcheck = Targets.Walls.Enabled,
					Sort = sortmethods.Distance
				})
				if plr and plr.Character then
					pcall(function()
						dragonSwordFire:FireServer({ target = plr.Character })
					end)
					task.wait(Delay.Value)
				end
				task.wait(0.1)
			until not AutoLian.Enabled
		end)
	end

	AutoLian = vape.Categories.Kits:CreateModule({
		Name = 'AutoLian',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto swings lian dragon sword at enemies'
	})

	Targets = AutoLian:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoLian:CreateSlider({
		Name = 'Range',
		Tooltip = 'how close a enemy gotta be',
		Min = 1,
		Max = 18,
		Default = 14
	})
	Delay = AutoLian:CreateSlider({
		Name = 'Delay',
		Tooltip = 'wait time between swings',
		Min = 0,
		Max = 2,
		Default = 0.5,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoFreiya
	local Targets
	local Range

	local useAbilityRemote = replicatedStorage:WaitForChild("events-@easy-games/game-core:shared/game-core-networking@getEvents.Events"):WaitForChild("useAbility")

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local root = entitylib.character.RootPart.Position
				for _, ent in entitylib.List do
					if not AutoFreiya.Enabled then break end
					if ent.Targetable and ent.Character and ent.RootPart then
						local isPlayer = ent.Player ~= nil
						if (isPlayer and Targets.Players.Enabled) or (not isPlayer and Targets.NPCs.Enabled) then
							if (ent.RootPart.Position - root).Magnitude <= Range.Value then
								local stacks = ent.Character:GetAttribute('IceQueenStacks') or 0
								if stacks >= 4 then
									pcall(function() useAbilityRemote:FireServer("ice_queen") end)
									task.wait(0.5)
									break
								end
							end
						end
					end
				end
				task.wait(0.1)
			until not AutoFreiya.Enabled
		end)
	end

	AutoFreiya = vape.Categories.Kits:CreateModule({
		Name = 'AutoFreiya',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto pops freiya ability when a enemy hits 4 stacks'
	})

	Targets = AutoFreiya:CreateTargets({
		Players = true,
		NPCs = true
	})
	Range = AutoFreiya:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 60,
		Default = 30
	})
end)

run(function()
	local AutoStyx
	local Range

	local function enemyInRange()
		if not entitylib.isAlive then return false end
		local root = entitylib.character.RootPart.Position
		for _, ent in entitylib.List do
			if ent.Targetable and ent.RootPart then
				if (ent.RootPart.Position - root).Magnitude <= Range.Value then
					return true
				end
			end
		end
		return false
	end

	AutoStyx = vape.Categories.Kits:CreateModule({
		Name = 'AutoStyx',
		Function = function(callback)
			if callback then
				repeat task.wait() until Range
				local exitPortalUUID = ""
				local entrancePortalData = nil

				AutoStyx:Clean(bedwars.Client:Get(remotes.StyxSpawnEntrancePortal):Connect(function(v1)
					if v1.entrancePortalData.player == lplr then
						entrancePortalData = v1.entrancePortalData
					end
				end))

				AutoStyx:Clean(bedwars.Client:Get(remotes.StyxSpawnExitPortal):Connect(function(v1)
					exitPortalUUID = v1.exitPortalData.uuid
					task.spawn(function()
						repeat task.wait(0.1) until not AutoStyx.Enabled or enemyInRange()
						if not AutoStyx.Enabled then return end
						bedwars.Client:Get(remotes.TryOpenStyxPortalExit):CallServer(exitPortalUUID)
					end)
				end))

				AutoStyx:Clean(bedwars.Client:Get(remotes.StyxExitPortal):Connect(function(v1)
					if entrancePortalData then
						task.wait(0.1)
						bedwars.Client:Get(remotes.StyxPortal):SendToServer({
							entrancePortalData = entrancePortalData
						})
					end
				end))
			end
		end,
		Tooltip = 'auto opens styx portal when a enemy is close'
	})

	Range = AutoStyx:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 60,
		Default = 20
	})
end)

run(function()
	local AutoDeathAdder
	local AutoCollect
	local CollectRange
	local CollectDelay
	local CrystalESP
	local ESPBackground
	local ESPColor
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local Reference = {}
	local crystalIcon
	local collectionActive = false

	local function getCrystalIcon()
		if not crystalIcon then
			crystalIcon = bedwars.getIcon({itemType = 'wild_flower'}, true)
		end
		return crystalIcon
	end

	local function Added(obj)
		if Reference[obj] then return end
		local part = not obj:IsA('Model') and obj or obj.PrimaryPart
		if not part then return end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'alchemy_crystal'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = part
		local blur = addBlur(billboard)
		blur.Visible = ESPBackground.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(36, 36)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
		image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
		image.BorderSizePixel = 0
		image.Image = getCrystalIcon()
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[obj] = billboard
	end

	local function Removed(obj)
		if Reference[obj] then
			Reference[obj]:Destroy()
			Reference[obj] = nil
		end
	end

	local function setupESP()
		for _, v in collectionService:GetTagged('alchemy_crystal') do
			Added(v)
		end
		AutoDeathAdder:Clean(collectionService:GetInstanceAddedSignal('alchemy_crystal'):Connect(function(v)
			if CrystalESP.Enabled then Added(v) end
		end))
		AutoDeathAdder:Clean(collectionService:GetInstanceRemovedSignal('alchemy_crystal'):Connect(function(v)
			Removed(v)
		end))
		local lastUpdate = 0
		AutoDeathAdder:Clean(runService.RenderStepped:Connect(function()
			if not CrystalESP.Enabled then return end
			local now = tick()
			if now - lastUpdate < 0.2 then return end
			lastUpdate = now
			for obj in pairs(Reference) do
				if not obj or not obj.Parent then
					Removed(obj)
				end
			end
		end))
	end

	local function startAutoCollect()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			while AutoDeathAdder.Enabled and AutoCollect.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = CollectRange.Value
				for _, obj in collectionService:GetTagged('alchemy_crystal') do
					if not AutoDeathAdder.Enabled or not AutoCollect.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						local id = obj:GetAttribute('Id') or obj:GetAttribute('id')
							or obj:GetAttribute('CollectableId') or tonumber(obj.Name:match('%d+'))
						if id then
							pcall(function()
								bedwars.GameAnimationUtil:playAnimation(lplr.Character, bedwars.AnimationType.PUNCH)
							end)
							pcall(function()
								bedwars.ViewmodelController:playAnimation(bedwars.AnimationType.FP_USE_ITEM)
							end)
							pcall(function()
								collectRemote:FireServer({ id = id, collectableName = obj.Name })
							end)
							if CollectDelay.Value > 0 then
								task.wait(CollectDelay.Value)
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	local function stopAutoCollect()
		collectionActive = false
	end

	AutoDeathAdder = vape.Categories.Kits:CreateModule({
		Name = 'AutoDeathAdder',
		Function = function(callback)
			if callback then
				if CrystalESP.Enabled then setupESP() end
				if AutoCollect.Enabled then startAutoCollect() end
			else
				stopAutoCollect()
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'grabs crystals + esp for deathadder'
	})

	AutoCollect = AutoDeathAdder:CreateToggle({
		Name = 'Auto Collect',
		Tooltip = 'auto grabs the crystals no cap',
		Function = function(callback)
			if CollectRange and CollectRange.Object then CollectRange.Object.Visible = callback end
			if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = callback end
			if AutoDeathAdder.Enabled then
				if callback then startAutoCollect() else stopAutoCollect() end
			end
		end
	})
	CollectRange = AutoDeathAdder:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 15,
		Visible = false
	})
	CollectDelay = AutoDeathAdder:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's',
		Visible = false
	})

	CrystalESP = AutoDeathAdder:CreateToggle({
		Name = 'Crystal ESP',
		Tooltip = 'shows u where the crystals at',
		Function = function(callback)
			if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and ESPBackground and ESPBackground.Enabled) end
			if AutoDeathAdder.Enabled then
				if callback then
					setupESP()
				else
					Folder:ClearAllChildren()
					table.clear(Reference)
				end
			end
		end
	})
	ESPBackground = AutoDeathAdder:CreateToggle({
		Name = 'Background',
		Default = true,
		Function = function(callback)
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and CrystalESP and CrystalESP.Enabled) end
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					local blur = v:FindFirstChild('Blur')
					if blur then blur.Visible = callback end
					v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
				end
			end
		end
	})
	ESPColor = AutoDeathAdder:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Darker = true,
		Function = function(hue, sat, val, opacity)
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					v.ImageLabel.BackgroundTransparency = 1 - opacity
				end
			end
		end
	})

	task.defer(function()
		local collectOn = AutoCollect and AutoCollect.Enabled
		if CollectRange and CollectRange.Object then CollectRange.Object.Visible = collectOn end
		if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = collectOn end
		local espOn = CrystalESP and CrystalESP.Enabled
		if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
		if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (espOn and ESPBackground.Enabled) end
	end)
end)

run(function()
	local AutoUmbra
	local Delay

	AutoUmbra = vape.Categories.Kits:CreateModule({
		Name = 'AutoUmbra',
		Function = function(callback)
			if callback then
				repeat task.wait() until Delay
				task.spawn(function()
					local notifApp = lplr.PlayerGui:WaitForChild('NotificationApp', 10)
					if not notifApp then return end
					AutoUmbra:Clean(notifApp.DescendantAdded:Connect(function(desc)
						if not AutoUmbra.Enabled then return end
						if desc:IsA("TextLabel") then
							local txt = string.lower(desc.Text)
							if string.find(txt, "teleport") then
								task.spawn(function()
									if Delay.Value > 0 then task.wait(Delay.Value) end
									for _ = 1, 10 do
										if not AutoUmbra.Enabled then break end
										if bedwars.AbilityController:canUseAbility('HATTER_TELEPORT') then
											bedwars.AbilityController:useAbility('HATTER_TELEPORT')
											break
										end
										task.wait(0.05)
									end
								end)
							end
						end
					end))
				end)
			end
		end,
		Tooltip = 'auto teleports for umbra when the game says so'
	})

	Delay = AutoUmbra:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.05,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local AutoCrypt
	local AutoCollect
	local CollectRange
	local CollectDelay
	local SoulESP
	local ESPBackground
	local ESPColor
	local Folder = Instance.new('Folder')
	Folder.Parent = vape.gui
	local Reference = {}
	local collectionActive = false
	local soulIcon = "rbxassetid://6307844310"

	local function Added(obj)
		if Reference[obj] then return end
		local part = not obj:IsA('Model') and obj or obj.PrimaryPart
		if not part then return end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'Gravestone'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(36, 36)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = part
		local blur = addBlur(billboard)
		blur.Visible = ESPBackground.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(36, 36)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(ESPColor.Hue, ESPColor.Sat, ESPColor.Value)
		image.BackgroundTransparency = 1 - (ESPBackground.Enabled and ESPColor.Opacity or 0)
		image.BorderSizePixel = 0
		image.Image = soulIcon
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[obj] = billboard
	end

	local function Removed(obj)
		if Reference[obj] then
			Reference[obj]:Destroy()
			Reference[obj] = nil
		end
	end

	local function setupESP()
		for _, v in collectionService:GetTagged('Gravestone') do
			Added(v)
		end
		AutoCrypt:Clean(collectionService:GetInstanceAddedSignal('Gravestone'):Connect(function(v)
			if SoulESP.Enabled then Added(v) end
		end))
		AutoCrypt:Clean(collectionService:GetInstanceRemovedSignal('Gravestone'):Connect(function(v)
			Removed(v)
		end))
		local lastUpdate = 0
		AutoCrypt:Clean(runService.RenderStepped:Connect(function()
			if not SoulESP.Enabled then return end
			local now = tick()
			if now - lastUpdate < 0.2 then return end
			lastUpdate = now
			for obj in pairs(Reference) do
				if not obj or not obj.Parent then
					Removed(obj)
				end
			end
		end))
	end

	local function startAutoCollect()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			while AutoCrypt.Enabled and AutoCollect.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = CollectRange.Value
				for _, obj in collectionService:GetTagged('Gravestone') do
					if not AutoCrypt.Enabled or not AutoCollect.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						local armorType = obj:GetAttribute('ArmorType')
						local weaponType = obj:GetAttribute('SwordType')
						local associatedPlayerUserId = obj:GetAttribute('GravestonePlayerUserId')
						local secret = obj:GetAttribute('GravestoneSecret')
						local position = obj:GetAttribute('GravestonePosition')
						if secret then
							local ok, result = pcall(function()
								return bedwars.Client:Get(remotes.ActivateGravestone).instance:InvokeServer({
									skeletonData = {
										armorType = armorType,
										weaponType = weaponType,
										associatedPlayerUserId = associatedPlayerUserId
									},
									secret = secret,
									position = position
								})
							end)
							if ok and result and result.success then
								local NecroController = bedwars.Knit.Controllers.NecromancerController
								if NecroController then
									pcall(function()
										NecroController:useGravestone(lplr, obj)
									end)
								end
							end
							if CollectDelay.Value > 0 then
								task.wait(CollectDelay.Value)
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	local function stopAutoCollect()
		collectionActive = false
	end

	AutoCrypt = vape.Categories.Kits:CreateModule({
		Name = 'AutoCrypt',
		Function = function(callback)
			if callback then
				if SoulESP.Enabled then setupESP() end
				if AutoCollect.Enabled then startAutoCollect() end
			else
				stopAutoCollect()
				Folder:ClearAllChildren()
				table.clear(Reference)
			end
		end,
		Tooltip = 'grabs gravestones + esp for crypt'
	})

	AutoCollect = AutoCrypt:CreateToggle({
		Name = 'Auto Collect',
		Tooltip = 'auto uses the gravestones no cap',
		Function = function(callback)
			if CollectRange and CollectRange.Object then CollectRange.Object.Visible = callback end
			if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = callback end
			if AutoCrypt.Enabled then
				if callback then startAutoCollect() else stopAutoCollect() end
			end
		end
	})
	CollectRange = AutoCrypt:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 12,
		Visible = false
	})
	CollectDelay = AutoCrypt:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's',
		Visible = false
	})

	SoulESP = AutoCrypt:CreateToggle({
		Name = 'SoulESP',
		Tooltip = 'shows u where the gravestones at',
		Function = function(callback)
			if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = callback end
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and ESPBackground and ESPBackground.Enabled) end
			if AutoCrypt.Enabled then
				if callback then
					setupESP()
				else
					Folder:ClearAllChildren()
					table.clear(Reference)
				end
			end
		end
	})
	ESPBackground = AutoCrypt:CreateToggle({
		Name = 'Background',
		Default = true,
		Function = function(callback)
			if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (callback and SoulESP and SoulESP.Enabled) end
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					local blur = v:FindFirstChild('Blur')
					if blur then blur.Visible = callback end
					v.ImageLabel.BackgroundTransparency = 1 - (callback and ESPColor.Opacity or 0)
				end
			end
		end
	})
	ESPColor = AutoCrypt:CreateColorSlider({
		Name = 'Background Color',
		DefaultValue = 0,
		DefaultOpacity = 0.5,
		Darker = true,
		Function = function(hue, sat, val, opacity)
			for _, v in Reference do
				if v and v:FindFirstChild('ImageLabel') then
					v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
					v.ImageLabel.BackgroundTransparency = 1 - opacity
				end
			end
		end
	})

	task.defer(function()
		local collectOn = AutoCollect and AutoCollect.Enabled
		if CollectRange and CollectRange.Object then CollectRange.Object.Visible = collectOn end
		if CollectDelay and CollectDelay.Object then CollectDelay.Object.Visible = collectOn end
		local espOn = SoulESP and SoulESP.Enabled
		if ESPBackground and ESPBackground.Object then ESPBackground.Object.Visible = espOn end
		if ESPColor and ESPColor.Object then ESPColor.Object.Visible = (espOn and ESPBackground.Enabled) end
	end)
end)

run(function()
	local AutoNyx
	local Targets
	local Range
	local Delay

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				if bedwars.AbilityController:canUseAbility('midnight') then
					local plr = entitylib.EntityPosition({
						Range = Range.Value,
						Part = 'RootPart',
						Players = Targets.Players.Enabled,
						NPCs = Targets.NPCs.Enabled,
						Wallcheck = Targets.Walls.Enabled,
						Sort = sortmethods.Distance
					})
					if plr then
						pcall(function() bedwars.AbilityController:useAbility('midnight') end)
						task.wait(Delay.Value)
					end
				end
				task.wait(0.1)
			until not AutoNyx.Enabled
		end)
	end

	AutoNyx = vape.Categories.Kits:CreateModule({
		Name = 'AutoNyx',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto pops nyx ability when a enemy is close'
	})

	Targets = AutoNyx:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoNyx:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 20
	})
	Delay = AutoNyx:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 2,
		Default = 0.5,
		Decimal = 10,
		Suffix = 's'
	})
end)

run(function()
	local AutoMarcel
	local Delay

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until Delay
			repeat
				if not entitylib.isAlive then task.wait(0.1); continue end
				local DefenderController = bedwars.Knit.Controllers.DefenderKitController
				if not DefenderController then task.wait(0.1); continue end
				for blockPos in DefenderController.currentSchematic do
					if not AutoMarcel.Enabled then break end
					if not entitylib.isAlive then break end
					pcall(function()
						local activeMode = bedwars.Store:getState().Kit.defenderScannerMode
						local blockType = DefenderController.currentSchematic[blockPos]
						if activeMode == 'upgrade' then
							DefenderController:requestScannerAction(blockPos, 'upgrade', blockType)
						elseif activeMode == 'refund' then
							DefenderController:requestScannerAction(blockPos, 'refund')
						else
							DefenderController:requestPlaceDefenderBlock(blockPos)
						end
					end)
					task.wait(Delay.Value)
				end
				task.wait(0.1)
			until not AutoMarcel.Enabled
		end)
	end

	AutoMarcel = vape.Categories.Kits:CreateModule({
		Name = 'AutoMarcel',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto builds ur whole defender layout'
	})

	Delay = AutoMarcel:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.05,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local AutoTerra
	local BlockCount

	local function startLoop()
		task.spawn(function()
			repeat task.wait() until BlockCount
			repeat
				if entitylib.isAlive and bedwars.AbilityController then
					local character = lplr.Character
					local blocks = 0
					if character then
						blocks = character:GetAttribute('BlockKickerKit_BlockCount') or lplr:GetAttribute('BlockKickerKit_BlockCount') or 0
					else
						blocks = lplr:GetAttribute('BlockKickerKit_BlockCount') or 0
					end
					if blocks <= BlockCount.Value and bedwars.AbilityController:canUseAbility('BLOCK_STOMP') then
						pcall(function() bedwars.AbilityController:useAbility('BLOCK_STOMP') end)
						task.wait(0.8)
					end
				end
				task.wait(0.1)
			until not AutoTerra.Enabled
		end)
	end

	AutoTerra = vape.Categories.Kits:CreateModule({
		Name = 'AutoTerra',
		Function = function(callback)
			if callback then startLoop() end
		end,
		Tooltip = 'auto stomps to refill ur blocks'
	})

	BlockCount = AutoTerra:CreateSlider({
		Name = 'Block Count',
		Tooltip = 'stomps when ur blocks drop to this',
		Min = 0,
		Max = 10,
		Default = 0
	})
end)

run(function()
	local AutoWarden
	local Targets
	local Range
	local Delay
	local collectionActive = false

	local function startLoop()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			while AutoWarden.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = Range.Value
				for _, obj in collectionService:GetTagged('jailor_soul') do
					if not AutoWarden.Enabled or not collectionActive then break end
					local part = not obj:IsA('Model') and obj or obj.PrimaryPart
					if part and (localPosition - part.Position).Magnitude <= range then
						pcall(function()
							bedwars.JailorController:collectEntity(lplr, obj, 'JailorSoul')
						end)
						if Delay.Value > 0 then
							task.wait(Delay.Value)
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	AutoWarden = vape.Categories.Kits:CreateModule({
		Name = 'AutoWarden',
		Function = function(callback)
			if callback then
				startLoop()
			else
				collectionActive = false
			end
		end,
		Tooltip = 'auto grabs the jailor souls for u'
	})

	Targets = AutoWarden:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoWarden:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 20
	})
	Delay = AutoWarden:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local AutoGrimReaper
	local Targets
	local Range
	local Delay
	local collectionActive = false

	local function startLoop()
		if collectionActive then return end
		collectionActive = true
		task.spawn(function()
			repeat task.wait() until Targets and Range and Delay
			while AutoGrimReaper.Enabled and collectionActive do
				if not entitylib.isAlive then
					task.wait(0.5)
					continue
				end
				local controller = bedwars.GrimReaperController
				if not controller or not controller.soulsByPosition then
					task.wait(0.2)
					continue
				end
				local localPosition = entitylib.character.RootPart.Position
				local range = Range.Value
				local char = lplr.Character
				local health = char and char:GetAttribute('Health')
				local maxHealth = char and char:GetAttribute('MaxHealth')
				local lowHealth = health and maxHealth and health <= (maxHealth / 4) and not char:GetAttribute('GrimReaperChannel')
				if lowHealth then
					for _, v in controller.soulsByPosition do
						if not AutoGrimReaper.Enabled or not collectionActive then break end
						if typeof(v) == 'Instance' then
							local part = not v:IsA('Model') and v or v.PrimaryPart
							if part and (localPosition - part.Position).Magnitude <= range then
								local secret = v:GetAttribute('GrimReaperSoulSecret')
								if secret then
									pcall(function()
										bedwars.Client:Get(remotes.ConsumeSoul):CallServer({ secret = secret })
									end)
									if Delay.Value > 0 then
										task.wait(Delay.Value)
									end
								end
							end
						end
					end
				end
				task.wait(0.1)
			end
			collectionActive = false
		end)
	end

	AutoGrimReaper = vape.Categories.Kits:CreateModule({
		Name = 'AutoGrimReaper',
		Function = function(callback)
			if callback then
				startLoop()
			else
				collectionActive = false
			end
		end,
		Tooltip = 'auto eats souls when ur hp is low'
	})

	Targets = AutoGrimReaper:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})
	Range = AutoGrimReaper:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 150,
		Default = 120
	})
	Delay = AutoGrimReaper:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100,
		Suffix = 's'
	})
end)

run(function()
	local KitESP
	local Notify
	local Background
	local Color = {}
	local Reference = {}
	local Folder = Instance.new('Folder')
    local currentSize = 36
	Folder.Parent = vape.gui
	
	local ESPKits = {
		alchemist = {'alchemist_ingedients', 'thorns'},
		beekeeper = {'bee', 'bee'},
		bigman = {'treeOrb', 'natures_essence_1'},
		ghost_catcher = {'ghost', 'ghost_orb'},
		metal_detector = {'hidden-metal', 'iron'},
		sheep_herder = {'SheepModel', 'purple_hay_bale'},
		sorcerer = {'alchemy_crystal', 'wild_flower'},
		star_collector = {'stars', 'crit_star'},
		black_market_trader = {'shadow_coin', 'shadow_coin'},
		miner = {'petrified-player', 'large_rock'},
		trapper = {'snap_trap', 'snap_trap'},
		mage = {'ElementTome', 'mage_spellbook'},
	}
	local NONTaggedKits = {
		necromancer = {'Gravestone', true},
		battery = {'Open', true},
	}
	local DescendantKits = {
		['farmer_cletus'] = {
			{'carrot', 'carrot_seeds'},
			{'melon', 'melon_seeds'},
			{'pumpkin', 'pumpkin_seeds'},
		},
	}

	local function isMineOnMyTeam(part)
		local teamAttr = part:GetAttribute('Team') or part:GetAttribute('TeamId') or part:GetAttribute('TeamID')
		if teamAttr then
			local myTeam = lplr:GetAttribute('Team') or lplr:GetAttribute('TeamId') or lplr:GetAttribute('TeamID')
			if myTeam and tostring(teamAttr) == tostring(myTeam) then
				return true
			end
		end
		local placedBy = part:GetAttribute('PlacedByUserId')
		if placedBy then
			local player = playersService:GetPlayerByUserId(placedBy)
			if player then
				local myTeam = lplr:GetAttribute('Team')
				if myTeam and player:GetAttribute('Team') == myTeam then
					return true
				end
			end
		end
		return false
	end

	local function getAlchemistImage(v)
		local name = v and v.Name or ''
		if name == 'Mushrooms' then
			return bedwars.getIcon({itemType = 'mushrooms'}, true)
		elseif name == 'Thorns' then
			return bedwars.getIcon({itemType = 'thorns'}, true)
		else
			return bedwars.getIcon({itemType = 'wild_flower'}, true)
		end
	end

	local function getStarImage(v)
		local parent = v and v.Parent
		if parent and parent:IsA("Model") then
			local modelName = parent.Name
			if modelName == "CritStar" or modelName:lower():find("crit") then
				return bedwars.getIcon({itemType = 'crit_star'}, true)
			elseif modelName == "VitalityStar" or modelName:lower():find("vitality") then
				return bedwars.getIcon({itemType = 'vitality_star'}, true)
			end
		end
		return bedwars.getIcon({itemType = 'crit_star'}, true)
	end

	local function Added(v, icon, non)
		if Reference[v] then return end
		if Notify.Enabled then
			vape:CreateNotification("KitESP", `New object is added {v.Name}`, 2)
		end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = icon
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(currentSize, currentSize)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = v
		local blur = addBlur(billboard)
		blur.Visible = Background.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(currentSize, currentSize)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
		image.BackgroundTransparency = 1 - (Background.Enabled and Color.Opacity or 0)
		image.BorderSizePixel = 0
		if non then
			image.Image = icon
		else
			image.Image = bedwars.getIcon({itemType = icon}, true)
		end
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[v] = billboard
	end

	local function AddedStar(v)
		if not v or not v.Parent then return end
		if Reference[v] then return end

		if Notify.Enabled then
			vape:CreateNotification("KitESP", `New object is added {v.Name}`, 2)
		end
		local billboard = Instance.new('BillboardGui')
		billboard.Parent = Folder
		billboard.Name = 'star'
		billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
		billboard.Size = UDim2.fromOffset(currentSize, currentSize)
		billboard.AlwaysOnTop = true
		billboard.ClipsDescendants = false
		billboard.Adornee = v
		local blur = addBlur(billboard)
		blur.Visible = Background.Enabled
		local image = Instance.new('ImageLabel')
		image.Size = UDim2.fromOffset(currentSize, currentSize)
		image.Position = UDim2.fromScale(0.5, 0.5)
		image.AnchorPoint = Vector2.new(0.5, 0.5)
		image.BackgroundColor3 = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
		image.BackgroundTransparency = 1 - (Background.Enabled and Color.Opacity or 0)
		image.BorderSizePixel = 0
		image.Image = getStarImage(v)
		image.Parent = billboard
		local uicorner = Instance.new('UICorner')
		uicorner.CornerRadius = UDim.new(0, 4)
		uicorner.Parent = image
		Reference[v] = billboard
	end
	
	local currentConnections = {}
	local currentKit = nil

	local function disconnectAll()
		for _, conn in ipairs(currentConnections) do
			conn:Disconnect()
		end
		table.clear(currentConnections)
	end

	local function isRealBee(v)
		local beeId = v:GetAttribute('BeeId')
		return beeId ~= nil and beeId > -1
	end

	local function addKit(tag, icon)
		if tag == 'bee' then
			local beeIcon = bedwars.getIcon({itemType = 'bee'}, true)
			for _, tagName in {'bee', 'MeadowBee'} do
				local connAdded = collectionService:GetInstanceAddedSignal(tagName):Connect(function(v)
					if v:IsA("Model") and v.PrimaryPart then
						task.wait(0.1)
						if isRealBee(v) then
							Added(v.PrimaryPart, beeIcon, true)
						end
					end
				end)
				table.insert(currentConnections, connAdded)
				local connRemoved = collectionService:GetInstanceRemovedSignal(tagName):Connect(function(v)
					if v.PrimaryPart and Reference[v.PrimaryPart] then
						Reference[v.PrimaryPart]:Destroy()
						Reference[v.PrimaryPart] = nil
					end
				end)
				table.insert(currentConnections, connRemoved)
				for _, v in collectionService:GetTagged(tagName) do
					if v:IsA("Model") and v.PrimaryPart and isRealBee(v) then
						Added(v.PrimaryPart, beeIcon, true)
					end
				end
			end
			return
		end
		if tag == 'alchemist_ingedients' then
			local connAdded = collectionService:GetInstanceAddedSignal(tag):Connect(function(v)
				if v.PrimaryPart then
					task.wait(0.1)
					if Reference[v.PrimaryPart] then return end
					local billboard = Instance.new('BillboardGui')
					billboard.Parent = Folder
					billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
					billboard.Size = UDim2.fromOffset(currentSize, currentSize)
					billboard.AlwaysOnTop = true
					billboard.ClipsDescendants = false
					billboard.Adornee = v.PrimaryPart
					local blur = addBlur(billboard)
					blur.Visible = Background.Enabled
					local image = Instance.new('ImageLabel')
					image.Size = UDim2.fromOffset(currentSize, currentSize)
					image.Position = UDim2.fromScale(0.5, 0.5)
					image.AnchorPoint = Vector2.new(0.5, 0.5)
					image.BackgroundColor3 = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
					image.BackgroundTransparency = 1 - (Background.Enabled and Color.Opacity or 0)
					image.BorderSizePixel = 0
					image.Image = getAlchemistImage(v)
					image.Parent = billboard
					local uicorner = Instance.new('UICorner')
					uicorner.CornerRadius = UDim.new(0, 4)
					uicorner.Parent = image
					Reference[v.PrimaryPart] = billboard
				end
			end)
			table.insert(currentConnections, connAdded)
			local connRemoved = collectionService:GetInstanceRemovedSignal(tag):Connect(function(v)
				if v.PrimaryPart and Reference[v.PrimaryPart] then
					Reference[v.PrimaryPart]:Destroy()
					Reference[v.PrimaryPart] = nil
				end
			end)
			table.insert(currentConnections, connRemoved)
			for _, v in collectionService:GetTagged(tag) do
				if v.PrimaryPart and not Reference[v.PrimaryPart] then
					local billboard = Instance.new('BillboardGui')
					billboard.Parent = Folder
					billboard.StudsOffsetWorldSpace = Vector3.new(0, 3, 0)
					billboard.Size = UDim2.fromOffset(currentSize, currentSize)
					billboard.AlwaysOnTop = true
					billboard.ClipsDescendants = false
					billboard.Adornee = v.PrimaryPart
					local blur = addBlur(billboard)
					blur.Visible = Background.Enabled
					local image = Instance.new('ImageLabel')
					image.Size = UDim2.fromOffset(currentSize, currentSize)
					image.Position = UDim2.fromScale(0.5, 0.5)
					image.AnchorPoint = Vector2.new(0.5, 0.5)
					image.BackgroundColor3 = Color3.fromHSV(Color.Hue, Color.Sat, Color.Value)
					image.BackgroundTransparency = 1 - (Background.Enabled and Color.Opacity or 0)
					image.BorderSizePixel = 0
					image.Image = getAlchemistImage(v)
					image.Parent = billboard
					local uicorner = Instance.new('UICorner')
					uicorner.CornerRadius = UDim.new(0, 4)
					uicorner.Parent = image
					Reference[v.PrimaryPart] = billboard
				end
			end
			return
		end
		if tag == 'stars' then
			local connAdded = collectionService:GetInstanceAddedSignal(tag):Connect(function(v)
				if v:IsA("Model") and v.PrimaryPart then
					task.wait(0.1)
					AddedStar(v.PrimaryPart)
				end
			end)
			table.insert(currentConnections, connAdded)
			local connRemoved = collectionService:GetInstanceRemovedSignal(tag):Connect(function(v)
				if v.PrimaryPart and Reference[v.PrimaryPart] then
					Reference[v.PrimaryPart]:Destroy()
					Reference[v.PrimaryPart] = nil
				end
			end)
			table.insert(currentConnections, connRemoved)
			for _, v in collectionService:GetTagged(tag) do
				if v:IsA("Model") and v.PrimaryPart then
					AddedStar(v.PrimaryPart)
				end
			end
			return
		end

		if tag == 'petrified-player' then
			local connAdded = collectionService:GetInstanceAddedSignal(tag):Connect(function(v)
				if v.PrimaryPart and not isMineOnMyTeam(v.PrimaryPart) then
					Added(v.PrimaryPart, icon, false)
				end
			end)
			table.insert(currentConnections, connAdded)
			local connRemoved = collectionService:GetInstanceRemovedSignal(tag):Connect(function(v)
				if v.PrimaryPart and Reference[v.PrimaryPart] then
					Reference[v.PrimaryPart]:Destroy()
					Reference[v.PrimaryPart] = nil
				end
			end)
			table.insert(currentConnections, connRemoved)
			for _, v in collectionService:GetTagged(tag) do
				if v.PrimaryPart and not isMineOnMyTeam(v.PrimaryPart) then
					Added(v.PrimaryPart, icon, false)
				end
			end
			return
		end

		local connAdded = collectionService:GetInstanceAddedSignal(tag):Connect(function(v)
			if tag == 'bee' and (v.Name:find('TamedBee') or v:FindFirstChild('TamedBee')) then return end
			Added(v.PrimaryPart, icon, false)
		end)
		table.insert(currentConnections, connAdded)
		local connRemoved = collectionService:GetInstanceRemovedSignal(tag):Connect(function(v)
			if Reference[v.PrimaryPart] then
				Reference[v.PrimaryPart]:Destroy()
				Reference[v.PrimaryPart] = nil
			end
		end)
		table.insert(currentConnections, connRemoved)
		for _, v in collectionService:GetTagged(tag) do
			if tag == 'bee' and (v.Name:find('TamedBee') or v:FindFirstChild('TamedBee')) then continue end
			Added(v.PrimaryPart, icon, false)
		end
	end

	local function addKitNon(objName, icon)
		if typeof(icon) == "boolean" then
			if objName == "Gravestone" then
				icon = "rbxassetid://6307844310"
			elseif objName == "Open" then
				icon = "rbxassetid://10159166528"
			else
				icon = bedwars.getIcon({itemType = icon}, true) or ''
			end
		else
			icon = bedwars.getIcon({itemType = icon}, true)
		end
		local connAdded = workspace.ChildAdded:Connect(function(child)
			if child:IsA("Model") and child.Name == objName then
				task.wait(0.1)
				if child.PrimaryPart then
					Added(child, icon, true)
				end
			end
		end)
		table.insert(currentConnections, connAdded)
		local connRemoved = workspace.ChildRemoved:Connect(function(child)
			if child:IsA("Model") and child.Name == objName then
				if Reference[child] then
					Reference[child]:Destroy()
					Reference[child] = nil
				end
			end
		end)
		table.insert(currentConnections, connRemoved)
	end

	local function addKitDescendant(partName, icon)
		local resolvedIcon = bedwars.getIcon({itemType = icon}, true)
		
		local function shouldSkip(obj)
			local p = obj.Parent
			while p and p ~= workspace do
				if p.Name == partName then return true end
				p = p.Parent
			end
			return false
		end

		scanDescendants(workspace, function(obj)
			if obj:IsA("BasePart") and obj.Name == partName and not shouldSkip(obj) then
				if not Reference[obj] then
					Added(obj, resolvedIcon, true)
				end
			end
		end)
		local connAdded = workspace.DescendantAdded:Connect(function(obj)
			if obj:IsA("BasePart") and obj.Name == partName and not shouldSkip(obj) then
				task.wait(0.1)
				if not Reference[obj] then
					Added(obj, resolvedIcon, true)
				end
			end
		end)
		table.insert(currentConnections, connAdded)
		local connRemoved = workspace.DescendantRemoving:Connect(function(obj)
			if obj:IsA("BasePart") and obj.Name == partName and Reference[obj] then
				Reference[obj]:Destroy()
				Reference[obj] = nil
			end
		end)
		table.insert(currentConnections, connRemoved)
	end

	local function setupKit(kitName)
		local kit = ESPKits[kitName]
		local nontag = NONTaggedKits[kitName]
		local desctag = DescendantKits[kitName]
		if kit then
			addKit(kit[1], kit[2])
		end
		if nontag then
			addKitNon(nontag[1], nontag[2])
		end
		if desctag then
			for _, entry in ipairs(desctag) do
				addKitDescendant(entry[1], entry[2])
			end
		end
	end

	KitESP = vape.Categories.Kits:CreateModule({
		Name = 'KitESP',
		Function = function(callback)
			if callback then
				task.spawn(function()
					while KitESP.Enabled do
						if not currentKit then
							repeat
								task.wait()
							until store.equippedKit ~= '' or not KitESP.Enabled
							if not KitESP.Enabled then break end
						end
						local newKit = table.concat(getLocalKits(), ',')
						if newKit ~= currentKit then
							disconnectAll()
							Folder:ClearAllChildren()
							table.clear(Reference)
							for _, kit in ipairs(getLocalKits()) do
								setupKit(kit)
							end
							currentKit = newKit
						end
						task.wait(1)
					end
					disconnectAll()
					Folder:ClearAllChildren()
					table.clear(Reference)
					currentKit = nil
				end)
			else
				disconnectAll()
				Folder:ClearAllChildren()
				table.clear(Reference)
				currentKit = nil
			end
		end,
		Tooltip = 'esp for kits'
	})
	Notify = KitESP:CreateToggle({
		Name = "Notify",
		Default = false
	})
	Background = KitESP:CreateToggle({
		Name = 'Background',
		Function = function(callback)
			if Color.Object then Color.Object.Visible = callback end
			for _, v in Reference do
				v.ImageLabel.BackgroundTransparency = 1 - (callback and Color.Opacity or 0)
				v.Blur.Visible = callback
			end
		end,
		Default = true
	})
    Color = KitESP:CreateColorSlider({
        Name = 'Background Color',
        DefaultValue = 0,
        DefaultOpacity = 0.5,
        Function = function(hue, sat, val, opacity)
            for _, v in Reference do
                v.ImageLabel.BackgroundColor3 = Color3.fromHSV(hue, sat, val)
                v.ImageLabel.BackgroundTransparency = 1 - opacity
            end
        end,
        Darker = true
    })
    Sizeable = KitESP:CreateToggle({
        Name = "Sizeable",
        Default = false,
        Function = function(callback)
            if callback then
                SizeSlider.Object.Visible = true
                currentSize = SizeSlider.Value or 36
                for _, v in Reference do
                    v.Size = UDim2.fromOffset(currentSize, currentSize)
                    v.ImageLabel.Size = UDim2.fromOffset(currentSize, currentSize)
                end
            else
                SizeSlider.Object.Visible = false
                currentSize = 36
                for _, v in Reference do
                    v.Size = UDim2.fromOffset(currentSize, currentSize)
                    v.ImageLabel.Size = UDim2.fromOffset(currentSize, currentSize)
                end
            end
        end
    })
    SizeSlider = KitESP:CreateSlider({
        Name = "Size",
        Default = 36,
        Min = 10,
        Max = 80,
        Visible = false,
        Function = function(value)
            if not Sizeable.Enabled then return end
            currentSize = value
            for _, v in Reference do
                v.Size = UDim2.fromOffset(value, value)
                v.ImageLabel.Size = UDim2.fromOffset(value, value)
            end
        end
    })

    task.defer(function()
        if Color and Color.Object then
            Color.Object.Visible = Background.Enabled  
        end
    end)
end)

run(function()
	local AutoZarrah
	local Targets
	local Range
	local gunRemote

	local function getGunRemote()
		if not gunRemote then
			pcall(function()
				gunRemote = replicatedStorage:WaitForChild('rbxts_include'):WaitForChild('node_modules'):WaitForChild('@rbxts'):WaitForChild('net'):WaitForChild('out'):WaitForChild('_NetManaged'):WaitForChild('HandGunFireRequest')
			end)
		end
		return gunRemote
	end

	local function fireGun(dir)
		local remote = getGunRemote()
		if not remote then return end
		pcall(function()
			remote:InvokeServer({ lookVector = dir })
			replicatedStorage['events-@easy-games/game-core:shared/game-core-networking@getEvents.Events'].useAbility:FireServer('hand_gun')
		end)
	end

	AutoZarrah = vape.Categories.Kits:CreateModule({
		Name = 'AutoZarrah',
		Function = function(callback)
			if callback then
				task.spawn(function()
					repeat task.wait() until Targets and Range
					repeat
						if not entitylib.isAlive then task.wait(0.1); continue end
						local plr = entitylib.EntityPosition({
							Range = Range.Value,
							Part = 'RootPart',
							Players = Targets.Players.Enabled,
							NPCs = Targets.NPCs.Enabled,
							Wallcheck = Targets.Walls.Enabled,
							Sort = sortmethods.Distance
						})
						if plr and plr.RootPart and entitylib.character.RootPart then
							local dir = (plr.RootPart.Position - entitylib.character.RootPart.Position).Unit
							fireGun(dir)
						end
						task.wait()
					until not AutoZarrah.Enabled
				end)
			end
		end,
		Tooltip = 'auto gunblade for zarrah, shoots ur nearest target'
	})

	Targets = AutoZarrah:CreateTargets({
		Players = true,
		NPCs = true,
		Walls = true
	})

	Range = AutoZarrah:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 40,
		Default = 10
	})
end)

run(function()
	local AutoCyber
	local DropMode
	local Collect
	local Target
	local Limit

	local genItems = {'emerald', 'diamond'}

	local teamCache, cacheExpire = nil, 0
	local function getTeamGenerator()
		if cacheExpire > tick() and teamCache and teamCache.Parent then
			return teamCache
		end
		teamCache, cacheExpire = collectionService:GetTagged(lplr:GetAttribute('Team') .. '_TeamOreGenerator')[1], tick() + 5
		return teamCache
	end

	local cache = nil
	local function getDrone()
		if Limit.Enabled and (not store.hand.tool or store.hand.tool.Name ~= 'drone') then
			return nil
		end
		if cache and cache.Parent then
			return cache
		end
		for _, v in collectionService:GetTagged('Drone') do
			if v:GetAttribute('PlayerUserId') == lplr.UserId then
				local Changed = function()
					if v:GetAttribute('HeldItem') then
						repeat
							bedwars.Handler:Get('DropDroneItem'):Fire('SendToServer', {
								direction = Vector3.new(1000, 10, 0),
								position = v.PrimaryPart.Position
							})
							task.wait(0.1)
						until not v:GetAttribute('HeldItem') or not AutoCyber.Enabled
					end
				end
				AutoCyber:Clean(v:GetAttributeChangedSignal('HeldItem'):Connect(Changed))
				AutoCyber:Clean(v:GetAttributeChangedSignal('HeldItemAmount'):Connect(Changed))
				cache = v
				return v
			end
		end
		if getItem('drone') and bedwars.Handler:Get('FireGuidedProjectile'):Fire('CallServer', 'drone') then
			task.wait(0.1)
			return getDrone()
		end
		return nil
	end

	local function getGenerator(drone, item)
		local children = collectionService:GetTagged(item .. '_OreGenerator')
		local pos = drone.PrimaryPart.Position
		table.sort(children, function(a, b)
			return (pos - a.PrimaryPart.Position).Magnitude < (pos - b.PrimaryPart.Position).Magnitude
		end)
		return children[1] and children[1].PrimaryPart or nil
	end

	local blacklist = {}
	local function getItemDrop(drone)
		local generator = getTeamGenerator()
		generator = generator and generator.PrimaryPart.Position or Vector3.zero
		local children = workspace.ItemDrops:GetChildren()
		local pos = drone.PrimaryPart.Position
		table.sort(children, function(a, b)
			return (pos - a.Position).Magnitude < (pos - b.Position).Magnitude
		end)
		local stealSplit = Collect.Value == 'Steal Split'
		local genOnly = Collect.Value == 'Generator'
		for _, v in children do
			local passesType = true
			if genOnly and not table.find(genItems, v.Name) then
				passesType = false
			end
			if tick() > (blacklist[v] or 0)
				and passesType
				and v.Position.Y > 0
				and math.abs(v.Velocity.Y) <= 0
				and (not stealSplit or (v.Position - generator).Magnitude > 20)
				and (not Target.Enabled or not entitylib.EntityPosition({
					Origin = pos,
					Range = 60,
					Part = 'RootPart',
					Players = true
				}))
			then
				return v
			end
		end
		return nil
	end

	AutoCyber = vape.Categories.Minigames:CreateModule({
		Name = 'AutoCyber',
		Function = function(callback)
			if callback then
				AutoCyber:Clean(workspace.ItemDrops.ChildAdded:Connect(function(v)
					task.wait()
					if v.Velocity.X > 100 then
						blacklist[v] = tick() + 5
						local Amount = v:GetAttribute('Amount')
						local LastParent = v.Parent
						if DropMode.Value == 'Player' then
							notif('AutoCyber', 'Collecting ' .. tostring(Amount) .. ' ' .. v.Name, 4, 'info')
							repeat
								v.Velocity = Vector3.zero
								v.CFrame = entitylib.character.RootPart.CFrame - Vector3.new(0, 4, 0)
								task.spawn(function()
									bedwars.Handler:Get('PickupItemDrop'):Fire('CallServerAsync', {
										itemDrop = v
									}):andThen(function(suc)
										if suc and bedwars.SoundList then
											bedwars.AudioManager:playAudio(bedwars.SoundList.PICKUP_ITEM_DROP)
											local sound = bedwars.ItemMeta[v.Name].pickUpOverlaySound
											if sound then
												bedwars.AudioManager:playAudio(sound, {
													position = v.Position,
													volumeMultiplier = 0.9
												})
											end
										end
									end)
								end)
								task.wait(0.02)
							until not v or v.Parent ~= LastParent

							notif('AutoCyber', `Collected {Amount} {v.Name}{Amount > 1 and 's' or ''}`, 4, 'info')
						else
							local start = tick()
							local generator = getTeamGenerator()
							if generator then
								repeat
									v.Velocity = Vector3.zero
									v.CFrame = generator.PrimaryPart.CFrame
									task.wait()
								until (tick() - start) >= 1 or not v or v.Parent ~= LastParent
								notif('AutoCyber', 'Dropped ' .. tostring(Amount) .. ' ' .. v.Name, 8, 'info')
							else
								notif('AutoCyber', 'Base generator not found', 20, 'alert')
							end
						end
					end
				end))

				repeat
					local drone = getDrone()
					if drone then
						local v = getItemDrop(drone)
						if v then
							task.wait(0.3)
							drone.PrimaryPart.AssemblyLinearVelocity = Vector3.zero
							drone.PrimaryPart.CFrame = CFrame.new(drone.PrimaryPart.CFrame.X, 10000, drone.PrimaryPart.CFrame.Z)
							local magnitude, lastmag = 0, 9e9
							local pos = v.Position
							repeat
								if drone and drone.Parent then
									pos = v.Position
									drone.PrimaryPart.CanCollide = false
									drone.PrimaryPart.AssemblyAngularVelocity = Vector3.zero
									drone.PrimaryPart.AssemblyLinearVelocity = CFrame.lookAt(drone.PrimaryPart.Position * Vector3.new(1, 0, 1), pos * Vector3.new(1, 0, 1)).LookVector * 30
									magnitude = ((drone.PrimaryPart.Position * Vector3.new(1, 0, 1)) - (pos * Vector3.new(1, 0, 1))).Magnitude
								else
									break
								end
								task.wait()
							until not v or v.Parent ~= workspace.ItemDrops or not AutoCyber.Enabled or magnitude <= 2
							if not AutoCyber.Enabled then
								break
							end

							if magnitude <= 5 then
								local start = tick()
								repeat
									if drone and drone.Parent then
										drone.PrimaryPart.AssemblyLinearVelocity = Vector3.zero
										drone.PrimaryPart.AssemblyAngularVelocity = Vector3.new(0, -30, 0)
										drone.PrimaryPart.CFrame = CFrame.new(pos - Vector3.new(0, drone.Hitbox.Size.Y, 0))
									end
									task.wait(0.02)
								until (tick() - start) >= 1.25
							end
						else
							drone.PrimaryPart.CFrame = CFrame.new(drone.PrimaryPart.CFrame.X, 10000, drone.PrimaryPart.CFrame.Z)
							drone.PrimaryPart.Velocity = Vector3.zero
							if Collect.Value == 'Generator' then
								for _, v2 in genItems do
									local gen = getGenerator(drone, v2)
									if gen then
										local magnitude = 0
										repeat
											if drone and drone.Parent then
												if getItemDrop(drone) then break end
												drone.PrimaryPart.CanCollide = false
												drone.PrimaryPart.AssemblyAngularVelocity = Vector3.zero
												drone.PrimaryPart.AssemblyLinearVelocity = CFrame.lookAt(drone.PrimaryPart.Position * Vector3.new(1, 0, 1), gen.Position * Vector3.new(1, 0, 1)).LookVector * 30
												magnitude = ((drone.PrimaryPart.Position * Vector3.new(1, 0, 1)) - (gen.Position * Vector3.new(1, 0, 1))).Magnitude
											else
												break
											end
											task.wait()
										until not v or v.Parent ~= workspace.ItemDrops or not AutoCyber.Enabled or magnitude <= 5
									end
								end
							end
						end
					end
					task.wait()
				until not AutoCyber.Enabled
			else
				local drone = getDrone()
				if drone then
					drone.PrimaryPart.CFrame = CFrame.new(drone.PrimaryPart.CFrame.X, 500, drone.PrimaryPart.CFrame.Z)
				end
			end
		end,
		Tooltip = 'uses ur drone to grab loot n bring it back gng'
	})

	DropMode = AutoCyber:CreateDropdown({
		Name = 'Drop mode',
		List = {'Player', 'Base'},
		Default = 'Player',
		Tooltip = 'where the drone drops the loot it grabs'
	})

	Collect = AutoCyber:CreateDropdown({
		Name = 'Collect',
		List = {'Generator', 'Closest', 'Steal Split'},
		Default = 'Closest',
		Tooltip = 'Generator = only diamond n emerald gens, Closest = grabs whatevers closest, Steal Split = jacks the loot off the gen'
	})

	Target = AutoCyber:CreateToggle({Name = 'Target check'})
	Limit = AutoCyber:CreateToggle({Name = 'Limit to item'})
end)

run(function()
	local AutoSheep
	local Delay
	local Range
	local Infinite
	
	AutoSheep = vape.Categories.Minigames:CreateModule({
		Name = 'AutoSheepHerder',
		Function = function(callback)
			if callback then
				local tameSheep = bedwars.Client:GetNamespace('SheepHerder'):Get('TameSheep')
	
				repeat
					local model = workspace:FindFirstChild('SheepModel')
					if entitylib.isAlive and model then
						local localPosition = entitylib.character.RootPart.Position
						for _, v in model:GetChildren() do
							if v.PrimaryPart and (Infinite.Enabled or (localPosition - v.PrimaryPart.Position).Magnitude <= Range.Value) then
								if Delay.Value > 0 then
									task.wait(Delay.Value)
								end
								tameSheep:SendToServer(v.SheepData.Value)
							end
						end
					end
					task.wait(0.1)
				until not AutoSheep.Enabled
			end
		end,
		Tooltip = 'auto tames sheep'
	})
	Range = AutoSheep:CreateSlider({
		Name = 'Range',
		Min = 1,
		Max = 200,
		Default = 20,
		Suffix = function(val)
			return val <= 1 and 'stud' or 'studs'
		end
	})
	Delay = AutoSheep:CreateSlider({
		Name = 'Delay',
		Min = 0,
		Max = 1,
		Default = 0.1,
		Decimal = 100
	})
end)

run(function()
	local path = 'aerov4/libraries/private.lua'
	local src = nil
	pcall(function() src = readfile(path) end)

	if not src or src == '' then
		local commit = readfile('aerov4/profiles/commit.txt')
		local ok, res = pcall(function()
			return game:HttpGet('https://raw.githubusercontent.com/wrealaero/aerov4/'..commit..'/libraries/private.lua', true)
		end)
		if not ok or not res or res == '' or res == '404: Not Found' then
			ok, res = pcall(function()
				return game:HttpGet('https://raw.githubusercontent.com/wrealaero/aerov4/main/libraries/private.lua', true)
			end)
		end
		if not ok or not res or res == '' or res == '404: Not Found' then
			warn('[aerov4] private.lua download failed')
			return
		end
		src = '--This watermark is used to delete the file if its cached, remove it to make the file persist after vape updates.\n'..res
		pcall(function() writefile(path, src) end)
	end

	local fn = loadstring(src, 'private')
	if not fn then
		warn('[aerov4] private.lua compile error')
		return
	end

	local okrun, privateFunc = pcall(fn)
	if not okrun or type(privateFunc) ~= 'function' then
		warn('[aerov4] private.lua init failed... noo claude do something')
		return
	end

	privateFunc(vape, run, bedwars, entitylib, lplr, store, remotes,
		inputService, runService, playersService, replicatedStorage, tweenService, httpService,
		collectionService, gameCamera, targetinfo, prediction, notif, collection, sortmethods,
		getSortList, switchItem, hotbarSwitch, getPlacedBlock, isFrozen, cloneRaycast, frictionTable,
		isFirstPerson, isGUIOpen, isnetworkowner, vapeEvents, vape.Libraries.auraanims, uipallet, tween, color,
		getcustomasset, sessioninfo)
end)